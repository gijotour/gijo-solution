# GIJO AS — RAG 구성 가이드 (청킹·임베딩·벡터DB·리트리버)

> 작성 2026-07-25. **코드 실제 값 + 운영 서버 실측**으로 작성한 기술 사양서입니다. 어려운 용어는 쉬운 풀이를 함께 실었습니다.
> 근거 파일: `server/src/engine/memory.ts`(청킹·벡터DB·검색) · `llm.ts`(임베딩 호출·주입) · `localengine.ts`(임베딩 서버 기동) · `ontology.ts`(하이브리드)
> ⚠ 값이 바뀌면 이 문서도 갱신할 것. 실측 시점의 적재량은 §7 참조(시간이 지나면 늘어남).

---

## 0. 용어 쉬운 풀이

| 용어 | 쉬운 설명 |
|---|---|
| **RAG** (검색 증강 생성) | AI가 답하기 전에 **사내 문서를 먼저 검색해, 찾은 내용을 근거로 답하는 방식**. "외운 걸로 답하지 말고 자료를 펴 놓고 답해라". |
| **청크(조각)** | 긴 문서를 검색하기 좋게 **문단 단위로 자른 것**. 문서 전체가 아니라 이 조각이 검색·주입 단위. |
| **겹침(overlap)** | 조각 경계에서 문장이 잘려 뜻이 반토막 나는 것을 막기 위해 **앞 조각 끝부분을 다음 조각 앞에 복사**하는 것. |
| **임베딩** | 문장을 **숫자 좌표(벡터)로 바꾸는 기술**. 뜻이 비슷하면 좌표가 가깝다 → 표현이 달라도 찾아냄. |
| **차원(1024)** | 좌표를 이루는 숫자 개수. bge-m3는 문장 하나를 1024개 숫자로 표현. |
| **벡터 DB** | 그 좌표들을 저장해 두고 "가장 가까운 N개"를 순식간에 꺼내는 **검색 전용 창고**. |
| **리트리버** | 질문을 좌표로 바꿔 **가까운 조각을 꺼내오는 부품**. "무엇을 근거로 줄지" 고르는 역할. |
| **거리(distance)** | 두 좌표가 얼마나 먼지. **작을수록 관련 있음**(0에 가까울수록 유사). |
| **topK** | 검색 결과 중 **상위 몇 개를 쓸지**. 우리는 답변 주입에 4개. |
| **scope** | 그 문서를 **누가 볼 수 있는지**. `global`=전체 에이전트 공용, `agentId`=그 에이전트 전용. |
| **온톨로지** | 보안 지식을 **관계망(그래프)으로 정리한 것**. 예: `CVE-2021-44228 —[유형]→ RCE`. 문서 검색과 별개 경로. |
| **그라운딩** | AI가 **자기 기억보다 검색된 사내 자료를 우선**하게 묶어두는 것. 환각(지어내기)을 막는 핵심. |
| **근거 배지(📄)** | 답변 아래 붙는 **출처 문서명**. 이 답이 어느 문서에서 왔는지 즉시 확인. |

---

## 1. 한눈에 보는 스펙

| 항목 | 값 | 정의 위치 |
|---|---|---|
| 청크 크기 | **800자** | `memory.ts` `CHUNK_SIZE` |
| 청크 겹침 | **100자** | `memory.ts` `CHUNK_OVERLAP` |
| 임베딩 모델 | **bge-m3** (GGUF 1.16GB) | `models/bge-m3/bge-m3.gguf` |
| 벡터 차원 | **1024** (실측) | 임베딩 서버 응답 |
| 임베딩 서빙 | 별도 llama-server **포트 8081 상주** | `localengine.ts` `EMBEDDING_PORT` |
| 벡터 DB | **LanceDB** — `data/memory.lancedb` | `memory.ts` `DB_PATH` |
| 테이블 | `documents` (documentId·chunkIndex·text·scope·vector) | `memory.ts` `MemoryRow` |
| 검색 상한 | **topK 4개**(답변 주입) / API 기본 5개 | `llm.ts:71` |
| 관련성 임계 | **거리 ≤ 0.95** 통과, 초과는 버림 | `RAG_RELEVANCE_MAX_DISTANCE` |
| 인입 배치 | 8청크씩 임베딩 | `EMBED_BATCH` |
| 인입 재시도 | 5회 · 10초 간격 | `GIJO_EMBED_RETRIES` |
| 하이브리드 | 벡터 RAG + 온톨로지 트리플 동시 주입 | `ontology.ts` `ontologyContextFor` |

**전체 흐름**

```
[문서 업로드]
   ↓ 전처리(PDF 잡음 제거) → 블록 분해(제목 결합) → 문장 경계 분할
[청크 800자 / 겹침 100자]
   ↓ bge-m3 임베딩 (8개씩 배치, 포트 8081)
[벡터 1024차원] → LanceDB documents 테이블 저장 (+ SQLite에 메타데이터)

[사용자 질문]
   ↓ 같은 임베딩으로 좌표화
[LanceDB 검색: scope 필터 + limit 4]
   ↓ 거리 ≤ 0.95 만 통과 (무관 조각 컷)
[근거 청크] + [온톨로지 관계] + [용어 정의]
   ↓ "사전지식보다 사내 자료 우선" 지시와 함께 주입
[LLM 답변] + 📄 근거 배지(sources)
```

---

## 2. 청킹 (문서 → 조각)

단순 800자 자르기가 아니라 **의미 경계를 지키는 3단 처리**입니다. (`chunkText()`)

### ① 전처리 — `cleanExtractedText()`
- **페이지 번호 줄 제거**: `- 134 -`, `134`, `page 12` 형태
- **반복 머리글/바닥글 제거**: 3회 이상 반복되는 짧은 줄(문서 제목이 매 페이지 반복되는 패턴)
- 근거: PDF 추출물의 레이아웃 잡음이 임베딩 품질을 떨어뜨리고 청크 앞머리를 낭비함(FOCS 매뉴얼 실측)

### ② 블록 분해
- 빈 줄 기준으로 문단 분리
- **제목 줄은 다음 문단에 붙임** — `#`, `1.1`, `제3장`, `■` 등으로 시작하고 80자 이하인 줄
- 목적: "제목만 남은 청크"(검색되어도 내용이 없는 쓸모없는 조각) 방지

### ③ 문장 경계 분할
- 800자를 넘는 블록만 문장 단위로 쪼갬 (+ 겹침 100자)
- 800자 이하 블록은 **그대로 한 청크** — 문단 하나가 온전히 보존됨

> **왜 800자인가**: 한국어 기준 한 문단~두 문단 분량. 너무 작으면 맥락이 끊기고, 너무 크면 무관한 내용이 섞여 검색 정밀도가 떨어집니다. 임베딩 모델의 컨텍스트(8192토큰) 안에 넉넉히 들어갑니다.

---

## 3. 임베딩 (문장 → 1024차원 좌표)

| 항목 | 값 | 이유 |
|---|---|---|
| 모델 | **bge-m3** | 한국어 성능이 강하고 다국어 지원, GGUF로 온프렘 구동 가능 |
| 차원 | 1024 | bge-m3 고정 |
| 서버 | llama-server `--embedding`, **포트 8081 상주** | 채팅 모델 풀과 분리 → 모델 스왑에 영향받지 않고 항상 대기 |
| API | OpenAI 호환 `POST /v1/embeddings` | `llm.ts embed()` |

### 기동 옵션 (필수)
```bash
llama-server -m models/bge-m3/bge-m3.gguf --embedding -ngl -1 \
  --ctx-size 8192 --batch-size 8192 --ubatch-size 8192 --port 8081
```

> ⚠ **`--ubatch-size` 명시는 필수입니다.** 기본값(512)으로 띄우면 512토큰을 넘는 입력(한글 약 2000자 이상)에서 **HTTP 500**이 나고, 큰 청크의 RAG 인입이 **조용히 실패**합니다(2026-07 실사고). 세 값을 bge-m3 최대치 8192로 맞춥니다.

### 인입 안정화 (실사고 대응)
- **배치 8개**: 청크당 ~500토큰 × 8 ≈ 4천 토큰 → 임베딩 서버 한도(8192) 안. 64개로 보내면 대용량 PDF(수천 청크)에서 서버가 무응답에 빠져 워치독 재기동까지 이어졌음.
- **배치 실패 시 청크 단위 재시도**: 건강할 땐 빠르게, 불안정할 땐 느리지만 반드시 들어가게.
- **재시도 5회 · 10초 간격**: 임베딩 서버가 워치독으로 재기동되는 ~15초 동안의 일시 오류로 문서 전체가 실패하지 않도록.
- ⚠ URL 주의: `EMBEDDING_SERVER_URL`에 이미 `/v1`이 포함 → 코드에서는 `/embeddings`만 붙임. `/v1/embeddings`를 붙이면 `/v1/v1/embeddings` 404로 **RAG가 조용히 죽음**(2026-07-19 실발생).

---

## 4. 벡터 DB (LanceDB)

**완전 폐쇄망 동작**: 서버 파일로만 존재하고 외부 서비스 의존이 0입니다.

```
data/memory.lancedb
└─ documents 테이블
   ├─ documentId  : 파일명 — 근거 배지·삭제·재인입의 키
   ├─ chunkIndex  : 조각 번호
   ├─ text        : 조각 원문 (LLM에 실제로 주입되는 내용)
   ├─ scope       : "global"(공용) 또는 agentId(전용)
   └─ vector      : float[1024]
```

### 자가 복구 장치 (조용한 오염 차단)
1. **차원 드리프트 감지** — 기존 벡터 차원 ≠ 새 벡터 차원이면 테이블 재생성. `add()`가 에러 없이 벡터를 기존 차원으로 **잘라 저장**해버려서 데이터가 조용히 오염되기 때문(임베딩 모델 교체 시).
2. **스키마 드리프트 감지** — `scope` 등 컬럼 추가 전에 만들어진 옛 테이블은 재생성.
3. **재인입 멱등성** — 같은 `documentId`는 **옛 청크를 먼저 삭제**한 뒤 추가. (2026-07-25 수정: 이전에는 `add()`만 해서 같은 파일 재업로드 시 옛/새 청크가 중복 누적됐고, 검색에 신·구 버전이 함께 잡혔음.)

### 메타데이터는 SQLite 분리
LanceDB 스키마는 건드리지 않고 `memory_documents` 테이블에 별도 보관:
`documentId · scope · chunks · embeddingModel · sourcePath · ingestedAt · docClass · uploadedBy(올린이)`

---

## 5. 리트리버 (검색 → 근거 선별)

```
질문 → bge-m3 임베딩(1024차원)
     → LanceDB 벡터 검색 (scope 필터 + limit topK)
     → 거리 ≤ 0.95 필터            ← 핵심 안전장치
     → 통과 = 근거 청크 (sources에 문서명)
     → 0개면 internalMiss=true → ☁ 외부(클라우드) 질의 제안
```

### 관련성 임계 0.95 — 실측으로 정한 값
| 구분 | 최근접 거리 실측 |
|---|---|
| 사내 자료에 **있는** 질문 4개 | **0.503 ~ 0.775** |
| 사내 자료에 **없는** 질문 4개 | **1.086 ~ 1.285** |

두 무리가 **0.311 간격으로 깨끗이 갈림** → 중간값(0.93)보다 살짝 느슨한 **0.95** 채택. (애매하게 관련된 실제 질문을 막기보다, 무관한 잡음을 거르는 것이 목적)

> **왜 이 컷이 반드시 필요한가**: 벡터 검색은 관련이 없어도 "가장 가까운" 청크를 무조건 돌려줍니다. 임계값이 없으면 "베트남 쌀국수 육수 내는 법"에도 보안 문서 조각이 딸려 나오고, 모델은 그것을 근거인 양 붙잡고 답합니다(2026-07-19 실측: 그라운딩 전용 에이전트가 없는 사실을 지어냄).

### 검색 함수 3종
| 함수 | 반환 | 용도 |
|---|---|---|
| `queryMemoryScored(q, topK, agentId)` | `{text, distance, documentId}[]` | 거리·출처까지 필요할 때(그라운딩 판단·근거 배지) |
| `queryMemoryRelevant(q, topK, agentId)` | `string[]` (임계 통과분) | 프롬프트 주입용 |
| `queryMemory(q, topK, agentId)` | `string[]` (임계 없음) | `/api/memory/query` 원시 검색 |

### scope 격리
`agentId`를 주면 **"그 에이전트 전용 문서 + 전역 문서"만** 검색합니다(대시보드·오케스트레이터는 전역만). 특정 에이전트에 귀속된 지식이 다른 에이전트로 새지 않게 하는 장치입니다.

---

## 6. 하이브리드 — 벡터 RAG + 온톨로지 + 그라운딩

문서 검색만으로는 "CVE-2021-44228 → KEV 등재 → 최우선 조치" 같은 **명시적 관계**를 정확히 잡지 못합니다. 그래서 두 경로를 함께 주입합니다.

```
최종 시스템 프롬프트 =
   에이전트 페르소나
 + 용어 정의 (GLOSSARY 그라운딩 — 약자 환각 방지)
 + 온톨로지 관계   ← "A —[관계]→ B (출처)" 형식, 트리플 2,166개에서 매칭
 + RAG 참고자료    ← 청크 4개
```

두 경로가 **독립적**이라, 임베딩 서버가 죽어 청크가 비어도 온톨로지 규칙은 걸립니다(반대도 성립).

### 그라운딩 지시문 (가장 중요한 한 줄)
```
참고 자료 — 사내 지식 베이스(장기 기억)에서 검색된 관련 내용입니다.
질문과 관련된 내용이면 네 사전지식과 다르더라도 이 자료를 우선 근거로 삼아 답하고,
질문과 무관하면 무시하세요.
```

> **실측 근거**(2026-07-25): 이전 문구("답변에 활용하되, 질문과 무관하면 무시하세요")는 약해서, 7B가 확신 있는 오답 prior를 가진 주제에서 정답 근거를 무시했습니다(금융권 망분리 근거를 "정보통신망법"으로 오답). **"사전지식과 달라도 사내 자료 우선"** 한 줄로 바꾸자 **같은 7B가 정답**(전자금융감독규정 제15조)을 냈습니다. **14B 모델로 교체하는 것보다 효과적**이었고 비용은 0입니다.

### 엄격 그라운딩 (GIJO Agent, `agentId=normaltic`)
사내지식 해설 전용 에이전트는 한 단계 더 강합니다: 답변 생성 **전에** `queryMemoryRelevant`로 관련 자료를 확인하고, **0건이면 LLM을 아예 부르지 않고** "등록된 사내 자료에는 관련 내용이 없습니다"로 답합니다. (프롬프트 규칙만으로는 지켜지지 않았음 — 코드로 보장)

---

## 7. 운영 적재 현황 (2026-07-25 실측)

| 구분 | 값 |
|---|---|
| 문서 | **80건** (전부 `global` scope = 팀 공용) |
| 청크 | **3,694개** (문서당 평균 46청크) |
| 벡터 DB 크기 | 23MB |
| 자동 분류 | 보고서 30 · 정책 8 · 매뉴얼 3 · 기타 6 · 미분류 33 |
| 온톨로지 트리플 | **2,166개** (KISA·ATLAS·OWASP·NIST·CWE·ATT&CK 6대 표준) |
| 임베딩 모델 파일 | 1.16GB (bge-m3.gguf) |

---

## 8. 관측·검증 수단

| 수단 | 용도 |
|---|---|
| `POST /api/memory/query` | 특정 질문에 **어떤 청크가 검색되는지** 직접 확인(리트리버 단독 검증) |
| `GET /api/memory/documents` | 문서별 청크 수·분류·올린이·업로드 시각 |
| 답변의 `sources` + 📄 배지 | 그 답이 실제로 **어느 문서를 근거로** 했는지 |
| `POST /api/handover/verify` | 올린 문서가 실제 답변 근거로 인용되는지 **자동 검증** |
| `tools/regress/run.mjs` | 검증된 사실 10케이스 회귀 검사 — **RAG·프롬프트·모델 변경 후 필수 실행** |
| `POST /api/kb-hygiene/scan` | 지식베이스 위생(상충·중복·신선도) 점검 |

---

## 9. 알려진 함정 (실사고 기반)

1. **임베딩 서버 `--ubatch-size` 미지정** → 512토큰 초과 입력 HTTP 500 → 큰 청크 인입이 조용히 실패. (§3)
2. **임베딩 URL 중복 `/v1`** → 404 → RAG 전체가 조용히 죽음. (§3)
3. **관련성 임계 없는 검색** → 무관 청크를 근거로 환각. (§5)
4. **재인입 시 중복 누적** → 같은 문서의 신·구 버전이 함께 검색됨. 2026-07-25 멱등화로 수정. (§4)
5. **약한 그라운딩 문구** → 모델이 정답 근거를 무시. 모델 크기 문제로 오진하기 쉬움. (§6)
6. **"검색 히트 있음 ≠ 정확한 근거"** → 무관한 온톨로지 조각이 임계 안에 들어와 `internalMiss=false`인데도 오답할 수 있음. 근본 해법은 **권위 있는 문서를 넣어 올바른 청크가 검색되게** 하는 것(`rag-seed/` 참조).

---

## 10. 튜닝 포인트 (바꿀 때 주의할 것)

| 값 | 올리면 | 내리면 | 변경 시 |
|---|---|---|---|
| 청크 800자 | 맥락 풍부, 검색 정밀도↓ | 정밀도↑, 맥락 끊김 | **전체 재인입 필요** |
| 겹침 100자 | 경계 손실↓, 저장량↑ | 저장량↓, 문장 잘림 | 전체 재인입 필요 |
| topK 4 | 근거 다양, 프롬프트↑(7B는 길어지면 품질↓) | 프롬프트 절약, 근거 누락 위험 | 즉시 반영 |
| 임계 0.95 | 더 관대(무관 유입 위험) | 더 엄격(있는 자료도 누락) | 즉시 반영, **하네스 필수** |
| 임베딩 모델 | 품질↑ 가능 | — | **차원 변경 시 테이블 재생성 + 전체 재인입** |

> 어떤 값을 바꾸든 **`tools/regress/run.mjs`를 돌려 회귀를 확인**하세요. 테스트는 통과하는데 실사용자에게 오답이 나가는 사고(2026-07-21)가 이 하네스를 만든 이유입니다.

---

## 관련 문서
- `GIJO_AS_인수인계_가이드.md` — RAG에 노하우를 남기는 절차(용어 풀이 포함)
- `GIJO_AS_디스패치_사전라우팅_가이드.md` — 도구 선택 품질(P3 설계 가이드)
- `GIJO_AS_온톨로지_강화_가이드.md` — 하이브리드의 온톨로지 축
- `rag-seed/security-references/` — 표준 근거 참고자료 시드(재시드 스크립트 포함)
