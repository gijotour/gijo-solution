# GIJO 제안자료 — 근거 없이는 답하지 않는 AI 데이터 HUB

> **GS텔레서비스 「AICC 지식 구조화 기반 AI 데이터 HUB 및 정합성 검증 PoC」 기술 제안**
> 완전 온프렘 로컬 LLM 기반 근거답변 검증 PoC — 내부 검토용 초안 **v0.7** (2026-08-10, 한국어 윤문 반영)
> 원본: 제안서 초안·제안요약 슬라이드 아티팩트(2026-08-08 작성, 2026-08-09 제품 문서 대조 갱신)
> ⚠ 실적·핵심인력 경력·가격제안(밀봉)·별첨 확약서 4종은 **확정 후 제출본에서 별도 작성**

| 제안 개요 | |
|---|---|
| 사업기간 | 계약일로부터 **12주** |
| 대상 업무 | **취소·반품·교환 SR 상담** (1개 브랜드 · 1개 상담조직) |
| 구동 방식 | **발주사 내부 GPU · 완전 온프렘** — 데이터 외부 반출 0 |
| 환경 | 읽기 전용 격리 PoC (운영계 쓰기 없음 · 고객 직접 응대 없음) |

---

## 1. 제안 요지 — 세 문장으로

이 PoC의 본질은 챗봇을 만드는 일이 아닙니다. AI가 믿고 쓸 수 있는 데이터를 만들고, 그 답이 옳음을 증명하는 일입니다.

| 원칙 | 뜻 |
|---|---|
| **① 근거가 곧 답변이다** | 모든 처리 안내에 출처 문서·구간·시행일·버전을 **필수 필드**로 붙입니다. 근거를 못 찾으면 그 답은 화면에 나가지 않습니다. |
| **② 모르면 멈춘다** | 필수 정보 누락·기준 충돌·권한 밖 질문은 추정하지 않고 "무엇이 부족한지"를 밝히며 **보류**합니다. "틀린 확답"보다 "정당한 보류"가 항상 이기는 정책이 중대오답 0건을 만듭니다. |
| **③ 데이터는 집 밖에 안 나간다** | 답변·검증·검색·임베딩 전 모델이 발주사 GPU에 상주합니다. 학습배제를 계약으로 약속하는 게 아니라 **통신 자체가 없어 원천 불가능**하게 만듭니다. |

> **차별점 한 줄.** 모델은 갈아끼우는 엔진입니다. 저희가 제안하는 것은
> 어떤 엔진을 꽂아도 근거 없이는 말 못 하게 만드는 차체입니다.
> 그래서 특정 모델이나 수행사에 묶이는 일이 구조적으로 생기지 않습니다.

이 골격은 제안용으로 그린 그림이 아닙니다. 자사 온프렘 AI 보안관제 제품에서 매일 운영하며
자동 회귀로 검증하고 있는 아키텍처입니다(실전 152상황을 매일 밤 다시 재고, 최근 회차는 151/152.
2026-08-10 기준). 본 PoC는 그 검증된 골격에 취소·반품·교환 업무 데이터를 얹는 작업입니다.

---

## 2. 합격선 (RFP 정량 목표)

| 지표 | 합격선 |
|---|---|
| 업무정확도 (비공개 300건) | **90% 이상** |
| 근거일치 · 적정 보류 | **95% 이상** |
| 출처·버전 표시 | **100%** |
| 중대오답 · 보안사고 | **0건** |
| 최종응답 P95 (20 동시세션) | **3.0초 이내** |
| 검색·확인시간 | **30% 단축** |

---

## 3. 아키텍처 — 5계층, 쉽게 보기

데이터는 위에서 아래로 흐릅니다. 오른쪽 기둥(감사로그·관측·평가 게이트)이 전 구간을 지켜봅니다.

```
① 원천 데이터 (발주사 시스템 · 읽기 전용)
   고객 · 주문 · 결제·배송 · 클레임 · 상담사례 · 업무기준 문서
        │  읽기 전용 수집 · 최소수집 · 가명처리
        ▼
② 데이터 정비·연결 (HUB)
   품질 측정·정제 이력 │ 식별자 연결·코드 매핑 │ 시행일·버전 구조화
        │  정비를 통과한 데이터만 색인
        ▼
③ 지식층 — 버전이 붙은 지식
   업무기준 저장소(시행일·버전·승인자) │ 업무규칙 온톨로지(예외·우선순위) │ 하이브리드 색인(의미+키워드)
        │  질문마다 "그 시점에 유효한 버전"만 검색
        ▼
④ 근거답변 엔진 — 전 모델 발주사 GPU 상주
   검색·상태 조회 → 답변 생성(로컬 LLM) → ★근거 검증 게이트(문장↔근거 대조)
        ├─ 근거 확인됨 → 근거답변 + 출처·버전
        └─ 부족·충돌   → 안전한 보류 + 사유
        ▼
⑤ 상담지원 화면 — AI 제안임을 명시
   상황요약·처리방향 │ 근거 원문·버전 원클릭 열람 │ 피드백(채택/수정/보류) → 평가·개선 자료로 환류

[전 구간 감시 기둥]  감사로그(위변조 방지) · 성능 관측(P50/P95/P99 단계별) · 평가 게이트(회귀 통과 없이는 어떤 변경도 반영 금지)
```

**각 층을 쉽게 말하면:**

- **① 원천:** 발주사 시스템을 읽기만 하는 통로로 연결합니다. PoC는 어떤 경우에도 운영계에 쓰지 않습니다.
- **② 정비:** AI가 쓰기 전에 데이터를 장부처럼 정리합니다. 빠진 값과 중복, 코드 불일치를 재고 고객에서 주문, 클레임으로 이어 붙이고, 정책 문서마다 시행일과 버전, 예외를 답니다. 무엇을 어떻게 고쳤는지는 정제 이력에 전부 남습니다.
- **③ 지식층:** 질문이 오면 그 시점에 유효한 버전만 검색합니다. 과거 기준과 현재 기준이 섞이는 사고를 구조로 막는 층입니다.
- **④ 엔진(핵심 차별점):** 모델이 답을 만들면 거기서 끝나지 않습니다. 답의 문장 하나하나를 검색된 근거와 맞춰 보는 검증 게이트를 거칩니다. 통과한 답만 출처·버전과 함께 나가고, 그렇지 못하면 보류 카드로 넘어갑니다.
- **⑤ 화면:** 질문과 요약, 처리방향, 근거, 보류사유가 한 화면에 있고 근거 원문은 클릭 한 번이면 열립니다. 상담사의 채택·수정·보류는 평가와 개선 자료로 되돌아옵니다.

---

## 4. 왜 로컬 LLM인가 — 세 가지 이유

1. **학습배제의 가장 강한 증빙(AQR-09):** 외부 API를 쓰면 "재학습에 안 쓰겠다"는 계약과 증빙이 필요합니다. 로컬 구동은 데이터가 밖으로 나가는 통신 자체가 없습니다. 증빙은 망 구성도와 외부 발신 차단 로그면 끝납니다.
2. **비용 고정·종속 차단(AQR-10):** 토큰당 과금이 없어 질의량이 늘어도 월 비용은 GPU 전기료 수준에 머뭅니다. 모델은 표준 파일(GGUF)이라 발주사가 직접 갈아 끼우고 다시 띄울 수 있습니다.
3. **3초를 우리가 통제(PER-01):** 외부 API의 네트워크와 대기열 변동이 사라집니다. 지연이 생기는 구간이 전부 내부에 있어 단계별로 재고 통제할 수 있습니다.

**모델은 못박지 않고 시험으로 뽑습니다** (RFP 4.8 개방성):
- G2(4주차)에 후보 2~3종(Qwen3-Next-80B, gpt-oss-120B 등 Apache 2.0 계열)을 동일 데이터·평가셋으로 **공개 비교**해 발주사와 채택 확정.
- 한국어 최상위 EXAONE은 **비상업 라이선스라 기본 구성에서 배제**하고 사유를 명시(라이선스 공개 원칙). 상업 이용이 자유로운 국산 경량 모델(카나나 등)은 보조 역할 후보로 병행 평가.
- 운영 전환 후에도 같은 절차 — GGUF 파일 교체 + 회귀 게이트 통과, 두 단계로 끝.

---

## 5. GPU 설계 — VRAM은 용량이 아니라 역할로 나눕니다

**제안 구조는 GPU 2장을 나눠 쓰는 것입니다(총 96GB).** "큰 GPU 하나에 다 넣기"는 하지 않습니다.
답을 만드는 무거운 일과 검증·검색처럼 가벼운 일을 물리적으로 갈라놓습니다. P95가 흔들리는 주원인이
자원 경합이기 때문입니다.

| GPU | 역할 | 배치 |
|---|---|---|
| **GPU A — 80GB급** | "말하는 GPU" (직렬 대자원) | 주력 답변 모델 80B 전문가 분업형(MoE) ≈55GB · 20 동시세션 대화 기억(KV 캐시) 12GB · 여유 13GB |
| **GPU B — 16GB급** | "검사하는 GPU" (병렬 소자원) | 검증·요약 보조 8B ≈9GB · 임베딩(검색 눈) 2.5GB · 리랭커(재정렬기) 2.5GB · 여유 2GB |

왜 이 구조인가: ① 답을 만드는 일과 검사하는 일이 GPU를 서로 뺏지 않습니다. 근거 대조를 생성과
나란히 돌려도 응답시간이 흔들리지 않습니다 ② 세션 캐시 12GB를 미리 떼어 두어 20 동시세션(PER-03)이
설계값입니다 ③ 남는 13GB는 G2에서 후보 모델을 갈아 끼워 시험하는 자리입니다.

**규모별 가이드** — 발주사 장비가 바뀌어도 구조는 그대로, 모델 부품만 바뀝니다:

| 등급 | VRAM | 구성 | 솔직한 기대 수준 |
|---|---|---|---|
| 최소 | 24GB | 32B급 4비트, 검증 겸임(직렬) | 개발·시연용. **합격선(20세션+P95 3초)은 위험** — 동시 5~8세션 한계 |
| 기본 | 48GB | 32B급 8비트 + 경량 보조 | **합격선 도전의 하한.** 복합·예외 시나리오에서 체급 한계 먼저 드러남 |
| **제안 ★** | **96~110GB** | **80B MoE + 8B 보조 병렬 검증** | **속도·품질·동시성의 균형점** — 3초와 20세션을 함께 노릴 수 있는 첫 구간. 발주사 보유 환경과 일치 |
| 확장 | 160GB+ | 235B급 또는 이중 A/B 상주 | 이득 완만 — PoC엔 과투자 |

> 자사 제품은 서버 GPU를 실측해 권장 등급을 자동으로 판정하고 안내하는 기능을 이미 갖고 있습니다.
> 장비가 확정되면 어느 구간인지 기계가 바로 판정합니다.

---

## 6. P95 3초 — 단계별 시간 예산으로 관리

"빨라졌다"가 아니라 어느 단계가 몇 초인지로 관리합니다. 요청 ID별 원시로그는 그대로 제출합니다
(사전 고정답변·실패질의 삭제 없음).

| 단계 | 예산 | 수단 | 담당 |
|---|---|---|---|
| 질문 이해·식별자 조회 | 0.2초 | 상태 조회는 정비된 HUB에서 — 원천 시스템 왕복 없음 | GPU B + DB |
| 하이브리드 검색 + 리랭킹 | 0.4초 | 벡터+키워드 병렬 · 유효 버전 사전 필터 | GPU B |
| 답변 생성 | 1.7초 | 근거 Top-k만 투입 + 구조 출력 + 스트리밍 | GPU A |
| 근거 검증 | 0.4초 | 생성 스트림과 **병행** 대조 — 직렬 대기 없음, 실패 시 즉시 보류 | GPU B |
| 여유 (P95 변동 흡수) | 0.3초 | 20 동시세션 연속 배칭 지터 포함 | — |

- 1~2주차에 발주사 실 GPU에서 토큰 속도를 실측해 예산표를 실측치로 갱신합니다.
- 20 동시세션 부하시험은 G3(8주차) 전에 자체 수행해 여유를 수치로 확인합니다.

---

## 7. 운영 중인 우리 제품 ↔ 본 PoC — 1:1 대응

왼쪽은 전부 자사 온프렘 AI 플랫폼에서 지금 돌아가고 있는 실코드입니다. 새로 발명하는 것이 아니라
도메인을 바꿔 얹는 일임을 보여드립니다.

| 운영 중인 구성요소 (자사 제품) | 본 PoC에서의 역할 |
|---|---|
| 로컬 LLM 프로세스 풀 — VRAM 예산·상주/교체·장애 자동 재기동 | 모델 4종 배치의 운용 엔진 (가용성 99.5%) |
| 모델별 자동 적응 — 파일 메타를 읽어 구동 파라미터 자동 교정 | 후보 모델 교체 시험을 안전하게 |
| 하이브리드 검색 — 의미(벡터)+키워드 병행 | 지식층 검색 (조항 번호·코드 + 의미 동시) |
| 온톨로지 지식망 — 6대 보안표준 약 2,000개 관계 운영 | 취소·반품·교환 규정의 예외·우선순위·충돌 구조화 |
| 문서 해시 버전 추적 — 1글자 변경도 재색인 강제 | "문서는 고쳤는데 AI는 옛 기준" 사고 차단 |
| 이질 소스 정규화 허브 — 3종 소스 공통 스키마 상관 | 주문·결제·배송·클레임 커넥터의 골격 재사용 |
| 그라운딩 스택 + 무근거 감지 — 근거 없으면 보류 전환 | 근거 검증 게이트 (실측: 그라운딩 강화 > 모델 2배 확대) |
| 3축 평가 게이트 + **매일 밤 자동 회귀**(실전 152상황·최근 151/152) | 모델·프롬프트·데이터 변경 통제, G2~G4 평가 대응 |
| 3중 방어 — 입구 가드레일 + **반입 문서 살균** + 출구 자격증명 가림, 자동 레드팀 상시 | 문서에 심어진 지시문이 AI를 조종하지 못하게 반입 단계 차단 |
| 개인정보 자동 마스킹 — 주민번호·카드번호는 AI 도달 전 가림 | 최소수집·가명처리의 내장 장치 |
| **거짓 완료 차단 관문** — 안 한 일을 "했다"고 답하면 출구에서 차단 | 중대오답 0건의 핵심 장치 |
| 감사로그 + 판정 이력 병기 | 위변조 방지 기록·답변 재현 |
| 승인 카드(결재판) — AI 제안·실행 분리, 원클릭 되돌리기 | 오인방지 + 채택·수정·보류 피드백 UI |
| 지연 안내 전환 + 단계별 소요 계측 | 시간초과 안전 안내 + P50/P95/P99 관측 |
| 백업·복원 검증 — "복구된다"를 실제로 시험 | 산출물 인수·재현시험, 보안 종료 증빙 |
| 브루트포스 방어·역할권한·2차 인증(TOTP) 하드닝 | PoC 계정의 최소권한·다중인증(폐쇄망 동작) |

> **쉽게 말하면,** 이 RFP가 요구하는 시스템의 축소판을 이미 다른 업종에서 매일 돌리고 있습니다.
> 그래서 12주를 엔진 개발이 아니라 데이터와 업무를 이해하는 데 쓸 수 있습니다.

---

## 8. 정량 목표 — 지표마다 책임지는 부품이 있습니다

| 지표 | 합격선 | 책임 부품 · 달성 방법 |
|---|---|---|
| 업무정확도 | 90%↑ | 그라운딩(근거 묶기) + 업무규칙 온톨로지(예외·우선순위를 모델 재량이 아닌 규칙으로) + 모범 사례 예시 |
| 근거일치 | 95%↑ | 보조 모델의 **문장 단위 근거 대조** — 근거 없는 문장은 화면 표시 전 차단 |
| 출처·버전 표시 | 100% | 출처·시행일·버전 = **필수 필드** — 누락 시 답변 조립 자체가 불가 |
| 정답 근거 Top-5 | 90%↑ | 하이브리드 검색 + 리랭커 정밀 재정렬 |
| 중대오답 | 0건 | 금전·약정 판단은 보류 우선 + **거짓 완료 차단 관문**(실제 데이터와 대조해 출구 차단 — 운영 중 기능) |
| 적정 보류 | 95%↑ | 필수항목 체크 + 기준 충돌 감지 + 보류 사유 명시 |
| P95 응답 | 3.0초 | 2-GPU 분리 + 단계별 예산 + 생성·검증 병행 |
| 검색·확인시간 | 30%↓ | 한 화면 통합 UI — 탐색 시간 자체를 제거, 그림자 운영 중앙값 측정 |

**요구사항 1:1 대응 판정 요약 (61항목):**

| 판정 | 수 | 뜻 |
|---|---|---|
| 충족 | **44** | 제품 보유 기능·구조 |
| 수행 | **14** | 방법론 보유, PoC 기간 수행 |
| 조건부 | **3** | 실측으로 확정 — AQR-03(비열위 95%) · PER-01(P95 3초) · PER-03(20세션) |
| 미충족 | **0** | — |

> RFP 4장 원칙("가능·지원 예정·협의 필요 표현 금지")에 따라, 실측 없이 확정할 수 없는 3건은
> 정직하게 조건부로 표기하고 확정 시점(G1~G3)을 명시했습니다.

---

## 9. 보안·개인정보 — 폐쇄형 구동의 이점

- **격리(SEC-01):** PoC 전용 계정·망·저장소, 운영계는 읽기 전용 통로만. 외부 통신 전면 차단(에어갭) 모드를 제품 기능으로 보유 — 차단 이력까지 기록.
- **학습배제(AQR-09):** 로컬 구동 — 모델 제공자에게 전송되는 데이터가 존재하지 않음. 증빙은 망 구성도 + 차단 로그.
- **최소권한(SEC-02):** 역할 기반 접근 + 폐쇄망에서 동작하는 2차 인증(TOTP) + 세션 통제.
- **암호화(SEC-03):** 저장 AES-256(선택형) — **키는 발주사 서버에서 생성·보관, 공급사는 모름** + 전송 암호화.
- **개인정보(SEC-04):** 주민번호·카드번호는 AI에 닿기 전 자동 마스킹, 가린 사실 기록 — 내장 기능.
- **로그(SEC-05):** 위변조 방지 기록 — **끌 수 없음**, 기본 3년 보관, 정리 사실도 기록. 동일 입력·동일 버전 재현 지원.
- **공격방어(SEC-06) — 3중 방어:** ① 입구 가드레일 ② **반입 문서 살균**(문서 속 숨은 지시문 무력화) ③ 출구 자격증명 가림 + 자동 레드팀 상시 회귀.
- **종료(SEC-08):** 데이터·로그·백업·모델 캐시 완전삭제 + 확인서. 삭제 전 "복구되는 백업인지" 검증 절차 보유.

---

## 10. 12주 수행 일정 — 실작업·협조·달력 시간을 갈라서 관리

| 주차 | 관문 | 🟢 제안사 실작업 | 🤝 발주사 협조 (요청 기한) |
|---|---|---|---|
| 1~2주 | **G1** 데이터 준비 | 원천 조사·품질 측정 가동, 실 GPU 속도 실측 — **실작업 ~3일** | 계정·망 접근(착수+3일) · 데이터 제공 승인(+7일) · 업무담당자 배정(+10일) |
| 3~4주 | **G2** 기준·평가설계 | 용어·코드 매핑, 평가셋 100건 초안, **모델 공개 비교** — ~4일 | 업무전문가 2인 정답 검토·승인 |
| 5~8주 | **G3** 기능·보안 준비 | **구현 전체**(커넥터·지식층·화면·레드팀·부하시험) — 당사 최단 구간 | 중간점검 참석 |
| 9~10주 | **G4** 비공개 평가 | 오류분석·수정·회귀 — **반복 2~3회전** | 비공개 300건 평가는 발주사 주관 |
| 11주 | **G5** 그림자 운영 | 관찰·즉시 수정 | 상담사 편성(10주차까지 확정) |
| 12주 | **G6** 최종 인수 | 산출물 정리·재현시험·교육·보안 삭제 확인서 | 인수 확인·교육 참석 |

> **발주사 협조 캘린더(착수계획서에 명문화):** 기한이 지나면 해당 관문 일정은 지난 날수만큼
> 뒤로 밀되 범위와 검증 기준은 그대로 둡니다. "기다리겠다"가 아니라 "챙겨드리겠다"는 협업 표입니다.
> 당사 실작업이 짧은 만큼 남는 시간은 같은 12주 안에서 검증을 2~3회전 더 돌리고 시연을 준비하는 데 씁니다.

---

## 11. 위험을 숨기지 않습니다 — 조건부 3건의 관리 계획

| 위험 | 내용 | 통제 |
|---|---|---|
| **AQR-03 비열위 95%** | 로컬 오픈 모델은 최상위 상용 모델과 맨몸 격차가 있음. 전략은 격차를 **데이터로 메우는 것** — 정비 데이터·온톨로지·근거 게이트가 붙은 로컬 구성 대(對) 맨몸 기준모델 비교(자사 실측: 그라운딩 강화 > 모델 2배 확대) | **G2(4주차) 조기 비교 자청** · 미달 시 승인 상용모델 대체경로 이중화(근거·보류·감사 골격은 동일 유지) |
| **PER-01·03 성능** | 3초·20세션은 GPU 기종·장수에 따라 달라지는 실측 값 | 1~2주차 실 GPU 실측 → 예산표 갱신 · 미달 시 양자화·모델 크기 조정(전부 회귀 게이트 통과 조건) · G3 전 자체 부하시험 |
| **데이터 품질 의존** | 정확도 90%는 원천 데이터가 받쳐줘야 함 — 핵심 결손은 어떤 모델도 못 메움 | G1 관문에서 결손을 **숫자로 보고** — 추정으로 메우지 않고 대체안 또는 범위 조정을 서면 제안. 커머스 커넥터는 신규 개발이나 정규화 허브 골격 재사용으로 파서 계층에 국한 |

---

## 12. 산출물·개방성 약속 — 발주사가 직접 굴릴 수 있는 상태로

- 데이터·규칙·평가셋: **CSV / JSON / SQL** — 원본 구조·의미 유지, 발주사 저장소에 이력 포함 제출
- 소스코드·설정·프롬프트: 버전 이력 + 빌드·재현 절차 — **제출물만으로 핵심 평가 재현 가능**을 인수 조건으로 수용
- 모델: **GGUF 표준 파일** + 정확한 출처·버전·라이선스 명세 — 발주사가 직접 교체·재구동 가능
- 전 구성요소의 이름·버전·라이선스·지원종료일 공개 — 비상업 라이선스 모델은 기본 구성에서 배제
- 종료 시 데이터·로그·백업·모델 캐시 **완전삭제 + 확인서**
- 발주사 데이터를 당사 모델 학습·벤치마크·홍보에 사용하지 않음 — 로컬 구동이라 기술적으로도 불가능

> **맺음 한 줄.** 상담사가 3초 안에 "된다/안 된다 + 왜 + 어느 규정 몇 판"을 받고
> AI가 모르면 모른다고 말하는 시스템입니다. 그 증명을 12주 안에 발주사 장비 안에서 끝내겠습니다.

---

## 부록. 용어 풀이 — 쉬운 말로

| 용어 | 쉬운 풀이 |
|---|---|
| 로컬 LLM | 외부 서비스가 아니라 회사 안 GPU에서 직접 돌리는 AI 모델 — 데이터가 밖으로 안 나감 |
| 전문가 분업형(MoE) | 큰 두뇌를 여러 전문가로 쪼개 질문마다 필요한 전문가만 깨우는 방식 — 큰 지식 + 빠른 속도 |
| 양자화(4/8비트 압축) | 모델 무게를 압축해 GPU 메모리를 아끼는 기술 — 숫자가 작을수록 가볍지만 미세하게 덜 정밀 |
| GGUF | 누구나 쓰는 표준 형식의 AI 모델 파일 — 특정 회사 전용이 아니라 직접 교체 가능 |
| 세션 캐시(대화 기억) | 동시 접속자마다 대화 앞부분을 GPU에 기억해 두는 메모장 — 접속자 수만큼 자리 필요 |
| 임베딩(검색 눈) | 문서와 질문을 "의미 좌표"로 바꿔 비슷한 규정을 찾게 하는 부품 |
| 리랭커(재정렬기) | 검색이 찾아온 후보들을 "진짜 근거 순"으로 다시 줄 세우는 정밀 심사관 |
| 하이브리드 검색(겹검색) | 의미로 찾는 검색과 정확한 단어·코드로 찾는 검색을 동시에 쓰는 방식 |
| 그라운딩(근거 묶기) | AI가 제공된 자료 밖의 내용을 지어내지 못하게 근거에 묶어 두는 기법 |
| 온톨로지(규정 관계망) | 규정·예외·우선순위를 그물처럼 연결해 둔 지식 관계망 — AI 재량이 아닌 규칙으로 판단 |
| P95 | 요청 100건 중 95번째로 느린 응답의 시간 — "대부분의 사용자가 겪는 최악값" |
| KV 캐시 | 대화 앞부분 계산 결과를 GPU에 저장해 재계산을 피하는 메모리 |

---

*본 문서는 제안 검토용 초안입니다. RFP 문의처: 이기열 팀장(AX추진팀) · 제안서 규격: A4 세로 · 맑은 고딕 · DOCX+PDF*
*원본 아티팩트: 제안서 초안(claude.ai/code/artifact/9050b56a…) · 제안요약 슬라이드(3fe08b05…)*
