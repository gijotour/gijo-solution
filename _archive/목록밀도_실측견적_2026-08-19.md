# 「엑셀급 촘촘 리스트」 전역 반영 — 실측 견적 (gijo-scout, 2026-08-19)

사장님 질문: 「세부 리스트들 엑셀처럼 촘촘하게 — 전체 반영에 어려움이 있을까?」 → **판정: 중간(가능)**.

## 핵심 숫자
- 공용 시트(gijo-ui.css) 주입: **18/18 화면 100%**(nav.js loadDesignSystem) — 그러나 표 규칙(.g-table) 소비 **0곳**.
- ⚠ 공용 시트의 `body.g-ui{line-height:1.7}`(08-01 가독성 조치)이 **div형 행을 오히려 부풀리는 중**(예외는 td/배지 등만).
- 행높이 계산값: **31.3~67.9px, 중앙값 ≈44px** (목표 24~26px).
- 엑셀 요소 채택 현황: 24~26px 행 0/18 · 홀짝 0/18 · 고정 헤더 1/18(threat, embed 한정 — nav.js:1366) · 숫자 우측정렬 2/18(kpi .num·sessions 일부).

## 반영 경로(작업량)
| 묶음 | 화면 | 방법 |
|---|---|---|
| 표 8 | vulnscan·sbom·report·threat·inventory·maintenance·kpi·handover | **gijo-ui.css 한 곳** — `.g-ui table td/th`가 전부 잡음 |
| div행 8 | approvals(.rv-row)·analysis(.risk-row)·sessions(.srow)·audit/syslog(.aud)·loganalysis(.frow/.erow)·assets(.pk-row) | gijo-ui.css에 **클래스 나열**(같은 파일에 선례 3회) |
| 마크업 수정 1 | compliance(행이 인라인 style·클래스 없음) | JS 템플릿에 클래스 부여 |
| 원천 제외 2 | hardening(카드그리드)·products(CRUD 카드) | 표 개념 아님 — 억지 적용 금지 |
| 숫자 우측정렬 | 표·행 전부 | **CSS로 불가** — 화면별 렌더에 .num 부여(유일한 화면별 작업) |

## 제약·함정(실측)
- uireadability 시험은 **font-size만** 검사(11px 하한) — padding/line-height 조임은 무충돌. 단 현 폰트가 이미 하한 근처(--g-t-xs=11px)라 **폰트 축소는 여지 0**.
- 배지 높이 ≈20px — 행 24px에 상하 패딩 3~4px면 맞닿음: 배지 있는 행은 26px 하한 권장.
- 주석↔실값 불일치 3건: loganalysis "26px"(실제≈31) · sessions "54px"(실제≈68 — 08-01 line-height 1.7 이후 낡음) · handover td 1.55는 .g-ui td 1.45에 짐(specificity).
- 홀짝·전역 고정 헤더는 신설 항목.

## 다음
밀도 규격 시안 1개(행 26px 기준·홀짝·고정 헤더 전역·.num 관례, 본보기 vulnscan 표+approvals 카드행, before/after 숫자) → 무확인 구현(사장님 위임) → 표8+div8+compliance 반영, .num은 화면별 점진.
