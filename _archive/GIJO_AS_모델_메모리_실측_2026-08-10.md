# GIJO AS — 모델 메모리·티어 실측 (2026-08-10, Mac + Windows)

**왜 남기나**: 티어표(`GIJO_TIERS`)가 **7B 시절 산술**이라는 것이 이날 드러났다.
기준 모델을 14B로 올린 결정(2026-08-07, 코드 반영 `719501f`)이 **티어표에는 안 따라왔다.**
아래 값이 없으면 다음 세션이 같은 것을 다시 재야 한다.

**측정 기계**: Metal = 이 Mac(통합메모리 32GB) · CUDA = 운영(RTX 3090 24GB, Windows 실측)
**측정 방법**: `ps -Ao rss` / `nvidia-smi`. **갓 띄워 예열 2회 뒤** 값 (아래 §4 함정 참조)

---

## 1. 실측 점유

### 채팅 모델

| 모델 | 파일 | ctx 8192 | ctx 16384 | ctx 32768 |
|---|---|---|---|---|
| `qwen3-14b` (Metal) | 8.4 GB | — | **11.00** | **16.00** |
| `qwen3-14b` (CUDA) | 8.38 GB | — | **10.70** | **13.20** |
| `gijo-main-orchestrator` 7.6B (Metal) | 5.07 GB | **5.45** | **6.04** | **6.85** |

### 임베딩 (`bge-m3` 566M)

| 설정 | Metal | CUDA |
|---|---|---|
| ctx/batch **8192** (현행 제품) | **1.55** | **2.50** |
| ctx/batch **2048** | **1.00** | (미측정) |

⚠ **CUDA의 KV 절감이 Metal보다 작다.** Metal은 32K→16K가 ×0.69인데 CUDA는 ×0.81이다.
Metal 값에 비율을 곱해 CUDA를 추정하면 **낙관적으로 틀린다**(이날 실제로 8.9GB로 추정했다가 10.7GB로 정정).

---

## 2. 프롬프트 실측 (`/api/llm-activity/history`, 14B)

| 호출 | 입력 토큰 |
|---|---|
| **구조화 응답 — 도구 고르는 결정문** | **7,984** |
| 응답 완료 (산문) | 3,028 / 2,620 |

⚠ `localengine.ts:777` `확인용칸()` — 칸당 = `fittedCtx/2`, **8192 미만이면 `--parallel 1`**.
ctx 16384면 칸당 **정확히 8192**라 문턱을 아슬하게 넘어 `--parallel 2`가 붙고,
프롬프트 7,984에 **여유 208토큰**만 남는다. 대화 이력 한 줄이면 넘친다.
→ **ctx 16384 이하에서는 `--parallel 1`이어야 한다.** 메모리는 안 늘어난다
(KV는 ctx 총량으로 잡히고 `--parallel`은 나누기만 한다).

---

## 3. 이 실측이 드러낸 결함 3가지

### ⓐ `needMb`가 ctx를 안 본다

```ts
localengine.ts:345  const needMb = modelFileSizeMb(modelId) + tier.overheadMb;  // 고정 5000
```

실측 오버헤드(점유 − 파일):

| | @16K | @32K |
|---|---|---|
| 7.6B (Metal) | +0.97 | +1.78 |
| 14B (Metal) | +2.60 | +7.60 |
| 14B (CUDA) | +2.30 | +4.90 |

**0.97 ~ 7.60GB(8배)를 고정 5000으로 덮는다.** 7.6B엔 과대(멀쩡한데 축출),
14B@32K Metal엔 과소(안 맞는 걸 들임). 축출량(`reclaimed`)도 같은 오차다.

⚠ **플랫폼 상수로 바꿔도 안 된다** — CUDA 32K는 4.9GB라 **코드 5000이 원래 맞았다.**
Metal용 8000으로 바꾸면 운영에서 needMb 16.4GB가 되어 **멀쩡한 적재를 막는다.**
→ 상수를 버리고 **첫 적재 때 재서 `(모델, ctx, 플랫폼)`으로 기억**하는 쪽이 맞다.

### ⓑ 임베딩이 항상 지는 경주다

`needMb`는 파일 전체에서 **345행 한 곳**뿐이고 `spawnEmbeddingServer`에는 검사도 축출도 **없다.**
그런데 기동 순서가 이렇다:

```
autoStartLocalEngines()
  → ensureModelLoaded(채팅)    ← 이때 bge-m3가 아직 없어 여유가 커 보인다 → 임베딩 몫까지 먹는다
  → spawnEmbeddingServer()    ← 검사 없이 남은 자리에 밀어 넣는다
```

빠듯한 기계에서 임베딩이 실패하거나 CPU로 떨어지면 **RAG가 죽는다** — 근거가 제품 핵심 약속인데
조용히 빠진다. 고칠 것 둘: ① `needMb`에 임베딩 상주분 포함 ② **기동 순서를 임베딩 먼저로**
(또는 채팅 적재 시 임베딩 몫을 예약).

### ⓒ 임베딩 배치 하한이 8192가 아니다

`spawnEmbeddingServer`가 `--batch-size 8192 --ubatch-size 8192`로 뜨는 근거는
*「기본값 512로 뜨면 512토큰 초과 입력에 HTTP 500 → 큰 청크가 조용히 인입 실패」*(2026-07-20 실측)다.
**경고는 유효하되 하한은 8192가 아니라 청크 최대치다.**

- `memory.ts:139` `CHUNK_SIZE = 800` — **글자** 단위
- 800자 한국어 청크를 `ubatch 2048` 서버에 실제로 보내 **HTTP 200 · 1024차원** 확인
- → 2048로 내리면 Metal 기준 **0.55GB 절약**(1.55 → 1.00)

---

## 4. ⚠ 측정 함정 — RSS는 시간이 지나면 줄어든다

같은 설정의 `bge-m3`가:

| | RSS |
|---|---|
| 제품 인스턴스 (오래 떠 있음) | **0.58 GB** |
| 같은 설정, 갓 띄움 | **1.55 GB** |

이 때문에 배치를 2048로 줄인 쪽(1.00GB)이 처음엔 **"더 크다"**고 나왔다.
`qwen3-14b@32K`도 예열 직후 16.00GB였다가 나중에 13.42GB로 내려갔다.

> **오래 떠 있는 프로세스와 갓 띄운 프로세스를 비교하지 마라.**
> 둘 다 갓 띄워 **같은 요청을 같은 횟수** 보낸 뒤 재야 한다.

같은 이유로 **적재 직후(추론 전) 값도 쓰면 안 된다** — 7.6B는 추론 전후가 같았지만(6.85→6.85)
모델마다 다르다. 예열 2회를 기본으로 한다.

---

## 5. 티어표 제안 (CUDA 기준 — 고객은 CUDA다)

`bge-m3` 2.5 · `14B@16K` 10.7 · `14B@32K` 13.2 에서 유도:

| 채팅 | @16K + 임베딩 | @32K + 임베딩 |
|---|---|---|
| 1개 | **13.2** | **15.7** |
| 2개 | 23.9 | 28.9 |
| 3개 | 34.6 | 42.1 |

| 티어 | VRAM급 | ctx | parallel | 채팅 최대 | CUDA 점유 | 여유 |
|---|---|---|---|---|---|---|
| — | **16GB 미만** | — | — | — | — | **미지원** |
| **Lite** | **16GB급** | 16384 | **1** | 1 | 13.2 | 2.8 |
| **Standard** | 24GB급 | 32768 | 2 | 1 | 15.7 | 8.3 |
| **Pro** | 32GB급 | 32768 | 2 | 2 | 28.9 | 3.1 |

**★ `maxLoadedModels`가 더 이상 티어의 주 변수가 아니다.** LoRA 설계(베이스 1 + 어댑터 N,
`localengine.ts:436`)라 에이전트 6명이 **한 베이스를 공유**한다 — 운영이 이미 그렇게 돈다.
기존 표가 「LLM 몇 개」로 등급을 나눈 건 7B·모델별 배정 시절 관점이다. **진짜 변수는 ctx다.**

### 12GB급은 뺀다 (2026-08-10 사용자 결정)

CUDA에서 `14B@16K + bge-m3 = 13.2GB > 12GB`. **본 제품으로는 12GB를 못 받는다.**
그 시장은 아래 라이트 버전이 받는다 — 본 제품을 깎지 않는다.

---

## 6. 라이트 버전 (별도 제품 — 담당자 PC 단독·최소 기능·7B)

| 구성 | 7B | 임베딩 | 합계 (Metal) | 필요 GPU |
|---|---|---|---|---|
| **ⓐ 라우팅 제거 + ctx 8192** | 5.45 | 1.00 | **6.45** | **8GB급** (여유 1.5) |
| ⓑ 라우팅 유지 + ctx 16384 | 6.04 | 1.00 | 7.04 | 8GB급 (여유 0.9 — 빠듯) |
| ⓒ 지금 제품과 같은 설정 | 6.85 | 1.55 | 8.40 | 12GB급 |

**ⓐ 채택.** 「최소 기능 = 라우팅 제거」와 **같은 하나의 결정**이다 — 라우팅을 빼야
프롬프트가 7,984 → 3,000대가 되고, 그래야 ctx 8192가 성립한다.
라우팅을 남기면 ctx 8192에 프롬프트 7,984라 여유가 없다(§2와 같은 함정).

⚠ **CUDA 미측정** — Metal 값이다. CUDA `bge-m3`가 8192에서 2.5GB(Metal 1.55GB보다 큼)였으니
CUDA 라이트는 8GB급이 빠듯하고 12GB급이 안전할 수 있다. 다음 세션에 잰다:
① `gijo-main-orchestrator` @ ctx 8192 단독 ② `bge-m3` @ ctx/batch 2048

### 라이트에 걸리는 것들

- ⚠ **규정 흔들림이 7B에서 더 심하다** — 「퇴사자 계정」 10회:
  7.6B는 `1년 4 · 2년 3 · 즉시+1년 2 · 불명 1`(4갈래 분산), 14B는 `즉시삭제 6`으로 쏠림.
  최소 기능에 법·규정 질문이 들어가면 **「숫자·조문은 원문에서 그대로 복사」가 필수**다.
- ⚠ **RAG는 빼면 안 된다** — 빼면 「근거」가 빠지고, 그게 제품이 파는 것이다.
- ⚠ `modeldefault.test.ts:48` `expect(engine).not.toBe("gijo-main-orchestrator")` —
  라이트가 7.6B를 정식 기본값으로 쓰면 **이 시험이 막는다.** 제품이 둘로 갈리면 시험도 갈려야 한다.
- ✅ **문체는 걱정 없다** — [[GIJO_AS_문체_AI스러움_실측_2026-08-10.md]]의 0.153은
  `chat_logs`(2026-07-16~08-09, 14B 승격 8/7 이전이 대부분)라 **대체로 7B가 만든 값**이다.
- 어느 7B인가는 미정: `gijo-main-orchestrator`(7.6B·qwen2·5.07GB) vs
  `merged-lily-gijo-loop-ai-securityllm`(7.2B·llama·**4.78GB**·보안 특화).
  보안 특화가 맞아 보이나 **게이트 점수를 모른다** — 착수 전 둘 다 돌려야 한다.

---

## 7. 다시 재는 법

```bash
# ① 배정 확인 — 기준 모델로 돌고 있나 (이걸 안 보고 재서 하루 값을 버렸다)
cd ~/gijo-as/server && node --input-type=module -e "
const {db}=await import('./dist/db.js');
db.prepare(\"SELECT key,value FROM app_state WHERE key LIKE 'agentModel:%'\").all()
  .forEach(r=>console.log(r.key,'=',r.value));"

# ② 배정 변경은 제품 API로만. 원래값은 /api/agents 의 assignedModelId (modelId 아님)
#    POST /api/agents/:id/model {"modelId":"qwen3-14b"}

# ③ 적재 확인 — 그 모델이 실제로 올라왔는지 보고 나서 잰다
#    GET /api/localengine/status 의 loaded[].modelId
#    (14B 예열은 ~98초. 예열이 몇 초면 안 올라온 것이다)

# ④ 점유
ps -Ao rss=,command= | grep "llama-server -m" | grep -v grep
```

⚠ 스크래치 스크립트는 **반드시 `cd ~/gijo-as/server`에서** 돌린다 — `db.ts:20`의 `DB_PATH`가
cwd 상대(`data/gijo-as.sqlite`)라, 다른 데서 돌리면 조용히 **빈 DB를 새로 만들고** 측정이 통째로 무효가 된다.

---

*근거: Mac 실측(2026-08-10) · Windows 운영 실측(RTX 3090, 같은 날) · `719501f`(14B 승격) ·
같은 날 [[GIJO_AS_RAG_오염_실측_2026-08-10.md]] · [[GIJO_AS_문체_AI스러움_실측_2026-08-10.md]]*
