# [인계] max → win — 결함 #1·#2 재현 완료 (2026-08-17)

win의 `인계_win_max_표준검증_결함재현_2026-08-17.md` 요청대로 **#2(대시보드 거짓 멈춤)·#1(how-to 리포트 전환)을 재현**했다.
둘 다 **win의 뿌리 가설이 정확**하다. 수정은 win(역할 분담 유지). 기준 hub 1ecd703.

재현 방식: **살아있는 라이트 앱(로그인됨)에 CDP(9400)로 붙어 런타임 실측** + 소스 경로 추적 + 8080/8081 서버 실측. **둘 다 실앱에서 증상 그대로 재현됨**(아래 「런타임 확정」).

> ⚠ CDP 붙이는 법(다음 검증자용): 앱은 `--remote-debugging-port=9400 --remote-allow-origins=*`로 띄운다. **allow-origins 없으면 Chrome 111+가 DevTools 엔드포인트를 행**시킨다(이번에 두 번 헛다리). `/json/list`는 **curl이 아니라 node 네이티브 fetch**로 — 이 Mac에서 curl은 행. gijo API는 `document.querySelector("iframe").contentWindow.gijo`.

---

## ✅ #2 재현 — 대시보드 「멈춰 있음」 거짓 (풀 vs 실서버 불일치) — **win 가설 확정**

**뿌리(코드 확정):** `server/src/engine/localengine.ts:237`
```
const loaded = [...pool.values()].map((m) => ({ modelId, port, ready, ... }));
```
→ status의 `loaded`는 **앱이 직접 띄운 pool만** 반영한다. **포트 health를 보지 않는다.** (win 추정 그대로 — 기본모델과 비교하는 로직이 아님.)

**대시보드 판정(`lite-dashboard.html:145-160`):**
- `적재 = (st.loaded||[]).filter(l=>l.ready)` 가 0개 **그리고** `listModels()`에 파일이 있으면 → **"멈춰 있음"**.

**★ 런타임 확정(살아있는 라이트 앱, 로그인됨, CDP):**
```
g.getLocalEngineStatus() → { loaded: [], modelId: null }     ← 풀 비어 있음
g.listModels()           → ["gijo-main-orchestrator"]        ← 파일은 있음(models.length=1)
8080 /v1/models          → models/qwen3-14b/qwen3-14b.gguf   ← 실제 서빙 중(health ok)
```
→ 대시보드 판정: `적재(=loaded.filter ready)=0` **그리고** `models.length=1` → **"멈춰 있음"**을 그린다.
그런데 8080은 실제 서빙(챗도 답이 나옴). **거짓 멈춤을 실앱에서 그대로 재현.**

**뿌리 정리:**
- 8080 llama-server는 **손으로 띄운 것**(`llama-server -m … -ngl` 직접 실행) — 앱의 `ensureModelLoaded` 경로가 아니다 → **pool에 없음** → `loaded` 빔.
- 즉 **8080은 서빙, 풀은 모름** = win이 말한 "풀 vs 실서버 불일치" 정확히 재현.

**win 질문 답:**
- "멈춤일 때 loaded 비었나?" → **예**(pool.values()가 빔 — 앱이 안 띄운 모델이라).
- "8080이 풀에 없나?" → **없다**(외부 기동 모델은 pool에 등록 안 됨).
- "qwen3-14b를 어떻게 올렸나?" → **CLI 수동 기동**(앱 밖). 이 경로는 pool에 안 들어간다.
- "부팅 자동로드가 돌았나?" → `localengine.ts:726` 자동로드는 **기본 7.6B(또는 마지막모델)**를 `ensureModelLoaded`하려 하는데, ⓐ 7.6B 파일이 없거나 ⓑ 8080 포트가 이미 수동 llama에 점유돼 있어 pool에 ready 항목이 안 생긴다 → 여전히 "멈춰 있음".

**⚠ 범위 주의(과대평가 방지):** *포장 배포본*에서 고객이 **앱을 통해** 모델을 올리면 pool에 등록돼 "돌고 있음"으로 정상 표시된다. 이 거짓 "멈춤"은 **모델이 앱 밖에서 도는 경우**(외부 GPU 박스·수동 기동·포트 드리프트·pool의 ready 유실)에 나온다. 우리 검증 Mac이 바로 그 케이스(수동 llama). **다만 원격 GPU·사내 vLLM을 붙이는 폐쇄망 시나리오에선 실고객도 맞을 수 있는 견고성 결함**이라 고칠 값이 있다.

**win 수리안(그대로 동의):** status가 **실제 liveness(8080 포트 health)도 보게** 한다 — pool에 없어도 지정 포트가 응답하면 "돌고 있음". (pool만 보지 않기.)

---

## ✅ #1 재현 — how-to가 즉답 대신 리포트로 전환 — **win 가설 확정**

**뿌리(코드 확정):**
- 라이트 「AI에게 물어보기」(`lite-chat.html:115`)는 `g.sendInstruction`을 부른다.
- preload:206 `sendInstruction` → `dispatchApi.send` → **비스트리밍 `/api/dispatch`**. (스트리밍 `sendInstructionStream`(preload:211)은 **안 쓴다**.)
- `/api/dispatch`(`dispatcher.ts:1550`)는 `LONG_ANSWER_MS`(=**30초**, `longanswer.ts:28`) 넘으면 **리포트로 핸드오프**(first===null → handedOff).
- 스트리밍 `/api/dispatch/stream`(1607)엔 이 전환이 **없다**(주석: "30초 리포트 전환은 여기 없다").

**★ 런타임 확정(살아있는 라이트 챗에 실제 태움, CDP):**
```
g.sendInstruction("프롬프트 인젝션 공격을 막으려면…")
  → 30,006ms 만에 반환, isReport=true, tools=[]
  → 응답: "시간이 걸리는 작업이라 리포트로 작성해 드리겠습니다.
           다 되면 알려드릴게요 — 다른 일 보셔도 됩니다."
```
**정확히 30초(LONG_ANSWER_MS)에 걸려 리포트로 전환** — 원 발견의 증상 문구 그대로. 결정적.

**win 질문 답:**
- "스트리밍 vs 논스트리밍?" → **논스트리밍**(`/api/dispatch`). 그래서 30초 전환에 걸린다.
- "14B how-to가 실제 몇 초?" → **30초 초과 확정**(런타임 30,006ms). 참고로 8080 직접 실측:
  - 맨 질문(프롬프트 47토큰·완성 400): **18.3초**.
  - **RAG 문맥 포함(프롬프트 1891토큰·완성 400): 28.5초** — 여기에 도구선택·출구관문이 더 붙어 30초 돌파.

**⚠ 티어 주의:** 이 측정은 **14B**(라이트 기준 7.6B보다 위)다. **출하 7.6B면 ~1.8배 빨라** 같은 문맥이 15초대로 내려가 30초 안에 들 가능성이 크다 — 즉 **티어 규율(7.6B)이 이 증상을 상당히 완화**한다. 그래도 문맥이 길거나 원격 느린 모델이면 언제든 재발하는 **구조적 결함**이다.

**수리안(둘 다 사장님 결정 필요 — max 추천 ⓐ):**
- **ⓐ (추천)** 라이트 「물어보기」를 **스트리밍 경로(`sendInstructionStream`)로 전환** — 스트리밍엔 리포트 전환이 없어, 느려도 글자가 흐른다. 챗 UX("물어보기")에 자연스럽다.
  - ⚠ 단, 비스트리밍이 돌려주던 **쓰기 결재판(`r.approval` → renderApproval)**을 스트리밍에서도 살려야 한다(작업추가 등 쓰기 도구가 조용히 증발하면 안 됨 — 공용 dispatchentrances 감시 대상).
- **ⓑ** 타임아웃을 모델 속도에 맞춰 상향(예: 라이트 로컬은 60초). 단순하나 "리포트 전환=배려"의 취지를 약화.

---

---

## ✅ ① 표준제품 Mac 클라 검증 완료 (win 요청)

표준 **5.21.0 arm64 dmg 빌드**(`client/release/`, ad-hoc 서명·봉인 확인) → **CDP(9401)로 실렌더 검증**.

**Mac 렌더 정상(win이 Windows exe로 못 보는 것):**
- 앱 빌드·기동·로그인(관리자)·대시보드·좌측 메뉴(발견·수집/우선순위/조치/검증/보고/등록부/AI)·우선순위(triage) 화면 **모두 정상 렌더**. 빈 화면 없음.
- **custom 제공자 렌더 확인** — 설정(settings.html?s=ai) 제공자 드롭다운: `Google Gemini · Anthropic Claude · OpenAI · **직접 입력 (OpenAI 호환)**` + 주소칸(`http://10.8.0.12:8080/v1`)·프록시칸까지 정상.

**취약점 한글 한 줄 — 3곳 렌더 코드 올바름·일관(확정):**
- ① `vulnscan.html:501` `f.plain ? <div class="vd-plain">→…</div> : ""`
- ② `approvals.html:549` `f.plain ? <div class="dt-plain">→…</div> : ""`
- ③ `map-view.js:150` "먼저 볼 것" → `f.plain ? <div class="mv-plain">→…</div> : ""`
- 셋 다 `f.plain` 있을 때만 회색 한 줄(원문 아래), 없으면 안 그림 — 설계대로.

**⚠ 라이브 픽셀 미검(데이터 한계):** 번들 서버(localhost:7445)엔 취약점 findings가 **샘플 4개·0 findings**뿐. max가 `importVulnScan`으로 **샘플 nessus CSV**를 넣어봤으나, `f.plain`은 **KEV/CVE 실인리치먼트로 채워지는 값**이라 **가짜 CVE(CVE-2024-0001)엔 안 붙어** 한 줄이 안 그려짐(코드의 "못 찾으면 안 그림"이 정확히 작동). → **라이브 한 줄 확인은 win 운영(10.8.0.1:4000)의 실 findings(실 CVE·KEV)로만 가능.** (테스트 자산은 검증 후 전량 삭제해 원복함.)

**결론:** 표준 Mac 클라는 정상 렌더. 취약점 한 줄 3곳 코드 올바름. **라이브 한 줄 픽셀만 운영 실데이터로 최종 확인 남음**(win 운영 접속 검증 회차 or 사장님 눈확인).

---

## ✅ win 수리(76687ea) 런타임 검증 완료 — 두 수리 작동·회귀 없음 (max, 2026-08-17)

라이트 **1.1.2 재빌드**(stage-llama-metal + #1·#2 반영)로 재기동·로그인 후 CDP 실측.

**#1 스트리밍 (사장님 ⓐ):**
- `sendInstructionStream(how-to)` → **32.7초** 걸렸으나 **279 델타 스트리밍 · isReport=false · 실답 도착**("프롬프트 인젝션 방지: 1.시스템 프롬프트 보안 2.입력 검증…"). **수리 전 30초 리포트 전환이 사라짐.** ✓
- **결재판 회귀 없음:** add_task는 `registry.ts:593 write:false`("나에게만 영향 — 즉시 담고 잘못 담으면 완료로 지움")라 결재판 없이 즉시 실행이 **정상**. 실제 `write:true` 도구는 스트리밍도 `done.result`로 approval 전달(스트리밍 핸들러가 같은 dispatchInstruction 사용, 코드 확인). 내가 우려한 「쓰기 증발」 없음. (검증 중 만든 테스트 작업은 삭제·원복)

**#2 포트 health:**
- `getLocalEngineStatus().loaded = [{qwen3-14b, port:8080, ready:true}]`, running:true → **"돌고 있음"**. **수리 전 `loaded:[]` 거짓 멈춤 해소.** ✓

**⚠ 빌드 절차 보정(win 인계의 stage-llama-cuda 관련):**
- Mac 라이트 재빌드는 `tools/stage-llama-cuda.mjs`(win Windows/CUDA용, 이 Mac에 없음)가 아니라 **`tools/stage-llama-metal.sh`**. 스킵하면 번들 llama 누락으로 **앱 부팅 즉사**(첫 빌드에서 실증). 라이트 **1.1.1→1.1.2**로 올림.

→ **#1·#2 완결.** Drive 1.1.2 교체는 검토관문 + 사장님 승인 후.

## 참고
- gb10: 이 문서 push 시점 hub 0/0 깨끗(win force-정리 끝남 확인). 새 문서만 ff.
