# GIJO AS 에이전트 자동화 계획서 — "보는 도구"에서 "시키는 도구"로

> **목표:** 보안담당자의 지시를 메인 오케스트레이터가 받아 필요한 입력을 되물어 채우고, 등록·조치·조회를
> 자동 실행한다. 화면 수동 입력을 최소화한다(자비스급이 아니라 "다 준비해놓고 결재만 받는 유능한 부하").
> **작성:** 2026-07-17. QA 결과 보고서(업로드 전 경로, Notion 2026-07-17)와 실측 벤치 결과 반영.

---

## 1. 진단 — 왜 지금 수동이 많은가

- `intent.ts` 의도 분류가 **scan/analyze/report/chat 4개 고정**, 복합 지시 계획(`dispatcher.ts`)도 정규식 3종.
- 자산 등록·조치 배정·보안제품 등록·유지보수 등 **수십 개 엔진 기능이 LLM에게 "도구"로 노출돼 있지 않다.**
- 필수 입력이 빠졌을 때 **되물어보는(slot filling) 루프가 없다.**
- 근본 병목은 모델이 아니라 아키텍처다 — 단, 모델도 역할별로 재정의가 필요했다(아래 §2 실측).

**QA 보고서에서 확인된 구조 원칙(준수):** 무거운 판단(파싱·검증·실행)은 규칙 코드, LLM은 도구 선택·인자 추출·서술만.
형식은 llama.cpp `json_schema` 강제 디코딩으로 보장(파싱 실패 클래스 제거), 실패 시 기존 정규식 폴백 유지.

---

## 2. 메인 LLM 모델 정의 (2026-07-17 실측 확정)

**벤치:** 한글 지시 10문항(도구 선택 + 한글 인자 추출 + "도구 없음" 절제 판단), json_schema 강제,
temperature 0, 실 RTX 3090. 스크립트 `server/tools/orchestrator-bench.mjs` (재실행 가능, 결과 `server/tools/bench-results/`).

| 후보 | 정체 | 도구선택 | 인자정확 | 평균응답 | 판정 |
|---|---|---|---|---|---|
| **gijo-main-orchestrator** | Qwen2.5 7.6B 머지 (제품 자체) | **10/10** | **10/10** | 232ms | ✅ **메인 확정** |
| gijo-orchestrator-ko | 〃 ko 변형 | 10/10 | 9/10 (id 환각 1) | 236ms | 예비 |
| Hermes-3 8B | function calling 특화 | 9/10 | 9/10 | 253ms | 학습루프 베이스 유지 |
| merged-lily-gijo (현행 기동) | Llama 7.2B 보안 합성 | 5/10 | 5/10 | 256ms | 오케스트레이션 **부적합** |

### 확정 모델 라인업

| 역할 | 모델 | 근거 |
|---|---|---|
| **오케스트레이터** (의도분류·도구호출·되물음) | **gijo-main-orchestrator** | 실측 만점. 잡담 절제·"도구 없음" 판단·한글 인자 추출 전부 정확 |
| 보안 채팅·해설 (normaltic 등 서술 담당) | merged-lily-gijo | 보안 도메인 지식 합성 모델 — QA 원칙대로 "서술" 전담 |
| RAG 임베딩 | bge-m3 | 변경 없음 |
| 학습루프 베이스 | Hermes-3 (HF fp16) | 변경 없음 |
| 추후 실험 | qwen3-30b-a3b (VRAM 단독), Qwen2.5-14B | QA 보고서 업그레이드 경로 — 데이터 축적 후 |

**조치:** 오케스트레이터 에이전트에 gijo-main-orchestrator 를 명시 할당(멀티모델 풀이 병렬 상주 —
merged-lily와 동시 로드, 실측 VRAM 여유 내). 전역 "마지막 사용 모델"에 의존하지 않는다.

---

## 3. QA 결과 보고서 반영 사항

| QA 발견 | 이 계획에서의 반영 |
|---|---|
| 스마트 통합 업로드 구현됨(파일 → 자동 분류·등록) | **업로드 경로는 자동화 완료** — 본 계획은 지시(채팅) 경로의 조회·등록·조치 자동화를 담당. 두 경로가 "수동 입력 최소화"의 양 날개 |
| 7B 한계: "모든 걸 다 처리"엔 부족, 설계로 보완 | 도메인 라우팅으로 한 턴에 노출되는 도구 5~6개 제한(메뉴 하나씩 진행과 정합). json_schema 강제. 규칙 폴백 유지 |
| P1 병렬 에이전트 실DB 오염 | 에이전트 루프 검증은 **격리 서버(별도 포트+임시 DB)** 로만. 실DB 검증은 조회성 도구까지만 |
| P2 깨진 CSV 조용한 0건 | Phase 2 쓰기 도구 작업 시 "0건 파싱 경고" 함께 처리 |
| P2 재시작 직후 임베딩 로딩 창 400 | Phase 2에서 클라이언트 재시도·대기 안내와 함께 처리 |

---

## 4. 단계별 계획 — 메뉴 하나씩 (총 예상 5~7일)

진행 순서는 IA 9메뉴 기준. 각 메뉴는 **조회 도구 → 실측 검증 → 쓰기 도구(확인 카드) → 다음 메뉴** 순.

### Phase 1 — 도구 레지스트리 + 에이전트 루프 + 첫 메뉴 「AI 자산」 (1.5~2일) ← **지금**
- `engine/agenttools.ts`: 도구 정의(이름·설명·파라미터 스키마·도메인·write 여부·실행 함수) 레지스트리
- `engine/agentloop.ts`: 오케스트레이터 루프 — 지시 → (도구 선택 JSON) → 규칙 검증 → 실행 → 결과 재주입 → 최종 답(최대 5회 반복). 협업 피드·LLM 활동 스트림에 각 도구 호출 표시
- `llm.ts`: `responseSchema` 옵션 추가(json_schema 강제 경로)
- 「AI 자산」 조회 도구: `list_assets` · `get_asset`(상세+finding) · `get_aibom` 등
- dispatcher 통합: chat 라우트에서 루프 먼저 시도, 실패·무관 시 기존 채팅 폴백(무해한 회귀)
- 검증: vitest(LLM 모킹) + 실 GPU end-to-end(Node fetch, 한글) + **벤치 케이스를 실제 루프로 재실행**

### Phase 2 — 쓰기 도구 + 되물어보기 「AI 자산 등록 / 취약점 관리」 ✅ **완료(2026-07-18)**
- ~~`ask_user` 도구~~ → **불필요**로 판명: 빠진 필수값이 결재판에서 빈 칸으로 뜨는 게 곧 되물음. 별도 도구 없이 해결.
- 쓰기 도구 3종 완료: `register_asset`(id 자동생성) · `assign_finding`(담당자·기한) · `update_finding_status`(오탐/조치완료).
- **결재판(시안 B)** 경유 확정 — 실행 전 값 검토·승인. 값 출처 배지(지시에서/조회 결과/자동생성/AI 추정/입력 필요).
- finding 지목: LLM은 today/search 결과의 assetId·심각도·유형을 **복사만**, 어느 finding인지 판단은 서버 규칙(`resolveFinding`, 0/2+건 거부). 쓰기 실패는 throw→400→"실행 실패"(완료 오인 방지).
- 근거 축에 **조회 결과(found)** 추가: `buildApproval(tool, args, instruction, toolResults)` — today가 찾아준 값을 환각으로 오판해 되묻던 문제 해소.
- 커밋: `4097234`. 테스트 `agentfinding.test.ts` 14케이스 + 전체 390 green.

### Phase 3 — 나머지 의도(intent) 확장 (진행 중, 2026-07-18 착수)
> **재설계 반영(commit adbee95)**: 도구 축이 "메뉴 미러링"에서 "사용자 의도"로 바뀌었으므로, 아래 옛
> 메뉴별 목록(`cti_status`·`list_products`…)은 그대로 쓰지 않는다. 메뉴를 가로지르는 **의도**가
> 기존 6도구로 안 풀릴 때만 새 도구를 추가한다(온톨로지·매칭이 메뉴 경계를 이미 가로지른다).

- ✅ `threats`(domain=cross) — "요즘 위협 있어? / 우리랑 관련?": CTI 피드 탐지 × 자산 신호 교집합. 기존 순수함수 `matchCtiToAssets` 재사용. (agentloop.test.ts 2케이스)
- ⏸ `report`(보고서 생성) — **보류**: 무겁다(자체 LLM 호출 + docx/pdf 파일 생성 + 긴 산출물이 루프에 재주입되면 컨텍스트 폭증). 넣는다면 짧은 요약만 반환하도록 래핑 + 실 LLM 검증 필요. 별도 세션에서.
- ⏸ `register_product`/`compliance_draft`/`list_docs` — 필요 의도가 실제로 관찰될 때 추가(현재 search/explain이 문서·제품을 이미 가로질러 조회). 선제 추가는 메뉴 미러링 회귀.
- 도메인 라우팅(1차 분류 → 해당 도구만 노출)은 도구 **15개 초과** 시점에 도입 — 현재 9개.

### Phase 4 — 자체 강화 (진행 중, 2026-07-18 데이터셋 기반 구축)
> 정조준 대상: 실측에서 드러난 `update_finding_status` 선택률(7B가 선언형 "~은 오탐이야"를 못 골라 폴백).

- ✅ **데이터셋 기반**(`orchestrator-dataset.ts`): 지시→도구결정을 학습루프가 소비하는 `{question, answer}`로 만든다. **question = 결정 프롬프트(카탈로그+규칙+지시), answer = 결정 JSON** — `buildDecisionPrompt`로 train==inference 보장. 도구 카탈로그 변경 시 재생성.
- ✅ **큐레이션 시드 31건**: 9개 도구 전부 + `final` 음성예시, **update_finding_status를 최다 가중**(선언형·명령형·동사변형). 시드는 실재 도구의 유효 호출만 가르친다(테스트가 `validateToolArgs`로 검증).
- ✅ **상시 축적(골드)**: 사람이 결재판에서 **승인한 쓰기**(지시→도구+인자)를 `data/orchestrator-gold.json`에 누적 → 빌드 시 시드와 합쳐 데이터셋에 포함. 승인=검증된 정답이라 모델 자기오류를 재강화하지 않는 안전 신호. (instruction을 결재판→approve로 전달)
- API: `GET/POST /api/learnloop/orchestrator-dataset[/build]` — 현황·빌드. 산출물 id=`orchestrator-tools`(→ 학습루프/파인튜닝이 이 id로 학습).
- ⏸ **실제 파인튜닝 실행은 사용자 트리거**: 학습 중 로컬 LLM(:8080)이 내려가고(GPU 1대), 설계상 "학습 시작=확인 필수·자동화 제외"라 자동 실행하지 않는다. 데이터셋 빌드 → 학습루프 확인 카드/`startLearnloopRun({datasetId:"orchestrator-tools"})`로 시작.
- (후순위) qwen3-30b-a3b 단독 벤치(VRAM 확보 시), 14B 실험 — QA 업그레이드 경로.

### 자동화 제외(설계상 수동 유지)
학습 시작 · 데이터 삭제/초기화 · 메일 발송 · 설정 변경 → 확인 카드 필수 또는 도구 미노출.
파일 업로드·자격증명은 `ask_user`로 요청만.

---

## 5. 리스크

| 리스크 | 대응 |
|---|---|
| 7B 모델의 멀티스텝 환각 | 한 턴 도구 1개 실행 → 결과 보고 다음 결정(ReAct 단일 스텝). 반복 상한 5회 |
| 잘못된 인자로 쓰기 실행 | 쓰기는 전부 확인 카드. 규칙 검증(존재하는 자산 id인지 등)이 LLM 출력 뒤에 항상 위치 |
| 루프 도입으로 기존 채팅 회귀 | 루프 실패·무관 → 기존 chat 폴백. 기존 294+ 테스트 유지 |
| 실DB 오염(QA P1 재발) | 루프 e2e는 격리 서버+임시 DB. reset류는 assertTestDb 가드 적용됨 |
| 지연 증가(결정 호출 추가) | 실측 결정 1회 232ms — 도구 1~2개 지시 기준 총 지연 +0.5s 내외로 미미 |
