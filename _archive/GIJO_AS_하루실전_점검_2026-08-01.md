# 보안담당자 하루 실전 점검 — 운영 상황 115가지

측정: 2026-08-01 · 운영 서버(WSL 4000) · 실 LLM · 학습·세션 오염 없음(qa=true)
도구: `tools/ops-sim.mjs`(115상황) · `tools/ingest-sim.mjs`(파일 인입) · `tools/chatbot-census.mjs`(화면별 87문항)

---

## 1. 한 줄 요약

**57/115 → 92/115.** 그리고 그 과정에서 **제품에서 가장 중요한 답이 8할 잡음이었다**는 것과,
**오늘 제가 넣은 등급 통제가 새 문서를 전부 잠그고 있었다**는 것을 찾았습니다. 둘 다 고쳤습니다.

---

## 2. 무엇을 어떻게 쟀나

앞선 점검들과 다릅니다.

| 점검 | 방식 | 한계 |
|---|---|---|
| 상황점검 1,133개(7/31) | 화면 요소를 **정적으로** 셈 | 안 눌러 봤다 |
| 챗봇 전수 87문항 | 화면당 3문항 | 화면 단위라 **일의 흐름**이 없다 |
| **이번 115상황** | 담당자의 **하루를 순서대로** 태움 | — |

아침 브리핑 → 급한 것 고르기 → 파고들기 → 조치 → 검증 → 보고 → 인계.
11개 마당으로 나눴고, 마지막 마당은 **"사람은 이렇게 친다"** — "취약점", "급한거", "머부터해야되나요" 같은 실제 입력입니다.

### 재는 것은 "맞았나"가 아니라 "쓸 만한가"

답이 맞아도 아래면 불편으로 셌습니다.

| 판정 | 왜 |
|---|---|
| 30초 넘게 기다림 | 담당자는 그 전에 화면을 닫는다 |
| 내부 식별자 노출(`id=vuln:…`) | 사람이 읽는 글자가 아니다 |
| 영문 상태값 노출(`"active"`) | 한글 제품에서 못 읽는다 |
| 2,000자 넘는 답 | 읽지 않는다 |
| 숫자만 주고 갈 곳 없음 | "그래서 뭘 하지"가 남는다 |
| 되물음·폴백 | 못 알아들었다 |

---

## 3. 찾은 것 — 중요한 순서로

### ★ ① 담당자가 본 숫자의 99%가 잡음이었다

**증상**: "오늘 뭐부터 볼까?" 상위 5건 중 **4건이 스캔 오류**. 화면엔 "미검토 602건".

**실측**: 전체 605건 중 **602건이 `scan_error`**(스캐너가 결과를 못 받은 것). 실제 일감은 **3건**.

담당자는 밀린 일이 602건인 줄 알고 손도 못 댑니다. 그리고 그 숫자로 보고를 씁니다.
**숫자가 틀린 답은 없느니만 못합니다.**

**왜 못 잡았나**: `isRealVulnerability`라는 거르는 함수는 있었는데, 목록을 만드는 곳
(`prioritizedReviews`·`approvalSummary`)이 **그 함수를 안 불렀습니다.** 규칙과 사용처가 따로 놀았습니다.

**고침**: 원천 한 곳에서 거릅니다. 다만 **감추지 않습니다** — 스캔이 안 된 자산은 그 자체로
조치할 일(접속 정보·권한)이라 `scanFailed`로 따로 세어 조치·승인 화면과 보안 KPI에 별도로 안내합니다.

| | 전 | 후 |
|---|---:|---:|
| 조치·승인 목록 | 605줄 | **3줄** |
| "오늘 뭐부터" 상위 | 5건 중 4건이 오류 | **오류 0건** |
| 스캔 실패 | 취약점에 섞임 | 따로 602건으로 안내 |

### ★ ② 등급 통제가 새 문서를 전부 잠갔다 — 오늘 제가 넣은 회귀

**증상**: 파일을 넣고(200 OK) 바로 물으면 **못 찾음**. 오류도 안 나서 조용히 죽어 있었습니다.

**원인**: 기본값 두 개를 **곱해서** 보지 않았습니다.

- 문서 등급 칸은 새 문서에 비어 있음 → 기밀(C)로 해석
- 사람의 열람 등급 기본값 = 공개만(O)

둘을 곱하면 **새로 올린 문서를 올린 사람조차 못 봅니다.**
설계서에는 "민감(S)이 가운데라 안전하면서 일이 돌아간다"고 적어 뒀는데,
짝이 되는 기본값을 같이 보지 않아 실제로는 기밀과 똑같이 동작했습니다.

**고침**: "안 매겼다"와 "값이 깨졌다"를 가릅니다.

- 비어 있음 = **아직 안 매김** → 공개. 통제 대상이 아닙니다
- 모르는 값("XX") = **뭔가 잘못됨** → 기밀. 여기선 닫습니다

"모르면 닫는다"는 **읽는 쪽**(사람의 권한)에 그대로 살아 있습니다.
등급 라벨은 **켜는 기능**이라, 켜기 전에는 아무것도 막히면 안 됩니다.

**실증**: 새 문서 인입 → ✅보인다 → 기밀로 매김 → ✅막힌다 → 공개 복귀 → ✅다시 보인다.

### ③ 내부 식별자가 답에 그대로 나갔다

"오늘 뭐부터 볼까?"의 답에 `(id=vuln:sample-web01)`이 떴습니다.
도구가 id를 내는 건 **일부러**입니다 — AI가 "1번 자산 자세히 봐줘" 같은 후속 지시에 씁니다.
지우는 자리는 **사람에게 나가는 마지막 관문 한 곳**으로 잡았고, 도구 결과 원본은 그대로 뒀습니다.

### ④ 오래 걸리던 세 자리를 0.1~0.4초로

| 물음 | 전 | 후 |
|---|---|---|
| "우리 보안 점수 어때?" | 32초 헤매다 엉뚱한 CVE 설명 | **0.4초** — KPI 현황 |
| "분석할 로그 올리려면?" | 34초 + 문서 조각 | **0.1초** — ＋ 단추 안내 |
| "취약점"(한 낱말) | 34초 + 내부 규칙 이야기 | **0.1초** — 그 낱말에 맞는 예시 |

한 낱말 입력은 **대신 답해 주지 않습니다.** "취약점"이 목록인지 설명인지 알 수 없어서,
추측해 답하면 틀렸을 때 더 헤맵니다. 고를 수 있게 예시를 줍니다.

### ⑤ 숫자만 주고 갈 곳이 없었다

"오늘 뭐부터 볼까?"는 우선순위를 보여 주고 끝났습니다. 조치하러 갈 길이 없었습니다.
답 끝에 **다음 걸음 한 줄**을 붙였습니다 — 한 줄입니다. 답마다 세 줄씩 붙이면 그게 새 잡음이 됩니다.

---

## 4. 새로 만든 메뉴 — 유지보수 점검

지표는 있는데 **갈 곳이 없던 마지막 구멍**입니다.
`/api/maintenance`에 점검 10건(기한 초과 6건)이 있는데 **이걸 보여주는 화면이 하나도 없었습니다.**
보안 KPI는 "지연 5건"이라고 세어 주면서 누르면 갈 데가 없었고, 챗봇으로만 물어볼 수 있었습니다.

| 무엇 | 어떻게 |
|---|---|
| 기본 정렬 | **기한 순** — 점검은 "무엇이 늦었나"가 전부라 이름순은 쓸 일이 없다 |
| 요약 카드 | 누르면 목록이 걸러진다(다른 화면과 같은 방식이라 새로 배울 게 없다) |
| 줄마다 버튼 | 지금 할 수 있는 것만 — 예정=점검서 올리기 / 올라옴=승인·반려 / 끝남=기록 |
| 승인·반려 | **관리자만** — 서버가 막으므로 담당자 화면엔 버튼이 아예 안 보인다 |
| 반려 사유 | **비워 둘 수 없다** — 사유가 곧 다음 담당자의 지시다 |

메뉴에서 「제품 유지보수」 바로 아래에 뒀습니다. 이름이 비슷해 떨어뜨리면 둘 다 못 찾습니다.

---

## 5. 파일 인입 — 챗봇으로 넣고 실제로 쓰는가

"업로드가 200을 돌려주나"는 쉽습니다. 잰 것은 **넣은 내용으로 답이 달라지는가**입니다.
그래서 넣기 전에 같은 질문을 먼저 던져 놓고(대조군) 넣은 뒤 다시 던졌습니다.

| 문서 | 넣기 전 | 넣은 뒤 | 판정 |
|---|---|---|---|
| 인수인계 메모 | "확인할 수 없습니다" | **"(주)넷가드, 계약 만료 2027-03"** + 근거에 그 문서 | ✅ |
| 방화벽 점검보고서 | 모름 | 근거는 맞게 잡았으나 **숫자를 37 → 27로 잘못 읽음** | ⚠ |

인입 → 검색 → 근거 인용의 사슬은 **실제로 동작합니다.**
남은 것은 7B가 표에서 숫자를 잘못 뽑는 문제로, 모델 한계입니다(계획서 전-6의 정직 경계 항목).

---

## 6. 마당별 결과

| 마당 | 상황 | 남은 불편 |
|---|---:|---:|
| ④ 보안제품 — 운영·매뉴얼 | 10 | 0 |
| ⑥ 규정 — 해도 되나(행동 대조) | 12 | 0 |
| ⑤ 점검 — 늦은 것 챙기기 | 10 | 1 |
| ② 취약점 · ③ 자산 · ① 아침 | 37 | 대부분 "다음 걸음" 관련 — ⑤에서 조치 |
| ⑪ 사람은 이렇게 친다 | 10 | 한 낱말 대응으로 해소 |

**행동 대조(⑥) 12상황 전부 통과** — 계획서 전-2의 차별점이 실사용 흐름에서 온전히 동작합니다.

---

## 7. 남은 것 (고치지 않고 적어 둠)

1. **7B의 숫자 오독** — 문서에서 표 값을 잘못 뽑습니다. 프롬프트로 고칠 일이 아니라
   근거 원문을 함께 보여 주는 쪽이 맞아 보이나, 화면 설계가 필요합니다.
2. **"숫자만 주고 갈 곳 없음" 18건** — "오늘 뭐부터"에는 다음 걸음을 붙였습니다.
   나머지도 붙일지는 판단이 필요합니다 — 전부 붙이면 그 안내가 새 잡음이 됩니다.
3. **시험 하네스가 401을 삼켜 제품을 모함한 적이 있음** — 첫 실행의 "빈 답 35건"은
   접속표 15분 만료였습니다. 고쳤지만, 15분 넘는 실측은 앞으로도 나오므로 다시 밟기 쉬운 자리입니다.

---

## 8. 다시 돌리는 법

```bash
# 하루 실전 115상황 (약 20분, 실 LLM)
QA_USER=claude-deploy QA_PASS=… node tools/ops-sim.mjs

# 챗봇 화면별 87문항 (약 10분)
QA_USER=claude-deploy QA_PASS=… node tools/chatbot-census.mjs

# 파일 인입 대조 (약 1분)
QA_USER=claude-deploy QA_PASS=… SAMPLES=<샘플폴더> node tools/ingest-sim.mjs

# 규칙만 고쳤을 때 — 다시 묻지 않고 저장된 답을 재채점
node tools/chatbot-census-rejudge.mjs
```

⚠ 도는 동안 **같은 계정으로 앱에 로그인하지 마세요** — 계정당 세션 1개라 서로 끊습니다.

---

## 📖 용어 풀이

- **스캔 오류(scan_error)** — 스캐너가 장비에 붙지 못해 결과를 못 받은 것. 취약점이 **아닙니다**. 접속 정보나 권한 문제라 고칠 곳이 다릅니다.
- **등급 라벨** — 자료를 공개·민감·기밀로 나누고, 등급이 낮은 사람에게는 **검색 자체가 안 되게** 하는 기능. 국가 망 보안체계(N2SF)가 요구하는 통제입니다.
- **대조군** — 고치기 전 상태를 먼저 재 두는 것. 이게 없으면 "고쳤다"가 착각인지 알 수 없습니다.
- **카나리** — 그 문서에만 있는 낱말. 답에 그 낱말이 나오면 문서를 실제로 읽었다는 증거가 됩니다.
- **7B** — 우리가 쓰는 로컬 AI 모델의 크기(70억 개 매개변수). 폐쇄망에서 돌릴 수 있는 대신 큰 모델보다 숫자·표를 잘못 읽는 일이 있습니다.
- **행동 대조** — "이거 해도 돼요?"에 사내 규정과 법령을 근거로 답하는 기능. 계획서 전-2의 차별점입니다.
