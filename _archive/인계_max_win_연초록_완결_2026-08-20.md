# 인계 — `max` → `win` (2026-08-20) · 라이트 연초록 **완결 회신**

## 0. 판정 — 통과 ✅

라이트 **Lite 1.2.0** 연초록 배색, **실기(Mac dmg) + 사장님 눈확인까지 완결.** 입구(로그인)부터 화면 13개까지 한 배색으로 이어진다.

## 1. 처리한 결함 3건

| # | 결함 | 발견 | 수리 |
|---|---|---|---|
| ① | 라이트 첫 화면(로그인)이 **다크** | max 실기 측정 | **win** — `?edition=lite` 배관 + `html.lite-edition` 덮개 |
| ② | 배지가 **「🏢 표준」** | max 실기 측정 | **max** — 타이밍(아래) |
| ③ | **「로그인」 단추만 주황** | **사장님 눈확인** | **max** — 덮개에 `--orange` 누락 |

### ② 배지 — `DOMContentLoaded`는 늦다
win 코드가 `DOMContentLoaded`를 기다렸는데, 본문 스크립트(355행 블록)의 배지 초기화가 **그 전에 동기로** 「🏢 표준」을 박는다(`login.html:631`은 먼저 채운 쪽이 이기는 구조). 그래서 `!b.textContent` 가드가 늘 거짓이었다. → **파서가 배지를 지난 직후 즉시** 채우도록 수리(`readyState==="loading"` 동안만 되돌아봄).

### ③ 단추 — 덮개에서 변수 하나가 빠졌다
`html.lite-edition`이 `--navy·--text·--blue·--amber·--red`는 반전했는데 **`--orange`가 빠졌고**, `login.html:146`이 `background: var(--orange)`(원래 `#dd5a1c`)다. 라이트 화면 13개는 주 단추에 `--blue(#2563EB)`를 쓰므로 거기 맞췄다.
⚠ **배색이 에디션 이름표인 이상, 입구 단추만 다른 계열이면 이름표가 흐려진다.**

## 2. 최종 실측값 (Lite 1.2.0)

```
로그인(로그인 전) : login.html?edition=lite · html.lite-edition=true · lite-green.css 적재
  --navy(반전)    : #E3EEDD      body 배경: rgb(227,238,221)   글자: rgb(22,36,26)
  배지            : 🪶 라이트     단추: 파랑 (사장님 눈확인 ✅)
셸(lite-app)      : --g-bg=#E3EEDD · --g-panel=#CFE4C4 · 사이드바 rgb(207,228,196)
화면(iframe)      : rgb(235,244,235)
서명              : Identifier=ai.gijo.as.lite · Authority=GIJO AS Lite Signing
```
다크 폴백 누수 0 · 폰트 게이트 위반 0 · max 재편(⑤연락처·⑦두뇌·⑧바깥연결·④지식창고) 온전 · 설정칸 수리(`min-width:330px`) 유지.

## 3. win에 남기는 것

1. **재발 방지**(②) — `login.html` 배지를 이제 **두 곳**(head 라이트 소비자 · 본문 초기화)이 쓴다. 배지 결정을 **한 함수**로 모으거나(에디션 우선 → 셸 모드), 「라이트로 열면 배지가 `🪶 라이트`」를 시험으로 못 박는 편이 낫다.
2. **덮개 완전성**(③) — `login/setup`이 쓰는 팔레트 변수 중 덮개에 없는 것이 또 있으면 같은 사고가 난다. `login.html`·`setup.html`의 `--*` 정의를 훑어 덮개와 대조하는 시험이 있으면 한 번에 막힌다.
3. **게시 판단은 사장님** — max 쪽 배색 작업은 끝났다. 게시하기로 하면 dmg는 `client/release-lite/GIJO AS Lite-1.2.0-arm64.dmg`.

## 4. 곁다리

- win 29커밋 중 라이트 관련은 로그인 건 하나. 문서 허브 v4·현황판·밀도 라운드는 표준 전용으로 라이트 무영향 확인.
- **LoRA 어댑터**(`48ffce5`) 회신 대기: **GPU VRAM 사양** · 운영 QA 로그 채굴.
