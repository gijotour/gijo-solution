# 고객 기계 실행환경 결정안 — 엔진·파이썬을 어떻게 할 것인가

**작성**: 2026-08-10 새벽 (Windows 세션) · **결정자**: 사장님 · **상태**: 결정 대기
**계획서 위치**: 전-5(파일럿 첫날) 선행 조건 · 후-4(에어갭)와 직결

---

## 0. 한 줄 요약

> **고객 기계에는 우리 개발 기계에 있는 것이 없다.** 어제 하루에 이 계열 결함이 **네 건** 나왔고
> (초기 계정·저장 암호화·pypdf·netmiko), 남은 두 개가 **파이썬**과 **LLM 엔진**이다.
> 개별로 하나씩 고치면 계속 나온다 — **한 번에 방침을 정하자**는 것이 이 문서다.

---

## 1. 어제 실제로 일어난 일 (근거)

| 무엇 | 증상 | 누가 찾았나 |
|---|---|---|
| 초기 계정 | 고객 기계에 `jyh/changeme`가 만들어짐. 로그인 화면은 그 계정을 안 알려줌 | Mac 첫 실행 검증 → 수리(setup.html) |
| 저장 암호화 | 켜는 방법이 **터미널 스크립트**뿐 — 고객에겐 열 터미널이 없음 | Mac → 수리(화면 버튼) |
| **pypdf** | requirements에 **적혀 있는데 설치 안 됨** → PDF 추출이 몇 달간 전멸 | Windows 실측 → 수리(venv) |
| **netmiko** | **적혀 있지도 않았음** → 장비 접속(CLI 터미널) 사망 | Windows 실측 → 수리 |
| 모델 받기 | `hf` CLI가 없어 「받기」가 error | Mac 실측 → **미해결** |
| **LLM 엔진** | 고객 기계에 llama-server가 없음 → 모델을 받아도 못 씀 | Mac 지적 → **미해결** |

공통 뿌리 한 줄: **「우리 기계에 있는 것이 고객 기계에도 있다」고 가정한 코드.**

---

## 2. 실측 숫자 (2026-08-10 운영 WSL에서 측정)

### 2-1. LLM 엔진(llama.cpp)

| 항목 | 크기 | 비고 |
|---|---|---|
| `llama-server` 본체 | **17.9 KB** | 얇은 껍데기다 |
| 엔진 본체 전체(`libggml*`·`libllama*`) | **4.0 MB** | 여기까지는 가볍다 |
| **CUDA 런타임**(`libcudart`·`libcublas`·`libcublasLt`) | **547 MB** | ⚠ 고객 기계엔 **없다** |

> **핵심**: 엔진 자체는 4MB로 아주 가볍다. 무거운 것은 **GPU 가속에 필요한 CUDA 런타임 547MB**다.
> 즉 「엔진 동봉」은 하나의 결정이 아니라 **두 개의 다른 결정**이다:
> - CPU로 도는 엔진 동봉 → **4MB. 사실상 공짜.**
> - GPU 가속까지 동봉 → **+547MB(NVIDIA 전용). 플랫폼마다 다름.**

- 현재 클라 설치본: **627 MB** → CPU 엔진을 넣어도 **+0.6%**
- macOS는 **Metal이 OS에 내장**이라 별도 런타임이 필요 없다(Mac 확인 필요)

### 2-2. 파이썬 의존 (서버가 실제로 쓰는 것)

| 모듈 | 쓰는 곳 | 대체 가능성 |
|---|---|---|
| `pypdf` | PDF 텍스트 추출 | **pdfjs(순수 JS)로 교체 가능** — Firefox가 쓰는 그것 |
| `hf` CLI | 모델 받기 | **불필요** — HTTPS 다운로드일 뿐, 직접 구현 가능 |
| `netmiko` | 장비 SSH 접속 | ssh2(JS) 대체 가능하나 장비별 방언 처리가 자산 |
| `modelscan` | 모델 파일 악성코드 검사 | **JS 대체 없음** — 이것만 파이썬 필요 |

---

## 3. 안 비교

### 안 ㉮ — 파이썬 동봉

앱에 파이썬 런타임을 통째로 싣는다.

- ✅ 코드 변경 최소
- ❌ 앱 +50~100MB, 플랫폼별 3벌 유지보수
- ❌ **파이썬 자체가 보안 제품의 공격면**이 된다(임의 코드 실행 환경을 고객 기계에 설치)
- ❌ 조달 심사에서 「왜 파이썬이 들어 있나」를 설명해야 한다

### 안 ㉯ — JS로 교체 + 파이썬은 선택 (**추천**)

| 기능 | 바꾸는 것 | 규모 |
|---|---|---|
| PDF 추출 | `pypdf` → **pdfjs-dist** | 반나절 |
| 모델 받기 | `hf` CLI → **직접 HTTPS 다운로드**(이어받기·해시검증 포함) | 반나절~1일 |
| 장비 접속 | 당분간 파이썬 유지 | — |
| 모델 검사 | 당분간 파이썬 유지 | — |

- ✅ **폐쇄망(에어갭)에서 유일하게 성립한다** — `pip install`이 불가능한 고객이 우리 주 고객이다
- ✅ 앱 크기 거의 안 늘어남
- ✅ 파이썬이 **없어도 핵심 기능(PDF·모델 받기)이 돈다.** 있으면 장비 접속·모델 검사가 추가로 열린다
- ❌ 재구현 1~2일

### 안 ㉰ — 축소(올인원에선 PDF·모델받기 포기)

- ❌ **1차 목표(리포트·매뉴얼 분석)를 훼손한다.** 탈락.

---

## 4. 추천 — 「돌아가는 최소본 + 선택 확장」

> **고객 기계는 아무것도 없어도 제품이 돌아야 한다. 있으면 더 잘 돌면 된다.**

### 4-1. 엔진 — **CPU 빌드를 동봉한다**(추천)

- 4MB로 **+0.6%**. 실질 비용이 없다
- 고객이 모델만 받으면 **바로 답이 나온다** — 지금은 받아도 못 쓴다
- GPU 가속은 **선택 확장**: NVIDIA가 있는 고객에게 별도 GPU 팩(547MB)을 안내
- ⚠ CPU 추론은 느리다(14B는 사실상 불가) — 그래서 **동봉 엔진 기본 모델은 작은 것**으로 안내해야 한다.
  「일단 돌아간다」와 「빠르다」를 화면에서 정직하게 구분해 보여줄 것

### 4-2. 파이썬 — **안 ㉯(JS 교체)**, 순서는 Mac 의견대로

Mac 지적이 맞다: **엔진이 없으면 파이썬을 다 걷어내도 고객은 LLM을 못 쓴다.**

```
1순위  엔진 CPU 빌드 동봉        ← 이게 되어야 제품이 성립
2순위  hf CLI 제거(직접 다운로드)  ← 모델 받기. 프록시 건과 함께
3순위  pypdf → pdfjs              ← PDF 추출
4순위  netmiko·modelscan          ← 파이썬 있으면 열리는 선택 기능으로 정직하게 표시
```

### 4-3. 공통 원칙 — **없으면 없다고 말한다**

기능이 파이썬·GPU에 기대면, 없을 때 **조용히 죽지 말고** 화면에서 이렇게 말해야 한다:

> 「이 기능은 ○○가 필요합니다 — 지금 이 기계에는 없습니다. [설치 안내]」

어제 네 건이 전부 **조용히 죽어 있었다.** 화면엔 아무 표시도 없었다.
이미 만든 `scripts/check-python-deps.mjs`를 **설정 화면에서도 보이게** 하는 것이 최소 구현이다.

---

## 5. 결정해 주실 것

| # | 질문 | 제 추천 |
|---|---|---|
| ㉠ | CPU 엔진을 앱에 동봉할까? (+4MB) | **예** |
| ㉡ | GPU 팩(547MB)은 별도 내려받기로? | **예** (기본 동봉은 과함) |
| ㉢ | 파이썬 의존을 JS로 교체할까? | **예** — 폐쇄망에서 유일하게 성립 |
| ㉣ | 순서는 엔진 → 모델받기 → PDF? | **예** (Mac 의견) |
| ㉤ | 파이썬 필요 기능은 「선택 기능」으로 표시? | **예** |

⚠ **이 문서는 판단 근거를 모은 것이지 구현이 아니다.** ㉠~㉤이 정해지면 그때 시안·구현으로 간다.
⚠ **아직 재보지 않은 것**: macOS Metal 빌드의 동봉 크기(Mac 확인 필요) · CPU 추론 실제 속도(모델 크기별).
  이 둘은 ㉠ 결정 전에 재는 편이 낫다면 먼저 재겠다.
