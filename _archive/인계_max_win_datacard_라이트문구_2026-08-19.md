# 인계 — `max` → `win` (2026-08-19) · 데이터 카드의 라이트 문구

받았고 동기화했다(데이터 카드 커밋 `a38c547`~`fe435bc`). **라이트 안 깨진다** — 분기가 `output`(텍스트)와 `dataCard`를 둘 다 리턴해서, 카드 못 그리는 라이트도 KPI 텍스트는 받는다. 현재 배포 빌드(라이트 1.1.11 = `2dcc8d1`)는 데이터 카드 **이전**이라 지금 영향 0.

## 사장님 결정 (2026-08-19)

**라이트는 (A) 그대로 둔다** — 다음 라이트 재빌드까지는 텍스트만. **카드를 라이트에 배선할지는 통합 제품 설계 때** 함께 정한다(지금 라이트 단독으로 손대면 통합 때 뒤집힌다).

## win에게 — datacard 2차 확장 때 부탁 ⚠

`hardeningStatusAnswer()`의 `output` 텍스트가 **카드가 그려진다는 전제**로 쓰였다:

> …N곳을 **카드로 보였습니다** — 전체는 **검증 화면**에서.

라이트에선 둘 다 어긋난다:
- **카드 미노출** — 라이트 챗(`lite-chat.html`)은 `chatparts.js` 미배선이라 `dataCard`를 못 그린다.
- **"검증 화면" 없음** — 라이트 메뉴엔 검증 전용 화면이 없다(대시보드·작업내역·로그분석·문서작성·물어보기·나만의자료·연락처·설정).

그리고 트리거는 **라이트에서도 걸린다** — `isHardeningStatusAsk`는 (검증|하드닝…)+(현황|상태…)면 참이고, 라이트 13도구에 `run_hardening_scan`이 있어 "검증 현황 보여줘"가 실제로 온다.

**부탁**: 2차 확장 때 **「카드 못 그리는 에디션」**을 한 갈래로 봐 달라 — 카드가 없을 땐 `output`에 표 요약(장비별 준수율)을 텍스트로 접어 넣고 "카드로 보였습니다/검증 화면" 문구는 빼는 식. 지금 datacard.ts를 라이트용으로 분기할지, 통합 제품에서 라이트에도 chatparts를 배선할지는 win·통합 설계에 맡긴다(max는 라이트 챗 렌더 쪽을 맡을 준비 됨).

> 참고: 라이트 재빌드 레시피·키체인·통합제품 라이트 보류는 오늘 앞선 인계(`인계_max_win_라이트1.1.11_키체인_통합제품_2026-08-19.md`)에 있다.
