# GIJO AS 챗봇 메뉴별 라우팅 정확도 리포트

- 측정 시각: 2026-07-20 22:43:44
- 대상 서버: http://localhost:4000
- **총계: 31/35 (89%)** — 비슷한 표현 변형이 같은 기능으로 라우팅된 비율

## 메뉴별 정확도

| 상태 | 메뉴 | 정확도 |
|---|---|---|
| ⚠️ | 취약점 | 3/5 (60%) |
| ✅ | 자산 목록 | 5/5 (100%) |
| ✅ | AI-BOM 구성 | 3/3 (100%) |
| ✅ | 조치·승인 | 2/2 (100%) |
| ✅ | 보안제품 | 3/3 (100%) |
| ✅ | 유지보수 | 3/3 (100%) |
| ✅ | 컴플라이언스 | 3/3 (100%) |
| ⚠️ | 리포트 | 1/2 (50%) |
| ✅ | 기억·학습 | 1/1 (100%) |
| ✅ | AI 견고성 | 2/2 (100%) |
| ✅ | 위협 인텔리전스 | 1/1 (100%) |
| ⚠️ | 대시보드(공용) | 4/5 (80%) |

## 오라우팅 — 개선 후보 (4건) → **전부 보강 완료**

아래 4건은 1회 측정으로는 진짜 오라우팅과 7B 라우팅 변동성을 구분할 수 없어, **각 케이스를 5회
반복 측정**(`menu-edgecheck.mjs`)해 판별한 뒤 보강했습니다.

| 메뉴 | 명령 | 기대 | 보강 전 | **보강 후(5회)** |
|---|---|---|---|---|
| 취약점 | "지금 열린 취약점 뭐 있어?" | `finding_status` | `today/chat` | ✅ **5/5** |
| 취약점 | "가장 급한 취약점 담당자랑 기한 배정해줘" | `assign_finding` | `today/chat` | ✅ **5/5** |
| AI-BOM | "자산별 구성요소 정리해줘" | `aibom_status` | `generate_sbom` | ✅ **5/5** |
| 리포트 | "주간 보안 리포트 작성해줘" | `report` | `briefing/chat` | ✅ **4/5** |
| 대시보드 | "오늘 브리핑 해줘" | `briefing` | `-/chat` | ✅ **5/5** |

### 보강 방법 — 결정적 라우팅 (7B 흔들림은 코드로)

교훈: 프롬프트·설명 규칙만으로는 7B 분류기의 실행별 변동을 못 잡는다. 명백한 문구는 **결정적
후처리**로 못박았다.

1. **설명 정교화**(`agenttools.ts`) — `finding_status`(열린/미조치 목록) vs `today`(우선순위),
   `aibom_status`(구성요소 조회) vs `generate_sbom`(파일 생성) 경계를 도구 설명에서 분리 → 앞 2건 회복.
2. **배정 강제 제외**(`agentloop.ts`) — 기존 `"가장 급한 취약점"→today` 강제 규칙이 배정 명령까지
   흡수했다. 지시문이 배정/지정 의도면(`isAssign`) `today` 강제를 건너뛰어 `assign_finding`으로 보냄.
3. **브리핑 강제 규칙**(`agentloop.ts`) — 대시보드(전 도구 노출)에서 잡담으로 새던 "브리핑"을
   `briefing` FORCED_INTENT로 못박음(단 "리포트/보고서" 문구는 제외).
4. **분류기 chat 흘림 복구**(`intent.ts`) — 분류기가 chat으로 흘렸는데 명시적 동작 동사(리포트 등)가
   있으면 결정적 규칙으로 되살림 → "주간 리포트"가 report↔chat 오가던 것을 1/3 → 4/5로.

> ⚠️ 측정 신뢰성: 병렬 스케줄 작업이 서버를 재배포·재시작하면 워밍 중 모델이 도구 선택을 흔들어
> 측정이 오염된다(실측 관찰). 최종 5회 측정은 **측정 전후 서비스 기동시각이 동일함을 확인**한
> 무재시작 구간에서 수행했다. 리포트 1건 흔들림은 분류기(routeIntent) 잔여 변동.

---
*카탈로그: server/tools/menu-scenarios.mjs · 전체 재측정: node tools/menu-dispatch-accuracy.mjs · 엣지 5회: node tools/menu-edgecheck.mjs*
