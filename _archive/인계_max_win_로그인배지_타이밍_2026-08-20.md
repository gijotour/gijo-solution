# 인계 — `max` → `win` (2026-08-20) · 로그인 라이트 배지 **타이밍 수리**

## 0. 판정

win의 `?edition=lite` 수리 — **배색은 실기 통과 ✅ / 배지는 안 먹었다 ❌ → max가 수리함.**

## 1. 배색 통과 (Mac 실기, Lite 1.2.0)

로그인 화면(로그인 **전**) CDP 실측:

```
주소            : login.html?edition=lite     ← 배관 작동
html.lite-edition: true
lite-green.css  : 적재됨
--navy(반전)    : #E3EEDD
body 배경       : rgb(227, 238, 221)   ← 연초록 (이전: 다크)
body 글자       : rgb(22, 36, 26)
```

max가 신고한 「라이트 첫 화면이 다크 → 로그인하면 연초록으로 바뀜」은 **사라졌다.** `html.lite-edition` 스코프 설계도 의도대로 — 라이트 화면 13개엔 무영향.

## 2. ❌ 배지는 여전히 「🏢 표준」이었다 — 원인은 **타이밍**

실측: 배색이 다 맞는데 배지만 `🏢 표준`.

**원인**: win 코드가 `DOMContentLoaded`를 기다린다.

```js
document.addEventListener("DOMContentLoaded", function () {
  var b = document.getElementById("modeBadge");
  if (b && !b.textContent) b.textContent = "🪶 라이트";   // ← 늘 거짓
});
```

본문 스크립트(355행 블록)의 배지 초기화가 **DOMContentLoaded보다 먼저 동기로** 돌아 「🏢 표준」을 박는다(`login.html:631` — 그쪽엔 `|| modeBadge.textContent` 가드가 있어 **먼저 채운 쪽이 이긴다**). 그래서 win의 `!b.textContent` 가드가 항상 거짓이 되어 **아무 일도 안 일어났다.**

`shellModeGet()`이 라이트에서 `{}`를 돌려주는 것도 확인했다(`쓸수있나` undefined → `m="standard"` 기본값 유지). 즉 라이트에서 「표준」이 박히는 것은 이 경로의 **정상 동작**이고, 이기려면 **더 일찍** 채우는 수밖에 없다.

**수리**(max, `login.html` head 블록): 기다리지 않고 **파서가 배지를 지난 직후 즉시** 채운다.

```js
(function 배지를라이트로() {
  var b = document.getElementById("modeBadge");
  if (b) { if (!b.textContent) b.textContent = "🪶 라이트"; return; }
  if (document.readyState === "loading") { setTimeout(배지를라이트로, 0); }
})();
```

**검증**:
- 로직 — 실행 중인 앱에서 빈 배지에 적용 → `🪶 라이트` ✅
- 배포본 — 빌드된 `app.asar` 안 `login.html`에 수리 실림 확인(옛 `DOMContentLoaded` 패턴 **0건**) ✅
- ⚠ 재빌드 뒤 **CDP가 안 붙어**(이 Mac 간헐 현상) 화면 눈확인은 사장님께 부탁드렸다. 정적 확증까지가 max 몫.

## 3. 재발 방지 제안 (win 판단)

`login.html` 배지는 이제 **두 곳**(head 라이트 소비자 · 본문 초기화)이 같은 자리를 쓴다. 감시가 없으면 다음에 또 어긋난다:
- 「라이트로 열면 배지가 `🪶 라이트`」를 시험으로 못 박거나,
- 배지 결정을 **한 함수**로 모으고(에디션 우선 → 셸 모드) 두 곳이 그것만 부르게.

## 4. 곁다리

- win 29커밋 중 라이트 관련은 이 건 하나. 나머지(문서 허브 v4·현황판·밀도 ⓔⓕ)는 표준 전용 — max 재편 가드 13곳·설정칸 수리(`min-width:330px`) 온전 ✅
- LoRA 어댑터 건(`48ffce5`) 회신 대기 중: **GPU VRAM 사양** · 운영 QA 로그 채굴.
