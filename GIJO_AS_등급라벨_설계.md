# GIJO AS — 등급 라벨(기밀·민감·공개) 접근 통제 설계

*작성 2026-07-31 · 계획서 **중-5 파생** · N2SF 대응 문서 7-2절이 짚은 최대 공백*

> **아직 구현하지 않았습니다.** 화면이 바뀌는 일이라 시안(mockups/grade-label/)을 먼저 보시고
> 결정하신 뒤 만듭니다. 이 문서는 **무엇을 만들지와 무엇이 먼저 필요한지**를 적습니다.

---

## 1. 왜 필요한가

N2SF는 업무정보를 **기밀(C)·민감(S)·공개(O)** 로 나누고 등급마다 다른 통제를 걸도록 합니다. 지금 GIJO AS는 **등급이라는 개념 자체가 없습니다.** 그래서:

- 기밀 자료와 공개 자료가 **같은 지식베이스에 섞이면** 검색이 등급을 넘나듭니다.
- 공공 조달 검토자가 "등급별 통제를 어떻게 거나"라고 물으면 **답할 것이 없습니다.**
- 지금 권고는 "한 대에 한 등급만 쓰세요"인데, 그건 **기능이 아니라 회피**입니다.

## 2. ★ 먼저 알아야 할 제약 — 지금 구조로는 못 한다

**RAG 검색 경로에 "누가 묻는지"가 흐르지 않습니다.**

```
queryMemory(question, topK, agentId, screen)     ← 사용자 신원 없음
chat(agentId, message, remember, screen, …)      ← 사용자 신원 없음
```

디스패치 라우트는 `req.user`를 알지만, **거기서 멈춥니다.** 그 아래로 내려가지 않습니다.

등급 통제의 핵심은 조사한 표준들이 입을 모아 말하는 **"검색 전에 막아라"** 입니다 — 답을 만든 뒤 거르는 게 아니라, **애초에 그 조각이 문맥에 들어가지 않게** 해야 합니다. 그러려면 검색하는 자리에서 "이 사람이 어디까지 볼 수 있나"를 알아야 합니다.

**즉 배관 공사가 선행됩니다.** 이걸 모르고 "등급 필드 추가"만 하면, 라벨은 붙었는데 아무것도 막지 못하는 **가짜 통제**가 됩니다.

## 3. 단계

### 1단계 — 배관: 사용자 신원을 검색까지 흘린다
- `chat()`·`queryMemory*()`에 `viewer: { userId, clearance }` 를 넘긴다.
- 넘기지 않은 호출은 **가장 낮은 권한으로 취급**한다(빠뜨리면 열리는 게 아니라 닫히게).
- ⚠ 부르는 곳이 많다. 기본값을 "공개만"으로 두어 **빠뜨림이 사고가 되지 않게** 한다.

### 2단계 — 라벨: 무엇에 등급을 다나
| 대상 | 등급 출처 |
|---|---|
| 지식 문서(RAG) | 업로드할 때 지정 · 등급 미지정이면 **공개(O)** |
| 자산·보안제품 | 등록할 때 지정 · 등급 미지정이면 **공개(O)** |
| 사용자 | 계정마다 **열람 가능 등급**(설정 > 관리자) · 기본 **공개만(O)** |

> ### ⚠ 이 표는 한 번 틀렸습니다 — 2026-08-01 실사고
>
> 처음엔 자료 기본값을 **민감(S)** 으로 적고, 이유를 "모르는 자료를 공개로 두면 새어 나간다.
> 둘 다 안전한 쪽으로 기울인다"라고 썼습니다. **그 판단이 틀렸습니다.**
>
> 사용자 기본이 **공개만(O)** 인데 자료 기본이 **민감(S)** 이면, 둘을 곱했을 때
> **새로 올린 문서를 올린 사람조차 못 봅니다.** 실측: 파일을 넣고(200 OK) 바로 물었더니
> "못 찾음" — 제품의 핵심 기능(문서를 올려 AI가 쓰게)이 오류 하나 없이 조용히 죽어 있었습니다.
> "가운데라 안전하면서 일이 돌아간다"고 적어 놓고, 실제로는 기밀과 똑같이 동작하고 있었습니다.
>
> **배운 것: 기본값은 하나만 보고 정하면 안 됩니다. 짝이 되는 기본값과 곱해서 봐야 합니다.**
>
> 고친 규칙 — "안 매겼다"와 "값이 깨졌다"를 가릅니다:
>
> | 등급 칸 상태 | 어떻게 보나 | 왜 |
> |---|---|---|
> | 비어 있음(null·공백) | **공개(O)** | 아직 안 매긴 것 — 통제 대상이 아니다 |
> | 아는 값(O·S·C) | 그대로 | — |
> | 모르는 값("XX") | **기밀(C)** | 뭔가 잘못됐다 — 여기선 닫는다 |
>
> "모르면 닫는다"는 **읽는 쪽**(사용자 열람 등급)에 그대로 살아 있습니다.
> 등급 라벨은 **켜는 기능**이라, 기관이 등급 운영을 시작하기 전에는 아무것도 막히면 안 됩니다.

### 3단계 — 강제: 검색 전에 막고, 막은 것을 기록한다
- 검색 질의에 `grade <= viewer.clearance` 조건을 **DB 단계에서** 건다(가져온 뒤 거르지 않는다).
- 막힌 조각이 있으면 **감사 기록에 남긴다** — 무엇을 몇 건 막았는지. 근거 없이 막으면 나중에 증명할 수 없습니다.
- 답에 "이 질문에 관련된 상위 등급 자료가 N건 있으나 열람 권한이 없습니다"를 **정직하게 표시**한다.
  (있는 걸 없다고 하면 담당자가 잘못된 판단을 합니다.)

### 4단계 — 화면
- 문서·자산 목록에 **등급 배지**(C 빨강 / S 주황 / O 회색)
- 업로드·등록 화면에 등급 고르기
- 설정 > 관리자에 **계정별 열람 등급**
- 대화창 답변에 "가려진 자료 N건" 표시

## 4. 안 하기로 한 것

- **등급별 벡터 저장소 분리** — 표준 문헌이 권하는 방식이지만(네임스페이스 분리), 우리 LanceDB 운용에서 저장소를 쪼개면 재인입·백업·검색 경합이 한꺼번에 복잡해집니다. **1차는 행 단위 필터**로 하고, 기밀(C)을 쓰는 기관이 실제로 생기면 그때 분리를 검토합니다.
- **자동 등급 판정(AI가 등급을 매기기)** — 등급은 기관이 정합니다. AI가 추천할 수는 있어도 **정하면 안 됩니다.**

## 5. 검증 계획 (만들 때 함께)

- 낮은 등급 계정으로 상위 등급 문서가 **검색 결과·답변 근거 어디에도 안 나오는지**
- 막힌 건이 **감사 기록에 남는지**
- `viewer`를 빠뜨린 호출이 **열리지 않고 닫히는지**(기본값 시험)
- 기존 기능 회귀 — 등급을 안 쓰는 기관에서는 지금과 똑같이 동작하는지

---

## 📖 용어 풀이

- **등급 라벨** — 자료마다 붙이는 기밀/민감/공개 표시. 누가 볼 수 있는지를 정하는 근거입니다.
- **열람 등급(clearance)** — 그 사람이 볼 수 있는 최고 등급. 민감까지면 기밀 자료는 검색에도 안 걸립니다.
- **검색 전 차단(pre-retrieval)** — 자료를 가져온 뒤 지우는 게 아니라, 애초에 안 가져오는 것. 가져온 뒤 지우면 AI가 이미 본 상태라 흔적이 답에 남을 수 있습니다.
- **배관 공사** — 기능 자체보다, 그 기능이 쓸 정보(여기선 "누가 묻는지")를 필요한 곳까지 전달하는 밑작업.

## 출처

- [RAG Security — OWASP Cheat Sheet Series](https://cheatsheetseries.owasp.org/cheatsheets/RAG_Security_Cheat_Sheet.html)
- [RAG Pipeline Security Best Practices for 2026 — Kiteworks](https://www.kiteworks.com/cybersecurity-risk-management/rag-pipeline-security-best-practices/)
- [AI Data Access Compliance: RAG Retrieval Logging Risks — Kiteworks](https://www.kiteworks.com/regulatory-compliance/rag-compliance-data-access-logging/)
- [Secure RAG: Authorisation-Aware Retrieval and Row-Level Security](https://photokheecher.medium.com/secure-rag-authorisation-aware-retrieval-and-row-level-security-c6542500ec21)
