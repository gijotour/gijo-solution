# GIJO AS 라이트 — 설치 안내서 (Mac)

2026-08-13 · 이 안내서의 시간·용량은 전부 실측입니다.

---

## 1. 준비물

| | |
|---|---|
| 설치 파일 | `GIJO AS Lite-5.15.1-arm64.dmg` — **약 1.3GB** (임베딩만 포함 · **챗 모델은 따로 등록**) |
| 챗 모델 파일 | GIJO가 드리는 `.gguf` 파일 (또는 인터넷에서 받기 — 4-2절) |
| 기계 | Apple Silicon Mac · 메모리 16GB 이상 권장 (Windows판은 GPU 10GB 이상 — 기능 등급 가이드 참조) |
| 인터넷 | 챗 모델을 **파일로 등록하면 필요 없습니다** — 폐쇄망 그대로 돕니다 (법령 원문 조회만 예외 — 6절) |

앱(1.3GB)과 챗 모델 파일을 **USB로 함께 전달**하는 것을 기준으로 합니다.

## 2. 설치

1. dmg를 열고 `GIJO AS Lite`를 응용 프로그램 폴더로 끌어 넣습니다.
2. 처음 실행은 **오래 걸립니다 — 기다려 주세요.**
   - macOS가 6.7GB 앱 전체의 서명을 검사합니다. 실측 **첫 화면까지 약 40초**,
     기계에 따라 2~3분까지 갈 수 있습니다. **아무 창도 안 뜨는 동안이 정상입니다.**
   - 두 번째 실행부터는 몇 초면 뜹니다.

## 3. 처음 설정 — 관리자 계정 만들기

첫 화면에서 **이 PC에 처음 설치**를 고르고 아이디·비밀번호를 정합니다.

- 비밀번호는 **12자 이상을 권합니다** (제품 최소는 8자).
- ⚠ **이 비밀번호는 이 기계에만 저장되고, 공급사도 알 수 없습니다.
  잊으면 되돌릴 방법이 없어 데이터를 지우고 다시 설치해야 합니다.** 적어서 안전한 곳에 보관하세요.
- 회사에 이미 GIJO 서버가 있다면 계정을 만들지 말고 「이미 GIJO 서버가 있어요」로 들어가세요.

## 4. 챗 모델 등록 (첫날 관문)

라이트는 **검색·임베딩은 바로 되지만**(임베딩 모델 포함), **대화·정리본은 챗 모델을 등록해야** 됩니다.
⚙ **설정 → 🧠 모델·엔진 → 📥 챗 모델 등록**에서 두 길 중 하나로:

**4-1. 파일로 (폐쇄망·권장)**
1. GIJO가 드린 `.gguf` 파일을 준비합니다(USB).
2. ⚙ 설정 → 챗 모델 등록에 적힌 **모델 폴더**에 그 파일을 넣습니다.
   (폴더는 대략 `~/Library/Application Support/GIJO AS Lite/models/<이름>/<이름>.gguf` 꼴입니다.)
3. **「다시 확인」**을 누르면 목록에 나타납니다 → **「올리기」**로 켭니다.
- 인터넷이 없어도 됩니다.

**4-2. 받아서 (인터넷)**
- ⚙ 설정 → 챗 모델 등록 → **「받기」**로 공개 모델을 내려받습니다. 수 GB라 몇 분 걸립니다.
- ⚠ 라이트는 **10GB급**이라 목록에서 **작은 모델(7~8B)**을 고르세요(큰 모델은 「라이트엔 큼」 표시).

등록·「올리기」 후 대시보드 「지금 돌고 있나」가 **돌고 있음**으로 바뀌면 대화 준비 완료(예열 30~60초).

설치 직후 이미 들어 있는 것: 제품 자료 20건(검색됨) · 샘플 할 일 3건(「샘플-웹서버」 표시 — 지워도 됩니다).

## 5. 꼭 알아 둘 것 둘

**① 정리본을 여러 개 잇달아 만들면 막힐 수 있습니다.**
정리본을 연속으로 만들면 이 PC의 대화 메모리가 차서 「정리본을 만들지 못했습니다」가 날 수 있습니다.
그럴 땐 **앱을 다시 열면** 됩니다(메모리가 초기화됩니다). 근본 개선은 진행 중입니다.
(문서를 넣어 두는 것·검색·대화는 그대로 됩니다.)

**② 지금은 글자 파일만 넣을 수 있습니다.**
매뉴얼·자료 넣기는 `.md` `.txt` `.log` `.csv`만 됩니다. PDF·워드는 추출기가 준비되면 열립니다 —
지금 넣으면 못 읽는 글이 저장되기 때문에 화면에서 막아 두었습니다.

## 6. 법령 원문 조회 켜기 (선택 · 유일하게 인터넷을 쓰는 기능)

라이트는 규제 근거 문서가 번들로 들어 있어 **끄고도 법령 질문에 답합니다.**
국가법령정보센터 **원문**까지 대조하려면:

1. law.go.kr에서 개인 인증키(OC)를 신청합니다 (무료).
2. ⚙ 설정 → 법령·판례 조회에 키와 신청 도메인을 넣습니다.
3. 이 기능만 바깥으로 나가고, 나머지는 전부 이 PC 안에 있습니다.

## 7. 데이터는 어디에 있나

| 무엇 | 어디 |
|---|---|
| 대화·문서·설정 | `~/Library/Application Support/GIJO AS Lite/` |
| 자동 백업 | 같은 자리 `backups/` — 24시간마다, 7개 보관 |
| 앱을 지우면 | 위 폴더는 남습니다. 완전히 지우려면 폴더도 지우세요 |

앱을 새 버전으로 바꿔도 데이터는 그대로입니다(데이터가 앱 밖에 있습니다).

## 8. 문제가 생기면

| 증상 | 조치 |
|---|---|
| 처음 실행에 아무것도 안 뜸 | 2~3분 기다리세요(서명 검사). 그래도 안 뜨면 재실행 |
| 「모델이 멈춰 있습니다」 | 예열 30~60초 대기 → ⚙ 설정에서 모델 「올리기」 |
| 긴 문서의 정리본이 「문맥 초과」로 막힘 | 문서를 짧게 나눠 시도 (5절 ①) |
| 로그인이 자꾸 풀림 | 같은 계정으로 다른 곳에서 접속하면 기존 세션이 끊깁니다(계정당 1세션) |
| 넣은 문서가 검색에 안 나옴 | 몇 초 뒤 다시 — 색인이 막 끝났을 수 있습니다 |

---

# Windows 설치 (2026-08-13 · win 추가)

> 위 절은 Mac(dmg) 기준입니다. Windows는 이 절 하나로 끝납니다 — **받자마자 혼자 돕니다.**

## 준비물
- Windows 10/11 64비트 · **NVIDIA GPU(전용 메모리 10GB급)** · NVIDIA 그래픽 드라이버(최신 권장)
- 그 외 설치할 것 **없음** — CUDA·파이썬·런타임 전부 설치본에 들어 있습니다.

## 받은 파일 확인 (USB로 받으셨을 때)
| | |
|---|---|
| 파일 | `GIJO AS Lite Setup 1.1.0.exe` (1,529MB) |
| SHA256 | `3529bd3a8c8ba31505a31b9c5255d956a688e06e26099e1dbc7bbbf1e1723ad8` |

명령 프롬프트에서 대조: `certutil -hashfile "GIJO AS Lite Setup 1.1.0.exe" SHA256`
→ 위 값과 **한 글자라도 다르면 설치하지 말고** 전달자에게 알려 주세요(전송 중 손상).

## 설치 → 첫 실행 (5분)
1. exe 실행 → 안내대로 설치(관리자 권한 불필요, 사용자 폴더 설치).
2. 첫 실행에 **관리자 계정을 직접 만듭니다**(아이디·비밀번호 8자+) — 기본 계정은 없습니다.
3. 로그인하면 라이트 화면 10개가 뜹니다. 서버는 이 PC 안(포트 7445)에서 자동으로 돕니다.
4. **챗 모델 등록**(대화 기능에 필요 — 검색·문서는 등록 전에도 됩니다):
   - 폐쇄망: GIJO가 드린 `.gguf` 파일을 **아래 모델 폴더**에 넣고 ⚙설정 > 챗 모델 등록에서 「다시 확인」
     - Windows: 탐색기 주소창에 `%APPDATA%\GIJO AS Lite\models` 를 붙여넣으면 바로 열립니다
       (실제 경로: `C:\Users\사용자이름\AppData\Roaming\GIJO AS Lite\models`)
     - 넣는 방법: 그 폴더 안에 **모델 이름 폴더를 만들고** 그 안에 `.gguf`를 둡니다 —
       예: `…\models\gijo-main-orchestrator\gijo-main-orchestrator.gguf`
   - 인터넷: ⚙설정 > 챗 모델 등록 > 「받아서」 목록에서 선택(GIJO 제공 모델은 HF 토큰 + 접근 승인 필요)

## 안 될 때
- **화면은 뜨는데 대화가 안 됨** → 챗 모델 미등록 상태입니다(위 4번). 정상 설계입니다.
- **GPU를 못 잡음** → NVIDIA 드라이버를 최신으로 올리고 재시작하세요(CUDA 툴킷 설치는 필요 없습니다).
- 확인 주소: 브라우저에서 `http://127.0.0.1:7445/api/health` 가 `{"ok":true…}`면 서버는 정상입니다.

---

# 1.1.0에서 새로 생긴 것 (2026-08-14)

## ① 📝 문서 작성 — 무료로 함께 드리는 편집기

위쪽 제목줄의 **「📝 문서 작성」**을 누르면 **GIJO Smart MD Studio**가 별도 창으로 열립니다.
사용료 없이 함께 드리는 도구입니다.

- **화면을 캡처해 Ctrl+V로 붙여 넣으면 그림이 문서 안에 들어갑니다** — 점검 증적을 남길 때 씁니다.
- 마크다운(.md)·HTML·PDF·워드로 내보냅니다. 인터넷이 필요 없고, 만든 글은 이 PC를 벗어나지 않습니다.
- **쓰는 것은 Smart MD, 읽고 답하는 것은 GIJO입니다** — 저장한 .md를 「나만의 기억」 폴더에 두고
  「다시 읽기」를 누르면 AI가 그 내용을 근거로 답합니다.

## ② 🖧 원격 GPU (VPN) — 이 PC에 GPU가 없어도 큰 모델을 쓴다

⚙ 설정 → **「📥 챗 모델 등록」** 맨 위에서 **어디서 돌릴지**를 먼저 고릅니다:

| 고르는 것 | 무엇인가 |
|---|---|
| 💻 **이 PC에 모델 두기** | 지금까지의 방식. 모델 파일을 이 PC에 두고 내 GPU로 돌립니다(NVIDIA 10GB급 필요) |
| 🖧 **원격 GPU(VPN)에 연결** | **이 PC에 GPU가 없어도 됩니다.** 사내·집의 큰 GPU를 VPN으로 빌려 더 큰 모델(14B·32B)을 씁니다 |

원격을 고르면 ⚙ 설정 → **「🖧 원격 GPU (VPN)」** 칸으로 안내합니다. 거기서 주소를 넣고
**[연결 테스트]** → **[켜기]** 하면 됩니다.

**빌려주는 쪽에서 할 일**: 그 기계의 GIJO(표준/프로)에서 ⚙ 설정 → 연동 → **「🎁 이 PC를 원격 GPU로
내주기」**를 켜면 **상대에게 알려 줄 주소가 화면에 만들어져 나옵니다**. 그것을 복사해 넣으세요.

⚠ 다만 **화면이 「이 주소는 상대가 쓸 수 없습니다」라고 하면 그대로 쓰지 마세요.** 그 기계에서
GIJO에 `localhost`로 접속하고 있으면 주소가 `127.0.0.1`로 만들어지는데, 그건 **받는 사람의 PC
자신**을 가리키는 주소라 연결되지 않습니다. 그때는 앞부분을 그 기계의 **VPN 주소**(예: `10.8.0.12`)로
바꿔서 알려 주세요. 화면이 그 경우를 알려 줍니다.

⚠ 꼭 알아 두실 것
- **사설 대역 주소만 됩니다**(10.x·172.16~31.x·192.168.x·100.64~127.x). 공인 IP나 도메인 이름은 받지 않습니다.
  ⚠ 정직하게 짚어 두면, 이 판정은 「VPN 안」과 **정확히 같지는 않습니다** — 사무실 LAN(192.168.x)에
  있는 기계도 이 대역에 들어갑니다. 즉 같은 사내망의 다른 PC도 조건상 붙을 수 있습니다.
  그래서 **빌려주는 쪽은 안 쓸 때 꺼 두는 것**이 실질적인 방어입니다.
- **켜면 질문이 그 기계로 전송됩니다.** 고객사 소유 기계 안이지만 「이 PC 안」은 아닙니다 —
  회사 정책상 괜찮은지 확인하고 켜세요. 끄면 곧바로 이 PC의 모델로 돌아옵니다.
- 빌려주는 쪽은 **VPN 안에서 인증 없이** 쓰이므로, 안 쓸 때는 꺼 두시기를 권합니다.

자세한 연결 순서와 안 될 때의 확인 목록은 별도 문서 **`GIJO_AS_원격GPU_사용안내.md`**에 있습니다.

## ③ 그 밖에

- 첫 화면(로그인)에서 **문서 작성**을 로그인 없이 열 수 있습니다.
- 대화창에 **「문서 작성 어디서 해?」**, **「원격 GPU」**라고 물으면 챗봇이 설명해 줍니다.
