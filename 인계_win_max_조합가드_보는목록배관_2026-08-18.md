# 인계 — `win` → `max` (2026-08-18)

> 두 커밋(`f83f4746` 커밋A, `a32f0146` 커밋B)이 **공용 파일과 라이트 파일을 함께** 건드렸다.
> `max`가 라이트를 만지기 전에 반드시 `hub`에서 최신을 받을 것(`/GIJOAS동기화`).
>
> **게시 안 함 · 운영 배포 안 함** — 사장님 허락 전이다. 검토관 결과 반영 뒤에 판단한다.

---

## 0. 먼저 — 왜 `win`이 라이트 파일을 건드렸나

CLAUDE.md 「뿌리가 공용이면 `win`이 고친다」에 따랐다. 한글 조합(IME) 결함은 **화면 하나의
문제가 아니라 저장소 전체에 흩어진 같은 패턴**이었다(25곳). 라이트만 빼면 라이트에만 남는
결함이 되고, 그건 이 저장소가 반복해 겪은 「같은 것을 여러 곳에 적으면 어긋난다」 그대로다.

**건드린 라이트 파일 7개** — `lite-chat.html` · `lite-law.html` · `lite-loganalysis.html` ·
`lite-manuals.html` · `lite-memory.html` · `lite-products.html` · `lite-records.html`
바뀐 것은 **각 파일에서 한 줄씩**, `if (e.key === "Enter" …)`에 `&& !e.isComposing`을 더한 것뿐이다.
`lite-manuals.html:402`(장비 주소 칸)에는 대신 `// 조합무관: 장비 주소(IP·호스트명)` 주석을 달았다.

⚠ `max`가 라이트를 고치다 충돌이 나면 **이 한 줄들은 살려 두고** 나머지를 병합할 것.

---

## 1. 무엇이 바뀌었나 — 셋

### ① 한글 조합 중 Enter로 **반 글자가 전송**됐다 (실결함)

한글은 「ㅎ→하→한」처럼 글자를 나눠 만든다(조합). 조합이 끝나기 전 Enter를 누르면
브라우저는 그걸 '글자 확정'으로 쓰는데, 화면이 '보내라'로 받아 **반 글자가 전송**됐다.
「김도희」를 치다 Enter → 담당자가 **「김도ㅎ」**로 배정된다.

⚠ 이 제품은 이 결함을 **이미 알고 있었다**(`titlebar.js:724·731`, `console.js:887`에 가드와 주석).
그런데 **주 대화창 입력칸 3곳에는 없었다** — `console.js:271` · `chatwidget.js:304` · `lite-chat.html:222`.

- 가드 22곳 추가 · 「조합무관: 이유」 6곳
- 소스 감시 `server/test/imecomposition.test.ts` — **예외는 이유를 적어야만 통과**한다
- 양방향 확인함: 고치면 초록 / 가드를 빼면 정확한 문구로 빨간불 2건

**`max`가 새 라이트 화면을 만들 때**: `Enter` 판정을 쓰면 이 시험이 막는다. 텍스트 칸이면
`&& !e.isComposing`, 아니면 줄 끝에 `// 조합무관: <이유>`.

### ② 감사 로그에 「무엇을 바꿨나」가 빈칸이었다 (실결함)

승인 실행 감사가 대상을 `args.assetId ?? args.code ?? null`로만 적었다. 그런데 가장 크게
바꾸는 쓰기인 `bulk_update_findings`는 자산 하나를 안 받고 조건·목록으로 수백 건을 바꾼다 →
**target이 null**이었다. `dispatcher.ts 감사대상(args)`으로 교체(실패 경로에도 남긴다).

### ③ ★「이것들 전부 배정해줘」가 **전건**을 대상으로 삼았다 (실결함)

`runBulkUpdate`는 「빈 filter」를 막아 뒀는데, 막을 것은 **「좁히는가」**였다.

실측(2026-08-18): `filter="이것들"` → **3건 = 전체 3건**. `"전부"`도 같다. `"critical"`은 1건.
「이것들」은 심각도·KEV 어디에도 안 걸리고, 남는 낱말이 "이" 한 글자라 키워드 거르기까지 건너뛴다.

고침: `handlers.ts 조건이좁히나(filter)` — **말만 보고** 판정한다(자료를 보면 날마다 답이 달라진다).
조건이 뜻을 잃으면 ⓐ 보던 목록이 있으면 그것, ⓑ 없으면 되묻는다.

⚠ **라이트도 같은 코드를 탄다** — 라이트 도구 13개에 `bulk_update`가 없다면 안 닿지만,
`lite-tools.json`을 확인해 볼 것. 있으면 라이트에서도 같은 구멍이 있었던 셈이다.

---

## 2. 새 배관 — 「보는목록」 (`max`가 라이트에 얹을지는 나중에 판단)

화면이 **지금 보여 주는 목록**을 대화창에 알리는 전선을 깔았다. 사장님 방향(대화창이 주인공)의
첫 걸음이다. 평소엔 아무 일도 안 하고, **조건이 뜻을 잃었을 때만** 그 목록이 대상이 된다.

⚠ 늘 대상으로 삼지 **않는** 이유: 그러면 화면이 사람 말을 조용히 덮는다.
"critical 전부"라고 했는데 화면에 medium만 떠 있다고 medium이 바뀌면 사고다.

```
approvals.html  gijo:view 발신 ({screen, label, ids:["자산::지문", …]})
      ↓
app.html        아는 값만 걸러 넘김 (:: 형식·길이·건수)      ← 공용 파일
      ↓
console.js      setViewList + 지시문에 #보는목록 표식        ← 공용 파일
      ↓
picklist.ts     parseViewIds (규칙으로 읽는다, LLM 아님)     ← 공용 파일
      ↓
registry.ts     bulk_update의 viewIds 파라미터              ← 공용 파일
dispatcher.ts   결재판 칸을 규칙으로 채움                    ← 공용 파일
```

⚠ **결재판 칸(registry params)에 반드시 등록해야 한다.** 승인 버튼은 화면에 보이는 칸만 모아
서버로 되돌리므로(`console.js` `collect()`), 칸 밖의 값은 **승인하는 순간 증발**한다.
`lite-chat.html`의 결재판도 같은 방식인지 `max`가 확인해 줄 것 — 다르면 라이트에서만 사라진다.

⚠ 상한 50건, 넘으면 **자르지 않고 아예 안 쓴다**. 잘린 절반을 「보던 것」이라 부르며 처리하면
담당자는 전부 바뀐 줄 안다. 또 수백 개짜리 값이 결재판 칸에 들어가면 승인 버튼이 밀려나
**관문 자체가 망가진다.**

⚠ 첫 소비자가 `approvals.html`인 이유: 이미 `findingKey`를 갖고 있어 **화면을 안 바꿔도** 된다.
`vulnscan.html`은 findingKey가 화면까지 안 와서 서버 응답 변경이 필요하다(범위 밖).

---

## 3. `max`가 할 일

| # | 무엇 | 왜 |
|---|---|---|
| 1 | `/GIJOAS동기화`로 최신 받기 | 라이트 7파일이 바뀌었다 — 안 받고 고치면 충돌 |
| 2 | `lite-tools.json`에 `bulk_update`가 있는지 확인 | 있으면 라이트에도 「이것들 전부」 구멍이 있었다 |
| 3 | `lite-chat.html` 결재판이 **보이는 칸만 모으는지** 확인 | 다르면 새 값(viewIds)이 라이트에서만 사라진다 |
| 4 | mac에서 라이트 대화창에 **한글 조합 중 Enter** 실제로 쳐 보기 | 코드는 고쳤지만 mac IME는 win과 다르다 — 실기기 확인은 `max` 몫 |

⚠ 4번이 특히 중요하다. `isComposing`은 브라우저·IME 구현에 따라 다르게 뜬다. Windows에서
초록이라고 mac에서도 초록이라는 보장이 없다 — **제품이 도는 환경에서 잰다**는 규칙 그대로다.

---

## 4. 아직 안 한 것 (정직하게)

- **게시·운영 배포 안 함** — 사장님 허락 전이다
- **실화면 확인 안 함** — 커밋 A·B 모두 시험만 초록이다. 실제로 앱을 띄워 조합 중 Enter를
  쳐 보고, 「이것들 전부 배정해줘」를 대화창에 넣어 결재판이 뜨는 것까지는 아직 안 봤다
- **검토관 결과 대기 중** — 게시 전 검토 관문(CLAUDE.md). 결과 반영 후에 게시를 판단한다
- **맥락 턴 규모 못 쟀다** — 운영 DB가 암호화 저장이라 직접 못 읽는다. 지금은 잠재 결함이다
- `vulnscan.html`·다른 목록 화면은 아직 `gijo:view`를 안 보낸다 (`approvals.html`만)

---

## 5. 시험

- WSL 311파일 전부 초록(종료코드 0) · Windows 전용 2파일 11개 초록
- 신규: `imecomposition`(4) · `contextturns`(7) · `auditbulktarget`(6) · `bulkviewscope`(8) · `viewcontract`(7)
- ⚠ 시험 오염 하나 발견 — `resetAssetsForTests`는 자산·스캔만 지우고 **판정 기록
  (`finding_approvals`)은 남긴다.** 같은 자산 id를 다시 쓰는 시험에선 앞 시험 배정이 살아 있어
  「1건만 바뀌어야」가 2건으로 보인다. 새 시험을 쓸 때 `db.exec("DELETE FROM finding_approvals;")`를
  `beforeEach`에 넣을 것.
