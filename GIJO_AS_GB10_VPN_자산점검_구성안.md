# GIJO AS — GB10 VPN 편입 · 자산 주기 점검 네트워크 구성안

- 작성: 2026-08-11 · 지시: "wireguard는 필요해. 우리 자산도 주기적으로 점검해야 하고 네트워크가 뚫려 있어야 할 듯"
- 성격: **설계·절차서**. 이 문서를 쓴 세션은 클라우드 컨테이너라 VPN·실장비에 붙지 못했다 —
  **아래 값 중 「확인」 표시가 붙은 것은 실물에서 확인해야 하고, 검증 전까지 이 문서는 미검증이다.**
- **2026-08-11 정정 이력** (실측이 들어올 때마다 고쳤다. **최신 상태는 7-0을 볼 것**):
  ① 터널은 **이미 구성돼 있었다**(NM 방식, 10.8.0.12) — 초판 4절의 "새로 깔고 키 발급"은
  틀렸고 위험했다 ② "허브를 GB10으로 옮기자" **철회**(공유기 포워딩 권한 없음 — 7-5 ⓷)
  ③ "터널이 죽었다"는 **오진이었다** — 60초 표본에서 양방향 활성 확인(7-0·7-2 교훈 항).
- 계획서 관계: 계획서 항목 아님(인프라). 다만 **전-1 시연 패키지에 쓸 실데이터**를 만들어 준다
  (뒤 6절) — 우리 자산을 우리 제품으로 점검한 기록이 곧 "지금 됩니다"의 증거다.

---

## 1. 먼저 짚을 것 — "네트워크를 뚫는다"는 두 가지 다른 일

지시를 두 갈래로 나눠 읽었다. 섞으면 위험해진다.

| | 무엇 | 누가 누구에게 붙나 | 뚫는 이유 |
|---|---|---|---|
| **(가) 관리 접속** | 사장님·Mac·Windows가 GB10에 닿기 | 사람 → GB10 | 어디서든 개발·검증·배포 |
| **(나) 점검 트래픽** | GB10이 자산에 SSH로 붙어 점검 | **GB10 → 자산** | 주기 하드닝 점검 |

**(가)는 WireGuard로 푼다. (나)는 WireGuard로 풀면 안 된다** — 자세한 이유는 3절.

⚠ **가장 중요한 사실 하나**: (나)를 켜는 순간 **GB10에 자산 접속 자격증명이 쌓인다.**
그 상자가 뚫리면 자산 전체가 따라 뚫린다. 그래서 5절(최소권한)을 건너뛰면 안 된다.
이건 나중에 **고객사에 제품을 넣을 때 똑같이 설명해야 하는 사항**이기도 하다.

## 2. 구성 (2026-08-11 실측 반영)

```
  Windows PC (10.8.0.1) ── WireGuard 서버 · 공인 IP 직결 · 운영 WSL(4000) · git 허브
        │                  ⚠ 2026-08-11 현재 GB10에서 안 닿음 (7-2)
        ├── Mac M1 Max  (10.8.0.11)         ※ 10.8.0.4 = 예비 슬롯(비어 있음)
        └── GB10        (10.8.0.12)  ← 신규가 아니라 **이미 붙어 있었다**(4절)
                 │
                 └─(LAN 직결)─▶ 우리 자산 (SSH 22)   ※ VPN 경유 아님 — 3절
```

- GB10 VPN 주소는 **10.8.0.12** — 새로 발급할 필요가 없다. 누적 RX 16.9GB가 남아 있다
  (모델·저장소가 이 통로로 내려온 흔적). 10.8.0.4 예비 슬롯도 그대로 보존돼 있다.
- WireGuard 서버는 **Windows를 그대로 둔다** — 공인 IP가 거기 있기 때문이다.
  (⚠ 대신 Windows가 꺼지면 VPN이 끊긴다. **이 약점이 하루 만에 실제로 발생했다** — 7-2.)

## 3. 점검 트래픽을 VPN에 태우지 않는 이유 — 여기가 설계의 핵심

GB10이 자산을 점검하는 경로는 **두 가지**가 가능하다.

| | A. GB10을 자산망에 직접 물림 **(추천)** | B. VPN 경유 (GB10→Windows→자산망) |
|---|---|---|
| 경로 | LAN 직행 | GB10 → VPN → Windows가 라우터 노릇 → 자산 |
| Windows가 꺼지면 | **점검 계속 됨** | **점검 멈춤** |
| 추가 설정 | 없음 | Windows에 IP 포워딩·NAT, 서버 AllowedIPs 확장 |
| 속도 | 빠름 | VPN 암복호 + 홉 하나 더 |
| 고객사 배치와의 관계 | **같은 모양** — 온프렘 제품은 원래 자산망 안에 놓인다 | 다름 — 시연·검증 가치가 떨어짐 |

**추천: A.** 자산 점검은 자산망 안에서 하는 것이 정상이고, 우리가 고객사에 넣을 모습과 같다.
VPN은 (가) 관리 접속 전용으로 두면 역할이 깔끔하게 갈린다.

**B가 필요한 경우는 하나뿐이다** — 점검할 자산이 GB10과 **다른 망**에 있을 때.
그 경우에도 `AllowedIPs`에 **자산 대역만** 적는다. `0.0.0.0/0`은 절대 쓰지 않는다
(전체 트래픽이 VPN으로 빨려 들어가 GB10이 인터넷도 Windows를 통해 나가게 된다).

> **[확인]** 우리 자산이 GB10과 같은 망에 있는가? 같으면 A로 끝난다. 다르면 B 절차를 추가한다.

## 4. 터널은 이미 있다 (2026-08-11 GB10 실측으로 정정)

> **⚠ 이 절은 처음에 틀리게 적혀 있었다.** "WireGuard를 새로 깔고 키를 만들어 피어를 발급하라"고
> 썼는데, **GB10에는 이미 터널이 구성돼 있었다.** 그대로 따라 했으면 잘 돌던 터널 옆에
> **충돌하는 두 번째 터널**을 만들 뻔했다. 실측으로 확인된 진짜 상태는 아래와 같다.

| 항목 | 실측값 |
|---|---|
| 관리 주체 | **NetworkManager 네이티브 WireGuard** (wg-quick 아님) |
| 연결 이름 | `client-GB10` · 상태 activated · **autoconnect 켜짐**(재시도 무제한) |
| 주소 | **10.8.0.12/32** · MTU 1420 |
| 라우팅 | `10.8.0.1 dev client-GB10 proto static metric 50` |
| 커널 모듈 | 적재됨 (wireguard 1.0.0) |
| `/etc/wireguard/` | **없음 — 이게 정상이다** (NM이 프로파일을 자체 보관) |
| `wg` / `wg-quick` / wireguard-tools | **없음** (7-1 참조) |
| 누적 트래픽 | RX 16.9GB · TX 164MB · 오류 0 · 드롭 0 → **잘 쓰이던 터널이다** |

**그러므로 하지 말아야 할 것:**
- ❌ `apt install wireguard` 후 `/etc/wireguard/wg0.conf` 작성 — 두 번째 터널이 생겨 충돌한다
- ❌ `systemctl enable wg-quick@wg0` — 이 기계는 NM이 관리한다
- ❌ 키 재생성 — 서버에 등록된 공개키와 어긋나 지금 되던 것마저 끊긴다
- ❌ `/etc/wireguard`가 비었다고 "미설치"로 판단 — **이 판정 실수가 이 절을 틀리게 만들었다**

**설정을 봐야 할 때는 `nmcli`로 본다:**
```bash
nmcli connection show client-GB10          # 프로파일 전체
nmcli -f WIREGUARD connection show client-GB10
ip -d link show client-GB10 ; ip -4 addr show client-GB10 ; ip route | grep 10.8.0
```

### 4-4. GB10 방화벽 — 열되, 좁게
```bash
sudo ufw allow from 10.8.0.0/24 to any port 22   proto tcp   # 관리 SSH: VPN에서만
sudo ufw allow from 10.8.0.0/24 to any port 4000 proto tcp   # GIJO AS 서버: VPN에서만
# 자산망 담당자가 웹 화면을 쓴다면 그 대역도 4000에 한해 추가
# sudo ufw allow from <자산망 대역> to any port 4000 proto tcp
sudo ufw enable && sudo ufw status numbered
```
⚠ **4000을 0.0.0.0/0으로 열지 말 것.** 로그인 화면이 있다고 안전한 게 아니다.

### 4-5. 상대 머신에서 확인
```bash
# Mac 또는 Windows에서
ping 10.8.0.12
ssh <계정>@10.8.0.12 "echo OK"
curl -s http://10.8.0.12:4000/api/health     # 서버가 떠 있으면 응답(app.ts에 있는 실제 경로)
```

## 5. 자산 점검 — 최소권한으로 여는 법

제품에는 **자동 주기 점검이 이미 있다**(`server/src/engine/hardeningtargets.ts` — 대상 등록 →
스케줄 등록 → 서버가 만기된 스케줄을 찾아 자동 실행 → 감사로그·악화 알림까지).
새로 만들 게 아니라 **경로만 열어 주면 된다.**

접속 방식: **SSH(기본 22)** 로 `username@host`에 붙어 **고정 진단 명령**을 실행한다
(명령·호스트·키를 배열 인자로 넘겨 셸 해석을 거치지 않는다 — 명령 주입 없음).

### 자산 쪽에 만들 것 (자산 1대마다)
```bash
# 점검 전용 계정 — 사람이 로그인하는 계정을 쓰지 말 것
sudo useradd -m -s /bin/bash gijo-audit
sudo mkdir -p /home/gijo-audit/.ssh && sudo chmod 700 /home/gijo-audit/.ssh
# GB10의 공개키만 등록. from= 으로 출처를 GB10으로 못박는다.
echo 'from="10.8.0.12,<GB10의 자산망 IP>" ssh-ed25519 AAAA... gijo-audit@gb10' \
  | sudo tee /home/gijo-audit/.ssh/authorized_keys
sudo chown -R gijo-audit:gijo-audit /home/gijo-audit/.ssh
sudo chmod 600 /home/gijo-audit/.ssh/authorized_keys
```
원칙 3가지:
1. **읽기 전용으로 족하다** — 하드닝 점검은 설정을 *읽는다*. `sudo` 전권을 주지 말 것.
   꼭 필요한 명령이 있으면 `sudoers`에 그 명령만 개별 허용한다.
2. **`from=`으로 출처 고정** — 그 키가 유출돼도 GB10 밖에서는 안 먹는다.
3. **점검 계정과 운영 계정을 섞지 말 것** — 감사 기록에서 "이건 제품이 한 일"이 구분돼야 한다.

### 제품에서 등록 (화면에서)
| 칸 | 값 |
|---|---|
| host | 자산 IP |
| port | 22 |
| username | `gijo-audit` |
| 인증 | 키 |
| 표준 | `kisa` / `cis` / `kisa_pc` / `kisa_net` 중 자산 성격에 맞게 |

등록 후 **스케줄을 걸면 서버가 알아서 주기 실행**한다. 사람이 눌러 줄 필요 없다.

> **[확인] 첫 대상은 GB10 자신으로.** `host = local`이면 SSH 없이 자기 자신을 점검한다.
> 남의 장비를 건드리기 전에 이걸로 파이프라인이 도는지부터 본다.

## 6. 덤 — 이게 왜 영업 자산인가 (전-1 연결)

우리 자산을 우리 제품으로 주기 점검하면, 계획서 **전-1(4대 시나리오 시연 패키지)** 에 쓸
**실데이터**가 매주 쌓인다. 지금 시연의 약점은 "데이터가 만든 것처럼 보인다"는 점인데,
몇 주치 실제 점검 이력·악화 알림·개선 추이는 만들어 낼 수 없는 종류의 증거다.
게다가 **"우리는 우리 제품을 우리 회사에 깔아 쓰고 있다"** 는 문장 자체가 보안 구매자에게 강하다.

## 7. 상태와 약점

### 7-0. 확정된 현재 상태 (2026-08-11 23:14 실측 — 여기가 최신이다)

| 항목 | 판정 |
|---|---|
| **WireGuard 터널** | ✅ **살아 있다.** `wg show` 실측: **최근 핸드셰이크 12초 전** |
| 엔드포인트 | ✅ 정상 수신 중 (`:51820`) — ⚠ 주소는 여기 적지 않는다(아래 보안 주의) |
| keepalive | ✅ `every 25 seconds` — 관측한 32바이트 단발 TX와 정확히 일치 |
| 누적 전송 | RX 15.71 GiB / TX 156.69 MiB — `/proc/net/dev` 값과 일치 |
| 암호화 | ✅ **PSK(사전공유키)까지 걸린 구성** — 기본보다 한 겹 더 |
| GB10 주소 | ✅ 10.8.0.12 · NM 관리 · autoconnect |
| GB10 sshd | ✅ 22번 수신 중 → **Windows가 GB10으로 push 가능** |
| Windows ICMP·22번 | ⚠ GB10에서 무응답 — 원인 미상(아래) |
| **GB10의 `allowed ips`** | ⚠ **`10.8.0.1/32`** — 아래 |

**⚠ 새로 드러난 제약 — GB10은 VPN으로 Windows밖에 못 본다.**
GB10 피어의 `allowed ips`가 **`10.8.0.1/32`(Windows 한 대)** 로 좁혀져 있다. 즉 **GB10 ↔ Mac
(10.8.0.11) 직접 통신은 지금 불가능**하다. Windows를 경유하는 구조다.
지금 하려는 일(코드 받기·자산 점검)에는 지장이 없지만, 나중에 **Mac에서 GB10 서버(4000)를
직접 보려 하면 막힌다.** 그때는 GB10 프로파일의 allowed-ips를 `10.8.0.0/24`로 넓히면 된다:
```bash
sudo nmcli connection modify client-GB10 +wireguard.peer... # [확인] NM에서 peer allowed-ips 수정 문법
# 또는 GUI/프로파일에서 AllowedIPs = 10.8.0.0/24 로
```
⚠ 서버(Windows) 쪽 피어의 `AllowedIPs`는 **`10.8.0.12/32`로 그대로 둔다** — 그건 좁을수록 안전하다.

**⚠ 보안 주의 — 이 문서에 적지 않는 것**: WireGuard 서버의 **공인 IP**는 여기 기록하지 않는다.
이 저장소는 외부 협업자와 공유되고(`GIJO_AS_공동작업_가이드.md`), 공인 IP는 **서버 위치를
가리키는 정보**다. 필요하면 `wg show`로 그때그때 본다. (peer 공개키는 원래 비밀이 아니지만
같은 이유로 굳이 적지 않는다. 개인키·PSK는 어떤 경우에도 기록 금지.)

**코드가 GB10에 들어가는 정규 통로 — VPN·GitHub 인증·공유기 전부 불필요:**
```powershell
# Windows에서 — GB10에는 GitHub 인증이 없으므로 Windows가 받아서 밀어 넣는다
cd "D:\Connect AI"
git remote -v                                        # gb10 리모트가 있는지 먼저
git fetch origin claude/gb10-remote-session-oikism
git push gb10 origin/claude/gb10-remote-session-oikism:refs/heads/claude/gb10-remote-session-oikism
```
막힐 만한 자리 셋:
- **`gb10` 리모트가 없다** → `git remote add gb10 ssh://<계정>@10.8.0.12/<GB10 저장소 경로>`
- **"refusing to update checked out branch"** → GB10 저장소가 그 브랜치를 이미 펼쳐 두고 있다.
  GB10에서 다른 브랜치로 옮기거나, 받은 뒤 `git checkout` 한다.
- **"unrelated histories"** → GB10 저장소가 이 저장소의 사본이 아니다. 그러면 밀어 넣는 대신
  GB10에서 새로 클론해야 한다: `git clone ssh://user@10.8.0.1/d:/gijo-hub.git`

> ⚠ **원인 미상으로 남겨 두는 것 — Windows 22번.** 터널이 이 정도로 건강한데(핸드셰이크 12초 전,
> 양방향 버스트가 전부 그 Windows와 오간 것) `10.8.0.1:22`만 타임아웃이다. 즉 **Windows PC는
> 켜져 있고 잘 응답하는데 22번만 안 열린다.** 원인은 터널이 아니라 Windows 쪽이다.
> `GIJO_AS_2머신_개발환경_가이드.md`는 `ssh user@10.8.0.1`(Mac→Windows)을 정규 경로로 적고 있으니
> sshd 자체는 돌 것이다. **가장 그럴듯한 것은 방화벽 허용 목록이 10.8.0.11(Mac)만 덮고
> 10.8.0.12(GB10)를 안 덮는 경우**, 또는 프로파일이 Public을 안 덮는 경우(7-3 ⓐ — 우리 실사고 이력).
> 지금 막힌 일과는 무관하지만(정규 방향은 Windows→GB10 push) **덮어 두지 않는다.**
> ```powershell
> Get-NetFirewallRule -Name "OpenSSH-Server-In-TCP" | Format-List Name,Enabled,Profile,Action
> Get-NetFirewallRule -Name "OpenSSH-Server-In-TCP" | Get-NetFirewallAddressFilter   # RemoteAddress 확인
> ```

### 7-1. 진단 도구가 그 기계 안에 없다

GB10에는 `wireguard-tools`가 없다. 그래서 **"터널이 진짜 살아 있는가"를 볼 수 없다.**

| 보고 싶은 것 | `nmcli`로 되나 | `wg show` |
|---|---|---|
| 인터페이스 UP·주소·라우팅 | ✅ | ✅ |
| **마지막 핸드셰이크 시각** | ❌ | ✅ |
| **현재 엔드포인트(상대 IP:포트)** | ❌ | ✅ |
| peer 공개키·전송량 | ❌ (`peer-routes` 한 줄뿐) | ✅ |

⚠ **인터페이스가 UP인 것은 터널이 살아 있다는 뜻이 아니다.** WireGuard는 상대가 없어도
인터페이스를 그냥 띄워 둔다. 살아 있음의 유일한 판정 기준은 **최근 핸드셰이크**이고,
그걸 볼 도구가 지금 없다.

**GB10은 이 터널이 유일한 통로다.** 끊기면 원격에서 손쓸 방법이 없는데 원인을 볼 도구가
기계 안에 없는 구조 — 이건 그 자체로 결함이다.

**권고: `sudo apt install -y wireguard-tools`** (읽기 전용 진단 도구 `wg`만 들어온다.
네트워크 설정을 바꾸지 않으므로 지금 도는 NM 터널에 영향이 없다.) 설치 뒤:
```bash
sudo wg show          # 마지막 핸드셰이크·엔드포인트·전송량
```

### 7-2. 나머지

| 약점 | 지금 | 대안 |
|---|---|---|
| **Windows에 의존** (VPN 서버 + git 허브) | 회선·권한이 그렇게 정함 | **[권고 철회 — 확정]** GB10은 VPN 서버가 될 수 없다. 공유기 관리 권한이 없어 포트포워딩을 못 바꾼다(7-5 ⓷) |
| **오진 사례 — 「터널이 죽었다」** | ping 100% 손실 + TCP 22 타임아웃을 보고 터널 사망으로 읽었다. **틀렸다** — 터널은 살아 있었다(7-0) | **교훈: ping·특정 포트 실패로 터널 생사를 판정하지 말 것.** 인터페이스 카운터를 **충분히 긴 창(60초)** 으로 보고, keepalive(32바이트 단발)가 도는지를 본다. 짧은 표본(11초)은 트래픽 공백에 통째로 들어가 거짓 사망 판정을 만든다 |
| GB10이 자산 자격증명 보관소가 됨 | — | 5절 최소권한 + GB10 자체를 `kisa` 표준으로 점검 대상에 넣기(자기 점검) |
| **[정정] 4절이 틀렸던 이유** | `/etc/wireguard` 없음 = 미설치로 판단 | NetworkManager 방식에서는 그 경로가 비는 게 정상이다. **파일 경로로 설치 여부를 판정하지 말 것** — `ip link`·`nmcli`·`lsmod`로 본다 |

### 7-5. [2026-08-11 3차] 세 대가 같은 회선이었다 — 판이 바뀐다

사용자 확인: **Windows·Mac·GB10이 모두 같은 회선.** Windows만 **유선**, Mac·GB10은 **무선**.
GB10 실측 무선 주소가 `192.168.219.66`이므로 LAN은 `192.168.219.0/24`로 보인다.

**⓵ VPN이 없어도 서로 통한다(가장 급한 결론).**
같은 LAN이면 GB10 ↔ Windows는 사설 IP로 직접 닿는다. 지금 VPN이 끊겨 막혀 있던 일 —
**GB10이 코드를 받는 것** — 을 VPN 수리를 기다리지 않고 지금 할 수 있다:
```bash
# GB10에서 (192.168.219.x = Windows 유선 주소)
ssh user@192.168.219.<Windows> "echo OK"
git remote add hub "ssh://user@192.168.219.<Windows>/d:/gijo-hub.git"
git fetch hub
```
> **[확인] Windows의 LAN 주소** — Windows에서 `ipconfig | findstr IPv4` 한 줄이면 나온다.
> 그 값이 `192.168.219.x`면 같은 LAN이 확정되고, 공인 IP가 나오면 세그먼트가 다른 것이다.

**⓶ 무선이 GB10의 약점이다 — 이게 지금 장애의 원인일 수 있다.**
GB10은 상시 서버 노릇(주기 점검·LLM 서빙)을 할 장비인데 무선은 그 용도에 약하다:
- **전원 절약(power save)** 으로 유휴 시 무선 칩이 졸면 UDP가 끊긴다 → **WireGuard 핸드셰이크 만료**
- 공유기의 NAT 매핑이 만료되면 서버가 GB10을 다시 못 찾는다 → `PersistentKeepalive` 25초가 필수
- 모델 GGUF(수십 GB) 전송이 유선보다 훨씬 느리다

```bash
# GB10 — 무선 절전이 켜져 있나 / keepalive가 걸려 있나
iw dev wlP9s9 get power_save
nmcli -f WIREGUARD connection show client-GB10 | grep -i keepalive
# 절전 끄기(임시): sudo iw dev wlP9s9 set power_save off
```
**권고: GB10을 유선으로 옮긴다.** 상시 서비스 장비를 무선에 두는 것은 이 문제를 계속 만든다.

**⓷ VPN 서버 이전은 최종 불가 — 이 건은 닫는다.**
사용자 확인: **공유기에 관리자로 들어갈 수 없어 포트포워딩을 바꿀 수 없다.**
VPN 서버가 되려면 밖에서 오는 51820/UDP를 그 기계로 보내 줘야 하는데, 그 스위치를 우리가
못 만진다. 장비·회선의 문제가 아니라 **관리 권한의 문제**다 — 논의를 여기서 닫는다.
(7-4의 결론이 다른 이유로 유지된다. 공인 IP 소재가 아니라 포워딩 권한 때문이다.)

**⚠ 이 사실에서 따라오는 취약점**: 지금 VPN이 밖에서 되는 것은 **누군가 예전에 걸어 둔
포워딩(또는 Windows의 공인 IP 직결)** 덕이다. ISP가 주소를 바꾸거나 공유기가 초기화되면
**우리 손으로 되살릴 수 없다.** 그래서 아래 ⓸가 중요해진다.

**⓸ LAN 직결은 포워딩이 필요 없다 — 예비 통로로 남겨 둔다.**
(터널이 살아 있음이 확인됐으므로(7-0) 당장은 VPN 경로를 쓴다. 아래는 VPN이 실제로 죽었을
때의 대비책이고, **자산 점검은 원래부터 이 LAN 경로를 쓴다**(3절 A안).)
포트포워딩은 **바깥에서 안으로** 들어올 때만 필요하다. 같은 공유기 안에 있는 기계끼리는
아무 설정 없이 그냥 통한다. 즉 **집 안에서 하는 일(코드 동기화·자산 점검·검증)은 VPN이
끊겨 있어도 전부 된다.**

| 하려는 일 | VPN 필요? | 포워딩 필요? |
|---|---|---|
| GB10 ↔ Windows 코드 동기화(git hub) | ❌ LAN으로 | ❌ |
| GB10 → 자산 주기 점검(SSH) | ❌ LAN으로 (3절 A안) | ❌ |
| GB10 서버 화면을 집에서 보기(4000) | ❌ LAN으로 | ❌ |
| **집 밖에서** GB10·Windows에 접속 | ✅ | 이미 걸린 것에 의존 |

그러므로 **당장 할 일은 VPN 수리가 아니라 LAN 경로를 쓰는 것**이다.
```bash
# GB10에서 — [확인] Windows의 LAN 주소를 넣는다(ipconfig | findstr IPv4)
ssh user@192.168.219.<Windows> "echo OK"
git remote add hub "ssh://user@192.168.219.<Windows>/d:/gijo-hub.git"
git fetch hub
```
⚠ Windows 방화벽 22번 규칙이 `10.8.0.0/24`만 허용하도록 좁혀져 있다(가이드라인 5절).
**LAN에서 붙으려면 LAN 대역도 허용해야 한다** — 넓히되 집 안 대역까지만:
```powershell
Set-NetFirewallRule -Name "OpenSSH-Server-In-TCP" -Profile Any `
  -RemoteAddress @("10.8.0.0/24","192.168.219.0/24")
```
⚠ **`Any`로 열지 말 것.** 공인 IP 직결이면 인터넷 전체에 SSH를 여는 셈이 된다.

### 7-4. [권고 철회 — 7-5에서 조건부로 재검토] "허브를 GB10으로"는 그때 조건에서 성립하지 않았다

초판 7절에서 **"상시 가동인 GB10으로 VPN 서버·git 허브를 옮기는 게 맞다"** 고 적었다.
**철회한다.** 우리 문서 두 곳이 이미 답을 갖고 있었는데 내가 안 보고 썼다:

> `GIJO_AS_2머신_개발환경_가이드.md`: "**이 PC는 공인 IP 직결**"
> `GIJO_AS_개발환경_가이드라인.md`: "**공인 IP 직결**이라 RemoteAddress 제한은 생명줄"

**VPN 서버는 밖에서 찾아올 수 있는 주소를 가져야 한다.** 공인 IP가 Windows에 붙어 있고,
GB10은 공유기 뒤(실측 192.168.219.66)다. 즉 GB10은 **서버가 아니라 클라이언트 자리**이고,
이건 장비 성능이나 상시성의 문제가 아니라 **회선이 정하는 일**이다.

> ⚠ **이 판단의 전제가 흔들렸다(7-5).** 세 대가 같은 회선이라면 공인 IP는 공유기가 갖고 있고,
> 포워딩 대상만 바꾸면 GB10도 서버가 될 수 있다. **아래 결론은 「Windows가 공인 IP 직결」이
> 사실일 때만 유효하다** — Windows의 LAN 주소를 확인하기 전까지는 미확정이다.

그리고 git 허브만 옮겨도 실익이 작다 — 모두가 GB10에 닿는 길이 여전히 Windows의 VPN을
통과하기 때문이다. **관문이 안 바뀌면 의존도 안 바뀐다.**

**층위를 갈라 보면 이렇게 정리된다:**

| 층위 | 내용 | 어디에 |
|---|---|---|
| **컨트롤**(권한·책임) | 게시(electron-builder·claude-deploy·실화면 검증) · 운영 WSL(4000) · 주개발 · 최종 판단 | **Windows** — 옮길 이유 없음 |
| **인프라**(상시성) | VPN 서버 · git 허브 | **Windows** — 회선이 그렇게 정함 |
| **작업 노드** | mac 검증 · **GB10 대용량 LLM·자산 점검** | Mac · GB10 |

**그래서 진짜 해법은 「옮기기」가 아니라 「Windows를 안 죽게 만들기」다:**
1. 전원 옵션 — 절전·최대 절전 안 함, 하드디스크 끄기 안 함(원격 상시 접속 장비의 기본)
2. WireGuard 터널 서비스 자동 시작 확인 (`wireguard.exe /installtunnelservice`로 서비스 등록된 상태)
3. GB10 쪽 진단 도구 확보(7-1) — 끊겼을 때 원인을 볼 수 있어야 한다
4. GB10은 **자산망 안에서 자기 일(주기 점검)을 계속하게** 둔다 — 3절 A안을 택한 이유가 이것이다.
   VPN이 끊겨도 점검은 멈추지 않는다.

> **단, 조건이 바뀌면 결론도 바뀐다.** GB10이 있는 공유기에 공인 IP가 들어오고 51820/UDP를
> GB10으로 포워딩할 수 있다면 그때는 옮기는 쪽이 낫다. [확인] GB10과 Windows가 같은 회선인가.

### 7-3. 터널이 죽었을 때 원인 가르기

**먼저 Windows에서 WireGuard 앱을 열어 「최근 핸드셰이크」를 본다.** 이 한 줄이 원인을 반으로 가른다
(별도 도구가 필요 없다 — GUI에 그대로 나온다).

| 최근 핸드셰이크 | 뜻 | 다음 |
|---|---|---|
| **최근(2분 이내)** | 터널은 살아 있다 → **Windows 방화벽 문제** | 아래 ⓐ |
| **없음·오래됨** | 터널이 안 붙는다 | 아래 ⓑⓒ |

**ⓐ 방화벽 — 우리에게 실사고 이력이 있는 함정 (Windows가 켜져 있는데 타임아웃이면 1순위)**
`GIJO_AS_개발환경_가이드라인.md`에 이미 적혀 있다:
> "**22번은 10.8.0.0/24만 + Profile=Any** — wg 인터페이스가 Public 분류라
> **Private 전용 규칙은 조용히 무시됨(무응답 타임아웃)**"

WireGuard 인터페이스는 Windows가 **Public**으로 분류한다. 규칙 프로파일에 Public이 빠지면
거부(즉시 실패)가 아니라 **무응답 타임아웃**이 난다 — 지금 증상과 정확히 같다.
Windows 업데이트·프로파일 재분류·규칙 재생성으로 되살아나기 쉽다.
```powershell
Get-NetFirewallRule -Name "OpenSSH-Server-In-TCP" | Format-List Name,Enabled,Profile,Action
Get-NetFirewallRule -Name "OpenSSH-Server-In-TCP" | Get-NetFirewallAddressFilter
# Profile에 Any(또는 Public 포함)가 아니면 이게 원인이다:
Set-NetFirewallRule -Name "OpenSSH-Server-In-TCP" -Profile Any -RemoteAddress 10.8.0.0/24
```

**ⓑ 서버 conf를 고치고 터널을 재설치하지 않았다** — 같은 문서에 적힌 함정:
> "서버 conf 수정 후엔 **터널 재설치 필요**. 실행 중 터널은 **dpapi 사본**을 쓰므로
> **파일만 고쳐선 반영 안 됨**"
```powershell
wireguard.exe /uninstalltunnelservice wg0-server
wireguard.exe /installtunnelservice "D:\GIJO-AS-vpn\wg0-server.conf"
```

**ⓒ 그 밖**

| 증상 | 그럴듯한 원인 | 확인법 |
|---|---|---|
| ping·TCP 모두 타임아웃, Windows는 꺼져 있음 | 상대 꺼짐·절전 | 전원 옵션(7-4) |
| ping만 안 되고 TCP는 됨 | Windows 방화벽의 ICMP 차단(기본값) | 정상. 무시해도 된다 |
| 한동안 되다가 끊김 | 엔드포인트(공인 IP) 변동 → 핸드셰이크 만료 | `wg show`(7-1) · `PersistentKeepalive` 25초 |
| 인터넷은 되는데 VPN만 안 됨 | 위 중 하나 | `ip -4 addr show`로 무선이 살아 있는지 먼저 |

## 8. GB10 클로드에 넘길 확인 항목

1~3번은 2026-08-11에 확인 완료(4절·7-2에 반영). **남은 것은 아래 둘이다.**

```
GIJO AS — GB10 확인 (남은 항목)

1) 우리 자산과 같은 망인가  ← 3절 A/B안이 여기서 갈린다
   ip -4 addr show | grep inet          # GB10의 LAN 주소(실측: 무선 192.168.219.66)
   ping -c3 <자산IP> ; nc -vz <자산IP> 22
   → 되면 A안(LAN 직결)으로 끝. 안 되면 B안(VPN 경유) 절차를 추가해야 한다.

2) 이미 열린 포트가 있나
   ss -lntp | grep -E ':(22|4000|8080|8081)'
   sudo ufw status verbose 2>/dev/null || echo "ufw 없음"

⚠ 확인만 한다. ufw 규칙·NM 프로파일 변경은 사장님이 결정한 뒤에 한다.
⚠ WireGuard를 새로 설치하거나 키를 만들지 말 것 — 이미 붙어 있다(4절).
   결과를 그대로 보고할 것 — 추정으로 채우지 말 것.
```
