# 고민 — BridgeAI · Smart MD Studio를 **전 제품에 선택 연동** (max, 2026-08-13)

사장님: 「이 두 기능을 우리 모든 제품에 고객이 **선택적으로** 사용했으면 한다. 고민해줘.」
두 저장소를 clone해서 읽고(외부 추정 아님), GIJO AS의 붙일 자리와 대조했다.

---

## 0. 두 저장소가 무엇인가 (읽고 확인)

| | GIJO_BridgeAI | gijo-smart-md-studio |
|---|---|---|
| 한 줄 | **원격 GPU/VRAM 오프로드** — 저사양 PC가 집·사무실의 대형 AI 서버 자원을 로컬처럼 씀 | **마크다운 저작 도구** — 붙여넣기 스크린샷·템플릿·.md 내보내기 |
| 구조 | Node/Express, `http-proxy`로 `/v1` → 원격 Ollama(11434), `ssh2`로 원격 `nvidia-smi` 수집, WireGuard/P2P | Electron 단독 앱(자체 BrowserWindow), annotate·export·templates·storage |
| 대상 기계 | GB10 · Mac Studio M4 128GB · 듀얼 4090 등 상시 켠 홈서버 | — (편집기라 무관) |
| 라이선스 | (repo에 명시 없음 — 우리 것이라 무방하나 확인 필요) | **MIT** — 번들·개조 자유 |

---

## 1. 왜 값이 큰가 — 둘 다 GIJO AS의 **약한 자리를 정확히 때린다**

### BridgeAI = 우리가 이번 주 내내 부딪힌 「VRAM 천장」의 해답
- 라이트는 10GB 기계라 **14B가 물리적으로 안 들어간다**(실측 10.2GB). 그래서 라우팅 2건을
  못 되찾고, 기능등급 가이드에 「더 크게 쓰면 좋아진다 = 큰 기계를 사라」로 적었다.
- BridgeAI는 **기계를 안 사고** 그걸 준다: 10GB 노트북이 집의 GB10에 붙어 **14B·32B를 로컬처럼**
  쓴다. 「더 크게 쓰면 좋아진다」가 **하드웨어 교체 없이** 실현된다.
- ★ **붙일 자리가 이미 있다.** GIJO AS는 채팅 URL을 `GIJO_LOCAL_LLM_URL`(llm.ts:24)로 받고,
  화면도 이미 **「② 사내 GPU 서버에 연결 — 설정에서 서버 주소 입력」**을 안내한다(llm.ts:719).
  BridgeAI의 프록시(`localhost:3000/v1`)는 **정확히 그 자리에 꽂는 물건**이다. 즉 GIJO AS 쪽은
  이미 원격 `/v1`을 받게 설계돼 있고, BridgeAI가 그 원격을 **쉽게 만들어 주는** 짝이다.

### Smart MD Studio = RAG의 「쓰는 쪽」
- GIJO AS는 지식을 **읽고 검색**한다(RAG). 라이트 메뉴 9는 .md 폴더를 **읽기 전용**으로 본다.
- Smart MD는 그 문서를 **만드는** 도구다 — 스크린샷 붙여넣기(폐쇄망에 딱), 템플릿, .md 저장.
- 지금은 라이트 메뉴 9에서만 「연동」으로 언급했는데, 사장님 말대로 **전 제품**에서 지식 저작
  companion으로 쓰면 스탠다드·프로 담당자도 화면 캡처·회의록을 바로 지식으로 만든다.

---

## 2. 「모든 제품에 선택적」을 어떻게 — 재사용할 기존 seam

핵심: **둘 다 새 큰 코드가 아니라 「연결점」이다.** 이미 있는 자리를 쓴다.

### BridgeAI 연동 (설정의 「원격 GPU」 스위치 하나)
```
설정 → 서버·AI → [원격 GPU(BridgeAI) 연결]  ← 선택
  · 주소·계정 입력 → GIJO AS의 채팅 URL을 BridgeAI 프록시로 돌린다(GIJO_LOCAL_LLM_URL)
  · 끄면 로컬 모델로 돌아온다(지금 그대로)
```
- 에디션 무관하게 **같은 스위치** — 라이트도 스탠다드도 프로도 「원격 GPU 켜기」 하나.
- 라이트: 로컬 7.6B 대신 원격 14B. 스탠다드/프로: 버스트 용량·A/B 상대 모델.
- BridgeAI 자체는 **별도 프로세스/앱**으로 두고(포함 아님, 연동) — Smart MD 결정 ④와 같은 원칙:
  **없어도 GIJO AS가 돈다.**

### Smart MD 연동 (office 창 패턴 재사용)
- GIJO AS엔 이미 **별도 창을 띄우는 패턴**이 있다(office.html · `officeWindow`, main.ts:458).
  Smart MD도 Electron 단독 앱이라, **같은 폴더를 여는 companion 창**으로 띄우면 된다.
- 흐름: Smart MD에서 저작 → .md 저장 → GIJO AS 「나만의 기억/지식」이 그 폴더를 읽어 RAG.
  (라이트 메뉴 9의 읽기 로직을 그대로 쓴다 — max가 이미 만든 것.)
- 전 제품 공통: 지식베이스 화면에 「Smart MD로 편집」 단추 → 있으면 그 앱을, 없으면 안내.

---

## 3. ⚠ 보안 제품이라서 **반드시** 짚을 것 (이게 진짜 고민거리)

### ✅ 사장님 결정(2026-08-13): **BridgeAI는 VPN으로만 동작한다** — 핵심 위험 해소
- 원래 걱정: BridgeAI를 켜면 질문(로그·취약점·사내문서)이 원격으로 나가 「데이터가 이 PC 안」
  약속과 부딪힌다.
- **VPN 전용으로 못 박으면 가장 큰 부분이 풀린다** — 공개 인터넷 노출·제3자 서버·도청이 사라지고,
  데이터는 **고객 VPN 안의 고객 소유 기계**(win·max·gb10 구성 그대로)에만 간다. 새 위험이 아니라
  **이미 있는 신뢰 경계 안**이다. → egress 걱정은 **설계로 닫혔다.**
- 그래도 얇게 지킬 것(막는 게 아니라 정직함):
  - **끈 상태가 기본 + 명시적으로 켜기** — 「질문이 VPN으로 원격 GPU에 전송됩니다」 한 줄 표기.
    고객 컴플라이언스가 스스로 판단하게(투명성이지 차단이 아니다).
  - **에어갭 고객**은 네트워크가 없어 이 기능을 그냥 안 쓴다 — 선택 기능이라 문제 없음(일관됨).
  - **자격증명(SSH2 원격 ID/PW)**은 우리 키체인/암호화 규칙에 태운다(화면·코드에 안 남김 원칙).

### 공통
- **라이선스**: Smart MD는 MIT라 안전. BridgeAI는 repo에 라이선스 파일이 안 보였다 — 우리
  것이면 사내 표기만 정리. 의존성(`http-proxy`·`ssh2`·`ws`)은 상용 배포 제약 확인.
- **QA 폭**: 「선택 기능」이 둘 늘면 **켠 경우/끈 경우**가 다 시험 대상이다. 게이트가 두 배가 되지
  않게, 연동은 **켜면 URL만 바뀌는** 얇은 층으로(로직 중복 없이) 두는 게 관건이다.

---

## 4. 단계 제안 (사장님 결정 대기)

| 단계 | 내용 | 선결 |
|---|---|---|
| **1** | **BridgeAI 연동을 「사내 GPU 서버 연결」 스위치로** — 이미 있는 `GIJO_LOCAL_LLM_URL` seam에 UI만 얹는다. **VPN 전용**(사장님 결정)·기본 꺼짐·켤 때 한 줄 표기 | ✅ egress 걱정은 VPN 전용으로 닫힘 — **착수 가능**. 남은 건 BridgeAI를 GIJO와 함께 낼지 별도 앱으로 둘지 |
| **2** | Smart MD를 companion 창으로 전 제품에 — office 창 패턴 재사용, 없어도 제품이 돎 | Smart MD 배포를 GIJO와 **함께/따로** 낼지 |
| **3** | BridgeAI의 「남은 VRAM 기반 모델 추천」을 우리 기능등급 가이드와 잇기 — 원격 자원에 맞는 모델을 자동 제안(모델 상시 탐색 원칙과 같은 결) | 1단계 후 |

## 5. max 의견 (한 줄씩)
- **BridgeAI는 값이 매우 크다** — 우리가 「등급=기계 크기」로 팔던 걸 「등급=기능, 큰 모델은
  원격으로」로 바꿔, 라이트 고객도 큰 모델을 쓰게 한다. 다만 **데이터가 나가는 순간 우리 제품의
  정체성이 바뀐다** — 이건 기술이 아니라 **사장님의 제품 결정**이다. 켜는 것을 기본으로 두지
  말고, **끈 상태가 기본 + 명시적으로 켜는** 구조를 강권한다.
- **Smart MD는 위험이 없다**(MIT·로컬·데이터 안 나감). 전 제품 companion으로 넣는 데 걸림이
  적다 — 먼저 해도 되는 쪽.
- 둘 다 **포함이 아니라 연동**으로(없어도 제품이 돎) — 사장님이 Smart MD에 이미 세운 원칙 ④를
  BridgeAI에도 그대로 적용하면 QA·배포가 안 터진다.

⚠ 이 문서는 **고민(설계 검토)만**이다 — 코드는 한 줄도 안 건드렸다. 방향을 정해 주시면 1단계부터.
