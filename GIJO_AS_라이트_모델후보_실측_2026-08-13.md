# 라이트 모델 후보 실측 — 「기능이 가능한 모델」을 찾아서 (max, 2026-08-13)

사장님 지시: 「기능이 가능한 모델로 찾아서 넣어주고」.
후보를 고르고 · 내려받고 · **출하 구성 그대로**(라이트 진입점 · 빈 DB · ctx 8192 · Metal) 쟀다.

## 1. 후보 선정

| 후보 | 판정 | 왜 |
|---|---|---|
| **Qwen3-8B** (Q4_K_M, 4.7GB) | **측정함** | 14B(게이트 통과 이력)와 같은 계열 · Apache 2.0 · 10GB 안에 듦 |
| EXAONE-3.5-7.8B | 제외 | 카탈로그 스스로 경고 — **비상업용 라이선스.** 파는 제품에 못 넣음 |
| Qwen3-14B | 기준점만 | 라이트 등급에서 10.2GB — **10GB 대상에 물리적으로 안 들어감** |

Qwen3-8B는 공식 저장소(Qwen/Qwen3-8B-GGUF)에서 받아 GGUF 서명 확인 후
`server/models/qwen3-8b/`에 두었다(이 Mac에 남겨 둠 — 재검증용).

## 2. 결과 — 세 모델 나란히

측정은 전부 같은 문항·같은 데이터. 흔들림을 가르려고 갈린 문항은 **각 3회** 반복했다(18회 전부 안정 — 흔들림 0).

| | **7.6B (현 출하)** | **Qwen3-8B** | 14B (기준점) |
|---|---|---|---|
| 게이트 | 30/33 | 30/33 | **31/33** |
| 「매뉴얼 어디 있더라」(search) | ✗✗✗ | **✓✓✓** | ✓ |
| 「내가 넣은 자료 뭐뭐」(kb) | ✗✗✗ | ✗✗✗ | ✓ |
| 「SQL 인젝션 뭐부터」(remediation) | **✓✓✓** | ✗✗✗ | ✓ |
| 정리본 값 보존(한 번에) | 65% | 50% | 안 잼 |
| 정리본 값 보존(두 번 나눠) | **95%** | 50% | 안 잼 |
| 메모리(ctx 8192, RSS) | **5.53GB** | 5.88GB | 10.20GB |

### ★ 숫자보다 중요한 것 — 실패의 **모양**이 다르다

「내가 넣은 자료 뭐뭐 있어?」에서 둘 다 도구를 안 불렀지만:

- **7.6B**: 어물어물한 일반 답 (나쁘지만 무해)
- **Qwen3-8B**: **문서 대장을 통째로 지어냈다** —
  > "현재 입력된 문서: **CrowdStrike 위협 보고서** · 등록일 2026-08-06 · 분류 위협 분석 ·
  > 접점: 우리 지식 그래프의 위협 모니터링 항목과 연결됨"

  이 DB에 그런 문서는 없다. 등록일·분류·그래프 연결까지 **전부 조작**이다.
  담당자가 이 답을 믿으면 없는 문서를 찾아 헤맨다 — 이 제품에서 가장 나쁜 실패 모양이다.

## 3. 판정 — **현 모델(7.6B) 유지**

- 게이트 총점은 같고(30/33), 이기고 지는 문항이 갈릴 뿐이다.
- 정리본에서 **크게 진다**(95% vs 50%).
- 실패할 때 **조작하는 성향**이 관찰됐다(1회지만 방향이 나쁘다).
- 메모리도 0.35GB 더 쓴다.

**「기능이 온전히 가능한」 모델은 14B이고, 그건 24GB 기계다** — 즉 모델의 문제가 아니라
**등급의 문제**다. 기능 등급 가이드의 「더 크게 쓰면 좋아지는 부분」이 정확히 이 자리다:
> 라이트를 24GB에 깔면 14B로 라우팅이 좋아진다(29→31). 기능 수는 그대로다.

남은 라우팅 2건(kb·search/remed)은 모델 교체가 아니라 **win의 도구 설명 다듬기 + 별칭 연결**로
되찾는 것이 맞다(도구 13개뿐이라 프롬프트에 자리가 넉넉하다).

## 4. 덤 — 측정 중 「긴 프롬프트 즉시 실패」가 스왑 없이도 재발

모델 재적재가 전혀 없는 구간에서 같은 고장이 났다가 ~10분 뒤 저절로 나았다.
인계_win_모델교체후_긴프롬프트실패 §5에 정정으로 붙였다 — 스왑은 방아쇠 중 하나일 뿐,
뿌리는 「잠시 걸리는 상태」다.


---

## 5. ★ 이 문서는 대장이다 — 닫힌 결론이 아니다 (사장님 결정 2026-08-13)

「LLM 모델은 기능 테스트 간 같이 찾아본다.」 새 후보가 생기면(카탈로그 추가 · 새 공개 모델 ·
새 한국어 모델) 아래 절차로 재서 §2 표에 **줄을 더한다.** 갈아탈지는 표가 정한다.

### 후보 측정 절차 (이번에 쓴 그대로)

1. **라이선스 먼저** — 파는 제품이다. 비상업(EXAONE류)은 여기서 끝.
2. **공식 저장소에서** 받는다(개조본 차단 — modelcatalog.ts의 교훈). GGUF 서명 확인.
3. **출하 구성 그대로** 잰다: 라이트 진입점 · 빈 DB · ctx 8192 · 동봉 llama-server.
   ⚠ 스왑으로 바꾸지 말고 `GIJO_DEFAULT_MODEL_ID`로 **처음부터 그 모델로 부팅**
   (긴 프롬프트 걸림 인계 참조).
4. 잴 것 넷: **게이트 33** · 갈린 문항은 **각 3회**(흔들림 가르기) · **정리본 값 보존**(진리표
   40개) · **메모리 RSS**. 점수 옆에 모델 이름을 항상 적는다.
5. **실패의 모양**을 본다 — 점수가 같아도 「어물어물」과 「조작」은 다르다(8B의 CrowdStrike
   조작 사례). 조작 성향이 보이면 점수와 무관하게 감점이 크다.

### 지켜볼 후보 (2026-08-13 기준)

- Qwen3 계열 후속(Apache) — 14B가 기준점이라 같은 계열 신형이 나오면 1순위
- 한국어 특화 신형 중 **Apache/MIT** 라이선스가 나오면 (지금은 EXAONE뿐이라 비상업에 막힘)
- gijo 자체 합성 모델의 후속(merged-lily 계열) — win의 학습 파이프라인 산출물
