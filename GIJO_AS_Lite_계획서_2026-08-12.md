# GIJO AS Lite — 계획서 (사장님 메뉴 9개 기준)

**작성 2026-08-12 (`max` 세션)** · **상태: 계획 · 구현 전**
**앞선 문서** `GIJO_AS_라이트_사양_2026-08-10.md`(방향·근거) · `GIJO_AS_지식그래프_LLM위키_도입계획.md`(ⓒⓐⓑ 세 조각)
**분담 2026-08-12**: 스탠다드는 `win`, 라이트는 `max`.

---

## 0. 한 줄 판단

> **메뉴 9개는 기존 라이트 사양(도구 8개)을 넓힌다.** 새 메뉴 셋(스캐너·법령·장비 등록부)이
> 사양에서 뺐던 자리다. **메뉴를 줄이자는 말이 아니라 값을 다시 치자는 말이다** — 셋 다 넣는
> 길이 있고, §2가 그 길이다. 값을 쳐 보니 **도구 8→13, 카탈로그 감소 91%→84%로 끝났다.**

> ### ⚠ 2026-08-12 정정 — 이 문서가 처음에 든 근거가 틀렸다
>
> 「도구를 빼서 프롬프트를 91% 줄인 것이 8GB의 근거」라고 적었다. **아니다.**
> `llama.cpp`는 KV 캐시를 **`--ctx-size`만큼 미리** 잡는다 — 카탈로그가 25,769자든 4,056자든
> **잡히는 메모리는 같다**(`win` 지적). 등급을 정하는 것은 `TIER_COST + EMBED_COST`다.
>
> **그리고 8GB는 원리상 안 들어간다**(`win` 검산, `max` 재검산):
> ```
>  8GB: 필요 9.15GB(Metal) vs 쓸 수 있는 7.60GB → 안 들어감
> 10GB: 필요 9.15GB(Metal) vs 쓸 수 있는 9.50GB → 들어감 (여유 0.35GB — 빠듯하다)
> ```
> → **라이트 등급은 10GB다**(사장님 결정 2026-08-12). 아래 §3의 A/B/C 선택지는 그 전제로 다시 읽을 것.
>
> **다만 도구 감축의 값은 없어지지 않았다 — 자리가 바뀌었다.** §3-끝에 적었다.

---

## 1. 메뉴 9개 ↔ 지금 있는 것

| # | 사장님 메뉴 | 지금 화면 | 라이트에서 |
|---|---|---|---|
| 1 | 대시보드 | `dashboard.html` | ⚠ **다시 만들어야 한다** — 지금 대시보드는 회사 자산·취약점 집계다. 혼자 쓰는 PC엔 그 데이터가 없다 |
| 2 | 작업 내역 | `records.html` | ✅ 거의 그대로 — 개인 할 일(`add_task`·`complete_task`)에 맞는 크기 |
| 3 | 로그파일 분석 + 공격경로 | `loganalysis.html` | 🔶 분석은 있다. **첨부 자산 연계·공격경로**가 새 일 (§4 Graphify) |
| 4 | 스캐너 | `vulnscan.html`·`hardening.html` | ⚠ **사양에서 뺐던 것** — 되살리는 결정이 필요 (§2-①) |
| 5 | 보안 장비 등록부 | `inventory.html` | 🔶 **뜻이 다르다** — 자산 관리가 아니라 「매뉴얼 넣고 찾기」면 RAG로 된다 (§2-②) |
| 6 | 법령 판례 조회 | `lawlookup.html` | ⚠ **사양에서 뺐던 것** — 외부망이 필요하다 (§2-③) |
| 7 | 제품 소개자료 | `products.html` | ✅ 번들 문서로 된다 — 회사 데이터 불필요 |
| 8 | 나만의 기억 (RAG·Graphify·위키) | `memory.html`·`docbox.html` | 🔶 RAG는 있다. **그래프·위키가 새 일** (§4) |
| 9 | 나만의 메모장 (Smart MD Studio) | 없음 | ⚠ **별도 제품** — 포함이냐 연동이냐 결정 필요 (§6-④) |

✅ 4개는 거의 그대로 · 🔶 3개는 뜻을 좁히면 됨 · ⚠ 나머지가 결정 대상.

---

## 2. ⚠ 기존 사양과 부딪히는 곳 셋

기존 사양이 뺀 69개 도구에는 **이유가 넷** 있었다(회사 데이터·외부 통신·다인 협업·운영자용).
새 메뉴 셋이 그중 앞의 둘에 걸린다.

### ① 스캐너 (메뉴 4) — 「회사 데이터가 있어야 돈다」에 걸렸던 것

빠졌던 이유는 스캔 **결과**를 놓을 자산 등록부가 없어서였다. 사장님 안은
**「스캔 결과를 첨부파일 자산에서 확인」** — 즉 등록부 대신 **첨부 파일**을 그릇으로 쓰자는 것이다.
**그러면 성립한다.** 자산 11개 도구 없이 스캐너 1~2개만 살릴 수 있다.

남는 물음: **어디까지 스캔하나.**
- 자기 PC만 → 권한 문제 없음, 값은 작음
- 같은 망 대역 → 담당자 PC에서 사내망 스캔은 **권한·정책 문제**가 붙는다(허가 없는 스캔은 사고다)
- ▶ 「본인 PC + 사용자가 명시한 대상만」이 안전한 선. 대상 입력을 사람이 하게 한다.

### ② 보안 장비 등록부 (메뉴 5) — 뜻이 둘로 갈린다

- **자산 관리**로 읽으면: 등록부·담당자·조치 검증이 딸려 와 라이트를 넘는다.
- **「매뉴얼 등록해서 특정 내용 확인」**으로 읽으면: 그건 **문서함 + RAG**다. 이미 있다.
- ▶ 사장님 문장은 뒤쪽이다. **그렇게 좁히면 새로 만들 게 거의 없다** — 문서에 「장비」 딱지만 붙이면 된다.

### ③ 법령 판례 조회 (메뉴 6) — 「외부 통신이 필요하다」에 걸렸던 것

기존 사양이 뺀 유일한 **외부망** 항목이다(`law_lookup`이 국가법령정보센터를 부른다).
사장님 안은 **「개인이 계정받아서 등록」** — 외부망을 **허용하는 쪽**이다. 이건 방향 전환이다.

⚠ 걸리는 것:
- **온프렘·폐쇄망 원칙과 부딪힌다.** 스탠다드는 「우리 데이터는 우리 안에만」이 파는 말이다.
  라이트만 밖으로 나가면 **같은 제품의 두 얼굴**이 된다 — 설명이 필요하다.
- 담당자 PC가 폐쇄망이면 계정이 있어도 못 쓴다. **꺼져도 제품이 도는 형태**여야 한다.
- ▶ 안전한 형태: **기본 꺼짐 · 사용자가 계정을 넣으면 켜짐 · 꺼져 있으면 번들 문서로 답한다.**
  번들에 `보안규제_법적근거_모음.md`가 있어 **끈 상태에서도 법규 질문은 답이 된다**
  (`registry.ts:1509`가 정확히 이 원칙을 적어 뒀다 — 「꺼진 기능이 멀쩡하던 답을 빼앗으면 안 된다」).

---

## 3. 리소스 — 8GB가 되나 (2026-08-12 `max` 실측)

### 방금 잰 값

```
라이트 모델 단독(gijo-main-orchestrator 7.6B) + 임베딩(bge-m3)
  적재 직후      7.96GB      ← 아직 재지 않는다(RSS 감쇠 함정)
  예열 1회 뒤    8.62GB   (55.6초)
  예열 2회 뒤    8.72GB   (21.2초)   ★ 측정값
  14B 내린 뒤 바닥 1.12GB (임베딩만)
```

### ⚠ 이 8.72GB를 「라이트는 8.7GB」로 읽으면 안 된다

**이 기계가 ctx 16384로 띄웠기 때문이다.** 코드의 라이트 티어가 아직 그렇다:

```
localengine.ts:113
  { id:"lite", vramLabel:"12GB급", minVramGb:12, ctxSize:16384, ... }
```

**사장님이 「라이트는 8GB, 12GB 삭제」를 정하셨는데 코드에 안 들어갔다.**
사양서의 8GB급 근거(Metal 6.45 / CUDA 6.48)는 **ctx 8192** 전제다.

| 구성 | 메모리 | 대화 길이 |
|---|---|---|
| 7.6B @ **ctx 8192** + bge | **6.45GB**(사양서) | 짧다 — 긴 문서 요약에 제약 |
| 7.6B @ **ctx 16384** + bge | **8.72GB**(오늘 실측) | 여유 있음 · **8GB 기계엔 안 들어간다** |

▶ **8GB로 가려면 ctx 8192로 내려야 한다.** 그리고 그건 「대화가 짧다」는 불만이 나올 자리라고
사양서가 미리 적어 뒀다. **메뉴가 9개로 늘면 그 압력이 더 커진다** — 화면이 많을수록
화면 설명·도구 카탈로그가 프롬프트를 먹는다.

### 메뉴 9개가 프롬프트에 미치는 값

기존 사양의 8GB는 **도구 8개 · 카탈로그 1,253자**가 전제였다(현행 77개·13,674자 대비 91% 감소).
새 메뉴 셋이 도구를 되살리면 그만큼 되돌아간다.

**2026-08-12 실측(추정 아님)** — `registry.ts`에서 실제로 세었다:

| | 도구 수 | 카탈로그 |
|---|---|---|
| 본 제품 | 78 | 25,769자 |
| 라이트 (확정 목록) | **13** | **4,056자 · 84% 감소** |

### ⚠ 그런데 이 감소는 VRAM을 안 줄인다

`llama.cpp`는 KV 캐시를 **`--ctx-size`만큼 미리** 잡는다. 카탈로그가 몇 자든 **잡히는 메모리는 같다.**
「91% 감소가 8GB의 근거」는 그래서 틀렸다(§0 정정).

**그럼 도구를 왜 줄이나 — 자리가 바뀌었을 뿐 값은 더 크다:**

| 버는 것 | 왜 결정적인가 |
|---|---|
| **ctx 안의 자리** | 78개 카탈로그가 실측 **7,984토큰**인데 라이트 ctx는 **8192**다. 프롬프트 하나가 문맥의 **97%**를 먹으면 RAG 조각도 대화 이력도 들어갈 자리가 없다. 13개(≈1,270토큰)로 줄여야 **ctx 8192가 성립한다** |
| 프리필 시간 | Metal 프리필 ≈3.0ms/토큰 — 7,984토큰이면 그것만 **24초**다. 1,270토큰이면 4초다 |

▶ **즉 도구 감축은 등급을 낮추는 수단이 아니라, 낮춘 ctx를 쓸 수 있게 하는 수단이다.**
　「프롬프트는 병목이 아니라 ctx가 병목」이라는 결론은 그대로다 — **근거가 바뀌었을 뿐 방향은 맞았다.**

### 결정 (2026-08-12) — **10GB급 · ctx 8192**

사장님이 C안(8GB + 문맥 절약)을 고르셨는데, **8GB는 산수가 안 맞아 10GB로 확정됐다**(`win` 검산 · `max` 재검산):

```
 8GB: 필요 9.15GB(Metal) vs 쓸 수 있는 7.60GB(여유계수 0.95) → 안 들어감
10GB: 필요 9.15GB(Metal) vs 쓸 수 있는 9.50GB            → 들어감 (여유 0.35GB · 빠듯)
```

`tierFits`가 임베딩을 **등급과 무관하게 항상** 더한다. 8GB에선 채팅에 5.1GB만 남아 7.6B급이 원리상 안 들어간다.

⚠ **다만 이 판정은 `TIER_COST.lite`(cuda 6.5 · metal 7.6)를 쓰는데, 그 값은 ctx 16384 때 것이다.**
`max`가 오늘 ctx 16384에서 잰 채팅 단독 7.60GB와 정확히 같다. ctx를 반으로 줄였으니 실제로는 더 낮다 —
사양서 §7은 ctx 8192에서 **5.45GB**로 적어 뒀다. `win`이 일부러 안 내렸는데 그 판단이 맞다:
**낮춰 잡으면 안 들어가는 기계에 권하게 된다.** 재측정 뒤에 내린다(§8).

▶ **8GB로 정말 내리려면 임베딩을 작은 모델로 바꾸는 것이 답이다**(`win` 제안 — 상주 유지, 6.5+0.5=7.0).
　상주를 없애는 길은 비싸다: 임베딩 상주가 채팅 적재 순서의 기준점이고, 없애면 첫 RAG 질문이 최대 60초다.

### ⚠ C안의 절반이 보류됐다

C안은 둘로 되어 있었다 — ① 도구 감축 ② **화면 설명을 프롬프트에서 빼 문서로 밀기**.
②는 `screenguide.ts` 합류점이 필요한데 **보류됐다**(사장님 결정: 라이트 때문에 본 제품 답 품질을
바꾸는 건 순서가 거꾸로다). 타당하다.

그래서 지금 라이트는 **「ctx 8192 + 도구 13개」**다 — 실질적으로 A안에 가깝다.
①만으로 얼마나 되는지는 §3 위 표가 답한다: 카탈로그가 7,984토큰 → ≈1,270토큰으로 줄어
**ctx 8192의 84%가 실제 일에 쓰인다.** ②가 없어도 성립한다. 필요해지면 그때 본 제품 방향으로 올린다.

---

## 4. RAG · Graphify · LLM 위키 — 라이트에서 어떻게 쓰나

`GIJO_AS_지식그래프_LLM위키_도입계획.md`의 판단이 그대로 유효하다:

> **우리는 이미 그래프를 갖고 있다**(온톨로지 2,186 트리플). Graphify를 통째로 들이는 것은 실익이 적다.

라이트에 맞춰 옮기면:

| 조각 | 라이트에서 | 메뉴 |
|---|---|---|
| **RAG** (있음) | 그대로 핵심. 임베딩 1.12GB는 **양보 불가**다 — RAG를 끄면 메뉴 3·5·7·8이 다 죽는다 | 3·5·7·8 |
| **그래프 ⓒ**(검색이 그래프를 쓰기) | 🔶 **공격경로 분석의 실체가 이것이다**(메뉴 3). 다만 도입계획 §5가 **접점이 데이터에 거의 없다**고 적어 뒀다(문서 트리플 3개) | 3 |
| **그래프 ⓐ**(보여 주기) | ✅ 라이트에 잘 맞는다. 개인 문서 사이의 연결을 그림으로 — **팔리는 그림**이다 | 8 |
| **LLM 위키 ⓑ**(정리본) | 🔶 「나만의 기억」에 딱 맞는다. 넣은 문서를 정리본으로 만들어 두면 ctx가 짧아도 답이 는다 | 8 |

⚠ **ⓑ가 §3의 ctx 문제를 덜어 준다.** 긴 원문을 매번 넣는 대신 정리본을 넣으면 토큰이 준다.
**8GB 고수(안 A)를 택하실수록 ⓑ의 값이 커진다.**

⚠ **Graphify 자체를 들이는 것은 여전히 반대다.** 그건 **코드베이스용**이고 우리는 보안 데이터다.
쓸 것은 「그래프 형태로 보여 주고 검색이 쓰게 한다」는 **발상**이지 그 도구가 아니다.

---

## 5. 메뉴별 품 (거칠게)

| # | 메뉴 | 새로 만들 것 | 품 |
|---|---|---|---|
| 2 | 작업 내역 | 거의 없음 | XS |
| 7 | 제품 소개자료 | 번들 문서 정리 | XS |
| 5 | 장비 등록부(=매뉴얼 문서함) | 문서에 「장비」 딱지 | S |
| 1 | 대시보드 | **개인용으로 새로** — 내 문서·내 할 일·최근 분석 | M |
| 6 | 법령 판례 | 계정 입력 UI + 꺼짐 기본값 + 폴백 | M |
| 4 | 스캐너 | 스캔 대상 입력 + 결과를 첨부로 | M |
| 3 | 로그 분석 + 공격경로 | 분석은 있음. **그래프 연계가 새 일**(ⓒ) | L |
| 8 | 나만의 기억 | RAG 있음. **ⓐ 보여주기 + ⓑ 위키가 새 일** | L |
| 9 | 나만의 메모장 | **별도 제품 연동** — 형태 미정 | ? |

---

## 6. 미결 — 사장님이 정하실 것

**① 메모리 등급 (§3)** — ✅ **닫힘: 10GB급 · ctx 8192**(2026-08-12).
　코드 반영됨(`localengine.ts` — `win`, `fd7d159`). C안의 ②(화면 설명 밀어내기)는 보류 — §3 끝.

**② 법령 조회의 외부망 (§2-③)** — 켜나 / 기본 꺼짐+선택 / 아예 뺌
　⚠ 온프렘 원칙과 부딪히는 유일한 자리다. **「기본 꺼짐 + 꺼져도 번들로 답함」**을 권한다.

**③ 스캐너 범위 (§2-①)** — 본인 PC만 / 사용자가 지정한 대상까지 / 대역 스캔
　⚠ 허가 없는 사내망 스캔은 사고다. **「본인 PC + 명시 대상」**을 권한다.

**④ Smart MD Studio (메뉴 9)** — 라이트에 **포함**이냐 **연동**이냐
　⚠ 포함이면 두 제품의 배포·버전이 묶인다. 연동이면 없어도 라이트가 돌아야 한다.
　▶ 사장님이 따로 업그레이드 중이시라니 **연동**이 안전해 보인다 — 서로의 일정에 안 묶인다.

**⑥ 8GB를 위해 RAG를 빼는 안 — `max` 의견: 반대** (2026-08-12, 화면 9개를 다 지은 뒤)

`win` 제안: 「8GB가 꼭 필요하면 임베딩을 손대는 대신 **라이트는 RAG 없이 시작하고 필요할 때 켠다**가 더 정직하다.」
정직한 제안이고 그래서 진지하게 봤다. **다만 화면을 다 지어 보니 답이 갈렸다.**

**RAG를 끄면 9개 중 무엇이 남나 — 세어 봤다:**

| 화면 | RAG 없으면 |
|---|---|
| 5 장비 매뉴얼 · 7 제품 소개 · 8 나만의 기억 | **완전히 죽는다** — 화면 전체가 RAG다 |
| 6 법령 | **꺼진 채로 답하는 길이 사라진다** → 외부망을 강제하게 되어 온프렘 원칙이 무너진다 |
| 3 로그분석 | 「이게 무슨 뜻인지 찾기」가 죽는다 — 로그 뷰어가 된다 |
| 1 대시보드 | 반쪽 |
| 2 작업내역 · 4 스캐너 | 영향 없음 |

▶ **남는 것은 할 일 관리와 PC 점검 둘뿐이다.** 그건 GIJO AS가 아니다 — 그 둘은 무료 도구가 널려 있다.
　임베딩은 Metal 1.25GB · CUDA 2.5GB다. **그 1.25GB가 9개 중 6개를 살린다.**

**세 갈래에 대한 판단**

| 안 | 판단 |
|---|---|
| **10GB 유지** | ✅ **권한다**(지금 결정 그대로) |
| 작은 임베딩 모델 | 🔶 조건부 — 아래 |
| RAG 없이 시작 | ✗ 반대 |

⚠ **작은 임베딩을 볼 때는 판정 기준을 바꿔야 한다.** 「메모리가 줄었나」가 아니라
**「구어체 질문에서 검색 거리가 얼마나 나빠지나」**다(2026-08-12 `win`의 측정법 그대로).
말투가 이미 0.19를 밀어내는데 임베딩을 약하게 하면 **그 격차가 더 벌어진다.**
그리고 라이트 사용자가 정확히 **그 구어체로 묻는 사람들**이다 —
8GB를 사려다 라이트의 답 품질을 파는 셈이 될 수 있다.

⚠ **재 보지 않은 의심 하나(추정이다):** 8GB는 사양이 아니라 **연식** 문제일 수 있다.
2026년에 새로 사는 담당자 PC는 대개 16GB고, 8GB 기계는 3~5년 된 것이다.
그런 기계는 CPU·GPU도 느려 **메모리를 맞춰도 7.6B가 실용 속도가 안 날** 수 있다.
▶ 8GB를 사업적으로 꼭 해야 한다면, 메모리보다 **그 기계에서 답이 몇 초에 나오는지**를
　먼저 재는 것이 순서다. 그게 진짜 판정선일 수 있다. **이건 아직 아무도 안 쟀다.**

**⑤ 번들 문서 19건을 라이트에도 그대로 넣나** (기존 사양 §9-4에서 넘어온 미결)
　본 제품 기능 설명이 섞여 있다. 라이트에 없는 기능을 설명하면 **없는 걸 있다고 말하는 셈**이다.

---

## 7. 착수 순서 (①이 정해진 뒤)

1. **코드의 `lite` 티어를 결정에 맞춘다** — `localengine.ts:113`(지금 12GB·16K). 두 플랫폼 각각 재고 큰 쪽으로.
2. **화면 9개 골격** — 2·7·5 먼저(품이 작고 바로 보인다). 대시보드는 개인용으로 새로.
3. **ⓑ LLM 위키** — ①에서 A나 C를 고르셨으면 여기가 ctx를 되사는 자리다.
4. **ⓐ 그래프 보여 주기** — 팔리는 그림. 메뉴 8에 붙인다.
5. **ⓒ 검색이 그래프를 쓰기** — 메뉴 3(공격경로)의 실체. 도입계획 §5의 C-0(접점 쌓기)부터.
6. **스캐너·법령** — 결정 ②③이 나온 뒤.
7. **전용 게이트** — `tools/evalgate/cases/lite-routing.json` 24문항 초안이 있다(구현 전이라 아직 돌리지 말 것).
   ▶ 2026-08-12에 만든 **음성 문항 축**을 라이트에도 둔다. 라이트는 분기가 적어 덜 위험하지만,
   **「없는 기능을 있다고 답하는 것」**이 라이트의 고유 위험이라 그게 음성 문항 1번이어야 한다.

---

## 8. 재는 법 — 그리고 **`max`에서는 재지 말 것**

### ⚠ 2026-08-12: `max`에서 세 번 재서 세 번 다 무효였다

| | 무엇을 놓쳤나 |
|---|---|
| 1차 | 티어가 `standard`라 **ctx 16384**로 떴다. 8.72GB는 라이트 구성이 아니었다 |
| 2차 | 티어를 바꾸면 풀이 **`defaultModelId`**로 재기동된다 — 내가 바꾼 에이전트 배정이 아니다. `qwen3-14b`가 떴다 |
| 3차 | 배정이 여전히 `qwen3-14b`라 **예열 dispatch가 14B를 같이 띄웠다**(7.27 → 20.37GB) |

세 번 다 가드가 잡아 **틀린 숫자를 표에 넣지는 않았다.** 그런데 세 번 다 **다른 변수**였다.
그게 결론이다:

> **32GB 기계에서 8~10GB 구성을 흉내 내 재는 것 자체가 함정이다.**
> 티어·배정·풀·`defaultModelId`가 각각 따로 놀아서, 하나를 맞추면 다른 하나가 어긋난다.
> **라이트 메모리는 라이트 티어가 「진짜」인 기계에서 잰다** — `gb10`이나 실제 8~10GB 기계.

### ✅ 그런데 재는 길이 있었다 — **llama-server를 직접 띄운다**

`win`이 짚어 줬다: **티어를 흉내 낼 필요가 없다.** 제품이 쓰는 인자 그대로 llama-server를
따로 띄우면 티어·배정·풀·`defaultModelId`가 아예 개입하지 않는다.
세 번의 실패가 전부 **제품 상태를 맞추려 한 것**이었다.

```bash
# 제품이 쓰는 모양 그대로. 포트만 비켜 준다.
server/llama.cpp/build/bin/llama-server \
  -m models/gijo-main-orchestrator/gijo-main-orchestrator.gguf \
  -ngl -1 --ctx-size 8192 --parallel 2 --port 8280
# 뜬 PID의 RSS만 본다: ps -o rss= -p <PID>
```

**2026-08-12 실측 (Metal · `max`)**

| | 값 |
|---|---|
| 적재 직후 | 5.57GB ← **표에 넣지 말 것** |
| 예열 1·2·3회 후 | 5.58 · 5.59 · **5.59GB** |
| **채팅 단독 @ ctx 8192** | **5.59GB** |

▶ **예열해도 안 늘었다**(+0.02GB). ctx 16384에서는 예열이 0.76GB 올렸었다 —
　**KV 캐시를 미리 잡으니 ctx가 작으면 나중에 늘 자리도 작다.** 「프롬프트 감축 ≠ VRAM 감축」의 재확인이다.
▶ `gb10`의 CUDA 실측 5.77GB와 거의 같다(`win`). 두 플랫폼이 ctx 8192에서는 다시 붙는다.

**등급 판정 (여유계수 0.95)**

```
 8GB(쓸 수 있는 7.60)   Metal 5.59+1.55 = 7.14 들어감 | CUDA 5.9+2.5 = 8.40 안 들어감
10GB(쓸 수 있는 9.50)   Metal 7.14 들어감            | CUDA 8.40 들어감
```

▶ **Metal만 보면 8GB가 되지만 CUDA가 막는다. 10GB가 맞다** — `win`의 판정 그대로다.
▶ `EMBED_COST.metal` 표값 1.55 vs 이 기계 실측 1.25 — **표값이 보수적이라 그대로 두는 것이 맞다.**

### ⚠ 2026-08-12 저녁 정정 — **내가 잰 인자가 라이트 인자가 아니었다**

앞의 5.59GB는 `--parallel 2`를 붙이고 잰 값이다. **라이트는 그 인자를 안 쓴다.**
`localengine.ts`의 `확인용칸()`이 막는다 — 내가 오늘 아침에 넣은 가드다:

```js
const 칸당_최소문맥 = 12288;
const 칸당 = Math.floor(fittedCtx / 2);
if (칸당 < 칸당_최소문맥) return [];      // ctx 8192 → 칸당 4096 → --parallel 안 붙임
return ["--parallel", "2"];
```

**같은 모델·같은 ctx로 인자만 바로잡아 다시 쟀다(Metal):**

| 인자 | 채팅 단독 | 슬롯당 문맥 |
|---|---|---|
| `--ctx-size 8192 --parallel 2` (내가 쓴 것) | 5.59GB | 4096 |
| `--ctx-size 8192` (**라이트가 쓰는 것**) | **4.27GB** | **8192** |

▶ **1.32GB 차이다.** 그리고 문맥도 4096이 아니라 8192를 온전히 쓴다.

**등급 판정이 달라진다:**
```
 8GB(쓸 수 있는 7.60)  Metal 4.27 + 1.55 = 5.82 → **들어감 (여유 1.78)**
                       CUDA  5.90 + 2.50 = 8.40 → 안 들어감  ← win 값(같은 인자 문제일 수 있다)
```

⚠ **CUDA를 다시 재야 결론이 난다.** `win`의 5.9도 내가 준 「제품 인자 그대로 `--parallel 2`」로
잰 것이면 같은 만큼 부풀어 있다. 같은 비율이면 CUDA도 8GB에 들어간다(추정 7.08) —
**추정이지 측정이 아니다.**

▶ 이 정정이 §6-⑥(8GB·RAG)의 전제를 바꾼다: **RAG를 빼지 않고도 8GB가 될 수 있다.**
　「RAG를 뺄까」를 논하기 전에 **CUDA를 바른 인자로 다시 재는 것**이 먼저다.

⚠ **교훈**: 「제품 인자 그대로」라고 적어 놓고 **제품이 실제로 쓰는 인자를 안 봤다.**
　`ps`로 본 것은 standard 티어(ctx 32768)의 인자였고, 라이트는 가드 때문에 다르다.
　**같은 코드가 티어마다 다른 인자를 낸다** — 그걸 놓쳤다.

### 잴 때 지킬 것

- **예열 2회 뒤에 잰다.** 적재 직후 7.96 → 예열 뒤 8.72로 **0.76GB 늘었다.**
- **혼자 띄우고 잰다.** 다른 모델이 같이 떠 있으면 무의미하다.
  ⚠ **에이전트 배정까지 바꾼다** — 안 바꾸면 예열 dispatch가 옛 모델을 다시 띄운다(3차 실패).
- **ctx를 함께 적는다.** 숫자만 적으면 다음 사람이 다른 구성에서 비교한다.
- **인자를 코드에서 확인한다** — 티어마다 다르다(`확인용칸`). `ps`로 본 다른 티어의 인자를 베끼면 틀린다.
- **두 플랫폼을 각각 잰다.** 같은 14B가 Metal 32K에서 16.0GB인데 CUDA는 13.2GB다(21% 차이).
  10GB급은 여유가 0.35GB뿐이라 그 차이가 바로 등급을 밀어 올린다. CUDA는 `tools/light-cuda-measure.sh`.
