# 3에디션 QA 결과 보고서 — 라이트 · 표준 · 프로

> 2026-08-19 밤샘 실측. 지시: 메뉴별 시나리오 → 순차 시험 → 문제는 추천으로 처리 → 장단점 분석.
> 조건: **가로 1920×1032 + 세로 1080×1872**(주모니터가 세로 — 지금까지 한 번도 안 잰 조건).
> 방법: 실제 앱(CDP)에서 화면마다 열고·누르고·재기. 판정식이 틀렸던 곳은 오탐으로 자인하고 기록.

---

## 1. 한눈에

| | 라이트 | 표준 | 프로 |
|---|---|---|---|
| 범위 | 10화면 × 2크기 + 대화 실답 | 고정 3 + 허브 판 17 × 2크기 + 새 기능 동선 | 셸 동선 7 + 2크기 |
| 결과 | **깨짐 0** (수리 2건은 소스 확인으로 발견) | 가로 깨짐 0 · **세로에서 6건 수리** | **7/7 통과** |
| 남긴 것 | 없음 | 경미 잔여 3(6~11px 내부) | 팝업 전환 난제 1(별항) |

**수리 총 8건 + 오늘 낮 3건(undo 감사·5.25.1 게시·프로 문구) = 11건이 이 회차에서 닫혔다.**
서버 시험 3,670개 전부 통과 상태로 마감(커밋 7b606186).

---

## 2. 핵심 발견 — 세로 모니터가 처음 드러낸 것

주모니터가 **세로(1080px 폭)**인데 지금까지 측정은 전부 가로(1280~1920)였다.
세로에서 허브 무대 폭이 **644px**로 줄자, `@media`가 없는 화면들의 고정열이 그대로 버텨 가로로 넘쳤다.

| 화면 | 뿌리 | 수리 |
|---|---|---|
| 보안 지표(kpi) | hero 300px 고정열 + 3열 headline, @media 0개 | @media 신설 → 1열 ✅재검증 |
| 취약점(vulnscan) | 히트맵 옆판 300px 고정 | 세로 쌓기(잔여 6px 경미) |
| 지식(memory) | dm-wrap 320px 고정열 | 1열(잔여 내부 클리핑 경미) |
| 안전장치(redteam) | cols 340px·kpis 4열 + **select 인라인 min-width:280px** | 세로 + !important 완화 |
| 학습(learnloop) | 긴 무공백 문자열이 카드 밀어냄 | overflow-wrap:anywhere(잔여 10px) |
| 대시보드 | **SVG 인라인 font-size 10·8.5** — CSS 소스 감시(uireadability)가 원리상 못 보는 자리 | 11로 ✅재검증 0곳 |

라이트 2건은 시나리오 설계 단계의 소스 대조가 찾았다(자동 렌더 검사로는 안 잡히는 유형):
- `lite-loganalysis` — 취약점 결과가 쓰는 `.run`·`.guard` CSS가 **없어** 경고가 경고로 안 보임
- `lite-records` — 10화면 중 유일하게 `max-width` 없음(넓은 화면에서 혼자 늘어남)

---

## 3. 에디션 장단점 분석 (실측 기반)

### 🪶 라이트 — 「혼자 쓰는 판」
**강점**
- 10화면 전부 900px 폭 통일이라 **가로·세로 어디서도 안 깨진다**(이번 실측 깨짐 0).
- 화면이 적고 단선적이라 첫날 헤맬 자리가 없다. 빈 상태 안내가 「뭘 하면 채워지는지」까지 말한다.
- 대화 → 작업 내역 → 대시보드가 한 줄로 이어진다(실측: 물으면 내역·최근활동이 같이 채워짐).

**약점**
- 세로 1872px에서 **아래 절반이 빈다**(카드가 400~700px뿐). 깨진 건 아니지만 「덜 만든 화면」 인상.
- 가로 1920px에서 900px만 쓰고 오른쪽이 빈다 — 밀도가 낮다.
- 화면에 지시 입력칸(할 일 담기)이 있다 — 표준의 「지시는 대화창에서만」 원칙과 다른 예외. 유지할지 판단 필요.

### 🏢 표준 — 「팀이 함께 쓰는 판」
**강점**
- 허브+판 구조가 가로에서 밀도가 가장 좋다(한눈에 띠 + 무대).
- 이번에 넣은 ⓪자산·🗂범위·작업내역 축이 실측에서 전부 동작(범위 걸면 취약점 54→1줄 + 「53건 숨김」 띠).
- 못 거르는 화면이 「전체를 보여 줍니다」라고 스스로 말한다 — 거짓 없는 상태.

**약점**
- **세로 모니터에 약했다**(이번 수리 6건 전부 표준 판). 화면들이 가로 전제로 설계돼 있었다.
- 허브 안 판이 2단 iframe이라 배관(범위 중계 등)이 한 단계씩 더 필요 — 이번에 실제로 그 단계가 빠져 수리했었다.
- 상시 클릭 표적 41개 — 셋 중 조작 밀도가 가장 높다(익숙해지면 강점, 첫날엔 부담).

### 🚀 프로 — 「대화가 주인공」
**강점**
- 셸 동선 7/7 통과 — 전환·복귀·계정 생존·☰=Ctrl+K·💬 홈·챗봇 「☰ 전체 메뉴」 안내까지.
- **세로 모니터와 가장 잘 맞는다**: 대화 홈에서 대화가 1024px(95%) 전폭. 상시 표적 12개로 가장 적다.
- 화면은 표준과 같아 표준 수리가 그대로 적용된다(별도 유지비 없음).

**약점**
- **캔버스를 열면 세로에서 둘 다 좁다**: 캔버스 644 + 대화 380. 사장님이 팝업을 요구한 이유가 이 수치다.
- 팝업 전환의 기술 난제 1건(아래 4절) — 지금 배관이 별도 창에서 죽는다.

### 종합
- **세로 모니터 사용자에게는 프로(대화 중심)가 구조적으로 맞다.** 표준은 가로 전용에 가깝던 것을 이번에 세로 대응시켰다.
- 라이트는 안정성 최상, 밀도 최하 — 1인용 목적에 부합.
- 프로의 완성은 **팝업 배치**에 달렸다(다음 절).

---

## 4. 프로 팝업 전환 — 시안 완료, 착수 전 난제 1건

시안 `mockups/프로_팝업배치/` (추천 1개, 눌러볼 수 있음). 핵심 결정:
1. 화면을 고르면 탭이 아니라 **처음부터 별도 창** — 기존 `chat-home` 상태를 상시화(새 CSS 상태 안 만듦).
2. 결재판은 본창 한 곳에만 — 가려지면 `flashFrame`(작업표시줄 깜빡임)으로 알림. 강제 포커스 전환·결재판 복제는 비교 후 기각.
3. ★ **난제(정직 고지)**: `gijo:select`·`view`·`scope` 배관이 전부 `window.top`/`postMessage`에 묶여 있는데, **별도 창에서는 `window.top`이 자기 자신**이라 오늘 코드로는 전혀 안 된다(코드 10곳 전수 확인). 팝업 전환 = 이 배관을 IPC로 옮기는 일까지 포함. **이게 실작업의 대부분이다.**

---

## 5. 이 회차에 함께 닫은 것 (QA 외)

- **D2 되돌리기(undo)** — 남의 자산까지 지우고 감사에 안 남던 결함. 시각 좁히기 + 감사 기록 + 실행자 기록(시험 8개, 뒤집기 검증).
- **5.25.1 게시** — #셸 표식 + 「보안 태세」 판 정리(발견·수집 3판→2판, kpi는 ⑤보고에 유지).
- **프로 챗봇 문구** — 「사이드바」→「☰ 전체 메뉴」, 실응답으로 겹말(「메뉴의 메뉴」)까지 잡아 수리.
- max 인계 수신 — 라이트 1.1.11 출시, win 작업 6건 전부 라이트 안전 확인(회귀 0), 키체인 ACL 뿌리 해결.

## 6. 남긴 것 (다음 회차)

| 항목 | 크기 | 비고 |
|---|---|---|
| 프로 팝업 구현(배관 IPC화 포함) | **대** | 시안 승인됨 · 4절 난제가 본체 |
| 자료 유실 D3~D8 (6건) | 중 | 전부 CONFIRMED·수리안 확보(세션 일괄삭제·재등록 이력삭제·지식드롭·cleanup 스냅샷·백업 세션폴더·일정삭제) |
| D1 잔여 — 감사에 계정 신원 없음 | 소 | 수리안 확보(target에 username) |
| 세로 잔여 경미 3건 | 소 | 6~11px 내부 클리핑 |
| 라이트 재빌드(max) | — | win이 고친 라이트 2파일 반영 |
| 게시(5.25.2) | — | QA 수리 8건이 게시본에 없음 · 허락 대기 |

---
*커밋: 7b606186 · 시험 3,670 통과 · 상세 결과: QA결과_라이트/표준/프로_2026-08-19.md*
