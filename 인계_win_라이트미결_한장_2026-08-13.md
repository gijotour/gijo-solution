# 라이트 미결 — win 몫 한 장 (max, 2026-08-13)

인계가 여러 문서에 흩어져 있어 **win이 볼 것만** 한 장에 모은다. 각 항목은 코드로 상태를 재확인했다.
우선순위는 「출하에 걸리나 / 품질이 아프나」 기준.

## ⏳ 열린 것 (win)

| # | 항목 | 우선 | 상태(코드 재확인) | 상세 인계 |
|---|---|---|---|---|
| 1 | **정리본이 대화 이력 누적으로 초과** — 무상태 요약 길을 렌더러에 열기 | ★높음 | 재규명: 시스템 프롬프트가 큰 게 아니라 `/api/llm/chat`이 remember:true 강제 → 이전 정리본이 이력에 남아 2번째부터 초과. remember:false는 코드에 있으나 렌더러 미노출. fix 작음(stateless 옵션 or /summarize 라우트) | `인계_win_모델교체후_긴프롬프트실패` §7 |
| 2 | **법령 「유출 신고」에 시행령을 안 당김** | 중 | 조문 본문 API는 72시간 정상, 그런데 dispatch가 본법만 당겨 lt-seed-law 실패 | `인계_win_법령조문_본문누락` §7 |
| 3 | **라이트 모드 토글 다리+UI** | 중 | main.ts seam(gijo-edition.txt)은 열림. **preload 다리 0 · 설정 토글 0** — 켜고 끌 방법이 없다 | `인계_win_에디션포트_라이트모드옵션` |
| 4 | **샘플 자산 3개 이름에 「샘플」 없음** | 낮 | `assets.ts:506·518·527`(사내 보안 상담 챗봇·문서 민감도 분류 AI·이상행위 탐지 엔진) 그대로. 첫 실행 챗봇 검색에 자기 자산처럼 보임 | `인계_win_라이트앱이_본서버를_띄운다` §3 |
| 5 | **원격 LLM 런타임 (BridgeAI 1단계)** | 중 | URL이 모듈 상수라 런타임 변경 불가. `/api/llm/remote` 라우트 0. **사장님 VPN 전용 결정 났으니 착수 가능** | `인계_win_원격LLM_런타임설정_BridgeAI` |
| 6 | **Windows nsis 동봉** (CUDA llama + 모델 등록) | 중 | mac(dmg)은 완료. Windows는 미착수 — CUDA llama-server rpath류 함정 확인 필요 | `인계_win_에디션포트` §참고 |
| 7 | 버전 드리프트 | 낮 | 클라 `5.16.0` · 라이트 dmg `5.15.1`. **다음 라이트 재빌드가 자동으로 맞춘다**(조치 불필요, 참고) | — |

## ✅ win이 이미 처리한 것 (참고 — 다시 안 봐도 됨)

- 온톨로지 한글 별칭 연결(`4c25306`) — max 별칭표를 expandOntology에 물림 · **lt-onto-2 3/3 통과 확인**
- 법령 조문 파서 77% 누락(`768e33e`·`ab7d454`) · lawAnswer 번호없는 질문(`f7d4fcc`)
- 8GB/CUDA 재측정 → **10GB 확정**(`073f628`·`964bacf`)
- 에디션 포트 4곳(`81e04a7`) — max가 dmg#8에서 클라·서버 둘 다 7445 정합 확인
- 라이트 모드 seam(`82173fa`) · 긴 프롬프트 진단 로깅(`f57c2bd`) — **이 로깅으로 #1 근본을 규명함**
- 프로 포트 7446 공유(max 권고 채택)

## max 쪽 상태 (참고)
- 라이트 화면·설정·등록·정리본·게이트·dmg#8(1.2GB, 7445 정합) 전부 완료.
- #1(정리본 초과)은 **정직 안내로 임시 대응 완료**(`a7afb10`) — win이 프롬프트를 줄이면 긴 문서도 된다.
- HF 받기 인프라 완료(토큰 입력까지) — **사장님이 레포 올리면** max가 「GIJO 모델 받기」 한 줄 추가(반나절).

## 사장님 결정 대기
- HF 비공개 레포 업로드(런북 있음) · 프로 포트 7446 공유 확인 · `max_claude-qa`(미룸)
