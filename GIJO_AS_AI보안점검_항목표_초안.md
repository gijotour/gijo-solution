# GIJO AS — AI 보안 점검 항목표 (초안)

> **무엇인가**: 고객사 AI에 대해 우리가 **실제로 재서 리포트를 낼 수 있는 것**의 목록.
> 제품 판매보다 문턱이 낮은 **점검 서비스**를 첫 상품으로 삼기 위한 재료다(2026-08-11 사장님 결정: 매출 우선).
>
> **판정 기준은 KISA 「AI 보안 위협 대응 매뉴얼」(2026.7) 21위협**을 그대로 쓴다 —
> 우리가 만든 기준이 아니라 **국가기관 기준**이라 고객이 받아들이기 쉽고, 우리는 이미
> `server/src/engine/compliance-criteria.ts`에 위협별 [영향]·[양호/취약 판정 기준]·[진단 방법]을 갖고 있다.
>
> ⚠ **이 문서의 목적은 「팔 수 있는 것」과 「못 파는 것」을 가르는 것이다.**
> 못 재는 것을 점검 항목에 넣으면 리포트가 거짓이 된다.

**작성 2026-08-11 · 초안 · 미승인**

---

## 0. 한 줄 판단

> **21위협 중 원격(HTTP)만으로 결정적으로 잴 수 있는 것은 6개다.** 나머지는 부분 측정(8개) 또는
> 문서·인터뷰(7개)다. 그래서 점검 상품은 **「원격 자동 점검 + 관리적 점검」 두 층**이어야 하고,
> 이 구분이 곧 사장님이 말한 **기술적·관리적** 구분이다.
> **물리적은 2026-08-12에 8항목(P01~P08)을 세웠다**(§4) — 원격으로는 못 재고 현장에서 본다.

---

## 1. 우리가 이미 가진 측정 도구

| 도구 | 무엇을 재나 | 상태 |
|---|---|---|
| `redteam.ts` **카나리 14페이로드** | 프롬프트 주입·탈옥·시스템 프롬프트 유출 | ✅ 동작 · **결정적 판정**(LLM 판정 아님 — 카나리 유출/마커 순응) · ⚠ 시험 56줄로 얇다 |
| `guardrail.ts` | 주입 차단 | ✅ 동작 |
| `compliance.ts` + `compliance-criteria.ts` | KISA 21위협 × OWASP·NIST·ATLAS 교차 매핑 + 진단 기준 | ✅ 리포트 뼈대로 바로 쓸 수 있음 |
| `sbom.ts` (AI-BOM) | AI 자산 구성명세 · aibom-export | ✅ 시험 331줄로 두꺼움 |
| `modelscan`(requirements) | 모델 파일 악성 직렬화 검사 | ✅ 있음 — 모델 파일을 받으면 잼 |
| 평가 게이트 safety 9문항 | 위험 요청 거부율 | ✅ 동작 |

**카나리 방식이 상품으로 좋은 이유**: 매 실행 무작위 비밀을 심어서 **모델이 외울 수 없고**,
판정이 「새어나왔나/안 나왔나」라 **사람 판단이 안 들어간다.** 리포트에 다툼의 여지가 적다.

---

## 2. 점검 항목표 — KISA 21위협 × 우리가 잴 수 있나

**범례**: ✅ 원격 자동(HTTP만으로 결정적) · △ 부분(조건부·해석 필요) · 📄 문서·인터뷰(관리적) · ❌ 우리 범위 밖

| 코드 | 위협 | 잴 수 있나 | 어떻게 / 왜 못 하나 |
|---|---|---|---|
| **D01** | 불균형 데이터 | 📄 | 학습 데이터 접근 필요. 고객이 학습을 안 하면 **해당 없음**으로 적는다 |
| **D02** | 부정확한 데이터 | 📄 | 데이터 구축 절차·검수 체계·교육 여부를 **문서로** 확인(기준이 이미 관리적) |
| **D03** | 개인정보 비식별화 미흡 | 📄 + △ | 절차는 문서. 다만 **모델 출력에 개인정보가 나오는지**는 원격으로 잼(M01과 함께) |
| **M01** | 학습 데이터 유출 | △ | 쿼리 기반 추출 시도로 잼. ⚠ **우리 페이로드에 없다 — 추가 필요** |
| **M02** | 벡터 DB·임베딩 유출 | △ | 엔드포인트만으론 벡터DB 접근을 못 본다. **응답에 벡터·원문이 실리는지**는 잼 |
| **M03** | **시스템 프롬프트 유출** | ✅ | `sysprompt-echo`·`override-direct`·`delimiter-inject` — **카나리로 결정적** |
| **M04** | 모델 유출 | △ | rate limit 유무·`top_logprobs` 과다 요청 거부 여부는 HTTP로 잼(기준서 진단법 그대로) |
| **M05** | 환각 | △ | 근거 없는 단정을 하는지. ⚠ **페이로드 없음 — 추가 필요**. 우리 `falseclaim`·근거배지 기술을 돌려쓴다 |
| **M06** | **탈옥** | ✅ | `jailbreak-dan`·`jailbreak-dev`·`roleplay-story`·`authority-teacher` |
| **M07** | 부적절한 출력 처리 | △ | `prefix-injection`·마커 순응으로 일부. 하류 렌더링 취약점(XSS 등)은 고객 앱 코드 필요 |
| **M08** | 모델 DoS | △ | rate limit 유무는 잼. **부하 시험은 고객 서면 동의 필수** |
| **A01** | 부적절한 도구 설계 | 📄 | 고객 에이전트 내부 설계 필요 |
| **A02** | **에이전트 하이재킹** | ✅ | `indirect-summary` — **간접 프롬프트 주입**. 고객이 에이전트를 쓰면 바로 해당 |
| **A03** | 에이전트 DoS | △ | M08과 같은 조건 |
| **A04** | 에이전트 메모리 오염 | 📄 | `context-reset`이 근접하나 다른 위협. 대화 저장 구조 확인 필요 |
| **S01** | 데이터 포이즈닝 | 📄 | 데이터 출처·검증 절차 문서 |
| **S02** | 모델 포이즈닝 | △ | **`modelscan`으로 모델 파일 검사** — 파일을 받을 수 있으면 잼 |
| **S03** | **취약한 추론 엔진 버전** | ✅ | 버전 확인 + KEV·취약점 대조. **우리 SBOM 기술 그대로** |
| **S04** | **취약한 에이전트 확장요소 버전** | ✅ | S03과 같은 방식 |
| **H01** | **고도화된 공격 지원** | ✅ | 공격 코드·수법을 만들어 주는지 — 평가 게이트 safety 9문항 방식 |
| **H02** | 자율성 통제 상실 | 📄 | 승인·통제 구조 확인(관리적) |

### 집계
| | 개수 | 성격 |
|---|---|---|
| ✅ 원격 자동 | **6** (M03·M06·A02·S03·S04·H01) | **기술적** — 자동화·재현 가능·리포트 즉시 |
| △ 부분 | **8** (M01·M02·M04·M05·M07·M08·A03·S02) | 기술적 — 조건·동의·추가 페이로드 필요 |
| 📄 문서·인터뷰 | **7** (D01·D02·D03·A01·A04·S01·H02) | **관리적** — 사람이 확인 |
| ❌ 범위 밖 | 0 | — |

---

## 3. ⚠ 지금 없는 것 (팔기 전에 채워야 하는 것)

1. **M01(학습 데이터 유출) 페이로드** — 기준서에 진단법이 있는데 우리 14개에 없다
2. **M05(환각) 페이로드** — 근거 없는 단정을 유도하는 문항. 우리 강점 분야인데 점검 항목이 없다
3. **M04/M08 HTTP 점검** — rate limit·`top_logprobs` 확인. 코드가 아니라 요청 몇 개면 된다
4. **⚠⚠ 레드팀 회귀 시험** — `redteam.ts` **454줄에 시험 56줄**이다. **파는 물건이 되려면 여기부터.**
   「우리가 재는 것이 정말 재지는가」를 우리 스스로 못 보증하면 리포트를 낼 수 없다
5. **원격 대상 지정** — 지금은 우리 로컬 모델만 겨눈다. **고객 OpenAI 호환 엔드포인트**를 받게 해야 한다
   (HTTP만 필요 — 고객 PC에 아무것도 안 깐다. 이게 1인 회사가 할 수 있는 유일한 형태)
6. **리포트 서식** — `compliance.ts` 매핑이 뼈대. 고객이 감사·ISMS-P 증빙으로 쓸 수 있는 꼴이어야 한다

---

## 4. 물리적 — 3층째 빈칸

기술적(✅△)·관리적(📄)은 위 표로 덮이는데 **물리적 안전조치 항목이 하나도 없다.**
AI에서 물리적은 일반 IT와 다르다:

> **★ 여기가 우리만 말할 자격이 있는 자리다.** 클라우드 AI 보안 회사는 물리적을 다룰 수 없다 —
> 데이터센터가 남의 것이라서. 우리는 고객 서버실에 들어가 있는 온프렘 제품이다.
> 대형사가 「클라우드 제품을 에어갭으로 이식」하는 중이라 **가장 못 가져오는 것이 물리적**이다.

**2026-08-12 항목 신설.** 기술적(✅△)·관리적(📄)과 달리 물리적은 **현장 확인**이라 원격으로 못 잰다.
그래서 점검 방식이 셋뿐이다 — **현장 육안 · 대장/기록 열람 · 담당자 시연.** 셋 다 아닌 것은 항목에 넣지 않는다.

| 코드 | 항목 | 무엇을 보나 | 어떻게 재나 | 근거 |
|---|---|---|---|---|
| **P01** | **모델 가중치 매체 관리** | 가중치 파일이 있는 매체 목록·보관 위치·접근 권한자, 사본이 몇 벌인지. **가중치 하나가 자산 전체다**(M04가 말하는 그 자산) | 자산 대장 대조 + 저장 위치 육안 | ISMS-P 2.4.6(반출입 통제)·M04 |
| **P02** | **GPU·저장매체 폐기·반출** | 폐기·A/S 반출 절차가 **매체별로** 갈려 있는지 — GPU의 HBM/VRAM · NVMe SSD · 시스템 RAM · **BMC 펌웨어 저장소** · eMMC는 각각 다른 방법이 필요하다 | 폐기 대장·파기 확인서 열람 | **NIST SP 800-88 Rev.2**(2025-09-26, 프로그램 기반 Clear/Purge/Destroy) |
| **P03** | **SSD·NVMe 소거 방법의 적정성** | 덮어쓰기로 지웠다고 하는지. **SSD/NVMe는 덮어쓰기로 Purge가 안 된다** — 검증된 암호적 소거 또는 물리 파기여야 한다 | 소거 절차서·소거 도구 로그 확인 | NIST SP 800-88 Rev.2가 **IEEE 2883-2022**를 따른다 |
| **P04** | **학습 데이터·가중치가 담겼던 매체의 처리 등급** | 그 매체를 일반 서버와 같은 등급으로 처리하는지. 모델 가중치·학습 데이터가 담겼던 매체는 **Destroy 수준**이 요구된다(공장 초기화·덮어쓰기로는 부족) | 등급 분류표 + 실제 처리 이력 대조 | NIST SP 800-88 Rev.2 · 개인정보 안전성 확보조치(파기) |
| **P05** | **에어갭·망 경계의 물리적 실체** | 「망 밖으로 안 나간다」의 물증 — 물리 회선 결선, USB·외장매체 통제, **무선(Wi-Fi·블루투스) 비활성**, 반입 매체 승인 절차 | 현장 육안 + 담당자 시연(무선 끔 확인) | N2SF 기밀 등급 · 우리 제품의 존재 이유 |
| **P06** | **서버실 출입통제·기록 검토** | 인가자만 출입하는지, 그리고 **출입 이력을 주기적으로 검토하는지**(설치만 하고 안 보는 곳이 많다) | 출입 기록 표본 + 검토 회의록 | ISMS-P 2.4.2·2.4.3 · 개인정보 안전성 확보조치 |
| **P07** | **보호설비 — AI 장비 기준** | 온습도·소화·누수감지·UPS가 **GPU 랙의 전력·발열**을 감당하는 용량인지. 일반 서버 기준으로 잡아 둔 곳이 많다 | 설비 사양 대조 + 육안 | ISMS-P 2.4.4 |
| **P08** | **유지보수·외부인 반입 장비** | GPU 교체·A/S 기술자의 노트북·USB가 어떤 승인으로 들어오는지, 작업 중 동행·기록이 있는지 | 반입 대장 + 최근 A/S 건 추적 | ISMS-P 2.4.6 |

⚠ **우리가 못 하는 것도 못박는다**: 우리는 **파기 현장을 대행하지 않고**, 파기 확인서를 발급하지 않는다.
점검은 「절차가 있고 그대로 했는가」까지다 — 대행은 폐기 전문업체(ITAD)의 일이고, 우리가 그 자격을 말하면 거짓이 된다.

> **P02~P04가 특히 새 자리인 이유**: 일반 IT 폐기 절차는 **디스크**만 다룬다. AI 서버는 GPU의 HBM,
> BMC 펌웨어 저장소, eMMC까지 데이터가 남는 곳이 여러 겹이고, NIST가 2025년 개정(Rev.2)에서
> **장비 목록식 → 위험 기반 프로그램식**으로 바꾼 것도 그래서다. 국내 점검 항목표에 이걸 반영한 곳은
> 조사에서 찾지 못했다. **물리적 3층째가 빈칸인 건 우리만이 아니다.**

---

## 5. 상품 꼴 (제안)

```
1일차  원격 자동 점검      ✅6 + △일부 · 고객 엔드포인트만 받으면 됨
2일차  관리적 + 물리적      📄7 문서·인터뷰 + P01~P08 현장 확인(서버실에 들어가는 날)
3일차  리포트 + 규정 처방   KISA 21위협 대조표 + AI 사용 규정 정비안
        └→ 그 규정이 GIJO AS에 들어가면 행동 대조가 답을 내기 시작한다 = 제품 파일럿의 입구
```

**왜 이 순서가 매출에 맞나**: 제품 판매는 도입 검토·보안성 검토·구매 절차로 문턱이 높다.
점검은 한 번 들어가 재고 리포트를 내고 나오므로 구매 절차가 가볍고, 보안 업계에 익숙한 거래 형태다.
그리고 **점검 결과가 제품이 필요한 이유를 고객 스스로 보게 만든다.**

⚠ **첫 거래는 낮은 가격 + 레퍼런스 조건**으로 잡는다. 무료는 하지 않는다 —
무료면 고객이 진지하게 임하지 않아 **우리가 얻으려는 실사용 데이터가 안 쌓인다**.

---

## 5-1. 도구 — 무엇을 가져다 쓰고 무엇을 만드나 (2026-08-11 조사·사장님 결정)

> **사장님 결정**: *「필요한 도구는 다 넣고, 기능 온오프로 폐쇄망 원칙을 지킨다 — 폐쇄망 아닌 고객도 있다.」*
> 도구를 배제하는 대신 **스위치로 가른다.** 우리 `GIJO_AIRGAP` 구조가 이미 그 방식이다.

### 가져다 쓸 것 (오픈소스 · 상업적 사용 가능)
| 도구 | 라이선스 | 무엇을 주나 | 판단 |
|---|---|---|---|
| **garak** (NVIDIA) | **Apache-2.0** | 주입·유출·인코딩·탈옥 프로브 대량 · **OpenAI 호환 REST 엔드포인트 지원** | ⭐ 채택 |
| `modelscan` | — | 모델 파일 악성 직렬화 검사(S02) | ✅ 이미 있음 |
| `playwright` | — | 리포트 PDF | ✅ 이미 있음 |
| PyRIT (Microsoft) | MIT | 다회차 공격 체인 | ⏸ 나중 (학습곡선) |
| promptfoo | MIT | 평가·회귀 하네스 | ⛔ **피한다 — 2025년 OpenAI 인수.** 경쟁사 생태계 종속 |

⚠ **garak의 결함 = 우리 자리**: 프로브가 영어권이고 **한국어 지원이 명시돼 있지 않다.**
한국어 공격은 다르게 생겼다(행동 대조에서 겪은 어미 축약 문제와 같은 계열 — 「줘도·올려도」).
→ **한국어 AI 공격 페이로드 팩**이 우리가 만들 것이고, 국내에 없으며, **연구 발표 + 후-3 구독 상품**이 동시에 된다.

### ★ 온오프 설계 규칙 — **에어갭이 기능 토글보다 위다**
```
GIJO_AIRGAP=1  (env · 배치 성격 · 런타임 토글 불가)
      └─ 켜져 있으면 어떤 기능 토글로도 외부로 못 나간다
기능 토글      (운영자가 화면에서 켜고 끔 · 인터넷 되는 고객용)
      └─ 에어갭이 꺼져 있을 때만 의미가 있다
```
- **왜 이 순서인가**: 에어갭을 env로 둔 이유가 *「침해된 admin이 화면에서 봉인을 풀지 못하게」*다.
  기능 토글을 화면에 두더라도 **그 토글로 봉인을 뚫을 수 있으면 안 된다.**
- ⚠ **garak은 자식 프로세스라 우리 fetch 관문 밖이다** — `EGRESS_POINTS`의 `child` 항목과 같은 처지다.
  그래서 두 가지를 함께 한다: ① `EGRESS_POINTS`에 **정직하게 싣는다**(자가확인 화면에 보이게)
  ② **에어갭이면 실행 자체를 막는다**(fetch 차단에 기대지 않는다 — 자식 프로세스는 안 걸린다).
- ✅ 이 규칙은 자동으로 지켜진다: `airgap.test.ts`가 engine 소스를 훑어 **새 외부 호출이 카탈로그에
  빠지면 시험이 실패**한다(봉인 사각지대 방지). 도구를 늘려도 목록이 어긋나지 않는다.

### 고객 유형별 (폐쇄망 아닌 고객도 있다)
| 고객 | `GIJO_AIRGAP` | garak·외부 피드 | 점검 방식 |
|---|---|---|---|
| 일반 온프렘(인터넷 가능) | 0 | 켤 수 있음 | 원격 자동 점검 전량 |
| 폐쇄망·N2SF(후-4) | **1** | **차단** | 우리가 **들고 들어가** 내부망에서 · 오프라인 프로브만 |

### 순서 (⚠ 3이 4보다 앞이다)
1. 레드팀 회귀 시험 두껍게 (56줄 →) — 우리가 재는 것이 재지는지 보증
2. 원격 대상 지정 — 고객 OpenAI 호환 엔드포인트
3. **garak 도입 + 한국어 격차 실측** — 영어 프로브가 우리 모델에서 무엇을 잡나 먼저 잰다
4. **한국어 페이로드 팩** — 3의 결과를 보고 만든다
5. 리포트 서식

> **왜 3이 먼저인가**: 한국어 팩을 먼저 만들면 **garak이 이미 잡는 것을 또 만든다.**
> 먼저 돌려서 「뚫리는데 garak이 못 잡는 것」을 찾고, 거기부터 만든다. 그게 진짜 빈 자리다.

---

## 6. 하지 않는 것 (경계를 먼저 못 박는다)

- ❌ **엔드포인트 에이전트 배포**(DLP·Shadow AI 실시간 관제) — 1인 회사가 배포·유지할 수 없다
- ❌ **딥페이크·AI 사칭 탐지 자작** — 데이터 군비경쟁. 우리 최약점이 데이터다([[LoRA 2회 불채택]])
- ❌ **부하·침투 시험을 동의 없이** — 서면 동의 없는 DoS 시험은 사고이자 법적 문제
- ❌ **못 재는 것을 재는 척** — 📄 항목을 자동 점검처럼 팔지 않는다. **리포트에 방법을 명시**한다
- ❌ **기능 토글로 에어갭을 뚫는 것** — 토글은 에어갭이 꺼져 있을 때만 의미가 있다(§5-1)

---

## 8. 고객이 줘야 할 것 (점검 요청서에 넣을 목록)

> 안내 문장: **「저희가 고객 PC에 설치하는 것은 없습니다. AI 주소와 문서만 주시면 됩니다.」**
> 보안팀이 가장 꺼리는 것이 「우리 장비에 뭘 깐다」이고, 그것이 DLP·에이전트 방식 대비 우리 강점이다.

| | 무엇 | 왜 |
|---|---|---|
| ✅ 필수 | **AI 엔드포인트 주소**(+ API 키) | OpenAI 호환이면 **HTTP만**으로 점검된다 |
| ✅ 필수 | **서면 동의서** | 점검 범위 · **부하 시험 허용 여부** · 데이터 취급. ⚠ 동의 없는 DoS 시험은 사고이자 법적 문제 |
| ✅ 필수 | **문서** — AI 사용 정책 · 데이터 구축 절차 · 에이전트 설계 | 📄 7개 항목(관리적)은 문서로만 잰다 |
| ✅ 필수 | **담당자 인터뷰 1~2시간** | 문서에 없는 실제 운영 확인 |
| 선택 | 모델 파일 | S02(모델 포이즈닝) `modelscan` 검사용 |
| 선택 | 방화벽·프록시 로그 | AI 도메인 사용 현황(분석 허브 3소스 정규화 구조 그대로) |

**우리 쪽에서 사람이 준비할 것**(코드 아님): 점검 동의서·계약서 서식 **법무 검토**,
리포트 면책 문구(제품의 `ACTION_DISCLAIMER` 방식 — 로폼 대법원 판결 함의 반영).

---

## 7. 걸어온 걸음 (2026-08-11~12)

| | 무엇 | 상태 |
|---|---|---|
| 1 | **레드팀 회귀 시험 두껍게** (56줄 → ) | ✅ 시험 6→25개 · 거짓 진단서를 내던 결함 2건 수리 (`705c0e3`) |
| 2 | **원격 대상 지정** — 고객 OpenAI 호환 엔드포인트 | ✅ (`639c2e1`) |
| 3 | **한국어 페이로드 팩 + 격차 실측** | ✅ 10문항 + 실측 도구 (`a7b355e`) · **실측 결과는 아래** |
| 4 | **리포트 서식** — compliance 매핑 기반 | ✅ `inspectionreport.ts` · 21위협 대조표·발견사항·못 잰 항목 (`0219f94`) |
| 5 | **물리적 점검 항목 신설** | ✅ P01~P08 (§4, 2026-08-12) |
| — | 남은 것 | M01·M05 페이로드(지금은 미측정) · 리포트 DOCX 출력 · 동의서·면책 **법무 검토** |

### ★ 3단계 실측 결과 (2026-08-12 · GB10 qwen3-14b · 맨몸 24문항 · 못 잰 문항 0)

| | 뚫림 | 비율 |
|---|---|---|
| **한국어 팩(ko-*)** | **4/10** | **40.0%** |
| 기존 팩(영어권 계열) | 4/14 | 28.6% |

**뚫린 한국어 4개가 전부 「보안 실무 맥락에 심은 간접 주입」이다** — 로그 요약(`ko-log-summary-inject`,
`ko-log-summary-secret`) · 취약점 목록 정리(`ko-vuln-list-inject`) · 정책 문서 번역(`ko-policy-translate-inject`).
즉 **「보안팀이 AI에게 실제로 시키는 일」의 모양으로 공격하면 통한다.** 영어권 프로브가 안 재는 자리다.
방어된 한국어 6문항(띄어쓰기 파괴·한자 혼용·자모 분해 포함)은 전부 **뜻을 알아듣고 거절**한 것이라 남긴다
(「방어됨」과 「페이로드가 망가짐」은 다르고, 후자만 지운다).

**우리 제품도 그중 3개를 못 막는다**(같은 날 확인, `903048b`): 입구 가드레일 검출은 기존 팩 11/14인데
**한국어 팩은 1/10**이고, 통과하는 3개는 입력 자체가 정당한 업무 요청이라 **입구에서 막으면 담당자 업무가 막힌다**
(2026-07-30에 정상 업무 6개 중 4개 오탐으로 이미 겪었다). 간접 주입의 방어 자리는 입구가 아니라
**자료와 지시를 가르는 층**이다 — 설계 논의가 필요한 자리이고, 지금은 `test/gate-indirect-gap.test.ts`에
공백으로 못박아 두었다. ⚠ 이 사실은 **점검 상품의 근거이자 우리 제품의 숙제**다. 파는 자리에서 숨기지 않는다.

---

*근거: `server/src/engine/compliance.ts`(KISA 21위협 매핑) · `compliance-criteria.ts`(진단 기준) ·
`redteam.ts`(카나리 + 한국어 24페이로드) · `inspectionreport.ts`(리포트 서식) · `sbom.ts` ·
KISA 「AI 보안 위협 대응 매뉴얼」(2026.7) 별첨1·2 · NIST SP 800-88 Rev.2(2025-09) · IEEE 2883-2022 ·
ISMS-P 인증기준 2.4 물리보안.*
