# GIJO AS 라이트 — 사양 (2026-08-10 결정분)

**무엇인가**: 담당자 PC에서 **혼자 도는** 별도 제품. 최소 기능. 7B.
**상태**: 방향·근거 확정, 구현 전. 아래 §7이 미결 목록이다.
**사용자 결정(2026-08-10)**: 「7B는 우리 제품의 최소기능만 넣은 라이트 버전으로 **별도로** 만든다」
「담당자 PC에서 혼자 도는 버전」 「**진짜 간단한 기능만**」 「최소 GPU가 있는 환경, 내부 LLM은 돌 것」

---

## 1. 정체성 — 한 줄

> **담당자의 개인 보안 지식 조수.** 회사 데이터를 다루는 제품이 아니라,
> **보안 지식·문서·규정에 근거 있는 답**을 혼자 쓰는 PC에서 주는 도구.

본 제품과 **시장이 겹치지 않는다.** 회사 인프라 연동이 필요해지는 순간이 본 제품으로 올라가는 자리다.

⚠ **12GB급 시장을 라이트가 받는다** — 본 제품은 Lite 티어를 16GB급으로 올리며 12GB를 뺐다
([[GIJO_AS_모델_메모리_실측_2026-08-10.md]]). 그 층을 여기서 받으므로 본 제품을 깎지 않아도 된다.

---

## 2. 데이터 — 이것이 기능 목록을 정한다

**라이트가 가진 데이터는 「번들로 들어오는 것」뿐이다**: 문서 19건 + 온톨로지 트리플.
자산·취약점·로그·SBOM·제품 등록부는 **회사 시스템에서 오는데 담당자 PC엔 없다.**

> **데이터가 없는 도구는 넣어도 「없습니다」만 답한다. 넣을 이유가 없다.**

---

## 3. 기능 — 도구 8개

| | 도구 | |
|---|---|---|
| **① 지식·근거** | `search` `explain` `remediation` `ontology_query` | 번들 문서 + 온톨로지로 답이 된다. **제품이 파는 것이 여기 다 있다** |
| **② 내 문서함** | `knowledge_status` `recent_documents` | 담당자가 자기 자료를 넣고 묻는 동선 |
| **③ 개인 할 일** | `add_task` `complete_task` | 혼자 쓰는 데 맞는 크기 |

**효과가 크다** — 도구 카탈로그가 프롬프트의 대부분이었다:

```
현행  77개 · 카탈로그 13,674자 · 구조화 프롬프트 실측 7,984토큰
라이트 8개 · 카탈로그  1,253자 · 프롬프트 대략 700~2,800토큰   (91% 감소)
```

### 빼는 것 — 69개, 이유는 넷

| 이유 | 예 |
|---|---|
| 회사 데이터가 있어야 돈다 | 자산 11 · 취약점 5 · 리포트 10 · SBOM 2 · 제품 2 · 유지보수 3 · `today`(전 자산 우선순위) |
| **외부 통신이 필요하다** | **`law_lookup`** — 국가법령정보센터를 부른다. 폐쇄망 담당자 PC에서 못 쓴다 |
| 여럿이 써야 뜻이 있다 | 결재·인계·감사·담당자 배정 |
| 운영자용이다 | 모델 채택·어댑터·레드팀·게이트·학습 루프 |

⚠ **법규 질문은 `law_lookup` 없이도 답한다** — 번들에 `보안규제_법적근거_모음.md`가 있고,
`registry.ts:1509`가 정확히 이 상황을 적어 뒀다: *「예전에는 사내 문서(RAG)로 답하던 질문이다 —
꺼진 기능이 멀쩡하던 답을 빼앗으면 안 된다」*.

## 4. 화면 — 5개

**대화창 · 문서함 · 할 일 · 설정 · 첫 실행.** (현행 38개 중. 나머지는 위에서 뺀 도구들의 화면이다.)

---

## 5. ★ 가드레일은 뺄 수 없다 — 「최소 기능」에서 제외

평가 게이트 실측(2026-08-10):

| | `gijo-main-orchestrator` 7.6B |
|---|---|
| **맨몸 모델 견고성** | **36점** (기준선 71 · 9회 중앙값 · 관측 29~50) |
| **제품 경로 실효 견고성** | **100점 · 뚫림 0/14 · 입구차단 13** |

7.6B 자체는 레드팀에 크게 약하다(정상 변동 폭 7점이라 71→36은 우연이 아니다).
**제품 가드레일이 14건 중 13건을 입구에서 막아** 실효 100점이 된다.

> **라이트에서 가드레일을 빼면 36점짜리 맨몸이 그대로 드러난다.**
> 그리고 담당자 PC에 혼자 있어 **봐 줄 사람이 없다.**

가드레일은 「기능」이 아니라 **관문**이다. 최소 기능을 깎을 때 여기를 건드리지 않는다.

## 6. 모델 — `gijo-main-orchestrator` (7.6B · qwen2 · 파일 5.07GB)

두 후보를 같은 게이트에 돌린 결과(문항셋 `9462db589b00e4ac`):

| 축 | **`gijo-main-orchestrator`** | `merged-lily`(보안 특화) | 14B 기준선 |
|---|---|---|---|
| routing | **64/66 = 97%** | 60/66 = 90.9% | 66/66 |
| safety | **9/9 = 100%** | 8/9 = 88.9% · **카나리 실패 1** | 9/9 |
| korean | **24/24 = 100%** | **15/24 = 62.5%** | 24/24 |
| 맨몸 견고성 | 36점 | **0점** (관측 0~86) | 71점 |
| 제품 경로 실효 | 100점 · 뚫림 0 | 100점 · 뚫림 0 | 100점 |

「보안 특화」라는 이름이 무색하게 `merged-lily`가 **모든 축에서 밀린다.**
카나리 1건 실패는 그 자체로 즉시 보류 사유다. 한국어 62.5%의 실제 모습:

> 「이번 주, 우리 시스템에서는 5건의 critical 취약점과 … **중간 중몰락 덤프**, Apache Log4j2…」 (한글 38%)

그리고 「온프렘 AI가 클라우드보다 나은 점」에 **FALCON Cloud Security · CROWDSTRIKE Intelligence**를
「사내 근거」로 인용했다 — RAG 오염이 답에 그대로 나온 사례다.

⚠ **routing 97%는 라이트에 그대로 적용되지 않는다** — **77개 도구 중** 고른 점수다.
라이트는 8개라 난이도가 다른 문제다. **라이트 구성으로 다시 재야 한다**(§7).

## 7. 구성 — ctx 8192 · 8GB급

| 구성 | 7B | 임베딩 | 합계 (Metal 실측) | 필요 GPU |
|---|---|---|---|---|
| **ⓐ 라우팅 축소 + ctx 8192** | 5.45 | 1.00 | **6.45 GB** | **8GB급** (여유 1.5) |
| ⓑ 라우팅 유지 + ctx 16384 | 6.04 | 1.00 | 7.04 | 8GB급 (여유 0.9 — 빠듯) |
| ⓒ 본 제품과 같은 설정 | 6.85 | 1.55 | 8.40 | 12GB급 |

**ⓐ 채택.** 「최소 기능 = 도구 8개」와 「ctx 8192」는 **같은 하나의 결정**이다 —
도구를 줄여야 프롬프트가 7,984 → 3,000 미만이 되고, 그래야 ctx 8192가 성립한다.

- 임베딩은 `--ctx-size/--batch-size/--ubatch-size 2048`로 충분하다.
  근거: `CHUNK_SIZE = 800`**자**이고, 800자 한국어 청크를 ubatch 2048 서버에 실제로 보내
  **HTTP 200 · 1024차원**을 받았다. (⚠ 2026-07-20의 「512에서 HTTP 500」은 512가 우리 청크보다
  작았던 것이지 8192가 필요했던 게 아니다 — 하한은 **청크 최대치**다.)
- `--parallel` 은 붙이지 않는다(1인 티어). 한 요청이 ctx를 다 쓴다.

⚠ **RAG는 뺄 수 없다.** `bge-m3`는 566M이라 CPU에서도 돈다. 빼면 **「근거」가 빠지고**,
그게 제품이 파는 것이다. 근거 없는 라이트는 우리 제품이 아니라 그냥 로컬 챗봇이다.

## 8. ⚠ 라이트에 걸리는 것들

- **규정 흔들림이 7B에서 더 심하다** — 「퇴사자 계정」 10회:
  7.6B `1년 4 · 2년 3 · 즉시+1년 2 · 불명 1`(4갈래 분산) vs 14B `즉시삭제 6`(쏠림).
  최소 기능에 법·규정 질문이 들어가면 **「숫자·조문은 원문에서 그대로 복사」가 필수**다(②의 몫).
- **`modeldefault.test.ts:48`** `expect(engine).not.toBe("gijo-main-orchestrator")` —
  라이트가 7.6B를 정식 기본값으로 쓰면 **이 시험이 막는다.** 제품이 둘로 갈리면 시험도 갈려야 한다.
- ✅ **문체는 걱정 없다** — [[GIJO_AS_문체_AI스러움_실측_2026-08-10.md]]의 0.153은
  `chat_logs`(14B 승격 이전이 대부분)라 **대체로 7B가 만든 값**이다.
- ✅ **RAG 오염은 구조적으로 작다** — 라이트에 들어가는 문서는 번들 19건뿐이고 **타사 0건**이다.
  다만 담당자가 자기 자료를 넣으면 생긴다. 그때는 본 제품의 `origin` 신호와 후보 풀 설정이
  그대로 필요하다([[GIJO_AS_RAG_오염_실측_2026-08-10.md]] §8).

## 9. 미결 — 다음에 정할 것

1. **CUDA 실측 2값** (Windows): ① 7.6B @ ctx 8192 단독 ② `bge-m3` @ ctx/batch 2048.
   ⚠ 갓 띄워 **예열 2회 뒤** 잴 것(RSS 감쇠 함정 — 모델 메모리 실측 문서 §4).
   Metal 기준 8GB급인데 CUDA는 더 클 수 있어 **12GB급이 될 가능성**이 있다.
2. **라이트 8개 도구 구성으로 routing 재측정** — 97%는 77개 기준이라 그대로 못 쓴다.
3. **화면 5개의 구체 범위** — 설정에서 무엇까지 남기나(모델 받기·티어·백업?).
4. **번들 문서 19건을 라이트에도 그대로 넣나** — 본 제품 기능 설명이 섞여 있다.
5. **가드레일을 어떤 형태로 싣나** — 관문만인가, 감사 기록까지인가.

---

*근거: 2026-08-10 Mac 실측 — 평가 게이트 2회(`9462db589b00e4ac`) · 도구 카탈로그 실측 ·
프롬프트 토큰 실측 · Metal 메모리 실측. 짝 문서:
[[GIJO_AS_모델_메모리_실측_2026-08-10.md]] · [[GIJO_AS_RAG_오염_실측_2026-08-10.md]] ·
[[GIJO_AS_문체_AI스러움_실측_2026-08-10.md]]*
