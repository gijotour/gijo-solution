# gb10 실측 인계 — AI 팀 증류·학습 (2026-09-03)

> 사장님 지시(2026-09-03): **「모델 확인 및 설계는 Fable(win)이, 실측은 gb10 Opus에서 하자」** + **「gb10 내리고 올리고 맘대로 사용해서 좋은 결과 보여줘」**.
> 이 문서는 win(Fable)이 gb10 세션(Opus)에 넘기는 실측 일감이다. gb10에서 `claude`를 열고 이 파일을 먼저 읽게 한다(`~/bench/인계.md`에 같은 사본).
> 결과는 **파일로 남긴다**(`~/bench/results/*.json`, `summary.md`, `~/bench/report-<날짜>.md`) — win이 그 파일을 읽어 계획서·모델가이드에 옮긴다. 판단·설계 변경은 win 몫.

## 0. 기계 규칙 (어기면 실측이 무효가 된다)
- **8080·8081은 제품(node)이 관리한다** — Flash-Next 125B·bge-m3. 손대지 마라. node를 재기동하면 `reapOrphanEngines`가 `--embedding` 없는 llama-server를 **전부 SIGKILL**한다(옆 포트 시험 서버도 죽는다).
- 시험용 포트는 **8093**(bench 하네스 전용)·**8094**(교사용 수동). 새 서버를 띄우기 전에 `ss -tln | grep :PORT`로 비었는지 본다 — 2026-09-03 실측: 8090에 남의 Coder-30B가 있어 첫 실행이 **남의 모델을 쟀다**.
- `pgrep -f`는 **자기 셸을 잡는다** — `pgrep -f "^\S*llama-server -m …"`처럼 앵커를 붙여라(같은 날 내 ssh 셸을 죽인 실사고).
- 비대화형 ssh는 `.bashrc`를 안 읽는다 — 명령마다 `. ~/gijo-env.sh` 먼저(node·CUDA 경로).
- 메모리 121GB 통합: Flash-Next 84 + bge 1.3이 상주. **동시에 30GB 넘는 시험 모델 두 개를 올리지 마라**(스왑 → 교사 속도 2 tok/s로 붕괴했다). `free -g`로 보고 하나씩.
- 통합메모리라 `nvidia-smi` 메모리와 RSS는 틀린 자다. VRAM 실측은 win 3090 몫.
- 결과 파일에 **어느 모델이 답했는지**(`/v1/models`)를 같이 남긴다.

## 1. 지금 gb10에 있는 것
| 무엇 | 어디 | 상태 |
|---|---|---|
| 팀원 역할별 시험 하네스 | `~/bench/run.mjs`·`tasks.mjs`·`models.json` (저장소 `tools/team-bench/`) | 1회차 13모델 완료 → `~/bench/results/summary.md` |
| 후보 GGUF 13종 | `~/gijo-as/server/models/<id>/<id>.gguf` | qwen3-14b·qwen38-27b(+mmproj)·qwen3-32b-q3·coder-30b·gpt-oss-20b·kanana-1.5-8b·ax-4.0-light·midm-2.0-base·kanana-2-30b-a3b·hcx-seed-think-32b(적재 불가)·exaone-4.0-32b·exaone-3.5-7.8b·gijo-main |
| 학습 환경 | `~/venv-train`(torch 2.14+cu130·transformers 5.16·peft 0.20·bnb 0.50, GB10 인식) | 100스텝 스모크 **아직 안 함** |
| Qwen3-14B 원본(HF safetensors) | `~/hf/Qwen3-14B` | 완료(30GB) |
| 증류기 | 저장소 `tools/distill.mjs`(win에서 돌린다) | 교사 끝점만 gb10 |

## 2. 실측 일감 (순서대로, 각각 파일로 남긴다)
### A. 팀원 시험 2회차 — 문항을 어렵게 (`tasks.mjs` 손대기)
1회차는 13개 중 9개가 리포트 구조화·16K 바늘에서 만점이라 변별력이 없다. 다음을 더한다:
1. **지저분한 리포트**: 표가 깨지고 항목이 중복·오탈자·영문 혼용인 취약점 리포트(8~10항목, 그중 중복 2·오탐 표시 1) → 정답 항목 수와 중복 제거를 채점.
2. **32K·64K 바늘**: `긴문서(50000)`·`긴문서(100000)`로 사실 5개 회수(ctx 65536으로 띄운다; 안 들어가는 모델은 「불가」로 표기).
3. **함정 위협정보**: 버전 경계(7.0.13 vs 7.0.14), 제품명 유사(FortiGate vs FortiWeb), 「해당 없음」 절제 3건 → 오탐에 감점.
4. **우선순위 6건**: 동률 깨기(둘 다 KEV일 때 노출·중요도).
5. 실행: `node run.mjs --port 8093 --ctx 65536 --out results-r2` → `summary.md`를 `results-r2/`에.
6. 보고: `~/bench/report-r2.md` — 모델별 평균·문항별 탈락·tok/s. **A.X-4.0-Light 7B(1회차 만점)가 어려운 문항에서 무너지는지**가 첫 질문.

### B. 교사 수율 비교 (증류 재료 품질)
같은 조각 20개로 교사 4종의 **근거 20자 겹침 통과율**을 잰다(win의 `tools/distill.mjs --no-intake`를 win이 돌리고, gb10은 교사만 띄운다):
- Flash-Next(정식 창구 8080) — 1회차 58%(12건)
- Coder-30B(8094 수동) — 1회차 42%
- Qwen3.8-27B(8094 수동, `--reasoning off`) — 미측정
- A.X-4.0-Light 7B(8094 수동) — 미측정
gb10 몫: 요청받은 모델을 8094에 띄우고 내리기(`ss`로 확인) · win이 결과 JSON을 `.tmp-reports/`에 남긴다.

### C. 학습 환경 100스텝 스모크 (「설치됐다 ≠ 돈다」)
`~/venv-train`으로 Qwen3-14B(`~/hf/Qwen3-14B`)에 **bf16 LoRA 100스텝**(시퀀스 4096, 재료는 `~/bench/smoke-train.jsonl` — 1회차 증류 문답 12건을 5번 복제해도 된다):
1. 손실이 내려가는지(첫 10스텝 vs 마지막 10스텝 평균) 2. 메모리 정점(`free -g` 60초 간격) 3. 스텝당 초 4. 어댑터 safetensors → `llama.cpp-next/convert_lora_to_gguf.py`로 GGUF 변환 5. `llama-server -m qwen3-14b.gguf --lora <어댑터> --port 8094`로 **적재까지** 6. 8094에 질문 1개 던져 답 나오는지.
보고: `~/bench/report-train-smoke.md`. ⚠ transformers 5.x라 저장소 `server/scripts/finetune_qlora14b.py`(4.x 기준)가 안 돌 수 있다 — 안 돌면 **무엇이 깨지는지**를 적고 멈춰라(고치는 건 win).

### D. 27B 파일 읽기 실측 (모델가이드 §6)
1. 27B를 8094에 `--ctx-size 65536`으로 띄우고 32K·64K 바늘 회수율·프리필 tok/s(20K 토큰 읽는 데 몇 초). 2. `--mmproj mmproj-F16.gguf`로 스캔본(이미지 PDF 10장을 png로) 읽기 정확도 vs RapidOCR(win 결과와 대조).
보고: `~/bench/report-27b-read.md`.

## 3. 하지 말 것
- 8080·8081 재기동 · node(4000) 재기동 · `~/gijo-as` 작업트리 편집(브랜치 `claude/hungry-blackwell-cd58b7`, win이 민다) · 운영 데이터(`~/gijo-as/server/data`) 쓰기 · HF 원본 삭제.
- 결과를 「좋다/나쁘다」로 요약만 하지 말 것 — **숫자와 실패 사유**를 파일에. 채점기가 틀렸다고 보이면 고치지 말고 「판정기 의심」으로 적어라(win이 고친다).

## 4. 돌려줄 것
`~/bench/report-*.md` 4개 + `results-r2/summary.md` + 실행 명령 그대로(재현용). win이 읽고 모델가이드 §7·계획서 이력에 옮긴다.
