# [인계] win → max — 검증 결과: 재편 ⑤⑨ + RAG 랭킹 (2026-08-17)

> 역할 변경(사장님 지시) 반영 — **max 구현, win 검토관문 검증**. 아래는 win이 max 커밋
> `b52efa2`(RAG 랭킹)·`00124a7`(재편 ⑤⑨)를 검증한 결과다. 기준 hub `bafe760`.

## ✅ b52efa2 — RAG 검색 랭킹 수리 (공용 엔진, 표준에도 영향)
- **hybridsearch.test.ts 32/32 통과**, 전체 게이트에서 **표준 RAG 회귀 없음**.
- memory.ts 변경(검색 시 `isBinaryLikeChunk`를 loop `continue`→`.filter()`)은 **win의 #3(chunkText HTML주석 제외, memory.ts:411)과 다른 함수** — 충돌·간섭 없음(#3 그대로 살아있음 확인).
- 판정: **좋음.** 공용 엔진 변경이지만 표준 검증 통과. (win이 공용이라 검증한 것 — 결함 없음.)

## ✅ 00124a7 main.ts (+7, 공용 파일) — 안전
- 허용스킴에 `mailto:`/`tel:` 추가(나만의 연락처 ⑤의 이메일·전화 직접 연락용).
- 위험 스킴(`javascript:`·`file:`·`data:`)은 **여전히 차단**, 웹/원격은 그대로. **표준 무해.** 판정: 안전.

## ❌ 00124a7 lite-contacts.html:45 — 게이트 FAIL (max가 고칠 것)
```
td .cnt{font-size:10px;opacity:.8;font-weight:600}   ← 10px
```
- **`uireadability.test.ts`(11px 미만 금지, 2026-08-02 「거의 안 보인다」 지적으로 제정)에 걸림.** 전체 게이트 **종료코드 1**의 유일 원인(나머지 3,453개 통과).
- **수리: `font-size:10px` → `11px`**(매트릭스 카운트 배지). 한 줄.
- ⚠ **재편 화면(lite-)이라 win이 안 고친다**(사장님 분담 — 중복·충돌 방지). max가 다음 재편 커밋에 함께 반영.
- ⚠ **현재 hub 게이트 RED** — 이 한 줄 고치면 초록. Drive 빌드 전 관문이라 넘어가면 안 됨.

## 참고 — win 쪽 오늘 마무리 상태(회귀 감시용)
- 전-7③④·#1·#2·#3·#5 배치 **검토관 통과**(치명0·높음0). #3(memory.ts)·#5(dataset.ts) **운영 라이브**(21:14 KST 기동, 디스크 17:28 이후). **#2(localengine.ts)는 운영 미배포**(눈에 보이는 표준 영향 없어 다음 배포에 실음 — 라이트 server-dist엔 max 재빌드로 들어감).
- #1(lite-chat 스트리밍)은 **게시된 5.21.0에 없음**(그 이후 커밋) → max 라이트 재빌드로 사용자 도달.
- GA 판정표 2026-08-17 이력 갱신(판정 불변). 라우팅 76→58은 **오늘 회귀 아님**(routes.ts 오늘 무변경, 재편은 설계 문서뿐).

## 남은 확인(win, 로그인 게이트) — 다음 앱-닫힘 회차
- 제품소개 로드맵 삭제(#4)의 **운영 RAG 재인입** 실측(안 되면 내부 로드맵 계속 샘) · GA A~I 라이브 재측정 · 전-1 시연 대본 재검증.
