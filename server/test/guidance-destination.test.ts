// guidance-check 관문의 느슨한 잣대(거짓 초록) — [2026-09-12 신설 · 2026-09-13 수리 · B-1 재확정 · 계획서 §13]
//
// ■ 왜 이 시험이 생겼나
//   tools/guidance-check.mjs는 「제품이 이렇게 말하세요라고 적어 준 명령」이 흔들리지
//   않는지를 재는데, 판정이 **정규식 하나에 걸리기만 하면 결정적**이라고 센다 — 그 정규식이
//   실제 라우팅 갈래인지, 어디로 보내는지, 제품이 실제로 거기로 가는지는 한 번도 안 본다.
//
//   2026-09-12 실측: 안내 문구 85개 중 제품 함수 `결정적도착지`(dispatcher.ts)로 다시 재면
//   **31개가 ⑨ 모델 선택**(결정적 갈래 어디에도 안 걸림)인데, guidance-check는 그중
//   **28개를 「결정적」으로 세고 있었다**(REPORT_DETAIL_RE·SPECIFIC_SUBJECT_RE·LEARN_TOPIC_RE·
//   DATA_TOOL_RE 같은 상수가 실제 라우팅 판별자가 아닌데도 「특수경로」로 긁혔기 때문).
//   2026-09-11 「보안설정 점검 안 한 장비 있어?」가 **틀린 도착지로 초록**이던 사고를 옛
//   나무(31aed984^)에서 그대로 재현해 확인했다 — 관문이 실제로 사람을 속인 적이 있다는 뜻이다.
//
//   그래서 이 시험은 guidance-check.mjs의 느슨한 분류를 고치지 않고 **옆에** 진짜 관문을
//   세운다 — 판정은 오직 제품 함수(`결정적도착지`)로만 하고, 예외는 이유·근거·확인일을 갖춘
//   표로만 허용한다(server/test/helpers/routing.ts·compare-concept-routing.test.ts와 같은
//   원칙 — 「제품 판정을 흉내 내면 안 된다, 제품을 부른다」).
//
// ■ 수확은 guidance-check.mjs --json을 그대로 쓴다(guidance-routing.test.ts:22-27과 같은
//   방식) — 안내 문구를 이 파일에 손으로 옮기면 그 순간 두 벌이 되어 어긋난다.
//
// ■ **어디까지 재고 어디부터 안 재는가** — 정직하게 적는다(2026-09-13 검토관 적발 ①).
//   ○ 잰다 — ① 제품 함수가 이 말을 결정적으로 거는가 ② 그 자리가 **진짜 도착지**인가.
//     차단(가드레일)·되묻기는 도착지가 **아니다.** 제품이 「이렇게 치세요」라고 적어 준 말이
//     「무슨 말인지 모르겠다」로 되물어지면 통과가 아니라 결함이다.
//   ○ 잰다 — 데이터에 달린 갈래(`조건부: true`, 제목 지목·내 할 일 이름)는 **그것만으로는**
//     도착으로 안 센다(아래 ㉥). 「간다」고 단정할 수 없는 자리를 결정적이라 세면 또 거짓이다.
//   ○ [B-1 · 2026-09-13 신설] **도착 이름**까지 대조한다(아래 `기대도착`). 「걸리긴 하는데
//     엉뚱한 도구로 간다」(2026-09-11 run_hardening_scan 사고 꼴)를 이제 이 관문도 잡는다 —
//     단 「걸린다」와 「도착 이름이 맞다」는 다른 질문이라, 도착이 있는데 **뜻과 어긋나 보이는**
//     자리(아래 ★검토 4건)는 고치지 않고 표에 남긴다(고치는 일은 B-2/별건).
//
// ■ B-1(2026-09-13) — 수확 구멍 **세 개**를 닫고, 그 결과로 표를 다시 확정했다. **라우팅은
//   한 줄도 안 바꿨다**(server/src/engine/** 무변경) — 아래 숫자는 전부 「더 정확히 잰
//   결과」이지 「고쳐서 준 결과」가 아니다.
//   ① 조사 앞머리 필터(guidance-check.mjs `조사필터떨굼목록`이 세는 그 자리) `/^[의를을이가에]/`는
//      **첫 글자만** 보고 걸렀다 — 「이 취약점…」·「이번 달…」처럼 낱말 전체가 그 글자로 시작할
//      뿐인 멀쩡한 안내까지 조사 이음매 찌꺼기로 오판해 통째로 떨궜다(실측: 26개·43회, 그중 11개가 ⑨).
//      `/^[의를을가에](\s|$)/`(「이」를 뺀 조사 한 글자 + 공백/끝)로 좁혀 되살렸다 — 「이」는
//      지시대명사(이 취약점·이번)로 홑글자+공백이 정상이라 char class에서 뺐다.
//   ② `○○` 두 자리(자산 id + 취약점 이름) 치환이 같은 이름("GIJO AS 서버" 중복)으로 채워
//      「GIJO AS 서버 GIJO AS 서버 조치 절차 알려줘」 같은 **사람이 안 치는 말**을 만들고
//      있었다 — 두 자리를 서로 다른 대표값(sample-web01 / Apache Log4j RCE)으로 채우도록
//      고쳤다(guidance-check.mjs 상수 한 곳). **2줄**이 ⑨에서 결정적([27] formatRemediation)으로
//      바뀌어 보였다(치환 문구가 바뀐 것은 3줄 — 나머지 1줄은 그대로 ⑨다).
//      ⚠ 그러나 그 2줄은 **결정적이라 못 박으면 안 된다**(2026-09-13 검토관 적발 ②): 그 도착은
//      대표값 하나에 달렸다. 실측 — 같은 시드 자산의 finding 4건 중 Log4j 갈래만 [27]이고
//      OpenSSH 사용자 열거·Oracle DB CPU 미적용·SSL 인증서 만료 임박 3건은 **⑨**다
//      (playbook.ts `플레이북영토인가`가 playbookFor 비-generic 또는 「취약점」 낱말을 요구).
//      그래서 두 줄은 `기대도착`이 아니라 **`데이터에달려도봐줌`**이 받는다(아래 이유 참조).
//   ③ [검토관 적발 ① 수리] 따옴표 후보 뽑기가 `"([^"\n]{4,44})"`였다 — **하한 4가 짝 맞추기에
//      끼어들어** 세 글자 리터럴(`hint: "결재판"`·`"RAG"`·`"SLA"`·`"브리핑"`·`"리포트"`)을 만나면
//      정규식이 한 칸 밀려 `", instruction: "`를 통째로 먹고, **그 다음 안내 문자열이 여는
//      따옴표를 잃고 사라졌다.** 짝 맞추기(`*`)와 길이 거르기를 분리해 고쳤다.
//      ★ 이 손실은 아래 커버리지 시험(★★★ 「모든 안내는 …」)이 **원리상 못 잡는 부류**였다 —
//        그 시험은 「수확된 것」만 훑기 때문이다. 초록이 완전성의 증거가 아니었다.
//   ④ 안내 문구 **93개 → 119개(①②) → 132개(③)**. 그중 **⑨(넓은 뜻: 결정적 도착 없음) 54개**
//      = 법령 10 + 쓰기 20 + 읽기 24(되묻기 전용 1건 포함). 결정적 78개 중 4개는 데이터에 달려
//      (데이터에달려도봐줌) `기대도착`에는 **74개**만 올린다. 74 + 54 + 4 = 132.
//
// ■ [B-2 · 2026-09-13 라우팅 변경] 하베스트 개수(132)는 안 바뀌었다 — B-2는 라우팅 규칙만
//   넓혔지 안내 문구를 새로 추가하지 않았다(scenarios.ts 1건은 **문구 두 글자 reword**, 개수
//   불변). 재분류만 일어났다: ⑨ **54 → 44**(법령10 + 쓰기21(+1, 담당부서 갈래이동) +
//   읽기13(−11: 결정적화 9 + reword 1 + 갈래이동 1)). 결정적 88개(78+10) 중 데이터조건부 4는
//   그대로라 `기대도착`은 **74 → 84**. 84 + 44 + 4 = 132.
//
// ■ [UI D · 2026-09-13] settings.html에 「자가 진단」 판이 생기며 ⓘ 버튼 문구
//   「자가 진단 사용법 알려줘」가 새로 수확됐다(132 → **133**). HELP_RE의 「사용법」 강한 가지가
//   화면 매치와 무관하게 이겨 결정적([22] formatScreenGuide(지금 화면))이다 — 예외표(⑨)가 아니라
//   `기대도착`으로 받는다: **84 → 85**. 85 + 44 + 4 = 133.
//
// ■ 근거 칸은 **파일:줄이 아니라 grep 되는 원문 조각**으로 적는다(2026-09-13 검토관 적발 ⑦).
//   첫 판은 줄 번호로 적었는데 **같은 날 안에 네 줄이 어긋났다** — 이 저장소는 실행자 여럿이
//   한 트리에서 동시에 커밋해서, 줄 번호는 몇 시간이면 낡는다. 낡은 근거는 「눈감기 목록」을
//   막으라고 둔 칸이 제 구실을 못 하게 만든다.
import { describe, expect, it } from "vitest";
import { execFileSync } from "node:child_process";
import { readFileSync } from "node:fs";
import { join } from "node:path";
import { 결정적도착지, type 도착단계 } from "../src/engine/dispatcher";
import { 쓰기흐름인가 } from "../src/engine/writeflow";

const ROOT = join(__dirname, "..", "..");
const 도구경로 = join(ROOT, "tools", "guidance-check.mjs");

interface 안내문구 {
  원문: string;
  질문: string;
}

function 수확(): 안내문구[] {
  const out = execFileSync(process.execPath, [도구경로, "--json"], { encoding: "utf8", cwd: ROOT });
  return JSON.parse(out);
}

/**
 * **차단·되묻기는 도착지가 아니다.**
 * 제품이 적어 준 말이 「해로운 요청 차단」이나 「뜻 모를 입력」에 걸리는 것은 통과가 아니라
 * 결함이다 — 그 둘을 통과로 세면 관문이 옛 도구의 느슨함을 층만 바꿔 그대로 물려받는다.
 * (층 이름은 dispatcher.ts 체인훑기의 `층:` 칸 글자 그대로다.)
 */
const 도착지아닌층 = new Set(["가드레일", "되묻기"]);

/** 이 말이 실제로 **닿는** 자리들 — 차단·되묻기를 걷어 낸 나머지, 체인 순서 그대로 */
async function 실제도착(말: string): Promise<도착단계[]> {
  const 걸림 = await 결정적도착지(말, { 역할: "admin" });
  return 걸림.filter((s) => !도착지아닌층.has(s.층));
}

/** 데이터에 달린 갈래만 걸렸는가 — 「간다」고 단정할 수 없는 자리다(dispatcher 도착단계.조건부) */
const 데이터에달림뿐 = (단계: 도착단계[]) => 단계.length > 0 && 단계.every((s) => s.조건부);

interface 예외 {
  /** 실제로 재는 질문 — 수확의 질문 필드(○○ 치환 후)와 글자가 같아야 한다 */
  말: string;
  갈래: "법령" | "쓰기" | "읽기";
  이유: string;
  /** **grep 되는 원문 조각 + 파일 이름** — 줄 번호는 몇 시간이면 낡는다(머리글 참조) */
  근거: string;
  확인일: string;
}

const 법령이유 =
  "법제처 연동(설정)이 꺼져 있으면 law_lookup이 forcedToolFor의 available 게이트(registry.ts의 lawOn)를 " +
  "통과하지 못해 ⑨로 빠진다 — 연동을 켜면 결정적이 된다(FORCED_INTENTS[57])";
const 법령근거 = 'agentloop.ts `tool: "law_lookup"` · registry.ts `lawOn` · routes.ts [57] 왜 칸';

const 쓰기이유 =
  "쓰기 지시라 결재판이 사람에게 확인받은 뒤 실행한다 — 모델이 대상을 잘못 골라도 승인 창에서 걸러진다" +
  "(guidance-routing.test.ts:52-54와 같은 예외). 쓰기흐름인가() 또는 guidance-check.mjs의 쓰기말 중 " +
  "하나 이상에 걸리는지는 아래 ㉤이 스스로 잰다";

const 읽기이유 =
  "route-explain(결정적도착지) 실측 — 결정적 갈래 어디에도 안 걸린다(⑨ 모델 선택). 새 라우팅 갈래를 " +
  "만드는 일이라 B-2(라우팅 변경 사슬)로 미룬다";

// ⚠ 이 표는 「고쳤다」가 아니라 「아직 못 고쳤다 — 왜」다. 줄이 늘면 실패하게 해 둔다(아래 ㉢).
//   낡은 줄(더 이상 수확 안 되거나 이제 결정적으로 걸리는 것)도 실패하게 해 둔다(아래 ㉡).
//   [B-1 · 2026-09-13] 법령 10줄은 그대로(36 → 54). 증감을 정확히 적는다 —
//   **−2**: 두 자리 치환 수리로 두 줄이 ⑨를 벗어났다(→ 데이터에달려도봐줌으로 이동).
//           ⚠ 치환으로 **문구가 바뀐 줄은 3줄**인데 갈래가 바뀐 줄은 2줄이다. 앞 판 주석이
//           이 둘을 섞어 「3줄」이라 적었다(2026-09-13 검토관 적발 ⑤ — 세 번째 줄
//           「…오탐으로 무시 처리해줘」는 문구만 갱신되고 쓰기 예외로 그대로 남아 있다).
//           산수: 36 − 2 + 14(조사 필터) + 6(짝 맞추기) = 54.
//   **+14**: 조사 필터 수리로 26줄이 새로 수확돼 그중 13줄이 여기로, 1줄(이거 쉽게 설명해줘)은
//           「되묻기 전용」이라는 별도 사유로 들어왔다.
//   **+6**: 짝 맞추기 수리(머리글 ③)로 13줄이 새로 수확돼 그중 6줄이 ⑨였다.
const 아직결정적아님: 예외[] = [
  // ㉮ 법령 연동 꺼짐(10) — 2026-09-13 실측(변경 없음)
  { 말: "개인정보 보호법 찾아줘", 갈래: "법령", 이유: 법령이유, 근거: `screenguide.ts lawlookup can[] \`"개인정보 보호법 찾아줘"\` · ${법령근거}`, 확인일: "2026-09-13" },
  { 말: "개인정보 안전성 확보조치 기준 고시 찾아줘", 갈래: "법령", 이유: 법령이유, 근거: `screenguide.ts lawlookup can[] \`고시·훈령(행정규칙)\` 줄 · ${법령근거}`, 확인일: "2026-09-13" },
  { 말: "개인정보 유출 관련 대법원 판례 있어?", 갈래: "법령", 이유: 법령이유, 근거: `screenguide.ts lawlookup can[] \`판례와 사건번호·선고일\` 줄 · ${법령근거}`, 확인일: "2026-09-13" },
  { 말: "접속기록 몇 년 보관해야 해?", 갈래: "법령", 이유: 법령이유, 근거: `screenguide.ts lawlookup can[] \`사내 규정과 함께 대조하려면\` 줄 · ${법령근거}`, 확인일: "2026-09-13" },
  { 말: "개인정보 유출 관련 판례 찾아줘", 갈래: "법령", 이유: 법령이유, 근거: `nextguide.ts \`"개인정보 유출 관련 판례 찾아줘"\` · ${법령근거}`, 확인일: "2026-09-13" },
  { 말: "개인정보 보호법 제34조 알려줘", 갈래: "법령", 이유: 법령이유, 근거: `scenarios.ts \`"개인정보 보호법 제34조 알려줘"\` · ${법령근거}`, 확인일: "2026-09-13" },
  { 말: "GIJO AS 서버 관련 판례 찾아줘", 갈래: "법령", 이유: 법령이유, 근거: `lawlookup.html 질문문구() \`esc(이름) + " 관련 판례 찾아줘"\` · ${법령근거}`, 확인일: "2026-09-13" },
  { 말: "GIJO AS 서버 고시 찾아줘", 갈래: "법령", 이유: 법령이유, 근거: `lawlookup.html 질문문구() \`esc(이름) + " 고시 찾아줘"\` · ${법령근거}`, 확인일: "2026-09-13" },
  { 말: "GIJO AS 서버 찾아줘", 갈래: "법령", 이유: 법령이유, 근거: `lawlookup.html 질문문구() \`이름 + " 찾아줘"\`(법령 갈래 기본) · ${법령근거}`, 확인일: "2026-09-13" },
  { 말: "개인정보의 안전성 확보조치 기준 고시 찾아줘", 갈래: "법령", 이유: 법령이유, 근거: `lawlookup.html FAQ \`안전성 확보조치 기준\` 항 · ${법령근거}`, 확인일: "2026-09-13" },

  // ㉯ 쓰기(21) — 2026-09-13 실측(원래 11 + B-1 조사필터 복구 7 + B-1 짝맞추기 복구 2 + B-2 갈래이동 1).
  //    쓰기흐름인가()·쓰기말 실측값은 ㉤이 잰다
  // [B-2 신규] writeflow.쓰기명령꼴인가()·guidance-check 쓰기말에 「담당\s*부서」를 같은 커밋에서
  //   더해(⑥) 이 줄을 「읽기(잣대 어긋남)」에서 「쓰기(정상)」로 옮긴다. 라우팅은 안 바꿨다 — 여전히
  //   ⑨(FORCED 규칙이 없다, assign_owner에 강제 규칙을 세우는 일은 이번 범위 밖)지만 갈래가
  //   뜻(쓰기)과 잣대(둘 다 true)에 맞게 정직해졌다.
  { 말: "GIJO AS 서버 자산 담당부서 지정해줘", 갈래: "쓰기", 이유: `${쓰기이유}. [B-2] 잣대 수리 전엔 쓰기흐름인가()·쓰기말 둘 다 false였다 — writeflow.쓰기명령꼴인가()와 guidance-check.mjs 쓰기말에 「담당\\s*부서」를 더해(같은 커밋) 이제 둘 다 true다. 실행 도구(assign_owner)에 FORCED 규칙을 세우는 일은 안 했으므로 여전히 ⑨ — 결재판이 실제로 뜨려면 그 규칙까지 있어야 한다(다음 라운드 대상)`, 근거: 'agentloop.ts `"○○ 자산 담당부서 지정해줘"`', 확인일: "2026-09-13" },
  { 말: "미배정 취약점 담당자 배정해줘", 갈래: "쓰기", 이유: 쓰기이유, 근거: 'handlers.ts `"미배정 취약점 담당자 배정해줘"`', 확인일: "2026-09-13" },
  { 말: "1번 담당자 배정해줘", 갈래: "쓰기", 이유: 쓰기이유, 근거: 'handlers.ts `"1번 담당자 배정해줘"`', 확인일: "2026-09-13" },
  { 말: "GIJO AS 서버 SBOM 만들어줘", 갈래: "쓰기", 이유: 쓰기이유, 근거: 'handlers.ts `"○○ SBOM 만들어줘"`', 확인일: "2026-09-13" },
  { 말: "SBOM 만들어줘", 갈래: "쓰기", 이유: 쓰기이유, 근거: 'handlers.ts `"SBOM 만들어줘"`(이름 없이 적어 준 자리)', 확인일: "2026-09-13" },
  { 말: "AI 자산 취약점 담당자 배정해줘", 갈래: "쓰기", 이유: 쓰기이유, 근거: 'agentloop.ts `"AI 자산 취약점 담당자 배정해줘"`', 확인일: "2026-09-13" },
  { 말: "인수인계 문서 만들어줘", 갈래: "쓰기", 이유: 쓰기이유, 근거: 'agentloop.ts `"인수인계 문서 만들어줘"`', 확인일: "2026-09-13" },
  { 말: "보안제품 등록해줘", 갈래: "쓰기", 이유: 쓰기이유, 근거: 'handlers.ts·nextguide.ts `"보안제품 등록해줘"`', 확인일: "2026-09-13" },
  { 말: "경계 방화벽 FW-01 등록해줘", 갈래: "쓰기", 이유: 쓰기이유, 근거: 'products.html `"경계 방화벽 FW-01 등록해줘"`', 확인일: "2026-09-13" },
  { 말: "GIJO AS 서버 다시 넣어줘", 갈래: "쓰기", 이유: 쓰기이유, 근거: 'mydocs.html `선택.id + " 다시 넣어줘"`', 확인일: "2026-09-13" },
  { 말: "GIJO AS 서버 취약점 담당자 배정해줘", 갈래: "쓰기", 이유: `${쓰기이유}. ⚠ 이 칩은 수확을 고치기 전까지 **통째로 안 세어지고 있었다**(map-view의 작은따옴표 이음매)`, 근거: 'map-view.js `esc(이름) + " 취약점 담당자 배정해줘"`', 확인일: "2026-09-13" },
  // [B-1 신규] 조사 앞머리 필터가 「이」로 시작하는 이 7줄을 통째로 떨구고 있었다 — 필터를
  //   좁힌 뒤 처음 수확됐다. 전부 쓰기말(배정/처리해/생성해)에 걸려 결재판이 받쳐 준다.
  { 말: "이거 조치완료 처리해줘", 갈래: "쓰기", 이유: `${쓰기이유}. [B-1] 조사 필터가 떨구고 있던 줄 — 필터 좁힘(guidance-check.mjs 「조사필터떨굼목록」이 세는 그 줄)으로 처음 수확됨`, 근거: 'nextguide.ts verify_finding 칸 · handlers.ts `"이거 조치완료 처리해줘"로 확정하세요`', 확인일: "2026-09-13" },
  { 말: "이 취약점 담당자 배정해줘", 갈래: "쓰기", 이유: `${쓰기이유}. [B-1] 조사 필터가 떨구고 있던 줄`, 근거: 'agentloop.ts 되물음() `"이 취약점 담당자 배정해줘"`', 확인일: "2026-09-13" },
  { 말: "가장 급한 취약점에 담당자 배정해줘", 갈래: "쓰기", 이유: `${쓰기이유}. [B-1] 조사 필터가 떨구고 있던 줄 — 「가」로 시작해 예전 필터가 첫 글자만 보고 걸렀다`, 근거: 'agentloop.ts nextguide today 칸 · briefing 칸 `"가장 급한 취약점에 담당자 배정해줘"`', 확인일: "2026-09-13" },
  { 말: "이 건 담당자 GIJO AS 서버로 배정해줘", 갈래: "쓰기", 이유: `${쓰기이유}. [B-1] 조사 필터가 떨구고 있던 줄(원문 「이 건 담당자 ○○로 배정해줘」)`, 근거: 'agentloop.ts nextguide urgent_todo 칸 `"이 건 담당자 ○○로 배정해줘"`', 확인일: "2026-09-13" },
  { 말: "이거 담당자 배정해줘", 갈래: "쓰기", 이유: `${쓰기이유}. [B-1] 조사 필터가 떨구고 있던 줄`, 근거: 'dispatcher.ts nextChips · scenarios.ts `"이거 담당자 배정해줘"`', 확인일: "2026-09-13" },
  { 말: "이 자산 SBOM 생성해줘", 갈래: "쓰기", 이유: `${쓰기이유}. [B-1] 조사 필터가 떨구고 있던 줄`, 근거: 'nextguide.ts get_asset·register_asset 칸 `"이 자산 SBOM 생성해줘"`', 확인일: "2026-09-13" },
  { 말: "이거 조치 완료 처리해줘", 갈래: "쓰기", 이유: `${쓰기이유}. [B-1] 조사 필터가 떨구고 있던 줄(공백 있는 꼴 — 「이거 조치완료 처리해줘」와 별개 문구)`, 근거: 'console.js `f.owner ? "이거 조치 완료 처리해줘" : …`', 확인일: "2026-09-13" },
  // [B-1 수리] 두 자리를 서로 다른 실이름으로 채우도록 고쳤다(guidance-check.mjs) — 이 줄은
  //   그래도 여전히 ⑨다(update_finding_status에 강제 규칙이 없다, B-2 대상). 문구만 갱신.
  { 말: "sample-web01 Apache Log4j RCE 오탐으로 무시 처리해줘", 갈래: "쓰기", 이유: `${쓰기이유}. [B-1] 두 자리를 실이름(자산 id + 취약점 이름)으로 갱신 — 뜻은 실행(update_finding_status)인데 강제 규칙이 없어 여전히 ⑨, 배열에 규칙을 새로 세우는 일이라 B-2 대상`, 근거: 'session-explorer.js `" 오탐으로 무시 처리해줘"`', 확인일: "2026-09-13" },
  // [B-1 신규 · 쓰기 2] 짝 맞추기 수리(머리글 ③)로 처음 수확된 줄 — `hint: "결재판"`(3자) 뒤에
  //   붙어 있어 여는 따옴표를 잃고 통째로 사라져 있었다. **둘 다 결재판이 뜨는 쓰기 칩**이다.
  { 말: "GIJO AS 서버의 가장 급한 취약점 담당자와 기한을 배정해줘", 갈래: "쓰기", 이유: `${쓰기이유}. [B-1] 짝 맞추기 수리로 처음 수확된 줄 — 여태 어느 관문도 안 재던 칩이다`, 근거: 'session-explorer.js `(a.name || a.id) + "의 가장 급한 취약점 담당자와 기한을 배정해줘"`', 확인일: "2026-09-13" },
  { 말: "sample-web01 Apache Log4j RCE 담당자와 기한 배정해줘", 갈래: "쓰기", 이유: `${쓰기이유}. [B-1] 짝 맞추기 수리로 처음 수확된 줄 — 두 자리 칩이지만 형제 둘(조치 절차·재스캔 확인)과 달리 **어느 대표값으로 재도 ⑨**라 여기 둔다(실측: Log4j·OpenSSH·Oracle·SSL 넷 다 ⑨)`, 근거: 'session-explorer.js `p.assetId + " " + name + " 담당자와 기한 배정해줘"`', 확인일: "2026-09-13" },

  // ㉰ 읽기인데 아직 ⑨(13 — 되묻기 전용 1건 포함)
  //    [B-2 · 2026-09-13] 24 → 13. **−9**: B-2가 결정적으로 만든 8줄(자산 목록·자산 취약점·
  //    위협·명령 추천·레드팀 2줄(제품명 붙은 것·「이」로 시작하는 것)·점검 일정 2줄) + reword로
  //    문구가 바뀐 1줄(침해 의심 — 「침해사고 의심」으로 두 글자 늘어 [33]로 확정, 옛 문구는 더
  //    이상 수확되지 않는다). 전부 기대도착으로 옮겼다(아래).
  { 말: "학습 루프 상태 알려줘", 갈래: "읽기", 이유: `${읽기이유}. 옛 도구는 LEARN_TOPIC_RE로 초록이라 했는데, 제품은 LEARN_TOPIC_RE **와** LEARN_RUN_RE가 함께 걸릴 때만 그리로 간다. ⚠ registry.ts 113개 도구에 「학습 루프 상태」를 답하는 도구가 아예 없다 — 도구 신설은 evalgate 재실행을 부르는 별건`, 근거: 'learnloop.html `"학습 루프 상태 알려줘"`', 확인일: "2026-09-13" },
  { 말: "GIJO AS 서버 재스캔해줘", 갈래: "읽기", 이유: `${읽기이유}. ⚠ 뜻은 실행(쓰기)인데 쓰기말에 「재스캔」이 없어 읽기로 센다 — registry.ts 113개 도구에 스캔을 실제로 돌리는 도구가 없다(scan_status는 상태 요약, register_scan_draft는 초안 등록). 도구 신설이 필요해 B-2로도 못 닫는다`, 근거: 'session-explorer.js `" 재스캔해줘"`', 확인일: "2026-09-13" },
  { 말: '"GIJO AS 서버" 어떻게 처리하면 될지 알려줘', 갈래: "읽기", 이유: `${읽기이유}. 고른 로그 한 줄을 따옴표째 넣어 보내는 칩이라 앞머리가 따옴표다. ★검토(B-1 도착 이름 대조 — 고치지 않고 표에 남긴다): 이 substitute("GIJO AS 서버")로는 ⑨지만, 설계관이 실제 로그 문구로 재보니(「"방화벽 로그 이상 급증" 어떻게 처리하면 될지 알려줘」) analysis_status로, (「"승인 대기 3건" …」) approval_status로 샌다 — 칩 hint는 「판단」인데 본문 낱말이 엉뚱한 현황 도구를 잡아채는 자리다(새 라우팅 갈래가 필요해 B-2 대상)`, 근거: 'session-explorer.js `\'"\' + text + \'" 어떻게 처리하면 될지 알려줘\'`', 확인일: "2026-09-13" },
  { 말: '"GIJO AS 서버" 관련 취약점 정리해줘', 갈래: "읽기", 이유: `${읽기이유}. 위와 같은 칩(고른 로그 한 줄). ★검토(B-1 도착 이름 대조): 칩 hint는 search인데, 설계관이 실제 로그 문구로 재니(「"방화벽 로그 이상 급증" 관련 취약점 정리해줘」) analysis_status로 샌다 — hint와 실측 도착이 어긋나는 자리(B-2 대상)`, 근거: 'session-explorer.js `\'"\' + text + \'" 관련 취약점 정리해줘\'`', 확인일: "2026-09-13" },
  // [B-1 신규 · 읽기 6] 조사 앞머리 필터가 「이」로 시작하는 이 줄들을 통째로 떨구고 있었다.
  { 말: "이 자산 취약점 우선순위 알려줘", 갈래: "읽기", 이유: `${읽기이유}. [B-1] 조사 필터가 떨구고 있던 줄`, 근거: 'agentloop.ts nextguide get_asset 칸 `"이 자산 취약점 우선순위 알려줘"`', 확인일: "2026-09-13" },
  { 말: "이 이벤트 어떻게 대응해?", 갈래: "읽기", 이유: `${읽기이유}. [B-1] 조사 필터가 떨구고 있던 줄`, 근거: 'logguide.ts · screenguide.ts `"이 이벤트 어떻게 대응해?"`', 확인일: "2026-09-13" },
  { 말: "이번 주 보고서 몇 건이야?", 갈래: "읽기", 이유: `${읽기이유}. [B-1] 조사 필터가 떨구고 있던 줄`, 근거: 'screenguide.ts `"이번 주 보고서 몇 건이야?"`', 확인일: "2026-09-13" },
  { 말: "이거 조치 절차 알려줘", 갈래: "읽기", 이유: `${읽기이유}. [B-1] 조사 필터가 떨구고 있던 줄 — 한 자리짜리 형제(「○○ 취약점 조치 절차 알려줘」)는 「취약점」 낱말 덕에 플레이북영토인가를 지나 결정적인데, 이 줄은 「취약점」이 없어 REMEDIATION_INTENT_RE는 걸려도 플레이북영토인가가 막는다(playbook.ts 「export function 플레이북영토인가」)`, 근거: 'screenguide.ts `"이거 조치 절차 알려줘"`', 확인일: "2026-09-13" },
  // [B-1 신규 · 되묻기 전용 1] — 이 칩은 화면에서 항목을 먼저 고른 뒤(옵션.선택 있음)에만
  //   노출되는데, 이 관문은 선택 없이 잰다. dispatcher.ts의 설명칩_RE는 **바로 이 문구를 위해**
  //   만들어진 안전장치다(검토관 B중2 — "기능어를 빼면 4자라 대명사 관문을 비켜 갔다") — 선택
  //   없이 이 말만 던지면 "가리킬 것 없는 대명사"로 되물어야 **맞는 동작**이다. 갈래는 읽기에
  //   두되(법령·쓰기 어디에도 안 맞는다) 「결정적 도착 없음」이 아니라 「되묻기로만 걸린다」는
  //   점이 달라 아래 ★★ 읽기 안내 시험에서 별도로 봐준다(되묻기로샘 예외).
  { 말: "이거 쉽게 설명해줘", 갈래: "읽기", 이유: "결정적도착지 실측 — 「가리킬 것 없는 대명사」(dispatcher.ts 설명칩_RE)로만 걸리고 실제 도구 도착은 없다. 선택 없이 재는 이 관문의 측정 방식과, 「선택 후에만 쓰라」는 실사용 맥락이 어긋나는 자리 — 새 라우팅 갈래가 아니라 선택 컨텍스트를 관문에 실어야 닫히므로 B-2로도 안 닫힌다", 근거: 'console.js data-prompts `"이거 쉽게 설명해줘"` · dispatcher.ts `const 설명칩_RE`(같은 파일 `설명칩_RE.test(t.trim())`에서 쓴다 — 줄 번호로 안 적는다)', 확인일: "2026-09-13" },
  // [B-1 신규 · 읽기 4] 짝 맞추기 수리(머리글 ③)로 처음 수확된 줄 — 세 글자 hint(`"RAG"`·
  //   `"리포트"`·`"SLA"`) 뒤에 붙어 있어 여는 따옴표를 잃고 사라져 있었다. 전부 화면 칩이라
  //   **담당자가 누르면 그대로 보내지는 말**인데, 여태 어느 관문의 모집단에도 없었다.
  { 말: "조치한 거 확인해줘", 갈래: "읽기", 이유: `${읽기이유}. [B-1] 짝 맞추기 수리로 처음 수확된 줄 — 업무 5단계 안내가 4단계에서 이 말을 적어 준다`, 근거: 'handlers.ts 업무 단계표 4번 칸 `말: \'"조치한 거 확인해줘"\'`', 확인일: "2026-09-13" },
  { 말: "GIJO AS 서버 매뉴얼에서 로그 설정 방법 찾아줘", 갈래: "읽기", 이유: `${읽기이유}. [B-1] 짝 맞추기 수리로 처음 수확된 줄 — 칩 hint는 RAG(문서 검색)인데 search 규칙의 앞말 토큰이 공백 있는 제품 이름을 못 받아 ⑨다(B-2의 search 넓힘 대상과 같은 뿌리)`, 근거: 'session-explorer.js `pd.name + " 매뉴얼에서 로그 설정 방법 찾아줘"`', 확인일: "2026-09-13" },
  { 말: "GIJO AS 서버 최근 운영 이슈 요약해줘", 갈래: "읽기", 이유: `${읽기이유}. [B-1] 짝 맞추기 수리로 처음 수확된 줄 — 칩 hint는 「리포트」인데 리포트 규칙이 「운영 이슈 요약」 꼴을 모른다`, 근거: 'session-explorer.js `pd.name + " 최근 운영 이슈 요약해줘"`', 확인일: "2026-09-13" },
  { 말: "SLA 기한이 임박하거나 초과한 조치 알려줘", 갈래: "읽기", 이유: `${읽기이유}. [B-1] 짝 맞추기 수리로 처음 수확된 줄. ⚠ 잣대 어긋남 — 뜻은 읽기(답이 그대로 나간다)인데 guidance-check.mjs의 쓰기말이 「기한」 한 낱말에 걸려 그쪽에서는 「쓰기 안내」로 센다. 갈래는 **뜻대로 읽기**로 둔다(결재판이 받쳐 주지 않으므로 쓰기 예외의 전제가 성립하지 않는다)`, 근거: 'session-explorer.js `"SLA 기한이 임박하거나 초과한 조치 알려줘"`', 확인일: "2026-09-13" },
];

/**
 * 데이터에 달린 갈래만 걸린 안내 — 한 줄(2026-09-13 메인이 이유를 들고 넣음).
 * 생기면 「제목이 겹칠 때만 간다」를 결정적이라 말하게 되므로, 사람이 이유를 들고 와야 한다.
 *
 * 「FW-01 하드닝 점검 돌려줘」(screenguide.ts verify.html can[] · 관문 동사 목록에 「돌려줘」를 더한 날 수확됨):
 *   [3] 하드닝 점검 갈래가 배열 안에서 **등록 장비 이름이 맞으면** scan_hardening_target(결재판), 아니면
 *   run_hardening_scan(자기 점검)으로 갈리므로 agentloop이 `데이터의존: true`를 돌려준다(D 사슬 2a4ea921).
 *   제목 지목처럼 「데이터가 없으면 아무 데도 안 간다」가 아니라 **어느 쪽이든 결정적 도구로 간다** — 도착
 *   이름만 데이터에 달린다. 그래서 ㉥의 뜻(간다고 단정 못 함)에는 안 걸리지만, 조건부 표식 하나로는 둘을
 *   못 가르므로 이유를 적고 봐준다. 갈래가 「데이터 없으면 ⑨」로 바뀌면 이 줄을 빼야 한다.
 * 「○○ 미검토로 되돌려줘」(dispatcher.ts 판정 뒤 「▸ 이어서」 칩 — 같은 날 「돌려줘」 수확으로 드러남):
 *   [7] 내 할 일 다시 열기(reopen_task)가 **열려 있는 내 할 일 이름과 겹칠 때만** 채 가고, 안 겹치면 ⑨다.
 *   되돌리기는 쓰기라 결재판이 받쳐 주고, 그 칩은 방금 판정한 그 항목 뒤에만 붙으므로 이름이 겹치는 것이
 *   전제다 — 제목 지목과 같은 「데이터가 맞아야 간다」 부류가 맞아 봐준다. 결정적으로 만들려면(항목
 *   id를 칩에 실어 [7]이 이름 대조 없이 가게) B단계 라우팅 변경이다.
 * 「sample-web01 Apache Log4j RCE 조치 절차 알려줘」·「sample-web01 재스캔해서 Apache Log4j RCE 해결됐는지
 *   확인해줘」(session-explorer.js 취약점 칩 두 개 — `p.assetId + " " + name + …`, name = finding_type):
 *   [2026-09-13 검토관 적발 ②로 여기로 옮김. 앞 판은 기대도착에 「확정」으로 올려 두었다.]
 *   위 둘과 **결이 다른 데이터 의존**이다 — 갈림이 dispatcher의 `조건부` 표식으로 안 드러난다.
 *   갈림목은 playbook.ts `플레이북영토인가`(playbookFor가 generic이 아니거나 본문에 「취약점」 낱말).
 *   칩이 잇는 finding_type이 무엇이냐에 따라 도착이 통째로 갈린다 — 실측(route-explain --no-build,
 *   2026-09-13, 같은 시드 자산의 finding 4건):
 *     · `Apache Log4j RCE`                          → [27] formatRemediation (playbook rce 매칭)
 *     · `OpenSSH < 9.6 사용자 열거 (CVE-2024-6387)`   → ⑨
 *     · `Oracle DB CPU 미적용 (CVE-2022-21432 외 16건)` → ⑨
 *     · `SSL 인증서 만료 임박`                        → ⑨
 *   수확이 쓰는 대표값(guidance-check.mjs `대표취약점이름`)이 하필 매칭되는 이름이라 「결정적」으로
 *   보였을 뿐이다. 대표값 하나에 달린 초록을 「확정」이라 적으면 그게 곧 거짓 초록이다.
 *   ★검토(뜻과 도착의 어긋남 — 고치는 일은 B-2/별건): 「재스캔해서 … 해결됐는지 확인해줘」 칩은
 *   hint가 「스캔→비교」인데 실제 도착은 **조치 절차 플레이북**(formatRemediation)이다. 스캔을 다시
 *   돌려 달라는 말이 절차 안내로 가는 자리라, 「긴급한 취약점 알려줘 → search」와 같은 부류다.
 *   ⚠ 조건부 표식이 없으므로 ㉥(데이터에달림뿐)에는 원래 안 걸린다 — 이 목록에 두는 뜻은
 *     「도착 이름을 못 박지 않는다」이고, 커버리지 시험(★★★ 두 번째)이 계속 훑게 하는 것이다.
 * ⚠ [B-1] 기대도착(아래)은 이 네 줄을 **넣지 않는다** — 데이터에 따라 이름이 갈리는 자리를 결정적이라
 *   못 박으면 또 거짓이다(도착 이름 대조표를 시험에 넣는 법 ⑥).
 */
const 데이터에달려도봐줌: string[] = [
  "FW-01 하드닝 점검 돌려줘",
  "GIJO AS 서버 미검토로 되돌려줘",
  "sample-web01 Apache Log4j RCE 조치 절차 알려줘",
  "sample-web01 재스캔해서 Apache Log4j RCE 해결됐는지 확인해줘",
];

/**
 * [B-1 · 2026-09-13 신설] **도착 이름 대조표** — 「걸린다」를 넘어 「어디로 걸리는가」까지 잰다.
 *
 * 열쇠 = 수확의 `질문` 글자 그대로(○○ 치환 후). 값 = `실제도착(질문)[0].도착` 글자 그대로 —
 * 사람이 지어내지 않는다, 전부 `결정적도착지`를 실제로 불러 얻은 값이다(위 아직결정적아님과
 * 같은 원칙). 판정은 아래 ★★★ 시험이 한다: `실제도착(x.질문)[0].도착 === 기대도착[x.질문]`.
 *
 * ㉮ **두 표(기대도착 · 아직결정적아님)의 열쇠는 절대 안 겹친다** — 겹치면 「결정적이면서
 *    동시에 ⑨」라는 모순이라, 시험이 그 겹침 자체를 실패로 잡는다.
 * ㉯ 조건부(데이터에 따라 도착이 갈리는) 갈래는 **여기 넣지 않는다** — 위 `데이터에달려도봐줌`이
 *    계속 받는다(예: FW-01 하드닝 점검 돌려줘 · GIJO AS 서버 미검토로 되돌려줘).
 * ㉰ ★검토 표식 1건(「긴급한 취약점 알려줘」) — 실제로 결정적이라 여기 들어가지만, 안내가 약속한 뜻과 실제 도착이
 *    **어긋나 보인다.** 시험은 통과시키되(오늘 실측값을 그대로 기대값으로 적었으니까) 주석으로
 *    남긴다 — 고치는 일은 B-2/별건이다.
 */
const 기대도착: Record<string, string> = {
  // [2026-09-13 UI D] settings.html scOpenGuide()의 ⓘ 버튼 문구 — HELP_RE의 「사용법」
  // 강한 가지가 화면·구역 매치와 무관하게 이긴다(screenguide.isHelpIntent). route-explain
  // 실측(--no-build): [22] 화면안내 → formatScreenGuide(지금 화면).
  "자가 진단 사용법 알려줘": "formatScreenGuide(지금 화면)",
  "GIJO AS 서버 가드레일 기재해줘": "set_aibom_field",
  "GIJO AS 서버 AI-BOM 보여줘": "aibom_status",
  "이번 주 취약점 보고서 만들어줘": "generateReport",
  "이번 주 보고서 만들어줘": "generateReport",
  "GIJO AS 서버 패키지 목록 읽어줘": "collect_packages",
  "새로 들어온 문서 알려줘": "recent_documents",
  "문서 목록 보여줘": "knowledge_status",
  "패키지 목록 읽어줘": "collect_packages",
  "이 취약점 조치 절차 알려줘": "formatRemediation",
  "GIJO AS 서버 서버 조치 검증 실행해줘": "verify_finding",
  "자산 현황 보여줘": "assetStatusAnswer(카드)",
  "지금 가장 급한 취약점 알려줘": "today",
  "조치 검증 결과 알려줘": "hardeningStatusAnswer(카드)",
  "이번 주 보안 현황 리포트 만들어줘": "generateReport",
  "컴플라이언스 리포트 만들어줘": "generateReport",
  "이번 달 보안 KPI 리포트 만들어줘": "generateReport",
  "이번 달 리포트 만들어줘": "generateReport",
  "보안 KPI 현황 알려줘": "kpi_status",
  "이거 해도 돼?": "runActionCheck",
  "통합 분석 현황 알려줘": "analysis_status",
  "자산 몇 대야?": "list_assets",
  "오늘 새로 들어온 위협 있어?": "threats",
  "미조치 취약점 알려줘": "finding_status",
  "KEV 몇 건이야?": "finding_status",
  "SBOM 없는 자산 알려줘": "sbom_coverage",
  "승인 대기 뭐 있어?": "approval_status",
  "미배정 몇 건이야?": "finding_status",
  "점검 안 한 장비 있어?": "hardeningStatusAnswer(카드)",
  "보안 점수 알려줘": "kpi_status",
  "조각 없는 문서 알려줘": "doc_chunk_gaps",
  "인수인계는 어떻게 해?": "formatScreenGuide + 화면 열기",
  "올린 문서가 잘 반영됐는지 확인해줘": "knowledge_status",
  "지식 번들 넣어줘": "knowledge_bundle_import",
  "기한 지난 점검 알려줘": "maintenance_status",
  // ★검토 — screenguide.ts dashboard.html can[] `"긴급한 취약점 알려줘" — 미조치 고위험 항목`이 이 안내를 「미조치 고위험 항목」으로 약속하는데 search는
  //   문서(RAG) 검색이다. 오늘 실측 그대로 기대값을 적어 시험은 통과시킨다(고치면 이 줄이
  //   먼저 빨간불을 내 알려준다) — 고치는 일은 B-2/별건.
  "긴급한 취약점 알려줘": "search",
  "하드닝 점검 스케줄 알려줘": "hardening_schedule_list",
  "SBOM 현황 알려줘": "sbom_coverage",
  "공격 경로 보여줘": "formatAttackPaths",
  "재스캔 상태 알려줘": "scan_status",
  "요청 현황 알려줘": "request_status",
  "검증 현황 보여줘": "hardeningStatusAnswer(카드)",
  "하드닝 점검해줘": "run_hardening_scan",
  "유지보수 점검 일정 알려줘": "maintenance_status",
  "보안제품 현황 알려줘": "product_status",
  "경영진 보고용으로 리포트 만들어줘": "generateReport",
  "지식 저장소 상태 알려줘": "knowledge_status",
  "작업 기록 보여줘": "work_session_status",
  "컴플라이언스 현황 알려줘": "compliance_status",
  "AI-BOM 현황 알려줘": "aibom_status",
  "레드팀 지난 결과 알려줘": "redteam_status",
  "자가 진단 해줘": "system_health",
  "어댑터 현황 알려줘": "adapter_status",
  "이것들 조치 요청서 만들어줘": "create_request_doc",
  "이거 검증 실행해줘": "verify_finding",
  "이 화면 사용법 알려줘": "formatScreenGuide(지금 화면)",
  '"GIJO AS 서버" 사용법 알려줘': "formatScreenGuide(지금 화면)",
  "최근 작업 기록 알려줘": "work_session_status",
  "최근 탐지 내역 알려줘": "threats",
  "침해사고 히스토리 보여줘": "incident_cases",
  "스캔결과 폴더 지켜봐 줘": "watch_folder_add",
  "이 취약점 조치 요청서 만들어줘": "create_request_doc",
  "지켜보는 폴더 알려줘": "watch_folder_list",
  "이번 주 리포트 스케줄 확인해줘": "report_schedule_list",
  "GIJO AS 서버 취약점 조치 절차 알려줘": "formatRemediation",
  // [B-1 · 2026-09-13 검토관 적발 ②] 두 자리 치환 칩 두 줄은 **여기 없다** — 대표 취약점 이름
  //   하나에 도착이 달려 있어서 위 `데이터에달려도봐줌`이 받는다(같은 시드의 다른 finding 3건은 ⑨).
  "오늘 제일 급한 취약점 상위 3건 정리해줘": "today",
  "구동 티어 사용법 알려줘": "formatScreenGuide(지금 화면)",
  "SMTP 설정 방법 알려줘": "howto 순서 안내 + 화면 열기",
  // [B-1 · 2026-09-13 짝 맞추기 수리로 처음 수확된 7줄] — 전부 route-explain --no-build 실측값.
  //   ★ 이 줄들은 **여태 어느 관문도 재지 않던 안내**다(수확 밖이었다) — 초록이 아니라 공백이었다.
  "기한 지난 일 보여줘": "urgent_todo",
  "우리 자산 현황 알려줘": "assetStatusAnswer(카드)",
  "이번 주 보안 현황을 요약해줘": "exec_brief",
  "이번 달 보안 지표를 지난달과 비교해줘": "kpi_status",
  // 판별자가 **함수**(scenarios.ts `isScenarioAsk`)라 guidance-check.mjs의 정규식 추출은 이 줄을
  //   「모델 판단」으로 오분류한다 — 그쪽은 함수경로확인 표가 받고, 도착 이름은 여기서 잰다.
  "시나리오 목록 보여줘": "scenarioAnswer(프롬프트북)",
  "이 제품 보안패치 요청서 만들어줘": "create_request_doc",
  "오늘 일일 브리핑 만들어줘": "briefing",
  // [B-2 · 2026-09-13 신규 10줄] FORCED_INTENTS 4건 넓힘 + 배열 끝 신규 2건 + MAINT_PROCEDURE_RE
  //   확장 + scenarios.ts reword로 결정적이 된 줄. 전부 route-explain --no-build 실측값 그대로.
  "자산 목록 보여줘": "list_assets",
  "GIJO AS 서버 자산 취약점 알려줘": "search",
  "내부 자산에 영향 주는 위협 알려줘": "threats",
  "점검 명령 추천해줘": "suggest_command",
  "GIJO AS 서버 레드팀 점검해줘": "run_redteam",
  "이 자산 레드팀 점검해줘": "run_redteam",
  "GIJO AS 서버 점검 일정 알려줘": "maintenance_status",
  "이번 달 점검 일정 뭐야?": "maintenance_status",
  // MAINT_PROCEDURE_RE에 「어떻게 해」 꼴을 더해 결정적이 됨(갈림 결정 (가)) — handlers.ts
  // runWorkSteps 되묻기 예시는 같은 커밋에서 「야간 백업 로그 확인 절차 알려줘」로 reword했다.
  "방화벽 점검 어떻게 해?": "explain",
  // scenarios.ts reword(「침해」→「침해사고」) — 옛 문구 「침해 의심될 때 대응 절차 알려줘」는
  // 더 이상 수확되지 않는다(위 아직결정적아님에서도 지웠다).
  "침해사고 의심될 때 대응 절차 알려줘": "침해사고초동절차",
};

describe("★★ guidance-check 관문의 느슨한 잣대 — 도착지는 제품 함수로만 잰다", () => {
  const 안내 = 수확();

  it("㉠ 수확이 헛돌지 않는가 — 오늘 132개, 129개 미만이면 추출이 눈을 감은 것이다", () => {
    // ⚠ 하한을 실측값에서 멀리 떨어뜨리면 **조용한 감소**가 초록으로 지나간다
    //   (첫 판은 하한 60인데 실측 83이었다 — 20개가 사라져도 빨간불이 안 났다).
    //   수확기(guidance-check.mjs)는 정규식이 소스 모양에 기대는 코드라, 제품 문구 꼴이
    //   바뀌면 조용히 덜 읽는다. 여유 3은 다른 실행자가 안내 한두 줄을 다듬는 것을
    //   견디기 위한 것이고, 그보다 크게 줄면 손이 아니라 **도구가 눈을 감은 것**이다.
    //   [B-1 · 2026-09-13] 93→119(조사 필터 좁힘 26개 복구)→**132**(따옴표 짝 맞추기 13개 복구)
    //   — 여유 3을 그대로 적용해 129. ⚠ 하한은 **재고 나서** 올린다(먼저 올려 두면 그 사이의
    //   손실이 초록으로 지나간다). 이 값은 2026-09-13 수리 뒤 실측 132에서 잡은 것이다.
    expect(
      안내.length,
      `안내 문구를 ${안내.length}개밖에 못 읽었다 — guidance-check.mjs 수확 로직이 눈을 감았을 수 있다(2026-09-13 B-1 실측 132)`,
    ).toBeGreaterThanOrEqual(129);
  });

  it("㉢ 예외표가 새 「눈감기 목록」이 되지 않는다 — 상한 44줄", () => {
    // [B-1 · 2026-09-13] 36 → 54(법령 10 변화없음 + 쓰기 11→20 + 읽기 15→24). 증감 산수는 위
    //   아직결정적아님 머리 주석에 적었다(−2 결정적화 · +14 조사필터 · +6 짝맞추기).
    // [B-2 · 2026-09-13] 54 → 44 — **줄었다**(라우팅을 넓혀 결정적으로 만들었으니 당연하다).
    //   −10: 방화벽 점검 어떻게 해(explain 확정) + 8줄 결정적화(자산목록·자산취약점·위협·
    //   명령추천·레드팀×2·점검일정×2) + 침해 의심 reword(옛 문구 소멸, 새 문구는 기대도착行).
    //   +1: 담당부서 갈래이동(읽기→쓰기, 표 안에서 옮긴 것이라 순감소는 −10+1행이 아니라
    //   테이블 총원에서 10개가 나간 것 — 갈래이동은 총원 불변). 54−10=44.
    //   먼저 낮춰 두지 않는다 — 라우팅을 넓히고 **실측한 뒤** 다시 못 박은 값이다.
    expect(
      아직결정적아님.length,
      `예외표가 ${아직결정적아님.length}줄이다 — 44줄을 넘겼다. 늘리려면 사람이 이유를 들고 온다(2026-09-13 B-2 실측 상한)`,
    ).toBeLessThanOrEqual(44);
  });

  it("예외표의 모든 줄에 이유·근거·확인일이 있다 — 빈 칸으로 못 채운다", () => {
    for (const e of 아직결정적아님) {
      expect(e.말.trim(), "말이 비어 있는 줄이 있다").not.toBe("");
      expect(e.이유?.trim(), `「${e.말}」의 이유가 비어 있다`).not.toBe("");
      expect(e.근거?.trim(), `「${e.말}」의 근거가 비어 있다`).not.toBe("");
      expect(e.확인일?.trim(), `「${e.말}」의 확인일이 비어 있다`).not.toBe("");
    }
  });

  it("㉡ 예외표는 지금도 수확되고 지금도 ⑨여야 한다 — 낡은 줄은 실패한다", async () => {
    const 수확된질문 = new Set(안내.map((x) => x.질문));
    for (const e of 아직결정적아님) {
      expect(
        수확된질문.has(e.말),
        `「${e.말}」이 더 이상 안내 문구로 수확되지 않는다 — 제품 문구가 바뀌었다면 이 줄을 지워라`,
      ).toBe(true);
      const 닿음 = await 실제도착(e.말);
      expect(
        닿음.length,
        `「${e.말}」이 이제 결정적으로 걸린다(도착: ${닿음[0]?.도착}) — 고쳐졌다는 뜻이니 예외표에서 이 줄을 지워라`,
      ).toBe(0);
    }
  });

  it("㉣ 기대도착과 예외표의 열쇠는 겹치지 않는다 — 겹치면 「결정적이면서 ⑨」라는 모순이다", () => {
    const 겹침 = Object.keys(기대도착).filter((k) => 아직결정적아님.some((e) => e.말 === k));
    expect(겹침, `두 표에 동시에 있는 줄: ${겹침.join(" · ")}`).toEqual([]);
  });

  it("㉧ 기대도착·데이터에달려도봐줌도 지금도 수확되어야 한다 — 낡은 줄은 실패한다", () => {
    // [2026-09-13 검토관 적발 ⑥] ㉡은 예외표에만 걸려 있었다. **더 큰 표(기대도착 74줄)에는
    //   그 대칭이 없어**, 제품 문구가 바뀌어 열쇠가 더 이상 수확되지 않으면 그 줄은 아무 시험도
    //   건드리지 않는 **죽은 줄**이 된다 — ★★★ 두 시험 모두 `안내`를 돌면서 `x.질문 in 기대도착`만
    //   보기 때문이다. 하한(㉠ 129, 여유 3)도 한두 줄 소멸은 통과시킨다.
    //   이 파일의 설계 원칙은 「낡은 줄은 실패하게 해 둔다」이므로 같은 장치를 여기에도 둔다.
    //   ⚠ 데이터에달려도봐줌도 같은 부류(하드코딩 사전)라 함께 잰다.
    const 수확된질문 = new Set(안내.map((x) => x.질문));
    const 죽은줄 = [...Object.keys(기대도착), ...데이터에달려도봐줌].filter((k) => !수확된질문.has(k));
    expect(
      죽은줄,
      "더 이상 안내 문구로 수확되지 않는 열쇠가 표에 남아 있다 — 제품 문구가 바뀌었으면 이 줄을 지우고, " +
        `수확기가 눈을 감은 것이면 그쪽을 고쳐라:\n${죽은줄.join("\n")}`,
    ).toEqual([]);
  });

  it("㉤ 쓰기로 표시한 예외는 실제로 쓰기다 — 쓰기흐름인가() 또는 guidance-check.mjs의 쓰기말", () => {
    // ⚠ 아래는 guidance-check.mjs `쓰기말`의 **사본이고, 이 시험은 그 사본으로 갈래를 판정한다**
    //   (첫 판의 주석은 「판정에 쓰지 않는다」였는데 바로 아랫줄이 판정에 쓰고 있었다).
    //   사본은 원본이 바뀌어도 안 따라오므로, 여기서 **원본과 글자가 같은지부터** 잰다.
    //   줄 번호로 가리키지 않는다 — 같은 파일을 고치면 번호가 곧바로 어긋난다.
    const 쓰기말 = /(배정|맡겨|기한|담당\s*부서|조치해|처리해|등록해|삭제|수집해|생성해|만들어|다시 넣|재인입)/;
    const 원본 = readFileSync(도구경로, "utf8").match(/const 쓰기말 = (\/.+\/);/);
    expect(원본, "guidance-check.mjs에서 `const 쓰기말 = /…/;`를 못 읽었다 — 이름이 바뀌었으면 여기도 고쳐라").toBeTruthy();
    expect(
      String(쓰기말),
      "guidance-check.mjs의 쓰기말과 이 시험의 사본이 어긋났다 — 원본을 그대로 옮겨 적어라",
    ).toBe(원본![1]);

    for (const e of 아직결정적아님.filter((x) => x.갈래 === "쓰기")) {
      const 쓰기맞다 = 쓰기흐름인가(e.말) || 쓰기말.test(e.말);
      expect(쓰기맞다, `「${e.말}」을 쓰기로 표시했는데 쓰기흐름인가()도 쓰기말도 아니다 — 갈래를 다시 골라라`).toBe(true);
    }
  });

  it("㉥ 데이터에 달린 갈래만 걸린 안내는 「결정적」이 아니다 — 지금은 0개", async () => {
    const 달림: string[] = [];
    for (const x of 안내) {
      const 닿음 = await 실제도착(x.질문);
      if (데이터에달림뿐(닿음) && !데이터에달려도봐줌.includes(x.질문)) 달림.push(`「${x.질문}」 → ${닿음[0].이름}`);
    }
    expect(
      달림,
      "이 안내는 **데이터가 맞아떨어질 때만** 그리로 간다(제목 지목·내 할 일 이름) — 결정적이라 말하면 거짓이다. " +
        `예외로 둘 이유가 있으면 데이터에달려도봄에 적어라:\n${달림.join("\n")}`,
    ).toEqual([]);
  });

  it("㉦ 이 관문이 헛돌지 않는가 — 차단·되묻기를 실제로 걷어 내는지 스스로 잰다", async () => {
    // 층 이름은 dispatcher.ts의 글자를 그대로 쓴다 — 거기서 이름이 바뀌면 이 관문은 아무것도
    // 안 걷어 내면서 조용히 초록이 된다(첫 판과 똑같은 느슨함으로 되돌아간다). 그래서 잰다.
    const 뜻모를말 = "ㅇㅇ";
    const 전부 = await 결정적도착지(뜻모를말, { 역할: "admin" });
    expect(
      전부.filter((s) => 도착지아닌층.has(s.층)).length,
      `되묻기·차단 층에 걸리는 표본을 못 찾았다(걸린 층: ${전부.map((s) => s.층).join(",") || "없음"}) — dispatcher.ts의 층 이름이 바뀌었는지 보라`,
    ).toBeGreaterThan(0);
    expect(
      await 실제도착(뜻모를말),
      "되묻기를 도착지로 세고 있다 — 도착지아닌층이 제 구실을 못 한다",
    ).toEqual([]);
  });

  it("★★ 읽기 안내는 제품 함수로 결정적이거나, 이유 있는 예외표 안에 있어야 한다", async () => {
    const 예외맵 = new Map(아직결정적아님.map((e) => [e.말, e]));
    const 안잡힘: string[] = [];
    const 되묻기로샘: string[] = [];
    for (const x of 안내) {
      const 전부 = await 결정적도착지(x.질문, { 역할: "admin" });
      const 닿음 = 전부.filter((s) => !도착지아닌층.has(s.층));
      if (닿음.length > 0) continue; // 제품 함수가 실제 도착지로 걸었다 — 통과
      if (예외맵.has(x.질문)) continue; // 이유·근거·확인일을 갖춘 예외 — 통과(⑨든 되묻기 전용이든)
      if (전부.length > 0) {
        // 차단·되묻기에만 걸렸고 예외표에도 없다 — 첫 판은 이것을 통과로 셌다
        되묻기로샘.push(`「${x.원문}」 → ${전부.map((s) => s.이름).join(" · ")}`);
        continue;
      }
      안잡힘.push(`「${x.원문}」(잴 때: ${x.질문})`);
    }
    expect(
      되묻기로샘,
      `제품이 「이렇게 치세요」라고 적어 준 말인데 **차단·되묻기로** 간다 — 안내가 자기 말을 못 지킨 것이다(이유 있는 예외는 위 아직결정적아님 표로):\n${되묻기로샘.join("\n")}`,
    ).toEqual([]);
    expect(
      안잡힘,
      `제품이 안내해 놓고 도착지가 없는(⑨ 모델 선택) 말이 예외표 밖에 남아 있다 — 이건 guidance-check가 ` +
        `놓치는 바로 그 결함이다:\n${안잡힘.join("\n")}`,
    ).toEqual([]);
  });

  it("★★★ [B-1 신설] 결정적 안내는 도착 이름까지 약속한 자리로 간다", async () => {
    const 어긋남: string[] = [];
    const 표밖: string[] = [];
    for (const x of 안내) {
      if (!(x.질문 in 기대도착)) continue; // 예외표·데이터조건부는 이 시험 대상이 아니다
      const 닿음 = await 실제도착(x.질문);
      if (닿음.length === 0) { 표밖.push(`「${x.질문}」 → 실제로는 결정적 도착이 없다(⑨) — 기대도착에서 이 줄을 빼고 예외표로 옮겨라`); continue; }
      if (닿음[0].도착 !== 기대도착[x.질문]) {
        어긋남.push(`「${x.질문}」 → 실제 ${닿음[0].도착} ≠ 기대 ${기대도착[x.질문]}`);
      }
    }
    expect(표밖, "기대도착에 있는데 실제로는 결정적이지 않은 줄:\n" + 표밖.join("\n")).toEqual([]);
    expect(
      어긋남,
      `제품이 「이렇게 치세요」라고 적어 준 말이 **약속과 다른 도구**로 간다(2026-09-11 run_hardening_scan 사고 꼴):\n${어긋남.join("\n")}`,
    ).toEqual([]);
  });

  it("★★★ [B-1 신설] 모든 안내는 기대도착·예외표·데이터에달려도봐줌 중 하나에는 있다", () => {
    const 빠짐: string[] = [];
    for (const x of 안내) {
      const 기대있음 = x.질문 in 기대도착;
      const 예외있음 = 아직결정적아님.some((e) => e.말 === x.질문);
      const 조건부있음 = 데이터에달려도봐줌.includes(x.질문);
      if (!기대있음 && !예외있음 && !조건부있음) 빠짐.push(`「${x.원문}」(잴 때: ${x.질문})`);
    }
    expect(
      빠짐,
      `새 안내가 어느 표에도 없이 조용히 늘었다 — 기대도착이나 아직결정적아님에 넣어라:\n${빠짐.join("\n")}`,
    ).toEqual([]);
  });
});
