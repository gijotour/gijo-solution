// engine/productfaq.ts — 제품 지식 즉답 카드.
//
// [2026-08-08 · 150상황 야간 회차의 마지막 불편 3건에서] 셋 다 **답이 변하지 않는 지식**인데
// 14B가 매번 30초씩 새로 썼다(CEF 헤더 · 학습 내용 확인 · 지어냄 식별). 데이터 조회를 즉답화한
// 것과 같은 원리의 지식판이다 — 검증된 정답을 코드가 그대로 낸다(0.1초·오답 0).
//
// 카드 기준(늘리기 전에 읽을 것):
//   ① 답이 데이터·날짜에 따라 변하지 않는다  ② 근거가 표준 문서나 우리 제품 사실이다
//   ③ 실측에서 느리거나 틀린 적이 있다 — 셋 다 만족할 때만 담는다. 백과사전을 만드는 게 아니다.
// ⚠ 판별은 좁게 — 개념의 딴 물음("CEF 로그 보여줘"=조회, "학습 시작해줘"=쓰기)은 안 삼킨다.

export interface FaqCard {
  id: string;
  re: RegExp;
  answer: string;
}

export const FAQ_CARDS: FaqCard[] = [
  {
    id: "cef-header",
    // 「CEF 로그 헤더 구조 알려줘」 — 야간 회차 32.7초. 헤더/구조/형식 앵커 필수:
    // 「CEF 로그 보여줘」(데이터 조회)는 다른 말이다.
    re: /CEF[^.\n]{0,10}(헤더|구조|형식|포맷)[^.\n]{0,8}(알려|설명|뭐|구성)|CEF[^.\n]{0,6}(로그|이벤트)?[^.\n]{0,6}(헤더|구조|형식).{0,6}(알려|설명|뭐)/i,
    answer: [
      "CEF(Common Event Format) 로그는 **머리(헤더) 7칸 + 확장(Extension)** 으로 구성되고, 머리는 파이프(|)로 구분합니다.",
      "",
      "`CEF:버전|장비 제조사|장비 제품|장비 버전|이벤트 분류 ID|이벤트 이름|심각도|확장`",
      "",
      "1. **버전** — CEF 규격 버전(보통 0)",
      "2. **장비 제조사 / 3. 제품 / 4. 버전** — 어느 장비가 낸 로그인지",
      "5. **이벤트 분류 ID** — 장비가 정한 이벤트 코드",
      "6. **이벤트 이름** — 사람이 읽는 제목",
      "7. **심각도** — 0~10(높을수록 심각)",
      "",
      "확장부는 `키=값`을 공백으로 이어 씁니다(예: `src=10.0.0.5 dst=10.0.0.9 spt=443`).",
      "예시: `CEF:0|Vendor|IPS|1.0|100|SQL Injection Attempt|8|src=10.0.0.5 dst=10.0.0.9`",
      "",
      "▸ 이어서 — 우리 제품은 통합 관제의 보안로그 반입에서 CEF를 받아 자산과 자동으로 묶습니다. 실제 로그를 보시려면 \"보안로그 최근 것 보여줘\".",
    ].join("\n"),
  },
  {
    id: "what-ai-learned",
    // 「AI가 뭘 학습했는지 볼 수 있어?」 — 야간 회차 30초대. 학습+보다/확인 앵커.
    // 「학습 시작해줘」(쓰기)·「학습 루프가 뭐야」(개념)는 안 삼킨다.
    re: /AI가?[^.\n]{0,6}(뭘|무엇을|어떤 걸)?[^.\n]{0,4}학습(했|하는지|한 내용|한 거)[^.\n]{0,8}(볼 수|보여|확인|알 수|알려)/,
    answer: [
      "볼 수 있습니다 — 이 제품의 학습은 전부 기록으로 남고, 두 갈래로 확인합니다.",
      "",
      "**① 지식(문서) — 무엇을 알고 답하나**",
      "· AI 운영 › **AI 지식** 화면 — 반입된 문서·업무영역·조각 수",
      "· 대화창에 \"새 문서 뭐 들어왔어?\" — 최근 반입과 세 줄 요약",
      "",
      "**② 학습(모델) — 무엇으로 훈련되나**",
      "· AI 운영 › **학습 루프** 화면 — 수집된 대화, 담당자가 👍로 승인한 것, 학습 이력",
      "· 학습 재료는 **담당자가 승인한 문답만** 쓰이고, 승인 전까지는 후보일 뿐입니다",
      "",
      "밖으로 나가는 것은 없습니다 — 수집·승인·학습이 전부 이 서버 안에서 끝납니다.",
    ].join("\n"),
  },
  {
    id: "how-to-spot-hallucination",
    // 「AI가 답을 지어내면 어떻게 알아?」 — 야간 회차 30.8초.
    re: /(AI|모델)가?[^.\n]{0,6}(답을?|정보를?)?[^.\n]{0,4}(지어내|꾸며내|할루시|환각|없는 말)[^.\n]{0,10}(알|어떻게|식별|구분|잡)/,
    answer: [
      "제품이 세 겹으로 표시해 드립니다 — 담당자는 표시만 보면 됩니다.",
      "",
      "1. **근거 배지** — 사내 자료로 답하면 어느 문서를 봤는지와 **원문 대목**이 답 옆에 붙습니다. 숫자가 의심되면 원문과 눈으로 대조하세요(모델이 표를 잘못 읽은 사고를 이걸로 잡았습니다).",
      "2. **근거 약함 표시** — 딱 맞는 자료가 없어 유추한 답은 앞머리에 ⚠근거 약함을 스스로 답니다. 그대로 쓰기 전에 확인하라는 뜻입니다.",
      "3. **모르면 모른다** — 근거가 아예 없으면 일반 지식으로 채우지 않고 \"사내 자료에 없습니다\"라고 답하게 돼 있습니다(관련성 게이트).",
      "",
      "그래도 틀린 답을 보시면 그 답에 **지적**을 남겨 주세요 — 지적은 매주 모아 고치고, 같은 오답이 반복되지 않게 학습에서 제외합니다.",
    ].join("\n"),
  },
];

/** 지시가 지식 카드에 맞으면 그 답을, 아니면 null. */
export function faqAnswerFor(instruction: string): { id: string; answer: string } | null {
  const t = String(instruction ?? "").trim();
  if (!t) return null;
  for (const c of FAQ_CARDS) if (c.re.test(t)) return { id: c.id, answer: c.answer };
  return null;
}
