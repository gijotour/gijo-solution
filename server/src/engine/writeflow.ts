/**
 * writeflow — 「조회 + 쓰기 지시」가 붙은 말인가를 재는 **잎 모듈**(2026-09-12 B10 수리).
 *
 * ■ 왜 파일을 갈랐나: 이 잣대를 **agentloop와 picklist가 함께** 불러야 한다.
 *   picklist.isFindingListAsk(dispatcher 특수경로 [14])는 agentloop보다 **먼저** 도는데,
 *   거기서 자기 낱말 목록(LIST_BLOCK_RE)으로 쓰기를 가리고 있어 실제로 어긋났다 —
 *   「패치 안 한 취약점 알려주고 **승인해줘**」가 목록 한 수로 끝나 승인이 영영 안 갔다
 *   (2026-09-12 검토관 [상] 실측). picklist가 agentloop를 부르면 3,400줄 모듈을 통째로
 *   끌어오므로, 잣대만 **아무것도 import하지 않는 잎 모듈**로 내린다(설계관 점검표 ⓔ
 *   「정규식은 잎 모듈 단일 출처 export · 시험은 그 함수를 그대로 호출」).
 * ⚠ 이 파일은 **아무것도 import하지 않는다** — 순환이 원리상 생기지 않게 하려는 것이다.
 */
// export: 시험이 **실제 라우팅 함수**를 그대로 불러 대조한다(정규식을 베껴 쓰면 드리프트한다).
/** 「…있으면 담당자한테 배정해줘」·「…알려주고 승인해줘」 같은 **쓰기 흐름**인가 —
 *  이 문장을 조회 도구(explain 등) 한 수로 못 박으면 배정·승인 단계에 영영 못 간다.
 *  **쓰기 흐름 판정의 유일한 권위**다 — 이 뜻의 정규식을 다른 곳에 또 적지 않는다.
 *
 *  ⚠ 왜 함수로 뽑았나(2026-09-12 B10-①): 원래 forcedToolFor 안의 지역 const 셋
 *    (isAssignQuery·isAssign·isWriteOrder)이었다. 배열 밖 손수 explain 분기 다섯 중
 *    제품설정질문·문서지목질문·사내규정질문은 forcedToolFor **머리**에 있어, 지역 const보다
 *    앞에서 이 잣대를 참조하면 TDZ(`ReferenceError: Cannot access before initialization`)로
 *    모든 지시가 죽는다. 함수로 뽑으면 위치 문제가 없고, 시험이 정규식을 베끼지 않고 이 함수를
 *    그대로 불러 검증할 수 있다(ⓔ 단일 출처). `instruction`은 forcedToolFor 안에서 한 번도
 *    재대입되지 않으므로(실측: grep 0건) 지역 변수를 그대로 함수 인자로 옮겨도 의미는 같다.
 *
 *  ⚠ memory.행동의도RE와 **서로 포함 관계가 아니다**(실측, 2026-09-12) — 둘 다 남긴다:
 *    이 함수만 잡는 말: 「…알려주고 김보안한테 **맡겨줘**」·「…담당자 **정해줘**」·
 *      「…항목 알려주고 **조치해줘**」·「…확인해서 김보안한테 배정 **부탁해요**」.
 *    행동의도RE만 잡는 말: 「…실무매뉴얼 새 버전으로 **올려줘**」(등록·업로드 계열 —
 *      이 함수는 「올려」를 모른다). 권위는 이 함수(쓰기 명령 자체), 행동의도RE는
 *      「문서 자체를 조작하라」는 명령을 막는 다른 성질의 belt다(memory.ts 머리글 참고). */
export function 쓰기흐름인가(instruction: string): boolean {
  // "가장 급한 취약점 담당자·기한 배정해줘"처럼 배정/지정 지시면 우선순위 조회(today)로 못박지 않는다
  // — LLM이 assign_finding(쓰기)을 고르도록 둔다(실측: today 강제가 배정 명령까지 흡수했었음).
  // ⚠ 사이에 **사람 이름이 들어간다**. 「담당자를 **김도희로** 지정해줘」처럼.
  //   예전 규칙은 담당자 뒤 2글자까지만 봐서 이 말을 놓쳤고, 그러면 today가 흡수해
  //   **쓰기 지시에 오늘 목록이 나왔다**(2026-08-09 파일럿 리허설 실측 — 대본 24~30분이
  //   바로 이 문장이라 시연이 그 자리에서 깨진다).
  // ⚠ 「미배정 몇 건?」 같은 **조회**는 뺀다 — 그건 세는 질문이지 시키는 말이 아니다.
  // ⚠ 「배정 **현황**」도 조회다(2026-09-08 검토관 적발) — 홑낱말 「배정」만 보고 쓰기로 읽으면
  //   「간밤에 터진 거 담당자 배정 현황 알려줘」가 강제에서 풀려 모델 재량으로 샌다(실측 ∅).
  // ⚠ 「배정 승인 대기 몇 건?」도 조회다(2026-09-11 실측 — 새 approval_status가 조회로못박지않을것에
  //   들어가면서 드러났다). 「배정」 낱말이 있으면 isAssign이 켜져 approval_status가 통째로
  //   continue돼 null로 샜다 — 담당자가 「미배정 취약점이 몇 건 승인 대기 중이냐」를 물었는데
  //   결재판 대신 모델 판단으로 갔다. 「배정+승인/결재+대기」는 배정 **명령**이 아니라 배정
  //   **여부를 필터로 쓴** 결재 현황 물음이다.
  // ★★★ 2026-09-12 B10 수리(검토관 [중] 적발) — **「배정·기한 지정」이 주제어인 규정 물음**도
  //   조회다. 「담당자 배정 규정 알려줘」·「기한 지정 규정 알려줘」·「방화벽 정책 예외 배정
  //   어떻게 확인해?」는 시키는 말이 아니라 **그 규정을 묻는 말**인데, 홑낱말 「배정」이
  //   isAssign을 켜서 사내규정질문·문서지목질문·제품설정질문의 explain 방어가 통째로 꺼졌다
  //   (착수 전 실측 13문장 전부 ∅ — 그 자리가 바로 2026-08-07 「접속기록 보관 기간 규정
  //   알려줘」가 법제처로 새어 **오류문이 담당자에게 그대로 나간** 사고의 자리다).
  //   ⚠ 여기에 넣어도 **쓰기 명령은 안 풀린다** — isWriteOrder(아래)는 isAssignQuery와
  //     무관하게 따로 서므로 「정책 배정해줘」·「…규정 알려주고 맡겨줘」는 그대로 쓰기다.
  //     그것이 이 함수를 두 겹(조회로 읽는 쪽·쓰기로 읽는 쪽)으로 둔 이유다.
  //   ⚠ 뒤 차례(규정→배정)는 **묻는 말이 뒤따를 때만** 본다 — 「정책 배정해줘」를 조회로
  //     읽지 않기 위해서다. 사이 간격은 8자까지만 본다(「정책 **예외** 배정」까지가 한계 —
  //     더 넓히면 「개인정보 보호 정책 알려주고 김도희한테 배정해줘」가 조회로 뒤집힌다).
  const 배정주제어물음 =
    /(배정|할당|기한\s*(?:지정|설정))\s*(?:관련\s*)?(?:규정|기준|절차|지침|정책|방침|매뉴얼|가이드|안내서)|(?:규정|기준|절차|지침|정책|방침|매뉴얼|가이드|안내서)[^\n]{0,8}(?:배정|할당)\s*(?:은|는|이|가)?\s*(?:어떻게|어디|뭐|무엇|절차|방법|기준|규정)/;
  const isAssignQuery = 배정주제어물음.test(instruction) || /미배정|배정\s*안\s*[된함]|배정\s*없|담당자?\s*없|배정\s*(현황|목록|상태|내역)|배정\s*(승인|결재)\s*대기/.test(instruction);
  const isAssign =
    !isAssignQuery &&
    /배정|담당자[^\n]{0,12}(지정|정해|배치|맡|넣)|기한\s*(을|를)?\s*(지정|정해|설정|잡)|맡겨|배치해줘/.test(instruction);
  // ★★ **배정만이 쓰기가 아니다**(2026-09-08 검토관 적발 · 실측 8꼴). 「…있으면 승인해줘」를
  //   조회로 못 박으면 목록만 나가고 승인은 영영 안 된다 — isAssign과 **똑같은 사고**인데
  //   막는 낱말이 배정 하나뿐이었다. 잣대는 아래 조회로못박지않을것 한 곳에서 함께 쓴다.
  // ⚠ **문장 끝**에만 건다. 낱말만 보면 「오늘 조치해야 할 거 뭐야?」 같은 **조회**까지 삼킨다 —
  //   그게 이 파일이 2026-08-01에 겪은 사고다(제외어 목록이 업무 이름을 잡아먹었다).
  // ★★★ 2026-09-11 **배정도 여기 넣는다**(검토관 [중] 적발). 위 isAssignQuery가 「배정 승인
  //   대기」를 조회로 읽으면서 **문장 전체에서 isAssign이 꺼진다** — 그런데 배정은 원래
  //   isAssign만 막던 쓰기라, 「배정 승인 대기 목록 보여주고 김도희로 배정해줘」가
  //   isAssign=false·isWriteOrder=false로 가드를 통과해 approval_status로 못 박혔다
  //   (실측 2026-09-11: forcedToolFor → approval_status. 목록만 나가고 배정은 영영 안 된다 —
  //    2026-08-09 파일럿 리허설에서 today가 배정 명령을 흡수한 것과 **같은 사고**다).
  //   가드의 짝을 맞춘다: 조회로 읽는 쪽(isAssignQuery)을 넓혔으면 쓰기로 읽는 쪽도 넓힌다.
  // ★★★ 2026-09-12 **「할당」도 여기 넣는다**(검토관 [상] 적발). 「…차이 알려주고 김보안한테
  //   **할당해줘**」가 isAssign(배정·맡겨·담당자 지정…)에도, 여기에도 없어 **쓰기 명령이
  //   조회로 못 박혔다**. 새 규칙에 낱말을 또 적지 않고 **잣대 한 곳**에 더한다 — 그래야 새
  //   비교 분기와 FORCED 루프(today·briefing·exec_brief·explain…)가 같은 답을 낸다.
  //   실측(더하기 전, 전부 「할당」 때문에 새던 것들): 「가장 급한 취약점 김보안한테 할당해줘」
  //   →[37] today · 「기한 지난 점검 있으면 … 할당해줘」→maintenance_status · 「승인 대기 목록
  //   보고 … 할당해줘」→approval_status · 「임원 보고용으로 세 줄 요약해서 … 할당해줘」→
  //   exec_brief · 「우선순위 기준 알려주고 … 할당해줘」→explain. 2026-08-09 파일럿에서
  //   today가 배정 명령을 흡수한 그 사고가 **낱말만 바꿔** 그대로 남아 있었다.
  // ★★★ 2026-09-12 B10 수리 **「맡겨·담당자 정해/지정」도 여기 넣는다**(검토관 [상] 적발 ·
  //   2026-09-11 「배정」 이동의 **나머지 절반**). 위 isAssignQuery가 참이면 isAssign이
  //   **문장 전체에서** 꺼지는데, 배정과 똑같은 뜻인 「맡겨·담당자 정해」는 isAssign에만
  //   있어서 함께 꺼졌다. 실측(수리 전, 전부 explain 한 수로 끝나 배정이 영영 안 갔다):
  //   「**미배정** 취약점 처리 규정 알려주고 김보안한테 **맡겨줘**」·「이 매뉴얼에서 **미배정**
  //   건 알려주고 … 맡겨줘」·「**배정 현황** 규정 알려주고 **담당자 정해줘**」·「**담당자 없는**
  //   취약점 규정 알려주고 … 맡겨줘」 — 앞머리 낱말(미배정·배정 현황·담당자 없는)만 빼면
  //   같은 말이 이미 ∅였다(시험이 양성으로 못박은 P05). 업무에서 가장 자연스러운 말투라
  //   이 비대칭이 남으면 이 라운드가 닫았다고 적은 구멍이 그대로 열려 있다.
  //   ⚠ **문장 끝 명령꼴**로만 본다(이 상수의 계약) — 「누구한테 맡겨야 하나?」 같은 물음은
  //     원래 isAssign이 이미 쓰기로 읽던 말이라 넓어지는 것은 없다(같은 말, 자리만 옮김).
  //   ★ **「기한 …정해/지정해/잡아」도 같이 넣는다** — 위 배정주제어물음으로 「기한 지정 규정」을
  //     조회로 읽게 만든 순간, 그 낱말이 든 **쓰기 명령**(「기한 지정 규정 알려주고 기한 잡아줘」)이
  //     같은 비대칭으로 죽는다(실측: 넣기 전 explain 한 수로 끝났다). 조회로 읽는 쪽을 넓혔으면
  //     쓰기로 읽는 쪽도 함께 넓힌다 — 2026-09-11·2026-09-12에 같은 짝 맞추기를 두 번 놓쳤다.
  //   ⚠ 위 ★★★ 셋이 말하는 「여기」는 **아래 쓰기명령꼴인가()**다(2026-09-12 B12 수리로 지역
  //     const isWriteOrder를 그 이름으로 내보냈다 — 글자는 한 자도 안 바뀌었고 자리만 옮겼다).
  return isAssign || 쓰기명령꼴인가(instruction);
}

/**
 * **문장 끝 쓰기 명령꼴**인가 — 위 쓰기흐름인가의 뒷가지(isWriteOrder)를 그대로 내보낸 것.
 *
 * ■ 왜 따로 내보내나(2026-09-12 B12 수리 · 검토관 [상] 적발): 쓰기흐름인가의 앞가지(isAssign)는
 *   **문장 끝에 고정돼 있지 않다** — 「배정」이라는 낱말이 어디에 있든 참이다. 그 넓은 잣대를
 *   화면 안내(screenguide.isHelpIntent) **맨 앞**에 두었더니, 시키는 말이 아닌 **사용법 물음**
 *   까지 안내를 잃었다. 낱말 하나만 바꾼 대조쌍 실측(route-explain --no-build, 2026-09-12):
 *     「배정 도움말」⑨ ↔ 「승인 도움말」[22] · 「배정 사용법 알려줘」⑨ ↔ 「일괄 처리 사용법
 *     알려줘」[22] · 「이 화면에서 배정 어떻게 해?」⑨ ↔ 「이 화면에서 승인 어떻게 해?」[22] ·
 *     「기한 설정 화면 사용법 알려줘」⑨ ↔ 「알림 설정 화면 사용법 알려줘」[22] ·
 *     「담당자 지정 사용법 알려줘」⑨ ↔ 「사용법 알려줘」[22].
 *   담당자 배정·기한 설정은 이 제품의 핵심 업무라 「배정 어떻게 해?」는 실사용에서 흔하다 —
 *   그 물음이 ⑨(모델 재량)로 새면 B12가 닫으려던 구멍이 배정 계열에서 그대로 열린다.
 * ■ **삼킴을 막는 데는 이 뒷가지면 충분하다**(실측): 화면 안내가 삼켜서 문제가 됐던 말은 전부
 *   문장 끝 명령꼴이다 — 「…알려주고 승인해줘/배정해줘/할당해줘/맡겨줘/담당자 정해줘/조치해줘」.
 *   앞가지(isAssign)는 조회·물음까지 함께 잡으므로 **안내 층에서는 쓰지 않는다.**
 * ■ 낱말은 여기 한 곳에만 적는다 — 쓰기흐름인가도 이 함수를 부른다(단일 출처).
 */
export function 쓰기명령꼴인가(instruction: string): boolean {
  // ★ 2026-09-13 B-2 ⑥ — 「담당부서」도 「담당자」와 같은 뜻의 배정 대상이다.
  //   실측(route-explain --no-build): 「GIJO AS 서버 자산 담당부서 지정해줘」가 이 함수 없이는
  //   ⑨(모델 재량)로 새 결재판 없이 지정이 처리될 수 있었다 — 이 한 줄로 문장 끝 명령꼴을 잡아
  //   쓰기흐름인가()가 참이 되게 한다(guidance-check.mjs 쓰기말과 같은 커밋에서 짝을 맞춘다 —
  //   guidance-destination.test.ts ㉤이 두 원본의 글자를 대조한다).
  // ★★ 2026-09-13 B-2 검토관 수리(적발 ⑩ [상]) — 「요청·등록·작성·만들어」 계열이 빠져 있었다.
  //   위 머리글 ⓑ가 「알고 두는 구멍」이라 적어 둔 자리인데, 같은 날 FORCED 규칙 4건을 넓히면서
  //   **구멍이 넓힌 만큼 커졌다**. 실측(수리 전 · 전부 착수 전에는 ⑨ 또는 쓰기 도구):
  //     · 「자산 목록 보여주고 조치 요청서 만들어줘」 → list_assets
  //       (대조군 「자산 목록 **보고** 조치 요청서 만들어줘」 → create_request_doc — 「보여」 한
  //        글자 차이로 **결재판이 받쳐 주던 쓰기가 통째로 사라졌다**)
  //     · 「자산 목록 보여주고 승인 요청해줘」·「자산 목록 보여주고 리포트 만들어줘」 → list_assets
  //     · 「내부 자산에 영향 주는 위협 알려주고 승인 요청해줘」 → threats
  //     · 「장비 목록 알려주고 점검 등록해줘」 → list_assets
  //     · 「점검 일정 확인하고 점검 결과 등록해줘」 → maintenance_status
  //   → 낱말을 **이 한 곳에만** 더한다(단일 출처). 조회로못박지않을것 Set을 지나는 모든 규칙과
  //     배열 밖 explain 분기 열 곳이 같은 함수를 부르므로 한 줄로 전부 닫힌다.
  //   ⚠ run_redteam·run_hardening_scan(2026-09-13 SLA/라우팅 라운드 ③부터 Set에 있다)은
  //     이 함수가 아니라 **Set 설계**(agentloop.ts 조회로못박지않을것 정의부 주석)의 몫이다 —
  //     「레드팀 점검해주고 …배정해줘」·「FW-01 하드닝 점검 돌려주고 …정해줘」는 이제 이
  //     도구 한 수로 못 박히지 않고 ⑨로 넘어가 뒤의 쓰기까지 처리된다(하드닝은 **등록 대상을
  //     못 맞히는 문장까지** 함께 — 2026-09-13 검토관 수리로 Set 원소를 파생지(scan_hardening_
  //     target)에서 뿌리(run_hardening_scan)로 옮겼다). 단일 명령은 이
  //     함수가 거짓을 주므로 여전히 그대로 결재판이다 — 이 함수는 그 갈림만 결정한다.
  //   ⚠ 새 낱말은 **부탁 어미(줘·주세요·라·다오)를 필수로** 받는다 — 옛 낱말처럼 선택으로 두면
  //     「허깅페이스 토큰 어디서 등록해?」(사용법 물음)까지 쓰기 명령이 되어 화면 안내를 잃는다
  //     (이 함수 머리글이 「배정 어떻게 해?」로 이미 겪은 함정 — screenguide.test.ts가 잡았다).
  //   ⚠ 「만들어줘」는 **만들어 두고 남는 산출물**일 때만 쓰기다 — 홑말로 받으면 「오늘 일일 브리핑
  //     만들어줘」(읽기 요약, 도착지 briefing)까지 쓰기가 되어 그 안내가 ⑨로 샌다
  //     (guidance-destination.test.ts가 잡았다). 그래서 목적어를 못 박는다.
  return /(승인|반려|조치|처리|마감|종결|차단|삭제|제거|회수|배정|할당)\s*해\s*(줘|주세요|라|다오)?\s*[.!?？~]*\s*$|(요청|등록|작성|생성)\s*해\s*(줘|주세요|라|다오)\s*[.!?？~]*\s*$|(요청서|보고서|리포트|문서|초안|SBOM)\s*(를|을|으로|로)?\s*만들어\s*(줘|주세요|라|다오)\s*[.!?？~]*\s*$|(지워|없애)\s*(줘|주세요)?\s*[.!?？~]*\s*$|(맡겨|맡기)\s*(줘|주세요|라|다오)?\s*[.!?？~]*\s*$|(?:담당자|담당\s*부서|기한)[^\n]{0,12}(?:(?:지정|배치|설정)\s*해|정해|잡아)\s*(줘|주세요|라|다오)?\s*[.!?？~]*\s*$/.test(instruction);
}
