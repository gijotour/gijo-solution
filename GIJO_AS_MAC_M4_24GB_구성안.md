# GIJO AS — Mac mini M4 24GB 구성안 (인계용)

> **읽는 대상**: Mac mini M4에서 GIJO AS를 세우는 사람(과 그 Claude).
> **먼저 읽을 것**: 저장소 루트의 `README.md` → `CLAUDE.md`. 이 문서는 그 위에 **M4 24GB에서만
> 다른 것**을 얹습니다. 일반 절차(빌드·실행·테스트)는 README에 있습니다.
> **짝 문서**: `GIJO_AS_MAC_올인원_배포_가이드.md`(2026-07-24) — Phase 1(크로스플랫폼 코드)은
> 이미 완료입니다. 이 문서는 그 뒤 바뀐 것과 24GB 특유의 함정을 더한 판입니다.

대상 사양: **Mac mini M4 · CPU 10코어 · GPU 10코어 · 통합메모리 24GB · SSD 512GB · 10GbE**

---

## 0. 결론부터

| 질문 | 답 |
| --- | --- |
| 이 Mac에서 GIJO AS가 도는가 | **돕니다** — 서버 + 로컬 LLM + 클라이언트 올인원 |
| 몇 명이 쓸 수 있나 | **1명(개발·시연)**. 다중 사용자 상시 관제는 부적합 |
| 채팅 모델 몇 개 | **1개** (+ 임베딩 bge-m3 상주) — 2개는 안 됩니다 |
| 지금 운영 기준 모델(qwen3-14b) **한 개**를 올릴 수 있나 | **네. 메모리는 충분합니다**(§3) — 재야 할 것은 속도뿐 |
| 무엇을 못 하나 | 2모델 동시 · 다중 사용자 동시 질의 · 파인튜닝(학습) |

**한 줄 요약**: *개발·데모·1인 파일럿에는 충분하고, 운영 서버 대체로는 부족합니다.*

---

## 1. ⚠ 가장 먼저 — 티어를 손으로 **Lite**로 내리세요

**이 문서에서 제일 중요한 항목입니다.**

제품의 티어 자동판정(`server/src/engine/localengine.ts` `recommendTier`)은 GPU 메모리 **총량**만
봅니다. Mac에서는 통합메모리 전체(24GB)를 GPU 메모리로 보고하므로 —

```
recommendTier(24576) → 24576 < 28000 → "standard"
standard = 채팅 LLM 2개 · 32K 컨텍스트 · 오버헤드 5000MB
```

**Standard를 권해 버립니다. 이건 M4 24GB에서 틀립니다.** macOS가 5~7GB를 쓰므로 모델이 실제로
쓸 수 있는 것은 **약 17GB**입니다.

- 14B **한 개**는 넉넉히 들어갑니다 — 약 13GB(§3 계산). **문제 없습니다.**
- 14B **두 개**는 8.4GB×2 + 오버헤드로 **22GB를 넘겨 불가능**합니다.

Standard로 둔 채 두 번째 모델이 올라가려 하면 스왑이 돌아 **응답이 수십 초로 늘거나 프로세스가
죽습니다.** 그리고 이건 "모델이 커서 느리다"로 오진하기 딱 좋은 증상입니다 — 실제 원인은 티어입니다.

### 해야 할 일
설정 → 구동 티어에서 **Lite**를 고르세요(화면에서 고른 티어가 자동판정·환경변수보다 우선합니다).
화면을 못 여는 상태면 환경변수로:

```bash
export GIJO_MAX_LOADED_MODELS=1
```

### 이건 제품 결함입니다 — 고칠 거리
`recommendTier`는 Windows/Linux의 전용 VRAM을 전제로 만들어졌습니다. Mac 통합메모리에서는
**OS 몫을 빼고 판정**해야 맞습니다(예: `IS_MAC`이면 총량의 65~70%를 유효값으로). 지금은
그 분기가 없습니다. 고칠 때 `tools/model-benchmark.mjs`의 판정 기준도 같이 봐야 합니다
(같은 기준을 쓴다고 주석에 적혀 있습니다).

> 손대기 전에 **저장소 주인에게 확인**하세요. 티어는 Windows 운영 서버 판정에도 쓰입니다.

---

## 2. 세울 순서

### 2-1. llama.cpp Metal 빌드 (Mac에서만 가능)

```bash
git clone https://github.com/ggml-org/llama.cpp && cd llama.cpp
cmake -B build -DGGML_METAL=ON
cmake --build build --config Release -j
```

산출물 `build/bin/llama-server`를 서버 cwd 기준 `llama.cpp/build/bin/`에 두거나
`GIJO_LLAMA_SERVER_PATH`로 지정합니다. 경로 해석(`server/src/util/llamabin.ts`)은 darwin을
이미 처리하므로 **코드 수정은 필요 없습니다**. `-ngl -1`도 Metal에서 그대로 동작합니다.

### 2-2. 저장소 · 의존성

```bash
git clone git@github.com:gijotour/AS-Private.git
cd AS-Private/server && npm install && npm run build && npm test   # 2880개, 모델 없이 돕니다
cd ../client && npm install && npm start
```

`npm test`가 통과하면 코드 쪽은 정상입니다. **모델 없이도 전부 통과**하도록 만들어져 있으니,
여기서 깨지면 모델 문제가 아니라 환경 문제입니다.

### 2-3. 모델 넣기

`server/models/<모델id>/<모델id>.gguf` 형태입니다(폴더명과 파일명이 같아야 합니다).
임베딩 `bge-m3`는 **반드시** 필요합니다 — 없으면 지식 검색이 통째로 죽습니다.

### 2-4. 데이터 이식 (선택)

Windows 운영 데이터를 가져오려면 `server/scripts/migrate-data-to-mac.mjs`의 export/verify를
쓰세요. `data/` + `models/` 복사가 전부입니다(bge 벡터는 LanceDB 안에 있어 재인입 불필요).

> ⚠ **실시간 동기화 금지.** `data/`를 두 머신이 동시에 쓰면 DB가 갈라지고 깨집니다.
> **시점 복사(scp)만** 하세요. 원본은 Windows WSL 운영본입니다.

---

## 3. 모델 선택 — 24GB에서 무엇을 올릴까

### 기본값: **qwen3-14b 한 개** (운영과 같은 모델)

**메모리는 넉넉히 들어갑니다.** Lite 티어(1모델·16K) 기준 계산:

| 항목 | 크기 |
| --- | --- |
| `qwen3-14b.gguf` | 8.4GB |
| 티어 오버헤드(KV 캐시 등 · 16K) | 3.5GB |
| `bge-m3` 임베딩(포트 8081 별도 프로세스) | 1.1GB |
| **GIJO 합계** | **약 13GB** |
| macOS | 5~7GB |
| **총** | **약 19GB / 24GB** |

macOS의 GPU 상주 메모리 상한(기본 총량의 약 75% ≈ 18GB)도 넘지 않습니다.

**운영 기준선이 qwen3-14b이므로 같은 모델로 재야 Windows 측정치와 비교가 됩니다.**
다른 모델로 재면 "Mac이 느린 것"인지 "모델이 다른 것"인지 못 가릅니다. 14B로 시작하세요.

### 디스크에 있는 것들 (운영 실측 크기)

| 모델 | 크기 | 쓰임 |
| --- | --- | --- |
| `bge-m3` (임베딩) | 1.1GB | **필수 · 상시 상주** — 없으면 지식 검색이 통째로 죽음 |
| `qwen3-14b` | 8.4GB | ⭐ **기본** — 운영 기준선과 동일 |
| `qwen2.5-14b-instruct` | 8.4GB | 대안 |
| `gijo-main-orchestrator` | 5.1GB | 우리 합성 모델 |
| `exaone-3.5-7.8b` | 4.5GB | 한국어 강함 · ⚠ **비상업 라이선스** |
| `r1-distill-1.5b` | 1.1GB | 가벼운 시험용 |

### 속도 — 여기가 유일한 변수 (추정치 · 반드시 실측하세요)

M4 기본형 메모리 대역폭은 약 120GB/s입니다(M1 Max는 400GB/s). 생성 속도는 대역폭에
거의 비례하므로:

- 14B Q4 → **8~11 tok/s** 정도
- 7~8B Q4 → **15~22 tok/s** 정도

제품 기준은 **답변 30초**입니다(넘으면 리포트로 전환). 짧은 조회는 14B로도 여유롭고,
긴 브리핑·리포트에서 이 선에 닿을 수 있습니다.

**판단 순서:**
1. 14B로 세우고 `tools/ops-sim.mjs`(150상황)를 돌려 **30초 초과가 몇 건인지** 셉니다.
2. Windows 최근 회차는 **150/150 · 최장 23.5초**였습니다. 이것이 비교 기준입니다.
3. 초과가 많으면 그때 8B급으로 내리고, **무엇이 몇 초에서 몇 초로 바뀌었는지** 남기세요.

> 속도가 모자라도 **먼저 티어부터 확인하세요**(§1). 느린 원인의 1순위는 모델 크기가 아니라
> 티어가 Standard로 잡혀 모델 2개를 올리려다 스왑이 도는 경우입니다.

> ⚠ **EXAONE은 비상업 라이선스입니다.** 사내 개발·시연에는 좋지만 **제품에 얹어 파는 경로에는
> 못 씁니다.** 고객 데모에 쓸 거라면 먼저 확인하세요.

### 컨텍스트

Lite 티어는 16K입니다. 24GB에서는 **컨텍스트를 늘리는 것이 곧 메모리**입니다 — 32K로 올리려면
더 작은 모델로 내려가야 합니다. 둘 다 크게 잡지 마세요.

---

## 4. ⚠ 함정 (전부 실제로 당한 것)

1. **임베딩 llama-server는 `ctx`·`batch`·`ubatch`를 8192로 명시해야 합니다.**
   안 하면 512자 넘는 한글 입력에서 HTTP 500이 납니다. 플랫폼 무관하게 재현됩니다.
2. **한글 검증에 `curl`을 쓰지 마세요** — 깨집니다. Node `fetch`로 하세요.
   한글 파일 조작도 `perl` 대신 Node. Python은 `PYTHONUTF8=1`.
3. **모델을 안 배정하면 조용히 기본 모델로 떨어집니다.** 에이전트별 모델 배정은 `app_state`를
   직접 확인하세요 — 화면만 보고 배정됐다고 믿으면 안 됩니다.
4. **7B급에 프롬프트 규칙을 더해 행동을 고치려 하지 마세요.** 반복 실패했습니다. 코드로 푸세요.
5. **테스트가 env를 덮어써서 제품이 아니라 테스트를 검증하는** 함정을 조심하세요.
   폴백 문구("죄송합니다" 등)가 나오면 통과가 아니라 **실패**로 칩니다.
6. **`client/server-dist/package.json`은 추적 산출물**입니다 — `npm run dist` 뒤 되돌리세요.
7. **Mac 앱은 서명이 없습니다**(자가 사용). 첫 실행은 **우클릭 → 열기**.
   ⚠ `npm run dist`는 **반드시 Mac에서** — Windows에서 mac dmg 크로스빌드는 안 됩니다.
8. **`build-server-dist.mjs`의 onnxruntime 네이티브 prune은 빌드 플랫폼 기준**입니다.
   이 분기가 없으면 mac 설치본에서 darwin 바이너리가 통째로 지워져 임베딩이 죽습니다(Phase 1에서 처리됨).

---

## 5. 세운 뒤 반드시 잴 것

순서대로 하고, **숫자를 남기세요**. "된다"가 아니라 "몇 초"가 판단 근거입니다.

| # | 무엇 | 통과 기준 |
| --- | --- | --- |
| 1 | preflight | GPU 항목이 **Apple Metal · pass** (경고 아님) |
| 2 | 티어 API | `Apple Metal · 통합메모리 24.0GB` 로 보고 · **Lite로 고정했는지 확인** |
| 3 | 임베딩 로드 | 8081 상주 · 한글 1000자 입력이 500 안 나는지 |
| 4 | 채팅 로드 | **qwen3-14b 1개만** 올라갔는지(2개면 티어 확인) · 첫 응답 몇 초 |
| 5 | tok/s | 실측치를 적으세요 — 14B 추정 8~11 tok/s와 대조 |
| 6 | RAG 히트 | 지식 문서 질의가 근거와 함께 오는지 |
| 7 | 서버 테스트 | `npm test` 2880개 통과 |

여유가 되면 `tools/ops-sim.mjs`(150상황)와 `tools/evalgate/run.mjs`(평가 게이트)까지 돌려
Windows 운영 기준선과 비교해 보세요. **다만 두 도구는 계정 하나를 잡으므로 동시에 돌리지 마세요.**

```bash
GIJO_ADMIN_USER=<계정> GIJO_ADMIN_PASSWORD=<비번> node tools/ops-sim.mjs
```

> 비밀번호를 코드에 적지 마세요. `no-hardcoded-credentials` 시험이 막습니다(그게 목적입니다).

---

## 6. 10GbE를 어떻게 볼 것인가

10기가 이더넷은 **여러 담당자가 붙는 서버**를 염두에 둔 사양입니다. 그런데 이 구성의 병목은
네트워크가 아니라 **메모리(24GB)** 입니다.

- 클라이언트가 서버에 붙는 트래픽은 REST/WebSocket이라 **1GbE로도 남습니다.**
- 여러 명이 동시에 물으면 채팅 모델 **1개를 줄 서서** 씁니다 — 두 번째 사람은 기다립니다.
- 10GbE가 실제로 값을 하는 곳은 **모델·데이터 이식**(수십 GB를 옮길 때)입니다.

**결론**: 10GbE 때문에 다중 사용자 서버로 쓰겠다는 판단은 하지 마세요. 그 용도라면
메모리가 큰 쪽(M4 Pro 48GB↑)이 답입니다.

---

## 7. 저장 공간 (512GB) 계산

| 항목 | 크기 |
| --- | --- |
| 모델 전부 옮기면 | 약 **38GB** |
| 권장(bge-m3 + qwen3-14b + 예비 8B 하나) | 약 **14GB** |
| `data/`(DB + LanceDB) | 운영본 기준 수 GB — 이식할 때 실측하세요 |
| Xcode + llama.cpp 빌드 | 약 20~40GB |
| macOS + 여유 | 100GB 이상 남기세요 |

512GB면 넉넉합니다. **모델을 전부 옮길 필요는 없습니다** — 쓸 것만 가져오세요.

---

## 8. 이 Mac에 맡기지 말 것

- **파인튜닝·LoRA 학습** — 24GB로는 무리입니다. Windows 쪽에서 하세요.
- **운영 서버 대체** — 운영은 Windows WSL(`gijo-as.service`, 포트 4000)입니다.
- **`data/` 양방향 동시 사용** — DB가 갈라집니다.
- **클라이언트 게시(`publish-release`)** — Windows 전용 절차입니다(계정·검증 방식이 묶여 있음).

---

## 9. 막히면 볼 곳

| 증상 | 먼저 볼 것 |
| --- | --- |
| 답이 느리거나 멈춤 | 티어가 Standard로 잡혔는지(§1) · 모델 2개가 올라갔는지 |
| 한글 입력에서 500 | 임베딩 ctx/batch/ubatch 8192(§4-1) |
| 지식 검색 0건 | bge-m3가 떠 있는지 · LanceDB 경로 |
| 앱이 안 열림 | 서명 없음 → 우클릭 → 열기(§4-7) |
| 화면에 새 메뉴가 없음 | 서버만 배포하면 화면은 안 바뀝니다 — 클라 빌드 필요 |
| 무엇이 어디로 가는지 모르겠음 | `node tools/route-explain.mjs "물어볼 말"` |
| 어떤 시험이 무엇을 지키는지 | `GIJO_AS_시험지도.md` |

---

## 10. 작업할 때 지킬 것 (저장소 규칙 요약)

`CLAUDE.md`에 전문이 있습니다. 특히 놓치기 쉬운 것:

- **모든 사용자 대상 텍스트는 한글로.** 어려운 말에는 쉬운 풀이를 곁들입니다.
- **UI 변경은 시안 먼저** — `mockups/<화면명>/`에 자체완결 HTML을 만들어 승인받고 구현합니다.
- **지시는 대화창에서** — 새 화면에 자유 입력칸을 두지 않습니다. 화면은 고르는 자리입니다.
- **정직한 구현** — 가짜 UI 금지. 폴백 문구가 나오면 실패로 칩니다.
- **게시·운영 배포는 허락받고** — 로컬 커밋은 자유입니다.

---

*작성 2026-08-09 · 근거: `localengine.ts` 티어 코드 실측, 운영 서버 모델 실크기,
`GIJO_AS_MAC_올인원_배포_가이드.md`(Phase 1 완료분). 속도 수치는 추정이며 실측으로 갱신하세요.*
