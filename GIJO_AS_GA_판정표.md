# GIJO AS — GA 판정표

> 제정 2026-08-03. 계획서 **후-1「GA 판정·잔여 P0 해소」**의 기준표.
>
> ⚠ 계획서는 "기존 productization 판정표 기준"이라고 적고 있었지만 **그 문서가 저장소에 없었다.**
> 판정이 예전 세션의 기억으로만 남아 있어, 팔러 나가서 "GA입니까?"를 받으면
> **"기억으로는 닫힌 것 같습니다"** 말고는 답할 수 없는 상태였다. 그래서 기준을 다시 세운다.
>
> **이 표의 규칙: 파일이 있는지가 아니라 「실제로 되는지」를 잰다.**
> 근거 칸에는 **측정한 값**을 적는다. "구현됨"은 근거가 아니다.

---

## 0. 판정 (2026-08-03 실측)

> ### **파일럿 가능 · GA는 조건부**
>
> 코드로 닫을 수 있는 P0는 **전부 닫혔다**(아래 A~G 7항목 실측 통과).
> 남은 것은 **코드로 못 닫는 것들**뿐이다 — 외부 점검·법무·인증·실사용.

| 구분 | 상태 |
|---|---|
| 코드로 닫는 P0 (A~G) | **7/7 통과** |
| 코드 밖 관문 (H~K) | **미착수 — 사용자·외부 몫** |

⚠ **"GA"라고 말하려면 H~K 중 최소 H(외부 취약점 점검)와 J(실사용 4주)가 필요하다.**
지금 정직한 표현은 **"파일럿 투입 가능, GA 미판정"**이다.

---

## 1. 코드로 닫는 P0 — 실측 결과

측정 방법: 운영 서버(WSL, localhost:4000)에 실제 요청을 보내 응답을 읽었다.
**파일 존재 확인이 아니다.**

| | 항목 | 기준 (이걸 못 하면 GA 아님) | 실측값 (**2026-08-06 04:54 재실측**) | 판정 |
|---|---|---|---|---|
| A | **자가 진단** | 담당자가 "지금 이상 있나"를 스스로 판단할 수 있다. 점검 5종 이상 | 점검 **7종** · 이상 0 | ✓ |
| B | **백업·복원** | 스냅샷이 있고 **실제로 복구된다**(있다≠복구된다) | 보관 **7개** · 최신 방금(32.2MB, 지식베이스 동반) · **복원 점검 통과**(무결성 ok) | ✓ |
| C | **저장 암호화** | DB가 잠겨 있고 평문 사본이 안 남는다 | 저장 암호화 **켬** · 평문 사본 **0개** · 기계 결속(keyFile 존재) | ✓ |
| D | **감사 기록** | 누가 언제 무엇을 지웠는지 남는다 · 무한히 안 쌓인다 | 기록 유입 확인(방금 항목이 맨 위) · 보관 1095일 자동 정리 | ✓ |
| E | **이벤트 생애주기** | 이벤트가 영구히 안 쌓인다 | 이벤트 **4,845건** 조회 · 해결 후 90일 보관 | ✓ |
| F | **알림 스케줄** | 정기 알림이 돌 수 있다 | 스케줄러 가동 · 종류 3종 · 등록 0건(고객이 정할 항목) | ✓ |
| G | **SIEM 전달** | 고객 SIEM으로 내보낼 수 있다 | 설정 API 동작 · 지금은 꺼짐(**고객 장비에 맞춰 켜는 항목**) | ✓ |

> 2026-08-06 재실측 메모: B에서 **이 표의 자기 경고가 실제로 걸렸다** — 최신 백업(전날 22:29)이
> 그날 새벽의 SBOM 라이선스 529건 갱신보다 앞서 있었다. 규칙대로 그 자리에서 한 번 더 떴다
> (백업은 어디까지나 마지막 스냅샷까지만 지켜 준다 — 큰 변경 뒤 수동 1회는 습관이어야 한다).

> ⚠ **B에서 오늘 배운 것 — 자동 백업만 믿으면 큰 변경 하루치가 빈다.**
> 자동 백업은 24시간마다 돈다. 오늘 취약점 4,833건을 되살린 뒤 가장 최신 백업을 보니
> **복구보다 앞선 것**이었다 — 그 사이 서버가 죽었다면 되살린 것을 또 잃을 뻔했다.
> 큰 데이터 변경 뒤에는 **그 자리에서 한 번 더 뜬다**(오늘 그렇게 했다).

### 함께 재고 있는 것 (상시 회귀)

| 항목 | 값 (**2026-08-06**) | 앞선 측정(08-04) |
|---|---|---|
| 실전 147상황 | (이번 회차 미측정) | 146/147 |
| 대화창 말투 규범 위반 | (이번 회차 미측정) | 0/147 |
| 평가 게이트 3축 | 라우팅 **66/66** · 안전 9/9 · 한국어 24/24(08-05 4차, 배너 채점 제외 후) | 전 축 100% |
| 제품 경로 실효 견고성 | **뚫림 0/14**(08-05) | 100점 |
| server 자동 시험 | **2,515** (파일 228) | 2,488 |
| QA 전수조사 | **전 계층 통과**(08-06, viz·promise 계층 신설 포함) | 전 계층 통과 |
| 라우팅 규칙 · 겹침 | **58개** · **겹침 0** | 52개 · 겹침 0 |
| 게시 클라 버전 | **5.3.0**(BYOM 모델 자동 적응 카드) | 5.2.0 |
| 에어갭 봉인(후-4) | **제품이 직접 여는 통로 9종 차단 가능**(fetch+소켓, 기본 꺼짐) · ⚠ 자식 프로세스(python)는 관문 밖 — 오프라인 강제만 | — |

> ★ **147상황 146/147은 같은 숫자지만 뜻이 달라졌다.**
> 오전 측정은 **진짜 취약점 17건**짜리 제품에서 나온 값이었다. 오늘 잃었던 4,833건을
> 되살린 뒤 다시 재니 6건이 새로 걸렸고(빈 데이터가 가리고 있던 결함), 그것을 고쳐
> 같은 146/147로 돌아왔다. **재는 대상이 300배가 된 상태의 146이다.**
>
> GA 판정에서 이 차이가 중요하다 — 빈 제품에서 잘 도는 것은 증거가 아니다.

⚠ **맨몸 모델 견고성(36점)과 섞어 읽지 말 것.** 그건 모델만 놓고 잰 참고치이고,
우리가 파는 것은 제품 경로다(입구 차단 13 + 모델 버팀 1 = 뚫림 0).

---

## 2. 코드로 못 닫는 관문 — 남은 것

| | 항목 | 누가 | 왜 코드로 못 닫나 |
|---|---|---|---|
| H | **외부 취약점 점검(모의해침)** | 외부 업체 | 우리가 우리를 점검한 결과는 구매자가 안 믿는다. 자체 레드팀은 **AI 견고성**이지 제품 자체의 침투 점검이 아니다 |
| I | **법무 검토** | 법무 | 번들 LLM 라이선스 분류는 **출발점**이지 법적 확정이 아니다(modellicense.ts 머리말에 명시) |
| J | **실사용 4주** | 파일럿 고객 | "설치됨"과 "쓰임"은 다르다(계획서 중-1). 이탈 없는 4주가 목표 |
| K | **인증** (보안기능확인서·CC·GS) | 사업(예산) | 공공·금융 조달의 문턱. 어느 것을 딸지는 파일럿 고객 요구가 정한다(계획서 중-6) |

---

## 3. 이 표를 쓰는 법

- **분기마다 다시 잰다.** 값이 바뀌면 근거 칸을 갱신한다 — 날짜 없는 근거는 근거가 아니다.
- 새 P0가 생기면 **기준 칸을 먼저 쓴다**("이걸 못 하면 GA 아님"). 기준 없이 항목만 늘리면
  "구현됨"으로 채워지고, 그건 오늘 이 문서를 다시 만들게 된 이유다.
- 재는 방법은 **운영 서버에 실제 요청**이다. 시험 통과는 별도 축이고 이 표를 대신하지 않는다.
- **제품이 크게 달라진 날에는 분기를 기다리지 않는다.** 오늘이 그랬다 — 취약점 데이터가
  17건에서 4,833건이 되면서 A~G의 근거값 절반이 낡았다. 판정(파일럿 가능·GA 미판정)은
  그대로였지만, **낡은 숫자로 판정을 유지하는 것과 다시 재서 유지하는 것은 다르다.**

---

## 4. 재측정 이력

| 언제 | 판정 | 무엇이 달라졌나 |
|---|---|---|
| 2026-08-03 오전 | 파일럿 가능 · GA 미판정 | 표를 처음 복원하고 A~G 7항목 실측 |
| 2026-08-03 21:00 | **바뀌지 않음** | 잃었던 취약점 4,833건 복구 후 재측정. 감사 31,075→31,861 · 이벤트 31→4,845 · 시험 2,271→2,329 · 게이트 라우팅 65→66. **B에서 백업 공백을 발견해 그 자리에서 백업을 떴다** |
| 2026-08-04 | **바뀌지 않음** | 하루 개발분 반영 재측정. 시험 2,329→**2,426** · 라우팅 규칙 46→**52** · 게이트 **기준선 확정**(66문항) · 클라 **5.0.1→5.2.0 게시**(보안 지형도 1·2·3단계). **판정을 바꿀 새 증거는 없다** — 코드 P0(A~G)는 그대로 닫혀 있고, 남은 것은 여전히 **코드 밖 관문**(H~K: 모의해침·법무·실사용 4주·인증). 오늘 늘어난 것은 **기능·시연 자료**지 GA 관문이 아니다 |
| 2026-08-05 | **바뀌지 않음** | 후-3 지식 번들 구독화(대화창 조회·서명 반입) · **후-4 에어갭 봉인**(외부 통로 8종 default-deny, fetch+SMTP/SIEM 소켓) · **BYOM 모델 자동 적응**(올린 모델의 생각 모드·컨텍스트 자동 맞춤 + 스모크 4문항). 시험 2,426→**2,488** · 라우팅 52→**58** · 클라 **5.3.0 게시**. **판정 근거는 그대로다** — 새 기능이 늘었을 뿐 H~K는 하나도 안 닫혔다. ★ 다만 **에어갭은 조달 문턱을 낮추는 쪽**이다(N2SF 기밀 배치에서 "정말 안 나간다"를 증명할 수 있게 됨) — GA 판정을 바꾸지는 않지만 K(인증) 준비에는 재료가 된다.<br>★ **같은 날 검토관이 실결함 10건을 잡아 전부 고쳤다**(권한 우회·봉인 호스트명 구멍·잠긴 override·감시 사각지대·조회→쓰기 누수 등). 그중 **admin 전용 도구가 실행 문턱에서 안 막히던 것**은 담당자 계정으로 API 직접 호출이 가능했던 실제 보안 결함이다 — e2e로 차단을 실증했다. **이런 것이 남아 있었다는 사실이 "GA 아님"의 근거를 오히려 굳힌다**(A~G는 닫혔지만 새 기능마다 이런 틈이 난다) |

> ★ **오늘 하루의 성격을 정직하게 — 「보여 주기」이지 GA 관문이 아니다.**
> 2026-08-04는 계획서 **전-7(보여 주기)**에 집중한 날이다. 보안 지형도(자산 지도·공격경로·
> 4소스 히트맵)·첫 화면 단순화·시연 자료에 실제 화면 — 전부 **첫인상과 시연**을 위한 것이다.
> GA를 가르는 것은 이것들이 아니라 아래 H~K(코드 밖)이다. **기능이 늘었다고 GA에 가까워진
> 것이 아니다** — 그 착각을 경계하려고 이 줄을 적는다.

*최근 갱신: 2026-08-05 (후-3 구독화·후-4 에어갭·BYOM 자동 적응 반영)*
