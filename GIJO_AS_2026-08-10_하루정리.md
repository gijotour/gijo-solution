# 2026-08-10 하루 정리 — 무엇이 바뀌었고 무엇이 열려 있나

**커밋 58건** (Mac·Windows 합산) · 저장소 초록(287 파일 · 3,043 시험) · HEAD `c8337f8`
**내일 여기서 시작하면 된다.** 자세한 근거는 각 실측 문서에 있다(아래 링크).

---

## 1. 오늘 실제로 고친 것

| | 무엇 | 근거 |
|---|---|---|
| **RAG 오염** | `origin='builtin'` 신호 + 소급(`73ebb8b`) · **후보 풀 10→16**(`49139a1`) | 외부 1위 **20 → 13건** · 우리 문서가 후보에 없음 **17 → 10건** |
| **규정 흔들림 ②** | `보안규제_법적근거_모음.md` 보강(`49139a1`) | 「퇴사자 계정」 **10/10 일관** — 「지체 없이 · 5일 이내 · 제21조」 고정 |
| **임베딩이 지는 경주** | 기동 순서를 임베딩 먼저로(`d21ffba`) | 부팅 로그로 검증 — 임베딩 준비(3초) 뒤 채팅 적재 |
| **칸 문턱** | `확인용칸` 8192 → **12288**(`d21ffba`) | 실측 프롬프트 7,984 + 답변분 800 = 8,784인데 문턱이 그보다 낮았다 |
| **정규식 트랩** | `cti` → `action_check`(`caedfe3`) | 실재하지 않는 도구가 두 도구에 부분일치 |
| **말투 시험** | 사전에 `💡` 추가(`0ddf871`) | **저장소가 빨간 상태였다** — 오늘 커밋 다섯 번을 빨강에서 했다 |
| **잣대 3건** | 상충 검사 가드 2건(`f65925c`) · 날짜 「일」 제외(`b777fed`) · `doc-probe`·`drawer-audit` 0건 가드(`c8337f8`) | 아래 §4 |

## 2. 오늘 측정으로 **막은** 것 — 하지 않기로 한 결정

측정이 착수를 막은 것이 오늘만 **네 번**이다. 재지 않았으면 넷 다 했을 일이다.

| 안 하기로 한 것 | 왜 |
|---|---|
| **C-1 접점 재정렬** | 접점이 타사에 쏠려 있었다(Tenable 73 : GIJO 매뉴얼 0) — **역효과** |
| **`ORIGIN_BOOST` 상향** | 외부 1위 20건 중 **16건이 「올릴 우리 문서가 후보에 없는」 것**. 최대 수확 4건인데 GIJO 문서끼리 순위만 흔들린다 |
| **im-not-ai 윤문 LoRA** | 우리 답의 「AI스러움」 **0.153** (GPT-4o 0.934 · 사람 0.059). **문체는 병목이 아니다** |
| **② 원문 표 결정화** | 결론은 이미 10/10 일관이었다 — **잣대가 맥락 수치를 세던 문제**였다 |

## 3. 오늘 나온 실측 — 다음 판단의 재료

- **[[GIJO_AS_RAG_오염_실측_2026-08-10.md]]** — 오염 53% → 진짜 원인은 **후보 풀 10칸 vs 타사 PDF 695청크**
- **[[GIJO_AS_모델_메모리_실측_2026-08-10.md]]** — 티어표가 **7B 시절 산술**. 14B 실측 Metal/CUDA
- **[[GIJO_AS_문체_AI스러움_실측_2026-08-10.md]]** — KatFishNet 방식. **음성 대조군이 결론을 만들었다**
- **[[GIJO_AS_라이트_사양_2026-08-10.md]]** — 담당자 PC 단독·최소기능·7B. 도구 8개·8GB급
- **[[GIJO_AS_RAG_오염_해결_계획.md]]** — 세 갈래 ①ⓑ→③→②

## 4. ★ 오늘 가장 값진 발견 — 「잰 것이 없는데 초록」

하루에 **다섯 번** 봤다. 우연이 아니라 패턴이다.

| # | 어디서 | 무엇이 |
|---|---|---|
| 1 | 7B 게이트 1차 | 모델이 안 올라왔는데 40분 돌 뻔했다 (`dispatch`가 LLM을 안 타 적재가 안 됨) |
| 2 | 7B 게이트 2차 | **토큰이 15분에 만료**됐는데 `-o /dev/null`이 401을 삼켜, 두 번째 후보가 같은 초에 「안 올라왔다」로 끝났다 |
| 3 | 스크래치 측정 | `cwd`가 달라 **빈 DB를 새로 만들고** 측정이 통째로 무효 |
| 4 | **상충 잣대 자신** | env 이름을 틀려 전 회차 401 → 빈 답 → **「상충 0/3 — 전부 일관 (통과)」** |
| 5 | `doc-probe`·`drawer-audit` | 문항 0건 → 실패 0건 → **종료코드 0(통과)** |

> **아무것도 안 잰 것을 초록으로 내면 잣대가 아니라 위장이다.**
> 판정 도구는 「실패 0」이 아니라 **「몇 건이나 쟀나」**를 먼저 물어야 한다.

`evalgate/run.mjs`가 모범이다 — 「실효 견고성 미측정」·「기준선 파일 없음」·「흔들림 있으면 확정 불가」로 **명시적으로 거부**한다.

## 5. ⚠ 새로 드러난 것 — 14B의 대가는 지연이다

`ops-sim` 152문항 (14B · Mac Metal · 22분):

```
131/152 불편 없음
불편 21건 중 **20건이 「너무 오래 걸림」** · 내용 문제는 1건
중앙값 0.2초(도구 즉답) · 평균 8.5초 · 최대 67.7초
30초 초과 23건(15%) · 45초 초과 8건(5%)
```

**도구가 답하는 건 즉시고, LLM이 답하는 문항이 전부 30~68초다.** 오늘 잰 프롬프트
**7,984토큰**(도구 카탈로그 13,674자)이 그 값의 원인이고, 14B가 7.6B의 3배라 그대로 드러난다.

⚠ **아침의 144/146과 비교하면 안 된다** — 그건 **7.6B** 값이다. 같은 잣대의 값이 아니다.

### ✅ 결론 — 이건 **Metal 특성이지 제품 결함이 아니다** (같은 날 저녁 확정)

운영 RTX 3090에서 같은 152문항: **6분 · 150/152 · 최대 19.4초 · 30초 초과 0건** (Windows 실측).

원인을 Mac에서 확인했다 — **출력을 32토큰으로 고정하고 프롬프트만 늘렸더니 시간이 선형으로 늘었다:**

| 입력 토큰 | 걸린 시간 |
|---|---|
| 109 | 2.1초 |
| 901 | 5.8초 |
| 2,881 | 12.7초 |
| 5,741 | **19.1초** |

기울기 **≈ 3.0ms/토큰**. 7,984토큰이면 ≈ 25.7초로, 실측 30~68초와 맞는다(RAG·도구 왕복 추가).
**Metal은 프리필(프롬프트 통독)이 느리고 CUDA는 빠르다.** 토큰 생성이 아니라 통독이 지배한다.

> **∴ 표준(14B)의 도구 카탈로그 축소는 운영에 불필요하다.**
> 운영에 남는 것은 리포트 3건(~19초)뿐이고 이미 비동기 전환 대상이다.
> **∴ 라이트는 Metal에서도 도는 제품이므로 도구 8개(≈3,000토큰)가 특히 유효하다** — 설계가 맞았다.

⚠ **그러므로 Mac에서 잰 지연은 운영 지연이 아니다.** 오늘 오간 「78초」·「49.7초」·「③ 지연」이
전부 Mac 값이었다. 지연을 논할 때는 **어느 기계에서 잰 값인지 먼저 밝힌다.**

## 6. 지금 열려 있는 것

| | 누구 | 막고 있는 것 |
|---|---|---|
| **라이트가 같은 저장소인가** | **사장님** | 정해져야 `modeldefault` 시험 형태와 빌드 구조가 갈린다 — **라이트 착수의 잠금** |
| 라이트 CUDA 2값 | Windows | 7.6B@8192 · bge@2048 (갓 띄워 예열 2회) |
| 라이트 전용 문항셋 | Windows | 기존 66문항 중 **2건만** 해당 |
| 「즉시」를 수치 집합에 둘 것인가 | Windows | 안 정하면 상충 1건이 상시로 남는다 |
| 티어표 개편 · `overheadMb` 설계 | Windows | [[GIJO_AS_모델_메모리_실측_2026-08-10.md]] §3·§5 |
| ~~LLM 경로 지연~~ | — | **닫힘** — Metal 프리필 특성. 운영 CUDA는 30초 초과 0건(§5) |
| 매뉴얼 공백 1건 | 낮음 | 「이 문서 등급 바꾸려면?」 — 제품이 **「근거 약함」으로 정직하게 답한다**(배지가 제 일을 함) |

---

## 7. 내가 오늘 틀렸던 것 — 같은 함정을 다시 밟지 않게

측정으로 잡은 내 오진이 **여섯 번**이다. 전부 「한 번 보고 단정」이었다.

1. **「조치 이력의 Tenable = 문턱 문제」** → 0.8455로 강함 한복판이었다
2. **「78초·49.7초 지연」** → 재보니 3.4초·3.9초. 회차 노이즈였다
3. **「다음은 RRF 융합」** → 근거가 **1건**뿐이었다(128건 중)
4. **「문서 공백 16건」** → 4건 중 **3건은 매뉴얼에 있었다**. 검색이 못 찾던 것
5. **「후보 확대의 부작용 100%→93%」** → `topK=4`면 4건만 돌려주니 **필연적으로 100%**. 분모가 달랐다
6. **「커밋에 NUL이 없다」** → **셸 파이프가 NUL을 삼켰다**

그리고 손대서 **나빠진 것 한 번**: 상충 잣대에 「다만/별도」 필터를 넣었다가
접속기록의 정당한 2년이 잘려 상충 1/3 → 2/3이 됐다. 되돌리고 주석에 남겼다.

> **설정을 바꿔 가며 잴 때는 기준을 먼저 고정한다.**
> **한 번 관찰로 방향을 넘기지 않는다.**

*작성: Mac 세션 2026-08-10*
