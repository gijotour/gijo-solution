# 인수인계 — win 구현 이관 · max 검증 전환 (2026-08-13)

사장님 지시로 **역할을 재편한다: win이 라이트 구현을 이어받아 몰고, max는 검증(QA)으로 전환.**
이 문서 한 장에 제품 목적 → 현재 상태 → win 남은 작업 → max 역할 → 규약 → 함정을 모은다.
세부는 각 `인계_win_*` 문서와 auto-memory(MEMORY.md)를 가리킨다.

---

## 1. 제품 목적 — GIJO AS Lite가 무엇인가

- **담당자 1인·PC 1대용 AI 보안 관리 제품의 라이트 에디션.** 폐쇄망(air-gap)을 기본 상정.
- **기능 티어로 등급을 나눈다**(VRAM은 가이드일 뿐):
  - 라이트(7445) = 핵심 메뉴 + **가벼운 관리**
  - 스탠다드(7446)·프로(7447) = **메뉴 수·관리 깊이**(자동탐지·다중사용자·자격증명 금고·커스텀 대시보드·취약점 운영·담당자 배정·조치 검증)
- **챗 LLM은 고객이 등록**(번들 안 함) · 임베딩 bge-m3만 동봉 → dmg ~1.2GB. 챗 모델은 상시 후보 탐색(현재 판정 7.6B 유지).
- **AI에게 물어보기 = 고객의 모든 자료(제품자료·장비 매뉴얼·넣은 로그·나만의 기억)를 RAG로 훑어 답 + 라이트 13개 도구.** "나만의 기억 확인" 형태.
- 화면 10개: `0 챗 · 1 대시보드 · 2 작업내역 · 3 로그분석 · 4 스캐너 · 5 장비등록부 · 6 법령 · 7 제품자료 · 8 나만의기억 · 9 메모장` + 설정.
- 폐쇄망 원칙: **법령 원문 조회만 선택적 외부.** BridgeAI/원격 LLM은 **VPN 전용** 결정(사장님).

---

## 2. 현재 상태 (검증 완료)

- **dmg#11 (5.16.0, 1.2GB, 7445 정합, ad-hoc 서명)** — 실사용 3건 반영본. **로그인 화면 정상 렌더 확인(사장님).** 버전 드리프트 해소(클라·dmg 둘 다 5.16.0).
- 최근 커밋: `37d2a49`(3기능)·`b141d7a`(인계) — **hub 반영 완료**. win 최근분(온톨로지 배분·HF 업로드·소스 배지·결재판)도 rebase로 통합.
- 반영된 3건(외부 사례로 라이트답게 맞춤 — Splunk·Elastic·Wazuh / NetBox·UniFi / MS Copilot):
  - **로그분석**: 「한눈에 보기」 = KPI 타일 + 심각도 막대 + 요일×시간 히트맵(주말 강조) + 출처 Top5. 인라인 SVG/CSS(폐쇄망).
  - **장비등록부**: 「등록 장비」 목록(localStorage) + 직접접근[열기(웹)/SSH/복사] + TCP 도달 확인. 매뉴얼 RAG는 유지.
  - **챗**: 근거 보기(도구 result 인용 펼침)·답 복사·틀 문구. **win 결재판(renderApproval)과 공존.**
  - 다리 신설: preload `openExternal`·`probeHost`, main `shell:openExternal`(스킴 검증)·`net:probe`(TCP, 한 번에 하나). Windows nsis에도 그대로 컴파일됨.

---

## 3. win 남은 작업 (우선순위 · 상세는 `인계_win_라이트미결_한장_2026-08-13.md`)

| # | 항목 | 우선 | 상세 인계 |
|---|---|---|---|
| 1 | **정리본 대화이력 누적 초과** → 무상태 요약 길(remember:false) 렌더러에 노출 | ★높음 | `인계_win_모델교체후_긴프롬프트실패` §7 |
| 8 | **챗 「네 자료엔 없음」 정직 응답** — RAG 빈손이면 일반지식으로 답 말고 "이 PC 자료엔 없다"고 | ★높음 | 한장 인계 #8 (dispatcher/llm) |
| 2 | 법령 「유출 신고」에 시행령 미당김(본법만 당김) | 중 | `인계_win_법령조문_본문누락` §7 |
| 3 | 라이트 모드 토글 다리+UI (seam은 열림, preload·설정 토글 0) | 중 | `인계_win_에디션포트_라이트모드옵션` |
| 5 | 원격 LLM 런타임 (BridgeAI 1단계, VPN 결정됨) | 중 | `인계_win_원격LLM_런타임설정_BridgeAI` |
| 6 | Windows nsis 동봉 (CUDA llama rpath 함정 확인) | 중 | `인계_win_에디션포트` |
| 4 | 샘플 자산 3개 이름에 「샘플」 없음(assets.ts:506·518·527) | 낮 | `인계_win_라이트앱이_본서버를_띄운다` §3 |
| 9 | (선택) 장비 등록부 서버 저장 — 지금 localStorage(백업 안 됨). 스탠다드/프로 승격 후보 | 낮 | 한장 인계 #9 |

**사장님 결정 대기**: HF 「GIJO 모델 받기」 설정 한 줄 추가 여부(레포 `gijohn/` 업로드 완료 — max 반나절) · `max_claude-qa` 계정(미룸).

---

## 4. max 새 역할 = 검증 (QA)

win이 올린 것을 max가 검증해 통과/실패 + 근거로 회신한다.

- **검증 수단(신뢰 가능한 것만)**: 라이트 게이트 러너 · 브라우저창 하네스(목 데이터로 화면 렌더) · 서버 헬스(curl 7445) · `check-inline-scripts` · `tsc --noEmit` · 공용 UI 테스트(deadelements·uireadability) · E2E 스모크(9화면+설정).
- **⚠ 스크린샷(computer-use)·osascript 창수는 오탐**(SCContentFilter 고장·커스텀 타이틀바 0 오독). **최종 렌더 여부는 사장님 눈으로 굳힌다.** → [[feedback_gijo_앱렌더_검증_도구착시]]
- **게이트는 출하 모델(gijo-main-orchestrator 7.6B)로** 측정 — 모델명 라벨 확인(다른 모델이면 점수 무의미).
- 회귀 우선 점검: 포트 정합(7445 4곳)·env.ts 첫 import·챗 결재판·근거 렌더·base64 ingest.

---

## 5. 협업 규약 (불변)

- 새 비밀번호 **12자+**(제품 최소 8은 바닥). 값은 **사장님이 직접 화면에서** — 채팅·코드·스크립트에 안 적음.
- **origin(GitHub) push는 사장님 명시 요청 때만** · **hub push는 자유.**
- 원본 백업(`data.before-migrate-*`) 수정·삭제 금지. 계정 생성은 사장님이.
- 앱 반복 강제종료·캐시삭제·SingletonLock 제거는 userData만 어지럽히니 삼감.
- 기계 이름은 **win·max·gb10**만("윈도우/맥"은 고객 OS 전용).
- 영역: win = 서버 엔진 + main.ts 포트/에디션 / max = 검증(+ 필요 시 라이트 화면 보정).

---

## 6. 핵심 함정 (win이 밟지 말 것)

- `lite/env.ts`는 **첫 import**여야 한다(import hoisting — localengine이 env를 모듈 상수로 읽음).
- **포트 단일출처 = main.ts `서버포트()`**(라이트 7445). 4곳(index.ts·health poll·core.ts·login seed) 정합.
- **정리본 초과 = 대화 이력 누적**(`/api/llm/chat` remember:true 강제), 시스템 프롬프트 크기 아님.
- 법령: 조문 본문 API는 정상, **dispatch가 시행령을 안 당김**.
- 메모리 ingest는 **base64**(평문 넣으면 쓰레기 저장·성공으로 표시).
- Electron을 macOS XProtect가 오탐 → **ad-hoc 재서명**으로 해결.
- **검증 도구 착시**: 스크린샷/창수가 "빈 화면"으로 오탐 — **앱 의심 전 도구부터 의심.**
