# GIJO AS — 공동작업 가이드 (사장님 ↔ 제임스)

> **결정일 2026-08-09.** 그전까지의 협업 문서(`GIJO_AS_2머신_개발환경_가이드.md`)는
> **한 사람이 두 대**를 쓰는 전제였습니다. 이 문서는 **두 사람**이 함께 하는 규칙입니다.
> 서로 어긋나면 **이 문서가 우선**합니다.
>
> 읽는 순서: `README.md`(제품 세우기) → `CLAUDE.md`(작업 규칙) → **이 문서**(협업 규칙)

---

## 0. 한 장 요약

| | 사장님 (GIJO) | 제임스 (Remarkable Soft) |
| --- | --- | --- |
| 머신 | Windows PC + M1 Max 32GB | **Mac mini M4 24GB** |
| 코드 원격 | `hub`(내부) + `origin`(GitHub) | **`origin`(GitHub)만** |
| 서버·데이터 | 운영 WSL(4000) + 실데이터 | **자기 Mac에 독립 구축** |
| 운영 서버 접근 | 있음 | **없음(차단)** |
| main에 직접 push | ❌ 공동작업 중엔 금지 | ❌ 금지 |
| 코드 합치기 | **PR 검토·병합** | **PR 올리기** |
| 운영 배포 | ✅ **우리 Mac(M1 Max)에서 트리거** | ❌ |
| 클라 게시 | ✅ **Windows에서만**(기술 제약) | ❌ |
| 제품 결정(시안 승인·기능 범위) | ✅ 사장님 | ❌ 제안까지 |

---

## 1. 코드는 PR로만 합칩니다

`main` 직접 push는 **양쪽 다 금지**입니다.

> **왜 이렇게 정했나** — 2026-08-09 하루에만 서로 다른 작업의 커밋이 두 번 끼어들었고,
> 한 사람이 편집 중이던 `screenguide.ts`가 **다른 쪽 커밋에 실려 들어갔습니다.**
> 한 사람이 두 대를 쓸 땐 순서만 지키면 됐지만, 두 사람이면 이건 사고가 됩니다.

### 흐름

```
① 브랜치 만들기      git switch -c feat/무엇을-하는지
② 작업 · 로컬 커밋   (자유 — 커밋 메시지는 한글, 왜/무엇 중심)
③ 시험              cd server && npm test        ← 실패하면 PR 올리지 않기
④ 올리기            git push origin feat/무엇을-하는지
⑤ PR 열기           gh pr create   (본문 형식은 §2)
⑥ 검토·병합         상대가 보고 병합. 올린 사람이 자기 PR을 병합하지 않는다.
```

### 브랜치 이름

| 앞머리 | 언제 | 예 |
| --- | --- | --- |
| `feat/` | 새 기능 | `feat/law-lookup-screen` |
| `fix/` | 결함 수리 | `fix/lawinfo-referer` |
| `docs/` | 문서만 | `docs/mac-m4-setup` |
| `chore/` | 도구·설정 | `chore/qa-account` |

### 시작 전 반드시

```bash
git fetch origin && git switch -c feat/... origin/main
```

**낡은 main에서 갈라져 나오면 충돌이 커집니다.** `/GIJOAS동기화`가 원격을 알아서 고릅니다
(`hub`가 있으면 hub, 없으면 origin).

---

## 2. PR에 적을 것

제목은 한글, 무엇을/왜. 본문은 이 형식을 지켜 주세요.

```markdown
## 무엇을
(한두 줄)

## 왜
(어떤 문제·요청에서 나왔는지. 실측·재현 절차가 있으면 함께)

## 어떻게 검증했나
- [ ] `cd server && npm test` → N개 통과
- [ ] `npx tsc --noEmit` 통과
- [ ] 실제로 돌려본 것: (무엇을 어떻게 했고 무엇이 나왔는지 — 숫자로)

## 주의
- 의존성 변경: 있음/없음   ← 있으면 받는 쪽이 `npm ci` 해야 함
- 마이그레이션 추가: 있음/없음
- 공용 파일 건드림: (§4 목록 참조)
- 화면 변경: 시안 승인 받았는지
```

> **"검증했나"에 「테스트 통과」만 적지 마세요.** 이 저장소의 기준은 *실제로 돌려서 무엇이
> 나왔는가*입니다. 폴백 문구("죄송합니다" 등)가 나오면 통과가 아니라 **실패**로 칩니다.

---

## 3. 검증 → 배포 → 게시

**제임스가 올린 것은 우리가 우리 Mac(M1 Max)에서 확인한 뒤 운영에 넣습니다.**

```
제임스 PR
   │
   ├─ ① 우리 M1 Max Mac에서 받아 확인   ← 여기서 통과해야 병합
   │     · npm test · 실제로 돌려보기
   │
   ├─ ② 병합 (main)
   │
   ├─ ③ 운영 서버 배포 — 우리 Mac에서 트리거
   │     ssh user@10.8.0.1 "powershell -NoProfile -ExecutionPolicy Bypass -File 'd:/Connect AI/tools/deploy-prod.ps1'"
   │     ⚠ 재시작하면 접속 중인 전 사용자 세션이 끊깁니다. 사장님 승인 후에만.
   │
   └─ ④ 클라 게시 — **Windows에서만**
         electron-builder Windows 설치본 + claude-deploy 계정 + 실화면 검증이 묶여 있어
         Mac에서 대체 불가입니다. 화면·메뉴가 바뀌었으면 이 단계가 있어야 사용자에게 보입니다.
```

> **⚠ 서버만 배포하면 새 화면은 안 보입니다.** 화면은 앱 안에 들어 있습니다.
> 클라이언트를 건드린 PR은 **게시까지 해야 끝난 것**입니다.

---

## 4. 충돌이 잦은 공용 파일 — 손대기 전에 말하기

아래 파일은 양쪽이 자주 건드립니다. **PR을 열기 전에 상대에게 알리세요.**

| 파일 | 왜 위험한가 |
| --- | --- |
| `client/src/renderer/pages/nav.js` | 메뉴 정의. 항목을 더하면 QA 기대값·촬영 목록도 같이 봐야 함 |
| `server/src/engine/screenguide.ts` | 모든 화면 안내의 단일 출처. 실제로 커밋이 섞인 적 있음 |
| `server/src/engine/agentloop.ts` | `FORCED_INTENTS` — **배열 순서가 곧 우선순위**라 중간 삽입이 위험 |
| `server/src/engine/routes.ts` | 위 규칙표. 어긋나면 route-explain 설명이 거짓이 됨 |
| `CLAUDE.md` · `README.md` | 규칙 문서. 양쪽 해석이 갈리면 일하는 방식이 갈림 |
| `tools/gen-screenshots.mjs` | 화면 추가 시 함께 고쳐야 하는 목록 |

**규칙**: 이 파일들을 **여러 줄 고치는 PR은 하나씩** 올리고 바로 병합합니다. 오래 열어 두지 마세요.

---

## 5. 환경은 각자 — 운영은 공유하지 않습니다

**제임스는 운영 서버(Windows WSL 4000)와 운영 데이터에 접근하지 않습니다.** 자기 Mac에
서버·모델·DB를 독립으로 세웁니다. 구축 절차는 **`GIJO_AS_MAC_M4_24GB_구성안.md`** 에 있습니다.

이유:
- 로그인은 **계정당 1세션**입니다. 둘이 같은 계정으로 붙으면 서로를 끊습니다(실제로 발생).
- 운영 서버·CDP(9223)는 **직렬 자원**이라 병렬 측정이 서로를 깨뜨립니다.
- 운영 DB는 하나뿐입니다. 양쪽이 쓰면 갈라지고 깨집니다.

### 그래서 이런 제약이 생깁니다

| 제임스가 못 하는 것 | 대신 |
| --- | --- |
| 실데이터로 검증 | 데모 데이터·시드로 검증하고, **실데이터 검증은 PR에 요청**으로 적기 |
| `tools/ops-sim.mjs`·평가 게이트를 운영 기준으로 비교 | 자기 Mac에서 재고, **수치를 PR에 남기기**(우리가 대조) |
| 운영 배포·게시 | PR까지. 나머지는 우리가 |

### 각자 챙길 것

- `GIJO_ADMIN_USER` / `GIJO_ADMIN_PASSWORD`는 **각자 자기 환경변수**로 둡니다.
  ⚠ **코드에 비밀번호를 적지 마세요** — `no-hardcoded-credentials` 시험이 막습니다(그게 목적입니다).
- 모델(`models/`)·데이터(`data/`)는 git에 없습니다. 각자 준비합니다.
- ⚠ **`data/`를 두 머신이 동시에 쓰지 마세요.** 옮길 땐 시점 복사(scp)만.

---

## 6. 지켜야 할 제품 규칙 (양쪽 공통)

`CLAUDE.md`에 전문이 있습니다. 공동작업에서 특히 어긋나기 쉬운 것만 추립니다.

1. **모든 사용자 대상 텍스트는 한글로.** 어려운 말에는 쉬운 풀이를 곁들입니다.
2. **UI 변경은 시안 먼저** — `mockups/<화면명>/`에 자체완결 HTML을 만들고 **사장님 승인 후** 구현.
   승인 없이 만든 화면은 PR에서 돌려보냅니다.
3. **지시는 대화창에서** — 새 화면에 자유 입력칸을 두지 않습니다. 화면은 고르는 자리이고,
   고른 것은 `window.gijo.askConsole(질문)`으로 넘깁니다.
4. **정직한 구현** — 가짜 UI 금지. 실제로 끝까지 돌려 보고, 폴백 문구가 나오면 실패로 칩니다.
5. **제품이 안내한 말은 결정적으로 걸려야 합니다** — 화면·안내에 "이렇게 물어보세요"를 적었으면
   `guidance-routing` 시험이 그 말을 검사합니다. 걸리면 시험이 아니라 **기능**을 고치세요.
6. **감시 시험을 지우지 마세요.** 걸렸다는 것은 대개 약속이 깨졌다는 뜻입니다.
   (어떤 시험이 무엇을 지키는지는 `GIJO_AS_시험지도.md`)

---

## 7. 슬래시 명령 — 누가 무엇을 쓰나

| 명령 | 사장님 | 제임스 |
| --- | --- | --- |
| `/GIJOAS동기화` | ✅ (`hub`) | ✅ (`origin` — 자동 판별) |
| `/GIJOAS인계` | ✅ | ✅ (브랜치+PR로 안내됨) |
| `/GIJOAS서버시작` · `/GIJOAS클라시작` | ✅ | ✅ (자기 Mac) |
| `/QA전수조사` · `/평가게이트` | ✅ | △ 자기 환경에서만 |
| `/GIJOAS배포` | ✅ | ❌ |
| `/GIJOAS게시` | ✅ Windows | ❌ |

> `/GIJOAS동기화`와 `/GIJOAS인계`는 **원격을 자동으로 가립니다**(`hub` 있으면 hub, 없으면 origin).
> 2026-08-09 이전 판은 `hub`를 박아 써서 제임스 머신에서는 첫 명령부터 실패했습니다.

---

## 8. 하지 말 것

- ❌ **`main`에 직접 push** (양쪽 다)
- ❌ **자기 PR을 자기가 병합**
- ❌ **시험 실패한 채로 PR 올리기** — 왜 실패하는지 모르겠으면 그 사실을 적어 올리세요
- ❌ **감시 시험·헛통과 방지 확인을 지워서 통과시키기**
- ❌ **비밀번호·키를 코드나 PR 본문에 적기**
- ❌ **운영 DB·`data/` 양방향 사용**
- ❌ **승인 없이 화면 만들기**(시안 먼저)
- ❌ **커밋 이력 강제 덮어쓰기**(`push --force`) — 상대 작업본이 깨집니다

---

## 9. 아직 안 끝난 일

- ⚠ **git 이력에 남은 비밀번호가 살아 있습니다.** 코드에서는 걷어냈지만(커밋 `8bf694f`)
  과거 커밋에는 그대로 있고, 그 이력이 GitHub에 올라가 있습니다.
  **계정 `jyh`와 WSL sudo 비밀번호를 실제로 바꿔야 무효화됩니다.** — 사장님 조치 대기
- GitHub 브랜치 보호(main 직접 push 차단)는 **아직 설정 전**입니다. 지금은 규칙으로만 막고 있습니다.
  설정하면 실수로도 못 하게 됩니다.

---

*작성 2026-08-09 · 결정: PR 필수 · 제임스 운영 접근 차단(Mac 독립) ·
제임스 작업은 우리 M1 Max Mac에서 확인 후 우리 Mac에서 운영 배포 트리거 · 게시는 Windows 전용*
