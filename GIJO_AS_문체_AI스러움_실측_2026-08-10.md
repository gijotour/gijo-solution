# GIJO AS — 답변 문체 「AI스러움」 실측 (2026-08-10, Mac)

**질문**: 한국어 윤문 스킬(im-not-ai)을 학습에 써서 우리 답의 문체를 다듬을 값이 있나?
**방법**: KatFishNet(ACL 2025) 방식 — 쉼표 기반 5특징 + 로지스틱 회귀
**결론**: **없다(지금은).** 우리 답은 이미 사람 쪽이다. 문체는 병목이 아니다.

---

## 1. 무엇을 어떻게 쟀나

KatFishNet은 한국어 LLM 생성문 탐지기다. ML에 실제로 쓰이는 특징은 **쉼표 5개뿐**이다
(`experiment/main.py:435` — POS n-gram 다양성은 분석용이고 분류기엔 안 들어간다):

```
[쉼표포함문장비율, 평균쉼표사용률, 평균상대위치, 평균분절길이, 쉼표앞뒤POS다양성]
학습 = 사람 80% + GPT-4o   ·   시험 = 남은 사람 + 다른 LLM(OOD)
```

⚠ **원본과 다른 점 하나** — 형태소 분석기가 `Kkma`(konlpy·Java)가 아니라 **`Kiwi`**다.
이 Mac에 Java가 없다. 태그셋이 달라 논문 수치와 직접 비교하면 안 된다.
그래서 **같은 파이프라인으로 그들 데이터도 함께 채점해 눈금을 맞췄다**:

| | 이 구현 | 논문 |
|---|---|---|
| AUC 사람 vs qwen2-72b (OOD) | **0.9627** | essay 평균 0.9488 |
| AUC 사람 vs llama3.1-70b (OOD) | 0.9246 | — |

**이식이 충실하다.** 분류기는 제대로 돈다.

---

## 2. 실측

점수 = 「LLM이 썼을 확률」(0=사람쪽 · 1=LLM쪽)

| 구분 | 건수 | 중앙값 | 평균 | >0.5 |
|---|---|---|---|---|
| 그들 사람 에세이 (검증된 사람) | 37 | 0.059 | 0.159 | 10.8% |
| 그들 GPT-4o (검증된 LLM) | 181 | 0.934 | 0.871 | 96.1% |
| **GIJO 제품 답변** (`chat_logs`, 200자↑) | 415 | **0.153** | 0.315 | **28.2%** |
| **GIJO 문서** (사람이 씀) ← **음성 대조군** | 468 | **0.052** | 0.156 | **10.7%** |

### 음성 대조군이 핵심이다

「우리 답은 표·번호·조문 인용이 많은 정형 문서라, **저자가 아니라 장르 때문에** AI로 찍히는 것
아닌가」가 이 측정의 가장 큰 위험이었다. 그래서 **사람이 쓴 우리 문서**를 같이 넣었다.

> GIJO 문서 **0.052** ≈ 그들 사람 에세이 **0.059**

**장르 효과가 없다.** 이 지표는 저자를 잰다. 우려는 기각됐다.

### 길이도 원인이 아니다

우리 답(중앙값 664자)이 대조군(288자)보다 길어서 생긴 차이일까 봐 길이별로 갈랐다:

| GIJO 답변 | 건수 | 중앙값 | >0.5 |
|---|---|---|---|
| 200~400자 | 97 | 0.146 | 26.8% |
| 400~800자 | 170 | 0.139 | 30.6% |
| 800자 이상 | 148 | 0.173 | 26.4% |

**평평하다.** 길이가 만든 값이 아니다.

---

## 3. 왜 우리 답이 사람 쪽인가

| 특징 | 사람 에세이 | GPT-4o | GIJO 답변 | GIJO 문서 |
|---|---|---|---|---|
| 쉼표포함문장비율 | 0.211 | 0.637 | 0.406 | 0.297 |
| 평균쉼표사용률 | 0.008 | 0.026 | **0.022** | 0.010 |
| 평균상대위치 | 0.076 | 0.186 | 0.169 | 0.152 |
| 평균분절길이 | 3.887 | 9.219 | **5.636** | 7.446 |
| 쉼표앞뒤POS다양성 | 0.202 | 0.612 | 0.343 | 0.270 |

**쉼표는 GPT-4o만큼 자주 쓰는데(0.022 vs 0.026), 쉼표로 나뉜 마디가 훨씬 짧다(5.6 vs 9.2).**
만연체가 아니다. 그래서 종합 점수가 사람 쪽으로 간다.

---

## 4. 이 측정이 막은 것

「AI 문체 같다」는 **인상이었지 측정된 적이 없었다.** 재보니 병목이 아니다.

- **im-not-ai → 윤문 LoRA**: 지금 할 일이 아니다. ①ⓑ(RAG 오염)·②(규정 흔들림) 앞에 둘 이유가 없다.
- ⚠ 게다가 **순서가 거꾸로면 해롭다** — 「퇴사자 계정」이 14B에서도 즉시삭제 6/1년 1/2년 0/불명 2로
  갈리는데(같은 날 실측), 문체만 다듬으면 **틀린 답이 더 그럴듯해진다.**
- **KatFishNet → 학습 보상(reward)으로는 쓰지 않는다.** 재는 것이 「AI가 썼나」이지
  「담당자가 쓸 수 있나」가 아니다. 지표를 고르면 그 지표에 최적화된다.
- **쓸 자리는 「전후 눈금」이다** — 나중에 문체 작업을 한다면, 이 기준선 대비 움직였는지 확인하는 용도.

⚠ 다만 **28.2%가 0.5를 넘는다**(사람 기준선 10.7%). 일부 답은 실제로 AI스럽다.
없앨 문제는 아니고 **나중에 볼 값은 있는** 크기다.

### 폐쇄망 제약 (윤문 LoRA를 언젠가 한다면)

`learnloop.ts` 첫 줄이 **「사용 데이터가 폐쇄망 밖으로 나가지 않는」** 자가학습 사이클이라고 적고 있다.
고객 대화(`chat_logs`)를 Claude에 보내면 **그 약속이 깨진다.**
→ 우리 자체 데이터(제품 문서·우리가 만든 예시)로만 어댑터를 굽고, 고객 기계에서는 쓰기만 해야 한다.
→ 그리고 Claude 출력으로 다른 모델을 학습시키는 것이 이용약관상 허용되는지 **먼저 확인**해야 한다.

---

## 5. 다시 재는 법

```bash
python3 -m venv kfenv && ./kfenv/bin/pip install kiwipiepy scikit-learn numpy
git clone --depth 1 https://github.com/Shinwoo-Park/katfishnet.git
# tools/katfish-style-check.py 안의 BASE를 작업 폴더로 맞추고
./kfenv/bin/python tools/katfish-style-check.py
```

입력 두 개가 필요하다 (스크립트가 읽는 파일):
- `gijo-answers.txt` — `chat_logs`의 answer 중 200자 이상, 줄바꿈은 `\n`으로 escape해 한 줄씩
- `gijo-humandocs.txt` — 사람이 쓴 우리 문서 문단(200자 이상). **음성 대조군이므로 빼지 말 것**

⚠ **음성 대조군 없이 이 숫자를 읽지 마라.** 대조군이 없으면 「우리 답이 AI스럽다」와
「이 지표가 이 장르를 AI로 본다」를 구분할 수 없다. 이 측정에서 둘을 가른 것이 대조군이다.

⚠ Java가 있는 기계에서는 원본대로 `konlpy.tag.Kkma`를 쓰는 편이 논문과 직접 비교된다.
이 값은 **Kiwi 기준**이다.

---

*근거: KatFishNet(ACL 2025, Shinwoo-Park/katfishnet) 데이터·방법 · GIJO `chat_logs` 415건 ·
GIJO 문서 468문단 · 이 Mac(Kiwi 0.23.2 · sklearn 1.6.1). 같은 날 [[GIJO_AS_RAG_오염_실측_2026-08-10.md]]와 짝.*
