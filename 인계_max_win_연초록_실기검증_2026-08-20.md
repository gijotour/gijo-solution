# 인계 — `max` → `win` (2026-08-20) · 라이트 연초록 **실기(mac dmg) 검증 회신**

## 0. 판정

**셸·화면 = 통과 ✅ / 로그인·설치 화면 = 결함 1건 ❌**
Mac 실기 빌드 **Lite 1.2.0**(`release-lite/GIJO AS Lite-1.2.0-arm64.dmg`, 1.2G, 인증서 서명 `Authority=GIJO AS Lite Signing`)로 실측.

⚠ **버전 정정**: max가 관성으로 1.1.12를 올렸다가 `GIJO_AS_버전관리_기준.md`의 「다음 라이트 게시 = **Lite 1.2.0**(minor)」에 맞춰 중단·재빌드했다. 대장의 그 규칙이 실제로 작동했다.

---

## 1. 통과한 것 — 실측값

**셸(lite-app.html)** — CDP 실측:
- `lite-green.css` 적재 **true**, 순서도 `gijo-ui.css` 뒤(계약대로)
- `--g-bg` = **#E3EEDD**, `--g-panel` = **#CFE4C4**
- body 배경 = `rgb(227,238,221)`, 사이드바 = `rgb(207,228,196)` — **연초록 실제 렌더 확인**

**화면(iframe)** — `lite-settings.html?embed=1` 배경 = `rgb(235,244,235)` ✅
개별 화면 13개는 `:root`에 팔레트 직접 박음(`--bg:#EBF4EB` · `--text:#16241A`) — green.css `<link>`가 없어 처음엔 누락으로 보였으나 **오탐**이었다(설계대로).

**정적 계약**:
- 공용 `gijo-ui.css` 안 건드림 ✅
- **다크 폴백 누수 0** — `lite-app.html`이 쓰는 `--g-*` 7개(`bg·panel·fg·border·hover·accent·accent-weak`)가 green.css에 **전부 정의**되어 `var(--g-border, #262a33)` 류 어두운 폴백은 안 탄다 ✅
- 폰트 게이트 최소 11px, 위반 0 ✅
- max 재편 온전 — ⑤연락처·⑦두뇌·⑧바깥연결(폐쇄망 칩)·④지식창고·글씨크기 ✅
- 부팅·백엔드(`dist/lite/index.js`)·서명 정상 ✅

---

## 2. ❌ 결함 — 로그인·설치 화면이 연초록 계약에서 빠졌다

**실측(라이트 1.2.0 첫 화면, 로그인 전)**:
```
url: login.html · body 글자: rgb(233,231,226)(밝은 회색=다크 전제)
--g-bg: (빈 값) · lite-green.css 적재: false · 배지: 🏢 표준
```

**원인**: 라이트도 `login.html`을 쓰는데(`main.ts:386` — `첫설치인가() ? setup.html : login.html`) 그 두 파일은
- `lite-green.css`를 **안 싣고**(`grep lite-green login.html setup.html` → 0),
- 인라인 색이 **다크 전제**(`#1f1e1d` 3곳·`#262624`·`#242320` 등).

**증상 3가지**:
1. 라이트를 켜면 **첫 화면이 다크** → 로그인하는 순간 연초록으로 **바뀐다**(에디션 이름표가 첫인상에서 거짓말).
2. 배지가 **「🏢 표준」** — 라이트인데 표준 이름표(`8dc19d4`에서 max가 곁다리로 넘겼던 그 건이, 배색 계약이 생기면서 **결함으로 승격**됐다).
3. `uireadability` 개정 계약 「에디션별 단일 바탕 + 에디션 간 상이 필수」를 **로그인 화면이 위반** — 라이트·표준 로그인이 같은 바탕이다.

**제안(win 판단에 맡김)**: `login.html`·`setup.html`에 라이트일 때만 `lite-green.css`를 얹는 갈래(셸이 쓰는 주입기와 같은 방식) + 배지를 에디션 우선으로(라이트면 「🪶 라이트」). 감시는 개정된 uireadability에 **로그인·설치 화면**도 에디션 축으로 넣으면 재발이 막힌다.

**게시 판단**: 이 결함은 **첫인상 전용**(기능 무해). 사장님이 「먼저 내보내고 로그인 화면은 다음 판」이라 하시면 1.2.0 그대로 나가도 되고, 「이름표는 첫 화면부터」면 수리 후 1.2.0 재빌드다. **사장님 결정 대기** — max는 어느 쪽이든 재빌드·재검증 준비됨.

---

## 2′. max 자기 결함 1건 — 발견 즉시 수리함 (`lite-settings.html`)

사장님 실화면 스크린샷에서 **원격 GPU 주소 칸이 「$」 한 글자로 찌그러져** 보였다. 실측:
`rgpuUrl` **폭 39px**(정상 칸 158px) · `rgpuBox` 189px(정상 330px).

**원인은 win의 배색이 아니라 max가 어제 넣은 한 줄 배치 규칙**이다:
`.card > *:not(h2):not(.sub){margin-left:auto}` — `margin-left:auto`만 주면 상자가 **콘텐츠 폭으로 줄어들어**, 안에 `flex:1` 입력이 있어도 같이 눌린다(placeholder `http://10.8.0.12:8080/v1`가 한 글자만 보였다).

**수리**: 그 규칙에 `min-width:330px;max-width:100%` 추가.
**실측 검증**(실행 중인 1.2.0에 주입): `rgpuUrl` 39 → **180px**, `rgpuBox` **330px**, 다른 칸 회귀 없음(`hfTok=483·pw1=158·lawKey=158·srvUrl=158`), 가로 넘침 없음.

## 3. 곁다리

- max 쪽 확인: win의 63커밋 중 `main.ts` 프로 셸 이탈 금지(`저장된셸모드()==="pro"` 조건)는 **라이트에서 조건 자체가 거짓** → 무해 ✅
- LoRA 어댑터 건(어제 인계 `48ffce5`)은 그대로 유효 — win 회신 대기 중: **GPU VRAM 사양** · 운영 QA 로그 채굴.
