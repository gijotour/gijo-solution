# GIJO AS 셸 재구축 계획서 — 내 문서가 메인이 된다

작성 2026-08-23 · 근거: 사장님 지시(「내 문서가 대시보드 메인 · +대화창+상단바로 시작 · 기존 디자인 다 걷어내고 기능 하나씩 다시 올리자 · 테스트는 기능 하나 만들 때마다」)

---

## 0. 한 줄 요약

**서버는 손대지 않는다.** `app.html`에 **셸 모드를 하나 더**(`shell=docs`) 만들어 「내 문서 + 대화창 + 상단바」만 보이게 하고, 기능을 하나씩 그 모드로 옮긴다. 모드는 **플래그 하나**라 언제든 끄면 지금 모습으로 돌아온다.

> ⚠ **2026-08-23 정정** — 처음엔 「새 파일 `shell.html`」로 계획했다가 설계 검토에서 뒤집었다. 근거는 §2′.

---

## 0″. ★ 방향 확정 — 5대 메뉴 + 안티그래비티 배치 (2026-08-31 사장님 지시, 실화면 캡처 3장)

> 「지금은 안티그래비티처럼 가는 걸로 변경했으니 3번 그림처럼 나와야 해」 ·
> 「모든 메뉴를 크게 5가지로 통합 및 그룹핑」 · 「라이트 버전은 내 지식과 내 문서만」

1. **배치**: 메뉴를 누르면 그 화면이 **왼쪽에 바로 열리고** 대화창은 **오른쪽 상시 도킹**.
   지금의 「메뉴=카드가 전부」 계약(5.41.0 — 메뉴 클릭은 현황 카드만)은 **이 지시로 개정**된다.
   (사장님이 캡처 1번(카드만)과 3번(나란히)을 대비해 3번을 지정)
2. **5대 메뉴** (사장님 원문 + 세부 매핑은 내 제안 — 갈림 2개는 아래):
   | 메뉴 | 사장님 정의 | 들어가는 것(제안) |
   |---|---|---|
   | 🧠 내 지식 | 학습한 데이터를 대화로 질문·확인 + 법령 질문 | 지식 창고 · 법령·판례 · GIJO AS 안내 · 보안제품 비교·소개 |
   | 📓 내 문서 | 모든 데이터 업로드·확인 + 위키처럼 폴더 지정 상시 확인 + **업무 넘기기 포함** | mydocs 전부 · handover · 작업 내역 · ★신규 「지켜보는 폴더」 |
   | 🩹 취약점 업무 | 취약점 분석 파일·보안로그 분석 → 담당자 지정·관리·보고 | ①~⑤ 절차 허브 전부(KPI·컴플라이언스 포함) · 보안 로그 파일 분석 |
   | 🧰 보안제품 관리 | 등록된 자산·보안제품 체크리스트, 스크립트 만들어 직접 스캔·보고 | 우리 보안제품 · 자산 관리(전체 — 고르기는 🩹 ⓪) · 하드닝 · 정기 점검 · 공급망 |
   | ⚙ 설정 | AI 팀 등 나머지 모두 | 설정 · AI 전부 · 기록(감사·시스템 로그) |
   - 갈림(내 기본값, 사장님 반려 가능): **자산→보안제품 관리**(「등록된 자산 및 보안제품」) ·
     **KPI·보고→취약점 업무**(보고 시나리오가 그쪽).
3. **업무 시나리오**: 내 문서=모든 데이터의 기준 → 내 지식에서 대화로 확인(+법령) →
   취약점 업무·보안제품 관리가 그 자산으로 일하고 보고.
4. **라이트 = 내 지식 + 내 문서 두 메뉴만** — 메뉴는 2026-08-31에 반영 완료(커밋 7219f8b2,
   🧠 내 지식·📓 내 문서 + ⚙는 상단바). ★**후속 지시(같은 날)**: 「프로 이번 제품 작업 이후
   내지식·내문서 라이트에도 반영」 — 프로 5대 메뉴 재편이 끝나면 **그때의 내 지식·내 문서
   화면(위키 폴더 포함)을 라이트에 동기화**한다. 순서: 프로 완료 → 라이트 반영 → max dmg 검증.
5. 진행: 시안 `mockups/menu5-antigravity/` + 정찰(연쇄 전수) → 설계관 → 구현.
   ⚠ 연쇄가 큰 자리: workflow.test 5단계 감시(「사이드바=서버 표」)·TAB_REDIRECT·screenguide
   흡수자리 표(2026-08-30에 채운 20줄이 또 낡는다)·qa-auto 정확 집합·카드 계약 관문.
6. **구현 완료(2026-08-31, 게시 대기)** — 커밋 `54857c1e`(계약 개정: 메뉴 클릭=화면+카드
   나란히, 부속 플래그 3종 걷음, wiringcontract·게시 관문 반전) + `0c81dd4a`(5그룹 재편:
   nav.js ⓐ′·절차 id 항목 이식·화면이름찾기 2패스·히어로 칩·qa 재작성). 검증: WSL 4,146 +
   win 11 전수 초록 · 실화면 CDP 실측 통과(5그룹·화면+카드 도킹·갈아타기 회피·칩 6개).
   **의도된 제외**(정직 보고): 조치요청서는 진입 주소가 없어 메뉴 항목 없음(별도 결정).
   ~~📂 지켜보는 폴더~~ → **2026-08-31 2단계 구현 완료**: 서버 watchfolder 엔진(주기 폴링·
   2단 스킵 멱등·충돌/남의 문서/등급 보호·문서 갈래 고정) + 대화 도구 3종(admin 결재판) +
   강제 라우팅 + 내 문서 📂 판(보기 전용) + 📓 메뉴 항목 + 관문 검사. 라이트는 v1 접힘
   (지정 도구가 화이트리스트 밖 — 라이트 반영 라운드에서 재판단).
   **시안과의 의도된 어긋남 2**(검토관 뒤 공개): ⑤ 보고 라벨의 「(KPI·컴플라이언스)」 꼬리는
   뗐다(서버 단계 이름과 글자 일치가 정본 — 2026-08-05 실사고 계보) · 자산 관리 메뉴 주소는
   hub=1이 아니라 **full=1**(hub=1은 허브 무대 표시 계약이라 「한눈에」 띠까지 숨긴다).

## 0′. 진행 현황 — **다음 세션은 여기서 이어간다** (2026-08-23 마감)

| 조각 | 상태 | 커밋 | 시안 |
|---|---|---|---|
| **0-1** 프로 홈 = 내 문서 + 대화창 나란히 | ✅ 완료 | `e5873ffa` | `01_프로홈_내문서+대화창.html` |
| **0-2** 56px 세로 레일 → 상단바 가로 줄 | ✅ 완료 | `cac2462c` | `02_상단바로_레일이사.html` |
| **0-3** 메뉴 판(200px·접기) — 전 화면 공통 | ✅ 완료(5.71.0 게시) | `b05574ac` 부근 | — |
| **0-4** 대화창 폭 끌개(‖·유령선) — 전 화면 공통 | ✅ 완료(5.72.0 게시) | — | — |
| **1-1** 내 문서 노트북형: ☑ 근거 지정(하드 필터+정직 배너) + 📎 지난 작업 첨부 | ✅ 구현(게시 대기) | 이 커밋 | `mockups/mydocs-notebook/` |
| **0-5** 길찾기 문구 (☰ 전체 메뉴 → 새 표현) | ✅ 완료 | `ca21f4a7` | — |

- **1-1 ③라운드(「123진행」, 2026-08-30)로 닫은 것**: ① 전체 지정 체크(상한 50 — 막고 이유 말함)
  ② 인용 문서명 클릭 → 내 문서에서 원문 열기(ack 핸드셰이크·데이터 판정·모달 존중 — 문단 좌표
  점프·형광펜은 원래 포기 항목 그대로) ③ 근거 범위 라벨(근거띠·칩 있을 때만) ④ 보낸 말풍선 아래
  「📚 지정 N · 📎 첨부 M」 ⑤ ⧉ 분리 대화창: ☑·📎 양방향 동기(ground:req·detach)와 인용 클릭
  (bridgeToShell). 검토 2회 누적 확정 54건(37+17) 전부 처리 — ③라운드는 오탐 0.
- **1-1 알려진 한계 — 🎯은 해소됐다**(2026-08-31 확인). 「⧉ 분리 대화창의 🎯 선택이 도킹
  인스턴스로 간다」는 **2026-08-30 커밋 `cf23b5d9`(「🎯 선택이 ⧉ 분리 대화창까지 닿는다 —
  네 방향 중계」)로 닫혔고** 5.77.0·5.80.0 게시본에 실려 있다. ☑·📎·인용은 그 전 ③라운드로
  해소됐으니 1-1의 한계는 **남아 있지 않다.**
  ⚠ 이 줄이 하루 넘게 낡은 채로 남아 있었다 — 코드가 고쳐질 때 계획서가 같이 안 움직이면,
    다음 사람이 **이미 닫힌 것을 또 연다**(오늘 실제로 그 후보로 올라왔다).
  📎 첨부는 세션 소유자 격리가 없는 현행
  work_sessions 정책을 따른다(sessions.html과 동일).
  → **2026-08-30 사장님 「123진행」으로 확정**: 첨부 권한 층은 현행 유지(ⓐ). 격리가 필요해지면
  (다중 사용자 파일럿 등) 별도 작업으로 뺀다 — userId 열 신설이 그때의 정석.
- **1-1 설계 요점**(2026-08-30, 사장님 「내문서는 NotebookLM처럼·작업내역은 첨부파일처럼」):
  통로=ALS 꼬리표 `ragscope.ts` 하나(viewerctx 무늬) — 대화 RAG·normaltic·근거배지·explain 도구가
  **한 기계**로 같은 범위를 봄. 지정 필터는 등급 가림에 AND(우회 원리상 불가). 첨부=결정적 압축
  (`attachSessionText`, 세션당 1,200자·최대 3). 셸 배치(메뉴판·‖·폭조절)는 **원래 전역** — 게시
  관문에 「셸 전역」 검사로 못박음(사장님 「전체 대시보드 적용」 지시).

- 시험 **4,087개 전부 초록** (WSL 4,076 + Windows 전용 11)
- 시안은 `G:\내 드라이브\GIJOAS_v2\`에 **순서대로** 쌓는다(사장님 지시 2026-08-23)
- 게시 **안 했다** — 설치본은 5.70.0 그대로. 다음 게시는 5.71.0

### ⏸ 사장님 확인 대기 중 (다음 세션 첫 질문)
개발 실행으로 앱을 띄워 드렸고 **실화면 확인이 아직 안 끝났다.** 다음에 이어갈 때 먼저 물을 것:

1. 첫 화면이 **내 문서**로 떴는가 · **대화창이 옆에** 보였는가
2. 상단바 아이콘 **15개가 다 눌렸는가** (안 눌리고 창만 끌리면 `-webkit-app-region` 문제)
3. **AI 팀 10개**(🧭◣◎▣🛰📚 E R G L)가 보였는가
4. ☰ 옆 **배지 숫자**가 보였는가 (기한 지난 업무 — 2026-08-01 사고 자리)
5. 오른쪽 끝 **계정·문서함**이 안 잘렸는가

### 미결 — 사장님 결정이 필요한 것
**AI 팀 로스터 10개를 상단바에 계속 둘까?** 상단바 가로의 절반(347px)을 쓴다.
① 그대로 ② 아이콘 하나(🤖)로 접기 ③ 대화창 머리로 이동
→ 2026-08-23에는 **①(그대로)**로 두고 진행하기로 했다. 0-3에서 다시 판단.

### 다음에 앱을 다시 띄우는 법
```
cd "D:\Connect AI\client"
npm run build
npx electron .
```
기본 셸 모드가 **프로**라(`main.ts 저장된셸모드`) 바로 새 모습이 뜬다.
⚠ 게시가 아니라 **개발 실행**이다 — 설치본(5.70.0)과 별개이고, 창을 닫으면 끝난다.
⚠ 운영 서버(4000) **실데이터**에 붙는다 — 고치면 진짜로 고쳐진다.

---

## 1. 지금 상태 (2026-08-23 실측)

| | 숫자 |
|---|---:|
| 화면 파일 | **56** (본체 43 · 라이트 13) |
| 화면 HTML 총량 | 31,304줄 |
| **화면 안 `<style>`** | **5,189줄 (17%)** ← 걷어낼 것 |
| 공용 부품 | 24개 파일 9,929줄 |
| 옛 셸 `app.html` | 1,389줄 (표준·프로 겸용) |
| `nav.js` **흡수 표** | 12건 (옛 화면명 → 새 자리) |
| `mydocs.html` | 2,005줄 · 서버 API **3개만** 호출 |

### 중요한 발견 두 가지

**① 내 문서는 이미 정문이다.** 서버 API를 3개(`/api/dispatch`·`/api/memory/documents`·`/api/upload/receipts`)만 부르고 나머지 일은 전부 **대화창(dispatch)을 지난다.** 「메뉴는 보기용, 대화창이 입력」 원칙이 이미 지켜져 있다 — 메인으로 올리는 건 큰 공사가 아니다.

**② `nav.js`에 흡수 표 12건이 있다.** `mywork.html→dashboard` · `ontology.html→memory` · `mcp.html→settings?s=link` 같은, 메뉴를 29→26으로 줄이며 쌓인 이력이다. **새 셸이 이걸 안 가져가면 옛 링크·즐겨찾기·대화 명령이 전부 깨진다.** 반드시 이관한다.

---

## 2. 원칙 (이번 재구축이 지킬 것)

1. **서버 무수정.** 화면만 바꾼다. 서버를 고쳐야 하면 그건 별도 작업으로 뺀다.
2. **새 파일에 짓는다.** 옛 `app.html`을 고치지 않는다 — 고치면 표준·라이트가 같이 흔들리고 되돌릴 길이 없어진다.
3. **화면에 입력칸을 두지 않는다.** 지시는 대화창에서만(기존 확립 원칙).
4. **화면엔 정체성 한 줄과 ⚠경고만.** 사용법·용어 풀이는 `screenguide` + ⓘ로.
5. **디자인은 나중.** 지금은 기능과 배선만. 촘촘하게(엑셀처럼), 예쁘게는 다 올린 뒤.
6. **한 기능이 끝나기 전에 다음으로 안 넘어간다.** 완료 기준은 §5.

---

## 2′. ★ 왜 새 파일이 아닌가 (2026-08-23 설계 검토, 3갈래 병렬)

처음 계획은 「새 파일 `shell.html`을 만들면 옛 셸이 무손상이라 되돌릴 수 있다」였다. **그 전제가 틀렸다 — `app.html`이라는 파일명 자체가 계약이다.**

### 새 파일이면 깨지는 것 (전부 file:line 확인)

| 어디 | 무엇 |
|---|---|
| `main.ts:588` | **셸 파괴 방지 안전망**이 `getURL().includes("app.html")`에 묶임. 새 파일은 문지기를 못 타 화면에서 `navigateTo`가 떨어지면 **본창이 화면 파일로 교체되어 셸이 사라진다**(2026-08-19 사장님 실사고의 수리본) |
| `main.ts:607·612·621·560` | `?shell=pro` 신호 · titleBarOverlay · 없는 화면 폴백 · 라이트 보정 |
| `publish-gate-ui.mjs:108·111` | 로그인 뒤 `app.html`을 찾아 셸로 잡고 40여 검사를 돌린다 → 못 찾으면 **게시가 중단된다** |
| `wiringcontract.test.ts:72` | 새 `.html`은 선택배선대장 미분류로 **무조건 실패** |
| `wiringcontract.test.ts:149` | `main.ts` 원문을 정규식으로 검사 — main.ts를 고치면 이 시험도 같이 |
| QA 도구 5종 | qa-shell · menu-sweep · window-sweep · ui-check · qa-auto |
| `shotlist.test.ts:28` | nav.js에 파일명을 적으면 「메뉴에 있는데 안 찍는 화면」으로 실패 |

### console.js가 셸에 기대는 것 (내가 과소평가했다)

- **message 리스너가 한 줄도 없다.** `app.html:863`이 유일한 수신부다. 8종(`gijo:openTab`·`pickdone`·`select`·`view`·`scope`·`explain`·`ask`·`prefill`) + 발신자 검증 `내것인가()`(848) + 값 재조립 `선택정돈()`(885). **없으면 화면에서 눌러도 아무 일도 안 일어난다 — 오류도 없이.**
- `window.gijoTabs` **6개 멤버**를 직접 부른다(`open`·`toChat`·`activeScreen`·`activeLabel`·`list`·`close`). 없으면 `navigateTo` 폴백 5곳이 발동해 **셸을 날린다.**
- `chatparts.js`를 **무가드로** 부른다(1678-1680) → 없으면 TypeError가 catch로 떨어져 **도착한 답이 오류 말풍선으로 교체된다.**
- body 클래스 `pro-shell`·`stage-on`·`chat-home`을 읽어 동작이 갈린다.
- `#consoleHost`는 **정적 마크업**이어야 하고 `<script src="console.js">`가 **그 뒤**에 와야 한다(파싱 시점에 잡는다). 부모가 높이를 확정 안 하면 **0px로 접힌다.**

### 그래서: `app.html`에 모드를 하나 더

`app.html`은 **이미** 모드 전환 장치를 갖고 있다 — `main.ts:607`이 `?shell=pro`를 붙이고 `app.html:265`가 읽어 `body.pro-shell`을 달면 CSS가 레이아웃을 통째로 바꾼다. 그 주석이 스스로 적어 뒀다: **「`embed=1`·`popout=1`과 같은 관례다 — 새 통로를 만들지 않는다」**

세 번째 모드 `shell=docs`를 더하면 위 표가 **전부 회피**되고, 되돌리기는 파일 교체가 아니라 **플래그 끄기**가 된다.

---

## 2″. 흡수 표 — 「12건」이 아니었다

처음에 「흡수 표 12건」이라 적었는데 **정확하지 않다.** 실제로는 **네 곳에 흩어져** 있고 각각 다른 경로를 막는다:

| 표 | 크기 | 막는 경로 |
|---|---|---|
| `nav.js:218` `TAB_REDIRECT` | **52키 / 32화면** | 화면 문서가 스스로 실린 뒤 자리 이동 |
| `app.html:394` `옛탭` | 5개 | localStorage 탭 복원 (파일이 **삭제된** 화면은 여기서만 막힌다) |
| `nav.js` TOP hidden | 3개 | 🔍 화면찾기 옛 이름 검색 |
| `screenguide.ts` 화면별칭 | — | AI가 「그거 어디야?」에 답할 때 |

★ 더 중요한 것: **TAB_REDIRECT 32화면 중 20개는 파일이 살아 있다.** 그건 「없어진 화면 표」가 아니라 **5단계 허브 배관**이다(`report.html` → `reporting.html?panel=report` → 허브 무대 iframe). 새 셸엔 그 허브가 없으므로 **그대로 옮기면 없는 자리를 가리킨다.**

→ **(A) 파일이 진짜 없는 12개**와 **(B) 허브 배관 20개**를 갈라, **(B)는 옮기지 않는다.**
→ (B)를 옮길 경우에만 `hub=1` 예외(nav.js:1665)까지 한 묶음으로. 빼면 자산 관리에 **영영 못 간다(오류도 안 난다).**

---

## 2‴. 서버가 화면 이름을 문자열로 아는 곳 5개

`screenguide.ts` GUIDES(≈45개)·화면별칭·별명열기탭 · `screencontext.ts` SCREENS · `workflow.ts` STAGE_SCREENS · `howto.ts` page.

⚠ **`getScreenContext`가 쿼리를 안 뗀다.** 새 셸이 `mydocs.html?tab=vendor`를 맥락으로 보내면 조회가 빗나가 **화면 맥락도 도구 좁히기도 안 걸린다 — 오류 없이.** → activeScreen()은 **쿼리 없는 파일명**으로 통일한다.

⚠ **길찾기 안내가 거짓말이 된다.** 새 모드엔 사이드바도 ☰ 전체 메뉴도 없는데 서버는 둘 중 하나로 답한다. 셸 표식(`#셸`)에 세 번째 값을 더해야 한다.

---

## 3. 새 셸의 모양

```
┌─────────────────────────────────────────────────────┐
│ 상단바   GIJO AS   [사람]        🔔  ⚙              │  ← 얇게
├──────────────────────────────┬──────────────────────┤
│                              │                      │
│   내 문서 (메인)              │   대화창              │
│   ├ 문서 트리                 │   (console.js 재사용) │
│   ├ 목록                      │                      │
│   └ 상세·편집                 │   여기가 입력구        │
│                              │                      │
└──────────────────────────────┴──────────────────────┘
```

- **어디에**: `app.html` 안 **세 번째 셸 모드** `?shell=docs` → `body.docs-shell`
- **재사용(그대로)**: `console.js` 대화창 · message 수신부 8종 · `gijoTabs` · `gijo-ui.css` 부품 30종 · 흡수 표 소비 코드
- **CSS로 감춤**: 56px 레일(`#gijoRail`) · 사이드바(`#gijoNav`) · 탭줄(`.shellfoot`)
- **새로 붙임**: 왼쪽 판에 `mydocs.html?embed=1` · 상단바에 「🏠 내 문서」 복귀
- **화면 열기 규약**: `gijoTabs.open()` → **왼쪽 판이 그 화면으로 바뀐다**(내 문서가 홈). 별도 창을 새로 만들지 않는다

### 왜 `gijo-ui.css`를 새로 안 만드나
이미 있다 — 토큰 3계층(원시→시맨틱→컴포넌트)·4px 간격 스케일·부품 30종(`.g-rows` `.g-card` `.g-btn` `.g-chip` `.g-table` `.g-modal` `.g-kpi`…). **「디자인 걷어내기」는 새로 만드는 일이 아니라 화면별 `<style>` 5,189줄을 지우고 `.g-*`로 옮기는 일이다.**

---

## 4. 기능 단위와 순서

### 단위는 「화면」이 아니라 「사용자가 하는 일」

`inventory.html` 1,841줄은 자산 목록·상세·가져오기·담당배정 넷이다. 한 번에 옮기지 않는다.

### 0단계 — 뼈대 (이것부터)

| # | 기능 | 무엇을 하나 |
|---|---|---|
| 0-1 | `shell=docs` 모드 배선 | `main.ts` 저장된셸모드에 `docs` 추가 → `app.html`이 `body.docs-shell` |
| 0-2 | 레일·사이드바·탭줄 감추기 | CSS만 (프로 모드가 이미 하는 방식 그대로) |
| 0-3 | 왼쪽 판 = 내 문서 | `mydocs.html?embed=1&theme=light` |
| 0-4 | 상단바 정리 + 🏠 복귀 | 제품명·사람·알림·설정만 남김 |
| 0-5 | `gijoTabs.open()` → 왼쪽 판 교체 | 화면 열기가 내 문서 자리로 오게 |
| 0-6 | 셸 표식 세 번째 값 | 길찾기 안내가 없는 사이드바를 가리키지 않게 |

**0단계가 끝나면 그것만으로 쓸 수 있는 제품이다.** 문서를 만들고·고치고·AI에게 물을 수 있고, AI가 「가서 하기」를 주면 왼쪽 판이 그 화면으로 바뀐다.

**0단계가 끝나면 그것만으로 쓸 수 있는 제품이다.** 문서를 만들고·고치고·AI에게 물을 수 있다.

### 1단계 — 매일 쓰는 것

| # | 기능 | 옛 화면 |
|---|---|---|
| 1-1 | 파일 올리기 · 반입 영수증 | console ＋ |
| 1-2 | 문서 편집 · 버전 이력 · 첨부 | mydocs |
| 1-3 | 연락처(담당자×제품) | lite-contacts |
| 1-4 | 조치 요청서 만들기 | mydocs·docbox |

### 2단계 — 보안 본업

| # | 기능 | 옛 화면 |
|---|---|---|
| 2-1 | 자산 목록·상세 | inventory·assets |
| 2-2 | 취약점 보기·조치 | vulnscan·verify·fix |
| 2-3 | 결재판 | approvals |
| 2-4 | 보안제품 | products |
| 2-5 | SBOM·공급망 | sbom·supplychain |
| 2-6 | 분석 허브 | analysis·loganalysis·syslog |

### 3단계 — 그다음

AI 지식(memory) · AI 팀(agent·office·supervision) · 리포트 · 컴플라이언스·하드닝 · 작업세션·인수인계 · KPI 현황판 · 학습 후보함 · 레드팀 · 설정 · 유지보수·감사 · 법령

### ⚠ 43개를 다 옮기지 않는다

흡수 표가 이미 12건을 흡수했다. **이번이 한 번 더 거를 기회다.** 3단계에 들어갈 때 「이 화면을 최근에 쓴 적이 있나」를 먼저 묻고, 아니면 **안 옮긴다.** 안 옮긴 것은 원장(§6)에 남긴다.

---

## 5. 기능 하나의 완료 기준 — 이걸 다 통과해야 다음으로

```
□ 1. 서버 API를 안 고쳤다                     (고쳐야 하면 별도 작업으로 분리)
□ 2. 화면 안 <style> 0줄 — 공용 토큰만 썼다
□ 3. 입력칸이 없다 — 지시는 대화창으로 간다
□ 4. 정체성 한 줄 + ⚠경고만. 설명은 screenguide에 넣고 ⓘ로 연다
□ 5. vitest 통과 (지금 3,641개) + 이 기능의 새 시험 추가
□ 6. 실화면 확인 — 띄워서 눈으로 본다 (게시 때는 9227 관문)
□ 7. 대화창에서 부를 수 있다 (screenCard 등록)
□ 8. 옛 화면 처분 결정 — 지우거나 흡수 표에 넣거나
```

**7번을 빠뜨리면 「화면은 있는데 대화창이 모르는」 상태**가 된다. 이 저장소가 반복해 겪은 배선 누락이다.

---

## 6. 옛 화면 처분 — 원장을 만든다

이 계획의 가장 큰 위험은 **완주하지 못하고 두 셸이 공존한 채 멈추는 것**이다. 그래서 원장을 둔다.

`client/src/renderer/pages/shell-ledger.json` — 기능마다 세 상태:

| 상태 | 뜻 |
|---|---|
| `옮김` | 새 셸에 있고 8개 기준 통과 |
| `대기` | 아직 안 옮김. 옛 화면이 살아 있음 |
| `버림` | 안 옮기기로 함 (이유 함께) |

시험 하나가 이 원장과 실제 파일을 대조한다 — **원장이 낡으면 시험이 깨진다.**

---

## 7. 위험과 대비

| 위험 | 왜 무서운가 | 대비 |
|---|---|---|
| **완주 못 함** | 두 셸 공존 = 어느 쪽이 진짜인지 아무도 모름 | §6 원장 + 매 기능 완료 보고 |
| **흡수 표 오이관** | (B) 허브 배관 20개를 옮기면 없는 자리를 가리킴 | §2″대로 (A)만. 소비 코드는 `nav.js`에 그대로 둠 |
| **라이트가 갈라짐** | 「라이트=프로 부분집합」 원칙 깨짐 | 모드가 `app.html` 안이라 라이트도 같은 코드. 라이트는 `docs` 모드를 **안 켜면** 그만 |
| **사장님 테스트와 충돌** | 지금 테스트 중 | **모드를 안 켜면 지금과 완전히 동일** — 병행 가능 |
| **셸 파괴** | `navigateTo` 폴백이 본창을 갈아치움 | `app.html`에 머무르므로 `main.ts:588` 안전망이 그대로 작동 |
| **대화창 반쪽** | `chatparts.js` 무가드 · `gijoTabs` 부재 | 부품·전역이 이미 app.html에 있으므로 해당 없음 |
| **디자인 유혹** | 예쁘게 하다 기능이 안 늘어남 | 원칙 5. 다 올린 뒤에 디자인 라운드 |

---

## 8. 진행 방식

- **한 기능 = 한 커밋**(또는 한 묶음). 완료 기준 8개를 보고에 체크로 적는다.
- **큰 묶음은 착수 전 설계관**(`gijo-architect`)에 태운다.
- **게시는 사장님 허락 후.** 0단계가 끝나면 한 번 게시해 실물로 확인받는다.
- 서버 의존 정리(되먹임 화살 12개)는 **이 작업과 안 겹친다**(서버/클라). 셸이 우선이고, 서버는 「쉬움 6개」만 사이사이.

---

## 9. 전중후 계획서와의 관계

계획서 **전-7 「보여 주기」의 연장**이다. 새 기능을 만드는 게 아니라 **있는 기능을 하나의 화면으로 모으는** 일이고, 파일럿에서 보여 줄 물건을 단단하게 만든다. 판매 구성(3에디션)·중단계 항목과 충돌하지 않는다.

---

## 📖 용어 풀이

- **셸(shell)** — 화면들을 담는 껍데기. 상단바·대화창·화면 자리를 배치하는 틀
- **흡수 표** — 없어진 화면 이름을 새 자리로 넘겨주는 표. 옛 링크가 안 깨지게 하는 장치
- **공용 토큰** — 색·간격·글자 크기를 한 곳에 정해 두고 모든 화면이 그것만 쓰는 방식. 화면마다 따로 칠하지 않는다
- **배선** — 화면과 대화창·서버가 서로를 알아보게 연결하는 일. 빠지면 「있는데 안 불리는」 상태가 된다
- **원장(ledger)** — 무엇을 옮겼고 무엇이 남았는지 적은 장부
