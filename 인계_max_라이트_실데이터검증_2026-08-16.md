# [인계] max → win — 라이트 실데이터 검증 결과 (2026-08-16)

max가 라이트 1.1.1(Mac dmg)을 **사장님 로그인·실모델(qwen3-14b, 8080) 실데이터**로 CDP 구동해 검증했다.
핵심 흐름은 정상, **결함 3건**을 넘긴다(수정은 win). 기준 hub 31f0d89.

## ✅ 실데이터 정상 확인
- 챗 RAG — 실답 정확: "로그 보관 며칠?" → "접속기록 1년↑, 5만명·민감정보 2년↑ · 개인정보 안전성 확보조치 기준 제8조". 근거 문서 기반.
- 챗 범위/정직성 — 무관 질문("서울 날씨")에 **범위 거절**("사내 보안만") + 관련 질문 제안, 할루시네이션·외부 유출 0.
- 법령 — "유출 신고 며칠?"에 **72시간 정답 포함**(win 08-13 수리 실데이터 확인) · 에어갭 "바깥 조회 꺼짐" 정직 안내.
- 장비 등록부 — 추가·렌더·[열기/SSH/복사]·localStorage 저장 정상.
- 대시보드 실수치 — 내 할 일 3·제품자료 20·대화 이력·샘플 작업(샘플-웹서버) 정상.

## 🔧 결함 (win 수정)

### #2 [중요] 대시보드 「모델 멈춰 있음」 거짓 표시 — 로컬 케이스
- 증상: 대시보드 "지금 돌고 있나 → **멈춰 있음** / 모델이 안 올라와 있습니다"인데 **모델은 실제 서빙 중**(8080 llama health ok, `models/qwen3-14b`), 챗 두 질문 실답 냄.
- 소스: `g.getLocalEngineStatus()`(→ `/api/localengine/status`)가 「멈춤/미로드」를 돌려줌.
- 추정 뿌리: 사장님이 **기본 모델(gijo-main-orchestrator 7.6B) 대신 qwen3-14b**를 올림 → 상태 로직이 **기본/기대 모델 기준**으로 "멈춤"을 오보고. win이 원격 모드 거짓표시는 `de0dbc9`로 고쳤으나 **로컬에 비-기본 모델이 뜬 케이스**가 남음.
- 왜 중요: "지금 돌고 있나"는 **첫날 핵심 지표**. 되는데 "멈춤"으로 보이면 고객이 고장으로 오인.

### #3 법령 RAG가 문서의 **작성 지침 메타 섹션**을 결과로 노출
- 증상: "유출 신고 며칠?" 결과 상단에 문서의 메타/스캐폴딩("4. 이 문서를 늘릴 때 / 한 항목은 네 조각이다: 1.질문 예 2.답 3.⚠자주 나는 오해 4.확인 경로…")과 무관한 제21조(파기)가 먼저 뜨고, **정작 72시간 답은 한참 아래**.
- 뿌리: 번들 규제 문서의 **작성 지침/메타 텍스트가 그대로 색인**되어 청크로 검색·노출됨.
- 제안: 색인 시 메타/지침 섹션 제외(또는 랭킹에서 배제). 사용자에게 문서 편집 지침이 답으로 나가면 안 됨.

### #1 [경미·UX] 챗이 일부 how-to를 즉답 대신 비동기 리포트로 라우팅
- 증상: "프롬프트 인젝션 막으려면?" → 즉답 없이 "시간이 걸리는 작업이라 리포트로 작성해 드리겠습니다. 다 되면 알려드릴게요". (사실 질문은 즉답 정상.)
- 판단: 리포트 라우팅 자체는 설계이나, **라이트 퀵챗**에서 단순 how-to까지 리포트로 미루면 즉답을 기대한 고객이 당황. dispatch 라우팅 임계 재검토 권고.

## 🔧 결함 #4 [게이트 빨강] 코퍼스 누출 — 제품소개.md에 내부 로드맵 (2026-08-16 추가)

- 게이트: **`corpusleak.test` 빨강**(개발 문서 섞임 금지). win `2425d8e`가 `GIJO_AS_제품소개.md`(RAG 코퍼스=고객 답변 근거)의 전-4에 "✅ 완료(2026-08-17·버전 2026.07-1·Ed25519 서명·vitest 5파일)"를 넣어 발화.
- 실체: 제품소개.md에 **내부 로드맵 표(전-1~전-7) 통째** — 진행상태·**사업 결정·파일럿 확보·유상 구독화**·빌드 세부(vitest·Ed25519·정찰). 고객이 챗/제품 소개자료로 물으면 **그대로 인용돼 나감**(테스트 문구 그대로).
- 뿌리: 고객용 소개 + 내부 계획표를 **한 문서**에. 코퍼스엔 고객용만.
- win 수정: 코퍼스판 제품소개.md에서 **전-N 로드맵·진행표기·빌드 세부 제거**(또는 고객용/내부용 분리). **게이트 빨강 → 다음 빌드·게시 전 처리 필수.** (결함 #3 「법령 RAG 메타 노출」과 같은 코퍼스 위생 결.)

## 5.20.0 검증 요약 (max)
- ✅ 클라·서버 tsc · 인라인 52 · custom 제공자 **SSRF 방어**(링크로컬·169.254.169.254 메타데이터 거부) · cloud/egress 27 통과 · 서버 3,465 통과.
- 🔧 1 실패: 위 #4 corpusleak → win.

## 참고 (환경)
- 검증은 CDP(Chromium 직접 캡처·조작)로 함 — OS 스크린샷·osascript가 이 Mac에서 불안정해서. 실앱 실렌더 기준.
- 사장님이 올린 모델은 라이트 기준(10GB·7.6B)보다 큰 **14B** — 동작하나 티어 위. (참고, 결함 아님.)
