# GIJO AS — RAG 근거 오염 해결 계획

**작성 2026-08-10 (Windows 세션)** · **상태: 방향 확정, 구현 전** · **근거 실측: [[GIJO_AS_RAG_오염_실측_2026-08-10.md]] (Mac)**

> 사용자 결정(2026-08-10): **「방향 정리 후 다음에 제대로」** — 오늘은 진단·방향까지, 착수는 다음 세션.
> 즉흥 착수는 오늘 세 번 막은 「전제 틀린 큰 수리」가 되므로 하지 않는다.

---

## 0. 한 줄 판단

> **근본은 RAG 오염이다** — 타사·타사고객 문서(Tenable 매뉴얼·안전대부 보고서)가 우리 질문의
> 근거 후보를 **절반 넘게(53%)** 차지한다. 오늘 고친 근거 배지는 그 **증상 하나**였고,
> C-1(접점 재정렬)은 오염이 타사에 쏠려 **역효과**였다. 세 갈래로 나눠 **①ⓑ → ③ → ②** 순서로 닫는다.

---

## 1. 근본 — 하나의 뿌리, 여러 증상 (오늘 실측)

```
RAG 오염(타사·엉뚱 문서가 우리 질문에 낌, 152문항 중 73건=53%, 1위 28건=20%)
        │
        ├─ 근거 배지 둔갑 ……… ✅ 오늘 고침(d14bea7) — 증상 하나
        ├─ C-1 접점 재정렬 …… ⛔ 역효과(접점이 타사 73:우리 0) — 실측으로 착수 막음
        └─ 규정 질문 흔들림 … 🔴 퇴사자 계정 1년/2년/즉시 번갈아 — 파일럿 치명
```

**확정된 사실(짐작 아님 — 실측):**
- 오염 규모: **73건(53%)에 타사 낌, 28건(20%)은 타사가 1위**. 두 번 돌려 동일(결정적) — Mac.
- 접점 비대칭: **Tenable 73개 : 안전대부 14개 : GIJO 자체 매뉴얼 0개** — Windows 운영 실측.
- 규정 질문: 「퇴사자 계정」 10회가 **찾고(1위 0.8583)·붙이고(근거 4건)·쓰는데** 1년/2년/즉시 번갈아 — Mac.
  → **검색·선별·답무시가 아니라 「모델이 회차마다 다르게 쓴다」**가 원인(Mac 재현으로 진단 정정).

---

## 2. 세 갈래 + 게이트

### ①ⓑ — 오염 근본: 「우리 질문엔 우리 지식 우선」 (전-7 차별점 신뢰성)
- **표식 정의 = 수집 경로**(Mac 확정): 프로그램 수집(`docsbundle.ts:189` ingestText)으로 들어온 것이 「내장」.
  `docs-manifest.json` 19건이 전부 `GIJO_AS_*`, 타사 0건 — 경로가 곧 귀속이다.
  ⚠ `docClass`로 정의하지 **않는다** — origin은 기계가 아는 사실, docClass는 사람이 정하는 값. 섞으면 못 가린다.
- **넣는 쪽은 한 줄**: `docsbundle.ts:189`의 ingestText 호출에 `origin:'builtin'` 표식.
  단 **앞으로 들어오는 것부터** 구분되고, 기존 문서는 **소급이 따로 필요**하다.
- **검색에서 쓰는 법**: hybridSearch 뒤 **재정렬 신호**로. **거르지 말고 올리기만**(접점 없는 새 문서가
  통째로 안 보이면 안 된다 — Mac). ⚠ C-1(접점)이 아니라 **origin(내장 여부)** 신호다 — 접점은 타사에 쏠려 역효과였다.

### ③ — 배지 정확도: 「답이 읽은 문서」를 배지로 (후-6 정직 + 지연 완화)
- 지금 배지는 **「답이 읽은 문서」가 아니라 「비슷한 조건으로 다시 찾은 문서」**다(Mac 지적).
  두 검색의 인자가 다르다: `llm.ts:116` `queryMemoryGraded(msg,4,agentId,screen,viewer)`
  vs `dispatcher.ts:762` `queryMemoryScored(instr,4,**undefined**,screen)` — 함수도 agentId도 다르다.
- **완화안**: 답을 만든 쪽(agentloop/llm)이 읽은 문서를 `result.sources`에 **확정** →
  `computeOfferSignals`가 `sourcesAlreadyDecided`로 재검색을 **건너뛴다**. 지연(+3.2s)도 없고 근거도 정확해진다.
- ⚠ **성능이 아니라 정확도 수리다**(Mac). d14bea7이 「언제 찾을지」를 고쳤다면 이건 「무엇을 보여줄지」를 닫는다.
- ⚠ 권한 구멍 **없음** 확인됨 — `dispatcher.ts:564` runWithViewer 폴백이 채운다(Mac 확인).

### ② — 규정 질문 결정화: 숫자·조문은 원문에서 그대로 (전-4 + 후-6)
- 「모델이 회차마다 다르게 쓴다」가 원인이라 **라우팅·검색을 고쳐도 안 닫힌다**(Mac).
- 닫는 법: ⓐ **숫자·조문을 원문에서 그대로 복사**(지식그래프 계획 §4 원칙과 같은 자리) ·
  ⓑ 기간을 단정하지 말고 **원문 표를 보여준다**. 7B에 프롬프트로 「정확히 써라」는 금지(확립 원칙).

### 게이트 판정 — 낱말 → 상충 감지 (중-3)
- `ops-sim.mjs:251` 「퇴사자 계정」 판정이 `{ 포함: [/개인정보|정보통신망|보관|삭제/] }` **낱말 포함**이라
  1년·2년·즉시가 **전부 통과**한다. 「흔들림 0 회차」 게이트가 잡으려던 게 이건데 이 판정으로는 못 잡는다.
- 개선: **상충 감지** — 같은 질문 N회 답의 핵심 수치(기간)가 갈리면 실패. LLM 답 내용 판정이라 규정 질문 한정.

---

## 3. 우선순위·착수 순서 (Mac 순서 수용)

**①ⓑ → ③ → ②**
1. **①ⓑ** — ⓑ만 **결정적으로 측정**됐다(53%, 두 번 동일). 여기가 근본이자 착수점.
2. **③** — ⓑ 전후 비교의 **전제**다(배지가 정확해야 ⓑ 효과를 화면에서 잰다).
3. **②** — 성격이 바뀌었으니(모델 흔들림) **고칠 곳부터 다시 정하고** 붙는다.

⚠ ①ⓑ와 ③은 검색·답 생성 **공용 경로**(memory·dispatcher·agentloop·llm)라, **Mac과 통과 기준을
나란히 두고** 진행한다 — 한쪽 데이터로만 재면 못 가린다(Mac 복원본 접점 3개 vs 운영 편중).

---

## 4. 통과 기준 (Mac 실측 문서 §, 각 기계 값 각각)

- **ⓑ**: ① 「조치 이력」에서 Tenable이 GIJO 매뉴얼 **아래로** 내려간다 · ② 137건 중 **타사 1위 28건**이 유의미하게 준다.
- **③**: 배지 문서 = 답이 실제 인용한 문서(재검색 결과와 답 내용이 일치).
- **②**: 「퇴사자 계정」 10회의 기간이 **일관**되거나, 단정 대신 **원문 표**를 보인다.
- ⚠ 값은 문서 구성에 따라 다르다 — 운영과 Mac 복원본 값을 **각각** 남긴다.

---

## 5. 계획서(전중후) 관계

| 갈래 | 매핑 | 성격 |
|---|---|---|
| ①ⓑ 오염 근본 | **전-7**(차별점·신뢰성) | 우리 지식이 우리 질문에 우선 — 제품 핵심 |
| ③ 배지 정확도 | **후-6**(정직) | 근거가 답과 일치 |
| ② 규정 결정화 | **전-4 + 후-6** | 규정 질문에 흔들리지 않는 답 |
| 게이트 상충 감지 | **중-3**(평가 게이트) | 상충을 게이트가 잡음 |

> RAG 오염은 **파일럿 전 반드시** 풀어야 한다 — 「정확한 근거」가 제품의 핵심 약속이고,
> 규정 질문이 회차마다 다른 답을 내면 담당자가 규정을 잘못 정한다.

---

*근거: [[GIJO_AS_RAG_오염_실측_2026-08-10.md]](Mac 실측·결정적) · 오늘 Windows 운영 실측(접점 분포·게이트 재현) · Mac 회신 3건(2026-08-10).*
