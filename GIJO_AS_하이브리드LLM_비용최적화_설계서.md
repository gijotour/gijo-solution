# GIJO 지능형 하이브리드 LLM 아키텍처 — 개발 생산성 유지 + Claude 토큰 극소화

작성 2026-08-27 · 사장님 지시 「온프레미스 하드웨어와 Claude API를 결합해 개발 생산성을 유지하면서 API 토큰 비용을 극소화」
계획서(전중후) 관계: **개발 인프라라 계획서 항목 밖**이다. 다만 GB10 상시 역할(전-7 「운영을 멈추지 않고 A/B·레드팀을 돌리는 검증 기계」)의 연장이고, 제품의 클라우드 하이브리드(후속, 명시적 에스컬레이션·기본 OFF)와 같은 설계를 공유한다.

---

## 0. 결론 먼저 — 세 문장

1. **로컬 주력 모델은 Qwen3-Coder-30B-A3B를 추천한다** (사장님이 말씀하신 27b/32B급에서 제 판단). 이유는 GB10의 병목이 **대역폭(273GB/s)**이라서다 — dense 32B는 실측 10.6 tok/s로 대화가 고통스럽지만, MoE(활성 3.3B)는 활성 파라미터가 1/10이라 수 배 빠르다. SWE-bench Verified 50.3%로 로컬 코더 중 실질 1위.
2. **라우팅의 잣대는 「난이도」가 아니라 「검증 가능성」이다.** 로컬이 틀려도 기계(시험·빌드·grep)가 잡을 수 있으면 로컬 완결, 틀림을 사람이나 Claude만 잡을 수 있으면 Claude. 난이도 자가 추정은 로컬 모델이 제일 못하는 일이다.
3. ★ **목표 2(압축)와 목표 3(캐싱)은 서로 싸운다.** LLM이 만든 압축본은 매번 바이트가 달라서, 캐시 접두사에 넣는 순간 캐시를 매번 깨뜨린다. 해법은 층 분리다 — **결정적 산출물은 캐시 안에, LLM 압축본은 캐시 밖(맨 뒤)에.** 이 한 줄이 이 설계서의 핵심이다.

---

## 1. 전제 실측 (이미 잰 것 — 다시 재지 않는다)

| 항목 | 값 | 출처 |
|---|---|---|
| GB10 | NVIDIA DGX Spark, ARM CUDA, 통합메모리 121GB(이번 예산 94GB) | 2026-08-11 편입 |
| 대역폭 | **273GB/s** (3090은 936 — 3.1배 차) | 2026-08-12 실측 |
| dense 32B 속도 | **10.6 tok/s** (72B는 4.5) | qwen2.5 실측 |
| llama.cpp 빌드 | `-DGGML_CUDA=ON -DCMAKE_CUDA_ARCHITECTURES=121` | 확립 |
| 모델 이송 | win이 `git push gb10 main` + scp 시점복사 (RTT 70~106ms — 대용량 느림) | 확립 |
| ⚠ 측정 함정 | nvidia-smi 메모리 `[N/A]`(unifiedmem.ts 한 곳 판정) · RSS도 틀린 자 | 2026-08-13 실측 |
| ⚠ llama.cpp 캐시 | 90초 ping이 프롬프트 캐시를 밀어냄 → `--parallel 2` | 실사고 |
| 동기 실증 | **이번 주 병렬 검토 워크플로가 지출 한도로 3회 전원 사망** | 2026-08-23 |

Claude 쪽 정확한 숫자(2026-08 현재, 공식):

| 모델 | 입력 $/1M | 출력 $/1M | 캐시최소 |
|---|---|---|---|
| Fable 5 | $10 | $50 | 512tok |
| **Opus 5** | $5 | $25 | **512tok** |
| Sonnet 5 | $2 | $10 | 1024tok |
| Haiku 4.5 | $1 | $5 | 4096tok |

캐시: **읽기 0.1× · 쓰기 1.25×(5분 TTL) / 2×(1시간 TTL)** · 중단점 최대 4개 · 배치 API는 50%.

⚠ **정직한 구분 하나** — 지금 개발은 Claude Code **구독**으로 돈다(한도 메시지가 claude.ai/settings였다). 구독에는 「API 청구서」가 없고 **사용량 한도**가 있다. 그래서 이 설계의 효과는 두 갈래로 갈린다:
- **개발(Claude Code)**: 절약 수단은 「입력을 줄이는 것」뿐이다(로컬이 미리 소화한 것만 읽게). 캐싱은 Claude Code 하네스가 **이미 자동으로** 하고 있어 §5의 페이로드 설계를 우리가 손댈 자리가 없다.
- **제품·자체 스크립트(API 직접 호출)**: §4·§5가 그대로 적용된다 — cloudllm 경로와 우리가 짜는 자동화 스크립트.

---

## 2. 로컬 모델 선정 — 「너가 판단」에 대한 답

### 추천: 2모델 상주 (94GB 안에서)

| 역할 | 모델 | 크기(Q4) | 기대 속도 | 왜 |
|---|---|---|---|---|
| **주력(빠른 일꾼)** | **Qwen3-Coder-30B-A3B-Instruct** | ~18GB | **40~80 tok/s 기대** (실측 필수) | MoE 활성 3.3B → 대역폭 273GB/s에서도 빠름 · 256K 컨텍스트 · SWE-bench 50.3% |
| **보조(압축·정리 담당)** | GLM-4.5-Air 또는 gpt-oss-120b급 대형 MoE | ~60GB | 15~25 tok/s 기대 | 요약·발췌의 **정확도**가 곧 Claude 입력 품질 — 큰 모델이 덜 빠뜨린다 |
| 임베딩 | bge-m3 (기존) | ~2GB | — | 그대로 |

합계 ~80GB + KV캐시 여유 = 94GB 안. **단, 속도 숫자는 전부 「기대」다 — 도입 1일차에 gb10에서 실측하고, 기대와 2배 이상 어긋나면 이 표를 다시 쓴다** (우리 원칙: 재기 전에 「됐다」 금지).

### 왜 dense 32B가 아닌가
GB10에서 dense 32B는 **10.6 tok/s 실측**이다. 1,000토큰 답에 94초 — 대화형 개발 보조로는 못 쓴다. GB10의 값어치는 속도가 아니라 용량이라고 이미 결론냈는데(2026-08-12), MoE는 그 용량으로 속도까지 사는 길이다. llama-server 구동은 기존 localengine 관례 그대로(GGUF, `-ngl -1`).

---

## 3. 요청① — 동적 라우팅: 로컬 완결 vs Claude 에스컬레이션

### 설계 원칙 (이 저장소의 실측에서 나온 것)

- **잣대는 검증 가능성.** 로컬 산출물이 기계로 검증되면(시험·tsc·grep·빌드) 틀려도 싸다. 검증이 사람뿐이면 로컬의 오류율이 곧 사람의 시간이다.
- **모델의 자가 난이도 판정을 믿지 않는다.** 「이건 제가 할 수 있어요」는 로컬 7B~30B가 가장 못하는 판단이다(우리 실사고: 7B에 프롬프트로 행동 교정 불가 — 코드로 해결). 라우팅은 **작업 유형 표 + 기계 신호**로 한다.
- **검토관은 내리지 않는다.** CLAUDE.md 명문: 「검토관은 이미 opus다 — 모델을 내리면 그럴듯한데 틀린 지적이 늘어 검증 비용이 더 든다」. 검토·설계·보안 경계는 에스컬레이션 대상이 아니라 **Claude 고정**이다.

### 4단 라우팅 표

| 단 | 무엇 | 예 (우리 실제 업무) | Claude 토큰 |
|---|---|---|---|
| **L0 결정적 도구** (LLM 자체가 불필요) | grep·AST·tsc·vitest·의존 그래프 | 의존 지도(depmap2.mjs — **이미 실증**: 순환 판정을 LLM 없이 냈다)·routes-renumber·qa-full | **0** |
| **L1 로컬 완결** | 산출물이 기계 검증되거나 3초에 눈으로 확인되는 것 | 커밋 메시지 초안 · 로그/시험실패 1차 분류 · 문서 요약 · 회의록 정리 · RAG 질의 응대 · 보일러플레이트(시험 뼈대) · 한↔영 번역 | **0** |
| **L2 로컬 준비 → Claude 마무리** | 로컬이 읽고 발췌, Claude가 판단 | 대형 파일 소화(3,431줄 handlers.ts → 관련 함수만) · 실패 로그 300KB → 핵심 20줄 · 다중 파일 영향범위 1차 수색 | 입력 5~20× 절감 |
| **L3 Claude 직행** | 틀리면 비싸고 기계가 못 잡는 것 | 설계 결정 · 검토관(병렬 5갈래) · 보안 경계 코드 · 다중 파일 리팩터 · 「왜」를 묻는 원인 분석 | 정가 (여기 아끼려다 재작업이 더 비쌈) |

### 판별 로직 (코드로 — 프롬프트가 아니라)

```
① 유형 표 우선 — 위 표를 설정 파일(routing.json)로 두고 작업 유형이 매칭되면 그대로.
   (제품 dispatcher의 FORCED_INTENTS와 같은 사상 — 규칙 먼저, 모델은 나중)
② L1 산출물엔 자가 검증을 강제 —
   커밋 메시지: git diff와 대조해 언급 파일이 실재하나 grep
   시험 뼈대: 그 자리에서 vitest 1회 실행
   요약: 원문에 없는 고유명사가 생겼나 대조(환각 검사 — 20자 겹침 실측 방식 재사용)
③ 2진 아웃 승격 — L1이 검증 실패를 2회 반복하면 자동으로 L2/L3 승격 + 그 유형을 기록.
   기록이 쌓이면 표를 고친다(학습 라우터는 데이터 300건 뒤 — 어댑터 개시선과 같은 논리).
④ 승격은 명시적으로 — 무엇이 왜 올라갔는지 한 줄 로그. 조용한 승격은 비용 추적을 죽인다.
```

---

## 4. 요청② — 컨텍스트 압축 파이프라인 (llama.cpp → Claude)

### ★ 먼저, 이 파이프라인의 제1 함정

**LLM 압축본은 같은 입력에도 매번 다른 바이트가 나온다.** 그걸 Claude 요청의 앞쪽(캐시 접두사)에 넣으면 **캐시가 매 요청 깨진다** — 압축으로 아낀 것보다 캐시 무효화로 잃는 게 클 수 있다. 그래서 파이프라인을 **결정성 기준으로 3층** 나눈다:

```
층A  결정적 (도구가 생성 — 같은 입력=같은 바이트)          → 캐시 접두사 안 ✅
     · AST 지도: tree-sitter/tsc로 export 시그니처·타입만 추출
     · 의존 그래프: depgraph.json (이미 있다)
     · 저장소 지도: 파일 목록+한줄 설명 (야간 생성, 커밋 해시가 버전)
층B  준정적 (LLM 요약 + 해시 캐시 — 파일이 안 바뀌면 같은 바이트)  → 캐시 접두사 안 ✅
     · 파일별 요약: 키=파일 sha256, 값=로컬 LLM 요약. 해시 같으면 재생성 안 함
       (docsbundle의 해시 비교 관례 그대로 — 새 발명 아님)
     · llama.cpp의 json_schema/GBNF로 출력 구조 강제 → 형식 흔들림 제거
층C  휘발 (이번 질문 전용)                                  → 캐시 밖, 맨 뒤 ✅
     · 질문 관련 발췌: 로컬 LLM이 고르되 **원문 그대로 인용** (요약·재서술 금지)
     · 실패 로그 핵심 N줄, RAG 상위 조각
```

### 층C의 원칙 — 「선별하되 다시 쓰지 않는다」

우리 제품 설계에서 이미 확정한 원칙을 그대로 쓴다: **자동 추출은 값이 아니라 인용까지만**(제품비교 설계에서 확립 — 재서술은 「도입형태=SaaS」식 단정 오류를 낳는다). 개발 컨텍스트에선 더 절실하다 — 이 저장소의 값어치는 **실사고 주석 1,711건**인데, 로컬 모델이 「요약」하면서 그 주석을 일반론으로 뭉개면 Claude는 함정을 모른 채 코드를 고친다. 그래서:

- 로컬 모델의 일은 **어느 줄을 보낼지 고르는 것**(줄 번호 선택). 보내는 내용은 원문 그대로.
- 압축률 목표: 파일 3,000줄 → 관련 원문 150~300줄 (10~20×). LLMLingua 계열 연구가 20× 압축에 품질 손실 2% 미만을 보고하지만, 그건 QA 벤치마크다 — **코드+실사고 주석에서는 우리가 직접 재야 한다**(도입 게이트 §7).
- 구조는 llama.cpp `json_schema`로 강제: `{files:[{path, reason, line_ranges:[[a,b],...]}]}` → 파이프라인이 그 범위의 원문을 기계로 잘라 붙인다. **모델이 본문을 생성할 기회 자체를 없앤다.**

### 배관 (기존 자산 재사용)

```
[요청] → 라우터(§3)
  L2면: gb10 llama-server(주력 30B-A3B)에 질문+파일 목록
        → json_schema 출력(줄 범위) → 기계가 원문 절단 → 층C 완성
  개발(Claude Code)에서의 소비: tools/local-digest.mjs CLI 한 개
        — 메인 에이전트(나)가 대형 파일을 직접 Read하는 대신 이 CLI를 불러 발췌만 받는다
  제품/스크립트에서의 소비: §5 페이로드의 층C 자리에 삽입
야간(예약): 층A 재생성 + 층B 해시 갱신 — gb10이 논다(상시 역할과 정합)
```

---

## 5. 요청③ — Claude API 페이로드 구성 (캐싱 극대화)

**적용처: 우리가 직접 짜는 API 호출**(제품 cloudllm 경로 · 자동화 스크립트). Claude Code 세션은 하네스가 캐싱을 자동 관리하므로 여기 손댈 것이 없다.

### 페이로드 배치도 (중단점 4개를 이렇게 쓴다)

```
[tools 정의]  ──────────────── 결정적으로 직렬화(이름순 정렬) — 절대 요청마다 바꾸지 않음
[system: 역할+규칙 (동결)]     ← 중단점① {type:"ephemeral", ttl:"1h"}   ...세션 내내 불변
[층A: 저장소 지도+AST]         ← 중단점②                                ...커밋 단위로만 변경
[층B: 관련 파일 요약 묶음]      ← 중단점③                                ...파일 해시 단위로만 변경
[대화 이력]                    ← 중단점④ 최근 턴의 마지막 블록            ...턴마다 자람
[층C: 이번 질문 발췌 + 질문]    ← 중단점 없음 (캐시 밖)                    ...매번 다름 — 여기가 유일한 정가 구간
```

### 지켜야 할 규칙 (공식 문서 확인분)

- **동결 원칙**: system에 날짜·세션ID·모드 플래그를 **절대 삽입하지 않는다** — 한 바이트가 바뀌면 그 뒤 전부 무효. 모드 전환은 messages 뒤쪽에 `{"role":"system", ...}` 메시지로(Opus 5는 베타 헤더 없이 지원 — 캐시 접두사를 안 건드리는 공식 통로).
- **모델·tools를 대화 중 바꾸지 않는다** — 캐시는 모델 단위, tools는 위치 0이라 전체 재구축.
- **TTL 선택**: 연속 작업 세션이면 1h(쓰기 2×지만 세션당 1회), 단발 스크립트면 5분(쓰기 1.25×, 2회면 본전).
- **검증은 usage로**: `cache_read_input_tokens`가 반복 요청에서 0이면 어딘가 조용한 무효화가 있다 — 두 요청의 렌더된 바이트를 diff.
- **긴 에이전트 턴**: 한 턴에 블록 20개 넘게 쌓이면 중단점이 이전 캐시를 못 찾는다(20블록 룩백) — 긴 턴엔 ~15블록마다 중간 중단점.

### ★ 병렬 워크플로에 바로 먹히는 것 — 프리워밍

공식 문서의 함정: **같은 접두사의 N개 병렬 요청은 전부 정가를 낸다**(첫 응답이 스트리밍을 시작해야 캐시가 읽힌다). 우리 병렬 검토관(5갈래 동시)이 정확히 이 모양이다. 수리법: **공통 접두사로 `max_tokens: 0` 프리워밍 요청 1발 → 응답 오면 5갈래 발사.** 워밍 1회 쓰기(1.25×) + 5회 읽기(0.1×씩) vs 지금 5회 정가 — 접두사가 클수록 이득이 커진다.

### 비용 셈 예시 (Opus 5, L2 경로 1회)

| | 지금 (원문 통째) | 하이브리드 |
|---|---|---|
| 입력 | 원문 60K tok 정가 = $0.30 | 접두사 20K 캐시읽기($0.01) + 층C 4K 정가($0.02) = **$0.03** |
| 출력 | 3K = $0.075 | 3K = $0.075 (동일 — 출력은 압축 못 함) |
| 합 | $0.375 | **$0.105 (−72%)** |

출력이 절반을 차지하게 되므로, 입력 최적화 뒤의 다음 지렛대는 **에스컬레이션 횟수 자체를 줄이는 것**(§3의 L0·L1)이다. 급하지 않은 배치성 작업은 배치 API로 추가 50%.

---

## 6. 구현 지점 4곳 (새 발명 최소화)

| # | 무엇 | 어디에 | 크기 |
|---|---|---|---|
| ⑴ | `tools/local-digest.mjs` — 파일/로그를 gb10에 보내 줄범위 발췌를 받는 CLI | tools/ 신설 | 소 |
| ⑵ | 야간 층A·층B 생성 루틴 | gb10 (예약 루틴 관례) | 소 |
| ⑶ | 라우팅 표 `routing.json` + 2진 아웃 기록 | tools/ | 소 |
| ⑷ | cloudllm 경로에 §5 페이로드 적용 (제품 — 기본 OFF 그대로) | server/src/engine/cloudllm | 중 |

---

## 7. 도입 단계 — 측정 게이트 (재기 전에 「아꼈다」 금지)

| 주 | 무엇 | 통과 기준 |
|---|---|---|
| 1 | gb10에 30B-A3B 올리고 **속도·품질 실측** (tok/s, 발췌 정확도 20문항) | tok/s ≥ 30 · 발췌가 정답 줄을 포함 ≥ 18/20 |
| 2 | ⑴⑵⑶ 구현 + **일주일 병행 측정** — 같은 작업을 지금 방식/하이브리드로 | Claude 입력 토큰 −50% 이상 · **재작업 횟수 증가 0** |
| 3 | 통과 시 상시화 + ⑷ 착수. 미달 시 그 단만 회수 | — |

**「재작업 횟수 증가 0」이 진짜 게이트다.** 토큰을 반으로 줄여도 발췌가 실사고 주석을 빠뜨려 내가 함정을 두 번 밟으면 총비용은 늘어난 것이다 — 이 저장소에서 시간을 먹는 건 타이핑이 아니라 검증이라는 게 확립된 실측이다.

## 8. 위험 — 정직하게

- **압축 환각**: 층C를 「인용만」으로 제한해 구조적으로 막았지만, **선별 누락**(중요 줄을 안 고름)은 남는다. 2주차 병행 측정이 이걸 잰다.
- **로컬 품질 과신**: L1 확장은 2진 아웃 기록을 보고 천천히. 검토관·설계는 영구 Claude.
- **gb10 단일 장애점**: gb10이 죽으면 라우터는 **전부 Claude로 폴백**(조용한 실패 금지 — 폴백 발생을 로그).
- **구독 vs API**: §5의 달러 계산은 API 경로 얘기다. Claude Code(구독) 쪽 효과는 한도 소진 속도 완화로 나타나며, 그 측정은 「주간 한도 도달까지 작업량」으로 잰다.

---

## 📖 용어 풀이

- **MoE(전문가 혼합)** — 모델 전체는 크지만 토큰마다 일부(전문가)만 계산하는 구조. 메모리는 많이 쓰고 속도는 작은 모델처럼 나온다 — 대역폭이 약한 GB10에 딱 맞는 성질
- **컨텍스트 증류(압축)** — 큰 원문에서 답에 필요한 부분만 추려 모델 입력을 줄이는 것
- **프롬프트 캐싱** — 요청의 앞부분(접두사)이 지난 요청과 완전히 같으면 그 부분을 90% 할인된 값으로 재사용하는 Claude API 기능. **한 바이트만 달라도** 그 지점부터 무효
- **프리워밍** — 본 요청 전에 접두사만 미리 보내 캐시를 만들어 두는 것 (`max_tokens: 0`)
- **GBNF/json_schema** — llama.cpp가 출력을 정해진 형식으로만 내게 강제하는 장치. 모델이 딴소리할 자유를 문법 수준에서 없앤다
- **2진 아웃(two-strike)** — 같은 유형에서 두 번 실패하면 자동으로 위 단계로 올리는 규칙
