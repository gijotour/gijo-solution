# [인계] max → win — 라이트 메뉴 재편 설계 (사장님 결정, 2026-08-16)

사장님과 고민해 라이트 메뉴를 단순화·정합하기로 했다. 결정 3건을 한 재편으로 묶는다. 구현은 win.

## 결정

### ① 나만의 기억 + 나만의 메모장 → 하나로 통합 ("나만의 자료")
- 근거(코드): **둘 다 `ingestMemoryFile`로 같은 지식창고에 넣는다** — 별도 데이터가 아니라 **입구만 다르다**.
  - 기억: 파일 하나씩(`ingestMemoryFile`) + 찾기(`queryMemory`) + 정리본(`g.chat` LLM 위키) + 목록
  - 메모장: **폴더 연결**(`pickWorkFolder`)→`.md` 통째로 읽어 넣기(`listDir`·`readFile`·`ingestMemoryFile`) + Smart MD 연동
- 통합 화면: **넣기(① 파일 ② 폴더 연결=Smart MD 흐름) · 찾기 · 정리본(위키) · 목록**.
- lite-screens.json에서 `lite-memo`(no=9) 제거, `lite-memory`를 "나만의 자료"로. 메모장의 폴더연결·Smart MD 연동 값은 통합 화면에 흡수.

### ② Smart MD Studio를 나비 메뉴 항목으로 (표준과 동형)
- 표준 nav.js: `{ win:"openSmartMd", label:"문서 작성 (창) · 무료", icon:"book", fixed:true }` (line 81).
- 라이트는 현재 제목줄 버튼(`lite-app.html #smdBtn`)뿐 — **나비 메뉴로 이관**(제목줄 버튼은 이관 또는 병존, 사장님 선택).
- ⚠ **구현 핵심**: 라이트 나비(lite-app.html)는 지금 lite-screens.json의 **iframe 화면만** 렌더한다. Smart MD는 **별도 창(`openSmartMd`)**이라, 표준 nav.js(line 1300 `page.win`)처럼 **창-실행 항목 타입**을 라이트 나비에 지원해야 한다(클릭 시 iframe 로드 대신 `window.gijo.openSmartMd()` 호출).
- 규약: 라벨 끝 **「(창)」**(menu-sweep이 그 표기로 창 항목을 건너뜀 — navwiring 시험) · **「· 무료」는 (창) 뒤에** · 미동봉 판 fallback("이 판엔 문서 작성 도구가 없습니다") 유지.

### ③ "물어보기" → "찾기" 개명 (사장님 확정 2026-08-16 「추천으로 진행」)
- 각 화면의 "물어보기"는 `queryMemory` **검색**(원문 조각 표시, 모델 불필요)이고, "AI에게 물어보기"는 `sendInstruction` **답 생성**(모델 필요)이다 — **다른 기능인데 이름이 같아 헷갈린다**.
- 권고: 검색형은 **「찾기/검색」**으로, "물어보기"는 AI 챗 전용. (검색은 모델 등록 전 유일한 질의 수단이라 유지.)
- 사장님 확정 시 적용.

## 제안 메뉴 (결과)
💬 AI에게 물어보기 · 1 대시보드 · 2 작업 내역 · 3 로그파일 분석 · 4 스캐너 · 5 보안 장비 등록부 · 6 법령 판례 조회 · 7 제품 소개자료 · 8 **나만의 자료**(통합) · 📝 **문서 작성 (창)**(Smart MD) · ⚙ 설정

## ④ 대시보드 재설계 — 메뉴 중복 최소화 + 「내 지식창고」 시각화 (사장님 방향)

- **빼거나 최소화**: 현재 타일(내 할 일·내 문서·작업 내역)은 **좌측 메뉴 바로가기 중복**. 1인용은 메뉴를 직접 쓰니 값이 낮다. "아직 안 끝낸 일"도 메뉴 2와 겹침.
- **남길 것**: **「지금 돌고 있나」**(모델 상태 = 첫날 관문). ⚠ **단 실데이터 결함 #2(거짓 「멈춰 있음」) 수리 후.**
- **새로 넣을 것 — 「내 지식창고」 시각화**(얼마나 쌓였나 한눈):
  - 문서·조각: 총 문서 N(제품 M / 내가 넣은 K) · **검색 조각(청크) C개** — `memory_documents.chunks` 합
  - **분류별 막대**: `category`(장비운영·법령·제품자료·나만의 기억…)별 문서/조각 — **로그 화면 「한눈에 보기」 막대 패턴 재사용**
  - 정리본(LLM 위키): 항목 수 · 마지막 갱신
  - 지식 그래프: 온톨로지 노드·관계 수(atlas/attack + 별칭 333주어)
  - 임베딩 모델 · 마지막 반입 시각
- 데이터 소스: `knowledgeBundleStatus`·`listMemoryDocuments`(chunks·category·ingestedAt)·온톨로지(트리플 수). 전부 있음.
- 값: **①나만의 자료 통합**과 맞물려 라이트 축이 「넣기 → 쌓임 시각화 → 물어보기」로 또렷해진다.

## ⑤ 신규 메뉴 — 나만의 연락처 (테리토리별·제품별 담당자) (사장님 요청)

- **목적**: IT/보안 업무에서 **테리토리별·보안 제품별 담당자**를 한눈에. 사고·점검 때 "누구한테?"를 즉시. `보안 장비 등록부`(무엇·어디)의 짝(누구).
- **두 축**: ① 테리토리별(본사망·지사·DMZ·클라우드…) 그 구역 담당 ② 제품별(방화벽·EDR·백신·NAC…) **사내 담당 + 벤더/총판**.
- **항목**: 이름 · 역할(사내/벤더/총판) · 담당 테리토리 · 담당 제품 · 전화 · 이메일 · 메모.
- **시각화 = 커버리지 매트릭스**(사장님 「시각화로」): **테리토리(행) × 제품(열) → 담당자(칸)** 한 판. **빈 칸(담당 없음) = 강조**(색·경고) → "DMZ 방화벽 담당 누구?" 공백이 **커버리지 구멍**으로 한눈에(보안 인사이트). 색: 담당 있음(초록)/벤더만(주황)/공백(빨강·회색) — **로그 화면 심각도·히트맵 그리드 패턴 재사용**. 칸 클릭 → 담당자 상세 + 직접 연락. 아래에 담당자 목록(상세·편집).
- **장비 등록부 연동(권장)**: 장비에 담당자 연결(선택) → 장비 [열기] 옆에 담당자·연락 바로. (장비=무엇/어디, 연락처=누구)
- **직접 연락**: `mailto:`·`tel:` — `openExternal` 재사용하되 **허용 스킴에 mailto·tel 추가**(win) 아니면 복사.
- **저장**: localStorage(이 PC 전용, 장비 등록부와 동형). ⚠ **전화·이메일 = 개인정보** — 이 PC를 벗어나지 않음·백업 안 됨 명시(장비 등록부 "비번 저장 안 함" 경고와 같은 결). 서버 저장은 스탠다드 후보.
- 메뉴 순증: ①통합으로 자리 하나 해소돼 부담 작음.

## ⑥ Smart MD 실업무 템플릿 (사장님 요청 · 크로스 레포)

- 목적: 빈 편집기 대신 **바로 쓰는 보안 실무 문서**로 Smart MD 첫날 값. 캡처 붙여넣기 증적이 강점.
- **초안 3종 작성 완료** → `smartmd-templates/` (사고대응 보고서·보안점검 기록·인수인계 문서). 형식·문구 기준.
- ⚠ **어디에**: Smart MD는 별도 제품(`gijo-smart-md-studio`, 이미 템플릿 기능 有) → **그 레포의 「보안 실업무」 카테고리에 반영**(win 조율/Smart MD 관리자). GIJO AS가 아님.
- 지식 루프: 각 템플릿에 "저장 → 나만의 자료 폴더 → AI 챗이 읽음" 표기 넣음 → ①④와 맞물려 「쓰기→쌓임→물어보기」 완성.
- 안전: 비번·키는 문서에 안 적게 명시 · 규제 숫자는 "법령 조회에서 원문 확인" 안내(지어내기 방지).
- 더 넣을 후보: 취약점 조치 계획·회의록·월간 리포트·규정 준수 확인서·위험 평가서.

## ⑦ 두뇌(모델) 선택 — 라이트에 OpenAI 호환 추가 (사장님 시나리오: OpenAI로 시작 → 자체 LLM)

- **현 상태 확인**: 라이트 「어디서 돌릴지」 2택(💻 이 PC · 🖧 원격 GPU)뿐. **OpenAI 호환 API 선택지 없음**(라이트 0 · 표준 21). 서버엔 있음(cloudllm custom = 임의 OpenAI 호환, 사내 vLLM·GB10·키 없이, egress 게이트·내부정보 시 로컬 폴백).
- **재설계(두 축 + 전환)**:
  - 축1 **자체 LLM**(폐쇄·데이터 안 나감): 💻 이 PC(10GB) ↔ 🖧 원격 GPU(**이 PC 부족 시** 사내 큰 GPU — 로컬의 확장, 동급 3번째 아님).
  - 축2 **OpenAI 호환 API**(라이트 신규): 🏢 사내 온프렘(vLLM·다른 GIJO, 키 없이 = **사내 유지·폐쇄망 OK**) / ☁ 공개 OpenAI(빠른 시작·외부·게이트).
  - **전환 한자리**, **나만의 기억 유지**(로컬 bge-m3 임베딩이라 챗 제공자와 무관 — 코드 확인). 각 카드에 **데이터 어디로**(안 나감/사내/외부) 명시.

## ⑧ 「바깥 연결」 — 오픈망↔폐쇄망을 고객이 선택 (사장님 전략)

- **전략**: 오픈망·폐쇄망을 **선택적으로 활용, 고객이 선택.** 구조는 이미 있음(airgap 단일 관문 + **10개 egress 통로** 각각 오프라인 대체, 봉인 안 하면 전부 기본 OFF).
- **문제**: 선택이 **흩어져 있음**(클라우드 LLM·법령·원격 GPU 따로) → "지금 오픈이냐 폐쇄냐, 뭐가 바깥과 통하냐" 한눈에 못 봄.
- **⑧ 화면 신설 — 「바깥 연결」 on/off 통합 + 시각화**:
  - **10개 통로를 한 판 on/off**(사장님 지시): 클라우드 LLM·법제처 법령·CISA KEV·CTI·SMTP·외부 레드팀 등. 켜면 어디로 / 끄면 사내 대체 표시.
  - **두뇌 선택(⑦)이 머리** — 가장 큰 통로.
  - **시각화**: "이 PC를 벗어나는 것" — 켜진 통로만 바깥 화살표, 나머지 사내 대체(목업: `external_connection_selector`).
  - **하드 봉인(완전 폐쇄망)은 이 화면 밖** — **향후 라이선스 정책 연계**(사장님). 침해 admin이 못 풀게(배치/라이선스 고정). 기밀·금융·방산용.
- 값: 흩어진 선택을 한 화면에 모으고 시각화 → 사장님 전략(오픈/폐쇄 선택적, 고객 선택)이 완성. ⑦과 한 몸.

## 참고
- 함께 넘긴 실데이터 검증 결함 4건: `인계_max_라이트_실데이터검증_2026-08-16.md` (#2 대시보드 모델상태 거짓은 이 대시보드 재설계 때 함께 수리).
- 이 재편은 라이트를 **단순·정합**하게 만드는 방향(외부 UX 원칙과 일치 — 라이트는 메뉴 수·중복 최소).
