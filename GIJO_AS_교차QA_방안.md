# GIJO AS — 교차 QA 방안 (`max` ↔ `win`)

> **2026-08-12 신설.** 서로의 클라이언트로 상대 서버에 붙어 QA한다. 사장님 지시.
> 기계 이름은 `win`·`max`·`gb10` — `CLAUDE.md` 「기계 이름」 절.

## 0. 왜 교차로 재나

혼자 재면 **자기 환경만** 잰다. 오늘 하루에만 그 사고가 여러 건이었다:

- **환경이 다르다.** `max`는 Metal·arm64, `win` 운영은 WSL·x86. 같은 질문에 답이 갈리면
  그게 환경 의존 결함이다. 한쪽에서만 재면 「통과」로 읽힌다.
- **데이터가 다르다.** `max`의 지식 저장소는 **시점 복사본**이다(1,547조각 ↔ `win` 1,526).
  운영에만 있는 문서에서 나는 문제는 `max`에서 원리상 못 잡는다.
- **클라-서버 조합이 다르다.** `win` 클라(exe)↔`max` 서버(Metal), `max` 클라(dmg)↔`win` 서버(WSL).
  클라이언트가 서버 특성에 몰래 기대고 있으면 교차했을 때만 깨진다.
- 실사고: 살균 오탐을 `max`가 3문서 4조각으로 보고했는데, `win`이 운영에서 재보니
  **규칙 하나가 문서 전체를 사정권에 두고 있었다**(2026-08-12, `6c2876a`).

## 1. 두 방향 — 권한이 **일부러** 다르다

| 방향 | 붙는 곳 | 계정 | 할 수 있는 것 |
|---|---|---|---|
| `max` 클라 → **`win` 운영** | `http://10.8.0.1:4000` | `claude-qa` | **읽기만** |
| `win` 클라 → **`max` 개발** | `http://10.8.0.11:4000` | `claude-qa` | 읽기·쓰기 자유 |

**왜 비대칭인가:** `win`의 4000은 **운영**이다 — 실데이터가 있고 사장님이 실제로 쓴다.
`max`의 4000은 개발 서버라 깨져도 다시 만들면 된다. 같은 규칙으로 묶으면 둘 중 하나가 틀린다.

### 운영에 붙을 때 지킬 것 (`max` → `win`)
- **읽기만.** dispatch·설정 변경·모델 배정·문서 인입 **금지**.
- 잰 값에는 **「`max` 세션이 운영에서 쟀음」**을 반드시 적는다 — 나중에 출처를 못 찾으면 못 쓴다.
- **재기 전에 무엇을 재는지 알린다.** 운영은 사장님이 보고 있는 화면이다.
- 원본 백업 폴더(`data.before-migrate-*`)는 손대지 않는다.

## 2. 계정 — 서버마다 따로 만든다

**사용자 DB는 기계마다 별개다.** `max` 앱에서 만든 계정은 `max`에만 생긴다.
2026-08-12에 이걸로 한 번 헤맸다 — 키체인에 비밀번호만 넣고 계정은 안 만든 상태로 401을 봤다.

> ⚠ **키체인에 넣는 것 ≠ 계정 만들기.** `security add-generic-password`는 비밀번호를
> 그 기계 금고에 **보관만** 한다. 계정은 **앱 화면**에서 만든다.

각 서버의 앱에서 관리자로 `설정 → 관리자 → 계정 관리 → 사용자 추가`:

| 칸 | 값 |
|---|---|
| 아이디 | `claude-qa` |
| 이름 | `Claude QA` |
| 역할 | **담당자**(`security_officer`) — 관리자로 주지 않는다 |
| 비밀번호 | 12자 이상 |

**만든 직후 열람 등급을 올린다.** 기본값은 비어 있고, 비면 「공개만」으로 읽는다
(`server/src/auth/users.ts:22`). 그 상태로 재면 **민감·기밀 문서가 안 보여서**
「문서가 잘렸다」로 잘못 보고한다 — 살균 오탐과 구분이 안 된다.
올리지 않기로 했다면 **어느 등급인지 QA 보고에 적는다.**

소속 팀은 비워도 된다(자산 접근·조치 검증을 안 쓴다면).

### 세션은 계정당 1개
계정을 나누는 진짜 이유가 이것이다. 같은 계정으로 두 곳에서 붙으면 **서로 끊는다.**
`claude-qa`는 QA 세션 전용 — 사장님 계정(`jyh`)과 섞지 않는다.

## 3. 붙는 법 — 코드 고칠 것 없다

로그인 화면의 **「서버 주소」 칸**에 상대 주소를 넣고 로그인한다(`login.html:135`).
최근 접속은 **서버+아이디 조합으로** 기억하고, 비밀번호는 OS 키체인에 넣는다
(macOS `safeStorage` / Windows DPAPI — `client/src/main.ts:41`). 평문 저장이 아니다.

```
max 클라에서 →  http://10.8.0.1:4000     (win 운영)
win 클라에서 →  http://10.8.0.11:4000    (max 개발)
```

**API로만 잴 때**는 비밀번호를 스크립트에 적지 않는다(`no-hardcoded-credentials` 시험이 막는다).
`max`에서는 키체인에서 꺼내 쓴다:
```bash
export P="$(security find-generic-password -a gijo-qa -s gijo-ops-claude-qa -w)"
```
⚠ `security add-generic-password`에 **`-U`를 붙이지 않는다** — 같은 이름의 기존 항목을
말없이 덮어쓴다. 2026-08-12에 이걸로 `jyh` 비밀번호를 날릴 뻔했다.

## 4. 그물망 (2026-08-12 실측)

| 경로 | 결과 |
|---|---|
| `max` → `10.8.0.1:4000` | HTTP 200 |
| `win` → `10.8.0.11:4000` | HTTP 200 · 왕복 5ms · 손실 0% |
| `max` → 자기 `10.8.0.11:4000` | **안 됨**(HTTP 000) |
| `gb10` `10.8.0.12` 4000·22 | 닫힘 — 아직 준비 안 됨 |

> `max`가 **자기 자신의** VPN 주소로 못 붙는 건 macOS WireGuard utun 특성이다
> (경로가 `LOCAL`로 잡힌다). 남에게서는 멀쩡하다. **이걸 「서버가 안 열렸다」로 읽지 말 것** —
> 자기 기계에서는 `localhost:4000`으로, 남의 기계 확인은 **그 기계에서** 재야 한다.

ICMP는 `win`이 막고 있어 `max`→`win` ping은 실패한다. **TCP는 된다** — ping 실패를
「연결 안 됨」으로 읽지 말 것.

## 5. 교차로 잴 만한 것

환경·데이터가 갈려야 값이 있다. 한쪽에서만 나오는 답을 노린다.

| 볼 것 | 왜 교차여야 하나 |
|---|---|
| 같은 질문에 답이 갈리는가 | Metal ↔ WSL/CUDA. 갈리면 환경 의존이다 |
| 운영에만 있는 문서의 검색·살균 | `max` 저장소는 시점 복사본이라 원리상 못 잡는다 |
| 클라 화면이 상대 서버에서 깨지는가 | 클라가 서버 특성에 기대고 있으면 여기서 드러난다 |
| 응답 시간 | 같은 질문의 체감차 — 티어 표의 근거가 된다 |

## 6. 측정 전 세 줄 확인 (교차에서 더 중요하다)

① **코드가 최신인가** — 빌드했나가 아니라 **재기동했나**.
② **겨눈 대상이 맞는가** — 포트·기계·적재된 모델. 교차에서는 **어느 서버에 붙어 있는지**가 여기 들어간다.
③ **잰 건수가 기대와 같은가** — 0건·미달이면 통과가 아니라 **중단**이다.

교차 QA에서 ②가 특히 잘 틀린다. 화면이 똑같이 생겼기 때문이다.
**보고서 첫 줄에 「어느 클라 → 어느 서버, 어느 계정」을 적는다.**
