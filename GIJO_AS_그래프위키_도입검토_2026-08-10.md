# Graphify · LLM Wiki 도입 검토 — 결정안 (2026-08-10)

**요청**: 사장님 NotebookLM 자료(「Gemini NotebookRAG vs Graphify vs LLM Wiki: 제품
어시스턴트 도입 의사결정 프레임워크」) 기반 도입 고민 · **계획서**: 차별점(온톨로지)·전-1·전-7·후-3 강화
⚠ NotebookLM 본문은 구글 로그인 뒤라 **제목만 확인**했다 — 본문을 붙여 주시면 대조해 반영한다.

---

## 0. 한 줄 결론

> **도구(Graphify)를 통째로 들이지 않고, 패턴을 두 슬라이스로 흡수하기를 추천한다.**
> 이유: 우리는 이미 그 구조의 **70%를 갖고 있고**, 나머지 30% 중 절반은 우리 정직 원칙과
> 충돌한다. 없는 것만 작게 만든다.

---

## 1. 두 개념이 무엇인가 (조사 근거)

**LLM Wiki** (Karpathy, 2026-04 Gist): 질문마다 원문을 검색(RAG)하지 말고, LLM이 원문을
**한 번 컴파일해** 상호 링크된 위키(요약 페이지·색인·모순 추적)로 만들어 두고 그 위키를
조회하자는 패턴. 구조는 raw/(원본 불변) + wiki/(LLM 생성) + 스키마, 작업은 ingest·query·lint.

**Graphify** (2026-04, Apache 2.0 오픈소스): 같은 발상의 **지식 그래프** 구현. 폴더를 넣으면
유형 있는 노드·가중치 간선·신뢰도 점수의 그래프를 만든다. 코드에는 AST(결정적, LLM 무사용),
문서에는 LLM 추출. 온디바이스. 질의당 토큰 ~1.7k vs 통짜 읽기 ~123k(71배 절약 주장).

---

## 2. 우리가 이미 가진 것 (오늘 소스에서 실측)

| Graphify/Wiki의 요소 | 우리 것 | 상태 |
|---|---|---|
| 지식 그래프(노드·간선) | **온톨로지 2,186 트리플**(subject–predicate–object, 6대 표준 큐레이션) | ✅ 있음 — 단 **표준 지식**이지 고객 문서에서 자라진 않음 |
| 문서↔그래프 연결 | **docdigest.matches** — 반입 때 온톨로지 접점을 결정적으로 저장 | ⚠ 있는데 **글줄(TEXT)**이라 따라가며 조회를 못 함 |
| 답에 그래프 주입 | `ontologyLinesFor()` — 자산 상세에 위협·완화통제 연계 | ✅ 있음 |
| lint(모순·중복·신선도) | **kbhygiene** — 주 1회 상충·중복·오래됨 점검(삭제 없이 리포트) | ✅ 있음 — Wiki의 lint와 같은 개념 |
| 컴파일된 페이지 | **productfaq 카드**(결정적 즉답) · **get_asset**(자산 종합 페이지 조립) · 용어사전 | ✅ 있음 — 단 **사람/코드가 조립**, LLM 컴파일 아님 |
| 그래프 시각화 | 보안 지형도 · 공격경로 · 4소스 상관 | ✅ 있음 |

즉 질문은 「도입하느냐」가 아니라 **「없는 30%를 무엇으로 채우느냐」**다. 없는 것:
① 문서↔그래프 간선이 조회 가능한 구조가 아님 ② 고객 문서에서 그래프가 자라지 않음
③ LLM이 위키를 컴파일·유지하지 않음.

---

## 3. 왜 통째 도입에 반대하나 (③을 안 하는 이유)

1. **정직 원칙과 정면 충돌.** RAG는 틀려도 근거 배지의 **원문 대목**으로 담당자가 검증한다.
   LLM이 컴파일한 위키는 **위키 자체가 출처**가 된다 — 14B가 컴파일 때 지어낸 문장이
   굳어서 매번 자신 있게 재서빙된다. 우리는 이번 주에만 14B의 숫자 오독·지어냄을 세 번
   막았다(근거 원문 병기·거짓 완료 관문·요약 폴백 정직화). 컴파일된 환각은 그보다 나쁘다.
2. **우리 병목은 검색이 아니다.** Graphify의 71배 토큰 절약은 12만 토큰짜리 코드베이스
   이야기다. 우리 지식은