# GIJO AS 다음 단계 가이드 — 무엇부터 할 것인가

> `GIJO_AS_개발자_온보딩_가이드.md` 위에 얹는 우선순위 로드맵. 온보딩 가이드 6절
> ("무엇이 진짜고 무엇이 스텁인가")이 원자료다 — 각 항목의 코드 위치와 기술적 상세는
> 거기에 이미 있으므로 여기서는 반복하지 않고, **순서와 판단**만 다룬다.
> 내부 기획/개발용 문서다. 영업 문서 아님.

---

## 0. 이 문서를 관통하는 기준

이 제품은 **한국 기업 보안담당자가 쓰는 간단한 툴**이다. 엔터프라이즈 보안 스위트도,
GRC 플랫폼도 아니다. 이 전제가 아래 모든 우선순위를 결정한다:

1. **보안담당자 한 명이 오늘 앉아서 실제 가치를 얻는 데 방해되는 것**이 최우선이다.
   엔터프라이즈 규모에서나 문제 되는 것은 뒤로 간다.
2. 백로그에 있다고 해서 다 만들어야 하는 건 아니다. "간단한 툴"이라는 목표에 안 맞는
   항목은 **만들지 않는 것도 정답**이다 — 4단계에 그런 항목들을 모아뒀다.
3. 코드로 풀 수 없는 것(벤더 계약, 운영 환경 설치, 제품 결정)은 개발 항목인 척하지 않고
   따로 표기한다.

### 한눈에 보기

| 단계 | 항목 | 유형 |
|---|---|---|
| **1. 지금 바로** | ~~실제 스캐너 연결 (`modelscan_wrapper.py`)~~ | ✅ 완료 (2026-07-15, gguf는 여전히 미지원 포맷) |
| | ~~threat.html 가짜 탐지 내역 제거~~ | ✅ 완료 (2026-07-15) |
| | ~~사용자 계정 관리 (최소한만)~~ | ✅ 완료 (2026-07-15) |
| **2. 다음으로** | ~~NSIS 설치본 생성 검증~~ | ✅ 완료 (2026-07-15, 설치 스모크 테스트는 남음) |
| | ~~`huggingface-cli` 설치 (배포 환경)~~ | ✅ 완료 (2026-07-15, 체크리스트에 반영) |
| **3. 여유 있을 때** | CTI 연동 — 무료·공공 소스(OTX→C-TAS) 우선, 상용은 계약 후 | OTX는 지금 개발 가능 / C-TAS는 기관 가입 대기 |
| | 파인튜닝 스크립트 (`finetune_unsloth.py`) | 지금 개발 가능하나 급하지 않음 |
| | 최종 LLM 모델 선정 | **제품 결정 필요** |
| | `server-java-reference/` 존폐 | **제품 결정 필요** |
| **4. 재검토 제안** | 컴플라이언스/승인 워크플로우 페이지 | 만들지 않기를 제안 |
| | SPDX 내보내기 | 필요성 확인 전 보류 제안 |
| | `engine/mcp.ts`, 모델 머지, Penligent | 명시적 동결 제안 |

---

## 1단계 — 지금 바로: 실사용을 막는 문제

이 단계의 판단 기준은 하나다. **보안담당자가 이 툴을 켜고 하루 업무에 쓸 수 있는가?**
지금은 못 쓴다. 아래 두 가지가 그 이유이고, 이 문서에서 가장 중요한 문장은 이것이다:

> **가장 시급했던 건 ① 실제 스캐너 연결(완료), ② threat.html 가짜 데이터 제거였다.**
> 나머지는 전부 그다음이다.

### 1.1 실제 스캐너 연결 — ✅ 완료 (2026-07-15)

- **했던 것**: `server/modelscan_wrapper.py` 작성. pip 패키지 `modelscan`을 CLI가 아니라
  파이썬 API로 직접 호출한다(CLI의 rich 콘솔 렌더러가 stdout에 줄바꿈을 강제로 끼워넣어
  파일로 리다이렉트해도 JSON 파싱이 깨지는 문제가 있었음). 실제 안전하지 않은 pickle
  페이로드로 critical finding 탐지 → 디스패처가 우선순위를 P0로 재산정하는 것까지 실제
  API로 검증했다(온보딩 가이드 6절).
- **알아둘 것**: modelscan은 pickle/PyTorch/Keras/H5/SavedModel/NumPy만 지원하고, 이
  프로젝트의 실제 자산 포맷인 `.gguf`는 지원 목록에 없다. gguf 자산을 스캔하면 빈 배열이
  아니라 `scan_not_supported`(low) finding이 뜨도록 만들었다 — "스캔해서 깨끗함"과
  "이 형식은 아직 못 봄"을 구분 못 하면 1.2와 똑같은 신뢰 문제가 생기기 때문이다.
- **남은 선택지**: gguf 실스캔이 필요하면 별도 스캐너(예: GGUF 헤더/메타데이터 검사,
  또는 자체 휴리스틱)를 두 번째 어댑터로 추가하는 게 다음 단계 — modelscan 하나로는
  이 제품의 실제 자산을 못 본다는 점은 인지하고 있을 것.
- **테스트**: `server/test/modelscan-wrapper.test.ts`(로컬 Python 없으면 자동 skip),
  CI에 `actions/setup-python` + `server/requirements.txt` 추가함.

### 1.2 threat.html 가짜 탐지 내역 제거 — ✅ 완료 (2026-07-15)

- **뭐가 문제였나**: "최근 탐지 내역" 표가 하드코딩된 정적 목업 HTML이었고, KPI 스트립의
  "이번 주 탐지 14 / 유출 자격증명 6 / 조치 완료율 78%"도 전부 조작된 숫자였다. 보안 툴이
  가짜 인시던트를 보여주는 "거짓 정보 표시" 문제 (온보딩 가이드 6절, 7절).
- **한 일**: 권장안대로 표를 실제 `GET /api/cti/findings`(`listFindings()`, preload의
  `listCtiFindings`는 이미 있었음)에 연결 — 벤더 연동 전까지는 빈 표 + "탐지 내역이 없습니다"
  안내가 뜨고, 연동되는 날 UI는 준비돼 있다. severity(critical/warning/info)→긴급/주의/정보
  배지 매핑, 값 HTML 이스케이프 포함. 가짜 KPI 3개는 실데이터 2개(탐지 내역 수 · 긴급 탐지
  수)로 교체, 하드코딩 시계는 다른 페이지들과 같은 실시간 시계로 교체. 같은 원칙으로:
  하드코딩된 "모니터링 대상" 도메인 목록은 미제공 안내 문구로, 6개 페이지 사이드바에 박혀
  있던 가짜 "SBOM 관리 ③" 알림 배지는 제거. 피드 키 설정 UI는 계획대로 건드리지 않았다.
- **검증**: `window.gijo` 스텁을 주입한 사본으로 빈 상태(빈 표 + KPI 0)와 데이터 있는
  상태(행 렌더링 + 배지 매핑 + KPI 집계) 모두 브라우저에서 실제 렌더링 확인. 서버 변경 없음.

### 1.3 사용자 계정 관리 — 최소한만 ✅ 완료 (2026-07-15)

- **뭐가 문제였나**: 계정이 `auth/users.ts` 인메모리 하드코딩 시드 1개(`jyh`/`changeme`)뿐이었다.
  SQLite 테이블조차 없고, 계정 추가/삭제 API·UI가 전혀 없었다.
- **한 일**: `db.ts`에 `users` 테이블(SQLite) 추가, `auth/users.ts`를 SQLite 기반 CRUD로
  재작성(`createUser`/`deleteUser`/`changePassword`/`listUsers`, 최초 기동 시 관리자 자동
  시드). `auth.ts`에 `adminMiddleware` 추가. `GET/POST /api/users`, `DELETE /api/users/:id`,
  `POST /api/users/:id/password` 라우트를 admin 전용(본인 비밀번호 변경은 예외)으로 등록.
  마지막 admin 삭제 방지, 본인 계정 삭제 방지 가드 포함. settings.html에 본인 비밀번호
  변경 폼과, admin 전용 "계정 관리" 패널(목록/추가/삭제, role이 admin일 때만 노출)을 추가.
  역할 계층(RBAC), 조직/팀 구조, SSO, 감사 로그는 계획대로 만들지 않았다 — admin/security_officer
  이분법이 상한선이다.
- **검증**: 서버 테스트 10개 전부 통과(`server/test/users.test.ts`, admin 게이팅/CRUD/자기
  자신 삭제 금지/마지막 admin 보호/비밀번호 변경 권한 포함). Playwright로 실제 Electron 앱을
  기동해 admin으로 로그인 → 계정 관리 패널 노출 확인 → 신규 `security_officer` 계정 생성 →
  로그아웃 후 그 계정으로 실제 로그인 성공 → 해당 계정엔 계정 관리 패널이 보이지 않음 확인 →
  본인 비밀번호 변경 → admin으로 재로그인 후 해당 계정 삭제까지 전 과정 실행 및 확인 완료.
  (이 검증 과정에서 `client/server-dist/`의 오래된 빌드가 새 라우트 없이 그대로 남아있어
  Electron이 그 스테일 서버를 먼저 집어 쓰는 버그를 발견 — `npm run build-server-dist`로
  재생성해 해결. 서버 코드를 바꿀 때마다 `server-dist`를 재빌드하지 않으면 dev 모드에서도
  같은 문제가 재발할 수 있으니 주의.)

---

## 2단계 — 다음으로: 배포 완성도

1단계가 끝나면 "쓸 수 있는 제품"이다. 이 단계는 그걸 **전달 가능한 제품**으로 만든다.
둘 다 코드보다는 운영/환경 작업이다.

### 2.1 NSIS 설치본 생성 검증 — ✅ 완료 (2026-07-15)

- **결과**: `dist/GIJO AS Setup 0.1.0.exe` (약 302MB) 생성 성공.
- **가설 검증 결과**: `CSC_IDENTITY_AUTO_DISCOVERY=false` 단독으로는 **해결 안 됨** —
  서명을 안 해도 electron-builder가 winCodeSign 압축해제는 수행하고, darwin dylib
  심볼릭 링크 2개에서 권한 오류로 죽는다(기각). 실제로 동작한 우회는 **캐시 수동
  압축해제**(darwin 심볼릭 링크 2개만 빠진 캐시를 `winCodeSign-2.6.0` 이름으로 미리
  배치 — 정확한 절차는 온보딩 가이드 10절). 개발자 모드 활성화도 되겠지만 시스템 설정
  변경 없이 우회 가능하므로 불필요.
- **남은 폴리시 항목** (별도 결정): 앱 아이콘 미설정(기본 Electron 아이콘), 코드 서명
  없음(고객 PC에서 SmartScreen 경고 — 인증서 구매 필요). 설치 파일을 실제 설치→기동→
  로그인까지 돌려보는 스모크 테스트는 아직 안 함.

### 2.2 `huggingface-cli` 설치 (배포 환경) — ✅ 완료 (2026-07-15)

- **한 일**: PC세팅 체크리스트 STEP 6에 "`huggingface-cli`는 수동 다운로드용이자 GIJO AS
  서버(`loadHfModel()`)의 런타임 의존성 — 서버 실행 계정 PATH에서 `huggingface-cli
  --version`이 동작해야 한다"는 확인 절차를 명시하고, 트러블슈팅 표에 "검색은 되는데
  다운로드만 실패" 증상 항목을 추가했다.

---

## 3단계 — 여유 있을 때: 외부 의존 · 제품 결정 대기

이 단계 항목들은 개발로 밀어붙일 수 없다. 각각 **벤더 계약** 또는 **제품 결정**이
선행돼야 하며, 그 전에 코드를 써봐야 헛돈다.

### 3.1 CTI 연동 — 무료·공공 소스 우선으로 재편 (2026-07-15 수정)

- **현황**: `listFindings()`가 항상 `[]`. 키 저장/암호화/복호화 측은 완성 (온보딩 가이드 6절).
  UI에는 KISA C-TAS(공유형)와 LevelBlue OTX가 "지원 예정" 피드로 노출돼 있다(키 설정 불가,
  `cti.ts`의 `PLANNED_FEEDS` — 연동 구현 시 `SEED_FEEDS`로 옮기면 키 설정이 열린다).
- **바뀐 전제**: 원래 "상용 벤더 계약 대기"로 동결했었으나, 내부 TI 연동 가이드라인 검토
  결과 계약 없이 진행 가능한 소스가 있다:
  - **KISA C-TAS (공유형)** — 기관 가입·승인만 필요(계약 불요). Export API가 피드형이라
    "최근 탐지 내역" 모델과 맞고, 국내 보안담당자 대상이라는 제품 포지셔닝과도 맞는다.
    키가 3종(expKey/colKey/orgCode)이라 피드당 암호화 문자열 1개인 현재 저장 형식을
    암호화 JSON으로 확장해야 한다. **선행 조건: 기관 가입·승인** — 실데이터 검증이
    필요하므로 승인 전에 코드부터 짜지 말 것.
  - **LevelBlue OTX** — 무료 API 키. Pulse 구조가 피드형이라 `listFindings()`에 바로
    맞는다. 무료 키로 즉시 실검증 가능 — **가장 먼저 붙일 후보**.
  - VirusTotal은 피드가 아니라 조회형(IOC를 넣어야 결과가 나옴)이라 "최근 탐지 내역"이
    아닌 별도 "IOC 검색" 기능감이다 — 필요성이 확인되기 전엔 보류(4단계 원칙).
- **구현 시 채택할 위생 항목** (내부 가이드라인에서 유효한 것만): `collected_at` 인덱스 +
  90일 TTL 정리, 30~60분 배치 폴링 + SQLite 캐시 테이블, 경량 서킷 브레이커(타임아웃 +
  실패 시 캐시 반환). **기각**: Redis/Kafka 큐, 분산 락, A01 모델 격리 — 단일 Node 프로세스
  + SQLite 제품에 다중 노드 수집기 아키텍처는 과하다(4단계 스코프 원칙).
- **상용 벤더**(Criminal IP / Flashpoint / SpyCloud / Recorded Future)는 기존대로 계약
  후 추가. 넷 다 동시에 붙일 필요 없다는 조언은 유효하다.

### 3.2 파인튜닝 스크립트 (`scripts/finetune_unsloth.py`)

- **현황**: 트리거 API와 `finetune:progress` WS 배관은 실제 코드, 학습 스크립트만 없다.
- **왜 급하지 않은가**: "단기 기억"(RAG)은 이미 동작한다. 보안담당자의 일상 사용에서
  파인튜닝("장기 기억")이 없어서 막히는 시나리오는 당장 없다. 지금 개발 가능한 항목이지만,
  1단계를 제치고 GPU 학습 파이프라인을 만들 이유가 없다.
- **작업 성격**: 지금 개발 가능. Unsloth QLoRA 스크립트 + progress 출력 포맷을 기존
  파서에 맞추기. 착수 전에 "고객이 실제로 파인튜닝을 돌릴 것인가"를 한 번 물어볼 것 —
  답이 불확실하면 3단계에 그대로 둔다.

### 3.3 최종 로컬 LLM 모델 선정 — 결정이 먼저

- **현황**: dev 기본은 Qwythos-9B. 후보로 Qwen3-30B-A3B, 다운로드해 둔 경량
  `segolilylabs/Lily-Cybersecurity-7B-v0.2`가 있다 (상세: `로컬LLM_프로젝트_가이드.md`).
- **왜 결정 사항인가**: 품질(30B) vs 고객 하드웨어 요구사양(7B/9B)의 트레이드오프는
  타깃 고객이 어떤 GPU를 갖고 있(거나 살 수 있)는지에 달렸다 — 벤치마크가 아니라
  **영업/제품이 답할 질문**이다. 여기서 답을 정하지 않는다. 다만 이 결정이 늦어지면
  배포 문서, 하드웨어 권장 사양 안내, 모델별 프롬프트 튜닝이 전부 표류하므로,
  1~2단계가 끝나는 시점까지는 정하는 것을 권한다.

### 3.4 `server-java-reference/` 존폐 — 결정이 먼저

- **현황**: 폐기된 Java/Spring 재작성. 금융권 등 규제 고객의 온프레미스 보안성 검토에서
  Java 스택이 요구될 경우의 폴백으로만 보관 중 (온보딩 가이드 2절).
- **트레이드오프를 이름 붙여 두면**: "한국 중소기업 보안팀용 간단한 툴"이 타깃이라면
  이 폴백이 발동할 시나리오는 사실상 없고, 저장소에 남아 있는 것 자체가 신규 개발자
  혼란 비용이다(온보딩 가이드가 두 번이나 "건드리지 말 것"이라고 경고해야 하는 상태).
  반대로 규제 산업 대기업을 노린다면 보관 가치가 있다. **이건 타깃 시장 결정이지 코드
  결정이 아니다** — 결정만 내리면 실행은 `git rm` 한 줄(히스토리에는 남는다)이거나
  현상 유지다. 어느 쪽이든 결정을 문서에 남길 것.

---

## 4단계 — 재검토 제안: 만들지 않는 게 나을 수 있는 것

백로그에 있다고 로드맵에 있어야 하는 건 아니다. 아래 항목들은 "언젠가 만든다"가 아니라
**"만들 가치가 있는지부터 다시 묻자"**가 제안이다. 근거와 함께.

### 4.1 컴플라이언스 대시보드 · 승인 워크플로우 (`compliance.html`, `approvals.html`)

- **현황**: 둘 다 백엔드 0줄의 플레이스홀더 페이지.
- **제안**: **로드맵에서 빼는 것을 진지하게 검토할 것.** 컴플라이언스 대시보드와 다단계
  승인 워크플로우는 GRC 제품의 기능이다. 그건 감사 조직과 결재 라인이 있는 대기업의
  문제를 푸는 것이고, 이 제품의 사용자 — 소규모 보안팀 — 는 승인 워크플로우가 아니라
  "스캔하고, 결과 보고, 보고서 뽑는" 루프가 필요하다. 이 두 페이지를 제대로 만들면
  사용자 관리·권한 체계·감사 로그가 줄줄이 딸려 오고, 그 순간 이 제품은 "간단한 툴"이기를
  멈춘다. 스코프 크리프의 전형적인 입구다.
- **최소 조치**: 만들기로 결정 나기 전까지는 빈 플레이스홀더 페이지를 **사이드바에서
  숨기는 것**을 권한다. 죽은 메뉴 2개는 데모/평가 때 제품이 미완성이라는 인상만 준다.
  컴플라이언스 요구가 실제 고객에게서 나오면, 대시보드가 아니라 "보고서에 관련 섹션
  추가" 수준으로 먼저 대응할 수 있는지부터 볼 것.

### 4.2 SPDX 내보내기

- **현황**: `exportSbom(format:"spdx")`가 명시적으로 throw. CycloneDX는 완동작.
- **제안**: **요구하는 고객이 나타나기 전까지 만들지 말 것.** CycloneDX 하나로 일반적인
  보안성 검토/공급망 점검 케이스는 커버된다. 두 번째 SBOM 포맷은 "있으면 좋은" 체크박스지
  사용자의 하루를 바꾸지 않는다. 착수 조건을 "SPDX를 명시 요구하는 고객 1곳"으로 걸어두면
  된다. 그전까지의 조치는 UI에서 SPDX 버튼을 숨기거나 "지원 예정"으로 비활성화하는 것.

### 4.3 `engine/mcp.ts` (MCP 서버 스캐폴드)

- **현황**: `app.ts`/`index.ts` 어디에도 연결 안 된 실험 코드. 오너도 유스케이스도 없다.
- **제안**: **명시적 동결.** 유스케이스 없는 실험 코드는 시간이 지나면 "이거 왜 있지?"라는
  온보딩 비용이 된다(이미 온보딩 가이드가 설명 한 줄을 할애하고 있다). 삭제하거나,
  남긴다면 파일 상단에 "미연결 실험, 오너 없음, 빌드 대상 아님" 주석을 박을 것.
  MCP 연동이 실제 제품 시나리오(예: 고객사의 다른 도구가 GIJO를 MCP로 호출)로
  구체화되면 그때 부활시키면 된다.

### 4.4 모델 머지 (`merge.html` / Evolutionary Model Merge)

- **현황**: 서버 로직 전무, "미구현" 명시 플레이스홀더.
- **제안**: **이 제품의 목표와 맞는지부터 의심할 것.** Evolutionary Model Merge는 ML 연구
  파이프라인이지 보안담당자의 도구가 아니다. 보안담당자는 모델을 머지하고 싶은 게 아니라
  취약점을 찾고 싶다. 연구 트랙으로서 가치가 있더라도 이 제품의 로드맵이 아니라 별도
  실험으로 분리하는 게 맞다. 4.1과 마찬가지로, 결정 전까지 사이드바에서 숨길 것.

### 4.5 Penligent 펜테스트 어댑터

- **현황**: `bridge.ts`에 TODO 주석뿐. 코드 주석 자체가 "로드맵 Phase 5"로 명시적 연기.
- **제안**: 이미 연기돼 있으니 그대로 두되, **1.1의 실제 스캐너가 자리 잡기 전에는 두 번째
  어댑터를 논하지 말 것.** 어댑터 하나가 제대로 돌면 레지스트리 구조 덕에 두 번째 추가는
  훨씬 싸진다 — 순서가 곧 비용 절감이다.

---

## 5. 요약 — 순서대로 하면

1. ~~**`modelscan_wrapper.py` 작성**~~ → ✅ 완료, 제품이 처음으로 진짜 스캔을 한다(gguf는 아직 못 봄).
2. **threat.html 목업 제거** → 거짓 화면이 사라진다. (반나절)
3. ~~**최소 계정 관리**~~ → ✅ 완료, 팀이 쓸 수 있게 됐다.
4. NSIS 우회책 검증 + HF CLI 설치 절차화 → 배포 가능해진다. (한나절~수 일)
5. 그동안 제품 측에서 결정: 최종 LLM 모델 / java-reference 존폐 / CTI 벤더 1곳 /
   컴플라이언스·승인·머지 페이지의 존폐.
6. 결정이 나온 것부터 3단계 항목 착수. 4단계 항목은 착수 조건(실고객 요구)이 충족되기
   전까지 건드리지 않는다.

1~3번이 끝난 시점의 GIJO AS는 "그럴듯해 보이는 데모"에서 "보안담당자가 오늘 쓰는 도구"로
바뀐다. 그 전까지의 다른 모든 작업은 그 전환을 늦출 뿐이다.
