# GIJO AS — 실사용 UX 피드백 (보안담당자 첫날 워크스루)

> 방법: 빈 DB + 실 gijo LLM/임베딩 인스턴스에서 온보딩 체크리스트를 API로 실제 실행(2026-07-17).
> "신규 보안담당자 첫날" 관점의 강점·아쉬운 점·권장.

## 실제로 잘 되는 것 (강점)
- **첫날 샘플 데이터 제공** — 자산·샘플 취약 호스트·보안제품이 미리 있어 빈 화면 아님(owner="샘플"로 구분)
- **취약점 우선순위 자동 산출** — 스캔 CSV 업로드 → 호스트 자동 자산화 + 각 취약점에 **EPSS·VPR·CISA KEV(실제 악용) 매칭**. 실측: Log4Shell → epss 0.94 / vpr 10 / **KEV=O 자동 표시**. 이게 이 제품의 핵심 가치이고 잘 작동함
- **RAG 정확** — 사내 규정 올리고 "승인권자는?" 질의 → "김민수" 정확히 검색(실 임베딩)
- **온톨로지·컴플라이언스·KPI·AI 채팅** 전부 실동작, 전부 온프레미스 로컬 LLM

## 아쉬운 점 (우선순위 순)

### 1. 조치에 "담당자·기한(SLA)"이 없다 〔✅ 해결됨 — commit `44b1252`〕
~~승인 워크플로우가 approve/reject만 지원, assignee·dueDate 필드 없음.~~
→ **조치 관리로 확장 완료**: finding별 담당자·기한(SLA) 배정, 판정과 별개로 배정 가능, 기한초과 시
'지연(overdue)' 자동 표시·집계(붉은 테두리·KPI 카드). 취약점관리 지침의 조치 단계와 정합.

### 2. "오늘 뭐부터" 통합 우선순위 뷰가 약하다
지표(EPSS/VPR/KEV)는 훌륭한데, **전 자산을 가로질러 "악용확률 순 오늘의 조치 Top N"**을 주는 화면/API가 없음(정렬은 각 화면 클라단). 담당자는 자산별로 흩어진 걸 각각 봐야 함.

### 3. 컴플라이언스 21개가 첫날 전부 "미평가(open)"
온톨로지·criteria에 위협별 양호/취약 기준이 다 있는데, 자산·스캔을 근거로 **자동 초안·추천을 안 해줌**. 21개 수작업 평가는 1인에겐 부담.

### 4. AI가 "판단"까지는 안 간다 〔핵심 의견〕
데이터·지표·지식은 최상인데, **"그래서 이 3개를 이 순서로, 이 담당이, 이 기한까지, 이렇게 조치하라"는 실행계획을 AI가 먼저 제시하지 않음**(담당자가 물어봐야 함). 1인 운영 도구의 결정적 한 걸음(의사결정 대행)이 비어 있음.

### 5. 소소한 러프엣지
- 자산 수동 등록 시 **id를 사람이 지정**해야 함(자동생성 없음)
- 비밀번호 변경 API는 되지만, 변경 후 재로그인/세션 안내 흐름 미흡(테스트 중 셀프 잠김 재현)

## 권장 (의견)
1. **[최우선] 조치 관리로 확장** — 승인 화면에 담당자·기한(SLA)·상태(조치중/완료) 추가. "오탐 판정"을 넘어 지침의 생애주기(조치 단계)와 정합.
2. **통합 우선순위 대시보드** — 전 자산 findings를 EPSS/VPR/KEV로 정렬한 "오늘의 조치 Top N" 단일 화면.
3. **AI 자동 triage** — 스캔 업로드 시 AI가 [우선순위 근거 + 권장 조치 순서 + 예상 담당/기한] 초안을 먼저 제시(온톨로지 완화통제 활용). 컴플라이언스도 자산·스캔 기반 상태 초안.
4. **소소한 것** — 자산 id 자동생성, 비밀번호 변경 후 안내.

> 요약: **"보는 도구"로는 이미 훌륭하다(지표·근거·온프레미스 AI). 부족한 건 "시키는 도구"** —
> 담당자의 다음 행동을 AI가 좁혀서 담당·기한까지 붙여주는 마지막 한 걸음.
