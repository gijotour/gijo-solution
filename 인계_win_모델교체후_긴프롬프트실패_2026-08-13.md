# win 인계 — **모델을 바꾸면 그 뒤로 긴 질문이 전부 실패한다** (max, 2026-08-13)

> 한 줄: 채팅 모델을 교체한 뒤부터 **300자 넘는 프롬프트가 0초 만에** 「⚠ AI 모델이 아직
> 준비되지 않았습니다」로 떨어진다. 짧은 질문은 정상이고, llama-server를 **직접** 부르면
> 같은 프롬프트가 200으로 답한다. **서버를 다시 띄우면 낫는다.**

## 1. 재현 (라이트 진입점 · 빈 DB · Metal)

```
① analysis 에이전트 = gijo-main-orchestrator 로 정상 동작 확인
   17자 → 5초 · 321자 → 7초 · 821자 → 4초 · 1,521자 → 13초 · 1,921자 → 7초   전부 정상

② 배정을 qwen3-14b 로 바꿔 스왑을 일으킨다 (POST /api/agents/analysis/model)

③ 그 뒤:
   17자 → 46초 답 정상
   321자 → **0초 · 준비 안 됨**
   821자 → **0초 · 준비 안 됨**
   1,521자 → **0초 · 준비 안 됨**

④ 배정을 다시 7.6B로 돌리고 POST /api/localengine/start 로 실제 스왑까지 확인해도
   (8080이 gijo-main-orchestrator를 서빙하는 것을 프로세스 인자로 확인) **여전히 실패**

⑤ 서버 프로세스만 다시 띄우면 → ①로 완전히 복귀
```

## 2. 무엇이 아닌지 (하나씩 지웠다)

| 의심 | 확인 결과 |
|---|---|
| 모델이 안 올라옴 | ✗ `/api/localengine/status` = `ready:true` · 프로세스 인자로 8080이 그 모델을 서빙 |
| llama-server 고장 | ✗ 같은 프롬프트를 8080에 **직접** 보내면 HTTP 200 · 정상 추출 |
| 문맥 초과 | ✗ 직접 호출로 `max_tokens` 512·2048·4096·8192 전부 200 |
| 문법(NO_HAN_GRAMMAR) | ✗ 직접 호출에 같은 grammar를 붙여도 200 |
| 특정 모델 | ✗ 7.6B·14B 양쪽에서 재현. **교체를 한 번 겪은 뒤**가 조건이다 |
| 프롬프트 내용 | ✗ 합성 반복문·실제 문서 모두 동일 |

즉 **llama.cpp 쪽은 멀쩡하고, 서버 안의 상태가 스왑 뒤에 어긋난다.**

## 3. 짚이는 자리

`llm.ts:685` — `ensureAgentModel(agentId)`가 baseUrl을 준다. 실패하면 `.catch`가
`LOCAL_LLM_BASE_URL`로 조용히 폴백한다.
`llm.ts:687` — `agentRequestExtras(agentId)`가 `lora` 배열을 준다. 주석(1099줄)에 따르면
**적재 인덱스**를 담는데, 스왑으로 풀이 바뀌면 그 인덱스가 옛 모델 것일 수 있다.
llama-server는 없는 어댑터 인덱스를 받으면 400을 낸다 — 그러면 `!res.ok`로 떨어지고
`llm.ts:720`이 그것을 「모델이 아직 준비되지 않았습니다」로 **뭉갠다.**

⚠ 길이 의존이 여기서 설명되는지는 내가 확인 못 했다(짧은 질문은 통과한다). 다만
**짧은 질문이 통과하는 경로와 긴 질문이 타는 경로가 다르다**면 거기서 갈릴 것이다.

▶ 무엇이든, **`!res.ok`를 「모델 준비 안 됨」으로 뭉개는 것은 고쳐야 한다.**
오늘 이 한 문장 때문에 「모델이 안 올라왔나」를 세 번 확인했다. 상태 코드와 응답 본문
앞부분을 로그에 남기면 이런 추적이 1분이면 끝난다.

## 4. 라이트에서 왜 더 잘 드러나나 (내 변경의 몫)

`lite/env.ts`에 라이트 등급 바닥값(**동시 1개**)을 넣은 것이 나다(38da243).
동시 2개(제품 기본)에서는 두 모델이 **공존**해 스왑이 아예 안 일어난다.
동시 1개면 모델을 바꿀 때마다 반드시 스왑이 나므로 **라이트가 이 결함을 정면으로 맞는다.**

지금 출하 dmg는 채팅 모델을 하나만 담으므로 기본 사용에서는 안 걸린다.
다만 **내가 만든 설정 화면(lite-settings)이 모델 목록과 시작 단추를 제공**하므로,
담당자가 모델을 바꾸면 그 뒤로 긴 질문이 전부 죽는다 — 그리고 화면은
「모델이 준비되지 않았습니다」라고만 말해서, 담당자는 앱을 껐다 켜기 전까지 이유를 모른다.

▶ 고쳐질 때까지의 임시 안전장치가 필요하면 말해 달라. 라이트 설정 화면에서
모델 바꾸기를 눌렀을 때 **「바꾼 뒤에는 앱을 다시 시작하세요」**를 띄우는 정도는
내 파일 안에서 바로 할 수 있다(제품 동작은 안 건드린다).


---

## 5. 정정 — 스왑 전용이 아니다 (max, 2026-08-13 13:30)

qwen3-8b 측정 중에 같은 고장이 **모델 재적재 없이** 재발했고(감시 재기동도 없음 —
llama 프로세스 시작 시각으로 확인), **~10분 뒤 저절로 나았다**(재기동 없이).

고장 구간의 갈림표:
- `/api/llm/chat` 긴 프롬프트(~2,000자) → **0~1초 즉시 실패** (연속 5회)
- 같은 시각 `/api/dispatch`(제품 대화) → **정상** (SQL 인젝션 질문에 실제 답)
- 같은 시각 llama-server 직접 호출 → 정상(어제 확인과 동일)
- 나은 뒤: chat 짧게 23초·길게 25초·dispatch 22초 — 전부 정상

즉 「스왑이 원인」이 아니라 **「무언가로 잠시 걸린 상태」**다. 스왑·배정 변경은 그 상태를
만드는 방아쇠 중 하나로 보이고(어제 재현 2회), 이번엔 방아쇠 없이도 걸렸다.
어제 「서버 재기동이 낫게 한다」는 관찰은 유효하다 — **기다리면 낫기도 한다**가 추가됐을 뿐.

짚이는 결: 진행 중이던 요청이 끊기며 슬롯·연결이 잠시 물리는 종류(keep-alive 소켓,
llama 슬롯 점유, cache_prompt류). 확정은 못 했다 — `!res.ok`를 뭉개지 말고 상태코드·본문을
로그에 남기면(§3 제안) 다음 재발 때 1분에 갈린다.
