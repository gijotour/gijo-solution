# GIJO AS — RAG 근거 오염 실측 (2026-08-10, Mac)

**측정 기계**: Mac 개발 DB(7월 운영 스냅샷 복원본) · 문서 93건 · 접점 3개
**측정 대상**: 검색 층(`queryMemoryScored`) — LLM을 타지 않는다
**재현성**: 같은 조건에서 두 번 돌려 **완전히 같은 값**. 결정적 측정이다.

---

## 1. 왜 남기나 — 회차 값과 기준선을 구분하려고

같은 날 `ops-sim` 152문항을 두 번 돌려 144 → 146을 얻었지만, **그 숫자는 기준선이 못 된다.**
「새로 불편해진 3건」 중 **둘이 재현되지 않았다**:

| 회차에서 잡힌 것 | 다시 재보니 |
|---|---|
| 「가장 급한 취약점에 담당자 배정해줘」 78초 | **3.4초**에 결재판 정상. 그 회차만 모델이 길을 잃어 CVSS 설명을 했다 |
| 「퇴사자 계정 언제까지」 근거 낱말 없음 | **5/5 통과**. 검색·선별·전달 전부 정상 |

→ `ops-sim`의 답 단위 숫자에는 **모델 흔들림이 섞여 있다.** ±몇 건은 노이즈로 본다.
→ 기준선으로 쓸 것은 **LLM을 안 타는 층의 값**이다. 이 문서가 그것이다.

⚠ 게다가 「퇴사자 계정」은 5/5 **통과했는데도 답이 서로 어긋났다**:
> 1회 「즉시 삭제」 · 3회 「즉시 삭제」 · 4회 「최소 2년 보관」 · 5회 「최소 1년 보관」

판정이 낱말(`개인정보|보관|삭제`) 포함 여부라 전부 초록불이 됐다. **낱말은 맞고 지침은 정반대다.**
담당자가 이 답으로 규정을 정하면 회차에 따라 다른 결론을 낸다. 판정 방식 자체를 볼 값이 있다.

---

## 2. 실측 — 타사·타사고객 문서가 근거 후보를 절반 넘게 차지한다

`ops-sim` 152문항을 검색 층에 그대로 넣고, 관련 문턱(≤0.95) 안 후보를 셌다.

| | 값 |
|---|---|
| 관련 문서가 잡힌 질문 | **137건** (152 중) |
| 문턱 안에 **타사 문서**가 낀 것 | **73건 (53%)** |
| 타사 문서가 **1위**인 것 | **28건 (20%)** |

「타사 문서」로 센 것: `Tenable*` · `FOCS*` · `(주)안전대부 …보고서`
(우리 제품 문서가 아니거나, 다른 회사 자산을 다루는 자료)

### 하필 핵심 질문들이다

| 질문 | 1위로 잡힌 문서 | 거리 |
|---|---|---|
| SSH 관련 취약점 찾아줘 | Tenable Vulnerability Mgmt | **0.7750** |
| 웹서버 취약점만 보여줘 | 안전대부 웹취약점 보고서 | **0.7701** |
| 미조치 취약점 알려줘 | 안전대부 웹취약점 보고서 | **0.7951** |
| 이 취약점 조치하면 점수 얼마나 올라? | Tenable Vulnerability Mgmt | 0.7878 |
| 우리 보안 점수 어때? | Tenable Vulnerability Mgmt | 0.8064 |
| 방화벽 뭐 쓰고 있어? | FOCS 매뉴얼 | 0.8648 |

**0.77~0.81은 「강함(≤0.85)」 한복판이다.**

---

## 3. 문턱으로는 못 막는다

| 시도 | 결과 |
|---|---|
| 0.95 → 0.85로 조이기 | 위 사례 대부분이 **그대로 남는다**(0.77~0.81) |
| 더 조이기 | 진짜 근거가 **먼저** 사라진다 — 「조치 이력」에서 GIJO 자체 매뉴얼이 0.8470으로 Tenable(0.8455)**보다 멀다** |

임베딩이 「타사 보안도구 매뉴얼」과 「우리 기능 질문」을 **진짜로 가깝다고 본다.**
둘 다 보안 도구 매뉴얼 어투에 관리·이력·작업 어휘를 쓴다.
**텍스트 유사도로는 「누구의 문서인가」가 구분되지 않는다.**

---

## 4. 지금 안 터지고 있는 이유 — 도구가 가려 준다

`d14bea7`의 `근거재검색대상인가()`는 **데이터 도구가 답한 질문에는 근거를 안 붙인다.**
「미조치 취약점 알려줘」는 `finding_status`가 답하므로 위 오염이 화면에 안 나온다.

→ **도구 없이 모델이 답하는 순간에만 드러난다.** 실제로 그렇게 두 번 관찰됐다:
- 「조치 이력 보여줘」 → 근거에 Tenable 매뉴얼
- 「퇴사자 계정 언제까지」 → 근거 2·3위가 Tenable

**잠재 오염은 53%이고, 지금은 도구가 덮고 있을 뿐이다.**

---

## 5. 다시 재는 법

```bash
cd server
node -e "
(async()=>{
  const m=await import('./dist/engine/memory.js');
  const 질문=JSON.parse(require('fs').readFileSync('../.tmp-reports/ops-sim.json','utf8')).map(c=>c.q);
  const 타사=/Tenable|FOCS|안전대부/i;
  let 낀것=0, 일위=0, 전체=0;
  for(const q of 질문){
    const r=await m.queryMemoryScored(q,5).catch(()=>null);
    const 관련=(r??[]).filter(c=>c.distance<=m.RAG_RELEVANCE_MAX_DISTANCE);
    if(!관련.length) continue;
    전체++;
    if(관련.some(c=>타사.test(String(c.documentId??'')))) 낀것++;
    if(타사.test(String(관련[0].documentId??''))) 일위++;
  }
  console.log(전체, 낀것, 일위);
})()"
```

⚠ **문서 구성이 다르면 값도 다르다.** 이 값은 Mac 복원본(문서 93건) 기준이다.
운영에서 재면 다른 숫자가 나온다 — 그것이 정상이고, **각 기계의 값을 각각 남겨야 한다.**

---

## 6. 이 실측이 뒷받침하는 결정

지식그래프 계획(`GIJO_AS_지식그래프_LLM위키_도입계획.md`) §5의 조각 ⓑ —
**「제품 내장 문서를 표시한다」**가 닫아야 할 것이 무엇인지, 이 숫자가 규모를 준다.

- 접점(ⓒ)으로는 못 푼다 — 접점은 「올라온 것」에 생기므로 **타사 문서에 더 많다**(운영 실측: Tenable 73개, 우리 매뉴얼 0개)
- 문턱으로도 못 푼다 — 위 3장
- 남는 것은 **문서에 「누구 것인가」를 붙이는 일**뿐이다

ⓑ 통과 기준 후보:
1. 「조치 이력 보여줘」에서 Tenable 매뉴얼이 GIJO 매뉴얼 **아래로** 내려간다
2. 위 137건 중 **타사 1위 28건**이 유의미하게 준다 (같은 방법으로 재측정)

---

## 7. ①ⓑ 배포 후 재측정 (같은 날 저녁, Mac)

**배포**: `73ebb8b`(origin 칸 + `applyOriginBoost` ORIGIN_BOOST=0.012 + `markDocumentsBuiltin`) · `71af1c8`
**Mac 소급**: 부팅 시 `docsbundle.ts:200`이 돌아 **builtin 19건 · 빈값 21건**

### 통과 기준 둘 다 ✅

**① 「조치 이력 보여줘」** — 상위 6건이 **전부 우리 문서**, Tenable은 사라졌다.

| 순위 | 문서 | 거리 |
|---|---|---|
| 1 | GIJO_AS_용어사전.md | 0.8196 |
| 2 | GIJO_AS_사용자_매뉴얼.md | 0.8350 |
| 3 | GIJO_AS_사용자_매뉴얼.md | 0.8470 |

배포 전에는 **Tenable 0.8455가 GIJO 0.8470보다 위**였다(§3).

**②** 관련 잡힘 **137건 그대로** · 타사 낌 73→**38**(53%→28%) · 타사 1위 28→**12**(20%→9%)

> **「관련 137건이 그대로」가 중요하다.** 부스트가 **거르지 않고 올리기만** 했다는 증거다
> (설계 조건: 접점 없는 새 문서가 통째로 안 보이면 안 된다).

### ⚠ 그런데 위 숫자는 과소집계다 — 정규식이 아니라 origin으로 세라

§2·§5의 「타사」 판정은 정규식(`Tenable|FOCS|안전대부`)이었다. **이제 `origin` 칸이 있으니
그것이 정답이다.** 고객 업로드·기타 외부 문서가 정규식에 안 걸려 우리 것으로 세어졌다:

| | 정규식 셈 | **origin 셈 (정확)** |
|---|---|---|
| 외부 낌 | 38건 (28%) | **60건 (44%)** |
| 외부가 1위 | 12건 (9%) | **20건 (15%)** |
| **우리 문서가 후보에 아예 없음** | (못 셈) | **16건 (12%)** |

### ▶ 그래서 ORIGIN_BOOST를 더 올리지 마라

> **외부 1위 20건 중 16건은 「올릴 우리 문서가 후보에 없는」 것이다.**
> 없는 것은 못 올린다 — 부스트를 아무리 세게 해도 **최대 수확이 4건**이고,
> 그 대가로 GIJO 문서끼리의 순위만 흔들린다(운영에서 제품소개가 이미 밀렸다 → `71af1c8`).

### 남은 것은 부스트가 아니라 문서·융합 문제다

**ⓐ 문서 공백** — 제품 사용법 질문인데 우리 매뉴얼이 후보에 **없다**:

| 질문 | 1위 | 우리 문서 |
|---|---|---|
| 사용자 추가하려면? | Tenablesc_API.pdf (0.7994) | **없음** |
| 이 문서 등급 바꾸려면? | Tenable_VM (0.8664) | **없음** |
| 누가 뭘 지웠는지 볼 수 있어? | FOCS 매뉴얼 (0.9212) | **없음** |

검색이 아니라 **매뉴얼에 그 절차가 없거나 검색에 안 걸리는** 것이다.

**ⓑ RRF 융합** — 거리가 훨씬 가까운데 순위가 뒤집힌 사례:

| 질문 | 1위 | 우리 문서 |
|---|---|---|
| 분석할 로그 올리려면 어떻게 해? | FOCS 매뉴얼 **0.8597** | GIJO 실무매뉴얼 **0.7202** |

거리 차가 0.14인데도 FOCS가 1위다. **거리 문제가 아니라 융합(lexical 가중치) 문제**라
`ORIGIN_BOOST 0.012`로 안 뒤집혔다. 부스트를 키울 게 아니라 융합을 봐야 하는 자리다.

### 다시 재는 법

`tools/`가 아니라 이 문서의 방법을 그대로 쓴다. ⚠ **`cd ~/gijo-as/server`에서** 돌리고,
`origin='builtin'` 집합으로 판정한다(정규식 금지 — 위에서 과소집계로 드러났다).

---

## 8. 진짜 원인은 후보 풀이었다 — 부스트도 융합도 아니다

§7에서 남은 것을 「문서 공백」과 「RRF 융합」으로 지목했다. **둘 다 재보니 틀렸다.**

### 정정 ① — 「융합을 보라」는 근거가 1건뿐이었다

128문항에서 **1위가 「가장 가까운 것」이 아닌 경우는 32건(25%)**이지만, 그건 RRF의 정상 동작이다
(lexical 신호를 반영하니까). 문제 삼을 만한 것 — **외부가 1위인데 더 가까운 우리 문서가 아래** — 은
**1건**(「분석할 로그 올리려면?」)뿐이었다. 사례 하나로 방향을 잡을 뻔했다.

### 정정 ② — 「문서 공백」도 대부분 아니었다

제품 사용법 4건을 매뉴얼에서 직접 찾아보니 **3건은 절차가 적혀 있었다**:

| 질문 | 매뉴얼에 | |
|---|---|---|
| 사용자 추가하려면? | **있음** (사용자매뉴얼 332행) | 검색이 못 찾음 |
| 누가 뭘 지웠는지? | **있음** (감사 3곳) | 검색이 못 찾음 |
| 중복된 문서 있어? | **있음** (실무매뉴얼) | 검색이 못 찾음 |
| 이 문서 등급 바꾸려면? | **없음** | 진짜 공백 |

### ▶ 진짜 원인 — 후보 풀 10개 vs 타사 PDF 청크 695개

```
memory.ts:827   const candidates = Math.max(topK * 2, 10);   // topK=4 → 후보 10개
```

| 문서 | 청크 |
|---|---|
| `Tenablesc_API.pdf` | **695** |
| `GIJO_AS_사용자_매뉴얼.md` | **30** |

**타사 PDF 하나가 후보 10칸을 채운다.** 우리 문서가 후보에 못 들어오면 `ORIGIN_BOOST`를 걸 대상이
없다 — **부스트는 후보 안에서만 작동한다.** ①ⓑ 배포 뒤에도 이 건들이 남은 이유가 이것이다.

재현(4개 질문, 제품 기본값은 topK=4):

```
topK 4·5  →  우리 문서 **없음**, 타사가 1위
topK 8 이상 →  우리 문서가 **1위**       ← 답을 가지고 있으면서 못 찾고 있었다
```

### ▶ 고칠 자리는 topK가 아니라 candidates다

`.slice(0, topK)`가 융합·부스트 **뒤**에 온다. 그러니 **후보만 넓히고 내보내는 건 4개로 두면**
프롬프트가 안 커진다(현행 구조 그대로, 상수 하나).

| 후보 풀 | 프롬프트 | 외부 1위 | 우리 문서가 후보에 없음 | 질문당 지연 |
|---|---|---|---|---|
| **10 (지금)** | 4 | **20 (15%)** | **17 (12%)** | 22ms |
| **16** | 4 | **12 (9%)** | **9 (7%)** | 19ms |
| 24 | 4 | 12 (9%) | 9 (7%) | — |
| 32 | 4 | 10 (7%) | 7 (5%) | 20ms |

> **`candidates = Math.max(topK * 2, 10)` → `Math.max(topK * 4, 16)`**
> 외부 1위 **20 → 12건(40% 감소)**. 프롬프트 그대로, 지연 그대로(노이즈 수준).
> ①ⓑ(부스트)가 28→20이었으니 **이쪽이 더 크다.** 16에서 대부분 얻고 24는 같다.

⚠ 이 값은 Mac 복원본(문서 40건·타사 PDF 다수) 기준이다. 운영은 문서 구성이 다르니 **각각 재야 한다.**

### 부작용도 쟀다 — 우리 문서를 놓치는 경우는 0건

넓히면 상위 4가 「가장 가까운 것」을 못 담는 경우가 는다. 그게 손실인지 목적인지를 갈랐다.

| 후보 풀 | 상위4 평균거리 | 기준 최근접이 상위4에 | **내장 문서를 놓친 것** | 외부 1위 |
|---|---|---|---|---|
| **10 (지금)** | 0.8318 | 92% | **0건** | 20 |
| **16** | 0.8381 | **89%** | **0건** | **14** |
| 32 | 0.8415 | 85% | **0건** | 13 |

**밀려난 최근접은 전부 내장이 아닌 문서였다** — Tenable·안전대부·위협동향 매뉴얼이 우리 지식에
자리를 내준 것이라 **목적한 바**다. 평균 거리가 멀어지는 것도 같은 이유(부스트 받은 문서가
거리로는 조금 멀다)라 결함이 아니다.

**32는 얻는 게 1건 더인데(14→13) 이탈이 4%p 더 는다 — 16이 맞다.**

⚠ **측정 함정 하나** — 처음엔 「100% → 93%」로 나와 부작용이 큰 줄 알았다. `topK=4`면
`queryMemoryScored`가 **4건만 돌려주므로** 「최근접」이 그 4건 안에서 계산돼 **필연적으로 100%**가
된다. 분모가 달라 비교가 안 됐던 것이다. 위 표는 **최근접을 항상 넓은 풀(K=16)에서 정해** 분모를
맞춘 값이다. 설정을 바꿔 가며 잴 때는 **기준을 먼저 고정하라.**
