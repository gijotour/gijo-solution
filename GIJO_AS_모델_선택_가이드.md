# 모델 선택 가이드 — 우리 장비에 맞는 AI 구성 고르기

GIJO AS는 온프렘 제품이라 **이 서버의 GPU 메모리(VRAM) 안에서** AI가 동작합니다.
장비 등급별 권장 구성입니다. 적용(동시 모델 수·문맥 길이)과 모델 받기는 **설정 > 서버·AI**에서 합니다.

## 등급별 권장 구성

### Lite — 12GB급 (RTX 3060/4060 등)
- **7B 단일 + 사내 근거(RAG)**: 보안 특화 7B 모델 1개에 사내 자료 검색을 붙여 답합니다. 빠르고 가볍습니다.
- 1인 담당자, 엔트리 GPU. 대부분의 보안 질의에 충분합니다.

### Standard — 24GB급 (RTX 3090/4090 등)
- **A. 7B 2개 병렬**: 총괄과 팀원이 서로 다른 모델로 동시에 일합니다. 반응이 빠릅니다.
- **B. 14B 단일(고품질)**: 강한 모델 1개가 전 역할을 맡습니다. 답이 더 간결·안정적이지만 2~3배 느리고 병렬은 포기합니다.
- 2~3인 팀. 반응 속도가 우선이면 A, 품질이 우선이면 B.

### Pro — 32GB급 이상 (A6000/L40/다중 GPU)
- **7B 3개 병렬**: 팀원 다수를 동시에 굴립니다. 관제(SOC) 팀 규모에 적합합니다.
- **14B/32B 단일·대문맥**: 최고 품질 노선. 32B는 문맥이 짧아지고 가장 느립니다.

## 💡 핵심 — 큰 모델보다 "사내 근거"가 답 품질을 좌우합니다

GIJO AS는 사내 자료를 근거로 답하도록(RAG 그라운딩) 설계돼 있습니다. 실측 결과 **7B도 근거만 있으면 정확**했고, 14B로 키워도 근거가 없으면 똑같이 틀립니다. 큰 모델은 속도·병렬을 내주고 품질을 조금 더 얻는 선택이지 필수가 아닙니다 — 대부분의 고객은 현재 등급의 7B 구성으로 충분합니다.

## 어디서 바꾸나

- 모델 검색·받기·팀원별 배정: **설정 > 서버·AI**
- 지금 무엇을 쓰는지 보기: **AI 메뉴(팀 판) > 팀원별 담당 모델**
- 동시 모델 수·문맥 길이(구동 등급): **설정 > 서버·AI > 구동 티어**
