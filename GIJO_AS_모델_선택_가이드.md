# 모델 선택 가이드 — 우리 장비에 맞는 AI 구성 고르기

GIJO AS는 온프렘 제품이라 **이 서버의 GPU 메모리(VRAM) 안에서** AI가 동작합니다.
장비 등급별 권장 구성입니다. 적용(문맥 길이·권장 사양)과 모델 받기는 **설정 > 서버·AI**에서 합니다.

> **지금 기본 구성** — 기반 모델 **한 개**(공식 저장소 `Qwen/Qwen3-14B-GGUF`) + **주제별 전문가 어댑터(LoRA)**.
> ⚠ 조직명(`Qwen/`)을 빼고 이름만으로 검색하지 마세요 — 안전장치를 일부러 뜯어낸 개조본이 먼저 나옵니다.
> 받으실 때는 **설정 > 서버·AI > 모델 받기**의 권장 목록에서 고르세요 — 이 목록은 조직명까지 고정돼 있습니다.
> 예전에는 7B 여러 개를 병렬로 굴리는 구성을 기본으로 안내했지만, 실측에서 **한 개를 잘 쓰는 쪽이
> 더 나았습니다**(아래 「왜 병렬을 접었나」). 물론 다른 모델을 쓰실 수 있습니다 — 아래 BYOM 참고.

## 등급별 권장 구성

| 등급 | VRAM | 권장 | 비고 |
|---|---|---|---|
| Lite | 10GB급 | **7~8B 한 개** + 사내 근거(RAG) | 문맥 8K. 14B가 안 올라갑니다. 긴 문서 요약·여러 명 동시 사용에 제약이 있고, 표준 기능 전량은 24GB급부터입니다 |
| Standard | 24GB급 (RTX 3090/4090) | **14B 한 개** + 어댑터 · 임베딩 상주 | 문맥 32K. **지금 운영 구성**입니다 |
| Pro | 48GB급 (A6000/L40/다중 GPU) | 14B 한 개 + 어댑터 여러 장 | 문맥 32K — ⚠ 지금은 Standard와 엔진 동작이 같습니다(문맥 길이가 같습니다). VRAM이 넉넉해 모델이 여러 개 함께 올라가 있을 뿐입니다 |
| Max (관제용) | 사양 확정 전 | 자동 작업 전용 두뇌를 따로 둠 | **준비 중인 등급이라 아직 고를 수 없습니다.** 야간 회귀·자동 스캔·리포트를 담당자 대화와 갈라 놓는 것이 목표이고, 필요 사양은 확정되면 여기에 적습니다 |

어느 등급이든 **임베딩 모델(bge-m3)이 따로 상주**합니다(사내 자료 검색용). VRAM 계산에 함께 넣으세요.

> ⚠ **모델을 몇 개 올려 두는지는 등급이 아니라 「지금 남은 VRAM」이 정합니다.** 등급이 정하는 것은
> 문맥 길이와 권장 사양입니다. 새 모델을 올릴 자리가 모자라면 오래 안 쓴 모델부터 내리기 때문에,
> VRAM이 넉넉하면 Standard에서도 여러 개가 함께 올라가 있습니다.

## 왜 「7B 여러 개 병렬」을 접었나

세 가지가 실측으로 확인됐습니다.

1. **GPU가 하나면 병렬이 아니다.** 모델을 여러 개 올려도 계산은 차례로 돕니다. 동시에 굴리면
   서로 GPU를 뺏어 오히려 답이 멈추는 일이 있었습니다.
2. **모델을 갈아 끼우는 비용이 크다.** VRAM이 모자라면 쓰지 않는 모델을 내리고 다른 것을 올리는데,
   그때마다 수십 초가 사라집니다.
3. **역할을 나누는 데 꼭 모델이 여러 개일 필요가 없다.** 지금은 기반 모델 하나에
   **주제별 어댑터(LoRA)**를 갈아 끼웁니다 — 모델 전체를 새로 올리지 않고 얇은 덧옷만 바꾸는 방식이라
   메모리도 시간도 훨씬 적게 듭니다.

## 전문가 어댑터(LoRA)란

기반 모델은 그대로 두고, **우리 현장 문답만 얇게 덧입힌 파일**입니다(수백 MB). 취약점·장비운영처럼
주제별로 만듭니다.

- 만들려면 그 주제의 **승인된 문답이 300건** 필요합니다(개시선). 미달인데도 강행하면 그 사실이
  어댑터 기록과 작업 기록에 남습니다 — 나중에 "이건 재료가 모자란 상태로 만든 것"임을 알 수 있게.
- 만들었다고 바로 쓰지 않습니다. **등록 ≠ 채택**입니다. 평가 게이트와 A/B 비교를 통과해야 채택합니다.
- 어디서 보나: **설정 > AI > 🤖 AI 팀**의 「팀원별 담당 모델」(전문 분야 배지), **설정 > 서버·AI > 전문가 어댑터 등록부**

## 💡 핵심 — 큰 모델보다 "사내 근거"가 답 품질을 좌우합니다

GIJO AS는 사내 자료를 근거로 답하도록(RAG 그라운딩) 설계돼 있습니다. 실측 결과 **근거만 있으면
작은 모델도 정확**했고, 모델을 키워도 근거가 없으면 똑같이 틀립니다. 모델 등급을 올리기 전에
**우리 문서가 지식 저장소에 제대로 들어가 있는지** 먼저 보시는 편이 이득이 큽니다.

## 다른 모델을 쓰고 싶다면 (BYOM)

고객이 직접 고른 GGUF 모델을 올릴 수 있습니다. 올리면 제품이 **자동으로 맞춥니다** —
생각 과정을 뱉는 모델(thinking)은 그 부분을 감추고, 문맥 길이는 VRAM에 맞춰 줄입니다.
적응 결과는 화면의 **대화창에 「모델 적응 상태」라고 물으면** 알려 줍니다 — 무엇을 맞췄는지(생각 모드·문맥 길이)와 간단한 동작 확인 4문항 결과를 함께 보여 줍니다.

> ⚠ 모델마다 **라이선스**가 다릅니다. 상업적 사용이 막힌 모델이 있으니(예: 일부 한국어 특화 모델)
> 도입 전에 확인하세요. 제품은 라이선스를 대신 판단하지 않습니다.

## 속도에 관해 알아 둘 것

- 답이 길수록 오래 걸립니다(초당 15~20토큰). 화면에서 읽는 답은 상한을 걸어 두었습니다.
- **한동안 쉬었다가 던진 첫 질문**이 느렸던 문제는 2026-08-09에 고쳤습니다(상태 점검이 프롬프트
  캐시를 밀어내던 것 — 확인용 칸을 따로 줘서 해결). 지금은 첫 질문도 몇 초 안에 옵니다.

## 어디서 바꾸나

- 모델 검색·받기·팀원별 배정: **설정 > 서버·AI**
- 지금 무엇을 쓰는지 보기: **설정 > AI > 🤖 AI 팀**의 「팀원별 담당 모델」
- 문맥 길이·권장 사양(구동 등급): **설정 > 서버·AI > 구동 티어**
- 어댑터 만들기·채택: **설정 > AI > 🎓 학습**(학습 후보함 · 학습 루프)
