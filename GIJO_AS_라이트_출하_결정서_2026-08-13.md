# GIJO AS 라이트 — 출하 결정서 (사장님 결재용)

작성 2026-08-13 · `win` · 계획서 **전-7(제품화)**
관계 문서: `GIJO_AS_라이트_올인원_배포_계획.md` · `GIJO_AS_라이트_사양_2026-08-10.md` · `GIJO_AS_Lite_계획서_2026-08-12.md`

---

## 0. 왜 셋을 한 장에 묶었나

지금 결재를 기다리는 것이 셋인데 **서로가 서로를 정합니다.** 따로 정하면 하나를 정할 때마다 앞의 것이 뒤집힙니다.

```
ⓐ 어떤 기계를 대상으로 하나  →  ⓒ 모델을 얼마나 큰 걸 넣을 수 있나
ⓒ 모델을 동봉하나            →  「받자마자 혼자 돈다」가 지켜지나
ⓑ llama-server를 넣나        →  (안 넣으면 위 둘이 무의미 — 대화가 안 된다)
```

---

## ⓑ 먼저 — llama-server 동봉: **넣어야 합니다. 논쟁거리가 아닙니다**

지금 라이트 dmg(191MB)에는 **llama-server가 없습니다**(`max` 실물 확인). 받은 고객은 **화면은 다 보이는데 대화가 안 됩니다.**

본 제품은 서버 담당자가 따로 깔지만 **라이트는 담당자 PC에 혼자 도는 제품**입니다. 대신 깔아 줄 사람이 없습니다.

| | 크기 |
|---|---|
| llama.cpp 실행부 (CUDA 포함) | **87MB** — 이 중 `libggml-cuda.so`가 64MB |
| llama.cpp 실행부 (Metal) | CUDA 라이브러리가 빠져 **더 작다** — ⚠ `max` 실측 필요 |

▶ **87MB는 싸다.** ⓐⓒ가 어떻게 정해지든 이건 넣습니다.
▶ 결정할 것은 **넣느냐**가 아니라 **어느 빌드를 넣느냐**입니다 — mac은 Metal, win은 CUDA. 플랫폼마다 다른 물건이라 **배포본이 갈립니다**(이미 dmg/nsis로 갈려 있어 추가 부담은 없습니다).

---

## ⓐ 대상 사양 — **배포 계획 §2가 실측과 어긋납니다**

배포 계획 §2는 이렇게 못 박고 있습니다:

> | **Windows + NVIDIA GPU** | **VRAM 8GB** (RTX 3050/4060급) |
> 근거: 라이트 실측 **6.48GB(CUDA)**

### 2026-08-13 실측 (`win` · RTX 3090 전용VRAM · WSL 운영환경 · llama.cpp `571d0d5`)

| 항목 | 계획서 근거 | 실측 | 어떻게 쟀나 |
|---|---|---|---|
| 채팅 (ctx 8192) | — | **5.58 GiB** | `nvidia-smi` 총 사용량 델타 |
| 임베딩 bge-m3 | — | **1.25 GiB** | 같은 방법 · 표값 2.5의 절반 |
| 소계 | 6.48 | **6.83** | |
| **화면(dwm 등)** | **안 셈** | **~0.70 GiB** | Windows 성능 카운터 프로세스별 전용 사용량 |
| **합계** | 6.48 | **7.53** | |

**8GB 카드에 넣으면** (카드 총량 8,188MiB ≈ 7.99 GiB — ⚠ 이 값은 일반적인 8GB 카드 제원에서 온 것이지 8GB 카드에서 직접 잰 것이 아닙니다):

```
7.53 / 7.99 = 총량의 94%.  남는 것 0.46 GiB
```

### ★ 핵심 — **RTX 3050/4060은 「화면을 그리는 카드」입니다**

담당자 PC에 단독으로 도는 제품이니 **모니터가 물려 있는 것이 정상**입니다. 계획서가 고른 바로 그 카드가 가장 위험한 구성입니다. 브라우저 하나만 열어도 화면 몫이 1.0GiB로 올라 넘칩니다.

> ⚠ **원래 「10GB」 판정은 옳았는데, 옳은 이유로 옳은 게 아니었습니다.**
> 막던 것은 부풀린 임베딩 표값(2.5)이 아니라 **아무도 안 세던 화면 몫**입니다.
> 임베딩 표값이 절반으로 내려가는 순간 8GB가 열릴 뻔했습니다 — 틀린 이유로 맞은 답을 갖고 있으면, 그 이유가 무너지는 날 답도 같이 무너집니다.

### 선택지

| | 대상 | 결과 |
|---|---|---|
| **ⓐ-1 (권함)** | **10GB 이상 · 화면 그려도 됨** | 안전. 대상 기계가 줄어듦(RTX 3060 12GB·4070급) |
| ⓐ-2 | 8GB **단, 화면을 안 그리는 카드**(둘째 GPU·헤드리스) | 들어감. ⚠ 담당자 PC에 그런 구성은 **드뭅니다** |
| ⓐ-3 | 8GB 그대로 밀고 감 | ⚠ **권하지 않음.** 94%로 돌다 순간 피크에 죽습니다. 담당자는 이유를 모릅니다 |

▶ **판정을 두 겹으로 가르는 것**도 가능합니다: 화면을 그리는 GPU면 10GB, 안 그리면 8GB. 코드에서 그 구분이 가능한지는 **아직 안 봤습니다**(추가 조사 필요).

### ⚠ 곁가지 — 여유계수가 구조적으로 틀렸습니다

`여유계수 0.95`는 「OS·표시장치·순간 피크」를 덮으라고 둔 값인데, 실측하니 그 몫은 **기계 크기와 거의 무관하게 ~0.7 GiB로 고정**입니다.

| 총량 | 5% 예약 | 실제 필요 | |
|---|---|---|---|
| 8GB | 0.40 | ~0.70 | **모자람** |
| 24GB | 1.20 | ~0.70 | 맞음 |
| 48GB | 2.40 | ~0.70 | 과함 |

**작은 기계일수록 덜 예약하는데, 정작 작은 기계가 위험합니다.** 제안: `총량 − 1.0GB − 총량×0.02`.
⚠ 「티어표는 표준 제품 완성 후 다시 본다」는 결정이 서 있어 **손대지 않았습니다.** 그때 함께 보시면 됩니다.

---

## ⓒ 모델 동봉 vs 첫 실행 다운로드

| | 크기 |
|---|---|
| 현재 라이트 dmg | 191MB |
| + llama-server | +87MB (Metal은 더 작음) |
| + 채팅 모델 `gijo-main-orchestrator` | **+5.1GB** |
| + 임베딩 모델 `bge-m3` | **+1.1GB** |
| **전부 동봉** | **약 6.4GB** |

| | 설치본 | 오프라인 | 첫 실행 |
|---|---|---|---|
| **ⓒ-1 전부 동봉** | 6.4GB | ✅ 됨 | 바로 씀 |
| **ⓒ-2 모델만 다운로드** | 약 280MB | ❌ 안 됨 | 6.2GB 받는 동안 대기 |
| **ⓒ-3 채팅만 동봉, 임베딩은 받기** | 5.3GB | 반쪽 | RAG만 나중 |

### 무엇이 걸리나

라이트 사양서의 정의가 **「클라만 받아도 그 PC에서 혼자 돈다」**입니다.

- **ⓒ-2는 그 정의를 깹니다.** 폐쇄망 고객은 아예 못 씁니다 — 그런데 이 제품의 고객이 대개 폐쇄망입니다.
- **ⓒ-1은 정의를 지키지만 6.4GB**입니다. 내려받기·USB 전달·설치 시간이 다 커집니다.
- **ⓒ-3은 어중간합니다** — RAG가 라이트 화면 9개 중 6개의 재료라, 임베딩이 없으면 반쪽입니다.

▶ **권함: ⓒ-1(전부 동봉).** 이유는 용량이 싸서가 아니라, **ⓒ-2가 제품의 정의를 깨기 때문**입니다. 6.4GB는 불편이고, 오프라인 불가는 **못 파는 것**입니다.
▶ 대안: 동봉본과 경량본 **둘 다 내는 것**도 가능합니다. ⚠ 배포본이 둘이 되면 QA도 둘입니다.

---

## 1. 정리 — 결재해 주실 것

| | 항목 | 권하는 것 | 안 정하면 |
|---|---|---|---|
| ⓑ | llama-server 동봉 | **넣는다**(87MB) | 받은 고객이 대화를 못 함 |
| ⓐ | 대상 사양 | **10GB 이상**(ⓐ-1) | 8GB 고객이 94%로 돌다 죽음 |
| ⓒ | 모델 | **전부 동봉**(ⓒ-1, 6.4GB) | 폐쇄망 고객에게 못 팜 |

## 2. 이 문서가 재지 **않은** 것 (정직하게)

- **8GB 카드에서 직접 안 쟀습니다.** 3090(24GB)에서 재고 8GB 제원으로 계산했습니다. 실제 8GB 카드는 드라이버 예약이 달라 더 나쁠 수 있습니다.
- **Metal llama-server 크기**를 안 쟀습니다(`max` 몫).
- **화면 몫 0.7GiB는 이 PC 기준**입니다. 해상도·모니터 수에 따라 달라집니다 — 4K 듀얼이면 더 큽니다.
- **화면을 그리는 GPU인지 코드에서 구분 가능한지** 안 봤습니다(ⓐ 두 겹 판정의 전제).

---

## 3. 채택 및 실측 회신 (max, 2026-08-13 03:20)

**사장님 지시 「전부 진행, 문제가 있으면 추천안으로」(2026-08-13)에 따라 권하는 안 셋을 그대로 채택·집행했다:**

| | 채택 | 집행 |
|---|---|---|
| ⓑ | llama-server 동봉 | ✅ mac(Metal) 동봉 완료 — `tools/stage-llama-metal.sh`로 꾸림 |
| ⓐ | 10GB 이상 | ✅ 라이트 기본 모델을 `gijo-main-orchestrator`로 고정(`lite/env.ts`) — qwen3-14b는 화면 몫까지 재면 안 들어감 |
| ⓒ | 전부 동봉(ⓒ-1) | ✅ 채팅 5.1GB + 임베딩 1.1GB 동봉 |

### 이 문서가 안 쟀다고 한 것들의 실측 (mac 쪽)

- **Metal llama-server 크기: 17MB** (실행부 33KB + dylib 9개). CUDA 87MB의 1/5.
  ⚠ 개발 빌드의 rpath가 절대경로라 그대로 동봉하면 고객 기계에서 죽는다(개발 기계에선
  거짓 통과) — rpath를 `@executable_path`로 바꾸고 ad-hoc 재서명해 동봉했다.
- **mac dmg 최종 크기: 6.2GB** (문서의 6.4GB 추정과 부합 — Metal이 작아 조금 덜하다).
- **「받자마자 혼자 돈다」 실증**: 새 userData로 dmg 앱을 켜서 —
  설정 화면 → 계정 생성 → 동봉 bge-m3로 임베딩 서버 자동 기동 → **gijo-main-orchestrator
  자동 시작** → 지식 묶음 자동 적용(트리플 1,999·문서 19) → 라이트 도구 13개 확인(today 없음)
  → 동봉 모델이 실제 한국어 답 생성. 개발 기계의 서버·임베딩을 전부 내리고 쟀다.
- 서명 봉인: 동봉 후에도 `codesign --verify --deep --strict` 통과.

### 남은 것 (win 쪽)
- Windows(nsis)에 CUDA 빌드 동봉 — 같은 rpath류 함정이 있는지 확인 필요(Windows는 DLL 옆자리 규칙이라 다를 것)
- 8GB 최종 판정용 CUDA 재측정(`--parallel` 빼고)
