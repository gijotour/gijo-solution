# GIJO AS — 클라이언트 설치 → 로그인 → 제품 UI 여정 점검 · 변경안 보고서 (2026-09-01 착수 · 2026-09-02 1차 초안)

> **요청**: "사용자 관점에서 클라이언트 설치부터 제품 로그인·제품 UI 모두 점검. GB10을 이용해서 코드는 병행으로 보고. 변경안 보고서."
> **계획서 관계**: 설치·로그인·첫 화면·문구 = **전-7(보여 주기 — 첫인상)** · 시연 자료 = **전-1** · 실사용 불편 = **중-1** · 설치·운영 안정·문서 정합 = **후-1(GA 관문)**. GB10은 계획서 전-7 하단에 **「편입 완료」**로 이미 적혀 있다(§4-1 참조) — 클라우드 세션이 옛 코드(08-09)에서 세운 「계획서에 없다」는 전제는 낡았다.
> **이 문서는 변경안(제안)이다.** 코드는 한 줄도 고치지 않았다. 화면 모양이 바뀌는 항목은 규칙대로 **추천 시안 1개 → 승인 → 구현** 순서를 따른다.
> **상태: 1차 초안.** 손으로 확인한 것과 GB10 실측만 담았다. 병행 워크플로 3개(여정 8갈래·GB10 3갈래·문서 81편 통독)는 **2026-09-02 10:30 실행에서 전부 실패**했다 — 코드 문제가 아니라 Claude 사용량 한도(「session limit · 8:10am 초기화」, 34개 에이전트 결과 0건). 재실행 뒤 §2·§3·§4-3·§6이 채워진다(§1-3).

## 0. 한 장 요약

- **로그인 화면이 첫인상에서 가장 약하다.** 앱 본체와 다른 팔레트, 서버 연결 실패와 비밀번호 오류를 같은 문구로 뭉갠 것, 관리자 2차 인증 「등록 필요」 응답을 화면이 처리하지 않는 것 — 세 가지가 **현재 허브 코드(클라 5.83.0)에도 그대로** 있다.
- **설치 문서가 제품보다 세 대(大)버전 뒤다.** 설치 가이드 v2.1.0(설치본 2.1.0)·배포 가이드 v2.5.0 vs 클라 5.83.0. 고객에게 손으로 건네는 문서라면 첫날부터 어긋난다.
- **GB10 실측: 서버 코드는 통합메모리를 이미 안다.** `nvidia-smi memory.free`가 `[N/A]`임을 다시 확인했고, 허브의 `unifiedmem.ts`가 이를 「GPU 없음」이 아닌 「통합메모리」로 갈라 놓았다. GB10에는 지금 제품 서버(4000)와 llama-server 3개가 실제로 돌고 있다.
- **결정이 필요한 것 2건**(§5): ① 로그인 화면 손질을 전-7로 넣을지 ② 설치·배포 가이드를 5.x 기준으로 다시 쓸지(문서 갱신은 요청 시만).

## 1. 어떻게 점검했나 — 지어내지 않았다

### 1-1. 손으로 확인 (클라우드 09-01 관찰 9건 → 허브 최신 코드에서 2026-09-02 재확인)
| # | 관찰 | 어디 | 허브 최신(5.83.0) 재확인 |
|---|---|---|---|
| 1 | 로그인 화면이 gijo-ui.css를 안 쓰고 자체 팔레트를 복붙(`--navy2` 선언만 있고 미사용) | `client/src/renderer/pages/login.html` `<style>` 31~32행·221행 | **그대로** — gijo-ui.css 링크 0건, `var(--navy2)` 사용 0건 |
| 2 | 서버 주소 `<input>`에 `/ aria-label=` 꼴의 어색한 표기 | `login.html` 309행 | **그대로**(HTML 파서가 홀로 선 `/`를 무시해 동작엔 영향 없음 — 정리 대상) |
| 3 | 로그인 실패 문구가 「서버 연결 실패」와 「아이디·비밀번호 오류」를 구분하지 않음 | `login.html` doLogin 554~560행 · `client/src/api/auth.ts` 53행 | **그대로** — api는 `code:"error"`+「서버에 연결할 수 없습니다」를 주는데 화면은 `locked`만 살리고 나머지는 전부 「아이디/비밀번호 또는 서버 주소를 확인하세요」 |
| 4 | `enrollRequired`(관리자 2차 인증 필수 정책 → 등록 전용 제한 세션) 응답을 화면이 처리하지 않음 | `client/src/api/auth.ts` 62행이 돌려주나 `pages/*.html` 0건 | **그대로** — 서버(`server/src/auth/auth.ts` 306~318행)는 등록만 되는 제한 토큰을 주고 그 밖을 403으로 막는다. 화면은 `finishLogin()`으로 그냥 들어가 **막힌 화면을 만난다** |
| 5 | `manual-update.ps1` 주석·안내 문구는 「oneClick 빌드」라 하나 `package.json`은 `oneClick:false` | `client/scripts/manual-update.ps1` 4·82행 · `client/package.json` 89행 | **그대로** — 사용자에게 「조용히 설치된다」고 말하는데 실제는 설치 마법사가 뜬다 |
| 6 | 서버 게시 저장소가 `.exe`만 받는다 — mac dmg·arm64 클라는 인앱 업데이트 경로 없음 | `server/src/engine/clientrelease.ts` 124행 | **그대로**(`GIJO-AS-Setup-*.exe` / `GIJO-AS-Lite-Setup-*.exe` 두 이름만) |
| 7 | 설치 가이드 v2.1.0·배포 가이드 v2.5.0 vs 클라 5.83.0 — 설치본 이름(`GIJO AS Setup 2.1.0.exe`)·「운영중(초록)」 배지·배포 폴더 구조가 낡음 | `GIJO_AS_설치_가이드_v2.1.0.md` 1·4·41~50·83행 · `GIJO_AS_클라이언트_배포_가이드.md` 1행 | **그대로** — 둘 다 `server/docs-manifest.json`(챗봇 RAG 근거 목록)에는 없다 = 내부 문서. 다만 손으로 건네는 고객 문서로 쓰이면 첫날 어긋난다 |
| 8 | git 첫 커밋 날짜가 문서 전부 2026-08-09(일괄 반입) — 「날짜별」은 본문 날짜로만 가능 | `git log --diff-filter=A` | 변동 없음. **루트 md는 81 → 142개**로 늘었다(문서통합 워크플로의 묶음 목록은 81개 기준 — 재실행 전 갱신 필요, §1-3) |
| 9 | `오늘업무_2026-07-13`은 md가 아니라 폴더 | 저장소 루트 | **그대로** |
| 10 | (새로 추가) Windows 설치본에 **코드 서명 설정이 없다** — `afterSign`은 mac 임시 서명만 | `client/package.json` 95행 | **확인됨** — 고객 PC에서 SmartScreen 경고·EDR 차단을 만날 수 있다. 실제 차단 여부는 고객사 환경에서만 확인 가능 |

### 1-2. GB10 실측 (읽기 전용 탐침 `tools/review-2026-09-01/gb10-probe.sh` · 결과 `결과/gb10-probe.txt`)
- 기계: `promaxgb10-1d0a`, Ubuntu 24.04.4, aarch64, CPU 20개, 메모리 121GB(사용 96GB · 가용 25GB · 스왑 7/15GB), 디스크 1.9TB 중 265GB 사용, CUDA 13.0(드라이버 580.173.02).
- **nvidia-smi 쿼리 3종 모두 메모리 칸이 `[N/A]`** — `name,memory.total`→「NVIDIA GB10, [N/A]」·`memory.free`→「[N/A]」·`utilization.gpu,memory.used,memory.total`→「0, [N/A], [N/A]」. 표 출력도 「Not Supported」. 허브 `server/src/util/unifiedmem.ts` 머리주석의 08-11 채취값과 **일치**.
- 돌고 있는 것: 제품 서버 `node dist/index.js`(4000, 11시간 32분째, `/api/health` ok · schema 50) · llama-server 8081(bge-m3 임베딩, ctx/batch/ubatch 8192) · 8080(Qwen3.8-Flash-Next Q3_K_XL 분할 GGUF, 59.6GB) · 8090(qwen3-coder-30b-a3b, 21GB — 절감 도구 두뇌).
- 네이티브 모듈 4종(`better-sqlite3`·`better-sqlite3-multiple-ciphers`·`@lancedb/lancedb`·`onnxruntime-node`) **모두 로드 OK**. llama-server는 `~/gijo-as/server/llama.cpp-next/build/bin/`(aarch64 빌드).
- 저장소 `~/gijo-as`는 브랜치 `claude/hungry-blackwell-cd58b7`(915a71fd — hub/main에 포함, 6커밋 뒤).
- 함정 하나: node는 `~/.local/bin`에 있어 **비로그인 셸(`ssh gb10 'bash -s'`)에서는 PATH에 없다** — 탐침 첫 실행에서 「node 없음」으로 나왔고 `bash -lc`로 다시 재야 했다. 운영 스크립트가 비로그인 셸로 node를 부르면 같은 함정을 밟는다.

### 1-3. 병행 워크플로 3개 — 실행 실패 기록 (정직하게)
| 워크플로 | 에이전트 | 결과 | 원인 |
|---|---|---|---|
| 여정점검(user-journey-review) | 10 착수 | 0건 | 「You've hit your session limit · resets 8:10am」 |
| GB10검토(gb10-code-review) | 5 착수 | 0건 | 같음 |
| 문서통합(docs-consolidation) | 19 착수 | 0건 | 같음 |
- 실행 전 고친 것: `win` 체크아웃에서 autocrlf가 `.claude/workflows/*.js`를 CRLF로 바꿔 Workflow 도구가 「숨은 제어문자」로 거부했다 → `.gitattributes`에 `eol=lf` 규칙 추가(커밋 c3fc131a). 이름(`name:`)으로 부르면 세션 시작 때 읽은 CRLF 사본을 써서 계속 거부되므로 **`scriptPath`로 불러야 한다**.
- 재실행: 2026-09-03 08:10 이후 `Workflow({scriptPath:".claude/workflows/<이름>.js", args:{root:"D:/Connect AI", canRun:true}})`. 문서통합은 묶음 목록(81개)을 현재 142개에 맞게 먼저 갱신할 것.
- 클라우드 세션(as-private-41)이 살아 있으면 그쪽 결과가 `결과/클라우드_*.json`으로 같은 브랜치에 올라온다 — 로컬 결과와 합쳐 쓴다(기각 사유가 다르면 둘 다 적는다).

## 2. 담당자 눈으로 본 여정 (설치 → 로그인 → 첫 화면 → 업무 화면 → 업데이트·세션)

담당자가 겪는 순서대로 여덟 갈래를 나눠 봤습니다. 갈래마다 따로 점검하고, 다른 에이전트가 그 지적을 반박해 살아남은 것만 실었습니다(90건 중 88건 통과, 2건 기각).

| 단계 | 무엇을 봤나 | 확인된 결함 | 그중 급한 것(P1) |
|---|---|---|---|
| **설치** | 설치본을 받아 깔고 처음 켜기까지 | 9 | 5 |
| **로그인** | 서버 주소·아이디·2차 인증 | 10 | 6 |
| **셸·첫화면** | 들어와서 처음 보는 화면 | 10 | 6 |
| **절차허브** | ①발견~⑤보고 업무 흐름 | 11 | 7 |
| **설정·기록·부가** | 설정·기록·부가 기능 | 12 | 5 |
| **업데이트·세션** | 새 버전 받기·세션 끊김 | 12 | 6 |
| **문서** | 매뉴얼·챗봇 근거 문서 | 12 | 7 |
| **여정전반** | 페르소나로 처음부터 끝까지 | 12 | 7 |
| **합계** | | **88** | **49** |

여정을 한 줄로 요약하면 이렇습니다. **설치본을 깔면 매번 「관리자 계정 만들기」부터 묻고**(F1-01), 로그인 칸에는 쓰지 말아야 할 주소가 회색으로 미리 채워져 있으며(F2-01), 들어가면 첫 화면이 매뉴얼과 다른 모습이고(F3-05), 서버가 끊기면 영문 오류가 날것으로 뜹니다(F3-06·F6-07). 담당자가 잘못한 것은 하나도 없는데 막히는 자리들입니다.

## 3. 변경안 — 사용자 여정 (갈래별·우선순위순)

우선순위는 **담당자가 막히는가**로 매겼습니다. P1은 그대로 두면 담당자가 스스로 빠져나오지 못하거나 제품이 거짓을 말하는 것, P2는 불편하거나 헷갈리는 것입니다.

### 설치 — 설치본을 받아 깔고 처음 켜기까지 (9건)

| | 무엇이 문제인가 | 어떻게 고치나 | 계획서 | 크기 | 시안 |
|---|---|---|---|---|---|
| **P1** | 표준 설치본 첫 실행이 매번 「처음 설정(관리자 계정 만들기)」 화면 — 분산 모드 담당자는 빠져나올 길이 없고, 설치 마법사 약속과도 어긋난다 | (2026-08-09 「고객이 관리자 비번을 정한다」·08-11 「아코디언 우회」 결정은 유지하되) ① setup.html의 「로그인 화면으로」 선택을 userData에 파일(예: gijo-setup-choice.txt)로 남기고 main.ts 1548행이 그 파일도 보게 해 다음 실행부터 login.html로 바로 가게 한다 ② 표준 배포본은 갈래 순서를 「이미 GIJO 서버가 있어요」 먼저로 바꾸고 예시 주소 `10.8.0 | 전-7 | M | 필요 |
| **P1** ⏸ | 표준 설치본의 단일 데스크톱 모드는 AI가 절대 뜨지 않는데(llama-server·모델 미동봉) 화면 문구 3곳은 「동봉」·「내려받아 로드하면 된다」고 말한다 | 실물 확인: 이 PC의 %LOCALAPPDATA%\Programs\gijo-as-client\resources\server-dist에 llama-cuda·models·llama.cpp 폴더가 없다(5.82.0 설치본). ① 표준 설치본이 실행부를 안 실을 거면 setup.html 갈래 1과 login.html 힌트에 「⚠ 이 설치본에는 AI 실행부·모델이 없습니다 — 사내 GPU 서버 연결을 권합니다」를 넣고 llm.ts 90 | 전-7 | M | 필요 |
| **P1** ⏸ | 라이트 설치본(1,616MB)은 서버 게시 한계(500MB)를 넘어 08-23에 만든 라이트 채널로 올릴 수 없다 — 그래서 로그인 화면의 라이트 내려받기가 구글 드라이브 외부 링크로 남아 있다 | 실측: D:\Connect AI\client\release-lite\GIJO AS Lite Setup 1.4.0.exe = 1,616.3MB. (2026-08-14 「임시 통로」 결정을 알고 있다 — 그 전제였던 「서버 배포」가 지금은 크기 때문에 막혀 있다.) ① clientrelease.ts 업로드를 express.raw 메모리 버퍼 대신 요청 스트림을 파일로 흘려 쓰는 방식(pipeline → fs.createWriteS | 전-7 | M | — |
| **P1** | 설치 문서의 숫자·경로가 5.83.0 실물과 다르다 — 설치본 247MB(문서 155~160MB)·설치 후 0.9GB(문서 「1GB 여유」)·경로 gijo-as-client(설치 가이드는 gijo-as)·동봉 서버 포트 7446(문서는 4000) | 실측(이 PC): release/GIJO AS Setup 5.83.0.exe 247.2MB · 설치 폴더 %LOCALAPPDATA%\Programs\gijo-as-client 889MB · 설치본+설치 폴더+임시 추출을 더하면 1.2GB 이상. 기존#7(문서 낡음)의 하위 항목이지만 숫자가 틀린 건 따로 위험하다 — 배포 가이드 §2·§3·§6.4·§5.2를 「247MB · 설치 후 약 0.9GB · 최소 여유 2GB · 경로 | 후-1 | S | — |
| **P1** | manual-update.ps1이 「설치된 버전」을 대상 PC가 아니라 클라 소스 package.json에서 읽는다 — 헬프데스크 PC에서 돌리면 「이미 최신」으로 오판하고, 에디션도 안 보낸다 | 현재 버전을 설치 실물에서 읽는다 — `(Get-Item "$env:LOCALAPPDATA\Programs\gijo-as-client\GIJO AS.exe").VersionInfo.ProductVersion`(이 PC 실측 5.82.0.0) 또는 HKCU Uninstall 키의 DisplayVersion; 없으면 「설치본이 없습니다」로 끊는다. `-Edition pro\|lite` 인자를 받아 `&edition=`을 붙이고, 8 | 후-1 | S | — |
| **P2** | 설치 완료 페이지의 WireGuard 인터넷 링크 — 폐쇄망 담당자에겐 열리지 않는 브라우저 창 | 2026-07-25 결정은 「동봉하지 않는다」(상표)이고 인터넷 링크를 두라는 결정은 아니다. MUI_FINISHPAGE_LINK/LOCATION 두 줄을 지우고 완료 페이지 본문(MUI_FINISHPAGE_TEXT)에 「원격 접속(VPN)이 필요하면 관리자에게 WireGuard 설치 파일과 설정 파일을 받으세요」 한 줄만 서술로 남긴다 — 온프렘·오프라인 원칙(④)과 상표 서술적 사용 둘 다 지켜진다. | 후-4 | S | — |
| **P2** | 설치 마법사가 설치 소요 시간·용량·두 운영 모드를 한 마디도 말하지 않는다 | 환영 페이지 마지막 두 줄을 「설치 위치: 내 문서 옆(사용자 폴더) · 약 0.9GB · 1~2분 / 설치 후 관리자에게 받은 서버 주소·계정으로 로그인합니다」로 바꾸고(fix-welcome-text.mjs LINES 배열 수정), 단독 모드를 표준 설치본에서 지원하지 않기로 하면(F1-02) 그 사실도 한 줄 넣는다. | 전-7 | S | — |
| **P2** | 서버 게시 저장소가 .exe 두 이름만 받는다 — mac dmg·arm64는 인앱 업데이트 경로 없음(publish-release도 release/*.exe만 찾음) | 기존#6 그대로 — 변경안 보고서 §3 P3 항목 유지(Mac 인계 보류 중). | 신규 | M | — |
| **P2** | Windows 설치본 코드 서명 없음(이 PC 설치 실물 Get-AuthenticodeSignature = NotSigned) — SmartScreen 「추가 정보 → 실행」 절차를 안내하는 문서도 없다 | 기존#10 그대로 — 서명 도입 검토와 별개로, 배포 가이드 §4에 SmartScreen 화면의 「추가 정보 → 실행」 두 단계와 sha256 대조 방법을 한 줄 넣는 것은 지금 바로 할 수 있다. | 후-1 | S | — |

### 로그인 — 서버 주소·아이디·2차 인증 (10건)

| | 무엇이 문제인가 | 어떻게 고치나 | 계획서 | 크기 | 시안 |
|---|---|---|---|---|---|
| **P1** | 서버 주소 칸이 「비워 두세요」라고 안내하면서 실제로는 localhost:7446이 회색 글자로 늘 채워져 있다 — 분산 모드 첫 로그인에서 잘못 읽힌다 | main.ts가 seed한 기본값은 값이 아니라 **placeholder**로 보이게 하고(칸은 비워 두고 placeholder="예: http://10.8.0.1:4000 — 이 PC에 동봉된 서버면 비워 두세요"), 값이 들어 있을 때는 본문색(var(--text))으로 그려 「입력된 주소」임을 분명히 한다. doLogin에서 칸이 비어 있으면 setServerUrl(기본서버주소)로 **되돌리고**, creds.save에도 | 전-7 | S | 필요 |
| **P1** | 「10.8.0.1:4000」처럼 http:// 없이 주소를 넣으면 fetch가 ERR_INVALID_URL로 터지는데 화면은 「아이디/비밀번호 확인」이라고 말한다 — 설치 첫 화면(setup.html)이 바로 그 형식을 예시로 든다 | core.ts setServerUrl에서 스킴이 없으면 `http://`를 붙이고 `new URL()`로 검증해 잘못된 주소면 즉시 「주소 형식이 틀렸습니다(예: http://10.8.0.1:4000)」를 login.html errMsg에 띄운다(fetch 전에). setup.html 138행 예시는 `http://10.8.0.1:4000`으로 고쳐 매뉴얼 §0.2와 같은 형식으로 맞춘다. 실행 검증: node로 `new UR | 전-7 | S | — |
| **P1** | 로그인 전에 서버가 살아 있는지 확인할 수단이 없다 — 매뉴얼은 「상단 서버 연결 점」을 보라 하지만 로그인 화면엔 ⚙ 하나뿐이고, GIJO가 아닌 서버(404)도 자격증명 오류로 뭉개진다 | 서버 주소 칸 아래에 한 줄 상태를 둔다: 입력이 멎으면(500ms) 무인증 `/api/health`를 불러 `service==="gijo-as-server"`면 「● 서버 연결됨 · 봉인 켜짐/꺼짐」, 아니면 「○ 이 주소에서 GIJO 서버를 찾지 못했습니다」를 보인다(showClockHint가 이미 쓰는 checkServerHealth 재사용). auth.ts loginFailure는 401/409/429가 아닌 상태(40 | 전-7 | M | 필요 |
| **P1** | 「이 PC에 로그인 정보 저장(서버·아이디·비밀번호)」를 해제해도 서버·아이디는 저장돼 최근 목록에 뜬다 — 문구가 거짓이고, 보안 제품인데 비밀번호 저장이 기본 체크다 | 두 가지 중 하나로 문구와 동작을 맞춘다. (권장) 체크박스를 「이 PC에 비밀번호도 저장」으로 바꾸고 **기본 해제**, 서버·아이디 저장은 항상 한다고 별도 한 줄로 밝힌다(「서버·아이디는 빠른 선택용으로 남습니다 — 목록의 ✕로 지울 수 있습니다」). 또는 해제 시 creds.save 자체를 건너뛰어 문구 그대로 동작하게 한다(main.ts creds:save에 savePw=false면 엔트리 미저장 분기). 기본 체크는 | 후-1 | S | — |
| **P1** | 세션이 끊겨 로그인 화면으로 튕길 때 사유(유휴 만료·다른 곳 로그인·서버 재시작)가 사라진다 — 서버는 그 사유 문장을 만들어 두었는데 클라이언트가 응답 본문을 읽지 않는다 | core.ts tryRefresh 실패 분기에서 응답 본문의 `error`/`message`를 읽어 메인 프로세스 authState에 `lastLogoutReason`으로 남기고(auth:setState 확장), login.html이 첫 로드 때 그 값을 errMsg 위 안내 상자로 한 번 보여 준 뒤 지운다: 「다른 곳에서 로그인되어 이 세션은 종료되었습니다 — 본인이 아니라면 관리자에게 알리세요」 / 「30분간 사용하지 않 | 후-1 | M | 필요 |
| **P1** | 로그인 요청에 시간제한이 없어 응답 없는 주소(방화벽 drop·엉뚱한 IP)를 넣으면 「연결 중...」이 수십 초 멈춘 채 취소도 못 한다 | authApi.login/loginMfa의 fetch에 `signal: AbortSignal.timeout(8000)`을 주고, 시간 초과는 `code:"error"`·message 「8초 안에 서버가 응답하지 않았습니다 — 주소·네트워크를 확인하세요」로 돌려준다. login.html은 「연결 중...」 3초 뒤 버튼 옆에 「취소」를 보여 AbortController로 끊을 수 있게 한다. grep 결과 client/src/a | 전-7 | S | — |
| **P2** | 중복 로그인(409) 안내가 「이미 다른 곳에서 로그인 중」만 말하고 어디서·언제인지 없다 — 서버는 IP·마지막 활동 시각을 갖고 있다 | 409 응답에 `since`·`lastSeenAt`·마스킹한 `ip`(예: 10.8.0.*)를 실어 보내고, dupBox 본문을 「10.8.0.* 에서 09:12부터 · 마지막 활동 3분 전. 본인이 아니라면 강제 로그인 대신 관리자에게 알리세요」로 채운다. 비밀번호가 맞은 뒤에만 내려가는 정보라 계정 열람 위험은 늘지 않는다. | 중-1 | S | 필요 |
| **P2** | 관리자가 발급한 임시 비밀번호로 첫 로그인해도 비밀번호 변경을 유도하는 장치가 없다 — 매뉴얼은 「권장」만 적어 둔다 | users 테이블에 `mustChangePassword`(관리자가 만들거나 재설정하면 1, 본인이 바꾸면 0)를 두고 로그인 응답에 실어, login.html finishLogin이 값이 있으면 app.html 첫 탭을 「설정 › 내 계정 › 비밀번호 변경」으로 열고 한 줄 안내(「관리자가 정한 비밀번호입니다 — 본인만 아는 것으로 바꿔 주세요」)를 gijoTell로 띄운다. 강제 차단은 두지 않는다(enrollRequired | 후-1 | M | 필요 |
| **P2** | 키보드·접근성 — 아이디·서버 칸에서 Enter가 무반응(form 없음), 최근 로그인 항목은 div 클릭 전용, 2차 인증 이름줄은 이스케이프 없이 innerHTML | 자격증명 칸을 `<form id="loginForm">`로 감싸 submit에서 doLogin을 부르고(아이디·서버 칸 Enter도 통하게, 조합 중 Enter 가드는 유지), 최근 로그인 항목은 `<button type="button" class="rc">`로 바꿔 Tab·Enter로 고를 수 있게 하며 ✕도 button으로. mfaWho는 이미 있는 `esc()`를 거치게 한다(450행). | 전-7 | S | — |
| **P2** | 복구 코드로 로그인하면 남은 개수를 localStorage에 적기만 하고 아무 화면도 읽지 않는다 — 「남은 개수를 알려야 한다」는 주석과 달리 담당자는 알림을 못 받는다 | app.html의 `로그인선택_못한것알리기`와 같은 자리에서 `gijo:recoveryRemaining`을 읽어 gijoTell로 「복구 코드로 로그인했습니다 — 남은 코드 N개. 설정 › 내 계정에서 2차 인증을 다시 등록하거나 코드를 다시 만들어 두세요」를 한 번 띄우고 지운다(0개면 ⚠로 강조). grep 결과 이 키를 읽는 코드는 client/src 전체에서 0건이다. | 후-1 | S | — |

### 셸·첫화면 — 들어와서 처음 보는 화면 (10건)

| | 무엇이 문제인가 | 어떻게 고치나 | 계획서 | 크기 | 시안 |
|---|---|---|---|---|---|
| **P1** | 프로 첫 화면 「내 문서 + 대화창 나란히」 코드가 도달 불능 — 실제 첫 화면은 빈 대화 홈, 시험은 글자 대조라 거짓 초록 | 사용자 결정을 먼저 받는다 — (a) 08-23 지시대로 살리려면 app.html:1420 프로 분기 안에서 `open("mydocs.html","내 문서")`+핀을 실행하고 1442 분기를 지운다, (b) 지금의 대화 홈을 확정하려면 1442~1451과 주석을 지우고 wiringcontract.test.ts:479~481을 「대화 홈」 검사로 바꾼다. 어느 쪽이든 시험은 정규식 글자 대조가 아니라 실제 boot 경로(도달 가 | 전-7 | S | — |
| **P1** | 챗봇 근거 문서(사용자 매뉴얼·이렇게 쓰면 됩니다)가 옛 셸·옛 메뉴를 설명 — 첫 5분 「화면 안내 보여줘」에 틀린 길을 안내 | 문서 갱신은 「요청 시」 규칙이라 먼저 사용자에게 알린다. 승인되면 매뉴얼 §0.3 표를 5그룹으로, §6 「프로 화면구성」을 「메뉴 = 화면 왼쪽 + 대화 오른쪽(⊟ 화면 접기·💬 손잡이)」로, 이렇게_쓰면의 메뉴 경로 5곳(16·23·24·36·44행)을 현 nav.js 라벨로 고친다. 그때까지는 임시로 docs-manifest에서 두 문서를 빼거나 「기준일 2026-08-08, 메뉴가 바뀌었을 수 있음」 머리말을 넣어 | 전-7 | M | — |
| **P1** | 프로 상단바의 사용자 영역(아바타→내 설정·⬆ 업데이트 칩)이 창 끌기 영역 안에 있어 눌러도 창만 끌린다 | app.html 178행 no-drag 목록에 `.topbar .gtb-userarea` 를 더한다(또는 titlebar.js 40행 .gtb-userarea 규칙에 `-webkit-app-region:no-drag` 추가). 실앱에서 프로 셸 아바타 클릭→내 설정 탭이 열리는지, 상단 빈 자리 끌기는 여전히 되는지 두 가지를 확인한다. | 전-7 | S | — |
| **P1** | 안내된 단축키(Ctrl+K 화면 찾기 · Alt+←/→ · Ctrl+B)가 화면(iframe)에 포커스가 있으면 동작하지 않는다 | main.ts bindZoom의 before-input-event(모든 프레임의 키가 거친다)에서 Ctrl/Cmd+K·Alt+←/→·Ctrl+B를 잡아 `mainWindow.webContents.send("shell:hotkey", …)`로 셸에 넘기고, app.html이 gijoOpenFinder/이력이동/gijoLeftCollapse를 부른다 — 확대·축소가 이미 쓰는 길이라 새 통로가 아니다. nav.js 1667행·ti | 전-7 | S | — |
| **P1** | 대시보드 「사용법 3단계」가 현 셸과 다르다 — 「아래 대화창」(기본은 오른쪽)·「탭으로 열립니다」(프로엔 탭줄이 없음) | dashboard.html 251~253행 문구를 셸 무관하게 참인 말로 바꾼다 — ① 「왼쪽 메뉴에서 화면을 고르면 왼쪽에 열립니다(보던 화면은 그대로)」 ② 「일은 <b>대화창</b>(오른쪽, 접혀 있으면 💬 손잡이)에 시킵니다 — 지금 보는 화면이 곧 지시의 대상입니다」. 표준·프로 두 셸에서 같은 문장이 참인지 실화면으로 확인. | 전-7 | S | — |
| **P1** | 유휴 만료(30분) 뒤 셸이 로그인으로 돌아가지 않고 대화창에 날것 「GIJO AS 서버 오류 401 /api/…」가 뜬다 | core.ts에서 refresh까지 실패해 토큰을 지운 순간 `auth:lost` 신호를 메인으로 보내고 main.ts가 navigate:to("login.html")로 통째 복귀시키며(별도 창 닫기 규칙은 568~576행에 이미 있다), login.html 상단에 「자리를 비워 세션이 끝났습니다 — 다시 로그인해 주세요」 한 줄을 띄운다. 만료 2분 전(칩이 빨강이 되는 시점) 대화창에 「2분 뒤 세션이 끝납니다 — 마우스를 | 중-1 | M | — |
| **P2** | 프로 상단바에 「☰ 전체 메뉴」와 「🔍 화면 찾기」가 나란히 있는데 둘 다 같은 검색 겹판을 연다 | 둘 중 하나로 줄인다 — 프로엔 이미 왼콃 상설 메뉴 판(‖로 접기)이 있으므로 ☰을 없애고 🔍 하나만 남기거나, ☰을 「메뉴 판 펴기(‖와 통합)」로 뜻을 바꾼다. 시안 1개로 상단바 단추 구성을 확정한 뒤 반영. | 전-7 | S | 필요 |
| **P2** | 셸 부팅 시 gijoTell이 아직 없다 — 로그인 선택 실패 알림이 ReferenceError로 프로 레일·끌개·온디맨드 설치를 통째로 막을 수 있다 | app.html 스크립트 목록에 `<script src="dialog.js">`를 console.js 앞에 정적으로 싣고(1764행 부근), 1748·744·1541행의 gijoTell 호출은 `window.gijoTell ? gijoTell(…) : console.warn(…)`로 감싼다. 시험: 셸 boot 순서에서 dialog.js가 정적 로드되는지 wiringcontract에 한 줄 추가. | 신규 | S | — |
| **P2** | 프로 첫 화면(대화 홈 히어로)에 핵심 규칙 「보는 일은 화면, 시키는 일은 대화창」 한 줄이 없고, 첫 메뉴 클릭 뒤 대화창이 40px 손잡이로 접히는 것을 아무도 예고하지 않는다 | 2026-08-31 온디맨드 결정(기본 접힘)은 그대로 두고, 히어로 ce-honest 아래에 한 줄 「보는 일은 왼쪽 화면에서, 시키는 일은 이 대화창에서 — 화면을 열면 대화창은 오른쪽 💬로 접히고 다시 누르면 돌아옵니다」를 넣는다(첫 로그인 1회만 보이게 localStorage 표식). 문구·위치는 시안 1개로 승인. | 전-7 | S | 필요 |
| **P2** | 좁은 창·세로 모니터에서 프로 격자가 하한을 못 지킨다 — 최소 창 900px에서 화면 판이 약 340px, 세로 모니터(1080폭)에서도 약 460px | 2026-08-20 「프로 팝업 자동 생성 폐지」 결정은 유지하고, 폭이 좁을 때(work 폭 < 644+360)만 ① 대화창을 접힘(console-folded) 기본으로 열고 ② 메뉴 판을 자동 접기(pro-menu-closed, 저장 안 함)로 두는 규칙을 대화폭클램프 옆에 더한다. 세로 모니터(1080×1920) 실측 스크린샷을 시안에 붙여 승인. | 전-7 | S | 필요 |

### 절차허브 — ①발견~⑤보고 업무 흐름 (11건)

| | 무엇이 문제인가 | 어떻게 고치나 | 계획서 | 크기 | 시안 |
|---|---|---|---|---|---|
| **P1** | 첫 화면·챗봇 안내가 「보안 태세·자산 요약은 ① 발견·수집에서」라고 말하지만, 그 판 둘은 이미 허브에서 빠졌다 | 세 문구를 현재 판 구성에 맞춘다 — dashboard.html:309는 「관제·위협 인텔은 ① 발견·수집, 보안 점수는 ⑤ 보고(KPI), 자산은 ⓪ 자산 고르기에서」로, screenguide.ts:285(dashboard 「한눈에」)는 「보안 태세 점수는 ⑤ 보고 > 보안 KPI 판」으로, discover.html:59·screenguide.ts:369는 「관제·위협」만 말하게 고친다. grouppanels.js:169-1 | 전-7 | S | — |
| **P1** | 허브 안에서 ⓘ를 누르면 그 판 화면(승인·취약점·하드닝…)의 안내가 아니라 허브 공통 문장만 나온다 | 두 층을 잇는다 — ① grouphub.js show()가 판을 바꿀 때 window.top에 「지금 무대 화면」(예: {type:"gijo:hubStage", page:"approvals.html"})을 알리고 셸이 활성 탭의 무대 화면으로 기억, ② console.js ⓘ·맥락 전송은 무대 화면이 있으면 그것을 screen으로 보낸다. 서버 쪽 대안으로 screenguide.getScreenGuide가 `?panel=` 값 | 전-7 | M | — |
| **P1** | 절차 띠의 큰 숫자에 이름이 없다 — 승인 시안은 「57 자산 · 18 미분류 · 3 진행 · 5 대기」였는데 구현은 숫자만 찍는다 | 서버 WorkflowStage에 `countLabel`(예: ①「자산」 ②「미조치」 ③「진행」 ④「실패 항목」 ⑤「이번 주 보고」)을 한 곳(workflow.ts)에서 정해 내려주고, workflowrail.js:61이 `<b>N</b> 자산` 꼴로 그린다(승인 시안 181-185행 복원). 아울러 ④ 허브 하드닝 판(grouppanels.js:479)에 「실패 항목」·「미점검 대상」 두 줄을 rail과 같은 잣대로 넣어 띠 | 전-7 | S | 필요 |
| **P1** | 허브 판이 「일부를 못 봤다」(확인못함)고 알려도 grouphub.js가 그 표식을 버려 0이 사실처럼 보인다 | grouphub.js summaryHtml에 `if (d.확인못함 && d.확인못함.length) h += '<div class="gh-foot" style="color:var(--amber)">⚠ 일부를 불러오지 못했습니다 — ' + esc(d.확인못함.join('·')) + '</div>'`를 foot 앞에 넣고, 못 본 항목의 숫자 줄은 0 대신 「-」로 바꾼다(hardening 판이면 「등록 장비 -」). 문구는 pan | 신규 | S | — |
| **P1** | ④ 검증 허브는 「고친 것이 닫혔는지 확인하는 자리」라 쓰여 있지만 실제 조치 검증(재스캔·조치 검증 실행·검증 대기)은 전부 ③ 조치의 승인 화면에 있다 | 2026-08-02의 「④ 숫자는 하드닝에서」 결정을 알고 내는 의견이다 — 둘 중 하나로 정합시킨다. (가) ④ 허브에 「🔁 조치 검증 대기」 판을 추가해 approvals.html을 `?status=verifying` 필터로 무대에 열고(approvals.html이 `?finding=` 쿼리는 이미 읽는다:821행), 서버 ④ count도 검증 대기 건수로 바꿔 시나리오 「조치 마감(시작→검증→확정)」이 ③→④로 흐르게 | 전-1 | M | 필요 |
| **P1** | ③ 조치·② 우선순위 무대의 빈 상태 문구가 영어 「finding」을 쓰고, 다음 행동(스캔 결과 올리기)을 없는 자리로 안내한다 | approvals.html:118·445·447·504의 「finding」을 「취약점」으로, 「자산 스캔을 먼저 실행하세요」를 「아직 검토할 취약점이 없습니다 — 아래 대화창 ＋로 스캔 결과 파일(Nessus 등)을 올리면 여기 쌓입니다」로 바꾼다. vulnscan.html:532·364도 「위에서」를 「아래 대화창 ＋로」로 고친다(원칙 ⑥·⑦: 다음 행동을 실제 있는 자리로). | 전-7 | S | — |
| **P1** | 절차 띠 ①의 「오늘 신규」가 UTC 날짜로 계산돼 한국 시간 오전 9시 전에는 어제 스캔이 「오늘」로 잡힌다 | workflow.ts:71을 `util/date.ts todayLocal()`로 바꾸고 lastScannedAt도 로컬 날짜로 잘라 비교한다(maintenance 판·datacard와 같은 잣대). grouppanels.js:680(지식 판 「오늘 반입」)도 같은 꼴이니 함께 고친다. | 전-1 | S | — |
| **P2** | ③ 조치 판 「🛠 정기 점검」을 누르면 무대는 「🔧 유지보수 점검」이라 하고, ④의 하드닝은 스스로를 「원격 정기점검 관제」라 부른다 — 같은 말이 두 화면을 가리킨다 | 이름을 한 벌로 통일한다 — 유지보수는 판·메뉴·정체성 줄 모두 「정기 점검」(maintenance.html:128 「🔧 정기 점검 — …」), 하드닝은 모두 「보안설정 점검」(hardening.html:72 제목·screenguide title). 「정기점검」이라는 낱말은 유지보수에만 쓰고 하드닝 쪽 문구에서 걷는다. | 전-7 | S | — |
| **P2** | 허브 안에서 판을 바꿔도 셸은 모른다 — 탭 이름·주소·대화 맥락이 처음 연 판에 머문다 | grouphub.js show()에서 판이 바뀔 때 `history.replaceState`로 자기 주소의 `panel=`을 갱신하고 window.top에 {type:"gijo:hubStage", panel, page, label}을 보낸다; 셸(app.html)은 활성 탭의 page를 새 `?panel=`로 바꿔 저장(gijo:tabs:v1)하고 탭 이름을 판 제목으로 갈아 준다. F4-02와 같은 배관으로 한 번에 처리한다 | 신규 | M | — |
| **P2** | ⑤ 보고(KPI)·⓪ 자산에서 「자산 관리」로 나가는 링크가 `hub=1`이라, 새로 열린 전체 탭에서 자산 화면의 「한눈에」 그림띠가 숨겨진다 | kpi.html:312·313·493·527과 assets.html:178의 `inventory.html?hub=1`을 `inventory.html?full=1`로 바꾸고, nav.js:288-291 주석(「assets.html이 hub=1로 여는 이유」)도 full=1로 고친다. 같은 주소가 되면 탭 이름도 메뉴 라벨 「자산 관리 (전체)」로 저절로 맞는다. | 신규 | S | — |
| **P2** | 무대 빈 상태 문구가 보이지 않는 자리를 가리킨다 — AI-BOM은 「위 Shadow AI에서 등록」(발견 없으면 숨김), 위협 인텔은 「연동된 피드가 없거나」로 끝난다 | sbom.html:759는 Shadow AI 배너가 보일 때만 그 문장을 붙이고, 아니면 「등록된 AI 자산이 없습니다 — ⓪ 자산 고르기 > 자산 관리(전체)에서 종류를 LLM 서비스·분류 모델로 등록하면 여기 나옵니다」로. threat.html:299는 「… 피드 연결은 위 🔌 MCP 연동(설정 > 연동)에서 합니다」를 덧붙인다. | 전-7 | S | — |

### 설정·기록·부가 — 설정·기록·부가 기능 (12건)

| | 무엇이 문제인가 | 어떻게 고치나 | 계획서 | 크기 | 시안 |
|---|---|---|---|---|---|
| **P1** | 보안제품 빈 상태 안내가 없어진 등록 폼을 가리킨다 | products.html:624 문구를 「아직 등록된 보안제품이 없습니다 — 아래 대화창에 "경계 방화벽 ○○ 등록해줘"처럼 말하면 승인 후 등록됩니다」로 바꾸고, 같은 문장을 screenguide 「제품 등록」 판과 맞춘다. | 전-7 | S | — |
| **P1** | 법령·판례가 꺼져 있을 때 담당자를 「설정 › 연동」으로 보내지만 그 판은 관리자 전용 | lawlookup.html 그리기상태()에서 window.gijo.me()로 역할을 확인해, 담당자에게는 「⚠ 꺼져 있습니다 — 관리자가 설정 › 연동에서 법제처 인증키·도메인을 넣어야 합니다(관리자에게 요청하세요)」로 바꾸고 goSet 링크는 관리자에게만 붙인다. | 전-2 | S | — |
| **P1** | 가드레일 끔/탐지/차단 단추가 담당자에게는 눌러도 아무 일이 없다(403을 삼킴) | redteam.html에서 역할을 읽어 담당자에게는 세그먼트를 disabled로 두고 옆에 「관리자만 바꿀 수 있습니다」를 적으며, catch에서는 gijoTell("바꾸지 못했습니다 — 관리자 권한이 필요합니다")로 이유를 말한다. | 신규 | S | — |
| **P1** | 담당자도 GPU 학습(루프 실행·파인튜닝)을 시작할 수 있어 전 사용자의 채팅이 멈춘다 | 서버 /api/learnloop/run·PUT /api/learnloop/config·/api/finetune/start에 adminMiddleware를 붙이고, learnloop.html·memory.html은 역할을 읽어 담당자에게는 실행 단추 대신 「학습 시작은 관리자만 — 후보 승인(👍)은 여기서 하세요」를 보인다(후보함 승인은 담당자 유지). | 중-4 | M | — |
| **P1** | 에이전트 AI 화면에 제목만 있고 내용이 없는 「모델 받기」 빈 판이 남아 있다 | 판을 지우거나, 안에 「설정 › 서버·AI에서 받습니다 →」 단추 하나(data-page="settings.html?s=ai")를 넣는다. 담당자에게는 모델 받기가 admin 전용이므로 지우는 쪽을 권한다. | 전-7 | S | — |
| **P2** | 유지보수 화면의 「샘플 문서 추가」가 확인 없이 가짜 문서를 전사 지식베이스에 넣는다 | 샘플 문서 단추·SAMPLE_DOCS를 걷고(시연용이면 QA 시드 스크립트로 이동), 151행 소개와 602행 빈 상태를 「대화창 ＋로 매뉴얼·케이스·에러로그를 올리면 여기서 검색됩니다」로 바꾼다. | 전-6 | S | — |
| **P2** | 「파일은 대시보드 ＋로」 안내가 4곳에 남아 있지만 대시보드에 ＋ 업로드가 없다 | 네 곳의 「대시보드 ＋」를 「대화창 ＋」로 고친다(screenguide는 이미 「아래 대화 콘솔의 ＋」로 맞다). | 전-7 | S | — |
| **P2** | 온톨로지 관계 0건 빈 상태가 존재하지 않는 「샘플 지식 불러오기」 단추를 가리킨다 | 문구를 「등록된 관계가 없습니다 — 문서를 올리면 관계가 자동으로 생기고, 기본 지식 번들이 적용되면 표준 관계가 들어옵니다. 아래 「관계 추가」로 직접 넣을 수도 있습니다」로 바꾼다. | 전-4 | S | — |
| **P2** | 학습 설정·파인튜닝 칸이 개발자 용어(HF repo·QLoRA·GGUF·에이전트 ID)로 담당자에게 노출된다 | 루프 설정·파인튜닝 칸을 설정 › 관리자(또는 서버·AI admin 판)로 옮기고, 담당자 화면에는 「지금 학습 두뇌: Qwen3-14B · 산출 이름: sec-expert」 읽기 전용 한 줄만 남긴다. 에이전트 ID 입력은 팀원 이름 select로, 용어 풀이는 screenguide 「학습 루프 4단계」 판에 둔다. | 중-4 | M | 필요 |
| **P2** | 「서버 파일 경로로 수집」 자유 입력칸 — 분산 모드 담당자에겐 없는 경로 | 이 접기 구역을 관리자에게만 보이게 하고(서버 디스크를 아는 사람), 담당자에게는 「문서는 대화창 ＋로 올립니다」 한 줄만 남긴다. 접기 제목에 「(서버 디스크 경로 · 관리자용)」을 붙인다. | 신규 | S | — |
| **P2** | 업무 넘기기 2단계 제목이 「문서 올리기」인데 올리는 칸이 없고, 진행 상태가 PC별 localStorage에만 남는다 | 제목을 「2. 문서 담기 — 파일은 대화창 ＋로 올린 뒤 여기서 고릅니다」로 바꾸고, 체크리스트·담은 문서를 서버의 사용자별 상태(app_state 또는 handover 테이블)로 옮겨 계정을 따라다니게 한다. | 전-1 | M | — |
| **P2** | 시스템 로그 줄마다 영문 갈래 식별자(finetune·hf-download·modelscan)가 그대로 찍힌다 | lineHtml에서 `${src}` 대신 `${갈래이름(src)}`를 쓴다(색 클래스는 그대로 영문 키 사용). | 전-7 | S | — |

### 업데이트·세션 — 새 버전 받기·세션 끊김 (12건)

| | 무엇이 문제인가 | 어떻게 고치나 | 계획서 | 크기 | 시안 |
|---|---|---|---|---|---|
| **P1** | 세션이 밀리거나(중복 로그인) 만료·서버 재시작으로 끊겨도 화면은 그대로 — 사유는 버려지고 「서버 오류 401」만 남는다 | core.ts tryRefresh가 refresh 실패 응답의 error 코드(session_superseded·session_idle_expired·invalid refresh token)를 읽어 메인 프로세스에 「끊긴 사유」로 남기고, 최상위 프레임(app.html)이 그 신호를 받아 곧바로 `login.html?reason=superseded\|idle\|restart`로 보낸다. login.html은 reason별로 한 줄 | 중-1 | M | 필요 |
| **P1** | 「다운로드·무결성 검증 완료」라고 말하지만 클라이언트는 sha256을 대조하지 않는다 | main.ts downloadToFile에서 스트림에 crypto.createHash("sha256")를 함께 물려 받은 뒤 latest-release의 sha256과 비교하고, 다르면 파일을 지우고 「받은 파일이 서버 게시본과 다릅니다(손상 가능) — 다시 시도하세요」로 실패시킨다. update:install 인자에 기대 sha256을 함께 넘기거나 설치 직전 latest-release를 한 번 더 조회한다. 대조가 들어가기 | 후-1 | S | — |
| **P1** | 업데이트 확인이 실패하면(서버 다운·401·500) 「최신 버전입니다」로 표시된다 | update:check가 실패 종류를 돌려주게 한다(`{ latest:null, updateAvailable:false, error:"unreachable"\|"unauthorized"\|"server" }`). settings.html checkUpdate는 error가 있으면 「확인하지 못했습니다 — 서버에 닿지 않습니다/로그인이 풀렸습니다」(회색)로 그리고, `latest===null && !error`일 때만 「게시된 배포 | 중-1 | S | — |
| **P1** | ⚙ 「앱 재시작」은 app.exit(0)로 나가 로그아웃을 건너뛴다 — 다시 켜면 자기 유령 세션에 「이미 다른 곳에서 로그인 중」으로 막힌다 | app:restart 핸들러를 `await logoutOnQuit(); app.relaunch(); app.exit(0);`으로 바꾼다(logoutOnQuit은 3초 타임아웃이 있어 서버가 죽어 있어도 멈추지 않는다). 동봉 서버 모드에서는 logoutOnQuit이 번들 서버까지 죽이므로 재시작 뒤 maybeStartBundledServer가 다시 띄우는지 함께 확인한다. | 중-1 | S | — |
| **P1** ⏸ | 서버 재시작·네트워크 단절 때 표준/프로 화면은 영문 「Failed to fetch」를 그대로 보인다 | core.ts request()·requestStream()의 fetch를 감싸 네트워크 예외를 한 곳에서 「서버에 닿지 못했습니다 — 서버가 재시작 중이거나 네트워크가 끊겼을 수 있습니다. 잠시 뒤 다시 시도해 주세요.」로 바꿔 던진다(기술 정보는 괄호 뒤에). 이렇게 하면 화면 39개를 고치지 않고 라이트 두 곳의 번역기도 불필요해진다. | 중-1 | S | — |
| **P1** | 비밀번호 규칙이 화면(4자 이상)과 서버(8자 이상)로 어긋난다 | 서버가 최소 길이를 `/api/auth/me` 또는 별도 `/api/users/password-policy`로 내려 주고, settings.html의 placeholder(267)·검사 문구(941·1471)·초기화 안내(1452)가 그 값을 쓴다. 서버 변경이 부담이면 우선 세 곳을 「8자 이상」으로 통일한다. | 중-1 | S | — |
| **P2** | 업데이트 설치 흐름의 문구·주석이 실제 동작(/S 없는 마법사 실행)과 세 갈래로 어긋난다 — 완료 페이지엔 외부 링크까지 뜬다 | 「설치 창을 보이게 한다」는 2026-07-29 사용자 결정은 그대로 두고, 그 결정에 맞춰 나머지를 정직하게 맞춘다: ① settings.html 2310~2313 문구를 「설치 창에서 [다음]→[설치]→[마침]을 누르면 새 버전이 열립니다(약 1분)」로, gijoAsk 문구(2288)도 동일하게 ② main.ts 1416~1420 주석을 「/S를 빼서 마법사가 보인다 — 자동 재기동은 마침 페이지의 체크박스가 담당」으로 갱 | 중-1 | S | — |
| **P2** | 설치 프로그램 실행(spawn)이 실패해도 잡지 않고 1.2초 뒤 앱을 끈다 | spawn 뒤 `child.once("spawn")`을 기다려 성공했을 때만 quitConfirmed·app.quit()으로 넘어가고, `child.once("error", …)`에서는 한국어로 「설치 프로그램을 실행하지 못했습니다(Windows 보안 프로그램이 막았을 수 있습니다). 받은 파일 위치: …」를 renderer에 던져 installUpdate의 catch가 보이게 한다(파일은 지우지 않고 경로를 알려 수동 실행 | 후-1 | S | — |
| **P2** | 비밀번호를 바꿔도 「최근 로그인」에 저장된 옛 비밀번호는 그대로 — 다음 로그인이 실패로 시작한다(UX피드백 5절 「셀프 잠김」의 현재 모습) | changeMyPassword 성공 직후 `window.gijo.creds.list()`에서 현재 서버·아이디 항목이 hasPw면 `creds.save(serverUrl, username, 새비번, true)`로 갱신하고, 상태 줄에 「✓ 비밀번호가 변경되었습니다. 이 PC에 저장된 로그인 정보도 새 비밀번호로 바꿨습니다.」를 적는다. 서버측 「셀프 잠김」(본인 세션 끊김)은 현재 코드에서 재현되지 않으므로 UX피드백 5절은 | 중-1 | S | — |
| **P2** | 업데이트 판은 「관리자」 구역에 있지만 담당자도 배지를 눌러 들어가 받을 수 있다 — 탭줄에 없는 구역에 떨어진다 | 정책을 하나로 정한다. (권장) 「업데이트는 누구나」 — 업데이트 판을 data-sec="my"(내 설정)로 옮기고 TAB_UPDATE를 settings.html?s=my로, 「게시된 배포판·누가 받았나」만 admin 구역에 남긴다. 반대로 「관리자만」이면 서버 download에 adminMiddleware를 붙이고 담당자에게는 배지 대신 「관리자에게 업데이트를 요청하세요」를 보인다. | 중-1 | M | 필요 |
| **P2** | WebSocket이 끊기면 5초마다 조용히 재접속만 한다 — 실시간 패널이 멈춘 걸 담당자는 모른다 | wsClient에 `onState(cb)`(connected/reconnecting)를 더하고, 재접속이 3회(15초) 이상 이어질 때만 titlebar.js 세션 칩 옆에 「⚡ 실시간 연결 끊김 — 다시 연결 중」 작은 회색 칩을 띄우고 복구되면 사라지게 한다(연결상태 배지를 없앤 결정을 알고 있어 상시 표시가 아닌 「끊겼을 때만」으로 제한). | 중-1 | M | 필요 |
| **P2** ⏸ | 수동 업데이트 스크립트가 에디션을 보내지 않아 라이트 PC에 프로 설치본을 받고, 기본 포트도 옛 4000이다 | manual-update.ps1에 `-Edition pro\|lite` 매개변수(기본은 설치 폴더의 에디션 표지 또는 package.json name으로 판정)를 넣어 `&edition=`을 붙이고, 기본 ServerUrl을 7446(라이트면 7445)으로 바꾸며, 받는 파일 이름도 서버가 준 filename을 쓴다. 배포 가이드 §6.3 예시의 `:4000`도 함께 갱신. | 전-7 | S | — |

### 문서 — 매뉴얼·챗봇 근거 문서 (12건)

| | 무엇이 문제인가 | 어떻게 고치나 | 계획서 | 크기 | 시안 |
|---|---|---|---|---|---|
| **P1** | 사용자 매뉴얼의 메뉴 지도(§0.3·§8)가 2026-08-31 「5대 메뉴」 재편 이전 구조를 설명한다 — 챗봇 RAG 1차 근거가 낡음 | §0.3 표와 §8 부록 표를 nav.js GROUPS 그대로(① 내 지식: 지식 창고·법령·판례·GIJO AS 안내·보안제품 비교 / ② 내 문서: 업로드·반입·문서작성·내 문서 목록·나만의 연락처·업무 넘기기·작업 내역·지켜보는 폴더·조치 요청서 / ③ 취약점 업무: ⓪ 자산 고르기·①~⑤ 절차·보안 로그 파일 분석 / ④ 보안제품 관리 / ⑤ 설정: 설정·AI·기록)로 다시 쓰고, 「절차 5단계는 ③ 취약점 업무 안의 항 | 신규 | S | — |
| **P1** | 실무매뉴얼 §0에 서로 다른 메뉴 지도가 두 개 있고 둘 다 낡음(07월 IA·08-02 IA) — 고객 RAG 문서 | §0-1 표를 삭제하고 §0 2항을 nav.js 5그룹으로 바꾸거나, 메뉴 지도는 사용자 매뉴얼 §8 한 곳만 정본으로 두고 실무매뉴얼은 「메뉴 지도는 사용자 매뉴얼 §8」로 넘긴다(두 문서가 각자 지도를 들고 있으면 한쪽은 반드시 낡는다). | 신규 | S | — |
| **P1** | 「이렇게 쓰면 됩니다」의 화면 경로 4곳이 낡음 — 챗봇이 이 문서로 직접 안내한다 | 경로를 「② 내 문서 > 업무 넘기기」「① 내 지식 > 지식 창고(또는 대화창 ＋로 올리기)」「③ 취약점 업무 > ③ 조치」「③ 취약점 업무 > ⑤ 보고」로 고치고, 8행은 「프로 화면구성(기본)은 현황 카드가 대화창에, 표준은 탭으로」로 바꾼다. | 전-1 | S | — |
| **P1** ⏸ | 단일 데스크톱 모드 기본 주소가 문서·챗봇 안내 전부 「localhost:4000」인데 코드는 7446(라이트 7445) — 따라 치면 연결 실패 | screenguide.ts 838행을 「동봉 서버는 주소를 비워 두면 앱이 알아서 맞춘다(포트는 에디션이 정한다) · 분산 모드는 사내 서버 주소(:4000 기본)」로 바꾸고, settings.html 248행 placeholder도 login.html처럼 숫자 없는 문구로 바꾼다(login.html 306행의 「숫자를 박지 않는다」 규약과 맞춘다). 배포 가이드·client/README도 같은 문장으로. | 전-7 | S | — |
| **P1** | screenguide 「계정」 판 설명이 관리자 계정 관리를 서술하고, 실제 admin 「계정 관리」 판은 안내가 없다 | screenguide.ts settings.html panels에 「계정」은 「내 아이디·이름·역할 확인, 비밀번호 변경(4자 이상), 로그아웃」으로 다시 쓰고, 「계정 관리」 항목을 새로 추가해 지금의 추가·삭제·역할·세션 1개 서술을 옮긴다(별명 표에 「계정 발급」「사용자 추가」도 건다). | 전-7 | S | — |
| **P1** | 대시보드 서술이 코드와 어긋남 — 「한눈에 보기」·「네트워크 그래프」는 없고, 「오늘 할 일은 읽기용」은 매뉴얼·screenguide 모두 코드(완료 체크·즉시 저장)와 반대 | 매뉴얼 §1을 dashboard.html 실제 순서(AI 팀 카드 → 내 지식창고(접힘) → 🧭 공격 경로 → ☑ 오늘 할 일—계획 짜기: 왼쪽 후보/오른쪽 오늘 내 계획(체크·빼기 즉시 저장, 직접 적기는 대화창))로 다시 쓰고 「한눈에」는 ① 발견·수집에 있다고 적는다. screenguide.ts 277행의 「읽기용 요약」 문장은 삭제하고 「체크·빼기는 이 판에서 바로, 새로 적기와 절차는 대화창」으로 고친다. | 전-7 | S | — |
| **P1** | 「우상단 서버 연결 배지(● 운영중 LIVE/초록 점)」 서술이 고객 문서 2곳에 남아 있음 — 배지는 2026-08-08에 없앴다 | 두 문서의 배지 문장을 titlebar.js 967행 주석대로 「연결이 끊기면 화면 값이 안 채워진다 → 설정 › 서버·AI › 서버 연결의 [연결 테스트] 또는 대화창 「시스템 괜찮아?」(자가 진단)로 확인」으로 바꾼다. | 신규 | S | — |
| **P2** | 사용자 매뉴얼 §5 설정 표가 실제 탭·판 배치와 어긋남 — 「5개 탭」·화면 크기·사용량 위치·2차 인증 누락 | §5 표를 「4개 탭(내 설정·서버·AI·연동·관리자) + 별도 화면 기록」으로 고치고, 내 설정=계정·사용량·비용·2차 인증, 관리자=계정 관리·계정별 2차 인증·저장 암호화·업데이트·게시된 배포판·누가 받았나·에디션으로 settings.html data-sec 기준으로 다시 적는다. 화면 크기는 「상단 ⚙ 팝오버」로 안내한다. | 신규 | S | — |
| **P2** ⏸ | 사용자 매뉴얼 §0.1·§0.2 로그인 절차가 5.83.0 로그인 화면(구동 모드 3택·AI 위치 3택·2차 인증 2단계)을 반영하지 않음 | §0.2를 「① 구동 모드(표준/프로/라이트) ② AI 위치(이 PC/원격 GPU/클라우드) ③ 서버 주소(동봉이면 비움, 분산이면 사내 서버) ④ 아이디/비밀번호 ⑤ (켜져 있으면) 인증앱 6자리 또는 복구 코드 ⑥ 이미 다른 곳에 로그인 중이면 확인 후 전환」으로 다시 쓰고, §0.1에 라이트는 서버 없이 한 PC에서 돈다는 문장을 더한다. 용어사전의 2차 인증 항목과 연결한다. | 신규 | S | — |
| **P2** | 제품소개 §4.15·§4.16이 소멸한 「추가 기능」 그룹과 삭제된 「제품 소개자료」 화면을 현행처럼 서술 | §4.15 마지막 줄을 「① 내 지식 · ② 내 문서 · ③ 취약점 업무(절차 5단계 포함) · ④ 보안제품 관리 · ⑤ 설정」으로, §4.16 제목을 「로그 분석(③ 취약점 업무) · 보안제품 비교·소개(① 내 지식) · 업무 넘기기(② 내 문서)」로 고치고 스크린샷 안내 371행의 「추가 기능 메뉴」 문구를 지운다. | 전-6 | S | — |
| **P2** | 클라이언트 배포 가이드 v2.5.0 — 업데이트 메뉴 위치·배지 모양·무인(/S) 서술·대시보드 구성이 5.83.0과 다름(기존#7이 짚지 않은 부분) | §6.2를 「설정 › 관리자 탭 › 업데이트 판(기본 펼침) · 새 버전이 있으면 사이드바 「설정」 옆 노란 숫자 배지와 상단 ⚙ 메뉴 「새 버전 설치 가능」 · 설치는 마법사 화면이 뜬 뒤 앱이 닫히고 마침 후 새 버전 실행」으로, §9 대시보드 서술은 삭제하거나 사용자 매뉴얼 §1로 넘긴다. installer.nsh 8행 주석도 「인앱 업데이트는 --force-run만(비무인)」으로 고친다. | 신규 | S | — |
| **P2** | 내부 README 2종·설치 가이드의 설치 경로·초기 계정·화면 목록이 낡음(설치 경로 `Programs\gijo-as` vs 실제 `gijo-as-client`) | 설치 가이드 54행을 `Programs\gijo-as-client`로(이 머신 %LOCALAPPDATA%\Programs 실측 일치), README 55행을 「개발 모드는 jyh/changeme, production은 admin + 콘솔 1회 출력(또는 GIJO_INITIAL_ADMIN_PASSWORD)」로 고치고, client/README의 구조·미결 항목 절은 현행 화면 목록(app.html 셸·nav.js·허브 화면)으로 | 신규 | S | — |

### 여정전반 — 페르소나로 처음부터 끝까지 (12건)

| | 무엇이 문제인가 | 어떻게 고치나 | 계획서 | 크기 | 시안 |
|---|---|---|---|---|---|
| **P1** | 분산 모드 담당자는 앱을 켤 때마다 「처음 설정」 화면부터 — 선택이 기억되지 않는다 | setup.html의 「로그인 화면으로」를 누르면 userData에 `gijo-connect-mode.txt`=`remote` 같은 표식을 쓰고(IPC 하나 추가), main.ts 1548의 첫설치 판정에 「표식이 remote면 첫설치 아님·동봉 서버도 띄우지 않음」 갈래를 더한다. 되돌리는 길은 설정 › 서버에 「이 PC에 서버 새로 설치」 한 줄로 둔다. | 후-1 | S | — |
| **P1** | 서버 주소 칸이 「비워 두세요」라 안내하면서 localhost:7446을 미리 채우고, http:// 없는 주소는 연결 실패로만 보인다 | core.ts setServerUrl에서 스킴이 없으면 `http://`를 붙이고, login.html 서버 칸 blur 시 `checkServerHealth()`로 「✓ 서버 연결됨 (v…)」/「✗ 연결 안 됨 — 주소·포트·서버 상태 확인」 한 줄을 칸 아래에 보인다. 분산 모드 안내 예시는 `http://10.20.30.40:4000` 꼴로 통일하고(setup.html 138), 미리 채우는 값은 저장된 로그인이 없을 때 | 전-7 | M | 필요 |
| **P1** | 서버가 꺼지거나 망이 끊기면 대화창에 「Failed to fetch」 영문 날것 오류가 뜨고, 그 밖엔 알려 주는 곳이 없다 | 배지를 되살리자는 것이 아니다(2026-08-08 결정 인지). core.ts request()/requestStream()의 fetch를 try/catch로 감싸 「서버에 연결할 수 없습니다 — 사내 GIJO 서버(주소)가 꺼졌거나 네트워크가 끊겼습니다. 잠시 뒤 다시 시도하거나 ⚙ › 서버 변경에서 주소를 확인하세요」 한국어 오류로 바꿔 던진다. 콘솔은 그 문구를 그대로 보이면 된다. | 중-1 | S | — |
| **P1** | 유휴 30분 만료 뒤 아무 안내 없이 모든 동작이 「unauthorized (서버 오류 401 …)」로 실패한다 | core.ts tryRefresh가 401/409로 실패하면 서버 message를 담아 IPC 이벤트(`auth:expired`)를 메인에 보내고, main.ts가 본창에 알림 한 줄(「오래 사용하지 않아 세션이 만료되었습니다 — 다시 로그인합니다」)을 띄운 뒤 `navigate:to login.html`로 보낸다(이미 있는 팝업·콘솔 창 정리 경로 576~580행을 그대로 탄다). 세션 칩이 0:00에 닿으면 같은 흐름을 탄 | 중-1 | M | — |
| **P1** | 비밀번호 변경·계정 추가 화면은 「4자 이상」이라 하고 서버는 8자를 요구한다 | settings.html 세 곳(267·941·1471)을 「8자 이상」으로 고치고 setup.html처럼 「12자 이상 권장」을 덧붙인다. 서버 정책값(GIJO_MIN_PASSWORD_LEN)이 바뀔 수 있으니 /api/auth/me 응답에 `minPasswordLen`을 실어 화면이 그 숫자를 읽게 하면 다시 어긋나지 않는다. | 후-1 | S | — |
| **P1** | 「초기 비밀번호 변경」은 문서로만 시키고 제품은 한 번도 권하지 않는다 | users 표에 `must_change_password`(관리자가 만들거나 초기화하면 1, 본인이 바꾸면 0)를 두고 로그인 응답에 `passwordChangeRequired`를 싣는다. app.html이 그 값을 보면 상단에 한 줄 띠 「관리자가 준 초기 비밀번호를 쓰고 있습니다 — 지금 바꾸기」를 보이고 누르면 settings.html?s=my 계정 판으로 간다(강제 차단은 하지 않는다). | 후-1 | M | 필요 |
| **P1** | 「이 PC에 비밀번호 저장」이 기본 켜짐 — 헬프데스크가 시험 로그인한 관리자 비밀번호가 10대 PC에 남는다 | 비밀번호 저장은 기본 꺼짐으로 바꾸고(서버·아이디 저장은 기본 유지) 문구를 「비밀번호도 저장(여러 사람이 쓰는 PC에서는 끄세요)」로 한다. 로그인 응답 role이 admin이면 비밀번호는 저장하지 않는다(finishLogin에서 `result.user.role` 확인 후 savePw=false로 넘김). | 후-1 | S | — |
| **P2** | 박주임의 「서버 주소 고정」 수단이 환경변수 하나뿐이고, 10대에 어떻게 넣는지 문서에 명령이 없다 | main.ts 기본서버주소()의 두 번째 출처로 `%APPDATA%\GIJO AS\gijo-server.json`({"serverUrl":…})을 읽게 하고(env가 있으면 env 우선), 배포 가이드 §5.1에 10대용 한 블록 — `GIJO-AS-Setup-x.y.z.exe /S` + `setx GIJO_SERVER_URL http://10.20.30.40:4000`(또는 그 json 파일 복사) — 을 그대로 복사해 쓸 | 후-1 | M | — |
| **P2** | manual-update.ps1은 클라이언트 소스가 있는 PC에서만 돌고, 설치된 버전이 아니라 소스 버전을 읽는다 | 스크립트를 설치본 resources에 동봉하고(package.json extraResources에 scripts/manual-update.ps1 추가), 설치 버전은 NSIS가 남기는 레지스트리 `HKCU\Software\Microsoft\Windows\CurrentVersion\Uninstall\<appId>\DisplayVersion`에서 읽는다. -ServerUrl은 필수 인자로 바꾸고, 가이드 §6.3을 「설치 폴더의 | 후-1 | M | — |
| **P2** | 비밀번호를 다섯 번 틀려도 「몇 번 남았는지·잠기면 몇 분인지·잊으면 누구에게」를 화면이 말하지 않는다 | auth.ts 401 응답에 `remaining: LOGIN_MAX_FAILS - fails`를 싣고, login.html은 remaining ≤ 5일 때 「N회 더 틀리면 15분 동안 잠깁니다」를 오류 아래 한 줄로 덧붙인다. 힌트 영역(365~368행)에 「비밀번호를 잊었으면 관리자에게 초기화를 요청하세요(설정 › 계정 관리 › 비번↺)」 한 줄을 더한다. 기존#3의 문구 분기와 같은 자리에서 함께 고친다. | 중-1 | S | — |
| **P2** | 로그인 화면의 구동 모드·AI 위치 선택을 담당자(비관리자)가 누르면 로그인 직후 「서버 오류 403 · /api/…」가 섞인 알림을 받는다 | login.html 고른것적용()에서 e.message의 첫 줄만 쓰고(`split("\n")[0]`), 403이면 「관리자만 바꿀 수 있는 설정입니다 — 관리자에게 요청하세요」로 바꿔 담는다. 알림 제목도 「적용되지 않았습니다」 대신 「일부 선택은 관리자 권한이 필요합니다」로 낮춘다. 시안 필요 없음(문구만). | 전-7 | S | — |
| **P2** | 인앱 업데이트가 띄우는 설치 마법사가 「GIJO AS 설치를 시작합니다 … 설치 후 서버 주소로 로그인하면」이라 새 설치처럼 읽힌다 | installer.nsh customWelcomePage에서 electron-builder가 주는 `${isUpdated}` 조건으로 갈라, 업데이트면 제목 「GIJO AS 업데이트 ${VERSION}」·본문 「설정·로그인 정보는 그대로 유지됩니다. 완료되면 앱이 다시 켜집니다」로 바꾸고 완료 페이지 VPN 링크는 새 설치일 때만 보인다. | 후-1 | S | — |

⏸ = 라이트 에디션에 걸린 항목. **사장님 지시로 보류**합니다(2026-09-02).

## 4. GB10(DGX Spark) 코드 병행 검토

### 4-1. 계획서와의 관계 — 「없다」가 아니라 「이미 있다」
계획서 전-7 하단(62·70·77~79행)에 **GB10 편입 완료 · 원격 GPU를 「고르는 자리」의 선택지로(전-7 하드웨어 갈래) · 실측 7.6B Q5@16K에서 GB10 38.3 tok/s vs RTX 3090 118.5 tok/s → 「큰 모델·큰 기계 = Pro」 길은 닫고 GB10의 값어치는 용량(121GB)**이라 적혀 있다. 따라서 이 검토의 물음은 「계획서에 넣을까」가 아니라 **「GB10을 고객 배치의 서버 후보로 보는가, 개발·실측·절감 도구 기계로만 보는가」**다(§5-③).

### 4-2. 코드가 GB10에서 하는 일 — 실측으로 대조 (gb10 발췌 도구로 읽음, §7)
| 자리 | 코드 | 실측 대조 | 판정 |
|---|---|---|---|
| GPU 감지 | `unifiedmem.ts parseGpuMemoryReport` — 이름은 있고 메모리가 숫자가 아니면 `unified` | 「NVIDIA GB10, [N/A]」 → unified | ✅ 맞게 가른다 |
| 여유 메모리 | `localengine.ts getFreeVramMb` 419행 — unified면 `/proc/meminfo MemAvailable` | MemAvailable 26.3GB(모델 3개 상주 중) | ✅ 값 나옴. ⚠ 25GB만 남아 새 모델 로드 시 LRU 스왑이 잦을 수 있음 |
| 화면 사용량 | `getGpuUsage` 462~477행 — 사용률은 nvidia-smi, 메모리는 시스템 값 | 사용률 0%·메모리 96/121GB | ✅ |
| 자가진단 | `preflight.ts` 34~37행 — 「통합메모리 121GB(CPU와 공유)」 pass | — | ✅ 티어 화면과 모순 해소됨(08-11 사고 후속) |
| 티어 문구 | `tierReason` 1191행 — 「NVIDIA GB10 · 통합메모리 121GB(CPU와 공유)」 | — | ✅ 「GPU를 찾지 못했습니다」 거짓 문구 사라짐 |
| 티어 판정 | `GIJO_TIERS` 127~144행 — VRAM 급으로 lite/standard/pro, max는 예정 | 121GB는 pro(48GB급) 상한을 넘음 → pro로 잡힐 것 | ⚠ 계획서는 「큰 기계=Pro 길을 닫는다」 — 티어 표와 계획서 문구의 관계를 사용자에게 확인(§5-③) |
| 스왑 | 스왑 7/15GB 사용 중 | 모델 3개(약 82GB)+시스템 | ⚠ 통합메모리는 스왑에 밀리면 tok/s가 급락 — `makeRoomFor` 예산에 스왑을 고려하지 않음 |

### 4-3. 3갈래 점검(GPU 감지/엔진 · 빌드/배포/네이티브 · 성능/용량) 결과
(워크플로 재실행 후 채움. 지금 확정: 네이티브 모듈 4종 aarch64 로드 OK, llama-server aarch64 빌드 가동, 제품 서버 11시간 무중단.)

### 4-4. 정직한 한계
- 성능 수치(tok/s)는 이번에 재지 않았다 — 운영 중인 서버에 부하를 주지 않기 위해 읽기 전용만 했다. 계획서 08-11 실측(38.3 tok/s)을 그대로 인용한다.
- GB10 저장소는 hub/main보다 6커밋 뒤라 본 코드는 허브 최신이고, 도는 서버는 6커밋 전 빌드다.

## 5. 사용자 결정이 필요한 것
1. **로그인 화면 손질(P0·P1 셋)을 전-7로 넣어 지금 하나** — 넣으면 시안 1개부터 낸다.
2. **설치·배포 가이드 5.x 재작성 여부** — 문서 갱신은 요청 시만이라 묻는다. 고객에게 손으로 건네는 문서인지가 판단 기준.
3. **GB10의 자리** — ⓐ 개발·실측·절감 도구 기계(지금) ⓑ 고객 배치의 「원격 GPU 서버」 후보(전-7 하드웨어 갈래). ⓑ면 티어 표에 「통합메모리 대용량」 갈래와 스왑 고려가 필요하다.
4. 문서 통합의 실제 병합·`_archive/` 이동은 통합본을 본 뒤 별도 승인.

## 6. 기각한 지적 (검증에서 걸러 낸 것)

반박 검증에서 걸러 낸 것입니다. 그럴듯했지만 근거가 약하거나 이미 의도된 설계였습니다.

- 점검이 낸 지적 90건 중 **2건이 기각**됐습니다(검증 통과 88건).
- 기각 사유는 대체로 「코드 주석에 사용자 결정으로 기록된 의도된 설계」이거나 「원문을 다시 읽으니 이미 그렇게 돼 있음」이었습니다.
- 검증관이 심각도를 낮춘 것도 있습니다 — 예를 들어 설치 첫 실행 문제(F1-01)는 두 번 눌러 빠져나올 수 있어 P0이 아니라 P1로 내렸습니다.

## 7. gb10 이용 내역 (작업 규칙 — 매 보고에 적는다)
- **무엇에 썼나**: ① 읽기 전용 실측 탐침 2회(`bash -s` · `bash -lc`) ② `node tools/local-digest.mjs file server/src/engine/localengine.ts "통합메모리 갈래…"` — 1,304줄 파일에서 관련 4구간(약 120줄)만 원문 발췌.
- **무엇을 돌려받았나**: 탐침 = §1-2 전부. 발췌 = §4-2 표의 코드 근거(getFreeVramMb·getGpuUsage·makeRoomFor·tierReason) — 파일을 통째로 읽지 않고 답을 얻었다.
- **값을 했나**: 탐침은 확실히(이 보고서 §1-2·§4-2가 전부 거기서 나왔다). 발췌는 **2분 11초** 걸렸다 — 8090 두뇌가 다른 모델 2개와 메모리를 나눠 쓰는 상태(가용 25GB)라 느렸을 가능성. 결과 품질은 좋았다(원문 그대로·빠진 구간 없음).
- 못 한 것: 워크플로 에이전트가 gb10을 쓰는지는 실행이 0건이라 셀 수 없었다.

## 📖 용어 풀이
- **통합메모리**: CPU와 GPU가 한 메모리를 함께 쓰는 구조. GB10·Apple Silicon이 이렇다. GPU 전용 메모리가 없어 `nvidia-smi`가 메모리 칸을 `[N/A]`로 낸다.
- **NSIS 설치본**: Windows용 설치 프로그램(.exe)을 만드는 도구 이름. `oneClick`이 켜지면 묻지 않고 조용히 설치되고, 꺼지면 설치 마법사가 뜬다.
- **GB10 / DGX Spark**: NVIDIA의 책상 위 AI 기계. ARM CPU + Blackwell GPU, 통합메모리 128GB. 우리 세 기계 중 `gb10`.
- **코드 서명**: 「이 프로그램은 누가 만들었고 바뀌지 않았다」를 증명하는 전자 서명. 없으면 Windows SmartScreen이 경고하고 보안 제품(EDR)이 실행을 막을 수 있다.
- **enrollRequired(등록 필요)**: 관리자에게 2차 인증을 필수로 걸어 둔 서버가, 아직 등록 안 한 관리자에게 「등록만 할 수 있는 제한 세션」을 주면서 함께 보내는 표시.
- **autocrlf / CRLF·LF**: 줄 끝 표시. Windows는 두 글자(CRLF), Linux·Mac은 한 글자(LF). git이 자동으로 바꾸다가 스크립트를 깨는 사고가 반복된다.
- **워크플로(Workflow)**: 여러 Claude 에이전트를 순서·병렬로 묶어 돌리는 대본. 「session limit」은 한 계정이 일정 시간에 쓸 수 있는 Claude 사용량 한도다.
