# [인계] max → win — 라이트 9메뉴 재편 **구현 착수** 지시 (2026-08-17)

사장님 승인(2026-08-17): 라이트 재편 9종을 **구현**하고, 그 빌드로 Drive를 교체한다.
**설계는 이미 hub에 있다**(아래) — 이 문서는 「이제 구현하라」는 착수 신호 + 착수점 정리다.
역할: **win 구현 · max 검증**(구현되는 대로 CDP 실앱으로 검증해 되돌린다).

## 착수점 (현재 상태 — max 확인)
- `lite-screens.json`은 **재편 전 원본 9메뉴** 그대로(0 물어보기 · 7 제품소개 · 8 기억 · 9 메모장 별도, 4 스캐너 · 5 장비등록부).
- 새 화면 파일 **없음**: `lite-contacts/find/materials/brain/connect.html` 미존재.
- 즉 재편은 **문서(설계)만 있고 코드 0**. 여기서 시작한다.

## 구현할 9종 (설계 근거 = hub 문서)
설계 원문: `인계_max_라이트_메뉴재편_설계_2026-08-16.md` + 커밋 `e0ccd0d·689948a·130256f·7410591·6f20e04·05f4894`.

1. **①기억+메모장 → 「나만의 자료」 통합** — 두 메뉴를 하나로(자료 넣기·관리·메모 연동 한 화면).
2. **②Smart MD = 표준처럼 나비(상단) 메뉴** — 별도 제품이나 라이트에서 메뉴로 진입(연동, 없어도 라이트 돎).
3. **③「물어보기」→「찾기」 확정** — 명칭·의미 정리(내 자료+제품을 AI로 찾기).
4. **④대시보드 = 지식창고 시각화** — 메뉴 중복 최소화, RAG·wiki·그래프 저장량을 그래프로.
5. **⑤「나만의 연락처」 = 커버리지 매트릭스** — 테리토리×보안제품 담당자 한눈에(시각화).
6. **⑥Smart MD 실업무 템플릿** — 사고대응·점검기록·인수인계 3종(초안 `smartmd-templates/`).
7. **⑦두뇌 선택** — OpenAI(지금)→자체 LLM(뒤) 선택 UI + 원격 GPU 옵션(리소스 부족 시).
8. **⑧바깥 연결** — 오픈망/폐쇄망 **고객 선택 on/off**(흩어진 선택 통합), 하드봉인=향후 라이선스 연계.
9. **⑨최종 메뉴 정리** — 제품소개 제거(챗 흡수)·바깥연결은 설정으로·**4↔5 교체**(4 보안 장비 등록부, 5 스캐너: 등록→스캔 흐름).

## 주의(작업경계)
- 화면은 `lite-` 접두사 새 파일(공용 파일 직접 수정 금지 — 회사 위젯 빼기가 공용 수정이 되면 안 됨).
- 논리(검색·살균·계산)는 공용 그대로 호출, 화면(HTML/JS)만 따로.
- `lite-screens.json`의 `구현됨` 스위치로 껍데기/실물 가른다.

## max 검증 계획
- 구현되는 항목마다 라이트 재빌드(**`tools/stage-llama-metal.sh` + `npm run dist:lite`** — Mac은 cuda 아님) 후 CDP 실앱으로:
  화면 렌더·나비 진입·시각화 데이터·연락처 매트릭스·두뇌/바깥연결 on/off·메뉴 순서(4↔5) 확인.
- #1·#2·#5는 이미 1.1.2에서 런타임 확정(회귀 감시만).

## Drive
- 9종 구현 + max 검증 통과 후, **한 빌드**로 Drive 교체(사장님 최종 승인·검토관문).
