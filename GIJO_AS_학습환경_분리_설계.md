# GIJO AS — 학습 환경 분리 설계 (계획서 중-4)

*작성 2026-07-31 · 근거: 운영 실측 + 사고 이력 + 외부 표준 조사*

계획서 **중-4 "게이트 위에서 합성·학습 향상 개시"** 를 시작하려다 막힌 지점과, 그 해법을 적는다.

---

## 1. 왜 지금 못 하는가 (운영 실측 2026-07-31)

| 확인한 것 | 값 |
|---|---|
| 학습 후보 대기 | **60건**(점수 4점 = 신호 강함) |
| 대화 로그 | 2,123건 · 그중 담당자가 평가한 것 **2건** |
| 학습 사전점검 | `unsloth` 없음 · `gguf` 없음 → **실패** |
| 운영 WSL의 torch | **2.13.0+cpu** — 그래픽카드를 못 쓴다 |

즉 **환류(피드백 루프)가 멈춰 있고, 돌리려 해도 학습이 안 돈다.**

## 2. 운영 서버에 학습 환경을 깔면 안 되는 이유

CUDA용 torch를 깔면 `nvidia-*` 파이썬 패키지들이 함께 들어온다. **그게 예전 사고의 원인이었다** — WSL에 nvidia 패키지가 들어간 뒤 llama.cpp 전 바이너리가 `munmap_chunk` 로 죽었고, 재부팅 후에야 증상이 나타나 진단이 오래 걸렸다.

지금 운영 4000 서버는 이 llama.cpp로 담당자에게 답하고 있다. **학습을 켜려다 답변을 끄는 것**은 맞바꿀 만한 거래가 아니다.

여기에 하나 더: 학습은 GPU를 통째로 오래 쓴다. 같은 카드에서 추론과 학습이 겹치면 담당자의 답이 느려지거나 모델이 스왑된다.

## 3. 외부 표준도 같은 말을 한다

조사한 문헌들이 공통으로 권하는 구조는 **"학습은 운영 경계(perimeter) 밖의 자체 워크스테이션에서, 산출물만 반입"** 이다. 이유도 우리와 같다 — 실험(학습)과 운영(추론)을 분리해야 후보 모델을 마음대로 시험하면서도 운영 정책·도구를 건드리지 않는다. 폐쇄망에서는 의존성·데이터셋·모델을 **미리 준비해 옮기는 것**이 전제이므로, 우리 온프렘 원칙과도 어긋나지 않는다.

## 4. 설계 — 3구역으로 나눈다

```
[학습 구역]                 [반입 관문]              [운영 구역]
Mac M1 Max (Metal)          파일 1개                 WSL 4000 서버
· 데이터셋 만들기      →    <모델>.gguf        →    · 모델 배치
· QLoRA 학습                (+ 데이터셋 지문)         · adopt.mjs 게이트
· GGUF 변환                                          · 통과분만 채택
```

**① 학습 구역 = Mac M1 Max 32GB.** 이미 Phase 1(크로스플랫폼 분기)이 끝나 있고 Metal 빌드가 예정된 기계다. 운영과 물리적으로 분리돼 있어 무엇을 설치해도 4000 서버가 다치지 않는다. 학습이 며칠 걸려도 담당자 업무가 안 멈춘다.

**② 반입 관문 = 파일 하나.** 옮기는 것은 `<모델>.gguf` 와 **데이터셋 지문(sha256)** 뿐이다. 파이썬 패키지·CUDA·학습 스크립트는 운영으로 넘어가지 않는다. 지문을 함께 옮기는 이유는 "이 모델이 무엇으로 학습됐는가"를 나중에 되짚기 위해서다(채택 원장에 기록).

**③ 운영 구역 = 지금 있는 것 그대로.** `tools/evalgate/adopt.mjs` 가 이미 **배정 → 3축 게이트 → 통과면 채택 / 보류면 원상복귀**를 강제한다. 새로 만들 것이 없다.

### 데이터셋은 어디서 만드나
데이터셋 제작(후보함 → 대화 로그 → 데이터셋)은 **운영에서** 하고 JSON만 Mac으로 옮긴다. 사내 데이터가 학습 구역에 오래 머물지 않게 하기 위해서다(학습이 끝나면 지운다).

## 5. 먼저 해결해야 할 것 — 데이터 위생

학습 환경이 준비돼도 **지금 후보함을 그대로 학습시키면 안 된다.** 실측한 오염 3종:

| 오염 | 실측 | 조치 |
|---|---|---|
| **같은 질문 반복** | 후보 200건 중 서로 다른 질문 **27개**("방화벽 월간 정기점검 절차" 44회) | 학습 전 중복 제거 필수. 그대로 넣으면 27문항만 잘하는 모델이 된다 |
| **회귀 문항 유입** | 회귀 하네스 11문항 중 **5건**이 후보함에 있음 | 제외. 안 그러면 하네스가 자기가 가르친 걸 채점한다 |
| **질문 자리에 맥락 덩어리** | `이전 대화 맥락(같은 세션): …` 이 질문으로 저장됨 | **수정 완료**(2026-07-31, `logQuestion`) — 이후 기록부터 사람이 한 질문만 남는다 |

평가 게이트 99문항과 겹치는 후보는 **0건**이었다 — `qa:true` 경로가 제대로 막고 있다(중-3 설계가 의도대로 동작).

## 6. 순서

1. **후보 60건 검토** — 담당자가 무엇을 학습에 넣을지 결정(중복·회귀 문항 제외한 목록을 제시)
2. **Mac Metal 빌드 + 학습 의존성 설치** — 운영과 무관하므로 자유롭게
3. 데이터셋 export → Mac 반입 → QLoRA 학습 → GGUF 변환
4. GGUF 반입 → `adopt.mjs` 로 게이트 → **통과분만 채택**
5. 채택 원장에 데이터셋 지문·축별 점수 기록

## 7. 하지 않기로 한 것

- **운영 WSL에 CUDA torch·unsloth 설치** — 2절의 사고 이력 때문. 필요해지면 그때 별도 결정.
- **자동 학습(사람 없이 도는 루프)** — 게이트는 자동이어도 **채택 결정은 사람**이다. 계획서 중-3의 원칙("하나라도 하락 시 채택 보류")을 유지한다.

---

## 📖 용어 풀이

- **환류(피드백 루프)** — 담당자가 좋다/나쁘다고 표시한 답을 모아 다시 학습에 쓰는 순환.
- **QLoRA** — 큰 모델을 적은 메모리로 조금씩 고쳐 학습시키는 방법. 전체를 다시 학습하지 않는다.
- **GGUF** — 우리 엔진(llama.cpp)이 읽는 모델 파일 형식. 학습 결과를 이 형식으로 바꿔야 쓸 수 있다.
- **데이터셋 지문(sha256)** — 학습에 쓴 데이터 묶음을 나타내는 짧은 값. 나중에 "이 모델이 무엇으로 배웠나"를 되짚는 용도.
- **운영 경계(perimeter)** — 실제 서비스가 도는 영역. 이 안에 실험 도구를 들이지 않는 것이 원칙이다.

## 출처

- [Air-Gapped AI Fine-Tuning: How to Train Custom LLMs Without Internet Access](https://blog.premai.io/air-gapped-ai-fine-tuning-how-to-train-custom-llms-without-internet-access/)
- [Air-Gapped LLM Deployment – Rexon Cyber](https://www.rexoncyber.com/air-gapped-llm/)
- [The 2026 Definitive Guide to Running Local LLMs in Production](https://www.sitepoint.com/the-2026-definitive-guide-to-running-local-llms-in-production/)
