# win 인계 — **출하 모델(7.6B)이 도구 호출 2건을 놓친다.** 그리고 14B는 라이트에 못 들어간다 (max, 2026-08-13)

> 한 줄: 라이트 게이트 「32/33」은 **qwen3-14b로 잰 값**이었다. 고객이 받는 dmg는
> `gijo-main-orchestrator`(7.6B)로 돈다. 같은 조건에서 다시 재니 **29/33**이고,
> 그 차이 2건은 전부 **도구를 아예 안 부르는 실패**다.

## 1. 무엇을 쟀나 — 변수를 하나씩만 바꿨다

전부 **고객이 받는 그대로**의 서버에서 쟀다: 라이트 진입점(도구 13개) · 빈 DB(지식 묶음 자동
적용으로 문서 19 · 트리플 1,999) · 동봉 llama-server(Metal) · 라이트 등급(ctx 8192).

| 모델 | 결과 | 떨어진 문항 |
|---|---|---|
| **`gijo-main-orchestrator` 7.6B (출하)** | **29/33** | lt-search-1 · lt-kb-1 · lt-onto-2 · lt-seed-law |
| `qwen3-14b` | **31/33** | lt-onto-2 · lt-seed-law |

**데이터를 고정하고 모델만 바꿨다.** 차이는 정확히 둘:

```
lt-search-1  「방화벽 매뉴얼 어디 있더라」   7.6B: 도구 미실행 → 14B: search 실행 ✓
lt-kb-1      「내가 넣은 자료 뭐뭐 있어?」   7.6B: 도구 미실행 → 14B: knowledge_status 실행 ✓
```

둘 다 **도구를 잘못 고른 게 아니라 아예 안 불렀다.** 담당자 눈에는 「모델 지식으로 대충 답한다」로
보인다 — 오늘 계속 나온 「기능은 있는데 말이 안 닿는다」의 모델판이다.

남은 둘은 모델과 무관하다(양쪽 다 실패):
- `lt-onto-2` — 온톨로지 별칭 미연결(「피싱」이 T1566에 안 닿음). **별칭 표를 붙이면 풀린다.**
- `lt-seed-law` — `lawAnswer`가 조문 본문을 안 준다(다른 인계 §6).

⚠ **이 문항이 별칭 표의 값을 증명한다** — 게이트가 붙잡고 있는데 모델을 키워도 안 풀리고,
표를 연결해야만 풀리는 유일한 종류다.

## 2. 그럼 14B로 출하하면 되나 — **안 된다. 실측으로 닫혔다**

라이트 등급(ctx 8192)에서 잰 실제 점유(Metal, RSS):

| | 점유 |
|---|---|
| 임베딩 `bge-m3` | **1.14 GB** |
| **`gijo-main-orchestrator` 7.6B** | **5.53 GB** |
| `qwen3-14b` | **10.20 GB** |

- 출하 구성(7.6B + 임베딩) = **6.67 GB** → 결정서 ⓐ-1의 10GB 대상에 여유 있게 들어간다
  (win이 잰 화면 몫 0.7GB를 더해도 7.37GB / 10GB = **74%**).
- 14B + 임베딩 = **11.34 GB** → **10GB에 아예 안 들어간다.** 12GB라도 화면 몫까지 재면 100%다.

▶ **결론: 라우팅 2건은 모델을 키워서 되찾을 수 없다.** 다른 길로 가야 한다:
  ① 별칭·재작성처럼 **모델 밖에서** 닿게 하는 장치(네가 이미 가는 방향)
  ② 라이트 프롬프트에서 도구 설명을 더 뾰족하게(도구 13개라 자리는 넉넉하다)
  ③ 7.6B~10B 사이 다른 후보를 재 보는 것(지금 기계에 그런 모델이 없어 못 쟀다)

## 3. 곁가지 — **라이트 등급의 「채팅 LLM 1개」가 안 지켜진다**

라이트 등급은 `maxLoadedModels: 1`인데, 실측에서 **채팅 모델 2개가 동시에 올라가 있었다**
(60초 뒤에도 그대로 · 합계 17.27GB).

자리는 `localengine.ts:484-493`이다:

```ts
const free = await getFreeVramMb();
if (free === null) {
  // nvidia-smi 없음 → 개수 상한 폴백
  while (pool.size >= tier.maxLoadedModels) { …축출… }
  return;                     // ← 개수 상한은 **여기서만** 쓰인다
}
let freeMb = free;
while (freeMb < needMb) { …축출… }   // ← 여유가 있으면 개수는 안 본다
```

**여유 메모리를 읽을 수 있는 기계에서는 개수 상한이 아예 적용되지 않는다.**
설정 화면은 고객에게 「채팅 LLM 1개」라고 말하는데 실제로는 2개가 상주할 수 있다.

지금 출하 dmg는 채팅 모델을 **하나만** 담으므로 당장 터지지는 않는다. 다만
① 등급의 약속과 동작이 다르고 ② 고객이 `models/`에 gguf를 하나 더 넣으면 그때부터 조건이 선다.
그리고 Metal의 「여유」는 OS가 회수할 몫까지 포함한 낙관적인 값이라, 네가 찾은
「화면 몫 0.7GB를 아무도 안 센다」와 **겹치면 과다 커밋**이 된다.

▶ 제안: 여유를 읽을 수 있어도 **개수 상한을 먼저 적용**한다(둘 중 엄한 쪽).
⚠ 다만 티어표는 「표준 제품 완성 후 다시 본다」는 결정이 서 있어 나는 손대지 않았다.

## 4. 내가 바꾼 것 / 안 바꾼 것

- 바꾼 것 없음. **측정과 기록만** 했다(등급·배정 변경은 전부 버리는 시험용 DB에서 했다).
- `lite/env.ts`가 라이트 기본 모델을 `gijo-main-orchestrator`로 잡는 것은 그대로 둔다 —
  위 실측이 그 선택을 뒷받침한다(14B는 대상 사양에 안 들어간다).
