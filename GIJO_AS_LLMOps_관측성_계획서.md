# GIJO AS — LLMOps 관측성 계획서 (영상 대조에서 출발)

- 작성: 2026-09-02 (사장님 지시 「계획서로 만들어서 나중에 필요할 때 업데이트 하자」)
- 성격: **살아 있는 문서.** 지금은 착수하지 않는다. 관측성 작업이 올라오거나 사장님이 결정을 주실 때 이 문서를 먼저 열고 갱신한 뒤 움직인다.
- 출발점: 유튜브 「에이전트끼리 일하게 만드는 자비스 관제 시스템 구축 과정 전체 시연」(양실장의 바이브코딩대학, 2026-09-02 게시, 45분, `wSIy25_JWYI`). 사장님 질문 「우리 제품에 참고할 만한 게 있을까」에 코드 대조로 답한 결과를 계획서로 옮긴 것.
- 판정 원칙: **있다/없다는 코드로 확인한 것만 적는다.** 아래 근거는 전부 파일:줄 번호다. 낡으면 이 표부터 고친다.

---

## 0. 이 문서의 자리 — 전중후 계획서와의 관계

| 항목 | 전중후 계획서 | 관계 |
|---|---|---|
| 후보 ① 실행 추적 · ② 오류 원인 분류 | **후-1** GA 판정·잔여 P0 해소(「백업·관측성 등」) | 후-1이 말하는 「관측성」의 구체 내용. 계획서 안이다. |
| 후보 ③ 평가 게이트 결과의 제품 노출 | **중-3** 평가 게이트 구축 | 중-3은 「구축」까지다. **제품 화면 노출은 계획서에 없다** → 사장님 결정 사항. |
| 기각 4건(Hermes·LangGraph·LLM Critic·Find Skills) | — | 계획서와 무관하거나 원칙과 충돌. §4. |

상태(2026-09-02): **착수 안 함 · 결정 대기.** 개발 기준은 프로 셸(2026-08-19) — 세 후보 모두 「대화창·카드로 되나」를 먼저 묻는다.

---

## 1. 영상이 보여준 것 (요약)

Gmail을 읽어 회신 필요 여부를 분류하고 회신 초안을 쓰는 멀티에이전트를 Codex로 만드는 전 과정. LangGraph(그래프 실행기) + LangSmith(호출 추적) 위에 3계층을 둔다.

- 절차: 설계 → PRD → **승인** → 목업 → 구축 → 실행 → 검증(LLMOps 진단). 단계마다 사람이 승인.
- 구조: Platform 감독 → Email 감독 → Intake·Triage·Security·Draft·Critic. 승인된 에이전트 스펙만 레지스트리 등록, 임의 생성 금지.
- 관측: 한 건(메일 하나)의 실행을 **노드-엣지 그래프**로 보고, 단계별 토큰·지연을 읽고, 실패 한 건을 추적해 **「모델 오류가 아니라 오케스트레이션 타임아웃(30초)」**을 찾아낸다. 릴리스 1.0.0(기준선) vs 1.0.1을 화면에서 비교.
- 영상 본인의 반성: 첫 목업이 「꾸미기식 토폴로지」(UI 슬롭)라 노드-엣지로 다시 시켰고, LLMOps 기능이 여러 페이지로 흩어져 한 흐름을 못 본다고 지적.

---

## 2. 대조표 — 이미 있는 것 (근거)

| 영상 항목 | 우리 제품 | 근거 | 판정 |
|---|---|---|---|
| 설계→PRD→승인→목업→구축→검증 | 시안→설계관→구현→검토관(병렬)→수리→게이트→배포→관문→게시 | `CLAUDE.md` 작업 규칙, `.claude/agents/` | 있음, **더 엄격** |
| Supervisor 감독형 | orchestrator(분배·취합) + dispatcher/agentloop, 팀원 8종 | `server/src/engine/agents.ts` AGENT_DEFS (orchestrator·scan·analysis·report·ti·normaltic·curator·bom — 2026-09-03 사서·부품 추가) | 있음 |
| 사람 승인 인터럽트 | 대화창 **결재판** — 쓰기 도구는 승인 전 실행 안 함 | `server/src/engine/agentloop.ts:105` | 있음 |
| Security 에이전트 | guard(입구 검사 `gateway.gateUserInput`) + 레드팀 카나리 30페이로드 | `server/src/engine/llmactivity.ts:18-19`, `client/.../redteam.html` | 있음 |
| 승인된 에이전트만 등록 | 로스터 고정(코드 정의), 화면에서 임의 생성 없음 | `agents.ts` | 있음 |
| 릴리스 게이트·스코어카드 | 평가 게이트 3축 138문항(라우팅 66·안전 48·한국어 24), 기준선 대비 하락 시 채택 보류, 견고성 9회 중앙값 | `tools/evalgate/run.mjs`, `baseline.json`(acceptedAt 2026-08-20, gitRev f0c2207c) | 있음(**개발자 도구**) |
| 실시간 「누가 일하나」 | 팀 사무실 창(canvas) + 레일 로스터 깜박임, `llm:event` 실신호 | `office.html:335`, `llmactivity.ts` | 있음(**실시간만**) |
| 감독 지표 | AI 팀 감독 — 규칙 계산(무호출·오류율 상위), 원천은 `llm_activity_daily` 하나 | `supervision.html:79,168`, `llmactivity.ts:44` | 있음(**개수만**) |

---

## 3. 후보 3건 — 없어서 남긴 것

### ① 한 질문의 처리 경로를 **사후에** 보는 실행 추적

**지금 상태(실측)**
- `llm:event`는 메모리 링버퍼 **200건**(`llmactivity.ts:36 HISTORY_LIMIT`) — 서버 재시작이면 사라진다.
- 영속은 **일 단위 집계뿐**: `llm_activity_daily(day, agent, kind, calls, errors, latencyMsSum)`. 한 건으로 되돌릴 수 없다.
- `chat_logs`는 `agentId·question·answer·rating·usedInDataset·createdAt`만(`db.ts:226`) — 어느 도구·단계를 거쳤는지 없다.
- 작업원장 `recordWork`는 `ts·kind·detail·actor·source` 한 줄(`worklog.ts:107`).
- 진행 알림 `reportProgress("write"/"review")`(`agentloop.ts:433-435`)는 **실시간 방송만**, 영속 없음.
- → 「이 답이 어느 팀원·도구·단계를 거쳐, 단계마다 몇 초·몇 토큰 썼나」를 **나중에 볼 길이 없다.**

**목표(영상이 보여준 것)** — 한 건 단위로 노드(단계)·엣지(흐름)와 단계별 지연·토큰·상태를 사후에 본다.

**우리 식 설계 방향(착수 때 설계관에 태울 초안)**
- 서버: 실행마다 `runId`를 발급하고 단계 목록을 영속(예: `run_traces` — runId·startedAt·agent·steps JSON[{step, agent, tool, kind, startMs, endMs, promptTokens, completionTokens, status, errorKind}]). 생산자는 agentloop(단계)·llm.ts(토큰·지연)·agenttools(도구) 세 곳 — **값을 누가 넣는가부터 짝을 맞춘다**(생산자 없는 소비자 금지).
- `qa:true` 실행은 남기지 않는다(중-3 오염 차단과 같은 규칙). 질문 본문은 `chat_logs` 참조로만(중복 저장 금지).
- 소비자: 프로 셸 **카드 「이 답 어떻게 나왔어?」**(지시는 대화창 원칙) + AI 팀 감독 화면의 「최근 실패 N건 → 경로 열기」. 화면 전용 조작을 새로 만들지 않는다.
- 그림: 영상의 교훈 그대로 **노드-엣지**로, 꾸미기식 토폴로지 금지. 밀도 우선(줄 높이·세로 총합 숫자로 시안).
- 보존: 기간·건수 상한을 정한다(200건 링버퍼가 있던 이유를 잊지 말 것).

**함정(이 저장소 실사고 기반)** — 필드명 오인(5회), 이미 세는 곳과 잣대 3중화(`llm_activity_daily`·`worklog`·새 표), 스트리밍 ALS 자리와의 충돌, 삼켜진 실패가 기준선 오염.

**예상 규모**: 서버 표+생산자 3곳+카드 1종 — **중(2~3일)**. 시안·설계관·검토관 포함.

### ② LLM 실패의 **원인 분류**

**지금 상태(실측)**
- 오류 발신부는 두 곳: `llm.ts:874` 「응답 시간 초과(N초)」, `llm.ts:904` 「로컬 LLM 실패 — <진단>」. 임베딩은 `embedding.ts:70`. **문자열 detail뿐, 분류 필드 없음.**
- 감독 화면은 `errors` **개수만** 센다(`supervision.html:130,164`). 「시간 초과였나, 모델이 죽었나, 연결이 끊겼나」를 화면이 말하지 못한다.
- 같은 부류의 실사고: 야간 회귀 「오류 N건」이 실은 서버 SIGTERM에 잘린 것(`project_nightly_regression_routine`).

**목표** — 영상의 핵심 장면: 실패 = 모델 탓이 아니라 오케스트레이션 타임아웃임을 화면에서 가려냄.

**설계 방향**
- `LlmActivityEvent`에 `errorKind: "timeout" | "model" | "connect" | "tool" | "guard"` 추가, 발신부 3곳에서 채운다. `llm_activity_daily`에 원인별 열(또는 별도 집계) — **원천은 여전히 하나**.
- 감독 화면 규칙 계산에 「원인별 상위」 한 줄 추가. LLM 문장 아님(기존 원칙 유지).
- ①과 같은 라운드에 넣으면 `steps[].errorKind`로 자연히 붙는다.

**예상 규모**: **소(반나절~1일)**. 단독으로도 된다.

### ③ 평가 게이트 결과의 **제품 화면 노출**

**지금 상태(실측)** — 게이트는 `tools/evalgate/run.mjs`(개발자 도구). 기준선 `baseline.json`: 2026-08-20 채택, 3축 138/138, 카나리 0. 고객은 모델 교체·BYOM·합성 뒤 「바꿔도 되나」를 **제품에서 볼 수 없다.**

**목표** — 영상의 1.0.0 vs 1.0.1 비교 화면.

**설계 방향(결정이 나면)**
- AI 팀 구성 화면에 **읽기 전용** 표시: 마지막 게이트 결과(기준선 rev·일시·3축 통과율·판정). 게이트 **실행**은 제품 안에서 돌리지 않는다 — 운영 4000·계정 1세션 직렬 자원, 학습 오염(`qa:true`) 문제.
- 「기준선 재확정(`--accept-baseline`)」은 사람의 채택 결정 그 자체 — 화면 버튼으로 만들지 않는다.

**결정 필요**: 계획서 중-3 밖이다. 사장님이 「판다」고 하실 때만 착수. **예상 규모**: 소~중(1~2일).

---

## 4. 기각 — 왜

| 영상 항목 | 기각 이유 |
|---|---|
| Hermes/텔레그램 게이트웨이 | 메일 본문을 외부 메시징+클라우드 LLM으로 보내는 구조. 온프렘 정체성과 정반대. 영상 본인도 이 대목에서 보안 정책에 걸림. |
| LangGraph 도입 | dispatcher/agentloop이 그 역할. 외부 다중에이전트 프레임워크는 2026-08-11 기각 원칙(직렬 자원 충돌·「같은 것을 여러 곳에」). |
| LLM Critic 에이전트 | 7B에 검수를 맡기면 못 믿는다. 결정적 관문(falseclaim·guidance-check)이 더 믿을 만함. 7B 규칙 교정 금지 원칙. |
| Find Skills·anti-UI-slop | 개발 도구 이야기. 우리는 design 스킬·시안 1개 규율로 갈음. |
| 목업을 실제 스택으로(영상 팁) | 이미 그렇게 한다(자체완결 HTML + gijo-ui.css). 새 규칙 아님. |

---

## 5. 착수 조건과 순서

- **트리거**: (a) 후-1 관측성 라운드가 열릴 때 (b) 사장님 지시 (c) 감독 화면에서 「오류 N건」의 원인을 물었는데 답을 못 한 실사고가 생길 때.
- **순서**: ②(작다, 단독 가능) → ①(②의 errorKind를 품는다) → ③(결정 뒤). ①은 **시안 1개(프로 셸 카드) → 설계관 → 구현 → 검토관 5갈래 병렬 → 수리 → 게이트 → 게시** 사슬 그대로.
- **하지 않을 것**: 게이트를 제품 안에서 실행 · 화면 전용 입력칸 · LLM이 쓰는 감독 문장 · 새 집계 원천.

---

## 6. 갱신 규칙 · 이력

- 이 문서는 요청 없이도 갱신하는 문서가 **아니다.** 관측성 작업을 받을 때 §3의 「지금 상태」를 **다시 실측**해 고치고(파일:줄이 밀렸을 수 있다), 결정이 나면 §0 상태 줄을 바꾼다.
- 후보를 닫으면 §3에서 지우지 말고 「닫힘 — 커밋 <해시>」로 남긴다(무엇이 바뀌었는지 아무도 모르게 되는 것을 막는다).

| 날짜 | 무엇 | 누가 |
|---|---|---|
| 2026-09-02 | 영상 대조 결과로 초안 작성. 상태=착수 안 함·결정 대기 | Claude(win) · 사장님 지시 |
