# GIJO AS 라이트 — 담당자 PC 올인원 배포 계획 (초안)

작성 2026-08-11 · 사용자 방향 확정: **라이트 전용 · 담당자 개인 PC 1대 · 기능 대폭 축소 · 계획서만**
관계: 전-7(제품화) · [라이트=같은 저장소 결정] · [라인업 2축] · [라이트 CUDA 6.48GB 실측]

> ⚠ 이 문서는 **방향 계획**이다. 구현 착수는 사장님 지시 후. WSL 이미지 배포(1번)는 「천천히」로 합의됨.

---

## 1. 목표 — 「클라만 받아도 그 PC에서 혼자」

라이트 에디션을 담당자 **개인 PC 한 대**에 **서버+클라 통째로** 얹어, 팀 서버(WSL 4000) 없이도
그 PC에서 혼자 쓰게 한다. 지금은 서버가 없는 PC에선 `localhost:4000`에 붙을 게 없어 로그인조차 안 된다 —
그 「서버 조각」을 담당자 PC에 미리 심어 두는 것이 이 계획이다.

---

## 2. 대상 환경 — 둘 중 하나 (그 외 미지원)

| 환경 | 최소 사양 | 서버가 도는 곳 | 엔진 |
|---|---|---|---|
| **Windows + NVIDIA GPU** | **VRAM 8GB** (RTX 3050/4060급) | WSL2 안 | CUDA |
| **Apple Silicon Mac** | **통합메모리 16GB** (M1~) | 네이티브(앱 안) | Metal |

- 근거: 라이트 실측 **6.48GB(CUDA) / 6.45GB(Metal)** — 8GB GPU·통합 16GB에 안착([[라이트 CUDA]]).
- ⚠ **GPU 없는 PC·저사양은 미지원.** Metal은 프리필이 느려(≈3.0ms/토큰) 8도구 축소가 특히 중요.

---

## 3. 라이트가 무엇인가 — 기능은 **사양서가 단일 출처**

> ⚠ **넣는 것·빼는 것·8개 도구 목록은 `GIJO_AS_라이트_사양` §3이 단일 출처다.** 이 배포 계획서는
> 「어떻게 굽고 보내나」만 다룬다 — 기능표를 두 벌 두면 한쪽만 고쳐진다(오늘 `buildRagQuery`에서 본
> 교훈 · Mac 지적 2026-08-11). 아래는 **배포 이미지에 담을 것**의 요약과 오해 방지뿐.

- **두뇌**: gijo-main **7.6B 1개** · ctx **8192** (이미지에 이 모델 gguf를 담는다).
- **도구**: 핵심 8개 — 목록은 사양서 §3(search·explain·remediation·ontology_query·knowledge_status·
  recent_documents·add_task·complete_task). 담당자 PC **개인 조수**용.
- **라이트에 없는 것**(배포 시 오해 방지 — 근거는 사양서/코드):
  - **법령 조회** — 폐쇄망에서 `www.law.go.kr`를 못 부른다(`airgap.ts:32`, 대체=사내 규정 RAG).
    실측: 「퇴사자 계정」이 법령 도구 **없이** 번들 문서만으로 「제21조·5일」 **10/10 일관**.
  - **취약점/자산 조회** — 담당자 PC엔 회사 자산 DB가 없다(넣어도 「없습니다」만 — 라이트에서 가장 위험한 실패).
  - **리포트** — 회사 데이터가 필요하고, 라이트(Metal 프리필)에서 가장 느리다.
  - AI-BOM · 레드팀 · 학습루프 · 3소스 상관분석(팀 기능).
- 축소는 `GIJO_EDITION=light` 플래그로 분기(같은 저장소 — 코드 한 벌, 상수만 갈림).

---

## 4. 배포 방식

### 🪟 Windows — WSL 이미지 통째 (`wsl --import`)
미리 구운 이미지 하나에 **전부** 담는다: Ubuntu + Node 서버(dist) + llama.cpp(CUDA 빌드) +
**7.6B·bge 모델(gguf)** + DB 초기값 + systemd 유닛.
- 설치본이 그 이미지를 `wsl --import`로 풀어 넣고 → 서버 자동 기동 → 클라가 `localhost:4000`으로 붙음.
- 환경 편차(드라이버·WSL 버전)의 대부분을 **우리가 미리 구운 이미지**로 흡수 → 유지 부담↓.
- ⚠ 이미지가 커진다(모델 수 GB) — 동봉 vs 첫 실행 다운로드는 §6 미결.

### 🍎 Mac — dmg + 모델 번들
arm64 dmg(서버 동봉, 이미 번들 서버 구조 있음 · main.ts:116) + 모델 gguf.
- 첫 실행 우클릭→열기(서명 없음). 서버가 앱 안에서 네이티브로 기동(Metal).

---

## 5. 단계 (제안 — 착수는 지시 후)

1. **라이트 에디션 플래그**(`GIJO_EDITION=light`) — 도구 8개·7.6B 기본·ctx8192·팀기능 비활성 분기.
   modeldefault 시험을 에디션별 상수로(같은 저장소 결정 반영).
2. **이미지 빌드 파이프라인**(Windows) — 서버+모델+systemd를 export해 배포용 `.tar` 굽기.
3. **설치본 통합** — 클라 설치 시 이미지 import(Win) / 서버·모델 동봉(Mac).
4. **첫 실행 preflight** — GPU(CUDA)/Metal·모델 파일·health 확인 후 `setup.html`. 실패 시 사람이 읽는 안내.
5. **첫 화면(setup) 통합** — 「관리자 만들기(=라이트 첫 설치) / 이미 서버 있어요(로그인)」 아코디언
   (시안 승인됨 2026-08-11 · `mockups/setup-bypass/시안.html`). ★ **관리자 만들기 갈래에 라이트 안내**
   (7.6B·핵심 8도구·개인 조수 · 팀 기능은 표준=회사 서버)를 넣는다 — **단, 라이트 에디션이 실제로
   구동될 때만**(지금 번들 서버는 표준이라 라이트라 쓰면 가짜다). 그래서 setup 우회+라이트 안내를
   **여기(라이트 구현)와 함께** 처리해 setup 화면을 두 번 안 건드린다.
   · 「로그인으로 →」는 login.html(서버 주소 입력)로 연결(main.ts/preload).
6. **실측** — 8GB GPU PC·Mac 16GB 각각에서 설치→기동→예열→답 확인(전-7 시연 기준).

---

## 6. 미결정 (사장님)

- **모델 동봉 vs 첫 실행 다운로드**: 동봉하면 설치본 6~15GB·오프라인 OK / 다운로드는 설치본 작으나 네트워크 필요.
- **라이트 가격·라이선스**: 라인업 판매가이드와 함께 정할 자리.
- **업데이트 경로**: 이미지 통째 교체 vs 서버만 rsync(담당자 PC엔 rsync 허브가 없다).

---

## 7. 왜 「완전 자동 설치」가 아니라 「이미지 배포」인가 (합의 근거)

완전 자동 설치(WSL·CUDA·드라이버까지 스크립트로)는 **환경 편차가 90%를 먹어** 유지가 만들기보다 크다
(우리가 [WSL NVIDIA 충돌]을 이미 겪음). **미리 구운 이미지**는 그 편차를 우리 쪽에서 한 번에 흡수하므로,
만드는 시간(초안 1~2주)·유지 부담 모두 작다. 완전 자동 설치는 그다음 단계 후보로 남긴다.
