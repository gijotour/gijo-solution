# GIJO AS — 지식 그래프·LLM 위키 도입 계획

**작성 2026-08-10 (Windows 세션)** · **상태: 계획(구현 전)** · **계획서 관계 아래 §8**
**2026-08-10 Mac 추가: §5 조각 ⓒ** — 접점을 쌓고(C-0) 검색이 쓰게 한다(C-1·C-2)
**⚠ 같은 절에 Mac의 정정 1건** — 「접점이 이미 있다」가 틀렸다. 착수 전 실측으로 잡았다

> 배경: 사장님이 NotebookLM 아티팩트(「NotebookRAG vs Graphify vs LLM Wiki 도입 의사결정」)와
> **Graphify형 지식 그래프**를 보고 "그래프 형태가 좋아 보인다, 도입 어떨까"를 물으셨다.
> 인터넷 조사(Karpathy LLM Wiki·Graphify) + 우리 코드 대조 후, **추천안**을 계획으로 정리한다.

---

## 0. 한 줄 판단

> **우리는 이미 「그래프」를 갖고 있다**(온톨로지 2,186 트리플). Graphify를 통째로 들이는 것은
> 실익이 적다. 진짜 새로운 것은 **LLM 위키(미리 정리해 두는 정리본)**인데, 우리 데이터는
> 고객 보안 판단의 근거라 **RAG를 위키로 대체하면 안 되고 보완만** 한다.
> → **얇은 세 조각**만 한다: ⓒ 있는 그래프를 **검색에 쓰기**(§5) · ⓐ **보여 주기**(전-7) ·
>   ⓑ 반입 문서를 **정리본으로**(후-6).
>
> ⚠ **ⓒ가 먼저다**(2026-08-10 Mac 추가). 그래프를 만들어 두고 **검색이 안 쓰고 있다.**
> 다만 착수 직전 재보니 **접점이 코드에만 있고 데이터에는 거의 없었다**(문서 트리플 3개, §5 정정).
> 그래서 ⓒ는 두 단계다 — **C-0 접점을 실제로 쌓고 → C-1 검색이 그것을 쓴다.**

---

## 1. 왜 — 세 개념의 실체와 우리가 이미 가진 것

### 세 개념 (2026-04 이후, 조사 실측)

| 개념 | 무엇 | 우리와의 관계 |
|---|---|---|
| **Graphify** | 폴더를 지식 그래프(노드+엣지)로. **코드베이스용**으로 뜬 오픈소스(무-LLM, 온디바이스) | 대상이 코드다 — 우리는 **보안 데이터**라 그대로 안 맞음 |
| **Karpathy LLM 위키** | RAG로 매번 뒤지는 대신, LLM이 원자료를 **한 번 위키로 정리**해 두고 참조. `raw/→wiki/→lint` 3단 | **개념은 새롭다** — 다만 개인 노트·코드용이라 그대로 쓰면 위험 |
| **NotebookRAG** | 구글 노트북LM식 문서 Q&A | 우리 RAG(LanceDB+bge-m3)가 이미 하는 일 |

### 우리가 **이미 가진 것** (실측 2026-08-10)

| 그들이 파는 것 | 우리 코드 | 실측 |
|---|---|---|
| 노드+엣지 지식 그래프 | `ontology.ts` 트리플(주어-관계-목적어) | **2,186개** · 관계 8종(완화통제 644·유형 431·전술 292·MITRE-ATLAS 26 …)<br>※ Mac 개발 DB에서는 2,166개 — 데이터 상태 차이다. 「이미 그래프가 있다」는 판단에는 영향 없다 |
| 문서→그래프 자동 연결 | `docdigest.ts` 온톨로지 접점 | 문서 반입 시 **접점 자동 생성** |
| lint(상충·중복 점검) | `kbhygiene.ts` | **완전/근접 중복·상충·신선도 이미 점검** |
| 6대 표준 오프라인 번들 | 온톨로지 하이브리드 | 폐쇄망 배송 가능 |

> **핵심**: Graphify가 "새로 만들어 준다"는 그래프를 우리는 **이미** 갖고 있고,
> 우리 것은 **보안 도메인(공격기법↔완화통제↔CVE)에 특화**돼 범용보다 우리에게 더 맞다.

---

## 2. 안 하는 것 (범위 경계 — 이걸 먼저 못 박는다)

- ❌ **Graphify 통째 도입** — 실익 적음(위 §1). 온톨로지가 이미 그래프다.
- ❌ **RAG를 LLM 위키로 전면 교체** — ⚠ 우리 데이터는 취약점 4,800건·자산·로그이고,
  "LLM이 정리한 위키"에 오답이 섞이면 그게 **고객 보안 판단의 근거**가 된다.
  RAG는 근거를 **원문으로** 대는데(근거 배지), 정리본은 그 원문에서 한 겹 멀어진다.
- ❌ **큰 신규 저장소·엔진 교체** — 온톨로지·RAG·kbhygiene가 이미 각 역할을 맡고 있다.

> 오늘(2026-08-10) 여러 번 겪은 **「전제가 틀린 큰 수리」**를 여기서 반복하지 않는다.
> 없는 것을 새로 만들기 전에, **있는 것을 쓴다.**

---

## 3. 조각 ⓐ — 있는 그래프를 **보여 주기** (전-7)

**계획서 전-7 ①「관계를 그림으로(…온톨로지)」의 구체화다.** 데이터는 이미 있으니 **UI만** 만든다.

| 슬라이스 | 무엇 | 통과 기준(전=시연) |
|---|---|---|
| A-1 | **온톨로지 그래프 화면** — 트리플을 점·선으로 그려 걸어다닌다(공격기법↔완화통제↔전술) | 시연: 「Log4Shell」을 눌러 연결된 완화통제·전술이 그림으로 펼쳐진다 |
| A-2 | **자산에서 그래프로 이어보기** — 한 자산의 취약점→관련 공격기법→통제를 한 화면 | 시연: 자산 하나에서 "이 위협은 어디로 번지나"를 선으로 |

- ⚠ **UI는 「추천 시안 1개」 먼저**(자체완결 HTML 목업, `mockups/온톨로지그래프/시안.html`) → 승인 후 구현.
  시안에 밀도 숫자·왜 이 안인지·제품 원칙 자가점검을 붙인다.
- ⚠ **viz.js 공용 모듈 재사용**(전-7에서 만든 것) — 새 그리기 엔진을 들이지 않는다.
- ⚠ **그래프는 보기용, 입력은 대화창**(제품 원칙) — 그래프 화면에 입력칸을 두지 않는다.
  "이 노드 뭐야?"는 대화창(screenguide+ⓘ)이 답한다.
- 실측: 트리플 2,186개가 화면에서 버벅이지 않는가(성능) · 관계 8종이 색·모양으로 구분되는가.

---

## 4. 조각 ⓑ — 반입 문서를 **정리본으로** (후-6, 파일럿 뒤)

**계획서 후-6「해자 강화」 아래.** 고객별로 쌓이는 지식 자산을 정리본으로 키운다.

| 슬라이스 | 무엇 | 통과 기준(후) |
|---|---|---|
| B-1 | **반입 문서 위키 페이지 자동 생성** — 지금 `docdigest`의 세 줄 요약을, 계속 갱신되는 정리본으로 | 새 문서가 같은 주제 정리본을 **갱신**한다(중복 페이지 안 만든다) |
| B-2 | **정리본 상충·신선도** — `kbhygiene`가 이미 하는 lint를 위키 페이지에 붙인다 | 옛 규정과 새 규정이 충돌하면 **표시**(삭제 아님 — 옛 규정도 유효할 수 있다) |

- ⚠ **정리본은 RAG를 대체하지 않는다** — 답의 근거는 여전히 **원문**으로 댄다(근거 배지 유지).
  정리본은 "빨리 훑어보는 목차" 역할이지, 판단의 최종 근거가 아니다.
- ⚠ **정리도 결정적으로** — 7B가 정리하다 지어내면 그게 위키에 굳는다.
  숫자·CVE·조문은 원문에서 **그대로 복사**, 문장만 다듬는다(오늘 여러 번 확인한 원칙).
- 실측: 같은 주제 문서 3건을 반입 → 정리본이 **1개로 합쳐 갱신**되는가(3개로 늘지 않는가).

---

## 5. 조각 ⓒ — 있는 그래프를 **검색에 쓰기** (2026-08-10 Mac 추가)

> 이 절은 Mac 세션이 실측 후 덧붙였다. 원안(ⓐ·ⓑ)은 그래프를 **보여 주고** 문서를 **정리한다**.
> 그런데 재보니 **그래프를 만들어 놓고 검색이 안 쓰고 있었다.** 새로 만들 것이 없고
> 이미 있는 두 층을 잇는 일이라, 셋 중 가장 싸면서 **측정된 결함을 직접 닫는다.**

### 실측 — 그래프는 만들기만 하고 찾을 때는 안 본다

| 어디서 | 무엇 |
|---|---|
| `memory.ts:630` | 문서 반입 시 **온톨로지 접점 생성**(문서↔자산↔제품↔취약점) |
| `memory.ts:998` | 문서 삭제 시 트리플 정리(고아 방지) |
| `queryMemoryScored` → `hybridSearch(question, topK, agentId, screen, viewer)` | **벡터 + 어휘만.** 온톨로지 인자가 없다 |
| `dispatcher.ts` · `verifyrag.ts` · `actioncheck.ts` · `kbhygiene.ts` | 온톨로지 참조 **0건** |
| `ontology.ts`를 import 하는 13개 파일 | 전부 **씨앗·정리·표시** 쪽(seed·docdigest·docenrich·teamview…). **검색 파일은 하나도 없다** |

> 즉 접점은 반입 때 심고 삭제 때 치우는데, **그 사이 정작 찾을 때는 아무도 안 본다.**

### 이것이 닫는 결함 (2026-08-10 실측)

고객이 「취약점 알려주세여」라고 물었더니 **다른 회사의 웹 점검 보고서**가 근거가 됐다.
`(주)안전대부 서비스 웹취약점 점검 결과 보고서_v1.0.pdf` — 우리 고객 자산과 무관한 문서다.
지어낸 답은 아니었지만(RAG는 제 일을 했다), **텍스트 유사도만 보니 「누구의 것인가」를 못 가렸다.**

### ⚠ 정정 — 접점은 **코드에만 있고 데이터에는 거의 없다** (2026-08-10 Mac 재실측)

이 절을 처음 쓸 때 「온톨로지 접점에 문서↔자산 구분이 **이미 들어 있다**」고 적었다.
**틀렸다.** 착수 직전에 트리플을 열어 보고 알았다:

| 실측 | 값 |
|---|---|
| 트리플 총수 | 2,166개 |
| 관계 종류 | 완화통제 644 · 유형 431 · 설명 294 · 전술 292 · 상위기법 69 · MITRE-ATLAS 26 · OWASP-LLM 25 … **전부 보안 지식 분류** |
| 문서가 주어·목적어로 등장하는 트리플 | **3개** (그마저 ATLAS 기법 설명이고 문서↔자산 관계가 아니다) |
| `(주)안전대부…pdf`가 온톨로지에 있는가 | **없다** |

원인은 `memory.ts:630`의 바로 그 줄에 적혀 있었다 —
> 「온톨로지 접점을 만든다. docsbundle 같은 **프로그램 수집은 uploadedBy가 없어 자연히 건너뛴다**」

지금 지식베이스 93건 대부분이 프로그램 수집(문서 번들·표준 코퍼스)이라 접점이 안 생겼다.
**접점을 만드는 코드는 있지만 실제 데이터에는 거의 안 걸려 있다.**

> ⚠ 같은 실수를 그날 세 번째로 했다 — **「코드에 있다」와 「데이터에 있다」를 구분하지 않았다.**
> `requirements.txt`에 적혀 있는데 설치가 안 돼 있던 것, 배포 목록에 `.py`가 없던 것과 같은 계열이다.
> 앞으로 이 계획의 어떤 조각이든 **착수 전에 데이터를 한 번 열어 본다.**

### 그래서 조각이 하나 앞에 붙는다

| 슬라이스 | 무엇 | 통과 기준 |
|---|---|---|
| **C-0** | 반입 문서에 접점이 **실제로 생기게** 한다 — 지금은 `uploadedBy`가 없으면 건너뛴다. 프로그램 수집분·기존 93건에도 접점이 붙어야 한다 | 문서가 등장하는 트리플이 **3개 → 유의미한 수**로 늘어난다. 안전대부 보고서가 어느 자산에도 안 이어진다는 것이 **데이터로** 확인된다 |
| C-1 | 검색 후보에 **온톨로지 접점 신호**를 더한다 — 물어본 자산·제품에 이어진 문서를 올리고, 이어지지 않은 문서를 내린다 | 같은 질문에서 남의 회사 보고서가 **1위에서 빠진다**. 우리 자산에 이어진 문서가 올라온다 |
| C-2 | 답의 근거에 **어떤 관계로 걸렸는지**를 함께 남긴다(문서명만이 아니라 「이 자산에 이어짐」) | 근거 배지에 관계가 보인다 — 담당자가 왜 이 문서인지 안다 |

> ⚠ **C-0 없이 C-1을 하면 아무 일도 안 일어나거나 나빠진다.** 접점이 없으면 모든 문서가
> 동률(접점 0)이 되어 순서가 그대로거나, 접점이 있는 3건만 부당하게 올라간다.
> **C-0에서 접점이 실제로 쌓이는지 재본 뒤에** C-1으로 간다.

- ⚠ **가중치를 세게 주지 않는다.** 접점이 없는 문서를 **버리면** 안 된다 — 아직 접점이 안 생긴
  새 문서가 통째로 안 보이게 된다. 순서를 바꾸는 정도로 시작하고 실측으로 조정한다.
- ⚠ **문턱(0.95)을 건드리는 것과 섞지 않는다.** 그건 별개 문제로 따로 재고 있다(Windows 측).
  두 개를 같이 움직이면 어느 쪽이 효과인지 못 가린다.
- ⚠ 실측 기준선이 이미 있다: `ops-sim` 152문항(`.tmp-reports/ops-sim.json`)과
  `route-explain --겹침`. 바꾸기 **전에** 한 번 재고, 바꾼 뒤 같은 것으로 다시 잰다.

---

## 6. 리스크·함정

| 함정 | 왜 위험 | 막는 법 |
|---|---|---|
| "그래프 새로 만들자" | 온톨로지를 두고 중복 구축 | **있는 트리플을 쓴다**(§1 실측) |
| RAG를 위키로 교체 | 근거가 원문에서 멀어짐 → 보안 판단 오류 | **보완만**, 근거는 원문 유지 |
| 7B가 위키를 지어냄 | 오답이 정리본에 굳음 | 숫자·조문은 원문 복사, 문장만 |
| 그래프 화면에 입력칸 | 제품 원칙(입력은 대화창) 위반 | 보기용만, ⓘ로 안내 |
| 1차 목표를 미룸 | 파일럿 핵심(분석 허브)이 밀림 | **ⓒ·ⓐ만 파일럿 전, ⓑ는 뒤로** |
| 그래프를 **보여만** 주고 안 씀 | 지금 상태가 그렇다 — 만들어 두고 검색이 안 본다 | **ⓒ를 먼저**(§5 실측) |
| ⓒ에 가중치를 세게 줌 | 접점 없는 새 문서가 통째로 안 보임 | 순서만 바꾸고 실측으로 조정(§5) |
| **C-0 없이 C-1** | 접점이 없어 전부 동률 — 안 바뀌거나 3건만 부당하게 올라감 | **C-0에서 접점이 쌓이는지 먼저 잰다**(§5) |
| 「코드에 있다」를 「데이터에 있다」로 읽음 | 이 계획이 실제로 그렇게 한 번 틀렸다(§5 정정) | **착수 전에 데이터를 연다** — 그날 세 번 반복된 실수다 |

---

## 7. 순서 (추천)

1. **ⓒ C-0** — 반입 문서에 접점이 실제로 생기게 한다. **이것이 안 되면 뒤가 전부 무의미하다**(§5 정정).
   끝나면 「문서 트리플이 몇 개로 늘었나」를 숫자로 확인한다.
2. **ⓒ C-1** — 검색이 그 접점을 보게 한다. 바꾸기 전/후를 ops-sim 152문항으로 같은 잣대로 잰다.
3. **ⓒ C-2** — 근거에 「어떤 관계로 걸렸는지」를 싣는다
4. **ⓐ A-1 시안** → 승인 → 구현·검증(전-7) — 사장님이 보신 "그래프 형태"의 실물
5. **ⓐ A-2** — 자산 연결
6. (파일럿) — 실사용 4주
7. **ⓑ B-1·B-2** — 파일럿에서 "정리본이 필요하다"가 실제로 나오면 착수

> **왜 ⓒ가 ⓐ보다 앞인가**(2026-08-10 Mac): ⓐ는 전-7 시연 항목이고 통과 기준도 「시연」이다.
> 그 자체로 값이 있지만, 그날 잰 지식베이스 실패 5건 중 **ⓐ가 닫는 것은 0건**이다
> (남의 회사 문서가 근거로 나옴 · analyze가 근거를 안 실음 · 법령 라우팅 · 문턱 0.95 ·
> DATA_TOOL_RE가 action을 삼킴). ⓒ는 그중 첫 번째를 직접 닫는다.
> 그리고 ⓒ로 그래프가 실제로 쓰이면, ⓐ에서 **보여줄 이유도 더 분명해진다.**

> ⚠ **ⓑ는 실사용 신호가 온 뒤에 판단한다.** 지금 만들면 "쓸까 말까 모르는 것"에 힘을 쓴다.
> 고객이 "문서가 쌓여서 못 훑겠다"고 하면 그때가 신호다.

---

## 8. 계획서(전중후) 관계 — 결정 대기(사용자)

| 이 계획 | 전중후 계획서 매핑 | 새 항목인가 |
|---|---|---|
| 조각 ⓐ (그래프 화면) | **전-7 ①**의 구체화 | 아니오 — 이미 있는 항목 |
| 조각 ⓑ (위키 정리본) | **후-6**(해자) 아래 신규 슬라이스 | **예 — 결정 필요** |
| 조각 ⓒ (접점을 쌓고 검색이 씀) | 기존 RAG·docdigest 개선 — 새 저장소·엔진 없음 | **예 — 결정 필요**(Mac 제안) |

**여쭙습니다**:
- **ⓒ를 ⓐ보다 먼저** 두는 것(2026-08-10 Mac 제안)에 동의하시는지.
  ⓒ는 새로 만들 것이 없고 측정된 결함을 닫지만, 검색 순서를 건드리므로 **회귀를 재야 한다**.
- ⓐ만 전-7로 먼저 하고, ⓑ는 파일럿 뒤로 미루는 것(**원안 추천**)에 동의하시는지.
- 아니면 ⓑ(위키 정리본)도 지금 우선순위에 올릴지 — 그러면 1차 목표(분석 허브)가 그만큼 밀린다.

정해 주시면 **ⓒ C-1**(검색 개선 — 시안 없이 코드+실측)이나 **ⓐ A-1 추천 시안 1개**부터
시작한다. ⓐ는 곧바로 구현하지 않는다(시안 승인 먼저).

---

*조사 출처: Karpathy LLM Wiki(2026-04 GitHub Gist) · Graphify(Safi Shamsi, Apache 2.0) · Analytics Vidhya·Data Science Dojo 가이드.*
