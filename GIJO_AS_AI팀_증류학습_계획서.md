# GIJO AS — AI 팀 증류·학습 계획서 (전문 팀원을 만들고, 쓰면서 키운다)

- 작성: 2026-09-03 (사장님 「이번 세션이 다 관련된 내용 — 고민해서 계획서 만들어서 진행하고 싶어, 원래 하고 싶었던 부분이니까」)
- 성격: **살아 있는 계획서.** 단계마다 「지금 상태」를 다시 재고 이력에 남긴다. 실행은 이 문서의 결정 칸(§9)이 채워진 뒤 단계 순서대로.
- 전중후 계획서와의 관계: **중-4** 「게이트 위에서 합성·학습 향상 개시 — 후보함→데이터셋→튜닝→게이트 통과분만 배포」의 실체. **후-6** 해자(고객별 축적 자산)의 실체. **전-7** gb10 = 「운영을 멈추지 않는 검증 기계」의 첫 상시 임무. 짝 문서: `GIJO_AS_LLMOps_관측성_계획서.md`(팀원이 무엇을 했는지 보는 쪽), `GIJO_AS_에이전트_자동화_계획서.md`(2026-07-17, 「보는 도구에서 시키는 도구로」).
- 판정 원칙: **코드·운영 데이터로 확인한 것만 「있다」고 적는다.** 아래 숫자는 2026-09-02~03 실측이며 낡으면 §1부터 고친다.

---

## 0. 목표와 성공 판정

**사장님의 꿈(이번 세션에서 말씀하신 그대로)**
1. 다양한 전문성 있는 LLM(팀원)으로 작업 효율을 높인다. 지금은 「큰 모델 하나」로 도는 느낌이다.
2. **6개 이상의 에이전트가 실제로 동작**한다.
3. 프로 모드에는 **실제 고객 환경에 맞게 만드는 기능**이 있다.
4. 전문 AI 팀을 **할당해 놓고 사용하면서 성장**한다.
5. gb10의 큰 모델로 우리 지식 코퍼스·자산·규정에 **근거를 인용해** 주제별 문답을 만들고(증류), **에이전트 특성별** 재료로 학습해 **기본 모델을 만들어 넣고**, 그 뒤로도 팀원들이 계속 배운다. 교사는 **에이전트마다 알맞은 모델**을 가져와도 된다. **파일을 읽어야 하는 에이전트**(긴 문맥)를 고려한다.

**성공 판정(숫자로)**
| 잣대 | 지금 | 목표 |
|---|---|---|
| 팀원별 채택 어댑터 | 0 | ≥5 (scan·analysis·report·ti·normaltic) |
| 실제 LLM 호출이 있는 팀원(30일) | 4/6 (scan·ti = 0) | 6/6 |
| 평가 게이트 | 3축 138문항 100% | 3축 무하락 + **에이전트 축 신설** 통과 |
| 주제별 승인 재료 | 취약점 40·장비운영 22·사내규정 6·위협대응 0 | 각 ≥300 |
| 중-2 KPI(아낀 시간) | 기준선 | 어댑터 채택 전후 비교로 증가 |

---

## 1. 왜 지금 이렇게 보이나 — 실측 (2026-09-02~03)

| 사실 | 근거 |
|---|---|
| 30일 LLM 호출: orchestrator 2,394(87%) · analysis 176 · report 151 · normaltic 17 · **scan 0 · ti 0** | `llm_activity_daily` |
| 6명 전부 `qwen3-14b`로 배정돼 있었다(부팅 기본은 7.6B → 첫 대화에서 14B를 또 올림, 주 20회). **사장님 「의도 아님」 → 2026-09-03 해제.** 누가 배정했는지 감사 기록 없음 | `app_state agentModel:*`, server.log, `setAgentModel`에 recordAudit 없음 |
| 팀원 차이 = 이름·역할 한 줄·약자 + RAG 우선영역 soft boost(scan→취약점·장비운영, analysis→취약점, ti→위협대응, report→사내규정). **도구 권한·일감 트리거는 팀원별로 없다** | `agents.ts`, `hybridsearch.ts ROLE_CATEGORY` |
| 어댑터 등록 1(`sec-expert-vuln-v1`, 40/300 강행) · 채택 0. **런타임은 준비됨**: 스폰 시 `--lora-init-without-apply`, 요청별 `lora:[{id,scale}]`, 캐시 오염(#26207) 방어 `cache_prompt:false` | `localengine.ts:625-646, 1154-1166` |
| **승인 문답은 학습 재료(chat_logs rating=1)로만 쌓이고 기억(RAG)에는 안 들어간다.** 오탐 족보 `fppattern.ts`는 소비처 0 | `learnloop.ts:111-128`, grep |
| 학습 스크립트는 transformers+peft **QLoRA 4bit**(`finetune_qlora14b.py`), 3090 24GB 기준. 긴 시퀀스 설정 없음 | `trainenv.ts`, 스크립트 109-136행 |
| 파일 분석이 LLM에 넘기는 문맥: 웹 취약점 리포트 **앞 20,000자**, 문서 요약 **5,000자**. 채팅 ctx 32,768 · 한 요청 최소 8,784토큰(도구 카탈로그) · 답 800토큰 | `webreport.ts:64`, `docdigest.ts:102`, `localengine.ts:1008` |
| LoRA 2회전 실패 원인 = **재료 40건**(형식만 배우고 사실 오류·한글 하락·2.4배 느림). 방법이 아니라 재료 | `project_lora_round2` |

---

## 2. 원칙 — 지키지 않으면 이미 실패해 본 이유가 있다

1. **별도 모델 N개가 아니라 베이스 1 + 어댑터 N.** 24GB에 2개·48GB에 3개가 물리 한계. 2026-07-17 2모델 협업 실증이 「어정쩡」으로 끝났다. 합성(SLERP)은 베이스를 고를 때만, 게이트 뒤에서.
2. **성장은 두 겹.** ① 기억 성장(즉시·되돌리기 쉬움: 승인 문답·조치 이력·오탐 판정을 그 팀원의 지식영역에 바로 반입) ② 가중치 성장(주기·게이트: 재료 문턱마다 어댑터 vN → 게이트 → 사람 채택). 업계 2026 표준도 이 하이브리드(빠른 갱신은 기억, 가중치는 주기적 검증).
3. **자동으로 어디에도 먹이지 않는다.** 데이터셋에 들어가는 유일한 문은 담당자 승인(`learncandidates.ts` 원칙). 증류 재료도 예외 없음 — 다만 §3.5의 「표본 검수 + 규칙 검증」 방식은 사장님 결정.
4. **근거 없는 문답은 재료가 아니다.** 증류 문답은 근거 조각 id를 달고 나오고, 코드가 인용이 실재하는지·본문에 그 내용이 있는지 검증한다. 교사 환각을 학습하는 길을 막는 첫 문.
5. **출하 베이스는 고객 데이터로 학습하지 않는다.** 자산·규정·고객 매뉴얼로 만든 재료는 **그 현장의 어댑터**에만 쓴다. 출하용 v0는 우리 코퍼스·공개 문서·시연 시드로만.
6. **게이트 없이 채택 없다.** 평가 게이트 3축(+에이전트 축) + A/B + 카나리 0 + 견고성 9회 중앙값. 자동 채택 금지. 기준선 재확정(`--accept-baseline`)은 사람.
7. **라이선스 게이트.** 교사·재료·베이스 셋 다 허가 라이선스만(§3.1 표). 클라우드 API 모델(Claude·GPT·Gemini) 출력은 약관상 학습 재료로 쓰지 않는다. 비상업 모델(EXAONE·Gukbap 등) 출력도 안 쓴다.
8. **학습·증류는 gb10, 운영은 win.** 운영(4000·3090)을 멈추지 않는다. gb10은 통합메모리 128GB라 3090이 못 하는 **bf16·긴 시퀀스** 학습이 된다.
9. **팀원이 실제로 움직이는 건 두뇌만으로 안 된다.** 일감(트리거)·권한/지식영역·두뇌 세 층이 각각 갈려야 한다(§7 병행).

---

## 3. 재료 만들기(증류) 설계

### 3.1 교사 모델 — 에이전트마다 알맞은 것을 고른다 (라이선스가 첫 잣대)
| 교사 | 라이선스 | 어디에 | 비고 |
|---|---|---|---|
| **Qwen3.8-27B**(dense) | Apache 2.0 | 한국어 서술·우선순위 판단·보고서(report·analysis·normaltic) | 한국어 공개 벤치 없음 → 스모크로 직접 잰다 |
| **Qwen3.8-Flash-Next 125B-A6B**(gb10 8080에 이미 가동) | **Qwen Community License 1.0** | 일반 지식·긴 문맥 요약 | 출력물 사용 제한 조항 **없음**. 2조 「Model as a Service 또는 AI Work Assistant 사업이면 별도 라이선스」 — 정의상 AI Work Assistant는 「코딩·사무 생산성 보조」이며 **「그 밖의 도메인용 보조(예: 쇼핑 보조)는 제외」**라 보안운영 보조인 우리는 예외로 읽힌다. 내부 사용(교사)은 명시 예외. **프로 원격 두뇌로 고객에게 내주는 것**은 법무 확인 1건(§9-5) |
| Qwen3-Coder-30B-A3B | Apache 2.0 | **파일 읽기**(스캐너 리포트·로그·설정 → 구조화) scan | gb10에 있었으나 자리 때문에 내림 — 27B와 교대 적재 |
| 보안 파인튜닝(RedSage-Qwen3-8B 등) | **확인 필요** | 보안 지식 문답 | 라이선스 확인 전 사용 금지 |
| ~~EXAONE · Gukbap · 클라우드 API~~ | 비상업/약관 | — | **금지** |

### 3.2 근거 출처 — 「외부 인터넷으로 기본 내용 학습」의 답
| 출처 | 허용 | 조건 |
|---|---|---|
| 우리 지식 코퍼스(공개 권위 가이드 9종 + 우리 글로 되찾은 7종) | ✅ | 이미 반입돼 있음(문서 115) |
| 법령(법제처 API) | ✅ | 조문 번호 인용 |
| 공공누리·NIST·OWASP 등 공개 문서 | ✅ | 라이선스 표기·**우리 글로 재작성** 원칙(2026-08-22 「20자 겹침 실측」 방식) 유지 |
| 인터넷 일반 문서·블로그 | △ | 저작권 확인된 것만. 「근거」로 쓰되 원문을 재료에 싣지 않는다 |
| 클라우드 모델이 만든 글 | ❌ | 약관 |
| 고객 자산·규정·매뉴얼 | ✅ **현장 전용** | 출하 베이스에는 안 쓴다(원칙 5) |

### 3.3 에이전트별 재료 종류 (「특성에 맞는」의 구체)
| 팀원 | 재료 종류 | 입력 크기 | 교사 |
|---|---|---|---|
| **scan (파일을 읽어야 하는 팀원)** | 스캐너 리포트·로그·설정 **발췌 → 구조화 JSON**(자산·취약점·심각도·근거 줄) | **길다**: 제품이 실제로 넘기는 크기(리포트 앞 20,000자 · 로그 청크)로 맞춘다. 학습 시퀀스 8K~16K | Coder-30B-A3B |
| analysis | 우선순위 판단 근거(KEV·CVSS·자산 중요도·노출) — 결론 + 근거 인용 | 중 | 27B |
| report | 보고서·조치 요청서 서식, 사내규정 인용 | 중 | 27B |
| ti | 피드↔자산 매칭 해석, 「해당 없음」 절제 | 짧음 | 27B / Flash-Next |
| normaltic | 용어 해설·사례 부연(코퍼스 근거) | 짧음 | 27B |
| orchestrator | 도구 선택·인자 추출 트레이스(라우팅 축) | 짧음 | 27B — **어댑터 금지**(설계 원칙) → 베이스 병합 재료로만 |

⚠ **파일 읽기는 학습만으로 풀리지 않는다.** 긴 문맥의 절반은 제품 경로(파서·청크·발췌)가 감당한다. 학습은 「제품이 실제로 건네는 발췌」에서 구조화를 잘하도록 맞춘다. 학습 예제 길이가 제품 발췌 길이와 다르면 현장에서 안 맞는다.

### 3.4 수량·시간
- 주제당 **500 생성 → 승인 300** 목표. 24 tok/s(Flash-Next) 기준 답 400토큰 × 500 ≈ 2.3시간. 27B는 gb10에서 12~35 tok/s. 파일 읽기 예제는 입력이 길지만 출력이 짧아 비슷.
- 4주제 + 파일 읽기 = 약 2,500건 생성, 교사 시간 ≈ 12~15시간(밤에 돌린다).

### 3.5 증류기 산출 규격
- 후보함(chat_logs)에 **`source="distill"` 컬럼 신설** + 교사 모델 id · 근거 조각 id 목록 · 프롬프트 해시(재현성). 실대화·작업내역·증류 세 출처가 한 후보함에서 같은 승인 문을 지난다.
- 인용 검증(원칙 4)에 실패한 문답은 후보함에 **안 들어간다**(버린 수를 센다 — 교사 품질 지표).
- 승인 방식은 **사장님 결정(§9-3)**: ⓐ 전수 승인(가장 안전, 300건 승인 부담) ⓑ 규칙 검증 통과분을 표본 검수(예: 10%)로 일괄 승인.

---

## 4. 학습과 「기본 모델」 만들기

### 4.1 학생(베이스) 후보
| 후보 | 라이선스 | 24GB(표준)에서 | 장단 |
|---|---|---|---|
| **Qwen3-14B** (1순위) | Apache 2.0 | 32K ctx 들어감, ~70 tok/s | 현행 학습 스크립트 대상·기술 깊이 최고 실측(2026-08-05) |
| Qwen3.8-27B (2순위) | Apache 2.0 | Q4 4K ctx 18GB · 64K 22GB, 34~40 tok/s | **긴 문맥 유리**(하이브리드라 KV 작음) · 속도 1/3 · 두 번째 모델 자리 없음 · 한국어 미검증 |
| 7.6B(gijo-main) | Apache 2.0 | 라이트 8GB | 라이트용 학생은 **별도 증류**(같은 재료, 작은 학생) |

### 4.2 학습 환경 = gb10
- 128GB 통합메모리: 14B **bf16 LoRA**(양자화 없이) + 시퀀스 8K~16K 가능. 3090은 4bit·짧은 시퀀스만.
- 길: ⓐ NVIDIA `dgx-spark-playbooks/nvidia/unsloth` 도커(Triton·xformers를 Blackwell용 소스 빌드, PyTorch 25.09 컨테이너) ⓑ 현행 transformers+peft를 bf16로. **ⓑ부터**(우리 스크립트 그대로, 의존성 적음) → 느리면 ⓐ.
- ⚠ 함정(실사고): npm allow-scripts처럼 **「설치됐다 ≠ 돈다」** — 첫 학습은 100스텝 스모크로 손실 하강·GGUF 변환·적재까지 끝까지 한 번 돌린다.

### 4.3 산출물
- 공통 재료(orchestrator·일반) → LoRA → **베이스에 병합**(peft merge_and_unload) → GGUF Q5_K_M → `gijo-base-v2`(출하 기본 모델 후보).
- 팀원 재료 → **어댑터 GGUF 5종**(vuln·ops·policy·threat·misc) → `data/lora/` 등록 → 게이트 → 채택 → 팀원 배정(`/api/agents/:id/adapter`).
- 라이트: 7.6B 학생 + 어댑터 없음(겹1만).

---

## 5. 게이트·평가
- 기존 3축(라우팅 66·안전 48·한국어 24) **무하락** + 카나리 0 + 견고성 9회 중앙값.
- **에이전트 축 신설**: 파일 분석 정확도(발췌→JSON 필드 일치) · 우선순위 판단(기대 등급) · 서식(필수 항목 존재) — 전부 **결정적 채점**(LLM 채점 금지, 게이트 원칙).
- A/B: `scripts/lora-ab.mjs`(운영 엔진 내리고 8090) — gb10에서 돌리도록 옮겨 운영 무중단.
- 채택은 사람. 어댑터 note에 게이트 결과 해시.

---

## 6. 현장 성장 — 프로 「우리 환경 맞춤」
1. **겹 1 배선**: 승인 → 그 팀원 지식영역(카테고리)에 조각으로 반입(기존 반입 엔진·`ROLE_CATEGORY` 재사용). 오탐 족보·조치 이력 소비 배선. 되돌리기 = 조각 삭제.
2. 후보함 출처 3(실대화·작업내역·증류)이 같은 승인 문.
3. **vN 재학습 제안 트리거**: 주제별 승인 **+100마다** 「학습할까요」 카드(자동 실행 아님). 학습은 고객 GPU 또는 우리 gb10(프로 원격) — 결정 §9-6.
4. 프로 마법사(시안 필요): 반입 → 주제별 재료 → 승인 → 학습 → 게이트 → 채택 → 팀원 배정. 게이트·A/B의 **제품 화면 노출**은 LLMOps 계획서 ③과 같은 결정.
5. **BYOM(고객이 가져온 베이스) — 이 계획의 한 축이다**(2026-08-08 재설계 「베이스 = 고객 BYOM 1개, 팀원 = 어댑터」 그대로). 두 길로 갈린다:
   | | 우리 기본 모델(gijo-base-v2) 고객 | BYOM 고객 |
   |---|---|---|
   | 첫날 전문성 | 출하 어댑터 v0 5종 + 지식영역·권한 | 지식영역·권한·자동 적응(modelquirks)만 — **우리 v0 어댑터는 못 붙는다**(어댑터는 베이스에 종속) |
   | 겹 1 기억 성장 | 즉시 | 즉시(베이스 무관) |
   | 겹 2 어댑터 | 현장 승인 → v1… | 현장에서 **자기 베이스 위에** v0부터 굽는다(프로 마법사). 재료는 §9-7의 「재료 팩」 또는 우리 gb10 원격 학습 |
   | 조건 | — | ① 라이선스 게이트 통과(비상업 베이스면 학습·배포 경고) ② **HF 원본 가중치 확보** — GGUF만 가져오면 어댑터 학습이 안 된다(2026-07-24 실측: 합성 GGUF만 있던 gijo-main은 학습 불가) ③ 아키텍처가 우리 학습 스크립트 범위 안 |
   ⚠ 「고객 환경에 맞게 만드는 기능」의 값어치는 BYOM에서 가장 크다 — 우리가 모르는 베이스 위에서도 그 현장의 팀원이 생긴다. 반대로 우리 v0 어댑터는 BYOM에 아무 소용이 없으니, BYOM 고객에게 줄 것은 **어댑터가 아니라 재료·절차·게이트**다.

---

## 7. 병행 — 팀원이 실제로 동작하려면 (두뇌 밖의 두 층)
- **일감**: scan·ti에 오는 일이 0인 이유는 트리거가 없어서다. 지켜보는 폴더(5.77.0)·스캔 반입·CTI 피드 갱신을 팀원 일감으로 연결하고, 상시 루프는 정직하게(없으면 「모니터링」이라 안 쓴다).
- **권한·지식영역**: 팀원별 도구 허용목록(지금은 전원 동일) + 지식영역을 soft boost에서 **범위 계약**으로.
- 설계관에 먼저 태운다(§8 단계 0).

---

## 8. 단계·일정 (승인 병목 포함 6~8주)
| 단계 | 무엇 | 산출 | 예상 | 게이트 |
|---|---|---|---|---|
| 0 | 결정(§9) + 설계관 검토(증류기·후보함 컬럼·겹1 배선·일감/권한 분리) | 검토 보고 | 2~3일 | 설계관 |
| 1 | **증류기 v1**(win 스크립트, gb10 교사 API): 근거 조각 → 문답 → 인용 검증 → 후보함 `distill` | 스크립트·시험·후보 500(취약점) | 1주 | 검토관 5갈래 |
| 2 | 에이전트별 재료(4주제 + scan 파일 읽기, 교사 교대 적재) | 후보 2,500 → 승인 ≥300/주제 | 1~2주(승인 속도가 상한) | 인용 검증 통과율 |
| 3 | gb10 학습 환경(bf16·긴 시퀀스) + 100스텝 스모크 | 환경·재현 절차 | 3일 | 끝까지 1회 |
| 4 | **기본 모델 v2**: 공통 LoRA → 병합 → GGUF → 3축+에이전트 축 → A/B | `gijo-base-v2` | 3~4일 | 게이트 통과·사람 채택 |
| 5 | 팀원 어댑터 v0 5종 → 등록 → 게이트 → 채택 → 배정 | 어댑터 5 | 1주 | 〃 |
| 6 | 겹 1 배선 + vN 제안 카드 + 프로 마법사 시안 | 배선·시안 1개 | 1주 | 시안 승인·검토관 |
| 7 | 게시(허락)·운영 배포·라이브 검증·이 문서 갱신 | 5.8x.0 | 2일 | 실화면 관문 |

---

## 9. 결정 필요 (사장님)
1. **교사에 Flash-Next 사용**: 라이선스 원문상 내부 사용·출력 제한 없음 → **사용 가능으로 읽힌다.** 다만 §3.1 2조는 법무 한 줄 확인을 권한다. 확인 전엔 27B(Apache) 우선.
2. **학생 베이스**: 14B(1순위) / 27B(긴 문맥). 추천 14B로 시작, 27B는 파일 읽기 팀원에 한해 실측 후.
3. **증류 재료 승인 방식**: ⓐ 전수 / ⓑ 규칙 검증 + 표본 검수. 추천 ⓑ(전수는 300×5주제를 사람이 못 넘긴다) — 단 첫 주제(취약점)는 ⓐ로 교사 품질을 눈으로 확인.
4. **기본 모델 v2 게시 = minor**(새 기능)로 본다. major(구조 개편)는 아님 — 다르게 보시면 알려 주십시오.
5. **프로 원격 두뇌(Flash-Next) 라이선스**: 2조 해당 여부 법무 확인 1건(정의상 예외로 읽히나 문서로 남긴다).
6. **현장 vN 학습 장소**: 고객 GPU(느림·독립) / 우리 gb10(빠름·데이터 반출) — 파일럿 고객과 정한다.
7. **BYOM 고객에게 v0 재료를 어떻게 주나**: ⓐ 승인 데이터셋을 「재료 팩」으로 동봉(BYOM 위에서 바로 v0를 굽는다 — 단 **재료는 후-6 해자 그 자체라 베끼기 쉬워진다**) / ⓑ 재료는 비공개로 두고 **우리 gb10에서 원격으로 구워 어댑터만 전달**(프로 서비스, 고객 베이스의 HF 가중치를 받아야 함) / ⓒ 둘 다(팩은 암호화·라이선스 묶음). 추천 ⓑ부터 — 해자를 지키면서 BYOM에도 첫날 전문성을 준다.

---

## 10. 위험·함정
- **교사 환각 학습**: 인용 검증 + 승인 + 게이트 3중. 검증 실패율이 30%를 넘으면 교사·프롬프트를 바꾼다.
- **자기 오염**: 학생이 만든 답이 재료로 되돌아오는 것 — `source` 컬럼으로 구분, 증류 재료는 학생 출력 금지.
- **망각·안전 하락**: 계획서 실증(무해 데이터 1에폭에 유해응답 5.5%→31.8%). 어댑터는 얇게(rank), 베이스 병합은 공통 재료만, 카나리 0.
- **긴 문맥 = VRAM**: 24GB에서 14B 32K가 상한. 파일 읽기는 제품 발췌 크기와 학습 길이를 맞춘다(§3.3).
- **승인 병목**: 300×5 = 1,500 승인. §9-3 ⓑ 없으면 일정이 두 배.
- **「설치됐다 ≠ 돈다」**: gb10 학습 환경·GGUF 변환·적재까지 끝까지 한 번(§4.2).
- **기준선 오염**: 게이트·A/B 실행은 `qa:true`로 세션·학습 수집을 건너뛴다(중-3 원칙).

---

## 11. 용어
- **증류**: 큰 모델(교사)이 만든 문답으로 작은 모델(학생)을 가르치는 것.
- **어댑터(LoRA)**: 베이스는 그대로 두고 얇은 층만 덧붙여 전문성을 주는 방식. 팀원 수만큼 둬도 VRAM이 거의 안 는다.
- **베이스 병합**: 어댑터를 베이스 가중치에 녹여 하나의 모델 파일로 만드는 것 — 「기본 모델 v2」.
- **인용 검증**: 문답이 가리키는 근거 조각이 실재하고 그 내용이 답에 있는지 코드로 확인하는 것.
- **겹 1 / 겹 2**: 기억 성장(즉시) / 가중치 성장(주기·게이트).

## 12. 갱신 이력
| 날짜 | 무엇 | 누가 |
|---|---|---|
| 2026-09-03 | 초안. 실측 §1, 원칙 §2, 교사·재료·학생·게이트·현장 성장·일정·결정 6건. 같은 날 6명 배정 해제(사장님 「의도 아님」) | Claude(win) · 사장님 지시 |
| 2026-09-03 | 사장님 「BYOM 개념에서 포함한 거지?」 → §6-5를 두 길(우리 기본 모델 / BYOM) 표로 또렷하게, BYOM 조건 3(라이선스·HF 가중치·아키텍처) 명시, 결정 §9-7(재료 팩 vs 원격 학습) 추가 | Claude(win) |
