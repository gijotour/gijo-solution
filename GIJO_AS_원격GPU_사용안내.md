# GIJO AS 원격 GPU — 큰 GPU를 나눠 쓰는 법

2026-08-14 · 이 문서의 시간·숫자는 전부 실측입니다.

---

## 1. 무엇을 해결하나

담당자 PC에 큰 GPU가 없어 **작은 모델밖에 못 쓰던 문제**를 기계를 바꾸지 않고 풉니다.

| | 전 | 후 |
|---|---|---|
| 10GB급 노트북 | 7~8B 모델만. 더 쓰려면 **기계를 사야** 했다 | 사무실·집의 큰 기계에 붙어 **14B·32B·72B** 사용 |
| GPU 서버 한 대 | 그 앞에 앉은 사람만 씀 | 팀원 여러 명이 VPN으로 나눠 씀 |

> 우리가 팔던 「등급 = 기계 크기」가 **「등급 = 기능, 큰 모델은 원격으로」**가 됩니다.

⚠ **속도는 로컬보다 느리지 않습니다** — 실측 왕복 **538ms**(win → VPN → gb10 GIJO → gb10 모델 → win).
느려지는 것은 네트워크 왕복(수십 ms)뿐이고, 답을 만드는 시간은 그 기계의 GPU가 정합니다.

---

## 2. 두 쪽이 짝을 이룹니다

이름이 헷갈리기 쉬워 먼저 가릅니다.

| | 어디서 켜나 | 무엇을 하나 |
|---|---|---|
| **원격 GPU** (붙는 쪽) | 빌려 **쓰는** 기계 | 저쪽 주소를 넣고 「내 질문을 그리로 보낸다」 |
| **원격 GPU 내주기** (받는 쪽) | 빌려 **주는** 기계 | 「내 GPU를 VPN 안 사람들에게 내준다」 |

둘 다 **기본은 꺼짐**이고, **admin만** 켤 수 있습니다.

---

## 3. 연결하는 순서 (5분)

### 준비물
- 두 기계가 **같은 VPN 안**에 있을 것 (WireGuard 등). 사설 주소(10.x·172.16~31.x·192.168.x·100.64~127.x)를 서로 알아야 합니다.
- 양쪽 다 GIJO AS가 설치돼 있을 것. **빌려주는 기계에는 챗 모델이 등록·구동 중**이어야 합니다.

### ① 빌려주는 기계에서 — 내주기를 켠다
1. ⚙ **설정 → 연동 → 「🎁 이 PC를 원격 GPU로 내주기」**
2. **[켜기]** — 「VPN 안에서 인증 없이 이 PC의 AI를 쓸 수 있습니다」 확인이 뜹니다.
3. 켜면 **주소가 화면에 만들어져 나옵니다**. 예:
   ```
   http://10.8.0.12:4000/api/llm/serve/v1
   ```
   **[복사]**를 누르세요. ⚠ 손으로 받아 적으면 반드시 틀립니다.
4. 화면이 **「이 주소는 상대가 쓸 수 없는 주소입니다」**라고 하면, 앞부분을 **그 기계의 VPN 주소**로 바꿔서 알려 주세요(예: `localhost`·`127.0.0.1` → `10.8.0.12`). 루프백 주소는 받는 사람의 PC 자신을 가리켜 연결되지 않습니다.

### ② 빌려 쓰는 기계에서 — 그 주소를 넣는다
1. **표준/프로**: ⚙ 설정 → 연동 → 「🖧 원격 GPU (VPN·선택)」
   **라이트**: ⚙ 설정 → 「🖧 원격 GPU (VPN)」
   (또는 **AI팀 구성**의 기반 두뇌 옆 「🖧 원격 GPU(VPN)」 칩, **챗 모델 등록**의 「원격 GPU에 연결」 갈래에서도 이 자리로 옵니다.)
2. 복사한 주소를 붙여넣고 **[연결 테스트]** — 「닿았습니다 — 모델 N개」가 나와야 합니다.
3. **[켜기]** — 「질문이 VPN으로 원격 GPU에 전송됩니다」 확인 후 켜집니다.

### ③ 확인
대화창에 아무거나 물어보세요. 답이 오면 끝입니다.
빌려주는 기계의 화면에는 **「마지막 사용 …」** 시각이 갱신됩니다.

---

## 4. 안 될 때

| 증상 | 원인과 조치 |
|---|---|
| 「사설 대역 주소만 됩니다」로 거절 | 공인 IP나 **도메인 이름**을 넣었습니다. 사설 IP로 넣으세요 — 이름은 어디로든 풀릴 수 있어 코드가 받지 않습니다 |
| 연결 테스트가 「닿지 못했습니다」 | ① VPN이 연결됐는지 ② 저쪽에서 **내주기를 켰는지** ③ 주소의 포트와 `/api/llm/serve/v1` 부분이 맞는지 |
| 연결 테스트가 **타임아웃**(aborted due to timeout) | VPN은 붙어 있는데 **그 기계까지 길이 없는 것**입니다. 허브형 VPN(WireGuard 등)이면 두 가지를 보세요: ① 내 터널 설정의 `AllowedIPs`가 서버 주소 하나(예: `10.8.0.1/32`)뿐이면 **상대 기계 주소가 터널을 안 탑니다** — `10.8.0.0/24`처럼 대역으로 넓히세요 ② VPN 서버가 기계끼리 **중계(포워딩)** 하도록 켜져 있어야 합니다(Windows 서버는 기본 꺼짐). 실사고(2026-08-14): 서버 자신에서는 되는데 다른 기계에서만 타임아웃 — 정확히 이 두 가지였습니다 |
| 「이 서버는 원격 GPU 제공이 꺼져 있습니다」 | 저쪽에서 내주기가 꺼졌습니다(껐거나, 서버가 재시작됐을 수 있습니다) |
| 「이 PC의 모델이 아직 준비되지 않았습니다」 | 빌려주는 기계에 챗 모델이 **등록돼 있지 않거나 로딩 중**입니다. 그쪽 설정에서 모델을 올리고, 로딩 중이면 잠시 뒤 다시 시도하세요 |
| 에어갭 모드라 안 켜짐 | 정상입니다 — 봉인 환경에서는 원격 기능 자체가 막힙니다 |

---

## 5. 보안 — 무엇을 지키고 무엇은 못 지키나

**코드가 지키는 것**
1. **사설 대역에서 온 요청만** 받습니다. 공인 IP는 거절합니다.
   ⚠ `X-Forwarded-For` 같은 헤더는 **믿지 않습니다** — 보내는 쪽이 지어낼 수 있어, 믿으면 바깥 사람이 사내인 척할 수 있습니다.
   ⚠ 그래서 **앞단에 프록시(nginx·LB·SSL 검사 장비)가 있으면 이 기능이 열리지 않습니다** — 그런 장비를 지나면 모든 요청이 그 장비 주소로 보여 「누가 보냈는지」를 확인할 수 없습니다. 확인할 수 없으면 막습니다.
2. **브라우저에서는 못 씁니다.** 웹페이지의 자바스크립트가 이 창구를 부를 수 없게 막습니다(사내망 PC에서 아무 사이트를 열어도 그 페이지가 이 GPU를 쓰지 못합니다).
3. **기본 꺼짐** · admin만 켤 수 있음 · **에어갭이면 켜져 있어도 막힘**.
4. **동시 2건**까지만 받습니다 — 세 번째는 「잠시 뒤 다시」로 거절합니다(GPU를 나눠 쓰다 모두 느려지는 것보다 낫습니다).
5. 내주는 것은 **그 PC의 모델뿐**입니다 — 요청을 다른 곳으로 넘기지 않습니다(열린 중계기가 되지 않게).

**정직하게 못 지키는 것**
- ⚠ **인증을 묻지 않습니다.** 아이디·비밀번호 없이 그 PC의 AI를 쓸 수 있습니다.
  근거는 「VPN 안은 이미 신뢰 경계 안」이라는 판단이며, 그래서 **기본이 꺼짐**이고 안 쓸 때 끄기를 권합니다.
- ⚠ **판정은 「VPN 안」과 정확히 같지 않습니다.** 코드가 보는 것은 **사설 대역**이라,
  사무실 LAN(192.168.x)의 다른 PC나 VPN에 들어온 외부인도 조건상 통과합니다.
  「VPN 전용」은 배치 방침이고, 코드가 보증하는 것은 「공인 인터넷에서는 못 온다」까지입니다 —
  그 차이를 아는 채로 켜세요. 실질적인 방어는 **안 쓸 때 끄는 것**입니다.
- ⚠ **질문이 그 기계로 전송됩니다.** 고객사 소유 기계 안이지만 「이 PC 안」은 아닙니다 —
  회사 정책상 괜찮은지 확인하고 켜세요. 제품은 켤 때 이 사실을 화면에 표시합니다.
- 상대가 진짜 GIJO인지 **확인하지 않습니다**(자격증명이 아직 없습니다). VPN 대역 판정이 그 자리를 대신합니다.

---

## 6. 실측 기록 (2026-08-14)

| 무엇 | 결과 |
|---|---|
| win 라이트 → gb10 llama 직접 | 375ms (exaone 7.8B) |
| **win 제품 chat() → VPN → gb10 GIJO → gb10 llama** | **538ms** (qwen3-14b) — 제품 대 제품 왕복 |
| gb10 창구 `/v1/models` | 114ms, 모델 목록 정상 |
| 꺼진 상태 | HTTP 404 「제공이 꺼져 있습니다」 |
| 인증 없이 상태 조회 | HTTP 401 |

⚠ 덧붙임: 실측 중 gb10의 **14B가 「KISA」를 「한국인터넷정보센터」로 잘못 답했습니다**(정답: 한국인터넷진흥원).
같은 질문에 7.8B는 맞혔습니다. **모델이 크다고 한국 고유명사를 더 잘 아는 것은 아닙니다** —
원격으로 큰 모델을 쓸 때도 사내 자료(RAG)에 근거를 두는 우리 방식이 그대로 중요합니다.
