# 프로 셸 사용자 중심 테스트 기록 — 2026-08-19

## 수리 결과 (같은 날 실화면 재검증)
| # | 수리 | 재검증 |
|---|---|---|
| P1 | 프로 부팅은 저장 탭을 복원하지 않는다(app.html boot 프로 분기 — 저장값은 보존, 표준 복귀 시 그대로 살아남) | ✅ 창 8+1개 → **1개** · localStorage 탭 8개 유지 확인 |
| P2 | body.pro-shell에서 탭줄(.shellfoot) 숨김 | ✅ display:none 실측 |
| P4 | applyPopout 릴레이의 ev.source 검사 제거(타입 화이트리스트 유지) — 허브 안 iframe發 선택이 버려지던 뿌리 | ✅ 우선순위 창 안 취약점 행 클릭 → 본창 📌 도달. bridgerelay.test 계약 반전(36개 통과) |
| P3 | 레일 하단 계정 글자 잘림 — **잔여**(경미) | 다음 라운드 |

## 업그레이드 방안 (외부 사례 조사와 결합 — 프로셸_외부사례조사_2026-08-19.md)
**1단계(이번에 끝)**: 위 P1·P2·P4 — 「버그 많다」 체감의 몸통. 게시 승인 대기.
**2단계(작음, 시안 불필요 — 다음 회차)**: ⓐ 결재판에 「범위」 라벨 명시(HITL 4요소 — 액션·범위·영향·이유 중 범위만 비어 있음) ⓑ 대화 홈 빈 상태에 워크드 예시 1개+한계 1줄(업계 4원칙 중 빠진 둘) ⓒ P3 레일 계정 잘림 ⓓ Ctrl+K 팔레트 상단에 최근 지시 5개.
**3단계(방향 결정 — 시안 1개 만들어 보고)**: ★ **「접는 패널(도킹)」 옵션** — 업계 최신 흐름(Security Copilot 사이드카·Atlas 지속형 사이드바·Claude Artifacts 분할)과 가장 벌어진 지점. 팝업(멀티모니터에 강함)과 도킹(한 화면에서 대화 안 가림) **둘 다 두고 사용자가 고르게** 하는 안을 추천 — 표준 셸이 팝업을 버린 이유(app.html:299)와 프로가 재채택한 이유가 다르므로 공존이 정직하다. 그다음 후보: 알림함(🕘 확장 — Warp 패턴)·Promptbook(다단계 절차 저장)·핀보드.
**안 할 것(과잉 방지)**: 결재판 골격 재설계(이미 HITL 충족)·깜빡임 확대(남용 금지 가이드)·팝업 기능 격차 걱정(?embed=1 재사용이라 구조적으로 없음).


> 사장님 지시: 「프로 제품 업그레이드 방안 — 외부사례 조사, 버그 많은 듯, 사용자 중심으로 테스트하며 진행」.
> 방법: 5.27.0 win-unpacked 별도 인스턴스(CDP 9223) + claude-deploy, 실제 사용자 동선 그대로.
> 외부 사례 조사(gijo-scout)는 병렬 진행 중 — 결과는 이 문서 아래에 합칠 것.

## 확정 결함 (사용자 눈 기준, 심각한 순)

| # | 결함 | 증거 | 뿌리 추정 | 처방 방향 |
|---|---|---|---|---|
| **P1** | **프로 부팅 시 창 폭탄** — 표준 시절 저장 탭 8개(sessions·fix·assets·triage·settings·discover·dashboard·handover)가 로그인 직후 **전부 별도 창으로** 쏟아짐 | CDP 페이지 수 9(본창+8) — 로그인 직후 측정 | app.html 탭 복원 루프가 프로 분기(open→openShellPopout)를 그대로 타는 것으로 추정 | 프로 부팅 시 저장 탭을 **복원하지 않거나**(대화 홈만), 「이전 화면 N개 다시 열기」 한 줄 제안으로 바꾸기 |
| **P2** | **프로인데 탭바 노출 + 옛 탭 잔존** — 상단에 「대시보드」「10.10.20.11」 탭 2개, 💬 복귀 후에도 빈 탭바 띠(display:flex·탭 0개) | pro-home.png · pro-test2 ④ | body.pro-shell에 #tabBar 숨김 규칙 없음 + 복원 탭 일부가 탭으로 남음 | pro-shell에서 #tabBar 숨김(시안대로 탭 개념 제거) — P1과 한 몸 |
| **P3** | **레일 하단 계정 글자 잘림** — 「…포 자동화 전용」이 56px 레일 밖으로 잘려 나옴 | pro-home.png 왼쪽 아래 | titlebar.js buildUserArea를 레일 폭 고려 없이 .rail-foot에 부착 | 레일에는 아바타/이니셜만, 상세는 클릭 팝오버로 |
| **P4** | **허브형 팝업에서 선택이 본창에 안 닿음** — 우선순위(triage) 창 안 취약점 목록에서 행(10.10.20.11) 클릭 → 본창 📌 없음. 단일 화면 팝업(approvals)은 되는데 **허브 창 안 iframe**은 죽는다 | pro-test4 ② | nav.js applyPopout 릴레이가 `ev.source !== window`를 버림 — 허브 안 iframe(vulnscan)發 메시지는 source가 iframe이라 릴레이 탈락. bridgerelay 시험도 이 검사를 「옳음」으로 잠가 사각 | 릴레이 조건을 「이 창 top에 도달한 gijo:* 전부」로 (origin·타입 화이트리스트는 유지). 시험도 허브-인-팝업 케이스 추가 |
| (철회) | Ctrl+K 안 열림 → **오탐** — ☰ 클릭·Ctrl+K 모두 팔레트 정상(화면 목록·↑↓/Enter/Esc) | pro-t3.png | 내 판정식이 placeholder 문구를 잘못 찾음 | — |

## 관찰(결함 아님)
- 같은 화면을 팔레트에서 다시 열면 **창 수가 안 는다**(기존 창 재사용/포커스로 보임) — 좋은 동작.
- 시나리오4 ③(쓰기 지시→결재판)은 스크립트 사고(fill 타임아웃)로 미완 — 다음 회에 재시도.

## 잘 되는 것 (과잉 수리 방지)
- 프로 선택 로그인 → `app.html?shell=pro` 도착, pro-shell 클래스·레일·콘솔 전폭(984px) ✓
- ☰/Ctrl+K 팔레트: 화면 이름 검색·↑↓·Enter·Esc, 「(창)」 표기 ✓
- 대화 홈: 이력·접기·「무엇을 할 수 있나 17가지」·ⓘ ✓
- 팝업→본창 IPC(선택→📌/🗂)는 5.26.0 게시 검증에서 라이브 증명 완료

## 아직 못 본 동선 (다음 확인)
- 팔레트에서 화면 열기 → **창으로 뜨는지**(P1 창들과 구분해 새로), 팝업 행 클릭→본창 📌(프로에서)
- 쓰기 지시 → 결재판 → 본창 가림 시 flashFrame
- 표준↔프로 전환 왕복(재시작 없음) · 프로에서 로그아웃/재로그인 생존
- 세로 모니터에서 프로 팝업 배치

## 테스트 환경 메모
- 본창 테스트 스크립트: scratchpad/pro-test1~3.mjs (로그인·dupForce 처리 포함)
- 스크린샷: scratchpad/pro-home.png·pro-t3.png

## 승인·진행 상태 (2026-08-19 오후)
- 사장님: 「1~4번 전체 진행 승인」 — ①사후검토(배경 진행 중) ②도킹 구현(시안 승인됨) ③B16 복제 ④suggestCommand 처리(대화창 도구화로 결정).
- 업데이트 ECONNRESET 실사고 → 클라 자동 재시도+한국어 안내(7dece49d). 서버 정상(완주 재현).

## ② 도킹 구현 계획 (시안 mockups/프로_도킹패널/시안.html §10 그대로)
1. app.html: 도킹 슬롯 1개 — 기존 .work.console-side 그리드 재사용(패널 유동·대화 700 고정), pro 전용.
   open() 프로 분기: 내부폭(창폭-56)≥1400 && 기억!=="팝업" → 도킹(iframe embed=1), else openShellPopout.
   판단 순서(시안 §3): 이미 도킹됨→포커스 / 이미 팝업→포커스 / 슬롯 참→팝업+토스트 / <1400→팝업(절대) / 기억 적용.
   패널 헤더: 이름 + ⧉ 창으로 + ✕ 접기(상태 버림 아님 — iframe 유지한 채 숨김? 시안 확인: 접기=닫기(슬롯 비움), 재열기는 팔레트).
   기억 키 gijo:pro:dockpref:v1 (⧉→"팝업" 기록, ⇤/도킹 열기→"도킹").
2. main.ts shell:popout: 본창이 있는 디스플레이 확인(screen.getDisplayMatching(mainWindow.getBounds())),
   다른 디스플레이 있으면 그중 가로(landscape) 우선 workArea에 popoutBounds 산출. 승인됨.
3. 팔레트(app.html 전체 메뉴): 상단 「최근 지시」 5개 — console.js submit()에서 localStorage gijo:recent-instr:v1(최대 5, 중복 제거) 기록, 팔레트가 읽어 첫 섹션으로(누르면 submit).
4. 시험: 감시 — 도킹 분기 존재·안전하한 1400 리터럴·dockpref 키·main.ts getDisplayMatching 사용(소스 감시, proshell/bridgerelay 파일에 추가). 실화면: 넓은 창 도킹·좁은 창 팝업·⧉/⇤ 왕복.
5. 그 뒤 ③ B16(별도 커밋 단위: vulnscan→analysis→loganalysis→sbom→… fields 붙이는 화면당 대화창에알림 패턴 복제) · ④ suggest_command 대화창 도구(read, registry+handlers, 허용목록 판정문 포함).
