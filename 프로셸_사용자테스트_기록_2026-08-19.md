# 프로 셸 사용자 중심 테스트 기록 — 2026-08-19 (진행 중)

> 사장님 지시: 「프로 제품 업그레이드 방안 — 외부사례 조사, 버그 많은 듯, 사용자 중심으로 테스트하며 진행」.
> 방법: 5.27.0 win-unpacked 별도 인스턴스(CDP 9223) + claude-deploy, 실제 사용자 동선 그대로.
> 외부 사례 조사(gijo-scout)는 병렬 진행 중 — 결과는 이 문서 아래에 합칠 것.

## 확정 결함 (사용자 눈 기준, 심각한 순)

| # | 결함 | 증거 | 뿌리 추정 | 처방 방향 |
|---|---|---|---|---|
| **P1** | **프로 부팅 시 창 폭탄** — 표준 시절 저장 탭 8개(sessions·fix·assets·triage·settings·discover·dashboard·handover)가 로그인 직후 **전부 별도 창으로** 쏟아짐 | CDP 페이지 수 9(본창+8) — 로그인 직후 측정 | app.html 탭 복원 루프가 프로 분기(open→openShellPopout)를 그대로 타는 것으로 추정 | 프로 부팅 시 저장 탭을 **복원하지 않거나**(대화 홈만), 「이전 화면 N개 다시 열기」 한 줄 제안으로 바꾸기 |
| **P2** | **프로인데 탭바 노출 + 옛 탭 잔존** — 상단에 「대시보드」「10.10.20.11」 탭 2개, 💬 복귀 후에도 빈 탭바 띠(display:flex·탭 0개) | pro-home.png · pro-test2 ④ | body.pro-shell에 #tabBar 숨김 규칙 없음 + 복원 탭 일부가 탭으로 남음 | pro-shell에서 #tabBar 숨김(시안대로 탭 개념 제거) — P1과 한 몸 |
| **P3** | **레일 하단 계정 글자 잘림** — 「…포 자동화 전용」이 56px 레일 밖으로 잘려 나옴 | pro-home.png 왼쪽 아래 | titlebar.js buildUserArea를 레일 폭 고려 없이 .rail-foot에 부착 | 레일에는 아바타/이니셜만, 상세는 클릭 팝오버로 |
| **P4** | **허브형 팝업에서 선택이 본창에 안 닿음** — 우선순위(triage) 창 안 취약점 목록에서 행(10.10.20.11) 클릭 → 본창 📌 없음. 단일 화면 팝업(approvals)은 되는데 **허브 창 안 iframe**은 죽는다 | pro-test4 ② | nav.js applyPopout 릴레이가 `ev.source !== window`를 버림 — 허브 안 iframe(vulnscan)發 메시지는 source가 iframe이라 릴레이 탈락. bridgerelay 시험도 이 검사를 「옳음」으로 잠가 사각 | 릴레이 조건을 「이 창 top에 도달한 gijo:* 전부」로 (origin·타입 화이트리스트는 유지). 시험도 허브-인-팝업 케이스 추가 |
| (철회) | Ctrl+K 안 열림 → **오탐** — ☰ 클릭·Ctrl+K 모두 팔레트 정상(화면 목록·↑↓/Enter/Esc) | pro-t3.png | 내 판정식이 placeholder 문구를 잘못 찾음 | — |

## 관찰(결함 아님)
- 같은 화면을 팔레트에서 다시 열면 **창 수가 안 는다**(기존 창 재사용/포커스로 보임) — 좋은 동작.
- 시나리오4 ③(쓰기 지시→결재판)은 스크립트 사고(fill 타임아웃)로 미완 — 다음 회에 재시도.

## 잘 되는 것 (과잉 수리 방지)
- 프로 선택 로그인 → `app.html?shell=pro` 도착, pro-shell 클래스·레일·콘솔 전폭(984px) ✓
- ☰/Ctrl+K 팔레트: 화면 이름 검색·↑↓·Enter·Esc, 「(창)」 표기 ✓
- 대화 홈: 이력·접기·「무엇을 할 수 있나 17가지」·ⓘ ✓
- 팝업→본창 IPC(선택→📌/🗂)는 5.26.0 게시 검증에서 라이브 증명 완료

## 아직 못 본 동선 (다음 확인)
- 팔레트에서 화면 열기 → **창으로 뜨는지**(P1 창들과 구분해 새로), 팝업 행 클릭→본창 📌(프로에서)
- 쓰기 지시 → 결재판 → 본창 가림 시 flashFrame
- 표준↔프로 전환 왕복(재시작 없음) · 프로에서 로그아웃/재로그인 생존
- 세로 모니터에서 프로 팝업 배치

## 테스트 환경 메모
- 본창 테스트 스크립트: scratchpad/pro-test1~3.mjs (로그인·dupForce 처리 포함)
- 스크린샷: scratchpad/pro-home.png·pro-t3.png
