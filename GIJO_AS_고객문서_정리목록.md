# GIJO AS — 고객에게 나가는 문서 정리 목록

> **언제 하나**: 라이트·표준·프로 **세 등급이 완성된 뒤**(사장님 지시 2026-08-18).
> 제품이 움직이는 중에 문서를 고치면 또 어긋난다 — 그래서 지금은 **목록만** 만들어 둔다.
>
> **왜 이 문서가 있나**: 2026-08-18에 「프로가 약속한 기능이 코드에 없다」를 발견했다.
> 그때 고칠 자리를 적어 두지 않으면, 완성 시점에 **무엇이 틀렸었는지를 다시 찾아내야 한다.**

---

## 0. 지금 상태 — 급한가?

**급하지 않다.** 잘못된 약속이 든 두 문서는 **AI가 안 읽는다**(RAG 코퍼스 20개에 없음, 실측 2026-08-18).
즉 고객이 챗봇에 물어도 그 말이 안 나온다. **사람이 직접 건넬 때만** 나간다.

⚠ 다만 **영업 자리에서 건네면 그 순간 약속이 된다.** 세 등급 완성 전에 누구에게든 건네야 할 일이
생기면, 아래 ①②는 **그때 먼저** 고칠 것.

---

## 1. ⚠ 사실과 다른 것 (완성 시 반드시)

### ① 프로 등급이 없는 기능을 판다 — `GIJO_AS_제품_기능등급_가이드_2026-08-13.md:98-100`

| 문서의 약속 | 코드 실측(2026-08-18) |
|---|---|
| **A/B 병행** — 새 모델을 운영 모델과 나란히 돌려 답을 비교 | **없음** (`abTest`·`compareModels` 0건) |
| **검증 병행** — 답을 다른 모델이 재검증(교차 확인) | **없음** (`재검증`·`crossVerify` 0건) |
| 전문가 어댑터 — 분야별 LoRA를 요청 단위로 | 화면에 **「준비 중」** 배지(LoRA 1·2회전 불채택) |

실제로 있는 것은 **「모델 2개를 메모리에 올릴 수 있다」**뿐이다(`localengine.ts:129` `maxLoadedModels: 2`).
답할 때 두 모델을 함께 쓰는 경로는 없다. 저장소의 `A/B`는 전부 **개발자가 손으로 하는 비교** 이야기다.

⇒ **둘 중 하나**: 기능을 만들거나(그러면 문서가 맞아진다), 문서에서 빼거나.
⚠ 프로의 문맥은 **스탠다드와 같은 32K**다(`localengine.ts:128-129`) — 「프로가 문맥이 넓다」고 적지 말 것.

### ② 라이트 도구 개수가 문서마다 다르다

- `GIJO_AS_제품_라인업_판매가이드.md:121·125` — **8종 / 8→77**
- `GIJO_AS_제품_기능등급_가이드:26` · 코드(`lite-tools.json`) — **13종 / 13→78**

⇒ 코드가 맞다. 판매가이드를 고칠 것.

### ③ 「라이트도 대화창은 그대로」가 과장 — `판매가이드:122`

실측: 라이트 대화창엔 **서랍·선택 칩·다건 선택(picklist)·다중 세션이 없다.**
결재판·스트리밍은 있다. ⇒ 「그대로」를 빼고 있는 것만 적을 것.

### ④ MCP 연동이 껍데기 — `settings.html:786·791·2353`

화면이 스스로 *"정의·정책만 등록됩니다(상태: 정의됨 · 연동 대기)"*라고 말한다.
⇒ 고객 문서에 MCP를 기능으로 적었다면 같은 표현으로 맞출 것.

---

## 2. 등급을 무엇으로 가를 것인가 (사장님 결정 대기)

지금 문서는 **기능 개수**로 가른다 — *"기능 13개는 그대로입니다 — 늘리려면 스탠다드로"*(`기능등급가이드:62`),
*"라이트→스탠다드는 기능이 달라집니다(개인 비서 → 팀 운영)"*(`:112`).

2026-08-17~18에 사장님이 더 강한 축을 짚었다 — **일하는 방식**:

| | 대화창이 |
|---|---|
| **라이트** | 없다시피 — 화면에서 **찾아 쓴다** |
| **스탠다드** | **모든 입력을 받는다** |
| **프로** | (검토 중) **주인공이다** — 화면이 불려 나온다 |

근거: 같은 주에 라이트는 「물어보기」→「찾기」로(`e14d97c`), 표준은 모든 입력을 대화창으로 갔다.
⇒ 「더 많이」가 아니라 **「일하는 방식이 다르다」**로 다시 쓰면 반박이 어렵다.
⚠ 프로 줄은 **검토 결과 나온 뒤에** 확정한다(2026-08-18 검토 진행 중).

---

## 3. 손볼 문서 목록

| 문서 | AI가 읽나 | 무엇을 |
|---|---|---|
| `GIJO_AS_제품_기능등급_가이드_2026-08-13.md` | 아니요 | ①④ · 등급 축 다시 쓰기(2절) |
| `GIJO_AS_제품_라인업_판매가이드.md` | 아니요 | ②③ · 등급 축 |
| `GIJO_AS_제품소개.md` | **예 ✅** | 등급 축 반영. ⚠ **AI가 읽으므로 여기 적은 것은 챗봇이 고객에게 말한다** — 없는 기능을 적지 말 것 |
| `GIJO_AS_등급라벨_설계.md` | 아니요 | 위와 어긋나지 않게 |
| `GIJO_AS_제품소개서.md` · `제품소개_시장경쟁분석.md` | 아니요 | 등급 표현 대조 |

---

## 4. 이 정리를 할 때 지킬 것

- **코드를 먼저 보고 문서를 쓴다.** 이번 건이 그 반대로 갔다가 생긴 일이다(문서를 읽고 「프로는 교차검증한다」고 말했다가 정정).
- **없는 기능은 적지 않는다.** 「예정」이면 예정이라고 적는다 — 이 저장소의 정직 원칙.
- **숫자는 코드에서 뽑는다**(도구 수·VRAM·문맥). 손으로 적으면 ②처럼 어긋난다.
- `GIJO_AS_제품소개.md`는 **AI가 읽는다** — 여기 적은 것은 챗봇이 고객에게 말한다. 가장 조심할 파일.
- 다 고친 뒤 **문서끼리 서로 어긋나지 않는지** 한 번 대조한다(오늘 ②가 그 어긋남이었다).
