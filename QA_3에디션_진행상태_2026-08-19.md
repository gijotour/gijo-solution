# 3에디션 QA — 진행 상태 (2026-08-19 밤샘)

> 사장님 지시: 라이트·표준·프로 메뉴별 시나리오 → 순차 시험 → 장단점 분석 → 문제는 추천으로 처리 → 내일 결과 보고서.
> 추가 지시: 가로(1920×1032)·세로(1080×1872) 두 비율 함께.
> ⚠ 이 파일은 **압축 대비 진행 기록**이다 — 시험이 끝날 때마다 여기 적는다. 보고서는 이 파일로 만든다.

## 준비물 위치
- 시나리오(라이트·표준): `C:\Users\user\AppData\Local\Temp\claude\D--Connect-AI\49c46830-ef57-4c81-b311-a394f08037a6\tasks\wqlguty4c.output` (JSON — 라이트 10화면·표준 화면별 steps/sizeChecks/knownRisk)
- 프로 시나리오: 워크플로 실패 → **직접 짠다**(셸 고유 동선 7가지: 전환·복귀·계정생존·☰/Ctrl+K·💬닫기·챗봇문구·결재판 + 가로세로)
- 자료유실 검증: `...\tasks\we5kqykkb.output` (7건 CONFIRMED — D2는 닫음, D3~D8 남음)
- 팝업 시안: `mockups/프로_팝업배치/` (★별도 창에서 gijo:select/view/scope 배관 죽음 — window.top이 자기 자신)

## 예비 실측 (완료)
- 모니터: 세로 1080×1920(주) · 가로 1920×1080
- 프로 셸 세로: 대화 380px 고정 → 화면칸 **644px**(가로에선 1484px). 사장님 팝업 요구의 수치 근거.
- `.two` 2열 화면 3개(analysis·assets·sessions) 전부 @media 0개. assets는 1열로 접힘(608px), sessions는 644px에서도 2열 고집(fr단위).
- 허브 격자 auto-fit 정상(279px×2).

## 시험 순서와 상태
| 순서 | 대상 | 상태 | 결과 파일 |
|---|---|---|---|
| 1 | 라이트 10화면 (가로→세로) | **시작 전** | QA결과_라이트_2026-08-19.md |
| 2 | 표준 (고정5+허브17판, 가로→세로) | 대기 | QA결과_표준_2026-08-19.md |
| 3 | 프로 (셸 동선 7 + 가로세로) | 대기 | QA결과_프로_2026-08-19.md |
| 4 | 장단점 분석 + 보고서 | 대기 | QA보고서_3에디션_2026-08-19.md |

## 오늘 이미 닫은 결함 (보고서에 넣을 것)
1. D2 undo — 남의 자산 삭제 + 감사 미기록 (커밋 e48f29b8, 시험 8개)
2. 5.25.1 게시 — #셸 표식·보안태세 판 정리
3. 프로 문구 「사이드바」→「☰ 전체 메뉴」 (겹말 「메뉴의 메뉴」도 실응답에서 잡아 수리)

## 시험 중 발견·수리 로그
(시험하며 여기 추가)

## 실행 메모
- 라이트: `GIJO_EDITION=lite` env로 electron 기동, 셸=lite-app.html, 프레임 `#liteScreen > iframe[src="lite-*.html?embed=1"]`. 번들서버 7446. ⚠ 운영 4000 무관.
- 표준/프로: CDP 9224(개발) 또는 9223(설치본). 프로 전환은 shellModeSet("pro") 후 navigateTo("app.html").
- 계정: claude-deploy (GIJO_ADMIN_PASSWORD env).
- 뷰포트: setViewportSize — 가로 1920×1032, 세로 1080×1872.

### 2026-08-19 밤 — 라이트 완료
- 가로·세로 10화면 전부 깨짐 없음(가로스크롤·넘침·11px미만·undefined 0건). 대화 실답 확인.
- 수리 2건(시나리오 경고를 소스로 확인 후):
  ① lite-loganalysis.html — .run/.guard CSS 부재(nessus 결과 뭉개짐·경고 무색) → lite-scan과 동일 값 추가
  ② lite-records.html — .wrap max-width 없음(10화면 중 유일) → 900px 통일
- ⚠ max 인계 필요: 라이트 파일 2개를 win이 고쳤다(사장님 「문제는 추천으로 처리」 지시). max 재빌드 때 담길 것.

### 표준 완료 (가로·세로, 고정3+판17 = 실제 지도: reporting 3판 · aiops 4판 · records 2판)
- ⚠ 1차 지도 오류 2건 자인: ⑤보고를 9판으로 잘못 셌다(실제 3판, team~safety는 AI허브 aiops, audit·syslog는 records.html). 탭 상한 8 때문에 6개째부터 이전 프레임을 잰 스크립트 결함도 있었다.
- 가로: 전 판 깨짐 없음(오탐 제외).
- 세로(1080): 수리 6건 —
  ① kpi.html @media 신설(hero 300px 고정열·headline 3열 → 1열) ✅검증
  ② vulnscan @media(mv-side 300px → 아래로) — 잔여 mv-tile 6px(경미)
  ③ dashboard SVG font-size 10·8.5 → 11 ✅검증(11px 미만 0)
  ④ memory dm-wrap 320px → 1열 — 잔여 내부 클리핑 53~86px(경미·화면 밖 아님)
  ⑤ redteam cols 340px·kpis 4열 → 세로, #targetSelect min-width:280 인라인 → !important 완화
  ⑥ learnloop loop-card overflow-wrap:anywhere — 잔여 10px(경미)
- 오탐 기록: sessions/approvals ellipsis 잘림(의도), redteam scoreText 9px(viewBox 스케일로 실제 16.5px).
- 새 기능 동선 ✅: 🗂범위 알약→취약점 판 1줄 필터+띠, 작업내역 축 칩(9건).
