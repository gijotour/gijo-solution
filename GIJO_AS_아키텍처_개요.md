# GIJO AS — 아키텍처 개요

> 이 문서는 **보안담당자와 보안성 검토 담당자**를 위한 것입니다.
> "이 제품이 어떻게 만들어졌고, 우리 데이터는 어디에 있으며, 무엇이 밖으로 나가는가"에 답합니다.
> 어려운 말에는 쉬운 풀이를 붙였습니다.

---

## 1. 한 장 요약

```
          ┌───────────────────────────────────────────────┐
          │              담당자 PC (Windows)              │
          │   ┌───────────────────────────────────────┐   │
          │   │  GIJO AS 클라이언트 (데스크톱 앱)     │   │
          │   │  화면 · 대화창 · 문서함               │   │
          │   └───────────────────┬───────────────────┘   │
          └───────────────────────┼───────────────────────┘
                                  │  사내망 (VPN 또는 LAN)
          ┌───────────────────────┼───────────────────────┐
          │            사내 GPU 서버 (온프레미스)         │
          │   ┌───────────────────▼───────────────────┐   │
          │   │  GIJO AS 서버                         │   │
          │   │   · 업무 처리 (자산·취약점·리포트)    │   │
          │   │   · AI 조율 (어느 AI에게 맡길지)      │   │
          │   │   · 안전장치 (검사·기록·권한)         │   │
          │   └───┬──────────────┬───────────────┬────┘   │
          │       ▼              ▼               ▼        │
          │  ┌─────────┐   ┌──────────┐   ┌───────────┐  │
          │  │데이터베이│   │지식베이스│   │ 로컬 LLM  │  │
          │  │스(암호화)│   │(문서·검색)│  │ (GPU 구동)│  │
          │  └─────────┘   └──────────┘   └───────────┘  │
          └───────────────────────────────────────────────┘
                    ⛔ 인터넷 없이 동작 — 밖으로 나가는 통로는 7장에 전부 적었습니다
```

**핵심 한 줄**: 화면·데이터·AI가 **전부 사내 서버 안**에 있습니다. 질문도 답도 회사 밖으로 나가지 않습니다.

---

## 2. 무엇으로 만들어졌나 — 3층

| 층 | 무엇 | 어디서 도나 |
|---|---|---|
| **클라이언트** | 담당자가 보는 앱(데스크톱). 화면·대화창·문서함 | 담당자 PC |
| **서버** | 업무 처리·AI 조율·안전장치. 모든 판단이 여기서 | 사내 GPU 서버 |
| **AI 엔진** | 실제로 글을 읽고 쓰는 부분(로컬 LLM) | 같은 서버의 GPU |

> 📖 **로컬 LLM** — 쉽게 말하면 **"우리 회사 서버 안에서만 도는 AI"**입니다. ChatGPT처럼 남의 서버로 질문을 보내지 않습니다.

**클라이언트는 서버 없이 아무것도 하지 않습니다.** 판단·데이터·권한이 전부 서버에 있어, PC를 잃어버려도 그 안에 회사 자료가 남지 않습니다.

---

## 3. 우리 데이터는 어디에 있나

| 저장소 | 무엇이 들어가나 | 보호 |
|---|---|---|
| **데이터베이스** | 자산·취약점·조치 이력·계정·작업 기록 | **저장 암호화(AES-256) 선택 가능** — 켜면 파일을 가져가도 못 엽니다 |
| **지식베이스** | 올린 문서(매뉴얼·점검 보고서)와 그 검색 색인 | 서버 안. 문서 원문은 이 저장소에 있습니다 |
| **모델 파일** | AI 두뇌 파일 | 서버 안. 회사 자료가 아니라 프로그램입니다 |

**백업**: 하루 한 번 자동으로 스냅샷을 만들고, **"정말 복구되는가"까지 열어서 확인**합니다. 파일이 있다는 것과 복구된다는 것은 다른 말이기 때문입니다.

> ⚠ **정직하게**: 저장 암호화를 켜면 **데이터베이스**는 잠기지만, **지식베이스(문서 원문)는 이 암호화 대상이 아닙니다.** 저장소 방식이 달라서입니다. 문서 원문까지 보호하려면 서버 디스크 암호화(BitLocker·LUKS)를 함께 쓰십시오.
> 또한 **설치 형태에 따라 이 기계에서는 켤 수 없는 경우**가 있습니다 — 지금 켤 수 있는지 아닌지는 제품의 자가 진단 화면이 사실대로 알려줍니다.

---

## 4. AI가 답을 만드는 과정 (5단계)

담당자가 지시를 입력하면 이 순서로 갑니다. **화면의 진행 카드가 이 단계를 그대로 보여줍니다.**

```
① 지시 확인 → ② 근거 찾기·도구 실행 → ③ 답 작성 → ④ 검수 → 답변
```

| 단계 | 무슨 일이 일어나나 |
|---|---|
| **① 지시 확인** | 안전 검사(가드레일)를 지나고, 무엇을 할지 정합니다 |
| **② 근거 찾기·도구 실행** | 사내 문서를 찾고(RAG), 필요하면 자산·취약점을 조회합니다 |
| **③ 답 작성** | 찾은 근거를 바탕으로 답을 씁니다 |
| **④ 검수** | 근거가 있는데 "없다"고 답하지 않는지 등을 확인합니다 |

> 📖 **RAG(근거 검색)** — 쉽게 말하면 **"AI가 답하기 전에 사내 문서를 찾아보는 것"**입니다. 지어내지 않고 우리 자료를 근거로 답하게 합니다. 답변 아래 **근거 문서 이름**이 붙는 이유입니다.

**문서를 찾는 방법은 두 갈래를 합칩니다** — ① 뜻이 비슷한 것을 찾는 방식(벡터 검색) ② 낱말이 정확히 맞는 것을 찾는 방식(어휘 검색). 둘을 합쳐야 "CVE-2021-44228" 같은 정확한 번호도, "로그 오래 보관하는 규정" 같은 두루뭉술한 질문도 잡힙니다.

**관계도(온톨로지)도 함께 씁니다** — 6대 보안 표준(KISA·ATLAS·OWASP·NIST·CWE·ATT&CK)의 용어 관계 약 2,000건이 기본 탑재라, 표준 코드끼리 교차 연결된 답이 나옵니다.

---

## 5. AI가 우리 데이터를 만지는 규칙

이 부분이 보안성 검토에서 가장 많이 묻는 대목입니다.

| 규칙 | 내용 |
|---|---|
| **도구를 통해서만** | AI는 데이터베이스를 직접 만지지 못합니다. 정해진 **도구 목록**을 통해서만 조회·변경합니다 |
| **조회와 변경을 나눔** | 조회는 바로 실행되지만, **변경(등록·수정·삭제)은 실행되지 않습니다** |
| **결재판 승인** | 변경이 필요하면 AI가 **값을 채운 확인창**을 띄웁니다. 사람이 값을 보고 승인해야 실행됩니다 |
| **되돌리기** | 실행된 변경은 되돌릴 수 있습니다 |
| **전부 기록** | 누가·언제·무엇을 했는지 작업 기록에 남습니다. **끌 수 없습니다** |

> 즉 **"AI가 지시만 듣고 데이터를 바꾸는 일은 구조적으로 없습니다."**

---

## 6. 안전장치 — 3중 방어

AI 제품에서 가장 알려진 공격은 **프롬프트 주입**(AI에게 몰래 다른 명령을 심는 것)입니다. 세 지점에서 막습니다.

| 지점 | 무엇을 막나 | 기본값 |
|---|---|---|
| **① 입구 — 가드레일** | 담당자가 입력한 말에 섞인 공격 시도 | **차단(켜짐)** |
| **② 자료 — 문서 살균** | **밖에서 받은 문서에 숨겨진 지시문**. 벤더 매뉴얼·협력사 산출물에 심어둔 명령이 AI를 조종하는 것을 막습니다 | 항상 |
| **③ 출구 — 자격증명 가림** | 비밀번호·API 키가 **리포트 파일이나 외부로** 나가는 것 | 항상 |

**③의 선 긋기**: 화면에서 담당자가 보는 것은 가리지 않습니다 — 그 문서를 볼 권한이 있는 분이고, 다 가리면 "매뉴얼을 물어봐도 안 알려주는 제품"이 되기 때문입니다. **되돌릴 수 없는 경로**(파일·외부 전송)에서만 가립니다.

**그 밖에**
- **2차 인증(TOTP)** — 휴대폰 인증앱 6자리. 인터넷 없이 동작해 폐쇄망에서 쓸 수 있습니다
- **계정당 세션 1개** — 같은 계정으로 두 곳에서 동시에 못 씁니다
- **작업 기록 보관** — 기본 3년. 오래된 것만 자동 정리하고, **정리한 사실도 기록에 남깁니다**
- **자가 진단** — "지금 이 시스템 괜찮은가"를 담당자가 직접 확인하는 화면. 백업·지식베이스·모델·디스크 상태를 사람 말로 알려줍니다

---

## 7. 밖과 이어지는 통로 — **하나만 빼고 전부 기본 꺼짐**

폐쇄망 운영이 기본입니다. 아래 통로는 **켜야만** 동작하며, 켜는 것은 관리자만 할 수 있고 기록이 남습니다.

> 딱 하나, 표 맨 아래 **KEV 목록 갱신**만 서버가 켜질 때 스스로 한 번 시도합니다. 우리 자료를 보내는 것이 아니라 **공개 목록을 받아 오는** 통로이고, 안 되면 갖고 있던 사본으로 그대로 동작합니다. 아래 「완전 봉인」을 켜면 이것도 막힙니다.

| 통로 | 무엇 | 기본 |
|---|---|---|
| **클라우드 LLM** | 내부 정보가 없는 일반 질문만 외부 AI에 보조 질의 | **꺼짐** |
| **SIEM 전달** | 보안 이벤트를 고객사 SIEM으로 전송 | **꺼짐** |
| **메일(SMTP)** | 리포트·알림 발송 | 꺼짐(사내 서버 지정 시 동작) |
| **위협 인텔 피드** | 외부 위협 정보 수집 | 꺼짐 |
| **MCP 도구 연동** | 외부 도구 연결 | 꺼짐 · **현재 정의만 저장되고 실제 연결은 동작하지 않습니다** |
| **법령·판례 조회** | 법제처 공개 법령 검색 | 꺼짐 — 발급받은 조회 키를 넣어야 켜집니다 |
| **모델 내려받기** | 새 AI 모델 검색·다운로드 | 관리자가 그 화면에서 직접 받을 때만 |
| **외부 저장소 스캔** | 코드 저장소의 부품 목록 점검 | 담당자가 실행할 때만 — 지정하지 않으면 공개 저장소(GitHub)를 보므로, 사내 저장소만 볼 때는 **사내 주소를 지정**하십시오 |
| **웹·유튜브 지식화** | 공개 웹 주소의 내용을 지식으로 들여옴 | 담당자가 주소를 넣을 때만 |
| **외부 AI 점검(레드팀)** | 점검 대상 AI 주소로 공격 문구를 보내 봄 | 담당자가 사외 대상을 지정할 때만 |
| **장비 원격 점검(SSH)** | 등록한 장비에 접속해 설정을 점검 | 기본은 **이 서버 자신**만 점검 — 담당자가 원격 장비를 등록해야 밖으로 나갑니다 |
| **KEV 목록 갱신** | 「실제로 악용된 취약점」 공개 목록을 내려받음 | ⚠ **서버 기동 때 자동으로 한 번 시도** — 우리 자료는 보내지 않고 공개 목록만 받아 옵니다. 실패하면 갖고 있던 사본으로 그대로 동작합니다 |

**클라우드 LLM을 켜도** 자산명·사설 IP·담당자 이름·자격증명이 섞인 질문은 **자동 차단**됩니다. 사내 문서(RAG) 내용은 애초에 실어 보내지 않습니다. 나간 사실은 전부 기록됩니다.

**완전 봉인(에어갭) 모드** — 기관이 원하면 서버를 봉인 모드로 띄울 수 있습니다. 켜면 위 통로가 **한 관문에서 모두 막힙니다.** 사내망(사설 IP)이나 명시적으로 허용한 곳으로만 통하고, 그 밖은 판단이 애매한 것까지 전부 거부합니다. 화면의 스위치가 아니라 **서버를 띄울 때 정하는 설정**이라, 관리자 계정을 빼앗겨도 봉인이 풀리지 않습니다.

> ⚠ **정직하게**: 학습에 쓰는 **바깥 도구(별도 프로그램으로 도는 것)** 는 이 관문 밖입니다. 오프라인으로 돌도록 눌러 두지만 관문이 직접 막는 것은 아닙니다. 통로 목록과 봉인 여부는 제품 화면에서 그대로 보여드립니다.

---

## 8. 필요한 장비

| 등급 | GPU 메모리 | 한 번에 다루는 글의 양 | 쓰임 |
|---|---|---|---|
| **Lite** (일부 기능 제약) | 10GB급 | 8K 토큰 | 1인 담당자, 기본 업무. 작은 모델을 전제로 해 긴 문서 요약·여러 사람 동시 사용에 제약이 있습니다 |
| **Standard** | 24GB급 | 32K 토큰 | 일반 운영 **(권장)** — 표준 기능 전량은 이 등급부터입니다 |
| **Pro** | 48GB급 | 32K 토큰 | GPU가 넉넉한 환경. ⚠ 지금은 Standard와 엔진 동작이 같습니다 |
| **Max** (관제용) | 사양 확정 전 | — | 24시간 관제용으로 **준비 중**입니다. 아직 고를 수 없습니다 |

> 📖 **토큰** — AI가 글을 세는 단위입니다. 한글은 한 글자가 대략 1~2토큰이라, 32K면 꽤 긴 보고서를 통째로 한 번에 읽힐 수 있습니다.

> ⚠ **AI를 몇 개 띄워 두는지는 등급이 정하지 않습니다.** 그때그때 **남은 GPU 메모리**가 정합니다 — 메모리가 넉넉하면 Standard에서도 여러 개가 함께 올라가 있고, 모자라면 가장 오래 안 쓴 것부터 내려갑니다.

서버 GPU를 실제로 측정해 **권장 등급을 자동으로 알려줍니다.** 등급을 바꾸면 AI가 약 30초 재기동되며, 그동안에도 문서 검색은 됩니다.

---

## 9. 지키는 것과 못 지키는 것 (정직 고지)

과장하지 않기 위해 함께 적습니다.

| 지킵니다 | 못 지킵니다 |
|---|---|
| 질문·답변·문서가 회사 밖으로 안 나감(기본 설정) | 서버가 **살아 있는 동안 그 서버를 장악당하면** 데이터를 볼 수 있습니다 |
| DB 파일·백업본을 가져가도 못 엶(암호화 켤 때) | **지식베이스 문서 원문**은 그 암호화 대상이 아닙니다 |
| 문서에 숨은 지시문이 AI를 조종하지 못함 | AI 모델 **자체**의 저항력은 소형 모델의 한계가 있습니다 — 그래서 앞뒤에 방어를 겹쳤습니다 |
| 변경은 사람 승인 없이 실행되지 않음 | AI 답변이 **항상 옳다는 보장은 없습니다** — 근거 문서를 함께 보여드리는 이유입니다 |
| 모든 작업이 기록에 남고 끌 수 없음 | 기록을 **읽을 수 있는 관리자**는 존재합니다(권한 관리 필요) |

---

## 10. 자주 받는 질문

**Q. 인터넷이 꼭 필요한가요?**
아니요. 설치 후에는 인터넷 없이 완전히 동작합니다. 기본 보안 지식도 함께 설치됩니다.

**Q. 우리 데이터가 GIJO(공급사)로 가나요?**
가지 않습니다. 저장 암호화의 **열쇠도 고객사 서버에서 만들어져 그 기계에 보관**되며, 공급사는 알지 못합니다.

**Q. AI가 실수하면 어떻게 되나요?**
데이터를 바꾸는 일은 **사람 승인 없이 실행되지 않습니다.** 답변이 틀렸다면 화면에서 지적할 수 있고, 그 지적은 품질 개선에 쓰입니다.

**Q. 다른 보안장비와 연동되나요?**
스캔 결과·보안 로그·제품 리포트를 읽어 들여 통합 분석합니다. 반대로 우리 이벤트를 고객사 SIEM으로 보낼 수도 있습니다(켤 때).

**Q. 서버가 죽으면요?**
하루 한 번 자동 백업이 있고, 복구 가능 여부까지 확인합니다. 복원 절차는 화면에서 안내합니다.

---

*이 문서는 제품 변경에 맞춰 갱신됩니다. 화면·기능 사용법은 **사용자 매뉴얼**과 제품 안 챗봇(각 화면에서 "이 화면 뭐 할 수 있어?")을 참고하십시오.*
