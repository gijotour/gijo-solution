# GIJO AS — 디자인·코드 검토 안내 (외부 검토자용)

> **읽는 대상**: 디자인·개발·코드 관점에서 이 제품을 봐 주실 분(과 그 Claude).
> **목적**: 의견이 헛돌지 않게 **왜 이렇게 생겼는지**와 **이미 아는 약점**을 먼저 알려 드립니다.
>
> 읽는 순서: `README.md`(제품 세우기) → **이 문서**(무엇을 어떻게 볼까) →
> `GIJO_AS_공동작업_가이드.md`(의견을 어떻게 반영할까)

---

## 1. 5분 요약 — 이 제품이 무엇인가

한국형 **온프레미스 AI 보안관리 플랫폼**. 보안담당자 한 사람이 취약점 스캔 결과·보안로그·
보안제품 리포트를 한자리에서 보고, **대화창에 물어서** 일을 처리합니다.

핵심은 화면이 아니라 **대화창**입니다. 메뉴는 보는 자리이고, 지시는 대화창 한 곳으로 모읍니다.
이 결정이 IA 전체를 지배하므로, 여기에 이견이 있으시면 **그것부터** 말씀해 주세요.

---

## 2. 바꿀 수 없는 제약 (이걸 모르면 제안이 헛돕니다)

| 제약 | 왜 | 그래서 못 하는 것 |
| --- | --- | --- |
| **온프렘·폐쇄망** | 금융·공공 고객. `GIJO_AIRGAP=1`이면 외부 연결 기본 차단 | CDN·웹폰트·외부 아이콘·분석 스크립트 **전부 불가**. 자산은 전부 동봉 |
| **렌더러에 빌드 단계 없음** | 화면은 순수 HTML/CSS/JS. Electron이 파일을 직접 읽음 | React/Vue/Tailwind/PostCSS 도입은 **구조 변경(major)** 급 결정 |
| **다크 팔레트 고정** | 관제 화면. 라이트 모드 요구가 아직 없음 | 테마 전환은 지금 구조로는 비쌈(§4-①) |
| **로컬 LLM 7~14B** | GPU 한 대에서 도는 작은 모델 | 프롬프트로 행동을 교정하려 하면 실패합니다 — **코드로 푸는 것이 이 저장소의 규범** |
| **지시는 대화창에서** | 쓰기 지시에 승인 창(결재판)을 그려야 함. 화면이 직접 API를 부르면 그 자리가 사라짐 | 새 화면에 **자유 입력칸을 두지 않습니다** |

> 제약을 **바꾸자는 제안 자체는 환영**합니다. 다만 "왜 지금 이런가"를 알고 하시는 편이
> 서로 시간을 아낍니다.

---

## 3. 우리 코드의 특이한 규범 (코드 리뷰 전에)

일반적인 TS/JS 관례와 다른 부분만 추립니다.

### ① 주석에 "왜"와 **실제 사고**를 적습니다
무엇을 하는지는 코드가 말합니다. 주석은 **왜 이렇게 됐는지**와 **어떤 사고가 있었는지**를 적습니다.

```ts
// ⚠ 법제처는 OC만으로 인증하지 않는다. Referer가 활용신청서의 "도메인주소"와 맞아야 답을 준다.
//   실사고(2026-08-09): OC·승인 모두 정상인데 조회가 계속 거부됐다. …
```

이 주석들은 장식이 아니라 **재발 방지 장치**입니다. 리팩터링하며 지우지 말아 주세요.

### ② 「감시 시험」이 있습니다 — 걸리면 시험이 아니라 기능을 고칩니다
제품 코드를 실행하는 대신 **소스·문서를 읽어 약속이 지켜지는지** 보는 시험이 33개 있습니다.

| 시험 | 막는 것 |
| --- | --- |
| `guidance-routing` | 제품이 "이렇게 물어보세요"라고 적어 준 말이 모델 판단으로 새는 것 |
| `no-hardcoded-credentials` | 비밀번호가 코드에 적히는 것 |
| `silentbuttons` | 눌러도 아무 말 없는 버튼 |
| `falseclaim` | 하지 않은 일을 했다고 말하는 답 |

전체 지도는 `GIJO_AS_시험지도.md`(275개 분류, `node tools/test-map.mjs`로 재생성).

### ③ 헛통과를 막는 확인이 시험 안에 들어 있습니다
감시 시험은 대상을 하나도 못 읽으면 「0건 발견」으로 **항상 통과**합니다. 그래서 각 시험은
"대상을 실제로 읽었는가"와 "예전에 있었던 위반을 다시 넣으면 잡히는가"를 함께 확인합니다.
**이 확인을 군더더기로 보고 지우지 말아 주세요.**

### ④ 순서가 의미를 갖는 자료구조
`server/src/engine/agentloop.ts`의 `FORCED_INTENTS`는 **배열 순서가 곧 우선순위**입니다.
중간 삽입은 뒤의 모든 규칙 동작을 바꿉니다. `routes.ts`의 규칙표와 짝을 이루며,
어긋나면 `node tools/route-explain.mjs "질문"`의 설명이 거짓이 됩니다.

### ⑤ 단일 출처
- 화면 설명·용어 풀이 → `server/src/engine/screenguide.ts` **한 곳**. 화면 HTML에 설명을 쓰지 않습니다.
- 화면에는 **정체성 한 줄 + ⚠경고**만. 나머지는 ⓘ로 엽니다.

### ⑥ 정직 규칙
- 폴백 문구("죄송합니다", "잘 모르겠습니다")가 나오면 **통과가 아니라 실패**로 칩니다.
- 자료를 바꾸는 일은 반드시 승인 창을 거칩니다. 지시만으로 바뀌지 않습니다.

---

## 4. 우리가 이미 아는 약점 (여기부터 봐 주시면 좋겠습니다)

숨기지 않고 먼저 적습니다. **이미 아는 것을 다시 지적받기보다, 어떻게 풀지를 듣고 싶습니다.**

### ① 디자인 시스템이 이름만 있습니다 ← **가장 크다고 봅니다**

```
화면 39개
├── gijo-ui.css를 링크한 화면       13개
└── 팔레트를 자기 파일에 복붙한 화면 37개   ← 색 하나 바꾸려면 37곳
```

`client/src/renderer/pages/gijo-ui.css`(340줄)가 있지만 절반도 안 쓰입니다. 대부분의 화면이
`:root { --bg:#262624; --panel:#30302e; … }`를 **자기 파일 맨 위에 복사**해 갖고 있습니다.
새 화면을 만들 때도 옆 화면을 복사하는 식이라 계속 늘어납니다.

**듣고 싶은 것**: 빌드 단계 없이(§2) 토큰을 한 곳으로 모으는 현실적인 경로.
한 번에 39개를 고치는 것은 위험하니 **점진 이행 순서**까지 제안해 주시면 좋겠습니다.

### ② 시안 규칙 — **추천 1개**입니다 (2026-08-09 정리 완료)
얼마 전까지 문서가 서로 어긋나 있었습니다(`CLAUDE.md`는 「3종」, 에이전트 정의는 「추천 1개」).
사용자 결정으로 **추천 1개**로 통일했습니다.

```
요청 → 추천 시안 1개(자체완결 HTML) → 승인 → 구현
```

- 시안은 `mockups/<화면명>/시안.html` (선례 125개 폴더)
- 시안에 반드시 붙입니다: **왜 이 안인지**(다른 길을 왜 안 골랐는지) · 무엇을 포기했는지 ·
  **밀도 숫자**(줄 높이·세로 총합·기존 대비 증감) · 제품 원칙 자가점검
- 다른 방향을 원하면 **그때** 대안을 만듭니다. 처음부터 3개를 만들지 않습니다.

> "깔끔해졌습니다"가 아니라 **"274px → 387px(+113)"** 로 말합니다 — 이 저장소의 요구입니다.

### ③ 화면이 39개, 메뉴 정리가 진행 중입니다
정보구조(IA) 통합이 1단계만 끝났고 2~5단계가 남았습니다. "화면이 많다"는 지적은 이미
알고 있으니, **무엇을 무엇으로 합칠지**를 짚어 주시면 값집니다.

### ④ HTML은 타입 검사 밖입니다
`tsc`는 HTML 안의 `<script>`를 모릅니다. `client/scripts/check-inline-scripts.mjs`가
빌드에서 문법만 봅니다(고아 `});` 하나로 셸 전체가 죽은 실사고 이후 생겼습니다).
**화면 로직의 안전망이 얇습니다.**

### ⑤ 문서가 낡기 쉽습니다
README는 최근까지 **백엔드를 Java/Spring Boot로 설명**하고 있었습니다(실제는 Node/TS).
2026-08-09에 다시 썼습니다. 같은 일이 또 생기지 않게 하는 방법도 관심사입니다.

---

## 5. 특히 봐 주셨으면 하는 곳

| 우선 | 어디 | 무엇을 |
| --- | --- | --- |
| 1 | `client/src/renderer/pages/*.html` (39개) | 디자인 토큰 통합 경로 · 컴포넌트 중복 |
| 2 | `client/src/renderer/pages/nav.js` | 메뉴 구조(IA). 지금 5단계 절차 + 등록부 + 추가 기능 |
| 3 | `server/src/engine/agentloop.ts` | 규칙이 154개까지 늘어난 라우팅. 유지보수 가능한 구조인가 |
| 4 | `server/src/engine/` 154파일 | 파일 하나=기능 하나 구조의 한계. 모듈 경계 |
| 5 | `client/src/preload.ts` | 노출 표면이 큽니다(`window.gijo.*`). 줄일 여지 |
| 6 | `server/test/` 275개 | 감시 시험 방식이 지속 가능한가 |

시작점으로는 **`server/src/engine/lawinfo.ts`**(400줄 내외, 최근 작업)를 권합니다 —
우리 코딩 규범·주석 문화·시험 방식이 한 파일에 다 들어 있습니다.

---

## 6. 의견을 주시는 방법

| 성격 | 방법 |
| --- | --- |
| 구조·방향 제안 | **GitHub Issue** — 코드보다 글이 빠릅니다 |
| 화면 제안 | `mockups/<화면명>/`에 **자체완결 HTML** + PR |
| 코드 수정 | 브랜치 → PR (main 직접 push 금지) |
| 리뷰 코멘트 | PR에 인라인으로 |

PR 본문 형식과 브랜치 규칙은 `GIJO_AS_공동작업_가이드.md`에 있습니다.

**"이건 왜 이렇게 했나요?"는 언제든 환영합니다.** 답을 못 하면 그건 대개 우리 문제입니다.

---

## 7. 몇 가지 부탁

- **운영 서버·운영 데이터에는 접근하지 마세요** — 계정당 1세션이라 서로를 끊습니다.
  자기 Mac에 독립으로 세우는 절차는 `GIJO_AS_MAC_M4_24GB_구성안.md`에 있습니다.
- **`main`에 직접 push하지 마세요** — 브랜치 + PR입니다.
- **감시 시험을 지워서 통과시키지 마세요** — 걸렸다면 대개 약속이 깨진 것입니다.
- **사용자에게 보이는 텍스트는 한글로.** 어려운 말에는 쉬운 풀이를 곁들입니다.
- 화면을 바꾸는 일은 **시안 → 승인 → 구현** 순서입니다. 제품 결정권은 사장님께 있습니다.

---

*작성 2026-08-09 · 수치는 실측(화면 39 · gijo-ui.css 링크 13 · 팔레트 복붙 37 ·
시안 폴더 125 · 시험 275 · engine 154).*
