# GIJO AS — 파일럿 첫날 키트

- 작성: 2026-08-01 · 대상: 파일럿 고객사에 처음 설치하러 가는 날
- 계획서 위치: **전-5(파일럿 확보)를 뒷받침하는 준비물.** 파일럿을 따오는 것은 대표님 몫이고, 이 문서는 **따온 뒤 첫날이 무너지지 않게** 하는 것이 목적이다.
- 짝 문서: 시연 대본은 `GIJO_AS_시연_패키지.md`(4대 시나리오, 22~25분). 이 문서는 그 앞 단계 — **설치부터 첫 지시까지**.

> **첫날의 성패는 기능이 아니라 30분이다.** 담당자가 첫날 자기 손으로 한 번 성공하면 다음 주에도 켠다. 첫날 막히면 기능이 아무리 좋아도 두 번째 로그인은 없다.

---

## 0. 가기 전날 — 30분 (여기를 건너뛰면 현장에서 30분을 잃는다)

| 확인 | 방법 | 통과 기준 |
|---|---|---|
| 설치본이 최신인가 | `client/release/GIJO AS Setup <버전>.exe` 날짜 확인 | 오늘 빌드분 |
| 서버가 뜨는가 | `GET /api/health` | `ok: true` |
| **사전점검 7종** | 관리자 로그인 → `GET /api/admin/preflight` | 7개 모두 `pass` |
| **시연 데이터가 살아 있는가** | 아래 §4 「시연 전 데이터 점검」 | 3줄 모두 통과 |
| 모델이 올라오는가 | 대화창에 아무 질문 하나 | 10초 안에 답 |

**사전점검 7종**(실제 항목): Node.js · GPU(nvidia-smi) · llama-server · 로컬 모델 · JWT 시크릿 · 기본 관리자 비밀번호 · 데이터 저장소 쓰기.

---

## 1. 첫 30분 대본

담당자가 **자기 손으로** 하게 한다. 옆에서 대신 눌러 주면 다음 주에 혼자 못 켠다.

| 분 | 담당자가 하는 것 | 우리가 하는 말 |
|---|---|---|
| 0~5 | 설치본 실행 → 다음 → 완료 | "인터넷 연결이 필요 없습니다. 이 PC 안에서 다 돌아갑니다" |
| 5~8 | 앱 실행 → **서버 주소 입력** → 로그인 | "서버는 사내에 있습니다. 주소만 맞추면 됩니다" |
| 8~12 | 대시보드가 뜨는 것을 본다. 아무것도 안 누른다 | "출근하면 이 브리핑이 먼저 와 있습니다" |
| 12~18 | **대화창에 자기 말로** 질문 하나 — "오늘 뭐부터 볼까?" | "메뉴를 외울 필요가 없습니다. 말로 물으면 됩니다" |
| 18~24 | 답에 붙은 **📄 근거**를 눌러 원문을 본다 | "AI 말을 믿으라는 게 아닙니다. 근거 원문을 같이 드립니다" |
| 24~30 | 쓰기 지시 하나 — "○○ 담당자 김○○로 지정해줘" → **결재판**에서 승인 | "고치는 일은 전부 여기서 사람이 승인합니다. 무인화가 아니라 승인 기반 반자동입니다" |

**첫날 하지 말 것**
- 메뉴 26개를 순서대로 돌지 말 것 — 담당자는 셋째 화면에서 흥미를 잃는다.
- AI-BOM·온톨로지·레드팀을 먼저 꺼내지 말 것. **차별점이지만 첫날의 관심사가 아니다.** 담당자의 첫날 관심사는 "내 일이 줄어드나"다.
- 고객 실데이터를 첫날 넣지 말 것. 스캐너 리포트 반입은 둘째 날 이후, 담당자가 스스로 원할 때.

---

## 2. 막혔을 때 — 자가진단

증상 → 볼 곳 순서. **위에서부터** 본다.

### 앱이 서버에 못 붙는다
1. 서버 PC에서 `GET /api/health` — 안 되면 서버가 안 뜬 것(앱 문제가 아니다)
2. 방화벽 4000/tcp — ⚠ **인터넷 쪽으로 열지 말 것.** 사내 대역으로만. (우리 서버 머신이 공인 IP 직결이었던 실사고가 있다)
3. 앱의 서버 주소 오타 — `http://`까지 다 적었는지

### 로그인이 튕긴다 / 다른 사람이 쫓겨난다
- **계정 하나에 세션 하나**가 원칙이다. 같은 계정으로 두 곳에서 켜면 먼저 것이 끊긴다.
- 파일럿에선 **사람마다 계정을 따로** 만든다. 첫날에 같이 만들어 둔다.

### 답이 안 온다 / 10초 넘게 멈춘다
1. 10초가 넘으면 제품이 **리포트로 전환**하고 완료되면 알려 준다 — 고장이 아니다. 그렇게 말해 준다.
2. 그래도 안 오면 모델이 안 올라온 것. `GET /api/localengine/status`
3. GPU가 잠들어 버리는 일이 있다(윈도우+WSL 조합). 서버 재시작으로 회복된다 — **접속 중인 사람은 전부 끊기니** 미리 말하고 한다.

### 답이 이상하다
- **"없습니다"라고 하는데 있는 것 같다** → 조건을 빼고 다시 물어보게 한다. (2026-08-01에 우리말 조건어를 못 알아듣던 결함을 고쳤다. 그래도 이상하면 그 질문을 그대로 적어 우리에게 보낸다 — 회귀셋에 넣는다)
- **숫자가 화면과 다르다** → 그 자리에서 화면 숫자를 정답으로 삼고, 질문·두 숫자를 적어 온다.
- 지어낸 것 같다 → 📄 근거 배지를 눌러 원문을 확인한다. 근거가 없으면 근거 없이 말한 것이니 **그대로 우리에게 보낸다.**

---

## 3. 고객 데이터 없이 보여줄 순서

첫 방문에는 고객 데이터가 없다. 그때 보여줄 순서다(각 3분, 총 12분).

1. **오늘 할 일** — 우리 샘플 데이터로 "아침 취합이 지시 세 줄이 되는 것"
2. **인수인계** — 담당자가 바뀔 때 머릿속을 남기는 자리 (경쟁사에 **아직 없는 것** 1호)
3. **행동 대조** — "이거 해도 돼?"에 사내규정+법령 양쪽 근거로 답 (**없는 것** 2호)
4. **결재판** — 쓰기는 전부 사람 승인. 보안 구매자가 가장 먼저 묻는 것

> 3·4번이 이 제품을 고르는 이유다. 1·2번은 왜 켜는지, 3·4번은 왜 **우리 것**인지를 설명한다.

---

## 4. 시연 전 데이터 점검 (3줄) — 빼먹지 말 것

시연 대본은 **데이터가 있어야 돈다.** 데이터가 비면 대본의 대사와 화면이 어긋나고, 그 자리에서 수습이 안 된다.

⚠ 2026-08-01에 실제로 이런 일이 있었다: 대본 2:30의 **"미조치 취약점 알려줘"**가 이전에는 CVE 목록을 보여줬는데, 그 사이 데이터가 **전부 스캔 오류**로 바뀌어 있어 보여 줄 것이 없었다. 대본은 그대로였다.

시연 직전 대화창에 이 셋을 그대로 던져 답을 눈으로 본다:

| 물을 말 | 이 답이 나오면 **시연 못 한다** |
|---|---|
| `미조치 취약점 알려줘` | "못 찾았습니다" / 0건 |
| `이번 주 예정된 점검 있어?` | "없습니다" |
| `컴플라이언스 대응 안 된 항목 알려줘` | "없습니다" |

비어 있으면 시연 전에 샘플 데이터를 다시 넣는다. **비어 있는 화면으로 시연하지 않는다** — "아직 데이터가 없어서요"는 그 자리에서 제품이 준비 안 됐다는 뜻으로 들린다.

또 하나: **「열린 취약점 0건」이 안전을 뜻하지 않는다.** 점검 자체가 실패했으면 아직 안 본 것이다. 지금은 KPI 카드가 "⚠ 점검 실패 N건은 아직 안 본 것"이라고 함께 적는다 — 이 문장을 시연에서 **일부러 짚는다.** 정직한 계기판이라는 것이 우리 쪽 강점이다.

---

## 5. 첫날 끝에 남기고 올 것

- 담당자 계정(본인 것) + 비밀번호 변경까지 마친 상태
- 대화창에 **담당자가 직접 던진 질문 3개**의 기록 — 다음 방문 때 "그때 물으신 것" 으로 이어 간다
- 다음 약속: **1주 뒤.** 이때 물어볼 것은 하나다 — "그 사이에 몇 번 켜셨나요"

> 파일럿의 성패는 설치가 아니라 **이탈 없는 4주**다(계획서 중-1). 첫날은 그 4주의 1일차일 뿐이다.
