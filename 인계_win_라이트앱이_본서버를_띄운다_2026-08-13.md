# win 인계 — **포장된 라이트 앱이 본 서버를 띄운다** (max, 2026-08-13)

> 한 줄: 라이트 진입점(`dist/lite/index.js`)은 앱에 **동봉돼 있는데**, `main.ts:119`가 언제나
> `server-dist/dist/index.js`(본 서버)를 부른다. 그래서 **포장된 라이트 앱은 도구 78개로 돈다.**
> 허용목록이 한 번도 안 걸린다.

## 1. 어떻게 확인했나

포장된 `GIJO AS Lite.app`을 **새 기계처럼** 띄웠다(`--user-data-dir`로 빈 userData).
설정 화면 → 계정 만들기 → 로그인까지 정상으로 갔고, 그때 4000을 잡은 프로세스를 봤다:

```
$ ps -o command= -p <pid>
…/GIJO AS Lite.app/Contents/Resources/server-dist/dist/index.js     ← 본 서버
```

번들 안에 라이트 진입점은 **있다**:
```
…/Resources/server-dist/dist/lite/  →  index.js · lite-tools.json
```

그리고 `setToolAllowlist`를 부르는 곳은 **`server/src/lite/index.ts:33` 한 군데뿐**이다.
그 파일이 안 돌면 `허용목록 === null`이라 `isToolAllowed`가 전부 통과시킨다.

⚠ **내가 라이트 게이트 32/33을 잰 대상은 `node dist/lite/index.js`였다.**
제품이 실제로 가는 길이 아니었다 — 오늘 아침에 배운 「겨눈 대상이 맞는가」가 내 것에도 그대로 걸렸다.

## 2. 무엇을 고치면 되나

`client/src/main.ts` — `번들서버구성()`의 진입점 선택(119줄)이 에디션을 따르게 한다.
그 파일에는 이미 네가 2026-08-13에 넣은 `에디션()`이 있다(360줄).

```ts
  // ⚠ 라이트는 서버도 라이트 진입점으로 띄워야 한다 — 그 파일만이 setToolAllowlist를 건다.
  //   화면만 lite-app.html로 돌리면 **도구는 78개 그대로**다(2026-08-13 실측).
  const 라이트 = 에디션() === "lite";
  const 패키징경로 = path.join(process.resourcesPath,
    라이트 ? "server-dist/dist/lite/index.js" : "server-dist/dist/index.js");
```

dev 갈래(122·123줄)도 같은 규칙을 따라야 한다.

⚠ 라이트 진입점은 `require("../index")`로 본 서버를 올린다 — 즉 **본 서버가 하는 일은 그대로 하고
도구만 줄인다.** 다른 부작용은 없다.

### 확인하는 법
띄운 뒤 서버 로그 첫 줄에 이게 떠야 한다:
```
[lite] 라이트 에디션으로 부팅합니다 — 도구 13개만 켭니다.
```

## 3. 같이 나온 것 — 첫 실행은 **완전한 0이 아니다**

새 기계에서 잰 값(고객이 아무것도 안 넣은 상태):

| 항목 | 건수 | 어디서 |
|---|---|---|
| 문서 | **19** | 지식 묶음 2026.07-1 자동 적용(부팅 23초) |
| 온톨로지 | **1,999** | 같은 묶음 |
| 자산 | **4** | `assets.ts` 시드(`origin: "sample"`) |
| 작업 | **3** | 같은 시드(제목에 「샘플-웹서버」 표시됨) |
| 계정 | 1 | 고객이 설정 화면에서 직접 만든 것 ✓ |

- 자산 4건은 `origin: "sample"`을 **데이터로 들고 있다.** 본 제품 `inventory.html`은 기본으로
  뺀다(514·606줄). 라이트 화면은 자산을 안 그리므로 화면에는 안 뜨지만,
  **챗봇 `search`는 본다** — 「우리 자산 뭐 있어?」에 샘플 셋이 나올 것이다(아직 안 재 봤다).
- 앞의 셋(`ai-secbot-01`·`ai-doccls-02`·`ai-anomaly-03`)은 **이름에 「샘플」이 없다.**
  담당이 「보안팀」·「정보보호팀」·「SOC」로 박혀 있어 고객 눈엔 자기 자산으로 보인다.
  넷째(`샘플-웹서버`)만 이름에 표시가 있다. → 이름에 표시를 넣든지, 라이트에서는 시드를 끄든지
  결정이 필요하다(사장님 판단 사안으로 본다).

## 4. 데이터에 없는 표시 하나 (참고, 급하지 않음)

장기기억 문서에 **「제품이 넣은 것」과 「내가 넣은 것」을 가르는 칸이 없다** —
`docClass`·`uploadedBy`·`grade`가 전부 null이고 `scope`는 전부 `global`이다.
본 제품 화면(`maintenance.html`)도 그 구분을 안 한다.

라이트는 `knowledge-bundle/status`의 `docs.fingerprints[].file`로 우회했다.
⚠ **지문은 `knowledge/…` 경로가 붙어 오고 장기기억은 파일명만 쓴다** — 그대로 맞대면
19건 중 7건이 안 겹쳐 「내가 넣은 것 7건」이 된다(오늘 실제로 그렇게 나왔다). 경로를 떼야 맞다.

언젠가 서버가 문서에 그 표시를 달아 주면 세 화면(`lite-dashboard`·`lite-manuals`·`lite-memory`)의
우회를 지울 수 있다.
