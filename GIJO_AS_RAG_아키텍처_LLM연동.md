# GIJO AS — RAG 아키텍처와 LLM 메뉴 연동

> **이 문서는 계속 갱신합니다.** RAG 구성·검색 규칙·도구 목록·화면 연동이 바뀌면
> 요청 없이도 그때 바로 고칩니다(용어사전과 같은 예외 문서).
>
> 최초 작성 2026-07-27 · 마지막 갱신 2026-07-27 (실측 기준: 운영 서버 문서 77건·조각 5,427개)

---

## 0. 한 장 요약

담당자가 질문하면 이렇게 흘러갑니다.

```
   질문
     │
     ├─(1) 화면 맥락 붙임      "지금 보고 있는 화면이 어디인가"
     │
     ├─(2) 무엇을 물었나 판단   도구를 쓸 일인가 · 지식을 찾을 일인가 · 화면 사용법인가
     │
     ├─(3ㄱ) 도구 실행 ────► 우리 DB(자산·취약점·점검·감사…)에서 사실을 가져옴
     │
     ├─(3ㄴ) 지식 검색 ────► 올린 문서에서 근거 조각을 찾음  ← 이게 RAG
     │        └ 의미 검색(벡터) + 글자 검색(BM25) 두 갈래를 합침
     │
     ├─(3ㄷ) 화면 안내 ────► 서버가 가진 정적 안내문(LLM 안 씀)
     │
     ├─(4) LLM이 문장으로 씀   ※ 근거 없이 지어내지 못하게 묶어 둠
     │
     └─(5) 답변 + 📄 근거 문서명
```

**핵심 원칙 하나**: LLM은 *문장을 만드는 일*만 합니다. 사실은 도구(DB)와 RAG(문서)가 댑니다.
이 크기의 모델에 프롬프트로 규칙을 더해 행동을 고치려 하지 않습니다 — 코드로 막습니다(모델 크기와 무관하게 지켜 온 원칙입니다).

---

## 1. RAG 구성

### 1-1. 저장소 두 곳

| | 무엇 | 어디 | 역할 |
|---|---|---|---|
| **벡터 저장소** | 문서 조각 + 임베딩 + 업무영역 | `server/data/memory.lancedb` (LanceDB) | 실제 검색이 일어나는 곳 |
| **메타 표** | 문서별 업로드 시각·올린 사람·업무영역 | `server/data/gijo-as.sqlite` `memory_documents` | 화면 목록·삭제·귀속 표시 |

> ⚠ **두 곳이 어긋날 수 있습니다.** 화면 목록은 SQLite를, 검색의 화면 맥락 부스트는
> **LanceDB의 category**를 씁니다. 어긋나면 "화면에는 위협대응이라 적혀 있는데 위협 화면에서
> 부스트를 못 받는" 조용한 문제가 생깁니다.
> 점검: `node scripts/check-category-sync.mjs` (읽기 전용)

### 1-2. 문서가 들어오는 길

문서가 들어오는 길은 **셋**입니다. 셋 다 같은 관문을 지나야 합니다 — 하나만 고치면 샙니다
(2026-08-08 실사고: 셋 중 둘이 관문 밖에 있었습니다).

```
① 파일 올리기(대시보드 아래 ＋)   /api/memory/ingest-file
② 서버 경로로 넣기                ingestDocument(path)
③ 부팅 때 기본 지식 번들          docsbundle
        │
        ├─ [관문 A] 글자 꺼내기 — .pdf·.hwpx·.docx·.xlsx 등은 **추출기 필수**
        │     extractDocumentText() → scripts/extract_doc.py (pypdf / OWPML)
        │     ⚠ 파이썬은 util/pythonbin.serverPython()이 고른다(맨 "python" 금지)
        │
        └─ ingestText()
             ① 텍스트 정리 (cleanExtractedText)
             ② 조각내기 (chunkText) — 이때 읽을 수 없는 조각을 버린다(isBinaryLikeChunk)
             ③ [관문 B] **인입 품질 판정** — 버려진 비율이 80%를 넘으면 오류로 알린다
             ④ 업무영역 정하기 (규칙 우선, 애매하면 LLM 분류)
             ⑤ 임베딩 (bge-m3, 8개씩 묶어서)
             ⑥ LanceDB 저장 + SQLite 메타 기록
```

**관문 A·B가 왜 생겼나 — 지식의 73%가 쓰레기였다(2026-08-08)**

운영 저장소 조각 5,631개 중 **4,113개(73%)가 사람이 읽을 수 없는 PDF 압축 바이트**였습니다.
Tenable 매뉴얼 4종이 통째로 그 상태였고, 검색이 그 쓰레기를 상위로 올려 진짜 근거를 밀어냈습니다.
원인은 두 가지가 겹친 것입니다.

- 운영(WSL)에 `python`이라는 명령이 **없습니다**(python3만 있습니다). 그런데 추출기는 그 이름을
  불렀습니다 → **추출이 한 번도 성공한 적이 없었습니다**. 서버 가상환경에는 pypdf가 멀쩡히 있었는데도요.
- 위 ②·③ 경로가 PDF를 **UTF-8 글자로 그냥 읽었습니다**. ① 업로드 경로만 추출기를 거치고 있었습니다.

숫자로는 "지식 5,631조각"이라 건강해 보였습니다. **내용을 열어 보기 전까지 아무도 몰랐습니다.**
그래서 넣는 순간 판정하는 관문 B를 뒀습니다 — 못 읽은 문서는 성공한 척하지 않고 오류로 알립니다.

**함정 3가지 (전부 실사고에서 나온 것)**

1. **임베딩 배치 크기는 8** — 64로 보내면 요청당 3만 토큰이 되어 임베딩 서버가 무응답에 빠집니다
   (Tenable 가이드 1.9M자 인입 실패, 2026-07-23). 배치가 실패하면 조각 단위로 쪼개 재시도합니다.
2. **임베딩 서버는 ctx/batch/ubatch를 8192로 명시**해야 합니다. 안 하면 512토큰 넘는 한글 입력에서
   HTTP 500이 나고 **큰 조각이 조용히 인입 실패**합니다.
3. **메타 기록 실패는 수집을 막지 않습니다**(일부러). 대신 메타 없는 문서가 생기므로 위 점검 도구로 봅니다.
4. **전문 검색 인덱스의 증분 갱신(`optimize()`)은 이 저장소에서 늘 깨집니다** — LanceDB 8.0.0의
   내부 디코드 버그입니다(깨끗하게 새로 지은 저장소에서도 같은 오류가 재현돼 **데이터 문제가
   아님**을 확인했습니다, 2026-08-08). 그래서 실패하면 **인덱스를 통째로 다시 만드는 길**로
   되돌립니다. 새로 넣은 조각은 색인 전에도 검색에 잡히므로 답이 틀리지는 않습니다.

### 1-3. 검색이 일어나는 법 — 두 갈래를 합침

```
질문
 ├─ 의미 검색(벡터)   bge-m3 임베딩 → LanceDB 최근접
 └─ 글자 검색(BM25)   전문 인덱스 → CVE-2024-XXXX · U-01 · IW-32 같은 코드를 정확히
        │
        └─ RRF로 순위 합침 → 화면 맥락 부스트(+0.008) → 역할 부스트(+0.02) → 관련성 게이트 → 상위 N개
```

**왜 두 갈래인가**: 벡터만 쓰면 `IW-32` 같은 코드를 못 집습니다(의미가 없는 문자열이라).
BM25만 쓰면 한국어 의미 질문을 못 잡습니다. 서로의 빈틈을 메웁니다.

| 항목 | 값 | 뜻 |
|---|---|---|
| 임베딩 모델 | `bge-m3` | 다국어. 별도 포트 8081에 상주 |
| 조각 크기 / 겹침 | `CHUNK_SIZE` / `CHUNK_OVERLAP` | 겹쳐야 문장이 잘려 뜻이 사라지지 않음 |
| BM25 토크나이저 | `simple` | 한국어 형태소기는 모델 파일이 필요해 오프라인 번들에 부담. 의미는 벡터가 담당 |
| 관련성 한계 | `RAG_RELEVANCE_MAX_DISTANCE = 0.95` | 이보다 멀면 "근거 없음"으로 봄 |
| 화면 맥락 부스트 | `CATEGORY_BOOST = 0.008` | RRF 1위 점수(≈0.0164)의 절반 — **밀어줄 뿐 밀어내지 않음** |
| 역할 부스트 | `ROLE_BOOST = 0.02` | 전문 에이전트가 제 업무영역 자료를 먼저 보게(2026-08-07). 화면 부스트보다 세지만 **여전히 벽이 아님** — 압도적 타영역 정답은 못 뒤집고, 타영역·「일반」이 목록에서 사라지지 않음(rolesearch.test가 계약을 지킴) |

> **부스트를 약하게 둔 이유**: 강한 필터로 두면 "컴플라이언스 화면에서 CVE를 물었을 때"
> 답이 아예 안 나옵니다. 비슷할 때만 그 영역이 이기게 합니다.

### 1-4. 업무영역 5종과 화면 연결

| 업무영역 | 이런 화면에서 우선 | 운영 실측(2026-07-27) |
|---|---|---|
| 취약점 | 취약점 · 조치·승인 · AI-BOM | 11건 |
| 장비운영 | 보안제품 · 유지보수 · 점검 콘솔 | 10건 |
| 사내규정 | 컴플라이언스 | 11건 |
| 위협대응 | 위협 인텔 · 레드팀 · MCP | 23건 |
| 일반 | (부스트 없음) | 22건 |

> **역할 연결(2026-08-07)**: 에이전트 역할도 업무영역과 이어짐 — scan·analysis→취약점, ti→위협대응, report→사내규정(`categoryForRole`). 「일반」 역할은 치우치지 않음.
> **소급 재분류(2026-08-07)**: 분류 신호 보강 뒤 기존 문서 86건을 재판정해 7건 이동(사내규정 21→118조각). ⚠본문은 LanceDB에 있으므로 재분류 스크립트는 **양쪽**(SQLite+LanceDB)을 고쳐야 함 — `scripts/recategorize-2026-08-07.mjs`.
> **분류 거부**: AI가 고른 영역에 단서 낱말이 0이면 그 분류를 버리고 「일반」(억지 이름표 방지).

### 1-5. 관련성 게이트 — 지어내지 않게 하는 장치

거리가 `0.95`를 넘으면 그 조각은 근거로 쓰지 않습니다. 근거가 하나도 없으면
**"사내 자료에 없습니다"**라고 답하지, 일반 지식으로 채우지 않습니다.

> 판단 근거는 두 가지입니다 — ① 벡터 거리 ② 질문의 코드가 조각에 **글자 그대로** 있었는가(`lexicalHit`).
> 코드가 실제로 들어 있으면 거리가 멀어도 근거로 인정합니다.

**주의**: `/api/memory/query`(화면의 문서 검색창)는 게이트를 적용하지 않고 가까운 순으로 보여 줍니다.
게이트는 **답변 경로**에만 겁니다 — 검색은 다 보여주고, 답은 확실할 때만 인용한다는 뜻입니다.

---

## 2. LLM ↔ 메뉴 연동

### 2-1. 화면 맥락 (지금 어디를 보고 있나)

팝업으로 화면을 열면 명령창 위에 **🎯 명령 맥락 칩**이 뜹니다. 이 값이 서버로 함께 갑니다.

```
gijoShell.activeScreen()  →  "vulnscan.html"
   ↓
dispatch(text, sessionId, screen)
   ↓
 ├ 도구 고를 때 참고 (그 화면 업무의 도구를 먼저 봄)
 ├ RAG 부스트 (categoryForScreen)
 └ 화면 안내 (screenguide)
```

> 분리창(별도 창)을 보고 있으면 그 화면이 맥락입니다. **대시보드에 포커스가 갔다고 맥락을 풀지 않습니다** —
> 명령을 치려면 대시보드에 포커스해야 하는데 그 순간 맥락이 사라지면 기능이 무용지물이기 때문입니다.

### 2-2. 도구 — LLM이 우리 DB를 만지는 유일한 통로

현재 **37종**. 도구는 사실을 가져오고, LLM은 그 사실을 문장으로 만듭니다.

| 업무영역 | 도구 | 관련 메뉴 |
|---|---|---|
| assets | `list_assets` `register_asset` `get_asset` `asset_coverage` `assign_owner` | 자산 허브 · 자산 목록 |
| vuln | `scan_status` `finding_status` `review_finding` `assign_finding` `update_finding_status` `bulk_update` `remediation` `today` | 취약점 · 조치·승인 |
| sbom | `aibom_status` `generate_sbom` | AI-BOM |
| products | `product_status` `register_product` | 보안제품 |
| maintenance | `maintenance_status` `schedule_maintenance` | 유지보수 |
| report | `report_schedule_list` `kpi_status` `compliance_status` `set_compliance_status` | 리포트 · KPI · 컴플라이언스 |
| knowledge | `knowledge_status` `ontology_query` `handover_status` | 기억·학습 · 온톨로지 · 인수인계 |
| threat | `threats` | 위협 인텔 |
| analysis | `analysis_status` | 보안 분석(통합 관제) |
| cross | `search` `explain` `briefing` `audit_search` `system_log_status` `law_lookup` `run_redteam` `run_hardening_scan` `work_session_status` | 어느 화면에서든 |

> ⚠ **도구를 늘리면 라우팅이 흔들립니다.** 2026-07-27 `ontology_query` 설명에
> "KISA·OWASP·NIST…"를 나열했더니 *"KISA 어떤 점검항목이야?"* 같은 단순 지식 질문까지
> 그 도구로 끌려가 회귀가 11/11 → 9/11로 떨어졌습니다.
> **도구 설명은 그 도구만 할 수 있는 일로 좁게 쓰고, 추가 후 반드시 `tools/regress`를 돌립니다.**

### 2-3. 답을 만드는 세 갈래

| 갈래 | 언제 | LLM 쓰나 | 근거 |
|---|---|---|---|
| **도구** | "미조치 몇 건이야?" | 문장 다듬기만 | 우리 DB |
| **지식(RAG)** | "방화벽 점검 절차 알려줘" | 씀 (근거 안에서만) | 올린 문서 |
| **화면 안내** | "이 화면 뭐 할 수 있어?" | **안 씀** | `screenguide.ts` 정적 안내 |

화면 안내를 LLM에 안 태우는 이유: 화면 열 때마다 모델을 깨우면 느리고, 답이 매번 달라집니다.

### 2-3½. 학습 수집은 출구 한 곳 (2026-08-07)

대화창 답변은 **dispatcher 출구 한 곳**에서 chat_logs로 수집됩니다 — 즉답·도구 답·LLM 답을 가리지 않습니다(예전엔 chat() 내부에만 걸려 즉답이 전부 빠졌음). 빠지는 것은 qa(측정)·noLearn(배포 계정)·결재판/확인 대기뿐. 수집 시 **주제 딱지(topic)** 를 코드로 판정해 붙입니다(취약점·장비운영·사내규정·위협대응 — 동점·애매면 null). 이 딱지가 주제별 전문가 LoRA의 재료 축입니다(`GET /api/learnloop/topics`).

### 2-3¾. 전문가 어댑터 체제 — 베이스 1 + LoRA N (2026-08-08 재설계)

AI팀은 **베이스 모델 하나** 위에 주제별 LoRA 어댑터를 갈아입히는 구조입니다(팀원마다 통째 모델을 두던 구조를 대체). 고객은 BYOM으로 베이스만 고르고, 전문성은 현장에서 굽습니다 — LoRA는 베이스에 종속되라 어댑터를 미리 배포하면 BYOM 자유가 죽기 때문입니다.

| 단계 | 내용 | 코드 |
|---|---|---|
| 재료 | 주제 딱지 붙은 승인 문답 — **주제별 300건이 개시선**(`topicTrainGate`, 1회전 실측 근거) | learnloop.ts |
| 학습 | QLoRA → GGUF LoRA 어댑터(`data/lora/<prefix>-<주제슬러그>-vN.gguf`) | finetune.ts·convert_lora_to_gguf |
| 등록 | 어댑터 등록부에 **미채택** 등록 — 자동 부착 금지 | adapters.ts |
| 채택 | 평가 게이트+A/B 통과 근거(note)와 함께 사람이 채택 — 채택분만 서빙 적재 | adapters.ts |
| 서빙 | 스폰 시 `--lora-init-without-apply`로 전부 적재, 요청마다 `lora:[{id,scale}]`로 팀원 어댑터 선택 | localengine.ts |
| 배정 | 팀원별 adapterId(오케스트레이터 금지 — 라우팅 결정성) | agents.ts |

⚠ 함정: 어댑터가 실린 모델은 요청마다 `cache_prompt:false` — llama.cpp가 어댑터가 다른 요청끼리 프롬프트 캐시를 재사용해 답이 오염되는 실측 이슈(#26207)의 방어입니다. 베이스를 바꾸면(BYOM 교체) 기존 어댑터는 무효 — 재학습이 필요합니다.

### 2-4. 온톨로지 — RAG의 짝

문서 검색과 별개로 **표준 코드 사이의 관계**를 그래프로 갖고 있습니다
(KISA·ATLAS·OWASP·NIST·CWE·ATT&CK 6종, 약 2,000개 관계).

* RAG = "이 내용이 어디 적혀 있나"
* 온톨로지 = "이 코드가 저 표준의 무엇과 이어지나"

질문에 표준 코드가 있으면 관련 관계를 함께 근거로 넣습니다(`expandOntology`).

### 2-5. 문서를 근거로 쓰지 못하게 막는 장치

| 장치 | 하는 일 |
|---|---|
| 관련성 게이트 | 먼 조각은 근거에서 뺌 |
| `internalMiss` | 사내 자료로 답 못 했음을 표시 — "사내 자료엔 없습니다"로 이어짐 |
| 근거 표시 | 답변 아래 📄 문서명. 근거가 없으면 배지도 없음 |
| 가드레일 | 프롬프트 인젝션 탐지·차단 (dispatch 경로) |
| egress 게이트 | 클라우드 LLM으로 나갈 때 **RAG 내용은 안 보냄** |

---

## 3. 지금 상태 (2026-07-27 실측)

```
문서 77건 · 조각 5,427개
업무영역  위협대응 23 · 일반 22 · 취약점 11 · 사내규정 11 · 장비운영 10 · (없음) 0
의미 검색 응답  약 90ms
코드 검색  U-01 ○ · IW-32 ○   (BM25 동작)
답변 근거  붙음 (예: "샘플_방화벽_정책_점검_절차.txt", "FOCS-메뉴얼-ver1.2.pdf")
```

조각 수 상위 4건이 전체의 **77%**입니다(Tenable 가이드 3종 4,198 + FOCS 97).
벤더 매뉴얼이 무거워, 사내 문서가 적을 때는 벤더 문서가 자주 걸릴 수 있습니다.

### 이번 점검에서 고친 것

| 문제 | 조치 |
|---|---|
| 문서 10건이 벡터엔 있는데 메타가 없어 업무영역이 '일반'으로 굳어 있었음 (위협 동향 보고서 7건 포함) | `scripts/backfill-orphan-docs.mjs`로 채움 → 위협대응 16→23건 |
| `migrate-category.mjs`가 LanceDB 단계에서 lance 디코드 오류로 죽음 | 문서 단위로 나눠 처리하는 스크립트로 우회. **데이터 손상은 없음**(어긋남 0건 확인) |
| 회귀 `kev-deadline`이 약자만 인정 | 전체 이름(Binding Operational Directive 22-01)도 정답으로 |

### 남은 것 (알고 있으나 안 고친 것)

* **데모 문서가 실제 문서를 밀어냅니다.** `샘플_방화벽_정책_점검_절차.txt`·`샘플_IPS_오탐_대응_사례.txt`가
  실제 문서와 같은 주제라 답변 1순위 근거로 잡힙니다. 제품의 위생 점검(`/api/kb-hygiene`)이 이미
  경고 중입니다. **담당자가 남겨두라고 하셔서 두었습니다** — 실사용 전환 시 삭제 검토.
* **`migrate-category.mjs` 전체 훑기는 이 데이터에서 죽습니다.** 큰 PDF 조각의 lance 디코드 오류로
  보입니다. 쓰지 마시고 `backfill-orphan-docs.mjs`를 쓰세요.

---

## 4. 점검·정비 도구

| 명령 | 하는 일 | 위험 |
|---|---|---|
| `node scripts/check-category-sync.mjs` | 두 저장소의 업무영역이 어긋났는지 | 없음(읽기 전용) |
| `node scripts/backfill-orphan-docs.mjs` | 메타 없는 문서 찾기 | 없음(시늉만) |
| `node scripts/backfill-orphan-docs.mjs --apply` | 메타·영역 채우기 | 낮음(문서 단위·실패 격리). **SQLite 백업 먼저** |
| `GET /api/kb-hygiene` | 제품 자체 위생 점검(중복·데모 겹침) | 없음 |
| `node tools/regress/run.mjs` | 고정 질문셋으로 답변 품질 대조 | 없음 |
| `node tools/qa-auto.mjs --layer=knowledge` | 지식 검색 시나리오 | 없음 |

**RAG를 건드린 뒤에는 반드시 `tools/regress`를 돌립니다.** 검색 규칙·도구·문서가 바뀌면
답이 조용히 나빠질 수 있고, 그건 화면을 봐서는 알 수 없습니다.

---

## 5. 자주 겪는 증상과 원인

| 증상 | 먼저 볼 곳 |
|---|---|
| 답이 일반론만 하고 근거가 안 붙음 | 관련성 게이트에 걸렸는지 → 그 주제 문서가 실제로 있는지 |
| 큰 문서를 올렸는데 검색이 안 됨 | 임베딩 서버 ctx/batch/ubatch 8192인지, 배치 8인지 |
| BM25가 0건 | LanceDB `optimize()` 누락 — 인덱스가 새 조각을 못 봄 |
| 화면 맥락 부스트가 안 먹음 | LanceDB category가 비었는지 (`check-category-sync`) |
| 엉뚱한 도구가 잡음 | 그 도구 설명이 너무 넓은지 → 좁히고 회귀 재실행 |
| 지식이 사라짐 | `addColumns` 순서 사고 이력 — 백업부터 확인 |
