# GIJO AS 버전 관리 기준

제정 2026-07-26 (사용자 결정). **클라이언트 버전(X.Y.Z)이 제품의 공식 버전**이다 — 고객이 보는 유일한 번호이며, 설치본·자동 업데이트·릴리스 노트가 모두 이 번호를 쓴다.

## ★ 라이트와 표준은 버전을 **따로** 키운다 (2026-08-14 사용자 결정)

- **표준(메인 제품)** = `client/package.json`의 `version` (5.x.x). 자동 업데이트(서버 latest-release)·`publish-release`가 이 번호를 쓴다.
- **라이트** = `client/electron-builder.lite.json`의 `extraMetadata.version` (**Lite 1.0.0부터 독자 semver**). electron-builder가 이 값으로 asar package.json을 덧써, 라이트 빌드(`dist:lite`)는 표준 번호와 무관하게 자기 번호를 쓴다.
- **왜 나눴나**: 둘이 `package.json` 하나를 공유하던 때, 라이트를 5.18.1→2→3으로 게시하는 동안 표준(5.18.0)의 번호 공간이 섞였다. 「Lite 1.2」처럼 한눈에 갈리고 번호 충돌이 원천 없어진다.
- ⚠ 감시: `lite-edition-shell.test.ts` ①″가 두 번호가 **다른지** 지킨다(같으면 분리가 무의미).
- ⚠ 라이트는 자동 업데이트가 아니라 **Drive 수동 교체**다 — Drive 파일명(`GIJO AS Lite Setup <Lite버전>.exe`)·설치 안내서도 이 독자 번호를 쓴다.

아래 X/Y/Z 자리 규칙은 **두 체계에 각각** 적용된다.

## 언제 어떤 자리를 올리나

| 자리 | 올리는 때 | 예 |
|---|---|---|
| **X (major)** | 쓰는 방식이 바뀌는 구조 개편 — 담당자가 다시 배워야 하는 수준. 반드시 사용자 결정으로만 올린다 | 3.0.0 챗 중심 대시보드 개편 |
| **Y (minor)** | 새 기능·새 화면·새 동작 추가. 기존 사용법은 그대로 유지된 채 할 수 있는 게 늘어난 것 | 3.2.0 팝업 셸(챗봇 메뉴 팝업·분리창·가로세로) |
| **Z (patch)** | 버그 수정·문구·크기 조정 등 위험 낮은 손질 | 3.1.1 대화 유지 수정, 3.1.2 삭제 동반정리 |

- 여러 변경이 섞이면 **가장 높은 자리 기준 하나만** 올린다(3.1.2 → 3.2.0, 3.2.1이 아님).
- 게시 없이 버전만 올리지 않는다 — **버전은 게시 순간에 확정**된다.

## 게시(=버전 확정) 전 필수 조건 4가지

1. **서버 테스트 전부 통과** (vitest)
2. **실화면 검증 통과** — 자동검사 + 육안(스크린샷). 자동만 믿지 않는다(실사고 다수)
3. **게시 노트 1~3줄** — 사용자에게 보이는 문구(서버 릴리스 등록에 실림). 무엇이 달라졌고 무엇을 하면 되는지
4. 게시 커밋 제목: `클라 X.Y.Z 게시 — <핵심 한 줄>` (지금까지의 관례를 규칙으로 승격)

## 서버는 번호를 따로 키우지 않는다

- 서버의 추적 단위는 **git 커밋 + 스키마 마이그레이션 이름**(`/api/health`가 count·latest 노출)이다. 별도 버전 번호를 관리하면 둘이 어긋나는 순간 거짓말이 된다.
- **클라가 서버 신기능에 의존하면: 서버 먼저 배포 → 클라 게시** (이 순서가 표준).
- 호환 원칙: **구버전 클라가 신서버에서 로그인·기본 사용이 되는 한 minor**로 충분. 그게 깨지는 변경은 major로 올리고 릴리스 노트에 "서버를 먼저 올려야 합니다"를 명시한다.

## 버전이 어디에 사는가 (게시 절차가 건드리는 곳)

- `client/package.json` `version` — 단일 출처. 빌드·설치본 파일명·업데이트 비교가 전부 여기서 나온다.
- 서버 릴리스 등록(`/api/client/*`) — 게시 스크립트가 sha256과 함께 올린다.
- `client/server-dist/package.json`은 추적 산출물 — 빌드 후 `git checkout`으로 원복(기존 규칙).

## 에디션 게시 대장 (2026-08-20 신설 — 사장님 「라이트 및 표준 버전관리 잘하고」)

에디션별 **최신 게시판**을 이 표 한 곳에서 추적한다. 게시할 때마다 이 표를 같이 고친다
(라이트는 Drive 수동 교체라 여기 안 적으면 어느 판이 나가 있는지 아무도 모른다).

| 에디션 | 최신 게시 | 게시일 | 반영 기준(hub 커밋) | 배포 통로 |
|---|---|---|---|---|
| 표준·프로 (win exe) | **5.46.0** | 2026-08-20 | 29d8c212 | 서버 자체 배포(자동 업데이트) |
| 라이트 (win exe / mac dmg) | **Lite 1.1.11** | (연초록 이전) | — | Drive 수동 교체 |
| 서버 | 번호 없음 | 2026-08-20 배포 | 29d8c212 | WSL deploy.sh |

- ⚠ 라이트 다음 게시 = **Lite 1.2.0**(minor — 연초록 배색+마라톤 공용 개선). 조건: max 실기
  검증(dmg) 통과. 게시 시 이 표의 「반영 기준」에 그 시점 hub 커밋을 적는다 — 라이트 결함
  회신이 오면 어느 코드 기준인지 즉시 알 수 있다.
- 표준·프로는 같은 바이너리(로그인에서 셸 선택)라 번호를 나누지 않는다 — 나누면
  「같은 것을 여러 곳에 적으면 어긋난다」가 번호에서 재발한다.
