# 고객 의견 반영 계획 (2026-08-04)

> **출처**: 파트너/영업 담당자가 제안자료·기능자료를 검토하고 보낸 의견.
> **원칙**: 고객 의견이라고 다 넣지 않는다. **우리 목표(전·중·후 계획서)에 부합하는 것만** 넣고,
> 안 맞는 것은 안 맞는다고 적는다. 넣기로 한 것은 전/중/후 어느 항목인지 표기한다.

---

## 0. 한 줄 판정

| | |
|---|---|
| 받은 의견 | 7가지(그림화 · SBOM · 자산정보 · 한글화 · 상관분석 그래프 · 첫화면 단순화 · "AI 아닌 듯한 화면") |
| **지금 반영** | **5가지** — 전-1 / 전-7(신설) / 중-7 / 후-6에 붙는다 |
| **조건부** | 1가지(침투 테스트 그림) — 우리가 하는 점검이 아니라 **범위를 먼저 정해야** 한다 |
| **반영 안 함** | 0가지 |
| 별도 답변 필요 | **"Claude 같은 AI와 뭐가 다른가"** — 기능이 아니라 **영업 답변**이 필요하다(아래 5절) |

> ⚠ **가장 값진 지적은 2번(SBOM)이다.** 확인해 보니 **맞는 지적이고, 우리는 실제로 그 한계에
> 걸려 있다.** 그런데 고치는 데 필요한 부품이 이미 다 있다 — 아래 2절.

---

## 1. 의견별 판정표

| # | 고객 의견 | 우리 현황 (실측) | 목표 부합 | 어디에 넣나 |
|---|---|---|---|---|
| 1 | 설명이 많다 · **연결 관계를 그림으로**(EDR 프로세스 트리처럼) | 공격 경로 분석은 **글자**로만 낸다(`ATTACK_PATH_INTENT_RE` → 문장 목록) | ✅ 부합 | **전-1**(시연 패키지) + 전-7 |
| 2 | **SBOM이 CPE 수준이면 부족** · Audit 파일로 패키지/라이브러리를 긁어 보강 | **맞다.** 스캐너 반입 자산의 구성요소는 **OS·배포판·커널 3개뿐**, 버전·라이선스는 `-` | ✅ 부합 (강하게) | **중-7**(커넥터 확대) |
| 3 | 자산↔SBOM 연계 · **EOS 정보** · 한 줄 + 클릭 팝업 | 연계는 됨. **EOS/EOL은 없다**(보안제품 등록부에만 있음). 한 줄+팝업은 이미 그 구조 | ✅ 부합 | **중-7**(EOS) + 전-7(표시) |
| 4 | 조치 설명에 영문이 보인다 | **취약점 이름 4,833건 중 96%에 한글이 하나도 없다** — 스캐너 원문을 그대로 쓴다 | ✅ 부합 (단, 아래 ⚠) | **전-7** |
| 5 | 상관/위험 분석을 **그래프로** + AI가 그 그림을 설명 | 통합 위험 목록은 **줄글 목록**. 4소스 병합 결과는 있음(P0 11·P1 1,390·P2 1,340) | ✅ 부합 | **전-7** |
| 6 | **첫 화면이 복잡하지 않게** | 3.5.0에서 한 번 줄였으나(조작 개념 18→10), 취약점이 4,833건이 되며 다시 빽빽해짐 | ✅ 부합 | **전-7** |
| 7 | **"AI 아닌 듯한 화면"** 고려 | 지금은 대화창이 전면이고 🤖 표식이 곳곳에 있다 | ⚠ 조건부 — 아래 4절 | **후-6**(해자) 검토 |

> ⚠ **4번(한글화)에 대한 우리 의견**: 취약점 **이름 자체를 번역하면 안 된다.**
> `CVE-2021-44228`을 검색하거나 벤더 공지와 대조할 때 원문 이름이 열쇠다. 번역하면 담당자가
> 구글에 검색해도 안 나온다. **원문은 남기고 그 아래 한글 한 줄(무엇이 위험한가·무엇을 하면 되나)을
> 붙이는 방식**을 제안한다. 이건 이미 우리가 쓰는 방식이다(「OpenSSH < 9.6 **사용자 열거**」).
> 지금 부족한 것은 **덮는 범위**이지 방식이 아니다.

---

## 2. 가장 값진 지적 — SBOM (중-7)

### 무엇이 문제인가 (실측)

스캐너(.nessus)로 들어온 호스트의 구성요소를 만드는 코드는 이렇다:

```
server/src/engine/vulnscan.ts:424~431
  meta.os("Linux Kernel 4.18.0 on Red Hat Enterprise Linux 8.10")를 쪼개
  → 배포판 1개 + 커널 1개.  버전·라이선스는 "-"
```

즉 **호스트 한 대의 SBOM에 부품이 2개뿐**이다. 고객 말대로 **CPE 수준**이고,
SBOM이 요구하는 Package/Library는 하나도 없다. 이 상태로 SBOM을 내면
"만들어지긴 한다"이지 "쓸 수 있다"가 아니다.

### 왜 우리가 이걸 할 수 있나 — **부품이 이미 있다**

고객이 제안한 방식(Audit 파일을 Windows/Linux에서 돌려 패키지 목록을 가져오기)에
필요한 것은 **원격에서 읽기 전용 명령을 돌리는 길**인데, 우리는 이미 갖고 있다:

```
server/src/engine/hardeningscan.ts
  · linux(bash) · windows(cmd) · network(장비 CLI) 셸별 실행
  · 고정된 읽기 전용 진단 명령만 (사용자 입력을 셸에 안 넣는다 — 인젝션 없음)
  · 판정은 전부 규칙 코드 (LLM 아님)
  · 원격 SSH 정기점검 스케줄러 + 관제 화면까지 이미 가동
```

**새 커넥터를 만드는 일이 아니라, 있는 길에 명령 3개를 더 태우는 일이다.**

### 할 일

| 단계 | 내용 | 비고 |
|---|---|---|
| ① | 패키지 수집 명령 추가 — `rpm -qa` · `dpkg -l` · `Get-Package`/`wmic product` | 읽기 전용. 기존 하드닝 명령표에 항목으로 추가 |
| ② | 출력 → `AssetComponent[]`(이름·버전·라이선스) 정규화 | 규칙 파싱. **LLM에 맡기지 않는다** — 목록 파싱은 코드가 정확하다 |
| ③ | 자산 구성요소에 병합 — **출처를 적는다**(스캐너 / 패키지 수집) | 「걸린 이유」와 같은 원칙: 어디서 온 값인지 밝힌다 |
| ④ | SBOM(CycloneDX/SPDX)에 반영 + **덮는 범위 표기** | "부품 N개 중 M개는 스캐너 추정치" |
| ⑤ | EOS/EOL 표시 (의견 3번) | 아래 참고 |

> ⚠ **AI가 하는 몫을 정직하게 나눈다.** 고객 자료는 "AI를 통한 SBOM 생성"이라 했지만,
> **패키지 목록 파싱은 코드가 한다**(정확해야 하고, 정확할 수 있으므로). AI는
> **알려진 취약점과 잇고 위험을 설명하는 자리**에 쓴다. 이 구분을 자료에도 적는다 —
> 7B에 목록 파싱을 맡기면 반드시 몇 줄을 흘린다(우리 실측 원칙).

> ⚠ **EOS/EOL 데이터는 어디서 오나.** 우리는 폐쇄망 제품이라 온라인 EOL API를 못 쓴다.
> **기본 지식 번들(전-4)에 EOL 표를 실어 오프라인으로 배포**하는 방식이 우리 구조와 맞는다.
> → 후-3(번들 구독화)의 상품 근거가 하나 더 생긴다.

---

## 3. 그림으로 보여 주기 (전-1 + 전-7 신설)

고객이 세 곳에서 같은 말을 했다(1·5번, 그리고 침투 테스트). **가장 자주 나온 요구다.**

### 우리가 이미 가진 것 — 데이터는 있고 그림이 없다

| 무엇 | 지금 | 그림으로 만들 수 있나 |
|---|---|---|
| 공격 경로(진입→거점→인접) | 글자 목록 | **관계도** — EDR 프로세스 트리와 같은 모양 |
| 4소스 상관(취약점·로그·운영·하드닝) | 줄글 목록 | **묶음 그림** — 어느 자산에서 4소스가 겹치는지 |
| 자산↔취약점↔표준(KISA·ATT&CK) | 온톨로지 트리플 ~2,000개 | **연결 그래프** |
| 하드닝 점검 결과 | 표 | 분류별 막대 |

> ★ **온톨로지가 여기서 값을 한다.** 우리는 이미 자산·취약점·표준을 잇는 관계 데이터를
> 갖고 있다. 경쟁 제품이 화면을 베껴도 이 관계는 못 베낀다(후-6 해자).

### 지키는 것

- **그림은 있는 관계만 그린다.** 없는 선을 그리면 그럴듯하게 틀린다 — 지금 답변에 적용한
  「지어내지 않는다」와 같은 원칙.
- **폐쇄망**이라 외부 차트 CDN을 못 쓴다 → 내장 렌더링만.
- **그림 옆에 AI가 한 문단**을 붙인다(고객 요구 5번). 단 **숫자는 코드가 세고 AI는 문장만** —
  오늘 실측한 보고서 숫자 불일치(총계 1,251 vs 합계 4,828)가 그 이유다.

### ⚠ 침투 테스트 그림 — 범위를 먼저 정해야 한다

고객은 "어떤 점검을 했고 어떤 결과를 받았는지"를 그림으로 원한다. 그런데 **우리가 하는 것은
침투 테스트가 아니다**:

| 우리가 하는 것 | 우리가 안 하는 것 |
|---|---|
| 스캐너 결과 분석 · 하드닝 점검 · **AI 레드팀**(가드레일 뚫기) | 시스템 침투 테스트(모의해침) |

GA 판정표 H 항목이 바로 "**외부 취약점 점검(모의해침)은 외부 업체 몫**"이다.
→ **결정 필요**: (a) AI 레드팀 결과를 그림으로 내고 "AI 견고성 점검"이라 정확히 부르기
  (b) 침투 테스트 결과 **반입**(외부 업체 보고서를 우리가 읽어 그림으로) 기능 추가
  **(a)를 권한다** — 우리가 실제로 하는 것이고, 이미 데이터가 있다.

---

## 4. "AI 아닌 듯한 화면" — 조건부

고객 의견 중 **우리 제품 정체성과 정면으로 부딪히는 유일한 항목**이라 따로 적는다.

### 부딪히는 지점

우리 제품의 확정된 핵심 원칙은 **「메뉴는 보기 용도, 지시는 대화창에서」**(2026-08-01 확정)다.
대화창을 감추면 그 원칙이 무너진다.

### 그런데 고객 말에도 옳은 부분이 있다

담당자가 싫어하는 것은 **"AI"라는 글자**가 아니라 아마 이것들이다:
- 답을 기다리는 시간 · 답이 매번 달라지는 것 · 🤖 표식이 곳곳에 있어 "이게 사실인가" 의심되는 것

**우리는 그것들을 이미 줄이고 있다** — 오늘만 해도 결정적 도구로 바꾼 답이 4개이고
(32초→0.1초, 28초→0.4초), 그 답들은 **AI가 아니라 코드가 낸 것**이라 매번 같다.

### 제안 (후-6 검토)

| 안 | 내용 | 판단 |
|---|---|---|
| A | 🤖 표식을 **코드가 낸 답에서 떼기** — AI가 쓴 문장에만 남긴다 | ✅ **권함**. 정직해지고, 화면이 덜 "AI스러워"진다 |
| B | 대화창을 접어 두고 화면 위주로 | ❌ 핵심 원칙과 충돌 |
| C | "AI" 낱말을 화면에서 빼기 | ⚠ 보류 — 우리 차별점이 AI인데 이름을 감추면 영업이 어려워진다 |

> A안은 **표식의 뜻을 바로잡는 일**이기도 하다. 지금 🤖는 "챗봇이 안내하는 글"인데,
> 코드가 결정적으로 낸 답에도 붙으면 담당자는 그것도 AI가 지어낸 줄 안다.

---

## 5. 별도 답변 필요 — "Claude 같은 AI와 뭐가 다른가"

고객이 **클라우드 가능 고객이 물을 것**이라고 짚었다. 이건 기능이 아니라 **영업 답변**이다.
개발 계획이 아니므로 여기서는 근거만 정리한다(자료화는 대표님 판단).

| 물음 | 우리 답 (근거 있는 것만) |
|---|---|
| Claude에 물으면 되지 않나 | Claude는 **우리 자산 4,833건을 모른다.** 우리는 등록부·스캔 이력·사내 규정을 근거로 답한다 |
| 데이터를 안 내보내나 | 폐쇄망 전제. 외부로 나가는 길은 **결정적 게이트**로 막고 감사에 남긴다 |
| 답이 틀리면 | **근거 배지**(찾음/들음/추정)와 **근거 원문**을 함께 낸다. 지적하면 회귀 문항이 된다(중-1) |
| 얼마나 믿나 | 평가 게이트 3축을 매 배포 통과해야 채택한다 · 제품 경로 뚫림 0/14 |
| 못 하는 건 | 7B 한계·단일 분야 깊이는 대형 모델에 못 미친다 — **자료에 계속 적는다**(전-6) |

---

## 6. 일정 제안

> ⚠ **UI 항목은 전부 시안 먼저.** 우리 규칙상 화면은 추천 시안 1개를 보이고 승인받은 뒤 구현한다.
> 아래 일정은 시안 단계를 포함한 것이다.

| 순서 | 항목 | 계획서 | 왜 이 순서인가 | 규모 |
|---|---|---|---|---|
| 1 | **SBOM 패키지 수집**(①~④) | 중-7 | 고객 지적 중 **유일하게 "틀렸다"에 해당**. 부품이 다 있어 조립에 가깝다 | 2~3일 |
| 2 | **그림 3종**(공격경로·4소스 상관·온톨로지) | 전-1/전-7 | 가장 자주 나온 요구. 시연 자료에 바로 쓰인다 | 시안 1일 + 3~4일 |
| 3 | **첫 화면 단순화 + 한 줄/팝업** | 전-7 | 그림이 들어가면 어차피 다시 짜야 한다 — 2번과 함께 | 시안 1일 + 2일 |
| 4 | **한글 한 줄 덮는 범위 넓히기** | 전-7 | 데이터 작업이라 화면과 독립 | 2일 |
| 5 | **EOS/EOL 표 번들** | 중-7/전-4 | 1번이 끝나야 붙일 자리가 생긴다 | 2일 |
| 6 | 🤖 표식 정리(A안) | 후-6 | 작지만 정직성에 직접 닿는다 | 0.5일 |

**합계 약 3주**(시안·검증 포함).

### 계획서에 넣을 항목 신설 제안

지금 계획서에는 **화면·표현을 다루는 칸이 없다.** 고객 의견 5가지가 전부 거기에 걸리므로
**전-7 「보여 주기 — 그림·첫인상·우리말」**을 신설할 것을 제안한다.
(전 단계에 두는 이유: 파일럿 **전에** 보여 줄 것이라서다. 통과 기준은 전-1과 같이
"파일럿 1곳에서 시나리오 ①~③이 실데이터로 시연됨".)

---

## 7. 이 계획에서 지키는 것

- **그림도 지어내지 않는다.** 있는 관계만 선으로 잇는다.
- **숫자는 코드가 센다.** 그림 옆 설명은 AI가 쓰되 숫자는 넘겨받는다
  (2026-08-04 실측: 보고서가 총계를 1,251이라 해 놓고 합계는 4,828이었다).
- **덮는 범위를 밝힌다.** SBOM 부품이 스캐너 추정인지 실제 수집인지 적는다.
- **화면은 시안 먼저.** 곧바로 구현하지 않는다.
- **못 하는 것은 못 한다고 적는다.** 침투 테스트는 우리가 하는 일이 아니다(GA 판정표 H).

---

*작성: 2026-08-04 · 근거는 전부 그날 운영 서버(localhost:4000) 실측과 소스 확인*
