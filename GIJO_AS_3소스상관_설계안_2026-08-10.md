# 3소스 상관이 왜 0건인가 — 실측과 설계안

**작성**: 2026-08-10 새벽 (Windows 세션) · **상태**: 설계안, 구현 전 · **계획서**: 전-1(1차 목표)

---

## 0. 한 줄

> 구조는 이미 있다. **소스마다 대상을 부르는 이름이 달라서** 겹치지 않을 뿐이다.
> 2개 이상 소스가 가리키는 대상 = **0건**.

---

## 1. 어제 판정을 정정한다

어제 「보안로그·운영리포트 **전용 테이블이 없다**」고 적었다. **틀렸다.**
`analysis_events` 한 테이블이 3소스를 통합 저장하는 구조로 **이미 있고, 데이터도 들어 있다.**

| source | 건수 |
|---|---|
| `vuln` | 4,828 |
| `hardening` | 10 |
| `product`(운영 리포트) | 4 |
| `log`(보안 로그) | 4 |
| **합계** | **4,846** |

⚠ 어제는 `security_logs`·`ops_reports` 같은 **이름**으로 테이블을 찾았고 없으니 「저장 안 됨」으로 읽었다.
**통합 테이블 설계를 몰라서 난 오판**이다 — 테이블 이름이 아니라 **데이터를 봤어야** 했다.

---

## 2. 진짜 원인 — 열쇠(entity)가 소스마다 다른 말

`peers`(소스 겹침)가 채워진 건수: **1 / 4,846**. 그 1건도 `203.0.113.77`(문서용 TEST-NET IP).

`entity` 실측:

| 소스 | entity에 적히는 것 | 예 |
|---|---|---|
| `vuln` | **이름 + (IP)** | `샘플-웹서버 (10.0.0.100)` · `SafeKey 발급 웹 서버 (cert.aj-safe.co.kr)` |
| `log` | **IP만** | `203.0.113.55` · `10.10.30.77` |
| `product` | **파일명 또는 IP** | `주간_운영_리포트.csv` · `5.5.13.3` |
| `hardening` | **표시 이름** | `GIJO AS 서버(로컬)` · `Brian PC` |

같은 서버를 가리켜도 글자가 달라 **문자열 비교로는 영영 안 겹친다.**
`10.0.0.100`에 대한 취약점과 로그가 둘 다 있어도 제품은 그것이 같은 대상인 줄 모른다.

---

## 3. 그래서 무엇을 만들어야 하나

### 3-1. 「대상 열쇠」를 하나로 (핵심)

`entity`(사람이 읽는 말)와 **별개로**, 겹침 판정에 쓸 **정규화 열쇠**가 필요하다.

```
entityKey  = 자산 id  (자산에 묶였을 때)
           | ip:10.0.0.100        (IP를 뽑아냈을 때)
           | host:cert.aj-safe.co.kr
           | file:주간_운영_리포트.csv   (자산이 아닌 것 — 겹치지 않는 게 정상)
```

- ⚠ **`entity`를 고치지 않는다.** 그건 화면에 그대로 나가는 사람 말이다. 열쇠는 **따로** 둔다.
- ⚠ **한 곳에서만 뽑는다.** 소스마다 정규화 규칙을 적으면 반드시 어긋난다
  (오늘만 세 번 겪은 「여러 곳에 적으면 어긋난다」).

### 3-2. 자산에 붙이기 — 이미 있는 것을 쓴다

`ctimatch` 엔진이 **위협 인텔 ↔ 자산 매칭**을 이미 한다. 같은 방식으로
로그·리포트의 IP·호스트명을 **자산 인벤토리에 대조**하면 열쇠가 자산 id로 통일된다.
→ 새로 만들 것이 아니라 **연결할 것**이다.

### 3-3. 겹침은 계산해서 저장한다

`peers`는 지금 인입 시점에 채워진다. 대상 열쇠가 생기면
「같은 entityKey를 가진 다른 소스의 이벤트」를 조회로 뽑을 수 있다.
⚠ **판정을 캐싱하지 말 것**(해자 슬라이스 교훈 — 캐시와 실재가 어긋난다). 병기만 한다.

---

## 4. 단계 (작게 잘라 검증하며)

| 단계 | 무엇 | 검증 |
|---|---|---|
| **0** | `entityKey` 컬럼 신설 + **한 함수**에서만 생성 | 시험: 네 소스 표본이 같은 서버면 같은 열쇠 |
| **1** | 기존 4,846건 **소급 채우기**(마이그레이션) | 실측: 2개 이상 소스가 가리키는 대상 **0 → N건** |
| **2** | 로그·리포트의 IP를 **자산에 매칭**(ctimatch 재사용) | 실측: 자산에 묶인 log 이벤트 수 |
| **3** | 화면 「소스 겹침」이 실제 겹침을 보여줌 | 실화면: 비어 있던 자리에 내용 |

⚠ **0단계 전에 데이터를 손대지 않는다.** 지금 4,846건은 실사용 데이터다.
마이그레이션은 백업 후, 행 수 대조로 확인한다.

---

## 5. 지금 재본 것 / 아직 안 잰 것

**잰 것**: 테이블 구조 · 소스별 건수 · entity 실제 값 · peers 채움 1건 · 2소스 이상 대상 0건

**안 잰 것** (구현 전에 재야 함):
- 로그 4건·리포트 4건이 **너무 적다** — 실사용에서 이 소스들이 제대로 쌓이는가?
  (「넣은 적 있다」는 사장님 말씀과 4건 사이의 간극을 먼저 확인할 것)
- `vuln`의 entity에서 IP를 **얼마나 안정적으로 뽑을 수 있나**(형식이 일정한가)
- 자산 인벤토리에 그 IP들이 **실제로 있는가**(없으면 매칭해도 0건)

⚠ 이 셋을 재기 전에 구현에 들어가면, 오늘 여러 번 겪은 「전제가 틀린 수리」가 된다.
