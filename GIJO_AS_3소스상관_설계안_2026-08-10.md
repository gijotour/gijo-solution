# 3소스 상관 — 진단 정정과 실제 수리 (2026-08-10)

**상태**: ⚠ **이 문서의 앞 판(같은 날 새벽)은 진단이 틀렸다.** 아래는 파고들어 정정한 것과
실제로 고친 것이다. · **계획서**: 전-1(분석 허브 = 1차 목표)

---

## 0. 한 줄

> **「소스 겹침 0건」은 사실이 아니었다.** 제품은 처음부터 맞게 계산하고 있었고,
> 내가 세는 방법을 틀렸다. 다만 파고들다 **진짜 결함**이 나왔고, 그것을 고쳤다.

---

## 1. 내가 무엇을 틀렸나

앞 판에서 「소스마다 대상을 부르는 이름이 달라서 안 겹친다 · 겹침 0건」이라고 적었다.
**둘 다 틀렸다.**

| 내가 적은 것 | 실제 |
|---|---|
| 겹침 0건 | **1건** — `10.10.20.41`이 취약점(Zerologon P0 · SMB 서명 미적용) **과** 보안로그(브루트포스 표적)에 함께 나타난다 |
| 이름 형식이 달라서 안 겹친다 | 형식 문제가 아니다. **로그의 공격 목적지는 「대상」 칸이 아니라 「동반 대상」 칸에 담긴다** — 내가 대상 칸만 셌다 |
| 열쇠 정규화를 새로 만들어야 한다 | 제품은 **이미** 두 칸을 다 보고 있다(2026-08-01에 그렇게 고쳐 뒀다) |

⚠ **왜 이런 오판을 했나**: 화면과 답변을 안 보고 DB만 뒤졌다. 나중에 대화창에 물어 보니
「상관관계: 10.10.20.41이(가) 취약점 + 보안로그에 동시 출현」이라고 **처음부터 답하고 있었다.**
그마저도 처음엔 못 봤다 — 답을 700자에서 **잘라서** 읽었고, 상관 줄은 그 뒤에 있었다.

> **교훈**: 「기능이 없다」고 결론 내리기 전에 **제품에 물어본다.** 그리고 답을 자르지 않는다.

---

## 2. 파고들다 나온 **진짜 결함** — 국내 보안장비 로그를 통째로 놓쳤다

인증 실패를 찾는 규칙이 **리눅스 sshd의 말투만** 알고 있었다.

```
Failed password ... from 203.0.113.9      ← 이건 잡는다
authentication failure; ... rhost=...     ← 이건 잡는다
```

그런데 안랩 TrusGuard·시큐아이 같은 **국내 장비는 CEF/LEEF·key=value로 보낸다**:

```
CEF:0|AhnLab|TrusGuard|1.0|100|auth failure|5|src=203.0.113.9 dst=10.0.0.5 dpt=22 ...
```

`from`이 없어서 **한 건도 안 잡혔다.** 그 장비를 쓰는 고객은 브루트포스가 **영영 0건**이다.

### 곁가지 결함 — 같은 파일에서 한쪽만 목적지를 뽑았다

방화벽 탐지기는 `dst=`(목적지)를 뽑는데 **브루트포스 탐지기는 안 뽑았다.**
브루트포스는 전통 syslog의 호스트 이름만 봤는데, CEF 줄에는 그 이름이 없다.
→ **같은 파일 안에서 한 탐지기는 우리 자산을 찾고 다른 탐지기는 못 찾는** 상태였다.

⚠ 상관은 **목적지(우리 자산)**로만 이어진다. 출발지는 남의 IP라 영영 안 겹친다.
목적지를 못 뽑으면 그 로그는 **어떤 취약점과도 묶이지 않는다.**

---

## 3. 고친 것

| 무엇 | 어떻게 |
|---|---|
| CEF·key=value 인증 실패 인식 | 인증 실패를 뜻하는 말 + `src=`가 **같은 줄**에 있으면 잡는다(말이 앞/뒤 둘 다 허용). ⚠ `src=`만으로는 안 잡는다 — 모든 장비 로그가 인증 실패가 된다 |
| 성공 로그도 같이 | 성공이 안 잡히면 「활성 악용」 신호를 못 단다 |
| 목적지 추출 공용화 | `목적지IP()` 한 함수로 빼고 **두 탐지기가 같이** 쓴다(두 벌로 적으면 반드시 어긋난다) |
| 처리·무시한 것은 상관에서 뺀다 | 같은 화면의 「최우선 항목」은 이미 거르는데 상관만 안 걸러, 담당자가 "이건 아니다"라고 표시한 것이 계속 떴다 |

### 실측 (운영 서버, 실제 인입)

```
CEF 로그 14줄 올림 → 브루트포스 이벤트 1건 생성 · 목적지 10.10.20.22 붙음
상관 1건 → 2건   (10.10.20.41 · 10.10.20.22)
시험 로그를 「무시」 처리 → 상관이 다시 1건으로  (필터가 실제로 동작)
```

시험 6종 신설(`logdest.test.ts`) · 서버 전체 282파일 통과 · **음성 대조로 빨간불 확인**

---

## 4. 남은 것 — 데이터가 부족한 것이지 구조가 아니다

지금 겹침이 1건뿐인 이유는 **재료가 없어서**다:

| 소스 | 지금 보고 있는 것 | 겹칠 수 있나 |
|---|---|---|
| 취약점 4,828 | 스캔한 서버 53대 | — |
| 보안로그 4 | 외부 공격자 IP(목적지가 담긴 건 1건) | 목적지가 담기면 겹친다 |
| 운영리포트 4 | 파일명 3 + IP 1 | 파일은 자산이 아니라 **원래 안 겹치는 게 정상** |
| 하드닝 10 | 우리 서버·PC 2대 | 자산 이름이 같으면 겹친다 |

**할 일이 아니라 쌓일 일이다.** 실사용에서 같은 장비를 두 소스가 보게 되면 자연히 늘어난다.
지금 할 수 있는 것은 **재료가 들어올 때 놓치지 않는 것**이고, 그것이 이번 수리다.

⚠ 다음에 확인할 것: 「Brian PC」(하드닝 대상)가 자산 등록부에 없다 — 등록하면 하드닝도 겹친다.

---

## 5. 이 일에서 남길 것

1. **「기능이 없다」 전에 제품에 물어본다.** DB만 봐서 두 번 틀렸다.
2. **답을 자르지 말 것.** 상관 줄이 700자 뒤에 있어서 못 봤다.
3. **비어 보이는 칸이 두 종류다** — 「못 뽑아서 빈 것」과 「등록부에 없어서 걸러진 것」.
   구별을 못 하면 판정을 틀린다. 그래서 시험은 **뽑는 단계를 직접** 잰다.
